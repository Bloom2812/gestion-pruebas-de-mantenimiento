const GLOBAL_LOGO = 'data:image/png;base64,';

import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";
import { getAuth, signInAnonymously, onAuthStateChanged, signInWithCustomToken, setPersistence, inMemoryPersistence } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";
import { 
    initializeFirestore, memoryLocalCache, collection, doc, onSnapshot,
    addDoc, setDoc, deleteDoc, getDocs, query, updateDoc, getDoc, where, deleteField,
    writeBatch, orderBy, limit, setLogLevel
} from "https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js";

import {
    getDatabase, ref as rtdbRef, set as rtdbSet, onValue as rtdbOnValue, update as rtdbUpdate, push as rtdbPush
} from "https://www.gstatic.com/firebasejs/10.12.2/firebase-database.js";

import {
    getStorage, ref, uploadBytes, getDownloadURL
} from "https://www.gstatic.com/firebasejs/10.12.2/firebase-storage.js";

import { OdooConnector } from "./odoo-connector.js";
import { apiRequest } from './api.js';

console.log("Script IA cargado");

// --- Firebase Config (placeholders will be populated by environment) ---
const firebaseConfig = typeof __firebase_config !== 'undefined' ? JSON.parse(__firebase_config) : {};
const appId = typeof __app_id !== 'undefined' ? __app_id : 'default-cmms-app';

console.log("🚀 CORINFAR CMMS - Versión Odoo Integration 2.0 cargada.");
// --- Firebase Initialization ---
const app = initializeApp(firebaseConfig);
const db = initializeFirestore(app, {
    localCache: memoryLocalCache()
});
setLogLevel('error');
const rtdb = getDatabase(app);
const storage = getStorage(app);
const missingOptimizedImagePaths = new Set();
const optimizedImageUrlCache = new Map();

// Export for verification
const auth = getAuth(app);
setPersistence(auth, inMemoryPersistence);

// --- App State ---
const state = window.state = {
    odoo: null, // Instancia del conector de Odoo
    storage: storage,
    rtdb: rtdb,
    currentUser: null,
    currentTab: 'dashboard',
    currentExecution: null,
    machines: [],
    parts: [],
    proveedores: [],
    technicians: [],
    workOrders: [],
    solicitudes: [],
    workPlans: [],
    activeWorkPlanTab: 'machine',
    workPlanPage: 1,
    workPlanPageSize: 9,
    workPlanViewMode: 'grid',
    workPlanCategoryFilter: 'all',
    workPlanExecutions: [],
    evaluationCriteria: [],
    technicianEvaluations: [],
    currentWoImages: [],
    calendarDate: new Date(),
    machineMaintFilter: null,
    assetCategoryFilter: 'all',
    partStockFilter: null,
    partClassificationFilter: 'all',
    monitoringLocationFilter: 'all',
    activeTimers: {},
    collections: {
        parts: collection(db, `/artifacts/${appId}/public/data/parts`),
        machines: collection(db, `/artifacts/${appId}/public/data/machines`),
        proveedores: collection(db, `/artifacts/${appId}/public/data/proveedores`),
        technicians: collection(db, `/artifacts/${appId}/public/data/technicians`),
        workOrders: collection(db, `/artifacts/${appId}/public/data/workOrders`),
        solicitudes: collection(db, `/artifacts/${appId}/public/data/solicitudes`),
        workPlans: collection(db, `/artifacts/${appId}/public/data/workPlans`),
        workPlanExecutions: collection(db, `/artifacts/${appId}/public/data/workPlanExecutions`),
        evaluationCriteria: collection(db, `/artifacts/${appId}/public/data/evaluationCriteria`),
        technicianEvaluations: collection(db, `/artifacts/${appId}/public/data/technicianEvaluations`),
        partMovements: collection(db, `/artifacts/${appId}/public/data/partMovements`),
        partRequests: collection(db, `/artifacts/${appId}/public/data/partRequests`),
        settings: collection(db, `/artifacts/${appId}/public/data/settings`),
        auditLogs: collection(db, `/artifacts/${appId}/public/data/auditLogs`),
        monitoringConfigs: collection(db, `/artifacts/${appId}/public/data/monitoringConfigs`),
        equipmentStatusHistory: collection(db, `/artifacts/${appId}/public/data/equipmentStatusHistory`),
        materialOut: collection(db, `/artifacts/${appId}/public/data/materialOut`),
        purchaseRequests: collection(db, `/artifacts/${appId}/public/data/purchaseRequests`),
        purchaseCart: collection(db, `/artifacts/${appId}/public/data/purchaseCart`),
    },
    monitoringConfigs: [],
    currentMonitoringConfig: null,
    activeMonitoringVar: null,
    partMovements: [],
    partRequests: [],
    requestCart: [],
    tempSolicitudItems: [],
    materialOut: [],
    purchaseRequests: [],
    purchaseCart: [],
    auditLogs: [],
    equipmentStatusHistory: [],
    rtdbMonitoring: {},
    modals: {},
    charts: {},
    searchTimeouts: {},
    signaturePad: null,
    currentTheme: 'light',
    lastNotifiedMachineStatus: {},
    reopenMachineDetail: null
};
window.state = state;
let currentPartDocs = [];
let currentAsset = null;

const GUEST_USER = {
    username: 'Sistema',
    role: 'Invitado',
    managedMachineIds: [],
    equipoAsignado: []
};

function getCurrentUser() {
    return state.currentUser || GUEST_USER;
}

const ASSET_CATEGORY_MAP = {
    produccion: { label: 'Producción', icon: 'fa-industry', class: 'bg-primary' },
    oficina: { label: 'Oficina', icon: 'fa-building', class: 'bg-info text-dark' },
    servicios: { label: 'Servicios', icon: 'fa-bolt', class: 'bg-warning text-dark' },
    infraestructura: { label: 'Infraestructura', icon: 'fa-warehouse', class: 'bg-secondary' }
};

const ASSET_TYPE_MAP = {
    maquina: 'Máquina',
    equipo_oficina: 'Equipo de Oficina',
    instrumento: 'Instrumento',
    herramienta: 'Herramienta',
    sistema: 'Sistema',
    instalacion: 'Instalación'
};

const PART_CLASSIFICATION_MAP = {
    insumo: { label: 'Insumo', class: 'bg-info text-dark' },
    repuesto: { label: 'Repuesto', class: 'bg-secondary' },
    accesorio: { label: 'Accesorio', class: 'bg-primary text-white' },
    consumible: { label: 'Consumible', class: 'bg-success text-white' },
    herramienta: { label: 'Herramienta', class: 'bg-dark text-white' },
    critico: { label: 'Crítico', class: 'bg-danger text-white' },
    mecanico: { label: 'Mecánico', class: 'bg-secondary' },
    electrico: { label: 'Eléctrico', class: 'bg-warning text-dark' },
    neumatico: { label: 'Neumático', class: 'bg-primary text-white' }
};

function getAssetCategory(machine = {}) {
    if (machine.assetCategory) return machine.assetCategory;
    if (machine.type === 'equipo_oficina') return 'oficina';
    if (machine.type === 'instalacion') return 'infraestructura';
    return 'produccion';
}

function getAssetCategoryInfo(machine = {}) {
    const category = getAssetCategory(machine);
    return ASSET_CATEGORY_MAP[category] || ASSET_CATEGORY_MAP.produccion;
}

function getAssetTypeLabel(type) {
    return ASSET_TYPE_MAP[type] || ASSET_TYPE_MAP.maquina;
}

function getMachineById(machineId) {
    return state.machines.find(m => m.id === machineId || m.fb_id === machineId);
}

function getMachineArea(machine = {}) {
    return machine.area || '';
}

function getDescendantAssetIds(machineId) {
    const descendants = new Set();
    const pending = [machineId];

    while (pending.length > 0) {
        const currentId = pending.pop();
        state.machines.forEach(machine => {
            if (machine.parentAssetId === currentId && !descendants.has(machine.id)) {
                descendants.add(machine.id);
                pending.push(machine.id);
            }
        });
    }

    return descendants;
}

function populateParentAssetSelector(currentMachineId = '', selectedParentId = '') {
    const select = document.getElementById('machine-parent-asset');
    if (!select) return;

    const currentId = currentMachineId || '';
    const blockedIds = currentId ? getDescendantAssetIds(currentId) : new Set();
    const options = state.machines
        .filter(machine => machine.status !== 'DADO_DE_BAJA')
        .filter(machine => machine.id !== currentId)
        .filter(machine => !blockedIds.has(machine.id))
        .sort((a, b) => {
            const categoryCompare = getAssetCategoryInfo(a).label.localeCompare(getAssetCategoryInfo(b).label);
            if (categoryCompare !== 0) return categoryCompare;
            return (a.name || '').localeCompare(b.name || '');
        })
        .map(machine => {
            const category = getAssetCategoryInfo(machine).label;
            return `<option value="${machine.id}">${machine.id} - ${machine.name} (${category})</option>`;
        })
        .join('');

    select.innerHTML = `<option value="">Sin activo padre</option>${options}`;
    select.value = selectedParentId || '';
}

function getPartClassificationInfo(classification) {
    return PART_CLASSIFICATION_MAP[classification] || PART_CLASSIFICATION_MAP.repuesto;
}

const FICHA_MAESTRA_TEMPLATE = `
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="qrcode.min.js"></script>
    <link href="https://fonts.googleapis.com/css2family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: { primary: '#1e3b8a' },
                    fontFamily: { sans: ['Inter', 'sans-serif'] }
                }
            }
        }
    </script>
    <style>
        /* Fallback basics in case Tailwind CDN is slow or fails in iframe */
        .flex { display: flex !important; }
        .grid { display: grid !important; }
        .flex-col { flex-direction: column !important; }
        .justify-between { justify-content: space-between !important; }
        .justify-end { justify-content: flex-end !important; }
        .items-start { align-items: flex-start !important; }
        .items-center { align-items: center !important; }
        .gap-2 { gap: 0.5rem !important; }
        .gap-4 { gap: 1rem !important; }
        .gap-6 { gap: 1.5rem !important; }
        .grid-cols-12 { grid-template-columns: repeat(12, minmax(0, 1fr)) !important; }
        .grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)) !important; }
        .col-span-7 { grid-column: span 7 / span 7 !important; }
        .col-span-5 { grid-column: span 5 / span 5 !important; }
        .col-span-12 { grid-column: span 12 / span 12 !important; }
        .border-b-2 { border-bottom-width: 2px !important; }
        .text-right { text-align: right !important; }
        .text-center { text-align: center !important; }
        .w-full { width: 100% !important; }

        @page {
            size: A4;
            margin: 0;
        }

        body { background: white; font-family: 'Inter', sans-serif; -webkit-print-color-adjust: exact; margin: 0; padding: 0; }
        .pdf-page {
            width: 210mm;
            height: 297mm;
            padding: 10mm;
            margin: 0;
            background: white;
            position: relative;
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }
        .table-header {
            background-color: #f8fafc !important;
            color: #64748b !important;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.7rem;
            letter-spacing: 0.05em;
        }
        img { max-width: 100%; height: auto; }
    </style>
</head>
<body class="text-slate-800">
    <main class="pdf-page">
        <!-- Header -->
        <header class="border-b-2 border-primary pb-3 mb-4 flex justify-between items-start">
            <div class="flex gap-4 items-center">
                <div class="border border-primary p-2 rounded-lg">
                    <img id="print-logo" src="" class="w-12 h-auto" alt="Logo">
                </div>
                <div>
                    <h2 class="text-xs font-bold text-slate-500 uppercase tracking-widest">CORINFAR</h2>
                    <h1 class="text-2xl font-extrabold text-primary">FICHA MAESTRA DE ACTIVO</h1>
                    <p class="text-[10px] text-slate-400">Codigo del Documento: CMMS-FA-001 | Version 1.0</p>
                </div>
            </div>
            <div class="text-right space-y-1">
                <div class="flex items-center justify-end gap-2">
                    <span class="text-xs font-medium text-slate-500">Codigo del Equipo:</span>
                    <span id="print-asset-id" class="text-lg font-bold text-slate-900 tracking-tight"></span>
                </div>
                <div class="flex items-center justify-end gap-2">
                    <span class="text-xs font-medium text-slate-500">Criticidad:</span>
                    <span id="print-criticidad" class="px-2 py-0.5 text-white text-[10px] font-bold rounded-full"></span>
                </div>
                <div class="text-[10px] text-slate-500 italic mt-1">
                    Ubicación: <span id="print-location"></span>
                </div>
                <div class="text-[10px] text-slate-500 italic">
                    Área: <span id="print-area"></span>
                </div>
            </div>
        </header>

        <!-- Asset Summary & Image -->
        <section class="grid grid-cols-12 gap-6 mb-4">
            <div class="col-span-7">
                <div class="bg-slate-50 border border-slate-100 p-4 rounded-lg">
                    <h3 class="flex items-center gap-2 text-primary font-bold text-xs uppercase mb-3 pb-2 border-b border-slate-200">
                        Información General
                    </h3>
                    <dl class="grid grid-cols-2 gap-y-2 gap-x-4 text-xs">
                        <div>
                            <dt class="text-slate-500">Marca</dt>
                            <dd id="print-brand" class="font-semibold"></dd>
                        </div>
                        <div>
                            <dt class="text-slate-500">Estado Actual</dt>
                            <dd id="print-status-badge"></dd>
                        </div>
                        <div>
                            <dt class="text-slate-500">Modelo</dt>
                            <dd id="print-model" class="font-semibold"></dd>
                        </div>
                        <div>
                            <dt class="text-slate-500">Num. de Serie</dt>
                            <dd id="print-serial" class="font-semibold"></dd>
                        </div>
                        <div>
                            <dt class="text-slate-500">Fecha de Compra</dt>
                            <dd id="print-purchase-date" class="font-semibold"></dd>
                        </div>
                        <div>
                            <dt class="text-slate-500">Puesta en Marcha</dt>
                            <dd id="print-commissioning-date" class="font-semibold text-primary"></dd>
                        </div>
                        <div>
                            <dt class="text-slate-500">Impacto en Calidad</dt>
                            <dd id="print-risk" class="font-semibold text-orange-700"></dd>
                        </div>
                        <div>
                            <dt class="text-slate-500">Tipo</dt>
                            <dd id="print-type" class="font-semibold"></dd>
                        </div>
                    </dl>
                </div>
                <div class="mt-4 flex gap-4 text-center">
                    <div class="flex-1 bg-slate-50 p-2 rounded-lg border border-slate-100">
                        <div class="text-[9px] text-slate-500 uppercase font-bold">Total Mant.</div>
                        <div id="print-total-mant" class="text-lg font-bold text-slate-800"></div>
                    </div>
                    <div class="flex-1 bg-slate-50 p-2 rounded-lg border border-slate-100">
                        <div class="text-[9px] text-slate-500 uppercase font-bold">MTBF</div>
                        <div id="print-mtbf" class="text-lg font-bold text-slate-800"></div>
                    </div>
                    <div class="flex-1 bg-slate-50 p-2 rounded-lg border border-slate-100">
                        <div class="text-[9px] text-slate-500 uppercase font-bold">Disponibilidad</div>
                        <div id="print-availability" class="text-lg font-bold text-green-600"></div>
                    </div>
                </div>
            </div>
            <div class="col-span-5">
                <div class="border-2 border-slate-100 rounded-lg p-1 bg-white h-[180px] flex items-center justify-center overflow-hidden">
                    <img id="print-asset-image" alt="Vista del Activo" class="max-w-full max-h-full object-contain rounded-sm" src="">
                </div>
            </div>
        </section>

        <!-- Qualifications -->
        <section class="mb-4">
            <h3 class="flex items-center gap-2 text-primary font-bold text-xs uppercase mb-3">
                Calificación del Equipo (IQ / OQ / PQ)
            </h3>
            <div class="overflow-hidden border border-slate-200 rounded-lg">
                <table class="w-full text-left text-xs">
                    <thead>
                        <tr class="table-header">
                            <th class="px-4 py-2 border-b">Etapa</th>
                            <th class="px-4 py-2 border-b">Estado</th>
                            <th class="px-4 py-2 border-b">Fecha Aprobación</th>
                            <th class="px-4 py-2 border-b">Vencimiento</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100">
                        <tr>
                            <td class="px-4 py-2 font-bold text-primary">IQ – Instalación</td>
                            <td class="px-4 py-2"><span id="print-iq-status" class="font-semibold uppercase text-[9px]"></span></td>
                            <td id="print-iq-date" class="px-4 py-2"></td>
                            <td class="px-4 py-2 text-slate-400 italic">N/A</td>
                        </tr>
                        <tr>
                            <td class="px-4 py-2 font-bold text-primary">OQ – Operación</td>
                            <td class="px-4 py-2"><span id="print-oq-status" class="font-semibold uppercase text-[9px]"></span></td>
                            <td id="print-oq-date" class="px-4 py-2"></td>
                            <td class="px-4 py-2 text-slate-400 italic">N/A</td>
                        </tr>
                        <tr id="print-pq-row">
                            <td class="px-4 py-2 font-bold text-primary">PQ – Desempeño</td>
                            <td class="px-4 py-2"><span id="print-pq-status" class="font-semibold uppercase text-[9px]"></span></td>
                            <td id="print-pq-date" class="px-4 py-2"></td>
                            <td id="print-pq-next" class="px-4 py-2 font-bold"></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </section>

        <!-- Maintenance and Spares -->
        <div class="grid grid-cols-2 gap-6 mb-4">
            <section>
                <h3 class="flex items-center gap-2 text-primary font-bold text-xs uppercase mb-3">
                    Mantenimientos Programados
                </h3>
                <div class="border border-slate-200 rounded-lg overflow-hidden">
                    <table class="w-full text-left text-[10px]">
                        <thead class="table-header">
                            <tr>
                                <th class="px-3 py-1.5 border-b">ID</th>
                                <th class="px-3 py-1.5 border-b">Fecha</th>
                                <th class="px-3 py-1.5 border-b">Tipo</th>
                            </tr>
                        </thead>
                        <tbody id="print-history-table-body" class="divide-y divide-slate-100">
                        </tbody>
                    </table>
                </div>
            </section>
            <section>
                <h3 class="flex items-center gap-2 text-primary font-bold text-xs uppercase mb-3">
                    Repuestos Vinculados
                </h3>
                <div class="border border-slate-200 rounded-lg overflow-hidden">
                    <table class="w-full text-left text-[10px]">
                        <thead class="table-header">
                            <tr>
                                <th class="px-3 py-1.5 border-b">Repuesto</th>
                                <th class="px-3 py-1.5 border-b">Estado</th>
                            </tr>
                        </thead>
                        <tbody id="print-parts-table-body" class="divide-y divide-slate-100">
                        </tbody>
                    </table>
                </div>
            </section>
        </div>

        <!-- Asset Relationships -->
        <section class="mb-4">
            <h3 class="flex items-center gap-2 text-primary font-bold text-xs uppercase mb-3">
                Relacion de Activos
            </h3>
            <div class="grid grid-cols-2 gap-6">
                <div class="border border-slate-200 rounded-lg overflow-hidden">
                    <div class="table-header px-3 py-1.5 border-b">Activo Padre / Equipo Principal</div>
                    <div id="print-parent-asset" class="text-[10px] p-3 text-slate-700"></div>
                </div>
                <div class="border border-slate-200 rounded-lg overflow-hidden">
                    <table class="w-full text-left text-[10px]">
                        <thead class="table-header">
                            <tr>
                                <th class="px-3 py-1.5 border-b">Equipo Vinculado</th>
                                <th class="px-3 py-1.5 border-b">Tipo</th>
                                <th class="px-3 py-1.5 border-b">Criticidad</th>
                            </tr>
                        </thead>
                        <tbody id="print-child-assets-table-body" class="divide-y divide-slate-100">
                        </tbody>
                    </table>
                </div>
            </div>
        </section>

        <!-- Documentation -->
        <section class="grid grid-cols-2 gap-6 bg-slate-50 p-3 rounded-lg border border-slate-100 mb-4">
            <div>
                <h4 class="text-xs font-bold text-slate-700 uppercase mb-2">Documentación</h4>
                <ul id="print-docs-list" class="text-[10px] space-y-1 text-slate-600">
                </ul>
            </div>
            <div>
                <h4 class="text-xs font-bold text-slate-700 uppercase mb-2">Biblioteca Digital</h4>
                <p class="text-[9px] text-slate-500 mb-2 italic">Acceso restringido para personal autorizado.</p>
                <div class="flex gap-4">
                    <span class="text-[9px] px-2 py-1 bg-white border border-slate-200 rounded text-slate-400">Manual Usuario 🔒</span>
                    <span class="text-[9px] px-2 py-1 bg-white border border-slate-200 rounded text-slate-400">Ficha Técnica 🔒</span>
                </div>
            </div>
        </section>

        <!-- Footer -->
        <footer class="mt-auto border-t border-slate-200 pt-4 flex justify-between items-end">
            <div class="space-y-1">
                <p class="text-[9px] text-slate-400 font-medium">Documento generado automáticamente por sistema CORINFAR CMMS</p>
                <p class="text-[9px] text-slate-400">Usuario: <span id="print-user" class="text-slate-600"></span> • Fecha: <span id="print-date"></span></p>
                <div class="flex gap-4 mt-2">
                    <div class="text-[10px] bg-slate-100 px-3 py-1 rounded font-bold text-slate-500">REF: CMMS-FA-001</div>
                    <div id="print-ref-id" class="text-[10px] bg-slate-100 px-3 py-1 rounded font-bold text-slate-500"></div>
                </div>
            </div>
            <div class="flex flex-col items-center gap-1">
                <div id="print-qr-container" class="w-16 h-16 bg-white p-1 border border-slate-200 rounded shadow-sm flex items-center justify-center">
                    <!-- QR Code will be injected here -->
                </div>
                <span class="text-[8px] text-slate-400 font-bold tracking-widest uppercase">Validación QR</span>
            </div>
        </footer>
    </main>
</body>
</html>
`;

// --- Utility Functions ---
async function requestMachineStatusOnPause(woType) {
    // Si es Correctivo o Emergencia, el equipo queda PARADO automáticamente
    if (['Correctivo', 'Emergencia'].includes(woType)) {
        return 'parado';
    }

    return new Promise((resolve) => {
        // Asegurarse de quitar el overlay de carga para que el usuario pueda interactuar
        showLoading(false);

        const btnOperativo = document.getElementById('btn-pause-operativo');
        const btnParado = document.getElementById('btn-pause-parado');

        const handleChoice = (status) => {
            btnOperativo.removeEventListener('click', opHandler);
            btnParado.removeEventListener('click', paHandler);
            state.modals.pauseStatus.hide();
            resolve(status);
        };

        const opHandler = () => handleChoice('operativo');
        const paHandler = () => handleChoice('parado');

        btnOperativo.addEventListener('click', opHandler, { once: true });
        btnParado.addEventListener('click', paHandler, { once: true });

        state.modals.pauseStatus.show();
    });
}

async function hashPassword(password) {
    if (!password) return null;
    const encoder = new TextEncoder();
    const data = encoder.encode(password + "CORINFAR-SALT-2025");
    const hashBuffer = await crypto.subtle.digest('SHA-256', data);
    const hashArray = Array.from(new Uint8Array(hashBuffer));
    return hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
}

async function updateRTDBMirror(collectionName, data, fbId) {
    if (!state.rtdb || !fbId) return;
    const appId = typeof __app_id !== 'undefined' ? __app_id : 'default-cmms-app';
    const cleanId = fbId.toString().replace(/[.#$\[\]]/g, '_');

    const updateMap = {
        'workOrders': 'workOrders',
        'solicitudes': 'solicitudes',
        'machines': 'machines',
        'partRequests': 'partRequests',
        'workPlanExecutions': 'executions'
    };

    const target = updateMap[collectionName];
    if (!target) return;

    try {
        const mirrorRef = rtdbRef(state.rtdb, `artifacts/${appId}/live/${target}/${cleanId}`);

        // Simple data for RTDB mirror to avoid bloat
        const mirrorData = {
            fbId: fbId,
            status: data.status || 'N/A',
            lastUpdate: new Date().toISOString(),
            updatedBy: getCurrentUser().username || 'system'
        };

        if (data.id) mirrorData.id = data.id;
        if (data.machineId) mirrorData.machineId = data.machineId;
        if (data.type) mirrorData.type = data.type;
        if (data.date) mirrorData.date = data.date;
        if (data.leadTechnician) mirrorData.leadTechnician = data.leadTechnician;

        await rtdbSet(mirrorRef, mirrorData);

        // Update global counters
        const countersRef = rtdbRef(state.rtdb, `artifacts/${appId}/live/counters`);
        const counters = {};

        if (collectionName === 'solicitudes' || collectionName === 'partRequests') {
            const pendingSols = state.solicitudes.filter(s => {
                if (s.status !== 'Pendiente') return false;
                let linkedWO = state.workOrders.find(wo =>
                    wo.fb_id === s.workOrderFbId ||
                    wo.id === s.workOrderId ||
                    wo.sourceSolicitudId === s.fb_id ||
                    (wo.solicitudId === s.id && s.id)
                );
                return !linkedWO;
            }).length;
            const pendingPartReqs = state.partRequests.filter(r => r.status === 'Pendiente').length;
            counters.pendingSolicitudes = pendingSols + pendingPartReqs;
        } else if (collectionName === 'workOrders') {
            const activeWos = state.workOrders.filter(wo => ['En Proceso', 'Pausado', 'Pendiente'].includes(wo.status));
            counters.activeWorkOrders = activeWos.length;
            counters.inProgressCount = activeWos.filter(wo => wo.status === 'En Proceso').length;

            // Add monthly stats for dashboard speed
            const now = new Date();
            const startIso = new Date(now.getFullYear(), now.getMonth(), 1).toISOString().split('T')[0];
            const endIso = new Date(now.getFullYear(), now.getMonth() + 1, 0).toISOString().split('T')[0];

            const monthOrders = state.workOrders.filter(o => {
                if (o.status === 'Cancelado' || o.status === 'Rechazado' || o.isExpired) return false;
                return o.date >= startIso && o.date <= endIso;
            });
            counters.monthPreventive = monthOrders.filter(o => ['Preventivo', 'Predictivo', 'Calibración', 'Mecanizado'].includes(o.type)).length;
            counters.monthCorrective = monthOrders.filter(o => ['Correctivo', 'Emergencia'].includes(o.type)).length;
        }

        if (Object.keys(counters).length > 0) {
            await rtdbUpdate(countersRef, counters);
        }

    } catch (error) {
        console.error("RTDB Mirror Error:", error);
    }
}

async function removeRTDBMirror(collectionName, fbId) {
    if (!state.rtdb) return;
    const appId = typeof __app_id !== 'undefined' ? __app_id : 'default-cmms-app';
    const cleanId = fbId.toString().replace(/[.#$\[\]]/g, '_');

    const updateMap = {
        'workOrders': 'workOrders',
        'solicitudes': 'solicitudes',
        'machines': 'machines',
        'partRequests': 'partRequests',
        'workPlanExecutions': 'executions'
    };

    const target = updateMap[collectionName];
    if (!target) return;

    try {
        const mirrorRef = rtdbRef(state.rtdb, `artifacts/${appId}/live/${target}/${cleanId}`);
        await rtdbSet(mirrorRef, null);

        // Update global counters
        const countersRef = rtdbRef(state.rtdb, `artifacts/${appId}/live/counters`);
        const counters = {};

        if (collectionName === 'solicitudes') {
            counters.pendingSolicitudes = state.solicitudes.filter(s => s.status === 'Pendiente').length;
        } else if (collectionName === 'workOrders') {
            counters.activeWorkOrders = state.workOrders.filter(wo => ['En Proceso', 'Pausado', 'Pendiente'].includes(wo.status)).length;
            counters.inProgressCount = state.workOrders.filter(wo => wo.status === 'En Proceso').length;
        }

        if (Object.keys(counters).length > 0) {
            await rtdbUpdate(countersRef, counters);
        }
    } catch (error) {
        console.error("RTDB Mirror Removal Error:", error);
    }
}

function updateUIFromRTCounters() {
    if (!state.liveCounters) return;
    const c = state.liveCounters;

    // Contadores en el Dashboard (si es que existen los IDs)
    // Solo actualizar si estamos en el periodo actual
    const { periodLabel } = getDashboardPeriod();
    const now = new Date();
    const currentMonthLabel = `${now.toLocaleDateString('es-ES', { month: 'long' })} ${now.getFullYear()}`;
    const isCurrentMonth = periodLabel.toLowerCase() === currentMonthLabel.toLowerCase();

    if (!isCurrentMonth) return;

    const solCountEl = document.getElementById('stat-solicitudes');
    if (solCountEl && c.pendingSolicitudes !== undefined) {
        solCountEl.textContent = c.pendingSolicitudes;
    }

    const prevCountEl = document.getElementById('stat-preventivos');
    if (prevCountEl && c.monthPreventive !== undefined) {
        prevCountEl.textContent = c.monthPreventive;
    }

    const corrCountEl = document.getElementById('stat-correctivos');
    if (corrCountEl && c.monthCorrective !== undefined) {
        corrCountEl.textContent = c.monthCorrective;
    }
}

async function recordAuditLog(collectionName, docId, action, oldData, newData, detailedAction = null) {
    // Evitar registrar contraseñas en el log de auditoría
    const sanitize = (data) => {
        if (!data) return null;
        const sanitized = { ...data };
        if (sanitized.password) sanitized.password = '********';
        return sanitized;
    };

    try {
        const logData = {
            collectionName,
            docId,
            action, // 'CREATE', 'UPDATE', 'DELETE'
            detailedAction,
            oldData: sanitize(oldData),
            newData: sanitize(newData),
            userId: state.currentUser ? (state.currentUser.fb_id || state.currentUser.id || 'unknown') : 'system',
            username: state.currentUser ? getCurrentUser().username : 'Sistema',
            timestamp: new Date().toISOString(),
            userAgent: navigator.userAgent
        };
        await addDoc(state.collections.auditLogs, logData);
    } catch (error) {
        console.error("Error recording audit log:", error);
    }
}

function showToast(message, type = 'info') {
    const toastContainer = document.querySelector('.toast-container');
    const toastId = 'toast-' + Date.now();
    
    const toast = document.createElement('div');
    toast.className = `toast align-items-center toast-${type}`;
    toast.setAttribute('role', 'alert');
    toast.setAttribute('aria-live', 'assertive');
    toast.setAttribute('aria-atomic', 'true');
    toast.id = toastId;
    
    toast.innerHTML = `
        <div class="d-flex">
            <div class="toast-body">
                ${message}
            </div>
            <button type="button" class="btn-close me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    `;
    
    toastContainer.appendChild(toast);
    
    const bsToast = new bootstrap.Toast(toast, {
        autohide: true,
        delay: 5000
    });
    
    bsToast.show();
    
    toast.addEventListener('hidden.bs.toast', () => {
        toast.remove();
    });
}

function showLoading(show = true, message = "Procesando, por favor espere...") {
    const loadingOverlay = document.getElementById('global-loading');
    if (loadingOverlay) {
        const loadingText = loadingOverlay.querySelector('.loading-text');
        if (loadingText) loadingText.textContent = message;

        if (show) {
            loadingOverlay.classList.remove('d-none');
        } else {
            loadingOverlay.classList.add('d-none');
        }
    }
}

function formatCurrency(value) {
    const number = parseFloat(value);
    if (isNaN(number)) {
        return 'N/A';
    }
    return `Lps ${number.toFixed(2)}`;
}

function debounce(func, wait) {
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(state.searchTimeouts[func.name]);
            func(...args);
        };
        clearTimeout(state.searchTimeouts[func.name]);
        state.searchTimeouts[func.name] = setTimeout(later, wait);
    };
}

// --- Initialization ---
document.addEventListener('DOMContentLoaded', async () => {
    if (!firebaseConfig.apiKey) {
        document.body.innerHTML = `<div class="alert alert-danger m-5"><strong>Error de Configuración:</strong> La configuración de Firebase no se ha cargado correctamente. La aplicación no puede iniciarse. Asegúrese de que las variables del entorno estén disponibles.</div>`;
        throw new Error("Firebase config is missing or invalid.");
    }

    // Init Bootstrap Modals
    state.modals.machine = new bootstrap.Modal(document.getElementById('machine-modal'));
    state.modals.part = new bootstrap.Modal(document.getElementById('part-modal'));
    state.modals.proveedor = new bootstrap.Modal(document.getElementById('proveedor-modal'));
    state.modals.technician = new bootstrap.Modal(document.getElementById('technician-modal'));
    state.modals.confirm = new bootstrap.Modal(document.getElementById('confirm-modal'));
    state.modals.workOrder = new bootstrap.Modal(document.getElementById('work-order-modal'));
    state.modals.partHistory = new bootstrap.Modal(document.getElementById('part-history-modal'));
    state.modals.manageParts = new bootstrap.Modal(document.getElementById('manage-parts-modal'));
    state.modals.solicitud = new bootstrap.Modal(document.getElementById('solicitud-modal'));
    state.modals.workPlan = new bootstrap.Modal(document.getElementById('work-plan-modal'));
    state.modals.prettyExecutionReport = new bootstrap.Modal(document.getElementById('pretty-execution-report-modal'));
    state.modals.criteria = new bootstrap.Modal(document.getElementById('criteria-modal'));
    state.modals.evaluation = new bootstrap.Modal(document.getElementById('evaluation-modal'));
    state.modals.technicianProfile = new bootstrap.Modal(document.getElementById('technician-profile-modal'));
    state.modals.machineHistory = new bootstrap.Modal(document.getElementById('machine-history-modal'));
    state.modals.machineDetail = new bootstrap.Modal(document.getElementById('machine-detail-modal'));
    state.modals.auditDetail = new bootstrap.Modal(document.getElementById('auditDetailModal'));
    state.modals.passwordConfirm = new bootstrap.Modal(document.getElementById('passwordConfirmModal'));
    state.modals.variableName = new bootstrap.Modal(document.getElementById('variable-name-modal'));
    state.modals.partDetail = new bootstrap.Modal(document.getElementById('part-detail-modal'));
    state.modals.measurementHistory = new bootstrap.Modal(document.getElementById('measurement-history-modal'));
    state.modals.forgotPassword = new bootstrap.Modal(document.getElementById('forgot-password-modal'));
    state.modals.invitationLink = new bootstrap.Modal(document.getElementById('invitation-link-modal'));
    state.modals.pauseStatus = new bootstrap.Modal(document.getElementById('pause-status-modal'));
    state.modals.decommission = new bootstrap.Modal(document.getElementById('decommission-modal'));
    state.modals.stockAdjustment = new bootstrap.Modal(document.getElementById('stock-adjustment-modal'));
    state.modals.purchaseReceive = new bootstrap.Modal(document.getElementById('purchase-receive-modal'));
    state.modals.materialDelivery = new bootstrap.Modal(document.getElementById('material-delivery-modal'));
    state.modals.confirmReceipt = new bootstrap.Modal(document.getElementById('confirm-receipt-modal'));
    state.modals.aiResults = new bootstrap.Modal(document.getElementById('ai-results-modal'));
    state.modals.autoPartRequest = new bootstrap.Modal(document.getElementById('auto-part-request-modal'));
    state.modals.purchaseCart = new bootstrap.Modal(document.getElementById('purchase-cart-modal'));

    setupEventListeners();

    // --- Modal Management (Accessibility & Layering) ---
    const highZModals = [
        { id: 'forgot-password-modal', focusId: 'forgot-username' },
        { id: 'invitation-link-modal', focusId: 'generated-link-input' }
    ];

    highZModals.forEach(m => {
        const el = document.getElementById(m.id);
        if (el) {
            el.addEventListener('show.bs.modal', () => {
                document.body.classList.add('high-z-modal-open');
            });
            el.addEventListener('shown.bs.modal', () => {
                if (m.focusId) {
                    const focusEl = document.getElementById(m.focusId);
                    if (focusEl) focusEl.focus();
                }
            });
            el.addEventListener('hidden.bs.modal', () => {
                document.body.classList.remove('high-z-modal-open');
            });
        }
    });

    const workOrderModalEl = document.getElementById('work-order-modal');
    if (workOrderModalEl) {
        workOrderModalEl.addEventListener('hidden.bs.modal', () => {
            if (state.currentTab === 'planificador') {
                requestAnimationFrame(initializeCalendarPopovers);
            }
        });
    }

    // Initialize Sortable for main task groups in work plan modal
    const taskGroupsContainer = document.getElementById('task-groups-container');
    if (taskGroupsContainer) {
        new Sortable(taskGroupsContainer, {
            animation: 150,
            handle: '.task-group-handle',
            ghostClass: 'sortable-ghost'
        });
    }

    updateCurrentDate();
    populateDateSelectors();
    initCharts();
    handlePeriodChange(); // Set initial state for period selectors
    
    // Call auto-closure/cancellation for old work orders on startup
    handleExpiredWorkOrders();

    // Hourly dashboard refresh for dynamic MTBF and other real-time stats
    setInterval(() => {
        if (state.currentUser && state.currentTab === 'dashboard') {
            updateDashboardData(true);
        }
    }, 60 * 60 * 1000);

    // Load Odoo Settings
    await loadOdooSettings();

    let listenersStarted = false;
    onAuthStateChanged(auth, async (user) => {
        showLoading(false);

        const urlParams = new URLSearchParams(window.location.search);
        const machineId = urlParams.get('machineId');
        const historyMachineId = urlParams.get('historyMachineId');
        const token = urlParams.get('token');

        if (token) {
            handleInvitationToken(token);
        }

        if (machineId) {
            document.getElementById('login-overlay').classList.add('d-none');
            document.getElementById('app-wrapper').classList.remove('d-none');
            document.body.classList.add('public-view');
        }

        if (user) {
            if (!listenersStarted) {
                await initializeSampleData();
                setupRealtimeListeners();
                listenersStarted = true;
            }

            if (machineId) {
                // Optimización: Intentar cargar la máquina específica directamente si tarda mucho la colección completa
                const tryLoadMachine = async () => {
                    if (state.machines.length > 0) {
                        renderPublicMachineReport(machineId);
                        return true;
                    }
                    return false;
                };

                if (!await tryLoadMachine()) {
                    let attempts = 0;
                    const checkInterval = setInterval(async () => {
                        attempts++;
                        if (await tryLoadMachine()) {
                            clearInterval(checkInterval);
                        } else if (attempts > 20) {
                            clearInterval(checkInterval);
                            showToast('Tiempo de espera agotado cargando datos', 'warning');
                        }
                    }, 500);
                }
            }

            if (historyMachineId) {
                const tryShowHistory = async () => {
                    if (state.machines.length > 0 && state.workPlans.length > 0 && state.workPlanExecutions.length > 0) {
                        showMachinePlanHistory(historyMachineId);
                        return true;
                    }
                    return false;
                };

                if (!await tryShowHistory()) {
                    let attempts = 0;
                    const checkInterval = setInterval(async () => {
                        attempts++;
                        if (await tryShowHistory()) {
                            clearInterval(checkInterval);
                        } else if (attempts > 30) {
                            clearInterval(checkInterval);
                            showToast('No se pudo cargar el historial del equipo', 'warning');
                        }
                    }, 500);
                }
            }
        } else {
             try {
                 if (typeof __initial_auth_token !== 'undefined' && __initial_auth_token) {
                     await signInWithCustomToken(auth, __initial_auth_token);
                 } else {
                     await signInAnonymously(auth);
                 }
            } catch (error) {
                 console.error("Error during sign-in:", error);
                 showToast('Error al conectar con el servidor', 'error');
            }
        }
    });
});

// --- Sample Data Initialization (Runs once if DB is empty) ---
async function initializeSampleData() {
    showLoading(true);
    try {
        const techniciansSnapshot = await getDocs(state.collections.technicians);
        if (techniciansSnapshot.empty) {
            
            const adminUser = { 
                username: 'admin', 
                password: 'admin', 
                role: 'Admin', 
                permissions: ['all'], 
                salario: 30000
            };

            await addDoc(state.collections.technicians, adminUser);
            
            showToast('Cuenta de administrador creada. ¡Bienvenido!', 'success');
        }
    } catch (error) {
        console.error("Error initializing administrator account:", error);
        showToast('Error al crear la cuenta de administrador', 'error');
    } finally {
        showLoading(false);
    }
}

function renderPurchaseCart() {
    const list = document.getElementById('purchase-cart-items-list');
    const emptyMsg = document.getElementById('purchase-cart-empty-msg');
    const totalEl = document.getElementById('purchase-cart-total');
    const countEl = document.getElementById('purchase-cart-count');

    if (!list) return;

    const items = state.purchaseCart;
    if (countEl) countEl.textContent = items.length;
    const badgeCountEl = document.getElementById('purchase-cart-badge-count');
    if (badgeCountEl) badgeCountEl.textContent = items.length;

    if (items.length === 0) {
        list.innerHTML = '';
        emptyMsg.classList.remove('d-none');
        totalEl.textContent = formatCurrency(0);
        return;
    }

    emptyMsg.classList.add('d-none');
    let total = 0;

    list.innerHTML = items.map(item => {
        const subtotal = item.quantity * (item.cost || 0);
        total += subtotal;
        return `
            <tr>
                <td>
                    <div class="fw-bold">${item.partId}</div>
                    <div class="small text-muted">${item.description}</div>
                </td>
                <td class="text-center">
                    <div class="input-group input-group-sm justify-content-center">
                        <button class="btn btn-outline-secondary" type="button" onclick="updatePurchaseCartQty('${item.partId}', -1)">-</button>
                        <input type="number" class="form-control text-center" value="${item.quantity}"
                            style="max-width: 60px;"
                            onchange="setPurchaseCartQty('${item.partId}', this.value)">
                        <button class="btn btn-outline-secondary" type="button" onclick="updatePurchaseCartQty('${item.partId}', 1)">+</button>
                    </div>
                </td>
                <td class="text-end">${formatCurrency(item.cost)}</td>
                <td class="text-end fw-bold">${formatCurrency(subtotal)}</td>
                <td class="text-center">
                    <button class="btn btn-sm btn-outline-danger" onclick="removeFromPurchaseCart('${item.partId}')">
                        <i class="fas fa-trash"></i>
                    </button>
                </td>
            </tr>
        `;
    }).join('');

    totalEl.textContent = formatCurrency(total);
}

async function setPurchaseCartQty(partId, value) {
    const item = state.purchaseCart.find(i => i.partId === partId);
    if (!item) return;

    const newQty = Math.max(1, parseInt(value) || 1);
    if (newQty === item.quantity) return;

    try {
        const cartRef = doc(state.collections.purchaseCart, partId);
        await updateDoc(cartRef, { quantity: newQty });
    } catch (error) {
        console.error("Error setting cart quantity:", error);
        renderPurchaseCart(); // Re-render to revert invalid input
    }
}

async function updatePurchaseCartQty(partId, delta) {
    const item = state.purchaseCart.find(i => i.partId === partId);
    if (!item) return;

    const newQty = Math.max(1, item.quantity + delta);
    if (newQty === item.quantity) return;

    try {
        const cartRef = doc(state.collections.purchaseCart, partId);
        await updateDoc(cartRef, { quantity: newQty });
    } catch (error) {
        console.error("Error updating cart quantity:", error);
    }
}

async function removeFromPurchaseCart(partId) {
    try {
        const cartRef = doc(state.collections.purchaseCart, partId);
        await deleteDoc(cartRef);
        showToast('Repuesto eliminado del carrito.', 'info');
    } catch (error) {
        console.error("Error removing from cart:", error);
    }
}

async function clearPurchaseCart() {
    if (state.purchaseCart.length === 0) return;

    if (!confirm('¿Está seguro de que desea vaciar el carrito')) return;

    showLoading(true, 'Vaciando carrito...');
    try {
        const batch = writeBatch(db);
        state.purchaseCart.forEach(item => {
            batch.delete(doc(state.collections.purchaseCart, item.partId));
        });
        await batch.commit();
        showToast('Carrito vaciado.', 'info');
    } catch (error) {
        console.error("Error clearing cart:", error);
    } finally {
        showLoading(false);
    }
}

async function submitPurchaseRequest() {
    if (state.purchaseCart.length === 0) {
        showToast('El carrito está vacío.', 'warning');
        return;
    }

    showLoading(true, 'Generando solicitud...');
    try {
        const now = new Date();
        const year = now.getFullYear();

        // Obtener último folio
        const q = query(state.collections.purchaseRequests, orderBy("folio", "desc"), limit(1));
        const snapshot = await getDocs(q);
        let lastFolio = 0;
        if (!snapshot.empty) {
            const lastReq = snapshot.docs[0].data();
            if (lastReq.folio && typeof lastReq.folio === 'number') {
                lastFolio = lastReq.folio;
            }
        }
        const newFolio = lastFolio + 1;
        const folioStr = `PR-${year}-${newFolio.toString().padStart(4, '0')}`;

        const totalCost = state.purchaseCart.reduce((sum, item) => sum + (item.quantity * item.cost), 0);

        const requestData = {
            folio: newFolio,
            folioStr: folioStr,
            date: now.toISOString(),
            createdBy: getCurrentUser().username,
            status: 'Pendiente',
            items: state.purchaseCart.map(i => ({
                partId: i.partId,
                description: i.description,
                quantity: i.quantity,
                cost: i.cost,
                subtotal: i.quantity * i.cost
            })),
            totalCost: totalCost,
            isoCode: 'FO-ALM-001' // Default editable code
        };

        const docRef = await addDoc(state.collections.purchaseRequests, requestData);

        // Limpiar carrito tras éxito
        const batch = writeBatch(db);
        state.purchaseCart.forEach(item => {
            batch.delete(doc(state.collections.purchaseCart, item.partId));
        });
        await batch.commit();

        await recordAuditLog('purchaseRequests', docRef.id, 'CREATE', null, requestData, `Nueva Solicitud de Compra ${folioStr}`);

        showToast(`Solicitud ${folioStr} generada correctamente.`, 'success');
        state.modals.purchaseCart.hide();

        // Generar PDF
        generatePurchaseRequestPDF({ ...requestData, fb_id: docRef.id });

        // Notificar a administradores, planificadores y grupo global
        const tgMsg = `🛒 <b>Nueva Solicitud de Compra</b>\n\n` +
                      `<b>Folio:</b> ${requestData.folioStr}\n` +
                      `<b>Solicitante:</b> ${escapeHTML(requestData.createdBy)}\n` +
                      `<b>Monto ? Est. :</b> $${requestData.totalCost.toLocaleString()}\n` +
                      `<b>Items:</b> ${requestData.items.length}\n\n` +
                      `Favor de revisar y procesar en el panel de compras.`;
        notifyAdminsAndPlanners(tgMsg).catch(err => console.warn("[Telegram Purchase Error]", err));

    } catch (error) {
        console.error("Error submitting purchase request:", error);
        showToast('Error al generar la solicitud.', 'error');
    } finally {
        showLoading(false);
    }
}

// --- Realtime Listeners ---
function setupRealtimeListeners() {
    // OPTIMIZED: Use docChanges for efficient DOM updates on simple list views
    const debouncedMachinesUpdate = debounce((requiresKpiUpdate) => {
        renderMachines();
        renderDecommissionedMachines();
        populateDynamicSelectors();
        updateMachineCriticidadChart();
        if (requiresKpiUpdate) updateDashboardData();
    }, 300);

    onSnapshot(query(state.collections.machines), snapshot => {
        let requiresKpiUpdate = false;
        let dataChanged = false;
        // Always populate dynamic selectors to keep state consistent
        populateDynamicSelectors();
        snapshot.docChanges().forEach(change => {
            dataChanged = true;
            const machineData = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.machines.findIndex(m => m.fb_id === change.doc.id);
            requiresKpiUpdate = true;

            if (change.type === "added") {
                if (index === -1) state.machines.push(machineData);
            }
            if (change.type === "modified") {
                if (index > -1) state.machines[index] = machineData;
            }
            if (change.type === "removed") {
                if (index > -1) state.machines.splice(index, 1);
            }
        });

        if (dataChanged) {
            debouncedMachinesUpdate(requiresKpiUpdate);
        }
    });
    
    const debouncedPartsUpdate = debounce((requiresCostUpdate) => {
        renderParts();
        if (state.currentTab === 'inventario-dashboard') updateInventoryDashboard();
        if (requiresCostUpdate) updateDashboardData();
    }, 300);

    onSnapshot(query(state.collections.parts), snapshot => {
        let requiresCostUpdate = false;
        let dataChanged = false;
        populateDynamicSelectors();
         snapshot.docChanges().forEach(change => {
            dataChanged = true;
            const partData = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.parts.findIndex(p => p.fb_id === change.doc.id);
            if (change.type !== "removed") {
                const oldPart = state.parts[index];
                if (!oldPart || oldPart.cost !== partData.cost) requiresCostUpdate = true;
            } else {
                requiresCostUpdate = true;
            }

            if (change.type === "added") {
                if (index === -1) state.parts.push(partData);
            }
            if (change.type === "modified") {
                if (index > -1) state.parts[index] = partData;
            }
            if (change.type === "removed") {
                if (index > -1) state.parts.splice(index, 1);
            }
        });

        if (dataChanged) {
            debouncedPartsUpdate(requiresCostUpdate);
            checkDailyLowStock();
        }
    });

     onSnapshot(query(state.collections.proveedores), snapshot => {
        let dataChanged = false;
        populateDynamicSelectors();
         snapshot.docChanges().forEach(change => {
            dataChanged = true;
            const provData = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.proveedores.findIndex(p => p.fb_id === change.doc.id);

            if (change.type === "added") {
                if (index === -1) state.proveedores.push(provData);
            }
            if (change.type === "modified") {
                if (index > -1) state.proveedores[index] = provData;
            }
            if (change.type === "removed") {
                if (index > -1) state.proveedores.splice(index, 1);
            }
        });

        if (dataChanged) {
            renderProveedores();
            populateSupplierSelectors();
        }
    });
    
    onSnapshot(query(state.collections.technicians), snapshot => {
        const wasEmpty = state.technicians.length === 0;
        if (wasEmpty) {
            showLoading(false);
        }
         snapshot.docChanges().forEach(change => {
            const techData = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.technicians.findIndex(p => p.fb_id === change.doc.id);

            if (change.type === "added") {
                if (index === -1) state.technicians.push(techData);
            } else if (change.type === "modified") {
                if (index > -1) state.technicians[index] = techData;
            } else if (change.type === "removed") {
                if (index > -1) state.technicians.splice(index, 1);
            }

            // Logout if current user is deactivated
            if (state.currentUser && techData.username === getCurrentUser().username && techData.isActive === false) {
                handleLogout(true);
            }
        });

        renderTechnicians(); // Full re-render is fine for this small/infrequent table
        populateDynamicSelectors();

        if (state.technicians.length > 0) {
            const loginStatus = document.getElementById('login-status-text');
            if (loginStatus && (loginStatus.textContent === 'Conectando al servidor...' || wasEmpty)) {
                loginStatus.textContent = 'Ingrese sus credenciales';
            }
            const userInp = document.getElementById('username');
            const passInp = document.getElementById('password');
            const loginBtn = document.querySelector('#login-form button[type="submit"]');

            if (userInp) userInp.disabled = false;
            if (passInp) passInp.disabled = false;
            if (loginBtn) loginBtn.disabled = false;
        }
    });
    
    // For complex views like dashboard, calendar, kanban, a full re-render on data change is more robust and acceptable.
    let firstWoLoad = true;
    const debouncedWorkOrdersUpdate = debounce(() => {
        populateWorkOrderSelectors();
        if (state.currentTab === 'planificador' || !initialLoadComplete) renderCalendar();
        if (state.currentTab === 'maquinaria' || !initialLoadComplete) renderMachines();
        if (state.currentTab === 'monitoreo-inteligente' || !initialLoadComplete) renderSmartMonitoring();
        if (state.currentTab === 'trabajo-activo' || !initialLoadComplete) renderActiveWorkView();
        if (state.currentTab === 'ordenes-asignadas' || !initialLoadComplete) renderAssignedOrders();
        if (state.currentTab === 'solicitudes' || !initialLoadComplete) renderSolicitudes();
        // Always try to update dashboard data; the updated hash logic will handle caching
        updateDashboardData();
    }, 300);

    onSnapshot(query(state.collections.workOrders), snapshot => {
        let changed = false;
        snapshot.docChanges().forEach(change => {
            changed = true;
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.workOrders.findIndex(wo => wo.fb_id === change.doc.id);

            if (change.type === "added") {
                if (index === -1) state.workOrders.push(data);
            } else if (change.type === "modified") {
                if (index > -1) state.workOrders[index] = data;
            } else if (change.type === "removed") {
                if (index > -1) state.workOrders.splice(index, 1);
            }
        });

        if (changed) {
            if (firstWoLoad && state.workOrders.length > 0) {
                handleExpiredWorkOrders();
                firstWoLoad = false;
            }
            debouncedWorkOrdersUpdate();
        }
    });

    const debouncedWorkPlansUpdate = debounce(() => {
        if (state.currentTab === 'maquinaria' || !initialLoadComplete) renderMachines();
        if (state.currentTab === 'planes-trabajo' || !initialLoadComplete) {
            renderWorkPlans();
        }
        populateExecutionReportSelector();
        updatePlanNotifications();
        updateDashboardData();
    }, 300);

    onSnapshot(query(state.collections.workPlans), snapshot => {
        let changed = false;
        snapshot.docChanges().forEach(change => {
            changed = true;
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.workPlans.findIndex(p => p.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.workPlans.push(data); }
            else if (change.type === "modified") { if (index > -1) state.workPlans[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.workPlans.splice(index, 1); }
        });
        if (changed) {
            debouncedWorkPlansUpdate();
            checkMaintenanceExpirations();
        }
    });

    const debouncedPlanExecutionsUpdate = debounce(() => {
        if (state.currentTab === 'monitoreo-inteligente' || !initialLoadComplete) renderSmartMonitoring();
        populateExecutionReportSelector();
        updateDashboardData();
    }, 300);

    onSnapshot(query(state.collections.workPlanExecutions), snapshot => {
        let changed = false;
        snapshot.docChanges().forEach(change => {
            changed = true;
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.workPlanExecutions.findIndex(e => e.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.workPlanExecutions.push(data); }
            else if (change.type === "modified") { if (index > -1) state.workPlanExecutions[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.workPlanExecutions.splice(index, 1); }
        });
        if (changed) debouncedPlanExecutionsUpdate();
    });

    const debouncedMonitoringConfigsUpdate = debounce(() => {
        if (state.currentTab === 'monitoreo-inteligente') renderSmartMonitoring();
    }, 300);

    onSnapshot(query(state.collections.monitoringConfigs), snapshot => {
        let changed = false;
        snapshot.docChanges().forEach(change => {
            changed = true;
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.monitoringConfigs.findIndex(c => c.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.monitoringConfigs.push(data); }
            else if (change.type === "modified") { if (index > -1) state.monitoringConfigs[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.monitoringConfigs.splice(index, 1); }
        });
        if (changed) debouncedMonitoringConfigsUpdate();
    });

    onSnapshot(query(state.collections.equipmentStatusHistory), snapshot => {
        snapshot.docChanges().forEach(change => {
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.equipmentStatusHistory.findIndex(h => h.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.equipmentStatusHistory.push(data); }
            else if (change.type === "modified") { if (index > -1) state.equipmentStatusHistory[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.equipmentStatusHistory.splice(index, 1); }
        });
    });

    // Listener for MaterialOut
    onSnapshot(query(state.collections.materialOut), snapshot => {
        let changed = false;
        snapshot.docChanges().forEach(change => {
            changed = true;
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.materialOut.findIndex(m => m.fb_id === change.doc.id);
            if (!state.materialOut) state.materialOut = [];
            if (change.type === "added") { if (index === -1) state.materialOut.push(data); }
            else if (change.type === "modified") { if (index > -1) state.materialOut[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.materialOut.splice(index, 1); }
        });
        if (changed && state.currentTab === 'inventario-dashboard') updateInventoryDashboard();
    });

    if (state.currentUser) {
        const solicitudesQuery = query(state.collections.solicitudes);

        const debouncedSolicitudesUpdate = debounce(() => {
            if (state.currentTab === 'solicitudes' || !initialLoadComplete) renderSolicitudes();
            if (state.currentTab === 'trabajo-activo' || !initialLoadComplete) renderActiveWorkView();
            // Always update dashboard data on solicitudes change
            updateDashboardData();
        }, 300);

        onSnapshot(solicitudesQuery, snapshot => {
            let changed = false;
            snapshot.docChanges().forEach(change => {
                changed = true;
                const data = { fb_id: change.doc.id, ...change.doc.data() };
                const index = state.solicitudes.findIndex(s => s.fb_id === change.doc.id);
                if (change.type === "added") { if (index === -1) state.solicitudes.push(data); }
                else if (change.type === "modified") { if (index > -1) state.solicitudes[index] = data; }
                else if (change.type === "removed") { if (index > -1) state.solicitudes.splice(index, 1); }
            });
            if (changed) debouncedSolicitudesUpdate();
        });
    }

    onSnapshot(query(state.collections.evaluationCriteria), snapshot => {
        let changed = false;
        snapshot.docChanges().forEach(change => {
            changed = true;
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.evaluationCriteria.findIndex(c => c.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.evaluationCriteria.push(data); }
            else if (change.type === "modified") { if (index > -1) state.evaluationCriteria[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.evaluationCriteria.splice(index, 1); }
        });
        if (changed && state.currentTab === 'evaluation-criteria') renderCriteria();
    });

    onSnapshot(query(state.collections.technicianEvaluations), snapshot => {
        snapshot.docChanges().forEach(change => {
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.technicianEvaluations.findIndex(e => e.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.technicianEvaluations.push(data); }
            else if (change.type === "modified") { if (index > -1) state.technicianEvaluations[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.technicianEvaluations.splice(index, 1); }
        });
    });


    onSnapshot(query(state.collections.partRequests), snapshot => {
        let changed = false;
        snapshot.docChanges().forEach(change => {
            changed = true;
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.partRequests.findIndex(r => r.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.partRequests.push(data); }
            else if (change.type === "modified") { if (index > -1) state.partRequests[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.partRequests.splice(index, 1); }
        });
        if (changed) renderPartRequests();
    });

    onSnapshot(query(state.collections.purchaseRequests), snapshot => {
        snapshot.docChanges().forEach(change => {
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.purchaseRequests.findIndex(r => r.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.purchaseRequests.push(data); }
            else if (change.type === "modified") { if (index > -1) state.purchaseRequests[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.purchaseRequests.splice(index, 1); }
        });
        if (state.currentTab === 'inventario-dashboard') renderPurchaseRequestsTable();
    });

    onSnapshot(query(state.collections.purchaseCart), snapshot => {
        snapshot.docChanges().forEach(change => {
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.purchaseCart.findIndex(i => i.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.purchaseCart.push(data); }
            else if (change.type === "modified") { if (index > -1) state.purchaseCart[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.purchaseCart.splice(index, 1); }
        });
        if (typeof renderPurchaseCart === 'function') renderPurchaseCart();
    });

    onSnapshot(query(state.collections.partMovements, orderBy("date", "desc"), limit(100)), snapshot => {
        snapshot.docChanges().forEach(change => {
            const data = { fb_id: change.doc.id, ...change.doc.data() };
            const index = state.partMovements.findIndex(m => m.fb_id === change.doc.id);
            if (change.type === "added") { if (index === -1) state.partMovements.push(data); }
            else if (change.type === "modified") { if (index > -1) state.partMovements[index] = data; }
            else if (change.type === "removed") { if (index > -1) state.partMovements.splice(index, 1); }
        });
        if (state.currentTab === 'inventario-dashboard') renderInventoryTraceability();
    });

    // --- RTDB Real-time High Frequency Listeners ---
    const monitoringRef = rtdbRef(state.rtdb, `artifacts/${appId}/monitoring`);
    rtdbOnValue(monitoringRef, (snapshot) => {
        const data = snapshot.val();
        if (data) {
            state.rtdbMonitoring = data;
            // Immediate UI update if in relevant tabs
            if (state.currentTab === 'monitoreo-inteligente' || !initialLoadComplete) renderSmartMonitoring();
            if (state.currentTab === 'maquinaria' || !initialLoadComplete) renderMachines();
        }
    });

    // Real-time Counters and Critical State Mirror
    const liveRootRef = rtdbRef(state.rtdb, `artifacts/${appId}/live`);
    rtdbOnValue(liveRootRef, (snapshot) => {
        const data = snapshot.val();
        if (data) {
            state.liveCounters = data.counters || {};
            state.liveWorkOrders = data.workOrders || {};
            state.liveSolicitudes = data.solicitudes || {};
            state.liveMachines = data.machines || {};

            updateUIFromRTCounters();

            // Targeted UI updates for speed
            if (state.currentTab === 'dashboard' || !initialLoadComplete) updateDashboardData(true);
            if (state.currentTab === 'solicitudes' || !initialLoadComplete) renderSolicitudes();
            if (state.currentTab === 'trabajo-activo' || !initialLoadComplete) renderActiveWorkView();
            if (state.currentTab === 'maquinaria' || !initialLoadComplete) renderMachines();
        }
    });
}


// --- Report Search Handlers ---
function handleReportMachineSearch(e) {
    populateDynamicSelectors(e.target.value, 'report-machine-select');
}
function handleReportMachinePartsSearch(e) {
    populateDynamicSelectors(e.target.value, 'report-machine-parts-select');
}
function handleReportWoSearch(e) {
    populateWorkOrderSelectors(e.target.value);
}
function handleReportExecutionSearch(e) {
    populateExecutionReportSelector(e.target.value);
}

// --- Event Listeners Setup ---
function safeAddEventListener(id, event, handler) {
    const el = document.getElementById(id);
    if (el) {
        el.addEventListener(event, handler);
    }
}

function setupEventListeners() {
    document.addEventListener('keydown', (event) => {
        if (event.key === 'F1') {
            event.preventDefault();
            event.stopPropagation();
        }
    }, true);

    safeAddEventListener('login-form', 'submit', handleLogin);
    safeAddEventListener('forgot-password-link', 'click', (e) => {
        e.preventDefault();
        showLoading(false);

        const forgotInput = document.getElementById('forgot-username');
        if (forgotInput) forgotInput.value = '';

        if (state.modals.forgotPassword) {
            state.modals.forgotPassword.show();
        } else {
            state.modals.forgotPassword = new bootstrap.Modal(document.getElementById('forgot-password-modal'));
            state.modals.forgotPassword.show();
        }
    });
    safeAddEventListener('submit-forgot-password', 'click', handleForgotPasswordRequest);
    safeAddEventListener('set-password-form', 'submit', handleSetPasswordSubmit);
    safeAddEventListener('copy-link-btn', 'click', () => {
        const input = document.getElementById('generated-link-input');
        input.select();
        document.execCommand('copy');
        showToast('Link copiado al portapapeles', 'success');
    });

    safeAddEventListener('theme-toggle-btn', 'click', toggleTheme);
    safeAddEventListener('password-toggle', 'click', togglePasswordVisibility);
    safeAddEventListener('solicitud-item-search', 'input', () => {
        populateSolicitudItemSelector();
    });

    safeAddEventListener('solicitud-type', 'change', (e) => {
        const type = e.target.value;
        const isInsumos = type === 'insumos';

        // Reset search and item selection on type change
        const searchInput = document.getElementById('solicitud-item-search');
        if (searchInput) searchInput.value = '';
        const itemSelect = document.getElementById('solicitud-item-part-select');
        if (itemSelect) itemSelect.innerHTML = '';

        document.getElementById('solicitud-extra-fields').classList.toggle('d-none', !isInsumos);
        document.getElementById('solicitud-multi-items-section').classList.toggle('d-none', !isInsumos);
        document.getElementById('solicitud-description-container').classList.toggle('d-none', isInsumos);
        document.getElementById('solicitud-quantity-container').classList.toggle('d-none', isInsumos);

        document.getElementById('solicitud-machine').required = !isInsumos;
        document.getElementById('solicitud-description').required = !isInsumos;

        document.getElementById('solicitud-machine-label').innerHTML = isInsumos ? 'Máquina (Opcional)' : 'Máquina';

        let descLabel = 'Descripción del Problema';
        if (type === 'insumos') descLabel = 'Descripción de lo solicitado';
        if (type === 'mecanizado') descLabel = 'Descripción del trabajo de mecanizado';
        document.getElementById('solicitud-description-label').innerHTML = descLabel;

        if (isInsumos) {
            populateSolicitudItemSelector();
        }
    });

    safeAddEventListener('solicitud-add-item-btn', 'click', () => {
        const partSelect = document.getElementById('solicitud-item-part-select');
        const manualDesc = document.getElementById('solicitud-item-description').value.trim();
        const quantity = parseInt(document.getElementById('solicitud-item-quantity').value);

        if (isNaN(quantity) || quantity < 1) {
            showToast('Indique una cantidad válida.', 'warning');
            return;
        }

        let description = '';
        if (partSelect.value) {
            const part = state.parts.find(p => (p.id || p.fb_id) === partSelect.value);
            description = part ? (part.description || part.name) : '';
        } else if (manualDesc) {
            description = manualDesc;
        } else {
            showToast('Seleccione un insumo o escriba una descripción.', 'warning');
            return;
        }

        state.tempSolicitudItems.push({
            description: description,
            quantity: quantity,
            partId: partSelect.value || null,
            deliveredQuantity: 0,
            status: 'Pendiente'
        });

        document.getElementById('solicitud-item-description').value = '';
        document.getElementById('solicitud-item-part-select').value = '';
        document.getElementById('solicitud-item-quantity').value = 1;

        renderSolicitudItems();
    });
    
    // Machine Search Listeners
    const machineSearchInputs = [
        { searchId: 'solicitud-machine-search', selectorId: 'solicitud-machine' },
        { searchId: 'kpi-machine-search', selectorId: 'kpi-machine-select' },
        { searchId: 'report-machine-select-search', selectorId: 'report-machine-select' },
        { searchId: 'report-machine-parts-select-search', selectorId: 'report-machine-parts-select' },
        { searchId: 'work-plan-machine-search', selectorId: 'work-plan-machine' },
        { searchId: 'decommission-replacement-search', selectorId: 'decommission-replacement' }
    ];

    machineSearchInputs.forEach(cfg => {
        const input = document.getElementById(cfg.searchId);
        if (input) {
            input.addEventListener('input', (e) => {
                populateDynamicSelectors(e.target.value, cfg.selectorId);
            });
        }
    });

    safeAddEventListener('logout-btn', 'click', handleLogout);
    safeAddEventListener('btn-filter-audit', 'click', renderAuditLogs);
    safeAddEventListener('generate-audit-report-btn', 'click', generateAuditReport);
    
    document.querySelectorAll('.nav-tab').forEach(tab => {
        tab.addEventListener('click', (e) => {
            e.preventDefault();
            switchTab(tab.dataset.tab);
            if(window.innerWidth < 1200) {
                toggleSidebar(false);
            }
        });
    });

    safeAddEventListener('sidebar-toggle', 'click', () => toggleSidebar());
    safeAddEventListener('sidebar-overlay', 'click', () => toggleSidebar(false));
    
    // Machine Listeners
    safeAddEventListener('add-machine-btn', 'click', () => showMachineModal());
    safeAddEventListener('search-decommissioned-input', 'input', debounce(renderDecommissionedMachines, 300));
    safeAddEventListener('decommission-form', 'submit', handleDecommissionSubmit);
    safeAddEventListener('decommission-modal', 'hidden.bs.modal', () => {
        if (state.reopenMachineDetail) {
            const machineId = state.reopenMachineDetail;
            state.reopenMachineDetail = null;
            showMachineDetail(machineId);
        }
    });
    safeAddEventListener('sync-all-machines-odoo-btn', 'click', () => syncAllMachinesToOdoo());
    safeAddEventListener('machine-form', 'submit', handleMachineSubmit);
    safeAddEventListener('global-machine-photo-input', 'change', handleGlobalMachinePhotoChange);
    safeAddEventListener('search-machine-input', 'input', debounce(renderMachines, 300));

    document.querySelectorAll('.asset-category-filters [data-category]').forEach(btn => {
        btn.addEventListener('click', () => {
            state.assetCategoryFilter = btn.dataset.category || 'all';
            renderMachines();
        });
    });

    // Filtros de resumen de maquinaria
    document.querySelectorAll('.machine-summary-header .summary-pill').forEach(pill => {
        pill.addEventListener('click', () => {
            const filter = pill.querySelector('.label').textContent.trim();
            state.machineMaintFilter = (state.machineMaintFilter === filter) ? null : filter;
            renderMachines();
        });
    });

    safeAddEventListener('disable-schedule-check', 'change', e => {
        document.getElementById('schedule-wrapper').style.display = e.target.checked ? 'none' : 'block';
    });

    safeAddEventListener('machine-type', 'change', e => {
        const categorySelect = document.getElementById('machine-asset-category');
        if (!categorySelect) return;
        if (e.target.value === 'equipo_oficina') categorySelect.value = 'oficina';
        else if (e.target.value === 'instalacion') categorySelect.value = 'infraestructura';
    });

    // Part Listeners
    safeAddEventListener('add-part-btn', 'click', () => showPartModal());
    safeAddEventListener('btn-show-purchase-cart', 'click', () => state.modals.purchaseCart.show());
    safeAddEventListener('btn-clear-purchase-cart', 'click', clearPurchaseCart);
    safeAddEventListener('btn-submit-purchase-request', 'click', submitPurchaseRequest);
    safeAddEventListener('sync-all-parts-odoo-btn', 'click', () => syncAllPartsToOdoo());
    safeAddEventListener('view-all-part-history-btn', 'click', () => showPartHistoryModal(null));

    const addPartMobile = document.getElementById('btn-add-part-mobile');
    if (addPartMobile) addPartMobile.addEventListener('click', () => showPartModal());

    const syncPartsMobile = document.getElementById('btn-part-sync-mobile');
    if (syncPartsMobile) syncPartsMobile.addEventListener('click', () => syncAllPartsToOdoo());

    const historyPartsMobile = document.getElementById('btn-part-history-mobile');
    if (historyPartsMobile) historyPartsMobile.addEventListener('click', () => showPartHistoryModal(null));
    safeAddEventListener('part-form', 'submit', handlePartSubmit);
    safeAddEventListener('btn-adjust-stock', 'click', showStockAdjustmentModal);
    safeAddEventListener('stock-adjustment-form', 'submit', handleStockAdjustmentSubmit);
    safeAddEventListener('purchase-receive-form', 'submit', handlePurchaseReceiveSubmit);
    safeAddEventListener('material-delivery-form', 'submit', handleMaterialDeliverySubmit);

    safeAddEventListener('add-part-doc-btn', 'click', () => {
        document.getElementById('part-doc-file').click();
    });

    safeAddEventListener('part-doc-file', 'change', (e) => {
        const file = e.target.files[0];
        if (file) {
            currentPartDocs.push({ name: file.name, file: file });
            renderPartDocsList();
            e.target.value = '';
        }
    });

    safeAddEventListener('part-image-file', 'change', (e) => {
        const file = e.target.files[0];
        if (file) {
            const reader = new FileReader();
            reader.onload = (event) => {
                const preview = document.getElementById('part-image-preview');
                preview.src = event.target.result;
                preview.classList.remove('d-none');
                document.querySelector('#part-modal .upload-placeholder').classList.add('d-none');
            };
            reader.readAsDataURL(file);
        }
    });
    safeAddEventListener('search-part-input', 'input', debounce(renderParts, 300));

    document.querySelectorAll('.part-classification-filters [data-classification]').forEach(btn => {
        btn.addEventListener('click', () => {
            state.partClassificationFilter = btn.dataset.classification || 'all';
            renderParts();
        });
    });

    // Filtros de resumen de repuestos
    document.querySelectorAll('.inventory-summary .summary-pill').forEach(pill => {
        pill.addEventListener('click', () => {
            let filter = '';
            if (pill.classList.contains('critical')) filter = 'critical';
            else if (pill.classList.contains('attention')) filter = 'attention';
            else if (pill.classList.contains('operative')) filter = 'operative';

            state.partStockFilter = (state.partStockFilter === filter) ? null : filter;
            renderParts();
        });
    });

    // Proveedor Listeners
    safeAddEventListener('add-proveedor-btn', 'click', () => showProveedorModal());
    safeAddEventListener('proveedor-form', 'submit', handleProveedorSubmit);
    safeAddEventListener('search-proveedor-input', 'input', debounce(renderProveedores, 300));

    // Technician/User Listeners
    safeAddEventListener('add-technician-btn', 'click', () => showTechnicianModal());
    safeAddEventListener('technician-form', 'submit', handleTechnicianSubmit);
    safeAddEventListener('btn-verify-telegram', 'click', handleVerifyTelegramConnection);
    safeAddEventListener('technician-role', 'change', (e) => {
        updateTechnicianModalUIForRole(e.target.value);
    });

    // Toggle all machines in technician modal
    document.querySelectorAll('.toggle-all-machines').forEach(btn => {
        btn.addEventListener('click', function() {
            const targetId = this.dataset.target;
            const container = document.getElementById(targetId);
            const checkboxes = container.querySelectorAll('input[type="checkbox"]');
            const allChecked = Array.from(checkboxes).every(cb => cb.checked);

            checkboxes.forEach(cb => cb.checked = !allChecked);
            this.textContent = !allChecked ? 'Deseleccionar todas' : 'Seleccionar todas';
        });
    });
    safeAddEventListener('technician-genera-gasto', 'change', (e) => {
        const wrapper = document.getElementById('technician-salary-wrapper');
        const salaryInput = document.getElementById('technician-salary');
        if (e.target.checked) {
            wrapper.classList.remove('d-none');
            salaryInput.required = true;
        } else {
            wrapper.classList.add('d-none');
            salaryInput.required = false;
        }
    });

    // Signature Listeners
    safeAddEventListener('toggle-draw-sig', 'click', () => {
        document.getElementById('signature-draw-container').style.display = 'block';
        document.getElementById('signature-upload-container').style.display = 'none';
        initSignaturePad();
    });
    safeAddEventListener('toggle-upload-sig', 'click', () => {
        document.getElementById('signature-draw-container').style.display = 'none';
        document.getElementById('signature-upload-container').style.display = 'block';
    });
    safeAddEventListener('clear-signature', 'click', () => {
        if (state.signaturePad) state.signaturePad.clear();
    });

    safeAddEventListener('signature-upload', 'change', (e) => {
        const file = e.target.files[0];
        if (file) {
            const reader = new FileReader();
            reader.onload = (event) => {
                const preview = document.getElementById('signature-preview');
                const previewContainer = document.getElementById('signature-preview-container');
                preview.src = event.target.result;
                previewContainer.classList.remove('d-none');
            };
            reader.readAsDataURL(file);
        }
    });
    
    // Solicitud Listeners
    safeAddEventListener('add-solicitud-btn', 'click', showSolicitudModal);
    safeAddEventListener('solicitud-form', 'submit', handleSolicitudSubmit);

    // Work Plan Listeners
    safeAddEventListener('add-work-plan-btn', 'click', () => showWorkPlanModal());
    safeAddEventListener('save-work-plan-btn', 'click', handleWorkPlanSubmit);
    safeAddEventListener('work-plan-search-input', 'input', debounce(() => {
        state.workPlanPage = 1;
        renderWorkPlans();
    }, 300));
    ['wp-filter-location', 'wp-filter-area', 'wp-filter-criticality', 'wp-filter-status', 'wp-filter-frequency'].forEach(id => {
        safeAddEventListener(id, 'change', () => {
            state.workPlanPage = 1;
            renderWorkPlans();
        });
    });
    safeAddEventListener('wp-clear-filters-btn', 'click', clearWorkPlanFilters);
    document.querySelectorAll('#wp-category-tabs [data-category]').forEach(btn => {
        btn.addEventListener('click', () => {
            state.workPlanCategoryFilter = btn.dataset.category || 'all';
            state.workPlanPage = 1;
            document.querySelectorAll('#wp-category-tabs [data-category]').forEach(el => el.classList.remove('active'));
            btn.classList.add('active');
            renderWorkPlans();
        });
    });
    safeAddEventListener('wp-grid-view-btn', 'click', () => setWorkPlanViewMode('grid'));
    safeAddEventListener('wp-list-view-btn', 'click', () => setWorkPlanViewMode('list'));
    document.querySelectorAll('#work-plan-group-tabs .nav-link').forEach(tab => {
        tab.addEventListener('click', (e) => {
            state.activeWorkPlanTab = e.target.dataset.group;
            document.querySelectorAll('#work-plan-group-tabs .nav-link').forEach(el => el.classList.remove('active'));
            e.target.classList.add('active');
            renderWorkPlans();
        });
    });
    safeAddEventListener('add-task-group-btn', 'click', () => addTaskGroup());

    // Criteria Listeners
    safeAddEventListener('add-criteria-btn', 'click', () => showCriteriaModal());
    safeAddEventListener('criteria-form', 'submit', handleCriteriaSubmit);

    // Evaluation Listeners
    safeAddEventListener('evaluation-form', 'submit', handleEvaluationSubmit);


    // Planner Listeners
    safeAddEventListener('prev-month-btn', 'click', () => changeMonth(-1));
    safeAddEventListener('next-month-btn', 'click', () => changeMonth(1));
    safeAddEventListener('planner-search-input', 'input', debounce(renderCalendar, 300));
    safeAddEventListener('add-preventive-task-btn', 'click', () => showWorkOrderModal(null, 'Preventivo'));
    safeAddEventListener('add-corrective-task-btn-planner', 'click', () => showWorkOrderModal(null, 'Correctivo'));

    // Work Order Listeners
    safeAddEventListener('wo-machine-search', 'input', (e) => populateWorkOrderMachineSelector(e.target.value));
    safeAddEventListener('wo-machine', 'change', (e) => {
        const machineId = e.target.value;
        const machine = state.machines.find(m => m.id === machineId);
        if (machine && machine.status === 'DADO_DE_BAJA') {
            showToast('ADVERTENCIA: Este equipo está DADO DE BAJA.', 'warning');
        }
    });
    safeAddEventListener('wo-type', 'change', handleWorkOrderTypeChange);
    safeAddEventListener('wo-save-btn', 'click', () => saveWorkOrder());
    safeAddEventListener('wo-start-btn', 'click', () => handleWorkOrderStart());
    safeAddEventListener('confirm-auto-request-btn', 'click', () => submitAutomaticPartRequest());
    safeAddEventListener('wo-pause-btn', 'click', async () => {
        const type = document.getElementById('wo-type').value;
        const machineStatusOnPause = await requestMachineStatusOnPause(type);
        showLoading(true); // El modal de pausa lo quita, hay que ponerlo para el guardado
        saveWorkOrder({ status: 'Pausado', machineStatusOnPause });
    });
    safeAddEventListener('wo-resume-btn', 'click', () => saveWorkOrder({ status: 'En Proceso' }));
    safeAddEventListener('wo-complete-btn', 'click', () => saveWorkOrder({ status: 'Completado' }));
    safeAddEventListener('wo-validate-btn', 'click', async () => {
        const fbId = document.getElementById('wo-fb-id-hidden').value;
        const now = new Date().toISOString();
        // Cambiamos a Completado directamente para que los datos queden registrados para auditorías
        await saveWorkOrder({
            status: 'Completado',
            isExpired: false,
            validatedBy: getCurrentUser().username
        });
        await completeLinkedPlanExecution(fbId, now, 'Completado', getCurrentUser().username);
    });
    safeAddEventListener('wo-reject-btn', 'click', async () => {
        const fbId = document.getElementById('wo-fb-id-hidden').value;
        await saveWorkOrder({
            status: 'Pausado',
            fechaFinalizacionReal: null,
            totalWorkDurationMs: null,
            technicianFinished: false,
            validatedBy: null
        });
        showToast('Orden rechazada y devuelta a estado Pausado.', 'info');
    });
    safeAddEventListener('wo-add-part-btn', 'click', () => addPartToWorkOrder());
    safeAddEventListener('wo-add-image-btn', 'click', () => document.getElementById('wo-image-input').click());
    safeAddEventListener('wo-image-input', 'change', handleWoImageUpload);
    safeAddEventListener('wo-add-to-request-cart-btn', 'click', handleAddToRequestCart);
    safeAddEventListener('wo-submit-request-btn', 'click', handlePartRequestSubmit);
    safeAddEventListener('part-request-status-filter', 'change', renderPartRequests);
    safeAddEventListener('part-request-month-filter', 'change', renderPartRequests);
    safeAddEventListener('wo-add-support-technician-btn', 'click', handleAddSupportTechnicianClick);
    safeAddEventListener('wo-lead-technician-select', 'change', handleLeadTechnicianChange);

    // Manage Parts Modal Listeners
    safeAddEventListener('add-update-part-btn', 'click', handleAddOrUpdatePartInModal);
    safeAddEventListener('save-managed-parts-btn', 'click', handleSaveManagedParts);

    // Criteria Listeners
    safeAddEventListener('add-criteria-btn', 'click', () => showCriteriaModal());
    safeAddEventListener('criteria-form', 'submit', handleCriteriaSubmit);

    // Evaluation Listeners
    safeAddEventListener('evaluation-form', 'submit', handleEvaluationSubmit);

    // Report Listeners
    safeAddEventListener('report-machine-select-search', 'input', debounce(handleReportMachineSearch, 300));
    safeAddEventListener('report-machine-parts-select-search', 'input', debounce(handleReportMachinePartsSearch, 300));
    safeAddEventListener('report-wo-search', 'input', debounce(handleReportWoSearch, 300));
    safeAddEventListener('report-execution-search', 'input', debounce(handleReportExecutionSearch, 300));

    safeAddEventListener('generate-general-report-btn', 'click', generateGeneralReport);
    safeAddEventListener('generate-wo-report-btn', 'click', generateWorkOrderReport);
    safeAddEventListener('generate-machine-report-btn', 'click', generateMachineReport);
    safeAddEventListener('generate-machine-parts-report-btn', 'click', generateMachinePartsReport);
    safeAddEventListener('generate-single-wo-report-btn', 'click', generateSingleWorkOrderReport);
    safeAddEventListener('generate-global-report-btn', 'click', generateGlobalReport);
    safeAddEventListener('generate-executive-report-btn', 'click', generateExecutiveReportData);
    safeAddEventListener('generate-execution-report-btn', 'click', generateExecutionReportPDF);
    safeAddEventListener('generate-technician-performance-report-btn', 'click', generateTechnicianPerformanceReport);
    safeAddEventListener('backup-db-btn', 'click', downloadBackup);
    safeAddEventListener('refresh-monitoring-btn', 'click', () => {
        renderSmartMonitoring();
        document.getElementById('last-update-text').textContent = 'Actualizado hace un momento';
    });

    // Monitoring Config Listeners
    safeAddEventListener('m-config-cancel-btn', 'click', hideMonitoringConfig);
    safeAddEventListener('m-config-save-btn', 'click', handleSaveMonitoringConfig);
    safeAddEventListener('m-config-add-var-btn', 'click', () => {
        document.getElementById('new-variable-name').value = '';
        state.modals.variableName.show();
    });

    safeAddEventListener('save-variable-name-btn', 'click', handleAddMonitoringVariable);
    safeAddEventListener('new-variable-name', 'keypress', (e) => {
        if (e.key === 'Enter') handleAddMonitoringVariable();
    });

    // Threshold Inputs Listeners
    ['normal', 'warning', 'critical'].forEach(type => {
        safeAddEventListener(`m-threshold-${type}-min`, 'input', updateActiveVarThresholds);
        safeAddEventListener(`m-threshold-${type}-max`, 'input', updateActiveVarThresholds);
    });

    safeAddEventListener('technician-list', 'click', (e) => {
        const viewButton = e.target.closest('.view-profile-btn');
        const editButton = e.target.closest('.edit-tech');
        const deleteButton = e.target.closest('.delete-tech:not([disabled])');
        const toggleStatusButton = e.target.closest('.toggle-tech-status');

        if (viewButton) {
            showTechnicianProfileModal(viewButton.dataset.id);
        } else if (editButton) {
            showTechnicianModal(editButton.dataset.id);
        } else if (deleteButton) {
            deleteTechnician(deleteButton.dataset.id);
        } else if (toggleStatusButton) {
            toggleTechnicianStatus(toggleStatusButton.dataset.id);
        }
    });


    // Dashboard Filters
    safeAddEventListener('dashboardDate', 'change', updateDashboardData);
    safeAddEventListener('dashboardWeek', 'change', updateDashboardData);
    safeAddEventListener('dashboard-period-select', 'change', handlePeriodChange);
    safeAddEventListener('dashboardMonth', 'change', () => {
        if (document.getElementById('dashboard-period-select').value === 'week') {
            populateWeekSelector();
        }
        updateDashboardData();
    });
    safeAddEventListener('dashboardYear', 'change', () => {
        if (document.getElementById('dashboard-period-select').value === 'week') {
            populateWeekSelector();
        }
        updateDashboardData();
    });
    safeAddEventListener('kpi-machine-select', 'change', updateDashboardData);

    const kanbanMonthFilter = document.getElementById('kanban-month-filter');
    if (kanbanMonthFilter) {
        kanbanMonthFilter.addEventListener('change', renderActiveWorkView);
    }

    const assignedMonthFilter = document.getElementById('assigned-month-filter');
    if (assignedMonthFilter) {
        assignedMonthFilter.addEventListener('change', renderAssignedOrders);
    }

    const solicitudesMonthFilter = document.getElementById('solicitudes-month-filter');
    if (solicitudesMonthFilter) {
        solicitudesMonthFilter.addEventListener('change', renderSolicitudes);
    }
    
    const statusIndicator = document.getElementById('connection-status-indicator');
    window.addEventListener('online', () => updateConnectionStatus(true));
    window.addEventListener('offline', () => updateConnectionStatus(false));
    updateConnectionStatus(navigator.onLine);

    // Plan Execution View Listeners
    const finishBtn = document.getElementById('execution-view-finish-btn');
    if (finishBtn) finishBtn.addEventListener('click', handleFinishPlanExecution);

    const pauseBtn = document.getElementById('execution-view-pause-btn');
    if (pauseBtn) pauseBtn.addEventListener('click', () => {
        const { execution } = state.currentExecution || {};
        if (execution && execution.status === 'Pausado') {
            handleResumePlanExecution();
        } else {
            handlePausePlanExecution();
        }
    });

    const closeBtn = document.getElementById('execution-view-close-btn');
    if (closeBtn) closeBtn.addEventListener('click', handleClosePlanExecution);

    const historyBtn = document.getElementById('execution-view-history-btn');
    if (historyBtn) historyBtn.addEventListener('click', () => {
        const { plan } = state.currentExecution || {};
        if (plan && plan.machineId) {
            showMachinePlanHistory(plan.machineId);
        } else {
            showToast('No se pudo identificar la máquina para ver el historial.', 'error');
        }
    });

    // Odoo Config Listeners
    const odooForm = document.getElementById('odoo-config-form');
    if (odooForm) odooForm.addEventListener('submit', handleOdooConfigSubmit);

    const testOdooBtn = document.getElementById('test-odoo-btn');
    if (testOdooBtn) testOdooBtn.addEventListener('click', testOdooConnection);

    // Telegram Config Listeners
    const telegramForm = document.getElementById('telegram-config-form');
    if (telegramForm) telegramForm.addEventListener('submit', handleTelegramConfigSubmit);

    const testTelegramBtn = document.getElementById('test-telegram-btn');
    if (testTelegramBtn) testTelegramBtn.addEventListener('click', testTelegramConnection);

    // AI Settings Listeners
    const aiConfigForm = document.getElementById('ai-config-form');
    if (aiConfigForm) aiConfigForm.addEventListener('submit', handleAIConfigSubmit);

    const testAiBtn = document.getElementById('test-ai-btn');
    if (testAiBtn) testAiBtn.addEventListener('click', testAIConnection);

    const toggleAiKeyBtn = document.getElementById('toggle-ai-key');
    if (toggleAiKeyBtn) toggleAiKeyBtn.addEventListener('click', () => {
        const input = document.getElementById('ai-apikey');
        const icon = toggleAiKeyBtn.querySelector('i');
        if (input.type === 'password') {
            input.type = 'text';
            icon.classList.replace('fa-eye', 'fa-eye-slash');
        } else {
            input.type = 'password';
            icon.classList.replace('fa-eye-slash', 'fa-eye');
        }
    });

    const refreshAiUsageBtn = document.getElementById('refresh-ai-usage-btn');
    if (refreshAiUsageBtn) refreshAiUsageBtn.addEventListener('click', loadAIUsage);

    safeAddEventListener('inv-trace-search', 'input', debounce(() => renderInventoryTraceability(), 300));
    safeAddEventListener('inv-trace-type-filter', 'change', renderInventoryTraceability);
    safeAddEventListener('export-inventory-trace-btn', 'click', exportInventoryTraceabilityCSV);

    const confirmReceiptSubmit = document.getElementById('confirm-receipt-submit');
    if (confirmReceiptSubmit) confirmReceiptSubmit.addEventListener('click', handleConfirmReceiptSubmit);

    const deliveryItemSelect = document.getElementById('delivery-item-index');
    if (deliveryItemSelect) deliveryItemSelect.addEventListener('change', handleDeliveryItemChange);
}

// --- Odoo Configuration Functions ---
async function loadOdooSettings() {
    try {
        const querySnapshot = await getDocs(state.collections.settings);
        const odooDoc = querySnapshot.docs.find(d => d.id === 'odoo_config');
        const telegramDoc = querySnapshot.docs.find(d => d.id === 'telegram_config');

        if (odooDoc) {
            const data = odooDoc.data();
            document.getElementById('odoo-url').value = data.url || '';
            document.getElementById('odoo-db').value = data.db || '';
            document.getElementById('odoo-username').value = data.username || '';
            document.getElementById('odoo-apikey').value = data.apiKey || '';
            document.getElementById('odoo-proxy').value = data.proxyUrl || '';

            state.odoo = new OdooConnector(data.url, data.db, data.username, data.apiKey, data.proxyUrl);
            updateOdooStatus('Configurado', 'success');
            console.log("[Odoo Sync] Configuración de Odoo cargada correctamente.");

            // Intentar detectar la versión de Odoo en segundo plano
            state.odoo.version().then(v => {
                if (v && v.server_version) {
                    // Extract only digits from version string (e.g., "saas~19.1+e" -> 19)
                    const match = v.server_version.match(/\d+/);
                    state.odooVersion = match ? parseInt(match[0]) : null;
                    console.log(`[Odoo Sync] Versión de servidor detectada: ${v.server_version} (Major: ${state.odooVersion})`);
                }
            }).catch(err => console.warn("[Odoo Sync] No se pudo obtener la versión de Odoo:", err));
        } else {
            console.log("[Odoo Sync] No hay configuración de Odoo guardada.");
        }

        if (telegramDoc) {
            const data = telegramDoc.data();
            document.getElementById('telegram-token').value = data.token || '';
            document.getElementById('telegram-chat-id').value = data.chatId || '';
            state.telegramConfig = data;
            updateTelegramStatus('Configurado', 'success');
        }
    } catch (error) {
        console.error("Error loading settings:", error);
    }
}

async function handleTelegramConfigSubmit(e) {
    e.preventDefault();
    showLoading(true);

    const configData = {
        token: document.getElementById('telegram-token').value.trim(),
        chatId: document.getElementById('telegram-chat-id').value.trim(),
        updatedAt: new Date().toISOString(),
        updatedBy: getCurrentUser().username || 'system'
    };

    try {
        await setDoc(doc(state.collections.settings, 'telegram_config'), configData);
        state.telegramConfig = configData;
        updateTelegramStatus('Guardado correctamente', 'success');
        showToast('Configuración de Telegram guardada', 'success');
    } catch (error) {
        console.error("Error saving Telegram config:", error);
        showToast('Error al guardar la configuración', 'error');
    } finally {
        showLoading(false);
    }
}

async function testTelegramConnection() {
    const token = document.getElementById('telegram-token').value.trim();
    const chatId = document.getElementById('telegram-chat-id').value.trim();

    if (!token || !chatId) {
        showToast('Complete Token y Chat ID para probar', 'warning');
        return;
    }

    showLoading(true);
    updateTelegramStatus('Enviando prueba...', 'info');

    try {
        const message = `🚀 <b>Prueba de Conexión CMMS CORINFAR</b>\nLa integración con Telegram ha sido configurada correctamente.`;
        const success = await sendTelegramNotification(message, chatId, token);
        if (success) {
            updateTelegramStatus('Conexión OK', 'success');
            showToast('Notificación de prueba enviada con éxito', 'success');
        } else {
            throw new Error("Fallo al enviar el mensaje.");
        }
    } catch (error) {
        console.error("Test Telegram error:", error);
        updateTelegramStatus('Error: ' + error.message, 'danger');
        showToast('Fallo al conectar con Telegram: ' + error.message, 'error');
    } finally {
        showLoading(false);
    }
}

function updateTelegramStatus(message, type) {
    const indicator = document.getElementById('telegram-status-indicator');
    if (indicator) {
        indicator.textContent = message;
        indicator.className = `small text-${type}`;
        const icon = document.createElement('i');
        icon.className = `fas fa-circle me-1`;
        indicator.prepend(icon);
    }
}

async function checkDailyLowStock() {
    // Solo ejecutado por Admin o Planificador
    if (!state.currentUser || !['Admin', 'Planificador'].includes(getCurrentUser().role)) return;

    const now = new Date();
    const todayStr = now.toISOString().split('T')[0];

    try {
        const statusDocRef = doc(state.collections.settings, 'low_stock_status');
        const statusSnap = await getDoc(statusDocRef);

        if (statusSnap.exists()) {
            const statusData = statusSnap.data();
            if (statusData.lastCheck === todayStr) return; // Ya se envió hoy
        }

        const lowStockParts = state.parts.filter(p => p.stockActual <= (p.reorderPoint || 0));

        if (lowStockParts.length > 0) {
            let message = `⚠️ <b>RESUMEN DIARIO DE STOCK BAJO</b>\n\n`;
            message += `Se han detectado ${lowStockParts.length} repuestos por debajo del punto de reorden:\n\n`;

            lowStockParts.sort((a,b) => (a.stockActual || 0) - (b.stockActual || 0)).slice(0, 15).forEach(p => {
                const status = p.stockActual === 0 ? '❌ AGOTADO' : '⚠️ BAJO';
                message += `• <b>${escapeHTML(p.id)}</b>: ${escapeHTML(p.description)}\n`;
                message += `  Stock: ${p.stockActual} | Reorden: ${p.reorderPoint || 0} (${status})\n\n`;
            });

            if (lowStockParts.length > 15) {
                message += `<i>...y ${lowStockParts.length - 15} repuestos más.</i>\n\n`;
            }

            message += `🔗 <a href="https://corinfar-cmms.web.app/?tab=repuestos">Abrir Gestión de Inventario</a>`;

            const success = await notifyAdminsAndPlanners(message);
            if (success) {
                await setDoc(statusDocRef, { lastCheck: todayStr, sentAt: now.toISOString() });
                console.log("[Low Stock Check] Resumen diario enviado a Telegram.");
            }
        } else {
            // Marcar como chequeado aunque no haya faltantes para no re-procesar entre usuarios
            await setDoc(statusDocRef, { lastCheck: todayStr, sentAt: now.toISOString(), empty: true });
        }
    } catch (error) {
        console.error("Error in checkDailyLowStock:", error);
    }
}

/**
 * Revisa planes de mantenimiento por vencer o vencidos y envía alertas diarias vía Telegram
 */
async function checkMaintenanceExpirations() {
    // Solo ejecutado por Admin o Planificador
    if (!state.currentUser || !['Admin', 'Planificador'].includes(getCurrentUser().role)) return;

    const now = new Date();
    now.setHours(0,0,0,0);
    const todayStr = now.toISOString().split('T')[0];

    try {
        const statusDocRef = doc(state.collections.settings, 'maint_exp_status');
        const statusSnap = await getDoc(statusDocRef);

        if (statusSnap.exists()) {
            const statusData = statusSnap.data();
            if (statusData.lastCheck === todayStr) return; // Ya se envió hoy
        }

        const alerts = {
            vencidos: [], // 1 día después
            hoy: [],      // Mismo día
            proximos: []  // 3 días antes
        };

        state.workPlans.forEach(plan => {
            if (!plan.isActive || !plan.nextDueDate) return;

            const dueDate = new Date(plan.nextDueDate + 'T00:00:00');
            const diffTime = dueDate.getTime() - now.getTime();
            const diffDays = Math.round(diffTime / (1000 * 60 * 60 * 24));

            if (diffDays === -1) alerts.vencidos.push(plan);
            else if (diffDays === 0) alerts.hoy.push(plan);
            else if (diffDays === 3) alerts.proximos.push(plan);
        });

        const hasAlerts = alerts.vencidos.length > 0 || alerts.hoy.length > 0 || alerts.proximos.length > 0;

        if (hasAlerts) {
            let globalMsg = `📅 <b>RESUMEN DIARIO DE MANTENIMIENTOS</b>\n\n`;

            if (alerts.vencidos.length > 0) {
                globalMsg += `❌ <b>VENCIDOS (Ayer):</b>\n`;
                alerts.vencidos.forEach(p => globalMsg += `• ${escapeHTML(p.name)} (${p.machineId})\n`);
                globalMsg += `\n`;
            }
            if (alerts.hoy.length > 0) {
                globalMsg += `⚠️ <b>PARA ? HOY :</b>\n`;
                alerts.hoy.forEach(p => globalMsg += `• ${escapeHTML(p.name)} (${p.machineId})\n`);
                globalMsg += `\n`;
            }
            if (alerts.proximos.length > 0) {
                globalMsg += `ℹ️ <b>PRÓXIMOS (3 días):</b>\n`;
                alerts.proximos.forEach(p => globalMsg += `• ${escapeHTML(p.name)} (${p.machineId})\n`);
                globalMsg += `\n`;
            }

            globalMsg += `🔗 <a href="https://corinfar-cmms.web.app/?tab=planes-trabajo">Ver Planes de Trabajo</a>`;

            // Enviar resumen global a Admin/Planificador
            const success = await notifyAdminsAndPlanners(globalMsg);

            if (success) {
                // Notificaciones personales a técnicos responsables
                const allAlerts = [...alerts.vencidos, ...alerts.hoy, ...alerts.proximos];
                for (const plan of allAlerts) {
                    if (plan.responsibleTechnician) {
                        const dueDate = new Date(plan.nextDueDate + 'T00:00:00');
                        const diffTime = dueDate.getTime() - now.getTime();
                        const diffDays = Math.round(diffTime / (1000 * 60 * 60 * 24));

                        let personalMsg = '';
                        if (diffDays === -1) personalMsg = `❌ <b>MANTENIMIENTO VENCIDO</b>\nEl plan "${plan.name}" venció ayer.`;
                        else if (diffDays === 0) personalMsg = `⚠️ <b>MANTENIMIENTO PARA HOY</b>\nEl plan "${plan.name}" debe ejecutarse hoy.`;
                        else if (diffDays === 3) personalMsg = `ℹ️ <b>MANTENIMIENTO PRÓXIMO</b>\nEl plan "${plan.name}" vence en 3 días.`;

                        if (personalMsg) {
                            personalMsg += `\n\n<b>Equipo:</b> ${plan.machineId}\n<b>Fecha:</b> ${plan.nextDueDate}`;
                            await notifyTechnicians(plan.responsibleTechnician, personalMsg);
                        }
                    }
                }

                await setDoc(statusDocRef, { lastCheck: todayStr, sentAt: now.toISOString() });
                console.log("[Maint Expiration Check] Alertas diarias enviadas a Telegram.");
            }
        } else {
            await setDoc(statusDocRef, { lastCheck: todayStr, sentAt: now.toISOString(), empty: true });
        }
    } catch (error) {
        console.error("Error in checkMaintenanceExpirations:", error);
    }
}

/**
 * Escapes HTML special characters to prevent Telegram API errors
 */
function escapeHTML(text) {
    if (!text) return '';
    return String(text)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#039;');
}

async function sendTelegramNotification(message, chatId = null, customToken = null) {
    const token = customToken || state.telegramConfig.token;
    const targetChatId = chatId || state.telegramConfig.chatId;

    if (!token || !targetChatId) {
        console.warn("[Telegram] Configuración incompleta. Token o Chat ID faltante.");
        return false;
    }

    const url = `https://api.telegram.org/bot${token}/sendMessage`;
    const body = {
        chat_id: targetChatId,
        text: message,
        parse_mode: 'HTML'
    };

    try {
        const response = await fetch(url, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(body)
        });

        const data = await response.json();
        if (!data.ok) {
            console.error("[Telegram API Error]", data);
            return false;
        }
        return true;
    } catch (error) {
        console.error("[Telegram Fetch Error]", error);
        return false;
    }
}

/**
 * Notifica a una lista de técnicos vía Telegram (Personal)
 * @param {string|string[]} technicianUsernames - Nombre de usuario o array de nombres de usuario
 * @param {string} message - Mensaje a enviar
 */
async function notifyTechnicians(technicianUsernames, message) {
    if (!technicianUsernames) return false;
    const usernames = Array.isArray(technicianUsernames) ? technicianUsernames : [technicianUsernames];

    // Eliminar duplicados y filtrar nulos
    const uniqueUsernames = [...new Set(usernames.filter(u => u))];
    let someSuccess = false;

    for (const username of uniqueUsernames) {
        const tech = state.technicians.find(t => t.username === username);
        // Solo notificar si el técnico está activo y tiene Chat ID
        if (tech && tech.isActive !== false && tech.telegramChatId) {
            // Añadir un pequeño retraso de 200ms entre envíos para evitar congestión y retrasos en la red
            await new Promise(resolve => setTimeout(resolve, 200));
            const sent = await sendTelegramNotification(message, tech.telegramChatId)
                .catch(err => {
                    console.error(`[Telegram Personal Error] User: ${username}`, err);
                    return false;
                });
            if (sent) someSuccess = true;
        }
    }
    return someSuccess;
}

/**
 * Notifica a todos los administradores y planificadores activos vía Telegram
 */
async function notifyAdminsAndPlanners(message) {
    const adminsAndPlanners = state.technicians
        .filter(t => t?.isActive !== false && ['Admin', 'Planificador'].includes(t?.role))
        .map(t => t.username);

    // Notificar al grupo global primero
    const globalSuccess = await sendTelegramNotification(message).catch(err => {
        console.warn("[Telegram Global Error]", err);
        return false;
    });

    // Luego notificar individualmente a cada admin/planificador
    const personalSuccess = await notifyTechnicians(adminsAndPlanners, message);

    return globalSuccess || personalSuccess;
}

async function handleOdooConfigSubmit(e) {
    e.preventDefault();
    showLoading(true);

    const configData = {
        url: document.getElementById('odoo-url').value.trim(),
        db: document.getElementById('odoo-db').value.trim(),
        username: document.getElementById('odoo-username').value.trim(),
        apiKey: document.getElementById('odoo-apikey').value.trim(),
        proxyUrl: document.getElementById('odoo-proxy').value.trim()
    };

    try {
        await setDoc(doc(state.collections.settings, 'odoo_config'), configData);
        state.odoo = new OdooConnector(configData.url, configData.db, configData.username, configData.apiKey, configData.proxyUrl);
        updateOdooStatus('Guardado correctamente', 'success');
        showToast('Configuración de Odoo guardada', 'success');
    } catch (error) {
        console.error("Error saving Odoo config:", error);
        showToast('Error al guardar la configuración', 'error');
    } finally {
        showLoading(false);
    }
}

async function testOdooConnection() {
    const url = document.getElementById('odoo-url').value.trim();
    const db = document.getElementById('odoo-db').value.trim();
    const username = document.getElementById('odoo-username').value.trim();
    const apiKey = document.getElementById('odoo-apikey').value.trim();
    const proxyUrl = document.getElementById('odoo-proxy').value.trim();

    if (!url || !db || !username || !apiKey) {
        showToast('Complete todos los campos para probar la conexión', 'warning');
        return;
    }

    // Validación de ayuda para el usuario
    if (db.includes('.odoo.com') || db.includes('http')) {
        showToast('Atención: El campo "Base de Datos" suele ser solo el nombre (ej: "miempresa"), no la URL completa.', 'warning');
    }

    showLoading(true);
    updateOdooStatus('Probando conexión...', 'info');

    const tempConnector = new OdooConnector(url, db, username, apiKey, proxyUrl);

    try {
        const uid = await tempConnector.authenticate();
        if (uid) {
            // Detectar versión
            const v = await tempConnector.version();
            if (v && v.server_version) {
                const match = v.server_version.match(/\d+/);
                state.odooVersion = match ? parseInt(match[0]) : null;
                console.log(`[Odoo Sync Test] Versión detectada: ${v.server_version} (Major: ${state.odooVersion})`);
            }

            // Prueba adicional: verificar permisos de mantenimiento
            try {
                await tempConnector.searchRead('maintenance.equipment', [], ['id']);
                const verSuffix = state.odooVersion ? ` (Odoo ${state.odooVersion})` : '';
                updateOdooStatus(`Conexión OK${verSuffix}`, 'success');
                showToast(`¡Conexión exitosa${verSuffix} y permisos de Mantenimiento verificados!`, 'success');
            } catch (permError) {
                updateOdooStatus('Conexión OK (Sin Permisos Mantenimiento)', 'warning');
                showToast('Conexión exitosa, pero el usuario no tiene permisos para el módulo de Mantenimiento.', 'warning');
                console.warn("Error de permisos Odoo:", permError);
            }
        } else {
             throw new Error("No se recibió un UID válido. Verifique sus credenciales (Email y API Key).");
        }
    } catch (error) {
        console.error("Test Odoo error:", error);
        updateOdooStatus('Fallo en la conexión: ' + error.message, 'danger');
        showToast('Fallo al conectar con Odoo: ' + error.message, 'error');
    } finally {
        showLoading(false);
    }
}

function updateOdooStatus(message, type) {
    const indicator = document.getElementById('odoo-status-indicator');
    if (indicator) {
        indicator.textContent = message;
        indicator.className = `small text-${type}`;

        const icon = document.createElement('i');
        icon.className = `fas fa-circle me-1`;
        indicator.prepend(icon);
    }
}

// --- Auth & Permissions Functions ---
async function handleLogin(e) {
    e.preventDefault();
    const username = document.getElementById('username').value;
    const password = document.getElementById('password').value;
    
    document.getElementById('login-text').classList.add('d-none');
    document.getElementById('login-spinner').classList.remove('d-none');
    document.getElementById('login-container').classList.add('loading');
    
    const hashedPassword = await hashPassword(password);
    const user = state.technicians.find(t => {
        if (t.username !== username) return false;
        if (t.passwordIsHashed) {
            return t.password === hashedPassword;
        } else {
            return t.password === password;
        }
    });
    
    if (user) {
        if (user.isActive === false) {
            document.getElementById('login-error').textContent = 'Su cuenta ha sido desactivada. Por favor, contacte al administrador.';
            document.getElementById('login-error').classList.remove('d-none');
            showToast('Cuenta desactivada', 'error');

            document.getElementById('login-text').classList.remove('d-none');
            document.getElementById('login-spinner').classList.add('d-none');
            document.getElementById('login-container').classList.remove('loading');
            return;
        }

        state.currentUser = user;

        // Aplicar preferencia de tema
        if (user.theme === 'dark') {
            document.body.classList.add('dark-mode');
            state.currentTheme = 'dark';
        } else {
            document.body.classList.remove('dark-mode');
            state.currentTheme = 'light';
        }
        updateChartsTheme();

        document.getElementById('login-overlay').classList.add('d-none');
        document.getElementById('app-wrapper').classList.remove('d-none');
        
        document.getElementById('user-display').textContent = user.username;
        document.getElementById('user-role').textContent = user.role;
        document.getElementById('user-avatar').src = `https://ui-avatars.com/api/name=${user.username}&background=0D8ABC&color=fff`;
        
        document.getElementById('login-error').classList.add('d-none');
        
        showToast(`Bienvenido, ${user.username}`, 'success');
        
        setupRealtimeListeners(); // Re-setup listeners with correct user role
        applyUserPermissions();

        // Forzar actualización del dashboard tras login exitoso para asegurar carga de gráficos
        setTimeout(() => {
            updateDashboardData(true);
            // Disparar un evento de resize global para obligar a Chart.js a recalcular dimensiones
            window.dispatchEvent(new Event('resize'));
        }, 800);

    } else {
        document.getElementById('login-error').classList.remove('d-none');
        showToast('Usuario o contraseña incorrectos', 'error');
    }
    
    document.getElementById('login-text').classList.remove('d-none');
    document.getElementById('login-spinner').classList.add('d-none');
    document.getElementById('login-container').classList.remove('loading');
}

function handleLogout(force = false) {
    const logoutAction = () => {
        window.location.reload();
    };

    if (force === true) {
        logoutAction();
    } else {
        showConfirmation('Cerrar sesión', '¿Está seguro de que desea cerrar la sesión', () => {
            logoutAction();
        });
    }
}

let sessionTimeout;
function resetSessionTimeout() {
    if (sessionTimeout) clearTimeout(sessionTimeout);
    if (state.currentUser) {
        sessionTimeout = setTimeout(() => {
            handleLogout(true);
        }, 5 * 60 * 1000); // 5 minutos de inactividad
    }
}
['mousedown', 'mousemove', 'keypress', 'scroll', 'touchstart'].forEach(name => {
    document.addEventListener(name, resetSessionTimeout, true);
});

function applyUserPermissions() {
    if (!state.currentUser) return;

    const { role, permissions = [] } = state.currentUser;
    const allTabs = document.querySelectorAll('.nav-tab');

    allTabs.forEach(tab => {
         tab.style.display = 'none';
    });
     document.querySelector('.nav-tab[data-tab="dashboard"]').style.display = 'flex';


    let visibleTabs = [];

    switch (role) {
        case 'Admin' :
            visibleTabs = ['monitoreo-inteligente', 'maquinaria', 'equipos-baja', 'repuestos', 'proveedores', 'tecnicos', 'trabajo-activo', 'planificador', 'planes-trabajo', 'reportes', 'auditoria', 'solicitudes', 'evaluation-criteria', 'ai-settings', 'configuracion', 'solicitudes-repuestos', 'inventario-dashboard', 'ordenes-asignadas'];
            break;
        case 'Planificador' :
            visibleTabs = ['monitoreo-inteligente', 'maquinaria', 'equipos-baja', 'repuestos', 'proveedores', 'trabajo-activo', 'planificador', 'planes-trabajo', 'reportes', 'solicitudes', 'ordenes-asignadas', 'solicitudes-repuestos', 'inventario-dashboard', 'ai-settings'];
            break;
        case 'Almacén' :
            visibleTabs = ['monitoreo-inteligente', 'repuestos', 'proveedores', 'solicitudes-repuestos', 'inventario-dashboard', 'reportes'];
            break;
        case 'Jefe de Area' :
            visibleTabs = ['monitoreo-inteligente', ...(permissions || [])];
            break;
        case 'Invitado' :
            visibleTabs = ['monitoreo-inteligente', 'maquinaria', 'repuestos', 'proveedores', 'tecnicos', 'trabajo-activo', 'planificador', 'reportes'];
            break;
        case 'Técnico' :
            visibleTabs = permissions.includes('all') ?
                ['monitoreo-inteligente', 'maquinaria', 'repuestos', 'proveedores', 'planificador', 'ordenes-asignadas', 'reportes']
                : ['monitoreo-inteligente', ...permissions];
            if(permissions.includes('ordenes-asignadas')) {
                 visibleTabs.push('trabajo-activo');
            }
            visibleTabs.push('solicitudes-repuestos');
            break;
        case 'Operario' :
        case 'Supervisor de Area' :
            visibleTabs = ['solicitudes', 'trabajo-activo', 'planificador', 'solicitudes-repuestos'];
            break;
    }

    visibleTabs.forEach(tabName => {
        const tab = document.querySelector(`.nav-tab[data-tab="${tabName}"]`);
        if (tab) tab.style.display = 'flex';
    });

    document.querySelectorAll('.admin-only').forEach(el => {
        el.style.display = (role === 'Admin') ? 'flex' : 'none';
    });

     const isAdmin = role === 'Admin';
     const isPlanner = role === 'Planificador';
     const canManageResources = isAdmin || isPlanner;
     const canManageUsers = isAdmin;
     const canPlan = isAdmin || isPlanner;

     document.getElementById('add-machine-btn').style.display = canManageResources ? 'inline-block' : 'none';
     document.getElementById('sync-all-machines-odoo-btn').style.display = (canManageResources && state.odoo) ? 'inline-block' : 'none';
     document.getElementById('add-part-btn').style.display = canManageResources ? 'inline-block' : 'none';
     document.getElementById('sync-all-parts-odoo-btn').style.display = (canManageResources && state.odoo) ? 'inline-block' : 'none';
     document.getElementById('add-proveedor-btn').style.display = canManageResources ? 'inline-block' : 'none';
     document.getElementById('add-technician-btn').style.display = canManageUsers ? 'inline-block' : 'none';
     document.getElementById('add-preventive-task-btn').style.display = canPlan ? 'inline-block' : 'none';
     document.getElementById('add-corrective-task-btn-planner').style.display = canPlan ? 'inline-block' : 'none';

    // Filter report options for Technicians
    const isTecnico = role === 'Técnico';
    const generalReportItem = document.getElementById('generate-general-report-btn').closest('.list-group-item');
    const woHistoryReportItem = document.getElementById('generate-wo-report-btn').closest('.list-group-item');
    const globalReportItem = document.getElementById('generate-global-report-btn').closest('.list-group-item');

    if (generalReportItem) generalReportItem.classList.toggle('d-none', isTecnico);
    if (woHistoryReportItem) woHistoryReportItem.classList.toggle('d-none', isTecnico);
    if (globalReportItem) globalReportItem.classList.toggle('d-none', isTecnico);

    // Refresh dynamic selectors after permissions are applied to ensure role-based filtering is active
    populateDynamicSelectors();
    populateWorkOrderSelectors();
    populateExecutionReportSelector();

    updateMachineCriticidadChart();
    renderMachines();
    renderParts();
    renderProveedores();
    renderTechnicians();
}


// --- UI Functions ---
async function switchTab(tabName) {
    cleanupPopovers();
    document.querySelectorAll('.main-content > .tab-content').forEach(tab => tab.classList.add('d-none'));
    document.querySelectorAll('.nav-tab').forEach(tab => tab.classList.remove('active'));
    
    const targetContent = document.getElementById(tabName);
    if (targetContent) targetContent.classList.remove('d-none');

    const activeTab = document.querySelector(`[data-tab="${tabName}"]`);
    if (activeTab) {
        activeTab.classList.add('active');
        const pageTitle = document.getElementById('page-title');
        if (pageTitle) pageTitle.textContent = activeTab.innerText.trim();
    }
    
    state.currentTab = tabName;
    if (tabName === 'ai-settings') {
        loadAIConfig();
        loadAIUsage();
    }

    document.getElementById('dashboard-date-filter').classList.toggle('d-none', tabName !== 'dashboard');

    // Refresh dynamic selectors when entering key tabs
    if (tabName === 'dashboard' || tabName === 'reportes' || tabName === 'equipos-baja' || tabName === 'planes-mantenimiento' || tabName === 'solicitudes') {
        // Clear searches
        if (tabName === 'dashboard') {
            const s = document.getElementById('kpi-machine-search');
            if (s) s.value = '';
        }
        if (tabName === 'reportes') {
            const s1 = document.getElementById('report-machine-select-search');
            if (s1) s1.value = '';
            const s2 = document.getElementById('report-machine-parts-select-search');
            if (s2) s2.value = '';
        }

        populateDynamicSelectors();
        populateWorkOrderSelectors();
        populateExecutionReportSelector();
        if (tabName === 'dashboard') updateDashboardData(true);
    }

    if (tabName === 'maquinaria') renderMachines();
    if (tabName === 'equipos-baja') renderDecommissionedMachines();
    if (tabName === 'repuestos') renderParts();
    if (tabName === 'monitoreo-inteligente') renderSmartMonitoring();
    if (tabName === 'solicitudes-repuestos') renderPartRequests();
    if (tabName === 'inventario-dashboard') {
        loadPartMovements();
        updateInventoryDashboard();
    }
    if (tabName === 'trabajo-activo') {
        const monthFilter = document.getElementById('kanban-month-filter');
        if (monthFilter && !monthFilter.value) {
            const now = new Date();
            const year = now.getFullYear();
            const month = (now.getMonth() + 1).toString().padStart(2, '0');
            monthFilter.value = `${year}-${month}`;
        }
        renderActiveWorkView();
    }
    if (tabName === 'ordenes-asignadas') {
        const monthFilter = document.getElementById('assigned-month-filter');
        if (monthFilter && !monthFilter.value) {
            const now = new Date();
            const year = now.getFullYear();
            const month = (now.getMonth() + 1).toString().padStart(2, '0');
            monthFilter.value = `${year}-${month}`;
        }
        renderAssignedOrders();
    }
    if (tabName === 'planificador') renderCalendar();
    if (tabName === 'evaluation-criteria') renderCriteria();
    if (tabName === 'auditoria') loadAuditLogs();
    if (tabName === 'solicitudes') {
        const monthFilter = document.getElementById('solicitudes-month-filter');
        if (monthFilter && !monthFilter.value) {
            const now = new Date();
            const year = now.getFullYear();
            const month = (now.getMonth() + 1).toString().padStart(2, '0');
            monthFilter.value = `${year}-${month}`;
        }
        renderSolicitudes();
    }
    if (tabName === 'planes-trabajo') renderWorkPlans();
    if (tabName === 'configuracion') await loadOdooSettings();
}

function togglePasswordVisibility() {
    const passwordInput = document.getElementById('password');
    const icon = document.getElementById('password-toggle').querySelector('i');
    
    if (passwordInput.type === 'password') {
        passwordInput.type = 'text';
        icon.classList.replace('fa-eye', 'fa-eye-slash');
    } else {
        passwordInput.type = 'password';
        icon.classList.replace('fa-eye-slash', 'fa-eye');
    }
}

function toggleSidebar(forceState) {
    const sidebar = document.querySelector('.sidebar');
    const overlay = document.getElementById('sidebar-overlay');
    const mainContent = document.querySelector('.main-content');
    
    const show = forceState !== undefined ? forceState : !sidebar.classList.contains('show');

    if (show) {
        sidebar.classList.add('show');
        overlay.style.display = 'block';
        if(window.innerWidth < 1200) {
           mainContent.style.marginLeft = '260px'; 
        }
    } else {
        sidebar.classList.remove('show');
        overlay.style.display = 'none';
        mainContent.style.marginLeft = '0';
    }
}

function showConfirmation(title, message, onConfirm) {
    document.getElementById('confirm-modal-title').textContent = title;
    document.getElementById('confirm-modal-body').textContent = message;
    
    const confirmBtn = document.getElementById('confirm-modal-btn');
    const newConfirmBtn = confirmBtn.cloneNode(true);
    confirmBtn.parentNode.replaceChild(newConfirmBtn, confirmBtn);
    
    const modalInstance = new bootstrap.Modal(document.getElementById('confirm-modal'));

    newConfirmBtn.addEventListener('click', () => {
        onConfirm();
        modalInstance.hide();
    });
    
    modalInstance.show();
}

function updateConnectionStatus(online) {
    const statusIndicator = document.getElementById('connection-status-indicator');
    state.isOnline = online;
    
    if (online) {
        statusIndicator.classList.remove('offline');
        statusIndicator.classList.remove('syncing');
        statusIndicator.title = 'Conectado';
        if (state.currentUser) showToast('Conexión restablecida', 'success');
    } else {
        statusIndicator.classList.remove('syncing');
        statusIndicator.classList.add('offline');
        statusIndicator.title = 'Sin conexión';
        if (state.currentUser) showToast('Se perdió la conexión. Algunas funciones pueden no estar disponibles.', 'warning');
    }
}

function updateCurrentDate() {
    const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
    const currentDate = new Date().toLocaleDateString('es-ES', options);
    document.getElementById('current-date').textContent = currentDate.charAt(0).toUpperCase() + currentDate.slice(1);
}

function populateDateSelectors() {
    const monthSelect = document.getElementById('dashboardMonth');
    const yearSelect = document.getElementById('dashboardYear');
    const globalMonthSelect = document.getElementById('report-global-month');
    const globalYearSelect = document.getElementById('report-global-year');
    const executiveMonthSelect = document.getElementById('report-executive-month');
    const executiveYearSelect = document.getElementById('report-executive-year');
    // REMOVED: const techMonthSelect = document.getElementById('report-technician-month');
    // REMOVED: const techYearSelect = document.getElementById('report-technician-year');
    const dateSelect = document.getElementById('dashboardDate');
    const now = new Date();
    const currentMonth = now.getMonth();
    const currentYear = now.getFullYear();

    const months = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio', 'Julio', 'Agosto', 'Septiembre', 'Octubre', 'Noviembre', 'Diciembre'];

    if (monthSelect) {
        monthSelect.innerHTML = '';
        months.forEach((month, index) => {
            const option = document.createElement('option');
            option.value = index;
            option.textContent = month;
            if (index === currentMonth) {
                option.selected = true;
            }
            monthSelect.appendChild(option);
        });
    }


    if (yearSelect) {
        yearSelect.innerHTML = '';
        for (let i = currentYear + 1; i >= currentYear - 5; i--) {
            const option = document.createElement('option');
            option.value = i;
            option.textContent = i;
            if (i === currentYear) {
                option.selected = true;
            }
            yearSelect.appendChild(option);
        }
    }

    if (globalYearSelect) {
        globalYearSelect.innerHTML = '';
        for (let i = currentYear + 1; i >= currentYear - 5; i--) {
            const option = document.createElement('option');
            option.value = i;
            option.textContent = i;
            if (i === currentYear) {
                option.selected = true;
            }
            globalYearSelect.appendChild(option);
        }
    }



    if (globalMonthSelect) {
        globalMonthSelect.innerHTML = '';
        months.forEach((month, index) => {
            const option = document.createElement('option');
            option.value = index;
            option.textContent = month;
            if (index === currentMonth) {
                option.selected = true;
            }
            globalMonthSelect.appendChild(option);
        });
    }

    if (executiveMonthSelect) {
        executiveMonthSelect.innerHTML = '';
        months.forEach((month, index) => {
            const option = document.createElement('option');
            option.value = index;
            option.textContent = month;
            if (index === currentMonth) {
                option.selected = true;
            }
            executiveMonthSelect.appendChild(option);
        });
    }

    if (executiveYearSelect) {
        executiveYearSelect.innerHTML = '';
        for (let i = currentYear + 1; i >= currentYear - 5; i--) {
            const option = document.createElement('option');
            option.value = i;
            option.textContent = i;
            if (i === currentYear) {
                option.selected = true;
            }
            executiveYearSelect.appendChild(option);
        }
    }

    const executiveAreaSelect = document.getElementById('report-executive-area');
    if (executiveAreaSelect) {
        const areas = [...new Set(state.machines.map(m => m.location).filter(Boolean))].sort();
        executiveAreaSelect.innerHTML = '<option value="all">Todas las Áreas</option>';
        areas.forEach(area => {
            const option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            executiveAreaSelect.appendChild(option);
        });
    }

    const executiveCCSelect = document.getElementById('report-executive-cost-center');
    if (executiveCCSelect) {
        const ccs = [...new Set(state.machines.map(m => m.centroCosto).filter(Boolean))].sort();
        executiveCCSelect.innerHTML = '<option value="all">Todos los Centros de Costo</option>';
        ccs.forEach(cc => {
            const option = document.createElement('option');
            option.value = cc;
            option.textContent = cc;
            executiveCCSelect.appendChild(option);
        });
    }

    if (dateSelect) {
        dateSelect.value = now.toISOString().split('T')[0];
    }
}

function populateWeekSelector() {
    const weekSelect = document.getElementById('dashboardWeek');
    const month = parseInt(document.getElementById('dashboardMonth').value);
    const year = parseInt(document.getElementById('dashboardYear').value);
    weekSelect.innerHTML = '';

    const firstOfMonth = new Date(year, month, 1);
    const lastOfMonth = new Date(year, month + 1, 0);

    let weekStart = new Date(firstOfMonth);
    weekStart.setDate(weekStart.getDate() - weekStart.getDay()); // Start from Sunday of the first week

    let weekCounter = 1;
    while (weekStart <= lastOfMonth) {
        const weekEnd = new Date(weekStart);
        weekEnd.setDate(weekEnd.getDate() + 6);

        const option = document.createElement('option');
        option.value = `${weekStart.toISOString().split('T')[0]}|${weekEnd.toISOString().split('T')[0]}`;
        option.textContent = `Semana ${weekCounter}: ${weekStart.toLocaleDateString('es-ES')} - ${weekEnd.toLocaleDateString('es-ES')}`;
        weekSelect.appendChild(option);

        weekStart.setDate(weekStart.getDate() + 7);
        weekCounter++;
    }
}

function initCharts() {
    const chartOptions = {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
            legend: {
                position: 'top',
            },
        }
    };

    state.charts.maintenance = new Chart(document.getElementById('maintenanceChart').getContext('2d'), {
        type: 'doughnut',
        data: {
            labels: ['Preventivo', 'Correctivo'],
            datasets: [{
                label: 'Tipos de Mantenimiento',
                data: [0, 0],
                backgroundColor: ['#3498db', '#e74c3c'],
            }]
        },
        options: { ...chartOptions, plugins: { ...chartOptions.plugins, title: { display: true, text: 'Tipos de Mantenimiento' }}}
    });

    state.charts.machineCriticidad = new Chart(document.getElementById('machineCriticidadChart').getContext('2d'), {
        type: 'doughnut',
        data: {
            labels: ['Baja', 'Media', 'Alta'],
            datasets: [{
                data: [0, 0, 0],
                backgroundColor: ['#2ecc71', '#f39c12', '#e74c3c'],
                borderWidth: 1
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '75%',
            plugins: {
                legend: {
                    display: false
                }
            }
        }
    });
    
    state.charts.failureType = new Chart(document.getElementById('failureTypeChart').getContext('2d'), {
        type: 'bar',
        data: {
            labels: ['Mecánica', 'Eléctrica', 'Electrónica', 'Operación'],
            datasets: [{
                label: 'Tipos de Falla',
                data: [0, 0, 0, 0],
                backgroundColor: '#8e44ad',
            }]
        },
        options: { ...chartOptions, indexAxis: 'y', scales: { y: { beginAtZero: true }}, plugins: { ...chartOptions.plugins, title: { display: true, text: 'Análisis de Fallas Correctivas' }}}
    });

    state.charts.taskStatus = new Chart(document.getElementById('taskStatusChart').getContext('2d'), {
        type: 'pie',
        data: {
            labels: ['Pendiente', 'En Proceso', 'Pausado', 'Pendiente de Evaluación', 'Completado', 'Cancelado'],
            datasets: [{
                label: 'Estado de Tareas',
                data: [0, 0, 0, 0, 0, 0],
                backgroundColor: ['#f39c12', '#3498db', '#6c757d', '#17a2b8', '#28a745', '#dc3545'],
            }]
        },
        options: { ...chartOptions, plugins: { ...chartOptions.plugins, title: { display: true, text: 'Estado de Órdenes de Trabajo' }}}
    });

    state.charts.correctiveTrends = new Chart(document.getElementById('correctiveTrendsChart').getContext('2d'), {
        type: 'line',
        data: {
            labels: [],
            datasets: [
                {
                    label: 'Preventivos',
                    data: [],
                    borderColor: '#3498db',
                    backgroundColor: 'rgba(52, 152, 219, 0.1)',
                    fill: true,
                    tension: 0.3
                },
                {
                    label: 'Correctivos',
                    data: [],
                    borderColor: '#e74c3c',
                    backgroundColor: 'rgba(231, 76, 60, 0.1)',
                    fill: true,
                    tension: 0.3
                }
            ]
        },
        options: { ...chartOptions, plugins: { ...chartOptions.plugins, title: { display: true, text: 'Tendencia Mensual (Últimos 12 Meses)' }}}
    });
}

// --- HTML creation helpers for table rows ---
function createPartRowHTML(part, status) {
    let actionsHTML = '';
    const userRole = getCurrentUser().role;

    const inCart = state.purchaseCart.find(i => i.partId === part.id);
    const cartActionHTML = inCart ? `<button class="btn btn-success" disabled title="Ya en carrito">
            <i class="fas fa-shopping-cart"></i>
        </button>` :
        `<button class="btn btn-outline-primary add-to-purchase-cart" data-id="${part.id}" title="Agregar al carrito">
            <i class="fas fa-cart-plus"></i>
        </button>`;

    if(userRole === 'Admin' || userRole === 'Planificador' || userRole === 'Almacén') {
        actionsHTML = `
            <div class="btn-group btn-group-sm">
                ${cartActionHTML}
                <button class="btn btn-outline-info view-part-detail" data-id="${part.id}" title="Ver Detalle">
                    <i class="fas fa-eye"></i>
                </button>
                <button class="btn btn-outline-secondary view-part-history" data-id="${part.id}" title="Ver Historial">
                    <i class="fas fa-history"></i>
                </button>
                <button class="btn btn-outline-primary edit-part" data-id="${part.id}" title="Editar">
                    <i class="fas fa-edit"></i>
                </button>
                <button class="btn btn-outline-danger delete-part" data-id="${part.id}" title="Eliminar">
                    <i class="fas fa-trash"></i>
                </button>
            </div>
        `;
    } else {
        actionsHTML = `
            <div class="btn-group btn-group-sm">
                ${cartActionHTML}
                <button class="btn btn-outline-info view-part-detail" data-id="${part.id}" title="Ver Detalle">
                    <i class="fas fa-eye"></i>
                </button>
                <button class="btn btn-outline-secondary view-part-history" data-id="${part.id}" title="Ver Historial">
                    <i class="fas fa-history"></i>
                </button>
            </div>
            <span class="text-muted fst-italic ms-1">Solo lectura</span>
        `;
    }

    const statusIcons = {
        critical: '<i class="fas fa-times-circle text-danger" title="Sin existencia"></i>',
        attention: '<i class="fas fa-exclamation-triangle text-warning" title="Mínimo"></i>',
        operative: '<i class="fas fa-check-circle text-success" title="Operativo"></i>'
    };

    const statusDotClass = `bg-${status}`;
    const classInfo = getPartClassificationInfo(part.classification);
    const classificationBadge = classInfo.class;
    const classificationText = classInfo.label;

    // Lógica para máquinas vinculadas
    let machineNames = 'N/A';
    const machineIds = part.machineIds || (part.machineId ? [part.machineId] : []);
    if (machineIds.length > 0) {
        if (machineIds.length === 1) {
            const machine = state.machines.find(m => m.id === machineIds[0]);
            machineNames = machine ? machine.name : machineIds[0];
        } else {
            machineNames = `<span class="badge bg-secondary">${machineIds.length} Equipos</span>`;
        }
    }

    return `
        <td class="text-center">
            <div class="d-flex flex-column align-items-center gap-1">
                ${statusIcons[status]}
            </div>
        </td>
        <td><span class="part-id-badge">${part.id || 'N/A'}</span></td>
        <td>
            <div class="fw-bold text-wrap" style="max-width: 250px;">${part.description}</div>
            <div class="d-flex gap-2 mt-1">
                <span class="badge ${classificationBadge} x-small" style="font-size: 0.65rem;">${classificationText}</span>
            </div>
        </td>
        <td>
            <div class="small text-muted"><i class="fas fa-truck me-1"></i>${state.proveedores.find(s => s.id === part.supplierId).nombre || 'N/A'}</div>
        </td>
        <td>
            <div class="small text-muted"><i class="fas fa-cogs me-1"></i>${machineNames}</div>
        </td>
        <td>
            <div class="small text-muted"><i class="fas fa-map-marker-alt me-1"></i>${part.location || 'N/A'}</div>
        </td>
        <td class="text-center">
            <span class="badge ${status === 'critical' ? 'bg-danger' : (status === 'attention' ? 'bg-warning text-dark' : 'bg-success')} rounded-pill stock-badge-large">
                ${part.stockActual}
            </span>
        </td>
        <td class="text-center">
            <div class="small text-muted mb-1">Mínimo</div>
            <span class="fw-bold">${part.minStock}</span>
        </td>
        <td class="text-center">
            ${actionsHTML}
        </td>
    `;
}

function createProveedorRowHTML(proveedor) {
    let actionsHTML = 'Sin permisos';
    const userRole = getCurrentUser().role;
    if(userRole === 'Admin' || userRole === 'Planificador') {
        actionsHTML = `
            <button class="btn btn-sm btn-outline-primary edit-proveedor" data-id="${proveedor.id}"><i class="fas fa-edit"></i></button> 
            <button class="btn btn-sm btn-outline-danger delete-proveedor" data-id="${proveedor.id}"><i class="fas fa-trash"></i></button>
        `;
    } else {
        actionsHTML = `<span class="text-muted fst-italic">Solo lectura</span>`;
    }
    return `
        <td>${proveedor.id}</td><td>${proveedor.nombre}</td><td>${proveedor.rtn || 'N/A'}</td>
        <td>${proveedor.telefono || 'N/A'}</td><td>${proveedor.direccion || 'N/A'}</td>
        <td>${proveedor.email || 'N/A'}</td><td class="text-center">${actionsHTML}</td>
    `;
}

// --- CRUD Machine Functions ---
function renderMachines() {
    if (state.currentTab !== 'maquinaria' && state.currentTab !== 'dashboard' && initialLoadComplete) return;
    requestAnimationFrame(() => {
    const container = document.getElementById('machine-distribution-container');
    if (!container) return;
    const searchTerm = document.getElementById('search-machine-input').value.toLowerCase();

    // Optimization: Group data once to avoid repeated filtering inside loops
    const ordersByMachine = new Map();
    state.workOrders.forEach(wo => {
        if (!wo.machineId) return;
        if (!ordersByMachine.has(wo.machineId)) ordersByMachine.set(wo.machineId, []);
        ordersByMachine.get(wo.machineId).push(wo);
    });

    const plansByMachine = new Map();
    state.workPlans.forEach(p => {
        if (!p.machineId) return;
        if (!plansByMachine.has(p.machineId)) plansByMachine.set(p.machineId, []);
        plansByMachine.get(p.machineId).push(p);
    });

    // 1. Filter machines based on permissions and search term
    const userRole = getCurrentUser().role;
    const managedMachineIds = getCurrentUser().managedMachineIds;
    const equipoAsignado = getCurrentUser().equipoAsignado;
    const hasPermissionFilter = (userRole === 'Jefe de Area') || (['Técnico', 'Operario', 'Supervisor de Area'].includes(userRole));
    let allowedMachineIds = null;
    if (hasPermissionFilter) {
        const ids = userRole === 'Jefe de Area' ? (managedMachineIds || []) : (equipoAsignado || []);
        allowedMachineIds = new Set(Array.isArray(ids) ? ids : []);
    }

    const filteredMachines = state.machines.filter(m => {
        if (m.status === 'DADO_DE_BAJA') return false;
        if (allowedMachineIds && !allowedMachineIds.has(m.id)) return false;
        if (state.assetCategoryFilter && state.assetCategoryFilter !== 'all' && getAssetCategory(m) !== state.assetCategoryFilter) return false;
        if (!searchTerm) return true;
        return m.id.toLowerCase().includes(searchTerm) ||
               m.name.toLowerCase().includes(searchTerm) ||
               (m.location && m.location.toLowerCase().includes(searchTerm)) ||
               (getMachineArea(m) && getMachineArea(m).toLowerCase().includes(searchTerm)) ||
               getAssetCategoryInfo(m).label.toLowerCase().includes(searchTerm) ||
               getAssetTypeLabel(m.type).toLowerCase().includes(searchTerm);
    });

    // 2. Pre-calculate machine statuses and stats
    const machineStatusMap = new Map();
    const machineWorkStatsMap = new Map();
    const machineNextMaintMap = new Map();

    let maintAltaCount = 0;
    let maintMediaCount = 0;
    let maintBajaCount = 0;

    filteredMachines.forEach(m => {
        // Status Logic
        const mOrders = ordersByMachine.get(m.id) || [];
        const activeOrders = mOrders.filter(wo => ['En Proceso', 'Pausado'].includes(wo.status));

        // El equipo está PARADO si:
        // 1. Tiene alguna orden de correctivo en proceso
        // 2. Tiene alguna orden pausada donde el usuario indicó que el equipo queda PARADO
        const hasCorrectiveInProgress = activeOrders.some(wo => {
            const fbId = wo?.fb_id || wo?.id;
            if (!fbId) return false;
            const cleanId = String(fbId).replace(/[.#$\[\]]/g, '_');
            const mirror = state.liveWorkOrders?.[cleanId];
            const currentStatus = mirror?.status || wo.status;
            const currentType = mirror?.type || wo.type;
            return currentStatus === 'En Proceso' && ['Correctivo', 'Emergencia'].includes(currentType);
        });

        const hasPauseParado = activeOrders.some(wo => {
            const fbId = wo?.fb_id || wo?.id;
            if (!fbId) return false;
            const cleanId = String(fbId).replace(/[.#$\[\]]/g, '_');
            const mirror = state.liveWorkOrders?.[cleanId];
            const currentStatus = mirror?.status || wo.status;
            return currentStatus === 'Pausado' && wo.machineStatusOnPause === 'parado';
        });

        // El equipo está EN MANTENIMIENTO si:
        // 1. No está parado pero tiene alguna orden preventiva/predictiva/etc en proceso
        const hasPreventiveInProgress = activeOrders.some(wo => {
            const fbId = wo?.fb_id || wo?.id;
            if (!fbId) return false;
            const cleanId = String(fbId).replace(/[.#$\[\]]/g, '_');
            const mirror = state.liveWorkOrders?.[cleanId];
            const currentStatus = mirror?.status || wo.status;
            const currentType = mirror?.type || wo.type;
            return currentStatus === 'En Proceso' && ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(currentType);
        });

        let status = (hasCorrectiveInProgress || hasPauseParado) ? 'parada' : (hasPreventiveInProgress ? 'mantenimiento' : 'operando');

        // Override with RTDB Live Status if available
        const liveData = state.rtdbMonitoring && state.rtdbMonitoring[m.id];
        if (liveData && liveData.liveStatus) {
            status = liveData.liveStatus; // e.g., 'parada', 'mantenimiento', 'operando', 'offline'
        }

        machineStatusMap.set(m.id, status);

        if (status !== 'operando') {
            if (m.criticidad === 'Alta') maintAltaCount++;
            else if (m.criticidad === 'Media') maintMediaCount++;
            else if (m.criticidad === 'Baja') maintBajaCount++;
        }

        // Work Stats Logic
        const validOrders = mOrders.filter(wo => !['Cancelado', 'Rechazado'].includes(wo.status));
        let corr = 0, prev = 0;
        validOrders.forEach(wo => {
            if (['Correctivo', 'Emergencia'].includes(wo.type)) corr++;
            else if (['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(wo.type)) prev++;
        });
        machineWorkStatsMap.set(m.id, { correctivos: corr, preventivos: prev });

        // Next Maint Logic
        const mPlans = plansByMachine.get(m.id) || [];
        let nextMaint = 'N/A';
        if (mPlans.length > 0) {
            const dates = mPlans.map(p => new Date(p.nextDueDate + 'T12:00:00').getTime()).filter(t => !isNaN(t));
            if (dates.length > 0) {
                nextMaint = new Date(Math.min(...dates)).toLocaleDateString('es-ES');
            }
        }
        machineNextMaintMap.set(m.id, nextMaint);
    });

    const countAltaEl = document.getElementById('count-maint-alta');
    const countMediaEl = document.getElementById('count-maint-media');
    const countBajaEl = document.getElementById('count-maint-baja');

    if (countAltaEl) countAltaEl.textContent = maintAltaCount;
    if (countMediaEl) countMediaEl.textContent = maintMediaCount;
    if (countBajaEl) countBajaEl.textContent = maintBajaCount;

    // Actualizar resaltado de filtros activos
    document.querySelectorAll('.machine-summary-header .summary-pill').forEach(pill => {
        const label = pill.querySelector('.label').textContent.trim();
        pill.classList.toggle('active-filter', state.machineMaintFilter === label);
    });

    document.querySelectorAll('.asset-category-filters [data-category]').forEach(btn => {
        btn.classList.toggle('active', (btn.dataset.category || 'all') === (state.assetCategoryFilter || 'all'));
    });

    // Aplicar filtro de mantenimiento si existe
    let machinesToShow = filteredMachines;
    if (state.machineMaintFilter) {
        machinesToShow = filteredMachines.filter(m => {
            return machineStatusMap.get(m.id) !== 'operando' && m.criticidad === state.machineMaintFilter;
        });
    }

    if (machinesToShow.length === 0) {
        container.innerHTML = '<div class="alert alert-light text-center">No se encontraron máquinas con los criterios seleccionados.</div>';
        return;
    }

    // 4. Group machines by location
    const machinesByLocation = machinesToShow.reduce((acc, machine) => {
        const location = machine.location || 'Sin Ubicación';
        if (!acc[location]) acc[location] = [];
        acc[location].push(machine);
        return acc;
    }, {});

    const criticalityOrder = { 'Alta': 0, 'Media': 1, 'Baja': 2 };

    // 5. Render groups and cards
    let finalHTML = '';
    Object.entries(machinesByLocation).forEach(([location, machines], index) => {
        // Sort machines by criticality (Higher first)
        machines.sort((a, b) => (criticalityOrder[a.criticidad || 'Baja'] || 2) - (criticalityOrder[b.criticidad || 'Baja'] || 2));

        const groupAlta = machines.filter(m => m.criticidad === 'Alta').length;
        const groupMedia = machines.filter(m => m.criticidad === 'Media').length;
        const groupBaja = machines.filter(m => m.criticidad === 'Baja').length;

        const collapseId = `collapse-machine-loc-${index}`;

        let cardsHTML = machines.map(m => {
            const status = machineStatusMap.get(m.id);
            const statusLabels = { 'parada': 'Parada (Correctivo)', 'mantenimiento': 'En Mantenimiento', 'operando': 'Operando' };
            const nextMaint = machineNextMaintMap.get(m.id);
            const stats = machineWorkStatsMap.get(m.id);
            const critClass = (m.criticidad || 'Baja').toLowerCase();
            const typeText = getAssetTypeLabel(m.type);
            const categoryInfo = getAssetCategoryInfo(m);
            const parentAsset = m.parentAssetId ? getMachineById(m.parentAssetId) : null;

            let actionsHTML = '';
            const userRole = getCurrentUser().role;
            const canEdit = (userRole === 'Admin' || userRole === 'Planificador');
            actionsHTML = `
                <div class="dropdown card-dist-actions">
                    <button class="btn btn-link dropdown-toggle no-caret" type="button" data-bs-toggle="dropdown">
                        <i class="fas fa-ellipsis-v"></i>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item view-machine-detail" href="#" data-id="${m.id}"><i class="fas fa-eye me-2"></i>Ver Detalle</a></li>
                        ${canEdit ? `
                            <li><a class="dropdown-item edit-machine" href="#" data-id="${m.id}"><i class="fas fa-edit me-2"></i>Editar</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item delete-machine text-danger" href="#" data-id="${m.id}"><i class="fas fa-trash me-2"></i>Eliminar</a></li>
                        ` : ''}
                    </ul>
                </div>
            `;

            return `
                <div class="machine-card-dist border-${critClass}">
                    <div class="card-dist-header">
                        <span class="card-dist-id">${m.id}</span>
                        ${actionsHTML}
                    </div>
                    <h5 class="card-dist-title clickable-detail" data-id="${m.id}" style="cursor: pointer;">${m.name}</h5>
                    <div class="card-dist-type d-flex align-items-center gap-2 flex-wrap">
                        <span>${typeText}</span>
                        <span class="badge ${categoryInfo.class}"><i class="fas ${categoryInfo.icon} me-1"></i>${categoryInfo.label}</span>
                    </div>

                    <div class="card-dist-status">
                        <div class="status-indicator-dot ${status}"></div>
                        <span>${m.status === 'FUERA_DE_SERVICIO' ? 'FUERA DE SERVICIO' : statusLabels[status]}</span>
                    </div>

                    <div class="card-dist-info-row">
                        <span class="card-dist-info-label">Criticidad:</span>
                        <span class="card-dist-info-value">${m.criticidad || 'Baja'}</span>
                    </div>
                    ${getMachineArea(m) ? `
                    <div class="card-dist-info-row">
                        <span class="card-dist-info-label">Área:</span>
                        <span class="card-dist-info-value">${getMachineArea(m)}</span>
                    </div>
                    ` : ''}
                    ${parentAsset ? `
                    <div class="card-dist-info-row">
                        <span class="card-dist-info-label">Pertenece a:</span>
                        <span class="card-dist-info-value">${parentAsset.name}</span>
                    </div>
                    ` : ''}
                    <div class="card-dist-info-row">
                        <span class="card-dist-info-label">Próximo Maint.:</span>
                        <span class="card-dist-info-value">${nextMaint}</span>
                    </div>

                    <div class="card-dist-stats">
                        <div class="stat-pill correctivos show-history" data-machine-id="${m.id}" data-type="Correctivo" style="cursor: pointer;" title="Ver historial de correctivos">
                            <span class="stat-count">${stats.correctivos}</span>
                            <span>Correctivos</span>
                        </div>
                        <div class="stat-pill preventivos show-history" data-machine-id="${m.id}" data-type="Preventivo" style="cursor: pointer;" title="Ver historial de preventivos">
                            <span class="stat-count">${stats.preventivos}</span>
                            <span>Preventivos</span>
                        </div>
                    </div>
                </div>
            `;
        }).join('');

        finalHTML += `
            <div class="machine-zone mb-4">
            <div class="zone-header collapsed" data-bs-toggle="collapse" data-bs-target="#${collapseId}" aria-expanded="false" role="button">
                <div class="d-flex align-items-center gap-3">
                    <span><i class="fas fa-map-marker-alt me-2"></i>${location.toUpperCase()}</span>
                    <span class="badge bg-primary text-white rounded-pill shadow-sm">${machines.length}</span>
                </div>
                <div class="header-stats d-none d-md-flex gap-2 ms-4">
                    ${groupAlta > 0 ? `<span class="badge rounded-pill bg-danger" style="font-size: 0.7rem;">${groupAlta} Alta</span>` : ''}
                    ${groupMedia > 0 ? `<span class="badge rounded-pill bg-warning text-dark" style="font-size: 0.7rem;">${groupMedia} Media</span>` : ''}
                    ${groupBaja > 0 ? `<span class="badge rounded-pill bg-success" style="font-size: 0.7rem;">${groupBaja} Baja</span>` : ''}
                </div>
                <i class="fas fa-chevron-down transition-icon ms-auto"></i>
            </div>
            <div id="${collapseId}" class="collapse">
                <div class="border-start border-end border-bottom rounded-bottom shadow-sm" style="background-color: var(--card-bg);">
                    <div class="cards-grid">
                        ${cardsHTML}
                    </div>
                </div>
            </div>
        </div>`;
    });

    container.innerHTML = finalHTML;

    // 6. Re-attach listeners
    container.querySelectorAll('.view-machine-detail, .clickable-detail').forEach(btn => btn.addEventListener('click', (e) => {
        e.preventDefault();
        showMachineDetail(btn.dataset.id);
    }));
    container.querySelectorAll('.edit-machine').forEach(btn => btn.addEventListener('click', (e) => {
        e.preventDefault();
        showMachineModal(btn.dataset.id);
    }));
    container.querySelectorAll('.delete-machine').forEach(btn => btn.addEventListener('click', (e) => {
        e.preventDefault();
        deleteMachine(btn.dataset.id);
    }));

    container.querySelectorAll('.show-history').forEach(pill => pill.addEventListener('click', () => {
        showMachineHistory(pill.dataset.machineId, pill.dataset.type);
    }));
    });
}

function showMachineDetail(machineId) {
    const machine = state.machines.find(m => m.id === machineId);
    if (!machine) return;

    currentAsset = machine;

    const container = document.getElementById('machine-detail-view-container');
    const actionsTop = document.getElementById('machine-detail-actions-top');

    // Status Logic
    const activeOrders = state.workOrders.filter(wo => wo.machineId === machineId && ['En Proceso', 'Pausado'].includes(wo.status));
    const hasCorrective = activeOrders.some(wo => ['Correctivo', 'Emergencia'].includes(wo.type));
    const hasPreventive = activeOrders.some(wo => ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(wo.type));

    let statusLabel = 'Operativo / Calificado';
    let statusClass = 'success';
    let statusBg = '#10b981';

    if (machine.status === 'DADO_DE_BAJA') {
        statusLabel = 'DADO DE BAJA';
        statusClass = 'dark';
        statusBg = '#1e293b';
    } else if (hasCorrective) {
        statusLabel = 'Parado / Falla Crítica';
        statusClass = 'danger';
        statusBg = '#ef4444';
    } else if (hasPreventive) {
        statusLabel = 'En Mantenimiento';
        statusClass = 'warning';
        statusBg = '#f59e0b';
    } else if (machine.status === 'FUERA_DE_SERVICIO') {
        statusLabel = 'FUERA DE SERVICIO';
        statusClass = 'secondary';
        statusBg = '#6c757d';
    }

    // IQ/OQ/PQ Logic for styling
    const iqVal = machine.iqStatus ? 'Validado' : 'Pendiente';
    const oqVal = machine.oqStatus ? 'Validado' : 'Pendiente';
    const pqVal = machine.pqStatus ? 'Vigente' : 'Vencido/Pendiente';
    const categoryInfo = getAssetCategoryInfo(machine);
    const typeText = getAssetTypeLabel(machine.type);
    const parentAsset = machine.parentAssetId ? getMachineById(machine.parentAssetId) : null;
    const childAssets = state.machines
        .filter(m => m.parentAssetId === machine.id && m.status !== 'DADO_DE_BAJA')
        .sort((a, b) => (a.name || '').localeCompare(b.name || ''));

    // History and Parts
    const orders = state.workOrders
        .filter(wo => wo.machineId === machineId && ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(wo.type))
        .sort((a, b) => new Date((b.date || '1970-01-01') + 'T12:00:00Z') - new Date((a.date || '1970-01-01') + 'T12:00:00Z'))
        .slice(0, 5);

    const criticalityOrder = { 'Crítico': 4, 'Alto': 3, 'Medio': 2, 'Bajo': 1 };
    const linkedParts = state.parts
        .filter(p => p.machineIds && p.machineIds.includes(machineId))
        .sort((a, b) => {
            const critA = criticalityOrder[a.criticality || 'Bajo'] || 0;
            const critB = criticalityOrder[b.criticality || 'Bajo'] || 0;
            if (critA !== critB) return critB - critA;
            return (a.stockActual || 0) - (b.stockActual || 0);
        })
        .slice(0, 5);

    // Top Actions
    const isDecommissioned = machine.status === 'DADO_DE_BAJA';
    const canDecommission = ['Admin', 'Planificador'].includes(getCurrentUser().role);

    actionsTop.innerHTML = `
        ${!isDecommissioned ? `<button id="analyze-ai-btn" class="btn btn-sm btn-outline-primary me-2"><i class="fas fa-robot me-1"></i>Analizar con IA</button>` : ''}
        ${!isDecommissioned && canDecommission ? `<button class="btn btn-sm btn-outline-danger me-2" onclick="showDecommissionModal('${machine.fb_id}')"><i class="fas fa-archive me-1"></i>Dar de Baja</button>` : ''}
        <button class="btn btn-sm btn-outline-secondary me-2 edit-machine-from-detail" data-id="${machine.id}"><i class="fas fa-edit me-1"></i>Editar</button>
        <button class="btn btn-sm btn-outline-secondary me-2 print-machine-label"><i class="fas fa-print me-1"></i>Imprimir</button>
    `;

    // Main Content
    container.innerHTML = `
        <div class="px-4 py-4">
            <div class="mb-4 d-flex justify-content-between align-items-start flex-wrap gap-3">
                <div>
                    <div class="d-flex align-items-center gap-3 mb-1">
                        <h2 class="h3 fw-bold mb-0">${machine.name}</h2>
                        <span class="status-tag">
                            <span class="rounded-circle" style="width: 8px; height: 8px; background-color: ${statusBg};"></span>
                            ${statusLabel}
                        </span>
                    </div>
                    <p class="text-muted mb-0">ID del Activo: <span class="text-primary-custom fw-bold">${machine.id}</span></p>
                </div>
            </div>

            <div class="row g-4">
                <aside class="col-lg-4">
                    <section class="section-card overflow-hidden">
                        <div class="p-3 bg-light border-bottom">
                            <h6 class="fw-bold mb-0">Imagen del Equipo</h6>
                        </div>
                        <div class="p-4 text-center d-flex align-items-center justify-content-center" style="min-height: 200px;">
                            ${machine.imageUrl ? `<img src="${machine.imageUrl}" alt="${machine.name}" class="img-fluid rounded shadow-sm border" style="max-height: 250px; object-fit: contain;">` :
                                `<div class="text-muted"><i class="fas fa-image fa-4x mb-2 d-block"></i>Sin Imagen</div>`
                            }
                        </div>
                    </section>

                    <section class="section-card">
                        <div class="p-3 bg-light border-bottom">
                            <h6 class="fw-bold mb-0">Acceso Rápido</h6>
                        </div>
                        <div class="p-4 text-center">
                            <div id="machine-detail-qr" class="d-inline-block p-3 border rounded-3 mb-3 shadow-sm bg-white">
                                <!-- QR Code will be generated here -->
                                <i class="fas fa-qrcode fa-5x text-muted opacity-25"></i>
                            </div>
                            <p class="small text-muted text-uppercase fw-bold mb-0" style="font-size: 0.65rem; letter-spacing: 1px;">Escanee para historial rápido</p>
                        </div>
                    </section>
                </aside>

                <div class="col-lg-8">
                    <section class="section-card p-4">
                        <h5 class="fw-bold mb-4 text-primary-custom d-flex align-items-center gap-2">
                            <i class="fas fa-file-contract"></i> Clasificación Técnica y Regulatoria del Activo
                        </h5>
                        <div class="row g-4">
                            <div class="col-md-4">
                                <p class="label-text">Categoria Operativa</p>
                                <p class="value-text"><span class="badge ${categoryInfo.class}"><i class="fas ${categoryInfo.icon} me-1"></i>${categoryInfo.label}</span></p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Tipo de Activo</p>
                                <p class="value-text">${typeText}</p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Activo Padre</p>
                                <p class="value-text">
                                    ${parentAsset ? `<a href="#" class="text-primary fw-bold parent-asset-link" data-id="${parentAsset.id}">${parentAsset.name}</a>` : 'N/A'}
                                </p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Marca</p>
                                <p class="value-text">${machine.brand || 'N/A'}</p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Modelo</p>
                                <p class="value-text">${machine.model || 'N/A'}</p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Número de Serie</p>
                                <p class="value-text font-mono">${machine.serialNumber || 'N/A'}</p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Impacto en Calidad del Producto</p>
                                <p class="value-text">${['Directo', 'Indirecto'].includes(machine.riskClassification) ? machine.riskClassification : 'N/A'}</p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Ubicación</p>
                                <p class="value-text">${machine.location || 'N/A'}</p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Área</p>
                                <p class="value-text">${getMachineArea(machine) || 'N/A'}</p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Fecha de Compra</p>
                                <p class="value-text">${machine.purchaseDate || 'N/A'}</p>
                            </div>
                            <div class="col-md-4">
                                <p class="label-text">Puesta en Marcha</p>
                                <p class="value-text">${machine.commissioningDate || 'N/A'}</p>
                            </div>
                        </div>
                    </section>

                    <section class="section-card p-4">
                        <h5 class="fw-bold mb-4 text-primary-custom d-flex align-items-center gap-2">
                            <i class="fas fa-certificate"></i> Estado de Calificación (IQ/OQ/PQ)
                        </h5>
                        <div class="row g-3">
                            <div class="col-md-4">
                                <div class="p-3 border rounded ${machine.iqStatus ? 'bg-success bg-opacity-10 border-success' : 'bg-light'}">
                                    <p class="small fw-bold text-muted text-uppercase mb-1" style="font-size: 0.65rem;">IQ - Instalación</p>
                                    <p class="fw-bold mb-0 ${machine.iqStatus ? 'text-success' : 'text-secondary'}">${iqVal}</p>
                                    <p class="small text-muted mb-0" style="font-size: 0.7rem;">Fecha: ${machine.iqDate || 'N/A'}</p>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="p-3 border rounded ${machine.oqStatus ? 'bg-success bg-opacity-10 border-success' : 'bg-light'}">
                                    <p class="small fw-bold text-muted text-uppercase mb-1" style="font-size: 0.65rem;">OQ - Operación</p>
                                    <p class="fw-bold mb-0 ${machine.oqStatus ? 'text-success' : 'text-secondary'}">${oqVal}</p>
                                    <p class="small text-muted mb-0" style="font-size: 0.7rem;">Fecha: ${machine.oqDate || 'N/A'}</p>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="p-3 border rounded ${machine.pqStatus ? 'bg-primary bg-opacity-10 border-primary' : 'bg-light'}">
                                    <p class="small fw-bold text-muted text-uppercase mb-1" style="font-size: 0.65rem;">PQ - Desempeño</p>
                                    <p class="fw-bold mb-0 ${machine.pqStatus ? 'text-primary' : 'text-secondary'}">${pqVal}</p>
                                    <p class="small text-muted mb-0" style="font-size: 0.7rem;">Vencimiento: ${machine.pqNextDate || 'N/A'}</p>
                                </div>
                            </div>
                        </div>
                    </section>

                    <section class="section-card overflow-hidden">
                        <div class="p-4">
                            <h5 class="fw-bold mb-4 text-primary-custom d-flex align-items-center gap-2">
                                <i class="fas fa-sitemap"></i> Equipos Vinculados
                            </h5>
                            <div class="table-responsive p-0 shadow-none border-0">
                                <table class="table table-hover mb-0">
                                    <thead class="bg-light">
                                        <tr>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Equipo</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Tipo</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Criticidad</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        ${childAssets.length > 0 ? childAssets.map(child => {
                                            const childCategory = getAssetCategoryInfo(child);
                                            return `
                                                <tr>
                                                    <td class="small">
                                                        <div class="fw-bold">${child.name}</div>
                                                        <div class="text-muted font-mono">${child.id}</div>
                                                    </td>
                                                    <td class="small">
                                                        <span>${getAssetTypeLabel(child.type)}</span>
                                                        <span class="badge ${childCategory.class} ms-1">${childCategory.label}</span>
                                                    </td>
                                                    <td class="small">${child.criticidad || 'Baja'}</td>
                                                    <td>
                                                        <button class="btn btn-sm btn-outline-primary view-child-asset" data-id="${child.id}">
                                                            <i class="fas fa-eye"></i>
                                                        </button>
                                                    </td>
                                                </tr>
                                            `;
                                        }).join('') : '<tr><td colspan="4" class="text-center text-muted py-3">No hay equipos vinculados</td></tr>'}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </section>

                    <section class="section-card overflow-hidden">
                        <div class="p-4">
                            <h5 class="fw-bold mb-4 text-primary-custom d-flex align-items-center gap-2">
                                <i class="fas fa-calendar-check"></i> Mantenimientos Programados
                            </h5>
                            <div class="table-responsive p-0 shadow-none border-0">
                                <table class="table table-hover mb-0">
                                    <thead class="bg-light">
                                        <tr>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">ID</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Fecha</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Tipo</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Técnico</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Estado</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        ${orders.length > 0 ? orders.map(o => {
                                            let statusClass = 'bg-light text-dark';
                                            if (['En Proceso', 'Pausado'].includes(o.status)) statusClass = 'bg-info text-dark';
                                            if (o.status === 'Completado') statusClass = 'bg-success';
                                            if (o.status === 'Cancelado') statusClass = 'bg-danger';
                                            if (o.status === 'Pendiente de Evaluación' || o.status === 'Pendiente de Aprobación') statusClass = 'bg-warning text-dark';

                                            return `
                                                <tr>
                                                    <td class="small fw-bold">${o.id}</td>
                                                    <td class="small">${o.date || 'N/A'}</td>
                                                    <td><span class="badge ${['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(o.type) ? 'bg-info' : 'bg-danger'}" style="font-size: 0.6rem;">${o.type}</span></td>
                                                    <td class="small">${o.leadTechnician || (o.technicians && o.technicians[0]) || 'N/A'}</td>
                                                    <td><span class="badge ${statusClass}" style="font-size: 0.6rem;">${o.status}</span></td>
                                                    <td>
                                                        <button class="btn btn-sm btn-outline-primary view-wo-from-detail-history" data-id="${o.fb_id || o.id}">
                                                            <i class="fas fa-eye"></i>
                                                        </button>
                                                    </td>
                                                </tr>
                                            `;
                                        }).join('') : '<tr><td colspan="6" class="text-center text-muted py-3">No hay registros</td></tr>'}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </section>

                    <section class="section-card overflow-hidden">
                        <div class="p-4">
                            <h5 class="fw-bold mb-4 text-primary-custom d-flex align-items-center gap-2">
                                <i class="fas fa-box-open"></i> Repuestos Vinculados
                            </h5>
                            <div class="table-responsive p-0 shadow-none border-0">
                                <table class="table table-hover mb-0">
                                    <thead class="bg-light">
                                        <tr>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Repuesto</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">ID</th>
                                            <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Stock</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        ${linkedParts.length > 0 ? linkedParts.map(p => `
                                            <tr>
                                                <td class="small">
                                                    <div class="d-flex align-items-center gap-2">
                                                        ${p.imageUrl ? `<img src="${p.imageUrl}" class="rounded border" style="width: 32px; height: 32px; object-fit: cover;">` : '<div class="rounded border bg-light d-flex align-items-center justify-content-center" style="width: 32px; height: 32px;"><i class="fas fa-box text-muted" style="font-size: 0.7rem;"></i></div>'}
                                                        <span class="fw-bold text-wrap" style="max-width: 200px;">${p.description}</span>
                                                    </div>
                                                </td>
                                                <td class="small font-mono">${p.id}</td>
                                                <td class="small fw-bold ${p.stockActual <= p.minStock ? 'text-danger' : ''}">${p.stockActual}</td>
                                            </tr>
                                        `).join('') : '<tr><td colspan="3" class="text-center text-muted py-3">No hay repuestos vinculados</td></tr>'}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </section>

                    <section class="section-card p-4">
                        <h5 class="fw-bold mb-4 text-primary-custom d-flex align-items-center gap-2">
                            <i class="fas fa-book"></i> Centro de Documentación
                        </h5>
                        <div class="row g-3">
                            <div class="col-md-6">
                                <div class="d-flex align-items-center p-2 border rounded ${machine.userManualUrl ? 'hover-bg-light cursor-pointer download-user-manual' : 'opacity-50'}">
                                    <div class="bg-danger bg-opacity-10 text-danger p-2 rounded me-3">
                                        <i class="fas fa-file-pdf"></i>
                                    </div>
                                    <div>
                                        <p class="small fw-bold mb-0">Manual de Usuario</p>
                                        <p class="text-muted mb-0" style="font-size: 0.7rem;">${machine.userManualUrl ? 'Manual del fabricante' : 'No disponible'}</p>
                                    </div>
                                    <i class="fas fa-download ms-auto text-muted"></i>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="d-flex align-items-center p-2 border rounded ${machine.techManualUrl ? 'hover-bg-light cursor-pointer download-tech-manual' : 'opacity-50'}">
                                    <div class="bg-primary bg-opacity-10 text-primary p-2 rounded me-3">
                                        <i class="fas fa-file-pdf"></i>
                                    </div>
                                    <div>
                                        <p class="small fw-bold mb-0">Manual Técnico</p>
                                        <p class="text-muted mb-0" style="font-size: 0.7rem;">${machine.techManualUrl ? 'Diagramas y esquemas' : 'No disponible'}</p>
                                    </div>
                                    <i class="fas fa-download ms-auto text-muted"></i>
                                </div>
                            </div>
                        </div>
                    </section>

                    <section class="section-card p-4">
                        <h5 class="fw-bold mb-4 text-primary-custom d-flex align-items-center gap-2">
                            <i class="fas fa-history"></i> Historial de Cambios de Estado
                        </h5>
                        <div class="table-responsive p-0 shadow-none border-0">
                            <table class="table table-sm table-hover mb-0">
                                <thead class="bg-light">
                                    <tr>
                                        <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Fecha</th>
                                        <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Estado</th>
                                        <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Motivo</th>
                                        <th class="small fw-bold text-muted text-uppercase" style="font-size: 0.65rem;">Por</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    ${(() => {
                                        const history = state.equipmentStatusHistory
                                            .filter(h => h.equipment_fb_id === machine.fb_id || h.equipment_id === machine.id)
                                            .sort((a, b) => new Date(b.change_date) - new Date(a.change_date));

                                        return history.length > 0 ? history.map(h => `
                                            <tr>
                                                <td class="small">${new Date(h.change_date).toLocaleDateString()}</td>
                                                <td><span class="badge bg-info bg-opacity-10 text-info" style="font-size: 0.6rem;">${h.new_status}</span></td>
                                                <td class="small">${h.reason || '-'}</td>
                                                <td class="small">${h.changed_by}</td>
                                            </tr>
                                        `).join('') : '<tr><td colspan="4" class="text-center text-muted py-3">No hay cambios registrados</td></tr>';
                                    })()}
                                </tbody>
                            </table>
                        </div>
                    </section>
                </div>
            </div>
        </div>

    `;

    // Re-attach listeners for buttons inside the modal
    document.getElementById('analyze-ai-btn')?.addEventListener('click', () => {
        console.log("Click en botón IA");
        // Using local machine object from showMachineDetail scope
        if (!machine) {
            console.error("No hay activo seleccionado");
            return;
        }
        analyzeAssetWithAI(machine);
    });

    actionsTop.querySelector('.edit-machine-from-detail')?.addEventListener('click', () => {
        state.modals.machineDetail.hide();
        showMachineModal(machine.id);
    });

    container.querySelector('.download-user-manual')?.addEventListener('click', () => {
        if (machine.userManualUrl) window.open(machine.userManualUrl, '_blank');
    });

    container.querySelector('.download-tech-manual')?.addEventListener('click', () => {
        if (machine.techManualUrl) window.open(machine.techManualUrl, '_blank');
    });

    container.querySelectorAll('.view-wo-from-detail-history').forEach(btn => {
        btn.addEventListener('click', () => {
            state.modals.machineDetail.hide();
            showWorkOrderModal(btn.dataset.id);
        });
    });

    container.querySelector('.parent-asset-link')?.addEventListener('click', e => {
        e.preventDefault();
        showMachineDetail(e.currentTarget.dataset.id);
    });

    container.querySelectorAll('.view-child-asset').forEach(btn => {
        btn.addEventListener('click', () => {
            showMachineDetail(btn.dataset.id);
        });
    });

    // Add listeners for Print button
    actionsTop.querySelector('.print-machine-label')?.addEventListener('click', () => {
        showLoading(true);
        generateFichaMaestra(machine.id);
    });

    state.modals.machineDetail.show();

    // Generate QR Code
    const qrContainer = document.getElementById('machine-detail-qr');
    if (qrContainer) {
        qrContainer.innerHTML = ''; // Clear icon
        const url = new URL(window.location.href);
        url.searchParams.set('machineId', machineId);

        new QRCode(qrContainer, {
            text: url.toString(),
            width: 128,
            height: 128,
            colorDark: "#000000",
            colorLight: "#ffffff",
            correctLevel: QRCode.CorrectLevel.H
        });
    }
}

async function showPartDetail(partId) {
    const part = state.parts.find(p => p.id === partId);
    if (!part) {
        showToast('Repuesto no encontrado', 'error');
        return;
    }

    const contentContainer = document.getElementById('part-detail-content');
    contentContainer.innerHTML = `
        <div class="p-5 text-center">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Cargando...</span>
            </div>
            <p class="mt-2">Cargando detalles...</p>
        </div>
    `;

    state.modals.partDetail.show();

    // Fetch last 5 movements
    let movements = [];
    try {
        // REMOVED orderBy to avoid requiring composite index in Firebase
        const q = query(state.collections.partMovements, where('partId', '==', partId), limit(50));
        const querySnapshot = await getDocs(q);
        querySnapshot.forEach(docSnap => {
            movements.push({ fb_id: docSnap.id, ...docSnap.data() });
        });
        // Sort in memory instead
        movements.sort((a, b) => new Date(b.date) - new Date(a.date));
        movements = movements.slice(0, 5);
    } catch (error) {
        console.error("Error fetching movements for detail:", error);
    }

    renderPartDetailHTML(part, movements);
}

function renderPartDetailHTML(part, movements) {
    const contentContainer = document.getElementById('part-detail-content');

    const classInfo = getPartClassificationInfo(part.classification);

    // Movements table rows
    let movementsHTML = '';
    if (movements.length === 0) {
        movementsHTML = '<tr><td colspan="4" class="text-center text-muted py-4">No hay movimientos registrados</td></tr>';
    } else {
        movements.forEach(m => {
            const qtyClass = m.quantity > 0 ? 'text-success' : 'text-danger';
            const qtyPrefix = m.quantity > 0 ? '+' : '';
            const actionDotColor = m.quantity > 0 ? '#10b981' : '#ef4444';

            movementsHTML += `
                <tr class="hover-bg-light transition-colors">
                    <td class="px-3 py-3 text-muted small">${new Date(m.date).toLocaleDateString('es-ES', { day: '2-digit', month: 'short', year: 'numeric' })}</td>
                    <td class="px-3 py-3">
                        <div class="d-flex align-items-center gap-2">
                            <span class="rounded-circle" style="width: 8px; height: 8px; background-color: ${actionDotColor};"></span>
                            <span class="small fw-medium">${m.type}</span>
                        </div>
                    </td>
                    <td class="px-3 py-3 fw-bold ${qtyClass}">${qtyPrefix}${m.quantity}</td>
                    <td class="px-3 py-3">
                        <span class="badge bg-light text-dark border font-monospace small" style="font-size: 0.75rem;">${m.orderHumanId || m.details.details || '-'}</span>
                    </td>
                </tr>
            `;
        });
    }

    const supplierName = state.proveedores.find(s => s.id === part.supplierId).nombre || 'N/A';

    let docsHTML = '';
    if (part.technicalDocs && part.technicalDocs.length > 0) {
        docsHTML = part.technicalDocs.map(doc => `
            <div class="col-md-6">
                <div class="tech-doc-item p-3 rounded-3 border bg-white d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center gap-3">
                        <i class="fas fa-file-pdf text-danger fa-2x"></i>
                        <div>
                            <p class="fw-bold mb-0 small text-truncate" style="max-width: 200px;">${doc.name}</p>
                            <span class="text-muted x-small">Ficha Técnica</span>
                        </div>
                    </div>
                    <a href="${doc.url}" target="_blank" class="btn btn-sm btn-outline-primary rounded-pill px-3">
                        Ver Documento
                    </a>
                </div>
            </div>
        `).join('');
    } else {
        docsHTML = '<div class="col-12"><p class="text-center text-muted py-3 small bg-white rounded-3 border">No hay fichas técnicas adjuntas</p></div>';
    }

    contentContainer.innerHTML = `
        <!-- Header -->
        <div class="border-bottom px-4 py-3 d-flex align-items-center justify-content-between" style="z-index: 1020; background-color: var(--modal-bg);">
            <div class="d-flex align-items-center gap-3">
                <button class="btn btn-link p-0 d-flex align-items-center justify-content-center" data-bs-dismiss="modal" style="width: 32px; height: 32px; border-radius: 50%; color: inherit;">
                    <i class="fas fa-arrow-left"></i>
                </button>
                <h5 class="fw-bold mb-0">Ficha de Repuesto</h5>
            </div>
            <button class="btn btn-link text-primary-custom fw-bold text-decoration-none" onclick="state.modals.partDetail.hide(); showPartModal('${part.id}')">
                Editar
            </button>
        </div>

        <div class="p-4" style="background-color: var(--content-bg);">
            <!-- Top Section: Image & Basic Info -->
            <section class="row g-4 mb-4 bg-white p-4 rounded-3 border mx-0 shadow-sm">
                <div class="col-md-4">
                    <div class="ratio ratio-1x1 rounded-3 overflow-hidden bg-light border d-flex align-items-center justify-content-center shadow-sm">
                        ${part.imageUrl ? `<img src="${part.imageUrl}" alt="${part.description}" class="img-fluid object-fit-cover">` :
                            `<div class="text-muted text-center"><i class="fas fa-box-open fa-3x mb-2 d-block"></i>Sin Imagen</div>`
                        }
                    </div>
                </div>
                <div class="col-md-8 d-flex flex-column justify-content-between">
                    <div>
                        <div class="d-flex align-items-center gap-2 mb-2">
                            <span class="badge ${classInfo.class} text-uppercase px-2 py-1" style="font-size: 0.7rem; letter-spacing: 0.05em;">${classInfo.label}</span>
                            <span class="text-muted small">ID: ${part.id}</span>
                        </div>
                        <h2 class="h3 fw-bold mb-2">${part.description}</h2>
                        <p class="text-muted small mb-4">${part.location ? `Ubicado en ${part.location}. ` : ''}Clasificado como ${classInfo.label}.</p>

                        <div class="d-flex gap-3 mb-4">
                            <div class="p-2 border rounded bg-light text-center" style="min-width: 80px;">
                                <small class="text-muted d-block text-uppercase fw-bold" style="font-size: 0.6rem;">Stock Mín</small>
                                <span class="fw-bold">${part.minStock || 0}</span>
                            </div>
                            <div class="p-2 border rounded bg-light text-center" style="min-width: 80px;">
                                <small class="text-muted d-block text-uppercase fw-bold" style="font-size: 0.6rem;">Stock Máx</small>
                                <span class="fw-bold">${part.maxStock || 0}</span>
                            </div>
                            <div class="p-2 border rounded bg-light text-center" style="min-width: 80px;">
                                <small class="text-muted d-block text-uppercase fw-bold" style="font-size: 0.6rem;">Pto Reorden</small>
                                <span class="fw-bold text-primary">${part.reorderPoint || 0}</span>
                            </div>
                        </div>
                    </div>

                    <!-- Stock Badge -->
                    <div class="d-inline-flex align-items-center gap-3 p-3 rounded-3 border" style="background-color: rgba(236, 91, 19, 0.05); border-color: rgba(236, 91, 19, 0.2) !important; width: fit-content;">
                        <div class="rounded-circle d-flex align-items-center justify-content-center text-white shadow-sm" style="width: 48px; height: 48px; background-color: #ec5b13;">
                            <i class="fas fa-boxes-stacked"></i>
                        </div>
                        <div>
                            <p class="text-uppercase fw-bold text-primary-custom mb-0" style="font-size: 0.65rem; letter-spacing: 0.1em;">Stock Disponible</p>
                            <p class="h2 fw-bold mb-0">${part.stockActual} <span class="h5 fw-medium opacity-50 mb-0">${part.uom || 'unidades'}</span></p>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Info Grid -->
            <section class="row g-3 mb-4">
                <div class="col-sm-3">
                    <div class="bg-white p-3 rounded-3 border d-flex gap-3 h-100 shadow-sm">
                        <i class="fas fa-store text-primary-custom mt-1"></i>
                        <div>
                            <p class="text-uppercase fw-bold text-muted mb-1" style="font-size: 0.65rem;">Proveedor</p>
                            <p class="fw-bold mb-0 small">${supplierName}</p>
                        </div>
                    </div>
                </div>
                <div class="col-sm-3">
                    <div class="bg-white p-3 rounded-3 border d-flex gap-3 h-100 shadow-sm">
                        <i class="fas fa-location-dot text-primary-custom mt-1"></i>
                        <div>
                            <p class="text-uppercase fw-bold text-muted mb-1" style="font-size: 0.65rem;">Ubicación</p>
                            <p class="fw-bold mb-0 small">${part.location || 'N/A'}</p>
                        </div>
                    </div>
                </div>
                <div class="col-sm-3">
                    <div class="bg-white p-3 rounded-3 border d-flex gap-3 h-100 shadow-sm">
                        <i class="fas fa-tag text-primary-custom mt-1"></i>
                        <div>
                            <p class="text-uppercase fw-bold text-muted mb-1" style="font-size: 0.65rem;">Categoría ABC</p>
                            <p class="fw-bold mb-0 small">Clase ${part.abc || 'C'}</p>
                        </div>
                    </div>
                </div>
                <div class="col-sm-3">
                    <div class="bg-white p-3 rounded-3 border d-flex gap-3 h-100 shadow-sm">
                        <i class="fas fa-ruler-combined text-primary-custom mt-1"></i>
                        <div>
                            <p class="text-uppercase fw-bold text-muted mb-1" style="font-size: 0.65rem;">U. Medida</p>
                            <p class="fw-bold mb-0 small">${part.uom || 'Unidades'}</p>
                        </div>
                    </div>
                </div>
            </section>

            <!-- History Table -->
            <section class="bg-white rounded-3 border shadow-sm overflow-hidden mb-4">
                <div class="p-3 border-bottom d-flex justify-content-between align-items-center bg-light">
                    <h6 class="fw-bold mb-0 d-flex align-items-center gap-2">
                        <i class="fas fa-history text-muted"></i>
                        Últimos 5 Movimientos
                    </h6>
                    <button class="btn btn-link btn-sm text-muted text-decoration-none fw-bold" onclick="state.modals.partDetail.hide(); showPartHistoryModal('${part.id}')">Ver todos</button>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0" style="font-size: 0.85rem;">
                        <thead class="bg-light text-muted text-uppercase" style="font-size: 0.65rem; letter-spacing: 0.05em;">
                            <tr>
                                <th class="px-3 py-2 border-0">Fecha</th>
                                <th class="px-3 py-2 border-0">Acción</th>
                                <th class="px-3 py-2 border-0">Cantidad</th>
                                <th class="px-3 py-2 border-0">Referencia / OT</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y">
                            ${movementsHTML}
                        </tbody>
                    </table>
                </div>
            </section>

            <!-- Technical Docs -->
            <section>
                <div class="d-flex align-items-center gap-2 mb-3">
                    <i class="fas fa-file-pdf text-primary-custom"></i>
                    <h6 class="fw-bold mb-0">Fichas Técnicas y Documentación</h6>
                </div>
                <div class="row g-3">
                    ${docsHTML}
                </div>
            </section>
        </div>
    `;
}

async function generateFichaMaestra(machineId) {
    const machine = state.machines.find(m => m.id === machineId);
    if (!machine) {
        showLoading(false);
        return;
    }

    // Create a hidden but layout-active iframe
    const iframe = document.createElement('iframe');
    iframe.style.position = 'fixed';
    iframe.style.top = '0';
    iframe.style.left = '0';
    iframe.style.width = '210mm';
    iframe.style.height = '297mm';
    iframe.style.opacity = '0';
    iframe.style.pointerEvents = 'none';
    iframe.style.zIndex = '-1';
    document.body.appendChild(iframe);

    const doc = iframe.contentWindow.document;
    doc.open();
    doc.write(FICHA_MAESTRA_TEMPLATE);
    doc.close();

    // Wait for Tailwind and QRCode to load and be ready
    await new Promise(resolve => {
        const timeout = setTimeout(resolve, 3000);
        const check = () => {
            if (iframe.contentWindow && iframe.contentWindow.tailwind && iframe.contentWindow.QRCode) {
                clearTimeout(timeout);
                resolve();
            } else {
                if (document.body.contains(iframe)) {
                    setTimeout(check, 100);
                } else {
                    resolve();
                }
            }
        };
        check();
    });

    // Re-acquire the iframe's document reference to ensure we have the fully loaded context
    const updatedDoc = iframe.contentWindow.document;

    // Populate the iframe data with safety checks
    const t = (id) => updatedDoc.getElementById(id);
    const setT = (id, prop, val) => {
        const el = t(id);
        if (el) el[prop] = val;
    };

    const historyBody = t('print-history-table-body');
    const partsBody = t('print-parts-table-body');
    const docsList = t('print-docs-list');

    // Basic Info
    setT('print-logo', 'src', GLOBAL_LOGO);
    setT('print-asset-id', 'textContent', machine.id);
    setT('print-ref-id', 'textContent', `ID: ${machine.id}`);

    const critEl = t('print-criticidad');
    if (critEl) {
        critEl.textContent = (machine.criticidad || 'BAJA').toUpperCase();
        const crit = (machine.criticidad || 'Baja').toLowerCase();
        const critBg = crit === 'alta' ? '#dc2626' : (crit === 'media' ? '#d97706' : '#16a34a');
        critEl.style.backgroundColor = critBg;
    }

    setT('print-location', 'textContent', machine.location || 'N/A');
    setT('print-area', 'textContent', getMachineArea(machine) || 'N/A');
    setT('print-brand', 'textContent', machine.brand || 'N/A');
    setT('print-model', 'textContent', machine.model || 'N/A');
    setT('print-serial', 'textContent', machine.serialNumber || 'N/A');
    setT('print-purchase-date', 'textContent', machine.purchaseDate || 'N/A');
    setT('print-commissioning-date', 'textContent', machine.commissioningDate || 'N/A');
    setT('print-risk', 'textContent', machine.riskClassification || 'N/A');
    setT('print-type', 'textContent', getAssetTypeLabel(machine.type));

    // Status Badge
    const activeOrders = state.workOrders.filter(wo => wo.machineId === machineId && ['En Proceso', 'Pausado'].includes(wo.status));
    const hasCorrective = activeOrders.some(wo => ['Correctivo', 'Emergencia', 'Calibración'].includes(wo.type));
    const hasPreventive = activeOrders.some(wo => ['Preventivo', 'Predictivo', 'Mecanizado'].includes(wo.type));

    let statusText = '● Operativo';
    let statusColorClass = 'bg-green-100 text-green-800';
    if (hasCorrective) {
        statusText = '● Parada Crítica';
        statusColorClass = 'bg-red-100 text-red-800';
    } else if (hasPreventive) {
        statusText = '● En Mantenimiento';
        statusColorClass = 'bg-yellow-100 text-yellow-800';
    }
    setT('print-status-badge', 'innerHTML', `<span class="inline-flex items-center px-2 py-0.5 rounded-full text-[10px] font-medium ${statusColorClass}">${statusText}</span>`);

    // Image
    setT('print-asset-image', 'src', machine.imageUrl || 'https://via.placeholder.com/400x300text=Sin+Imagen');

    // Monthly KPIs Logic
    const now = new Date();
    const startOfMonth = new Date(now.getFullYear(), now.getMonth(), 1);
    const machineWorkOrders = state.workOrders.filter(o =>
        o.machineId === machineId &&
        new Date(o.date || o.createdAt) >= startOfMonth
    );
    const correctiveOrders = machineWorkOrders.filter(o => ['Correctivo', 'Emergencia'].includes(o.type));

    let mtbf = 'N/A';
    let calcStartDate = startOfMonth;
    if (machine.createdAt && new Date(machine.createdAt) > startOfMonth) {
        calcStartDate = new Date(machine.createdAt);
    }

    if (calcStartDate < now) {
        const totalTimeMs = now - calcStartDate;
        const mtbfMs = totalTimeMs / (correctiveOrders.length + 1);
        mtbf = `${(mtbfMs / (1000 * 60 * 60)).toFixed(0)} h`;
    }

    let availability = '100%';
    const completedCorrectives = correctiveOrders.filter(o => o.status === 'Completado');
    if (calcStartDate < now) {
        const scheduledUptimeMs = calculateScheduledUptimeMs([machine], calcStartDate, now);
        const totalDowntimeMs = completedCorrectives.reduce((sum, o) => sum + getTotalWorkDurationMs(o), 0);
        if (scheduledUptimeMs > 0) {
            const avail = ((scheduledUptimeMs - totalDowntimeMs) / scheduledUptimeMs) * 100;
            availability = `${Math.max(0, avail).toFixed(1)}%`;
        }
    }

    setT('print-total-mant', 'textContent', machineWorkOrders.length);
    setT('print-mtbf', 'textContent', mtbf);
    setT('print-availability', 'textContent', availability);

    // Qualifications
    const iqStatusEl = t('print-iq-status');
    if (iqStatusEl) {
        iqStatusEl.textContent = machine.iqStatus ? 'VALIDADO' : 'PENDIENTE';
        iqStatusEl.className = `font-semibold uppercase text-[9px] ${machine.iqStatus ? 'text-green-600' : 'text-slate-400'}`;
    }
    setT('print-iq-date', 'textContent', machine.iqDate || 'N/A');

    const oqStatusEl = t('print-oq-status');
    if (oqStatusEl) {
        oqStatusEl.textContent = machine.oqStatus ? 'VALIDADO' : 'PENDIENTE';
        oqStatusEl.className = `font-semibold uppercase text-[9px] ${machine.oqStatus ? 'text-green-600' : 'text-slate-400'}`;
    }
    setT('print-oq-date', 'textContent', machine.oqDate || 'N/A');

    const pqStatusEl = t('print-pq-status');
    if (pqStatusEl) {
        pqStatusEl.textContent = machine.pqStatus ? 'VIGENTE' : 'VENCIDO/PENDIENTE';
        pqStatusEl.className = `font-semibold uppercase text-[9px] ${machine.pqStatus ? 'text-blue-600' : 'text-red-600'}`;
    }
    setT('print-pq-date', 'textContent', machine.pqDate || 'N/A');
    setT('print-pq-next', 'textContent', machine.pqNextDate || 'N/A');

    // Highlight PQ near expiration
    if (machine.pqNextDate) {
        const next = new Date(machine.pqNextDate + 'T12:00:00');
        const diffDays = Math.ceil((next - new Date()) / (1000 * 60 * 60 * 24));
        if (diffDays > 0 && diffDays < 30) {
            const pqRow = t('print-pq-row');
            if (pqRow) pqRow.style.backgroundColor = '#fffbeb';

            const pqNextEl = t('print-pq-next');
            if (pqNextEl) {
                pqNextEl.style.color = '#b45309';
                pqNextEl.innerHTML = `⚠️ Vence en ${diffDays} días`;
            }
        }
    }

    // History Table
    const recentOrders = [...machineWorkOrders]
        .filter(o => ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(o.type))
        .sort((a, b) => new Date(b.date || '1970-01-01') - new Date(a.date || '1970-01-01'))
        .slice(0, 5);

    const historyHTML = recentOrders.map(o => `
        <tr>
            <td class="px-3 py-1.5 font-medium border-b border-slate-100">${o.id}</td>
            <td class="px-3 py-1.5 border-b border-slate-100">${new Date(o.date + 'T12:00:00Z').toLocaleDateString('es-ES')}</td>
            <td class="px-3 py-1.5 border-b border-slate-100 ${['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(o.type) ? 'text-green-600' : 'text-red-600'} font-medium italic">${o.type}</td>
        </tr>
    `).join('') || '<tr><td colspan="3" class="px-3 py-1.5 text-center text-slate-400">Sin historial registrado</td></tr>';
    if (historyBody) historyBody.innerHTML = historyHTML;

    // Parts Table
    const criticalityOrder = { 'Crítico': 4, 'Alto': 3, 'Medio': 2, 'Bajo': 1 };
    const sortedParts = state.parts
        .filter(p => p.machineIds && p.machineIds.includes(machineId))
        .sort((a, b) => {
            const critA = criticalityOrder[a.criticality || 'Bajo'] || 0;
            const critB = criticalityOrder[b.criticality || 'Bajo'] || 0;
            if (critA !== critB) return critB - critA;
            return (a.stockActual || 0) - (b.stockActual || 0);
        })
        .slice(0, 5);

    const partsHTML = sortedParts.map(p => {
        let stockStatusActual = 'OK';
        let stockClassActual = 'bg-green-100 text-green-800';
        if (p.stockActual <= 0) {
            stockStatusActual = 'SIN STOCK';
            stockClassActual = 'bg-red-100 text-red-800';
        } else if (p.stockActual <= p.minStock) {
            stockStatusActual = 'BAJO STOCK';
            stockClassActual = 'bg-yellow-100 text-yellow-800';
        }
        return `
            <tr>
                <td class="px-3 py-1.5 font-medium border-b border-slate-100">${p.description}</td>
                <td class="px-3 py-1.5 border-b border-slate-100"><span class="${stockClassActual} px-1 rounded text-[8px] font-bold">${stockStatusActual}</span></td>
            </tr>
        `;
    }).join('') || '<tr><td colspan="2" class="px-3 py-1.5 text-center text-slate-400">No hay repuestos vinculados</td></tr>';
    if (partsBody) partsBody.innerHTML = partsHTML;

    // Asset Relationships
    const parentAsset = machine.parentAssetId ? getMachineById(machine.parentAssetId) : null;
    const childAssets = state.machines
        .filter(m => m.parentAssetId === machine.id && m.status !== 'DADO_DE_BAJA')
        .sort((a, b) => (a.name || '').localeCompare(b.name || ''));

    setT('print-parent-asset', 'innerHTML', parentAsset ? `<div class="font-bold text-primary">${parentAsset.id} - ${parentAsset.name}</div><div class="text-slate-500">${getAssetTypeLabel(parentAsset.type)} | ${getAssetCategoryInfo(parentAsset).label}</div>` : '<span class="text-slate-400 italic">Este activo no tiene activo padre asignado.</span>'
    );

    const childAssetsBody = t('print-child-assets-table-body');
    const childAssetsHTML = childAssets.map(child => `
        <tr>
            <td class="px-3 py-1.5 font-medium border-b border-slate-100">${child.id} - ${child.name}</td>
            <td class="px-3 py-1.5 border-b border-slate-100">${getAssetTypeLabel(child.type)}</td>
            <td class="px-3 py-1.5 border-b border-slate-100">${child.criticidad || 'Baja'}</td>
        </tr>
    `).join('') || '<tr><td colspan="3" class="px-3 py-1.5 text-center text-slate-400">No hay equipos vinculados</td></tr>';
    if (childAssetsBody) childAssetsBody.innerHTML = childAssetsHTML;

    // Documentation List
    let docsHTML = '';
    if (!machine.userManualUrl) docsHTML += `<li class="flex items-center gap-2"><span class="w-1 h-1 bg-red-400 rounded-full"></span> Manual de Usuario NO DISPONIBLE</li>`;
    if (!machine.techManualUrl) docsHTML += `<li class="flex items-center gap-2"><span class="w-1 h-1 bg-red-400 rounded-full"></span> Manual Técnico NO DISPONIBLE</li>`;
    if (machine.userManualUrl) docsHTML += `<li class="flex items-center gap-2"><span class="w-1 h-1 bg-green-400 rounded-full"></span> Manual de Usuario: DISPONIBLE ✅</li>`;
    if (machine.techManualUrl) docsHTML += `<li class="flex items-center gap-2"><span class="w-1 h-1 bg-green-400 rounded-full"></span> Manual Técnico: DISPONIBLE ✅</li>`;
    setT('print-docs-list', 'innerHTML', docsHTML);

    // Footer Info
    setT('print-user', 'textContent', getCurrentUser().username || 'Sistema');
    setT('print-date', 'textContent', new Date().toLocaleString('es-ES'));

    // Generate QR Code
    const qrContainer = t('print-qr-container');
    if (qrContainer && iframe.contentWindow.QRCode) {
        qrContainer.innerHTML = '';
        const publicUrl = `${window.location.origin}${window.location.pathname}machineId=${machine.id}`;
        new iframe.contentWindow.QRCode(qrContainer, {
            text: publicUrl,
            width: 60,
            height: 60,
            colorDark: "#000000",
            colorLight: "#ffffff",
            correctLevel: iframe.contentWindow.QRCode.CorrectLevel.H
        });
    }

    // Force Tailwind to re-process the document after content injection
    if (iframe.contentWindow.tailwind) {
        try {
            // Some versions of Tailwind CDN auto-detect, but we can try to nudge it
            // by adding a dummy class to body
            doc.body.classList.add('tailwind-applied');
        } catch(e) {}
    }

    // Give extra delay for images and Tailwind JIT to process the new content
    await new Promise(r => setTimeout(r, 2000));

    iframe.contentWindow.print();
    setTimeout(() => {
        if (document.body.contains(iframe)) document.body.removeChild(iframe);
        showLoading(false);
    }, 1000);
}

function showMachineHistory(machineId, type) {
    const historyList = document.getElementById('machine-history-list');
    const title = document.getElementById('machine-history-modal-title');
    const machine = state.machines.find(m => m.id === machineId) || { name: machineId };

    title.textContent = `Historial: ${machine.name} (${type})`;
    historyList.innerHTML = '';

    const orders = state.workOrders
        .filter(wo => wo.machineId === machineId && wo.type === type)
        .sort((a, b) => new Date((b.date || '1970-01-01') + 'T12:00:00Z') - new Date((a.date || '1970-01-01') + 'T12:00:00Z'));

    if (orders.length === 0) {
        historyList.innerHTML = '<tr><td colspan="5" class="text-center">No hay registros para este tipo.</td></tr>';
    } else {
        orders.forEach(order => {
            const tr = document.createElement('tr');

            let statusClass = 'bg-light text-dark';
            if (['En Proceso', 'Pausado'].includes(order.status)) statusClass = 'bg-info text-dark';
            if (order.status === 'Completado') statusClass = 'bg-success';
            if (order.status === 'Cancelado') statusClass = 'bg-danger';
            if (order.status === 'Pendiente de Evaluación' || order.status === 'Pendiente de Aprobación') statusClass = 'bg-warning text-dark';

            tr.innerHTML = `
                <td><strong>${order.id}</strong></td>
                <td>${order.date || 'N/A'}</td>
                <td>${order.type}</td>
                <td><span class="badge ${statusClass}">${order.status}</span></td>
                <td>
                    <button class="btn btn-sm btn-outline-primary view-wo-from-history" data-id="${order.fb_id || order.id}">
                        <i class="fas fa-eye"></i>
                    </button>
                </td>
            `;
            historyList.appendChild(tr);

            tr.querySelector('.view-wo-from-history').addEventListener('click', () => {
                state.modals.machineHistory.hide();
                showWorkOrderModal(order.fb_id || order.id);
            });
        });
    }

    state.modals.machineHistory.show();
}

async function getOptimizedStorageUrl(originalRef, maxRetries = 3, delay = 1200) {
    const originalPath = originalRef.fullPath;
    const originalUrl = await getDownloadURL(originalRef);
    const lastDotIndex = originalPath.lastIndexOf('.');
    const extension = lastDotIndex !== -1 ? originalPath.substring(lastDotIndex + 1).toLowerCase() : '';
    const imageExtensions = ['jpg', 'jpeg', 'png', 'webp', 'gif', 'bmp'];

    if (!imageExtensions.includes(extension)) return originalUrl;

    const pathWithoutExtension = lastDotIndex !== -1 ? originalPath.substring(0, lastDotIndex) : originalPath;
    const optimizedPath = `${pathWithoutExtension}_1024x1024.webp`;
    const optimizedRef = ref(state.storage, optimizedPath);

    if (optimizedImageUrlCache.has(optimizedPath)) return optimizedImageUrlCache.get(optimizedPath);
    if (missingOptimizedImagePaths.has(optimizedPath)) return originalUrl;

    for (let i = 0; i < maxRetries; i++) {
        try {
            const url = await getDownloadURL(optimizedRef);
            optimizedImageUrlCache.set(optimizedPath, url);
            return url;
        } catch (error) {
            if (error.code === 'storage/object-not-found') {
                if (i < maxRetries - 1) {
                    await new Promise(resolve => setTimeout(resolve, delay));
                }
            } else {
                console.warn('[Storage] No se pudo obtener imagen optimizada, usando original:', error.code || error.message || error);
                return originalUrl;
            }
        }
    }

    missingOptimizedImagePaths.add(optimizedPath);
    return originalUrl;
}
async function uploadMachineFile(machineId, file, type) {
    if (!file) return null;

    // Validar tamaño (10MB)
    const MAX_SIZE = 10 * 1024 * 1024;
    if (file.size > MAX_SIZE) {
        throw new Error(`El archivo ${file.name} excede el límite de 10MB.`);
    }

    const fileName = `${type}_${Date.now()}_${file.name}`;
    const storageRef = ref(state.storage, `artifacts/${appId}/machines/${machineId}/${fileName}`);

    try {
        const snapshot = await uploadBytes(storageRef, file);
        const downloadURL = await getOptimizedStorageUrl(snapshot.ref);
        return downloadURL;
    } catch (error) {
        console.error(`Error uploading ${type}:`, error);
        throw new Error(`Fallo al subir el archivo ${file.name}`);
    }
}

function showMachineModal(machineId = null) {
    const form = document.getElementById('machine-form');
    form.reset();
    const idInput = document.getElementById('machine-id');
    const scheduleWrapper = document.getElementById('schedule-wrapper');
    const disableScheduleCheck = document.getElementById('disable-schedule-check');
    
    document.querySelectorAll('.day-check').forEach(c => c.checked = false);
    document.getElementById('machine-start-time-weekday').value = '';
    document.getElementById('machine-end-time-weekday').value = '';
    document.getElementById('machine-start-time-saturday').value = '';
    document.getElementById('machine-end-time-saturday').value = '';
    document.getElementById('machine-start-time-sunday').value = '';
    document.getElementById('machine-end-time-sunday').value = '';

    // Limpiar estados de archivos
    document.getElementById('machine-image-status').innerHTML = '';
    document.getElementById('machine-user-manual-status').innerHTML = '';
    document.getElementById('machine-tech-manual-status').innerHTML = '';
    document.getElementById('machine-image-file').value = '';
    document.getElementById('machine-user-manual-file').value = '';
    document.getElementById('machine-tech-manual-file').value = '';

    if (machineId) {
        document.getElementById('machine-modal-title').textContent = 'Editar Activo';
        const machine = state.machines.find(m => m.id === machineId);
        if (machine) {
            document.getElementById('machine-id-hidden').value = machine.id;
            idInput.value = machine.id;
            document.getElementById('machine-name').value = machine.name;
            document.getElementById('machine-type').value = machine.type || 'maquina';
            document.getElementById('machine-asset-category').value = getAssetCategory(machine);
            populateParentAssetSelector(machine.id, machine.parentAssetId || '');
            document.getElementById('machine-location').value = machine.location;
            document.getElementById('machine-area').value = getMachineArea(machine);
            document.getElementById('machine-status').value = machine.status || 'ACTIVO';
            document.getElementById('machine-criticidad').value = machine.criticidad || 'Baja';
            document.getElementById('machine-monitoring-enabled').checked = machine.monitoringEnabled !== false;
            idInput.setAttribute('readonly', true);

            // New fields
            document.getElementById('machine-brand').value = machine.brand || '';
            document.getElementById('machine-model').value = machine.model || '';
            document.getElementById('machine-serial').value = machine.serialNumber || '';
            document.getElementById('machine-risk').value = machine.riskClassification || '';
            document.getElementById('machine-centro-costo').value = machine.centroCosto || 'General';
            document.getElementById('machine-costo-paro').value = machine.costoParoHora || 0;

            // Mostrar estado de archivos actuales
            if (machine.imageUrl) {
                document.getElementById('machine-image-status').innerHTML = `<img src="${machine.imageUrl}" class="img-thumbnail" style="max-height: 100px;"><p class="small text-muted mb-0">Imagen actual</p>`;
            }
            if (machine.userManualUrl) {
                document.getElementById('machine-user-manual-status').innerHTML = `<a href="${machine.userManualUrl}" target="_blank" class="btn btn-sm btn-outline-info mt-1"><i class="fas fa-file-pdf me-1"></i>Ver Manual Actual</a>`;
            }
            if (machine.techManualUrl) {
                document.getElementById('machine-tech-manual-status').innerHTML = `<a href="${machine.techManualUrl}" target="_blank" class="btn btn-sm btn-outline-info mt-1"><i class="fas fa-file-pdf me-1"></i>Ver Manual Actual</a>`;
            }

            document.getElementById('machine-purchase-date').value = machine.purchaseDate || '';
            document.getElementById('machine-commissioning-date').value = machine.commissioningDate || '';
            document.getElementById('machine-iq-status').checked = machine.iqStatus || false;
            document.getElementById('machine-iq-date').value = machine.iqDate || '';
            document.getElementById('machine-oq-status').checked = machine.oqStatus || false;
            document.getElementById('machine-oq-date').value = machine.oqDate || '';
            document.getElementById('machine-pq-status').checked = machine.pqStatus || false;
            document.getElementById('machine-pq-next-date').value = machine.pqNextDate || '';

            const scheduleDisabled = machine.scheduleDisabled || false;
            disableScheduleCheck.checked = scheduleDisabled;
            scheduleWrapper.style.display = scheduleDisabled ? 'none' : 'block';

            if (machine.schedule) {
                if (machine.schedule.weekday || machine.schedule.saturday || machine.schedule.sunday) {
                    if (machine.schedule.weekday) {
                        machine.schedule.weekday.activeDays.forEach(day => {
                            const checkbox = document.querySelector(`.day-check[value="${day}"]`);
                            if (checkbox) checkbox.checked = true;
                        });
                        document.getElementById('machine-start-time-weekday').value = machine.schedule.weekday.startTime || '';
                        document.getElementById('machine-end-time-weekday').value = machine.schedule.weekday.endTime || '';
                    }
                    if (machine.schedule.saturday.active) {
                        document.getElementById('day-sat').checked = true;
                        document.getElementById('machine-start-time-saturday').value = machine.schedule.saturday.startTime || '';
                        document.getElementById('machine-end-time-saturday').value = machine.schedule.saturday.endTime || '';
                    }
                    if (machine.schedule.sunday.active) {
                        document.getElementById('day-sun').checked = true;
                        document.getElementById('machine-start-time-sunday').value = machine.schedule.sunday.startTime || '';
                        document.getElementById('machine-end-time-sunday').value = machine.schedule.sunday.endTime || '';
                    }
                }
            }
        }
    } else {
        document.getElementById('machine-modal-title').textContent = 'Añadir Activo';
        document.getElementById('machine-id-hidden').value = '';
        disableScheduleCheck.checked = false;
        scheduleWrapper.style.display = 'block';
        document.getElementById('machine-type').value = 'maquina';
        document.getElementById('machine-asset-category').value = 'produccion';
        document.getElementById('machine-area').value = '';
        populateParentAssetSelector('', '');
        idInput.removeAttribute('readonly');
    }
    state.modals.machine.show();
}

async function handleMachineSubmit(e) {
    e.preventDefault();
    const originalId = document.getElementById('machine-id-hidden').value;
    const machineId = document.getElementById('machine-id').value.trim();

    if(!machineId) {
        showToast('El ID de la máquina no puede estar vacío.', 'error');
        return;
    }

    const parentAssetId = document.getElementById('machine-parent-asset').value || '';
    if (parentAssetId === machineId || getDescendantAssetIds(machineId).has(parentAssetId)) {
        showToast('El activo padre seleccionado no es válido.', 'error');
        return;
    }

    const imageFile = document.getElementById('machine-image-file').files[0];
    const userManualFile = document.getElementById('machine-user-manual-file').files[0];
    const techManualFile = document.getElementById('machine-tech-manual-file').files[0];

    showLoading(true);

    const weekdayActiveDays = [];
    document.querySelectorAll('#machine-form .day-check:checked').forEach(c => {
        const day = parseInt(c.value);
        if (day >= 1 && day <= 5) {
            weekdayActiveDays.push(c.value);
        }
    });

    const schedule = {
        weekday: {
            activeDays: weekdayActiveDays,
            startTime: document.getElementById('machine-start-time-weekday').value,
            endTime: document.getElementById('machine-end-time-weekday').value,
        },
        saturday: {
            active: document.getElementById('day-sat').checked,
            startTime: document.getElementById('machine-start-time-saturday').value,
            endTime: document.getElementById('machine-end-time-saturday').value,
        },
        sunday: {
            active: document.getElementById('day-sun').checked,
            startTime: document.getElementById('machine-start-time-sunday').value,
            endTime: document.getElementById('machine-end-time-sunday').value,
        }
    };

    const machineData = {
        lastModified: new Date().toISOString(),
        id: document.getElementById('machine-id').value.trim(),
        name: document.getElementById('machine-name').value,
        status: document.getElementById('machine-status').value || 'ACTIVO',
        type: document.getElementById('machine-type').value,
        assetCategory: document.getElementById('machine-asset-category').value || 'produccion',
        parentAssetId: parentAssetId,
        location: document.getElementById('machine-location').value,
        area: document.getElementById('machine-area').value || '',
        criticidad: document.getElementById('machine-criticidad').value,
        monitoringEnabled: document.getElementById('machine-monitoring-enabled').checked,
        scheduleDisabled: document.getElementById('disable-schedule-check').checked,
        schedule: schedule,
        brand: document.getElementById('machine-brand').value || '',
        model: document.getElementById('machine-model').value || '',
        serialNumber: document.getElementById('machine-serial').value || '',
        riskClassification: document.getElementById('machine-risk').value || '',
        purchaseDate: document.getElementById('machine-purchase-date').value || '',
        commissioningDate: document.getElementById('machine-commissioning-date').value || '',
        centroCosto: document.getElementById('machine-centro-costo').value || 'General',
        costoParoHora: parseFloat(document.getElementById('machine-costo-paro').value) || 0,
        iqStatus: document.getElementById('machine-iq-status').checked || false,
        iqDate: document.getElementById('machine-iq-date').value || '',
        oqStatus: document.getElementById('machine-oq-status').checked || false,
        oqDate: document.getElementById('machine-oq-date').value || '',
        pqStatus: document.getElementById('machine-pq-status').checked || false,
        pqNextDate: document.getElementById('machine-pq-next-date').value || '',
    };

    const existingMachine = state.machines.find(m => m.fb_id === machineId) || (originalId ? state.machines.find(m => m.fb_id === originalId) : null);

    try {
        // --- Procesar Subida de Archivos ---
        let imageUrl = existingMachine.imageUrl || '';
        let userManualUrl = existingMachine.userManualUrl || '';
        let techManualUrl = existingMachine.techManualUrl || '';

        if (imageFile) imageUrl = await uploadMachineFile(machineId, imageFile, 'img');
        if (userManualFile) userManualUrl = await uploadMachineFile(machineId, userManualFile, 'manual_user');
        if (techManualFile) techManualUrl = await uploadMachineFile(machineId, techManualFile, 'manual_tech');

        machineData.imageUrl = imageUrl;
        machineData.userManualUrl = userManualUrl;
        machineData.techManualUrl = techManualUrl;
        if (existingMachine && existingMachine.createdAt) {
            machineData.createdAt = existingMachine.createdAt;
        } else if (!originalId) {
            machineData.createdAt = new Date().toISOString();
        }

        const docRef = doc(state.collections.machines, machineData.id);
        await setDoc(docRef, machineData, { merge: true });
        await updateRTDBMirror('machines', machineData, machineData.id);

        if (originalId && originalId !== machineData.id) {
             await deleteDoc(doc(state.collections.machines, originalId));
             await removeRTDBMirror('machines', originalId);
             await recordAuditLog('machines', machineData.id, 'CREATE', null, machineData, 'Cambio ID Máquina');
             await recordAuditLog('machines', originalId, 'DELETE', existingMachine, null, 'Eliminación por cambio ID');
        } else if (!originalId) {
             await recordAuditLog('machines', machineData.id, 'CREATE', null, machineData, 'Creación Máquina');
        } else {
             await recordAuditLog('machines', machineData.id, 'UPDATE', existingMachine, machineData, 'Modificación Máquina');

             // Registrar historial de estado si cambió
             if (existingMachine && existingMachine.status !== machineData.status) {
                 const historyData = {
                     equipment_id: machineData.id,
                     equipment_fb_id: machineData.id,
                     old_status: existingMachine.status || 'ACTIVO',
                     new_status: machineData.status,
                     changed_by: getCurrentUser().username,
                     change_date: new Date().toISOString(),
                     reason: 'Cambio manual en edición de equipo',
                     notes: ''
                 };
                 await addDoc(state.collections.equipmentStatusHistory, historyData);
             }
        }

        // --- Sincronización con Odoo (Segundo Plano) ---
        if (state.odoo) {
            syncMachineToOdoo(machineData).catch(err => {
                console.warn("[Odoo Background Sync] Error:", err);
            });
        }

        showToast('Activo guardado correctamente', 'success');
        state.modals.machine.hide();
    } catch (error) { 
        console.error("Error saving machine: ", error);
        showToast('Error al guardar la máquina: ' + error.message, 'error');
    } finally {
        showLoading(false);
    }
}

function deleteMachine(machineId) {
     showConfirmation('Eliminar Activo', `¿Está seguro de que desea eliminar el activo ${machineId}`, async () => {
        try { 
            const existingMachine = state.machines.find(m => m.fb_id === machineId);
            await deleteDoc(doc(state.collections.machines, machineId)); 
            await recordAuditLog('machines', machineId, 'DELETE', existingMachine, null, 'Eliminación Máquina');
            showToast('Activo eliminado correctamente', 'success');
        } catch (error) { 
            console.error("Error deleting machine: ", error);
            showToast('Error al eliminar la máquina', 'error');
        }
    });
}

// --- CRUD Part Functions ---
function renderParts() {
    if (state.currentTab !== 'repuestos' && state.currentTab !== 'dashboard' && initialLoadComplete) return;
    requestAnimationFrame(() => {
    const criticalList = document.getElementById('parts-list-critical');
    const attentionList = document.getElementById('parts-list-attention');
    const operativeList = document.getElementById('parts-list-operative');

    if (!criticalList || !attentionList || !operativeList) return;

    const searchTerm = document.getElementById('search-part-input').value.toLowerCase();

    let countCritical = 0;
    let countAttention = 0;
    let countOperative = 0;

    const userRole = getCurrentUser().role;
    const managedMachineIds = getCurrentUser().managedMachineIds;
    const equipoAsignado = getCurrentUser().equipoAsignado;
    const hasPermissionFilter = (userRole === 'Jefe de Area' && Array.isArray(managedMachineIds)) || ((userRole === 'Técnico' || (['Operario', 'Supervisor de Area'].includes(userRole))) && Array.isArray(equipoAsignado));
    const allowedMachineIds = hasPermissionFilter ? new Set(userRole === 'Jefe de Area' ? managedMachineIds : equipoAsignado) : null;

    // Optimization: Build HTML as strings and update innerHTML once at the end
    let criticalHTML = '';
    let attentionHTML = '';
    let operativeHTML = '';

    state.parts.forEach(part => {
        // 1. Permission filter
        if (allowedMachineIds) {
            const machineIds = part.machineIds || (part.machineId ? [part.machineId] : []);
            if (!machineIds.some(mId => allowedMachineIds.has(mId))) return;
        }

        if (state.partClassificationFilter && state.partClassificationFilter !== 'all' && part.classification !== state.partClassificationFilter) return;

        // 2. Search filter
        if (searchTerm) {
            const matches = (part.id || '').toLowerCase().includes(searchTerm) ||
                          (part.description || '').toLowerCase().includes(searchTerm);
            if (!matches) return;
        }

        const currentStock = Number(part.stockActual) || 0;
        const minStock = Number(part.minStock) || 0;

        let status = 'operative';
        if (currentStock === 0) {
            status = 'critical';
            countCritical++;
        } else if (currentStock <= minStock) {
            status = 'attention';
            countAttention++;
        } else {
            countOperative++;
        }

        // 3. Status filter
        if (state.partStockFilter && state.partStockFilter !== status) return;

        const rowHTML = `<tr id="part-row-${part.fb_id}">${createPartRowHTML(part, status)}</tr>`;

        if (status === 'critical') criticalHTML += rowHTML;
        else if (status === 'attention') attentionHTML += rowHTML;
        else operativeHTML += rowHTML;
    });

    criticalList.innerHTML = criticalHTML || '<tr><td colspan="9" class="text-center text-muted">No hay repuestos en esta categoría</td></tr>';
    attentionList.innerHTML = attentionHTML || '<tr><td colspan="9" class="text-center text-muted">No hay repuestos en esta categoría</td></tr>';
    operativeList.innerHTML = operativeHTML || '<tr><td colspan="9" class="text-center text-muted">No hay repuestos en esta categoría</td></tr>';

    // Re-vincular event listeners delegados (mejor rendimiento que iterar y vincular a cada uno)
    [criticalList, attentionList, operativeList].forEach(list => {
        list.onclick = (e) => {
            const btn = e.target.closest('button');
            if (!btn) return;

            if (btn.classList.contains('view-part-detail')) showPartDetail(btn.dataset.id);
            else if (btn.classList.contains('view-part-history')) showPartHistoryModal(btn.dataset.id);
            else if (btn.classList.contains('edit-part')) showPartModal(btn.dataset.id);
            else if (btn.classList.contains('delete-part')) deletePart(btn.dataset.id);
            else if (btn.classList.contains('add-to-purchase-cart')) addToPurchaseCart(btn.dataset.id);
        };
    });

    // Actualizar contadores
    document.getElementById('count-critical').textContent = countCritical;
    document.getElementById('count-attention').textContent = countAttention;
    document.getElementById('count-operative').textContent = countOperative;

    // Actualizar resaltado de filtros activos
    document.querySelectorAll('.inventory-summary .summary-pill').forEach(pill => {
        let pillStatus = '';
        if (pill.classList.contains('critical')) pillStatus = 'critical';
        else if (pill.classList.contains('attention')) pillStatus = 'attention';
        else if (pill.classList.contains('operative')) pillStatus = 'operative';
        pill.classList.toggle('active-filter', state.partStockFilter === pillStatus);
    });

    document.querySelectorAll('.part-classification-filters [data-classification]').forEach(btn => {
        btn.classList.toggle('active', (btn.dataset.classification || 'all') === (state.partClassificationFilter || 'all'));
    });

    // Mostrar/ocultar y expandir zonas según filtros y contenido
    const zones = {
        critical: document.getElementById('zone-critical'),
        attention: document.getElementById('zone-attention'),
        operative: document.getElementById('zone-operative')
    };

    Object.keys(zones).forEach(key => {
        const zone = zones[key];
        const count = key === 'critical' ? countCritical : (key === 'attention' ? countAttention : countOperative);

        // Si hay un filtro activo, solo mostramos la zona filtrada
        if (state.partStockFilter) {
            zone.style.display = (state.partStockFilter === key && count > 0) ? 'block' : 'none';
            // Expandir automáticamente si está filtrado
            if (state.partStockFilter === key) {
                const collapseEl = document.getElementById(`collapse-${key}`);
                if (collapseEl) collapseEl.classList.add('show');
                const headerEl = zone.querySelector('.zone-header');
                if (headerEl) headerEl.classList.remove('collapsed');
            }
        } else {
            zone.style.display = count > 0 ? 'block' : 'none';
        }
    });

    // Manejar estado vacío global
    const totalDisplayed = countCritical + countAttention + countOperative;
    if (totalDisplayed === 0) {
        operativeList.innerHTML = '<tr><td colspan="10" class="text-center text-muted py-4">No se encontraron repuestos o insumos</td></tr>';
        document.getElementById('zone-operative').style.display = 'block';
    }

    // Mostrar alerta de error de sincronización Odoo si existe
    const existingPill = document.getElementById('odoo-sync-error-pill');
    if (existingPill) existingPill.remove();

    if (state.odooSyncError) {
        const errorPill = document.createElement('div');
        errorPill.id = 'odoo-sync-error-pill';
        errorPill.className = 'alert alert-danger d-flex align-items-center mb-4 w-100';
        errorPill.style.cursor = 'pointer';
        errorPill.innerHTML = `
            <i class="fas fa-exclamation-triangle me-2"></i>
            <div class="flex-grow-1">
                <strong>Error de Sincronización Odoo:</strong> ${state.odooSyncError.length > 100 ? state.odooSyncError.substring(0, 100) + '...' : state.odooSyncError}
                <br><small>Haga clic para limpiar este aviso.</small>
            </div>
            <button type="button" class="btn-close" aria-label="Close"></button>
        `;
        errorPill.onclick = () => {
            state.odooSyncError = null;
            errorPill.remove();
        };
        const partsTab = document.getElementById('repuestos');
        if (partsTab) partsTab.prepend(errorPill);
    }
    });
}

function addOrUpdatePartInTable(part) {
    // En la nueva arquitectura de 3 zonas, reconstruimos las listas para asegurar la correcta categorización
    renderParts();
}

function buildPartMovementData(partId, quantity, type, details = {}) {
    const part = state.parts.find(p => p.id === partId);
    const numericQuantity = Number(quantity) || 0;
    const stockBefore = details.stockBefore ?? details.stockAnterior ?? Number(part.stockActual || 0);
    const stockAfter = details.stockAfter ?? details.stockPosterior ?? (Number(stockBefore) + numericQuantity);
    const unitCost = details.unitCost ?? details.cost ?? Number(part.cost || 0);
    const orderHumanId = details.orderHumanId || details.reference || null;
    const movementSource = details.movementSource || details.source || (
        type.includes('Compra') ? 'COMPRA' :
        type.includes('OT') ? 'ORDEN_TRABAJO' :
        type.includes('MATERIAL') ? 'SOLICITUD_MATERIAL' :
        type.includes('AJUSTE') || type.includes('Ajuste') ? 'AJUSTE_INVENTARIO' :
        'INVENTARIO'
    );

    return {
        date: details.date || new Date().toISOString(),
        partId: partId,
        partDescription: details.partDescription || (part ? part.description : 'Desconocido'),
        classification: details.classification || part?.classification || 'repuesto',
        quantity: numericQuantity,
        movementDirection: numericQuantity > 0 ? 'ENTRADA' : (numericQuantity < 0 ? 'SALIDA' : 'SIN_CAMBIO'),
        type: type,
        stockBefore: Number(stockBefore) || 0,
        stockAfter: Number(stockAfter) || 0,
        unitCost: Number(unitCost) || 0,
        movementValue: Math.abs(numericQuantity) * (Number(unitCost) || 0),
        orderId: details.orderId || null,
        orderHumanId: orderHumanId,
        reference: orderHumanId,
        referenceType: details.referenceType || movementSource,
        movementSource: movementSource,
        user: details.user || getCurrentUser().username || 'Sistema',
        recipientUser: details.recipientUser || (orderHumanId ? state.workOrders.find(o => o.id === orderHumanId)?.leadTechnician : null),
        reason: details.reason || details.adjustmentReason || null,
        details: details.details || '',
        ...details
    };
}

async function recordPartMovement(partId, quantity, type, details = {}) {
    try {
        const movementData = buildPartMovementData(partId, quantity, type, details);
        await addDoc(state.collections.partMovements, movementData);
        await recordAuditLog('partMovements', movementData.partId, 'CREATE', null, movementData, `Movimiento Inventario: ${movementData.type}`);
    } catch (error) {
        console.error("Error recording part movement:", error);
    }
}

async function showPartHistoryModal(partId = null) {
    state.currentPartIdForHistory = partId;
    if (partId) {
        const part = state.parts.find(p => p.id === partId);
        document.getElementById('part-history-modal-title').textContent = `Historial de: ${part ? part.description : partId}`;
    } else {
        document.getElementById('part-history-modal-title').textContent = `Historial General de Movimientos`;
    }
    state.modals.partHistory.show();
    await loadPartMovements(partId);
}

function handleAddToRequestCart() {
    const description = document.getElementById('wo-request-description').value.trim();
    const quantity = parseInt(document.getElementById('wo-request-quantity').value);

    if (!description || isNaN(quantity) || quantity < 1) {
        showToast('Complete la descripción y cantidad válida.', 'warning');
        return;
    }

    state.requestCart.push({
        description: description,
        quantity: quantity,
        status: 'Pendiente',
        deliveredQuantity: 0
    });

    document.getElementById('wo-request-description').value = '';
    document.getElementById('wo-request-quantity').value = 1;
    renderRequestCart();
}

function renderRequestCart() {
    const container = document.getElementById('wo-request-cart-container');
    const submitBtn = document.getElementById('wo-submit-request-btn');

    if (state.requestCart.length === 0) {
        container.innerHTML = '';
        submitBtn.style.display = 'none';
        return;
    }

    submitBtn.style.display = 'inline-block';
    let html = '<div class="mt-2"><table class="table table-sm table-bordered x-small mb-0"><thead class="table-light"><tr><th>Descripción</th><th>Cant.</th><th>Acción</th></tr></thead><tbody>';

    state.requestCart.forEach((item, index) => {
        html += `<tr>
            <td>${item.description}</td>
            <td>${item.quantity}</td>
            <td class="text-center">
                <button class="btn btn-xs btn-link text-danger p-0" onclick="removeFromRequestCart(${index})"><i class="fas fa-trash"></i></button>
            </td>
        </tr>`;
    });

    html += '</tbody></table></div>';
    container.innerHTML = html;
}

window.removeFromRequestCart = function(index) {
    const item = state.requestCart[index];
    if (item) {
        const reason = prompt(`Indique el motivo por el cual elimina "${item.description}" de la solicitud:`);
        if (reason === null) return;
        if (!reason.trim()) {
            showToast('El motivo de eliminación es obligatorio.', 'warning');
            return;
        }

        const obsField = document.getElementById('wo-observaciones');
        const timestamp = new Date().toLocaleString();
        const removalNote = `\n[${timestamp}] Ítem "${item.description}" ELIMINADO del carrito de solicitud. Motivo: ${reason}`;
        obsField.value += removalNote;
    }
    state.requestCart.splice(index, 1);
    renderRequestCart();
};

async function handlePartRequestSubmit() {
    const urgency = document.getElementById('wo-request-urgency').value;
    const woFbId = document.getElementById('wo-fb-id-hidden').value;
    const woId = document.getElementById('wo-id').value;
    const machineId = document.getElementById('wo-machine').value;

    if (state.requestCart.length === 0) {
        showToast('Agregue al menos un ítem a la solicitud.', 'warning');
        return;
    }

    if (!woFbId) {
        showToast('Debe guardar la orden de trabajo primero para poder solicitar repuestos.', 'warning');
        return;
    }

    showLoading(true);
    try {
        const requestData = {
            id: generateNextPartRequestId(),
            date: new Date().toISOString(),
            workOrderFbId: woFbId,
            workOrderId: woId,
            machineId: machineId,
            requester: getCurrentUser().username || 'Desconocido',
            items: state.requestCart,
            urgency: urgency,
            status: 'Pendiente'
        };
        const docRef = await addDoc(state.collections.partRequests, requestData);
        await updateRTDBMirror('partRequests', requestData, docRef.id);
        await recordAuditLog('partRequests', woId || docRef.id, 'CREATE', null, requestData, 'Creación Solicitud Repuesto');

        state.requestCart = [];
        renderRequestCart();
        showToast('Solicitud de repuestos enviada.', 'success');
        renderWoPendingRequests();
    } catch (error) {
        console.error("Error submitting part request:", error);
        showToast('Error al enviar la solicitud.', 'error');
    } finally {
        showLoading(false);
    }
}

function renderWoPendingRequests() {
    const list = document.getElementById('wo-pending-requests-list');
    if (!list) return;
    const woFbId = document.getElementById('wo-fb-id-hidden').value;
    if (!woFbId) {
        list.innerHTML = '';
        return;
    }

    const requests = state.partRequests.filter(r => r.workOrderFbId === woFbId);
    if (requests.length === 0) {
        list.innerHTML = '';
        return;
    }

    list.innerHTML = '<h6 class="mt-2 small fw-bold">Solicitudes Enviadas para esta OT:</h6>';
    const table = document.createElement('table');
    table.className = 'table table-sm table-bordered x-small mb-0';
    table.innerHTML = `
        <thead class="table-light">
            <tr>
                <th>Descripción</th>
                <th>Cant.</th>
                <th>Estado</th>
                <th style="width: 40px;"></th>
            </tr>
        </thead>
        <tbody></tbody>
    `;
    const tbody = table.querySelector('tbody');
    requests.forEach(r => {
        if (r.items && Array.isArray(r.items)) {
            r.items.forEach((item, idx) => {
                const tr = document.createElement('tr');
                let statusClass = 'bg-secondary';
                if (item.status === 'Pedido' || item.status === 'Aprobado') statusClass = 'bg-info text-white';
                if (item.status === 'Recibido' || item.status === 'Entregado') statusClass = 'bg-success';
                if (item.status === 'Rechazado' || item.status === 'Cancelado') statusClass = 'bg-danger';

                const canRemove = !['Recibido', 'Entregado', 'Rechazado', 'Cancelado'].includes(item.status);

                tr.innerHTML = `
                    <td>${item.description}</td>
                    <td>${item.quantity}</td>
                    <td><span class="badge ${statusClass}">${item.status}</span></td>
                    <td class="text-center">
                        ${canRemove ? `<button type="button" class="btn btn-xs btn-link text-danger p-0" onclick="removeRequestFromWorkOrder('${r.fb_id}', '${item.description.replace(/'/g, "\\'")}', true)"><i class="fas fa-trash"></i></button>` : ''}
                    </td>
                `;
                tbody.appendChild(tr);
            });
        } else {
            const tr = document.createElement('tr');
            let statusClass = 'bg-secondary';
            if (r.status === 'Pedido' || r.status === 'Aprobado') statusClass = 'bg-info text-white';
            if (r.status === 'Recibido' || r.status === 'Entregado') statusClass = 'bg-success';
            if (r.status === 'Rechazado' || r.status === 'Cancelado') statusClass = 'bg-danger';

            const canRemove = !['Recibido', 'Entregado', 'Rechazado', 'Cancelado'].includes(r.status);

            tr.innerHTML = `
                <td>${r.description}</td>
                <td>${r.quantity}</td>
                <td><span class="badge ${statusClass}">${r.status}</span></td>
                <td class="text-center">
                    ${canRemove ? `<button type="button" class="btn btn-xs btn-link text-danger p-0" onclick="removeRequestFromWorkOrder('${r.fb_id}', '${r.description.replace(/'/g, "\\'")}', false)"><i class="fas fa-trash"></i></button>` : ''}
                </td>
            `;
            tbody.appendChild(tr);
        }
    });
    list.appendChild(table);
}

async function removePartFromWorkOrder(partId, description) {
    const woFbId = document.getElementById('wo-fb-id-hidden').value;

    if (woFbId) {
        const reason = prompt(`Indique el motivo por el cual elimina el repuesto "${description}" de la orden:`);
        if (reason === null) return;
        if (!reason.trim()) {
            showToast('El motivo de eliminación es obligatorio.', 'warning');
            return;
        }

        const obsField = document.getElementById('wo-observaciones');
        const timestamp = new Date().toLocaleString();
        const removalNote = `\n[${timestamp}] Repuesto "${description}" ELIMINADO. Motivo: ${reason}`;
        obsField.value += removalNote;
    }

    const list = document.getElementById('wo-parts-list');
    const row = list.querySelector(`[data-part-id="${partId}"]`);
    if (row) {
        row.remove();
        showToast('Repuesto eliminado de la orden.', 'info');
    }
}

async function removeRequestFromWorkOrder(requestFbId, itemDescription, isSubItem = false) {
    const reason = prompt(`Indique el motivo por el cual cancela la solicitud de ? "${itemDescription}" :`);
    if (reason === null) return;
    if (!reason.trim()) {
        showToast('El motivo de eliminación es obligatorio.', 'warning');
        return;
    }

    showLoading(true);
    try {
        const request = state.partRequests.find(r => r.fb_id === requestFbId);
        if (!request) throw new Error("Solicitud no encontrada");

        const now = new Date().toISOString();
        const timestamp = new Date().toLocaleString();

        if (isSubItem && request.items) {
             const updatedItems = request.items.map(it => {
                 if (it.description === itemDescription) {
                     return { ...it, status: 'Cancelado', removalReason: reason };
                 }
                 return it;
             });
             await updateDoc(doc(state.collections.partRequests, requestFbId), {
                 items: updatedItems,
                 lastModified: now
             });
        } else {
             await updateDoc(doc(state.collections.partRequests, requestFbId), {
                 status: 'Rechazado',
                 rejectionReason: `Cancelado por técnico en OT. Motivo: ${reason}`,
                 lastModified: now
             });
        }

        const obsField = document.getElementById('wo-observaciones');
        const removalNote = `\n[${timestamp}] Solicitud de "${itemDescription}" CANCELADA. Motivo: ${reason}`;
        obsField.value += removalNote;

        showToast('Solicitud cancelada.', 'info');
        renderWoPendingRequests();
    } catch (error) {
        console.error("Error removing request:", error);
        showToast('Error al cancelar la solicitud.', 'error');
    } finally {
        showLoading(false);
    }
}

function renderPartRequests() {
    if (state.currentTab !== 'solicitudes-repuestos' && initialLoadComplete) return;
    const list = document.getElementById('part-request-list');
    if (!list) return;
    const statusFilter = document.getElementById('part-request-status-filter').value;
    const monthFilter = document.getElementById('part-request-month-filter').value;

    list.innerHTML = '';
    const user = state.currentUser;
    let filtered = state.partRequests.filter(r => statusFilter === 'all' || r.status === statusFilter);

    if (monthFilter) {
        filtered = filtered.filter(r => {
            if (!r.date) return false;
            return r.date.substring(0, 7) === monthFilter;
        });
    }

    // Filtrar por solicitante para Operarios y Jefes de Área
    if (user && user.role !== 'Admin' && user.role !== 'Planificador') {
        filtered = filtered.filter(r => r.requester === user.username);
    }

    filtered.sort((a, b) => new Date(b.date) - new Date(a.date)).forEach(r => {
        const tr = document.createElement('tr');
        const date = new Date(r.date);

        let statusClass = 'bg-secondary';
        let statusLabel = r.status;

        if (r.status === 'Pendiente') {
            statusLabel = 'Solicitado';
        }
        if (r.status === 'Pedido' || r.status === 'Aprobado') {
            statusClass = 'bg-info text-white';
            statusLabel = 'Aprobado';
        }
        if (r.status === 'En Despacho') {
            statusClass = 'bg-warning text-dark';
            statusLabel = 'En Despacho';
        }
        if (r.status === 'Recibido' || r.status === 'Entregado') {
            statusClass = 'bg-success';
            statusLabel = 'Entregado';
        }
        if (r.status === 'Rechazado') {
            statusClass = 'bg-danger';
            statusLabel = 'Rechazado';
        }

        let urgencyClass = 'text-muted';
        if (r.urgency === 'Alta') urgencyClass = 'text-danger fw-bold';
        if (r.urgency === 'Media') urgencyClass = 'text-warning fw-bold';

        let actions = '';
        const userRole = getCurrentUser().role;
        const isApprover = ['Admin', 'Planificador', 'Jefe de Area'].includes(userRole);
        const isWarehouse = ['Admin', 'Planificador', 'Almacén'].includes(userRole);
        const isRequester = getCurrentUser().username === r.requester;

        if (r.status === 'Pendiente' && isApprover) {
            actions += `<button class="btn btn-xs btn-outline-info me-1" onclick="updatePartRequestStatus('${r.fb_id}', 'Aprobado')"><i class="fas fa-check me-1"></i>Aprobar</button>`;
            actions += `<button class="btn btn-xs btn-outline-danger me-1" onclick="updatePartRequestStatus('${r.fb_id}', 'Rechazado')"><i class="fas fa-times me-1"></i>Rechazar</button>`;
        } else if (r.status === 'Aprobado' && isWarehouse) {
            actions += `<button class="btn btn-xs btn-outline-warning me-1" onclick="updatePartRequestStatus('${r.fb_id}', 'En Despacho')"><i class="fas fa-truck me-1"></i>Despachar</button>`;
        } else if (r.status === 'En Despacho' && isWarehouse) {
            actions += `<button class="btn btn-xs btn-success me-1" onclick="showDeliveryModal('${r.fb_id}')"><i class="fas fa-box-open me-1"></i>Entregar</button>`;
        }

        if (r.status === 'En Despacho' && (isRequester || isApprover)) {
             actions += `<button class="btn btn-xs btn-outline-primary me-1" onclick="showConfirmReceiptModal('${r.fb_id}')"><i class="fas fa-check-double me-1"></i>Recibir</button>`;
        }

        if (r.status === 'Recibido' || r.status === 'Entregado') {
             actions += `<button class="btn btn-xs btn-outline-danger me-1" onclick="generatePartRequestGroupPDF('${r.fb_id}')"><i class="fas fa-file-pdf me-1"></i>PDF</button>`;
        }

        let description = r.description;
        let qtyDisplay = r.deliveredQuantity > 0 ? `${r.deliveredQuantity} / ${r.quantity}` : r.quantity;

        if (r.items && Array.isArray(r.items)) {
            description = r.items.map(i => i.description).join(', ');
            if (description.length > 50) description = description.substring(0, 47) + '...';

            const totalQty = r.items.reduce((sum, i) => sum + i.quantity, 0);
            const totalDelivered = r.items.reduce((sum, i) => sum + (i.deliveredQuantity || 0), 0);
            qtyDisplay = totalDelivered > 0 ? `${totalDelivered} / ${totalQty}` : totalQty;
        }

        tr.innerHTML = `
            <td class="fw-bold">${r.id || '---'}</td>
            <td>${date.toLocaleDateString()}</td>
            <td>${r.workOrderId || 'N/A'}</td>
            <td>${description}</td>
            <td>${qtyDisplay}</td>
            <td class="${urgencyClass}">${r.urgency}</td>
            <td>${r.requester}</td>
            <td><span class="badge ${statusClass}">${statusLabel}</span></td>
            <td class="text-center">${actions}</td>
        `;
        list.appendChild(tr);
    });
}

window.updatePartRequestStatus = async function(fbId, newStatus) {
    const request = state.partRequests.find(r => r.fb_id === fbId);
    if (!request) return;

    if (newStatus === 'Rechazado') {
        const reason = prompt('Por favor, ingrese el motivo del rechazo:');
        if (!reason) return;

        showLoading(true);
        try {
            const docRef = doc(state.collections.partRequests, fbId);
            const updates = {
                status: 'Rechazado',
                rejectedBy: getCurrentUser().username,
                rejectionReason: reason,
                processedDate: new Date().toISOString()
            };
            if (request.items) {
                updates.items = request.items.map(item => ({ ...item, status: 'Rechazado' }));
            }
            await updateDoc(docRef, updates);
            await updateRTDBMirror('partRequests', { ...request, ...updates }, fbId);
            showToast('Solicitud rechazada.', 'info');
            return;
        } catch (e) {
            console.error(e);
            showToast('Error al rechazar.', 'error');
            return;
        } finally {
            showLoading(false);
        }
    }

    showLoading(true);
    try {
        const docRef = doc(state.collections.partRequests, fbId);
        const oldRequest = JSON.parse(JSON.stringify(request));
        const updates = {
            status: newStatus,
            processedBy: getCurrentUser().username,
            processedDate: new Date().toISOString()
        };

        if (newStatus === 'Aprobado') {
            updates.approvedBy = getCurrentUser().username;
            updates.approvedSignature = state.currentUser.signature || null;
            if (request.items) {
                updates.items = request.items.map(item => ({ ...item, status: 'Aprobado' }));
            }
        }

        if (newStatus === 'En Despacho') {
            if (request.items) {
                updates.items = request.items.map(item => ({ ...item, status: 'En Despacho' }));
            }
        }

        await updateDoc(docRef, updates);
        await updateRTDBMirror('partRequests', { ...request, ...updates }, fbId);
        const detailedAction = updates.status ? `Solicitud Repuesto: ${updates.status}` : 'Actualización Solicitud Repuesto';
        await recordAuditLog('partRequests', request.workOrderId || fbId, 'UPDATE', oldRequest, { ...request, ...updates }, detailedAction);

        if (newStatus === 'Recibido') {
            await handlePartRequestReceived(request);
        }

        let displayStatus = newStatus;
        if (newStatus === 'Pedido' || newStatus === 'Aprobado') displayStatus = 'Aprobada';
        if (newStatus === 'Recibido') displayStatus = 'Entregada';
        showToast(`Solicitud marcada como ${displayStatus}.`, 'success');
    } catch (error) {
        console.error("Error updating part request:", error);
        showToast('Error al actualizar el estado.', 'error');
    } finally {
        showLoading(false);
    }
};

window.showConfirmReceiptModal = function(fbId) {
    document.getElementById('receipt-request-id').value = fbId;
    document.getElementById('receipt-password').value = '';
    state.modals.confirmReceipt.show();
};

async function handleConfirmReceiptSubmit() {
    const fbId = document.getElementById('receipt-request-id').value;
    const request = state.partRequests.find(r => r.fb_id === fbId);
    if (!request) return;

    // Usar el flujo estándar de firma electrónica
    requestSignature(async () => {
        showLoading(true);
        try {
            const docRef = doc(state.collections.partRequests, fbId);
            const updates = {
                status: 'Recibido',
                receivedBy: getCurrentUser().username,
                receivedSignature: state.currentUser.signature || null,
                receivedDate: new Date().toISOString()
            };

            if (request.items) {
                updates.items = request.items.map(item => ({ ...item, status: 'Recibido' }));
            }

            await updateDoc(docRef, updates);
            await updateRTDBMirror('partRequests', { ...request, ...updates }, fbId);
            await handlePartRequestReceived({ ...request, ...updates });

            state.modals.confirmReceipt.hide();
            showToast('Recepción confirmada y firmada electrónicamente.', 'success');
        } catch (e) {
            console.error(e);
            showToast('Error al confirmar recepción.', 'error');
        } finally {
            showLoading(false);
        }
    });
}

async function handlePartRequestReceived(request) {
    // Si la solicitud está vinculada a una OT, marcar los repuestos como entregados en la OT
    if (request.workOrderFbId) {
        const wo = state.workOrders.find(o => o.fb_id === request.workOrderFbId);
        if (wo) {
            const receivedItems = request.items || [{ description: request.description, quantity: request.quantity }];
            const updatedPartsUsed = (wo.partsUsed || []).map(p => {
                const match = receivedItems.find(ri =>
                    (ri.partId === p.partId || ri.description === p.description) &&
                    (ri.status === 'Recibido' || ri.status === 'Entregado')
                );
                if (match) return { ...p, delivered: true };
                return p;
            });

            await updateDoc(doc(state.collections.workOrders, wo.fb_id), { partsUsed: updatedPartsUsed });
            await updateRTDBMirror('workOrders', { ...wo, partsUsed: updatedPartsUsed }, wo.fb_id);
            console.log("OT actualizada: Repuestos marcados como entregados.");
        }
    }

    // Registrar el movimiento en el Kardex pero NO incrementar stock global
    // (el stock ya fue descontado por el almacén al entregar)
    const itemsToProcess = request.items || [{
        description: request.description,
        quantity: request.quantity
    }];

    for (const item of itemsToProcess) {
        let part = state.parts.find(p => (p.id === item.partId) || (p.description.toLowerCase() === item.description.toLowerCase()));
        if (part) {
            await recordPartMovement(part.id, 0, 'Confirmación Recepción', {
                details: `Recepción confirmada por ${request.receivedBy}. Solicitud OT: ${request.workOrderId}`,
                orderId: request.workOrderFbId,
                orderHumanId: request.workOrderId
            });
        }
    }
}

async function loadPartMovements(partId = null) {
    const list = document.getElementById('part-history-list');
    if (list) {
        list.innerHTML = '<tr><td colspan="5" class="text-center text-muted"><i class="fas fa-spinner fa-spin"></i> Cargando movimientos...</td></tr>';
    }

    try {
        let q;
        if (partId) {
            // REMOVED orderBy to avoid requiring composite index in Firebase
            q = query(state.collections.partMovements, where("partId", "==", partId), limit(300));
        } else {
            q = query(state.collections.partMovements, orderBy("date", "desc"), limit(300));
        }
        const snapshot = await getDocs(q);
        let movements = snapshot.docs.map(doc => ({ fb_id: doc.id, ...doc.data() }));

        // Sort in memory for part-specific query
        if (partId) {
            movements.sort((a, b) => new Date(b.date) - new Date(a.date));
        }

        state.partMovements = movements;
        renderPartHistory();
    } catch (error) {
        console.error("Error loading part movements:", error);
        showToast("Error al cargar historial de movimientos", "error");
    }
}

function renderPartHistory() {
    const list = document.getElementById('part-history-list');
    if (!list) return;
    list.innerHTML = '';

    const partId = state.currentPartIdForHistory;
    const movements = state.partMovements
        .filter(m => !partId || m.partId === partId)
        .sort((a, b) => new Date(b.date) - new Date(a.date));

    movements.forEach(m => {
        const tr = document.createElement('tr');
        const date = new Date(m.date);
        const qtyClass = m.quantity < 0 ? 'text-danger' : (m.quantity > 0 ? 'text-success' : 'text-muted');
        const stockBefore = m.stockBefore ?? '-';
        const stockAfter = m.stockAfter ?? '-';
        const movementValue = Number(m.movementValue || 0);
        const reference = m.orderHumanId || m.reference || 'N/A';
        const material = partId ? (m.partDescription || m.partId) : `${m.partDescription || 'Material'} (${m.partId})`;
        tr.innerHTML = `
            <td>${date.toLocaleDateString()} ${date.toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})}</td>
            <td><span class="badge bg-light text-dark border font-monospace">${reference}</span></td>
            <td>${escapeHTML(material)}</td>
            <td class="${qtyClass} fw-bold">${m.quantity > 0 ? '+' : ''}${m.quantity}</td>
            <td><span class="text-muted">${stockBefore}</span> <i class="fas fa-arrow-right mx-1 text-muted"></i> <span class="fw-bold">${stockAfter}</span></td>
            <td>${movementValue ? formatCurrency(movementValue) : '-'}</td>
            <td>${m.type}</td>
            <td>${m.user}</td>
            <td class="text-muted small">${escapeHTML(m.details || m.reason || '')}</td>
        `;
        list.appendChild(tr);
    });
}

function renderPartDocsList() {
    const list = document.getElementById('part-docs-list');
    list.innerHTML = '';
    currentPartDocs.forEach((doc, index) => {
        const item = document.createElement('div');
        item.className = 'tech-doc-item d-flex align-items-center justify-content-between p-2 rounded-3 bg-white shadow-sm';
        item.innerHTML = `
            <div class="d-flex align-items-center">
                <i class="fas fa-file-pdf text-danger me-3"></i>
                <span class="small fw-medium text-truncate" style="max-width: 200px;">${doc.name}</span>
            </div>
            <button type="button" class="btn btn-sm text-danger remove-doc-btn" data-index="${index}">
                <i class="fas fa-trash-alt"></i>
            </button>
        `;
        list.appendChild(item);
    });

    list.querySelectorAll('.remove-doc-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            currentPartDocs.splice(btn.dataset.index, 1);
            renderPartDocsList();
        });
    });
}

function showPartModal(partId = null) {
    const form = document.getElementById('part-form');
    form.reset();
    populateSupplierSelectors();
    const idInput = document.getElementById('part-id');

    // Reset image and docs
    const preview = document.getElementById('part-image-preview');
    preview.src = '';
    preview.classList.add('d-none');
    document.querySelector('#part-modal .upload-placeholder').classList.remove('d-none');
    document.getElementById('part-image-url').value = '';
    document.getElementById('part-image-file').value = '';
    currentPartDocs = [];
    renderPartDocsList();

    const machinesListContainer = document.getElementById('part-machines-list');
    machinesListContainer.innerHTML = '';
    const availableMachines = state.machines
            .filter(machine => machine.status !== 'DADO_DE_BAJA')
            .sort((a, b) => {
                const categoryCompare = getAssetCategoryInfo(a).label.localeCompare(getAssetCategoryInfo(b).label);
                if (categoryCompare !== 0) return categoryCompare;
                return (a.name || '').localeCompare(b.name || '');
            });
    if (availableMachines.length > 0) {
        availableMachines.forEach(machine => {
            const categoryInfo = getAssetCategoryInfo(machine);
            const div = document.createElement('div');
            div.className = 'form-check py-1 border-bottom';
            div.innerHTML = `
                <input class="form-check-input part-machine-check" type="checkbox" value="${machine.id}" id="part-machine-check-${machine.id}">
                <label class="form-check-label small" for="part-machine-check-${machine.id}">
                    <span class="fw-bold">${machine.name}</span> (${machine.id})
                    <span class="badge ${categoryInfo.class} ms-1">${categoryInfo.label}</span>
                    <span class="text-muted ms-1">${getAssetTypeLabel(machine.type)}</span>
                </label>
            `;
            machinesListContainer.appendChild(div);
        });
    } else {
        machinesListContainer.innerHTML = '<p class="text-muted small">No hay equipos registrados.</p>';
    }
    
    if (partId) {
        document.getElementById('part-modal-title').textContent = 'Editar Repuesto';
        document.getElementById('part-stock').setAttribute('readonly', true);
        document.getElementById('btn-adjust-stock').classList.remove('d-none');
        const part = state.parts.find(p => p.id === partId);
        if (part) {
            document.getElementById('part-id-hidden').value = part.id;
            idInput.value = part.id;
            document.getElementById('part-description').value = part.description;
            document.getElementById('part-classification').value = part.classification || 'insumo';
            document.getElementById('part-supplier').value = part.supplierId || '';
            
            const machineIdsToSelect = part.machineIds || (part.machineId ? [part.machineId] : []);
            machineIdsToSelect.forEach(machineId => {
                const checkbox = document.getElementById(`part-machine-check-${machineId}`);
                if (checkbox) checkbox.checked = true;
            });

            document.getElementById('part-cost').value = part.cost;
            document.getElementById('part-stock').value = part.stockActual;
            document.getElementById('part-criticidad').value = part.criticality || 'Bajo';
            document.getElementById('part-minStock').value = part.minStock || 0;
            document.getElementById('part-maxStock').value = part.maxStock || 0;
            document.getElementById('part-reorderPoint').value = part.reorderPoint || 0;
            document.getElementById('part-uom').value = part.uom || 'Unidades';
            document.getElementById('part-abc').value = part.abc || 'C';
            document.getElementById('part-location').value = part.location || '';

            document.getElementById('part-image-url').value = part.imageUrl || '';
            if (part.imageUrl) {
                preview.src = part.imageUrl;
                preview.classList.remove('d-none');
                document.querySelector('#part-modal .upload-placeholder').classList.add('d-none');
            }

            currentPartDocs = Array.isArray(part.technicalDocs) ? [...part.technicalDocs] : [];
            renderPartDocsList();

            idInput.setAttribute('readonly', true);
        }
    } else {
        document.getElementById('part-modal-title').textContent = 'Añadir Repuesto';
        document.getElementById('part-id-hidden').value = '';
        idInput.removeAttribute('readonly');
        document.getElementById('part-stock').removeAttribute('readonly');
        document.getElementById('btn-adjust-stock').classList.add('d-none');
        document.getElementById('part-classification').value = 'insumo';
    }
    state.modals.part.show();
}

async function handlePartSubmit(e) {
    e.preventDefault();
    const originalId = document.getElementById('part-id-hidden').value;
    const partId = document.getElementById('part-id').value.trim();

    if(!partId) {
        showToast('El ID del repuesto no puede estar vacío.', 'error');
        return;
    }

    const selectedMachineIds = [];
    document.querySelectorAll('#part-machines-list .part-machine-check:checked').forEach(checkbox => {
        selectedMachineIds.push(checkbox.value);
    });

    const imageFile = document.getElementById('part-image-file').files[0];
    let imageUrl = document.getElementById('part-image-url').value;

    try {
        showLoading(true, 'Guardando repuesto...');

        if (imageFile) {
            imageUrl = await uploadPartFile(partId, imageFile, 'img');
        }

        // Subir nuevas fichas técnicas
        const updatedDocs = [];
        for (const doc of currentPartDocs) {
            if (doc.file) {
                const url = await uploadPartFile(partId, doc.file, 'doc');
                updatedDocs.push({ name: doc.name, url: url });
            } else {
                updatedDocs.push(doc);
            }
        }

        const partData = {
            lastModified: new Date().toISOString(),
            id: partId,
            description: document.getElementById('part-description').value,
            classification: document.getElementById('part-classification').value,
            supplierId: document.getElementById('part-supplier').value,
            machineIds: selectedMachineIds,
            cost: parseFloat(document.getElementById('part-cost').value) || 0,
            stockActual: parseInt(document.getElementById('part-stock').value) || 0,
            criticality: document.getElementById('part-criticidad').value,
            minStock: parseInt(document.getElementById('part-minStock').value) || 0,
            maxStock: parseInt(document.getElementById('part-maxStock').value) || 0,
            reorderPoint: parseInt(document.getElementById('part-reorderPoint').value) || 0,
            uom: document.getElementById('part-uom').value,
            abc: document.getElementById('part-abc').value,
            location: document.getElementById('part-location').value,
            imageUrl: imageUrl,
            technicalDocs: updatedDocs
        };

        const docRef = doc(state.collections.parts, partData.id);
        const oldPart = state.parts.find(p => p.fb_id === partData.id) || (originalId ? state.parts.find(p => p.fb_id === originalId) : null);
        const oldStock = oldPart ? (oldPart.stockActual || 0) : 0;
        const stockDelta = partData.stockActual - oldStock;

        await setDoc(docRef, partData, { merge: true });

        if (stockDelta !== 0) {
            await recordPartMovement(partData.id, stockDelta, 'Ajuste Manual');
        }

        if (originalId && originalId !== partData.id) {
             await deleteDoc(doc(state.collections.parts, originalId));
             await recordAuditLog('parts', partData.id, 'CREATE', null, partData, 'Cambio ID Repuesto');
             await recordAuditLog('parts', originalId, 'DELETE', oldPart, null, 'Eliminación por cambio ID');
        } else if (!originalId) {
             await recordAuditLog('parts', partData.id, 'CREATE', null, partData, 'Creación Repuesto');
        } else {
             await recordAuditLog('parts', partData.id, 'UPDATE', oldPart, partData, 'Modificación Repuesto');
        }

        if (state.odoo) {
            syncPartStockToOdoo(partData.id, partData).catch(err => {
                console.warn("[Odoo Background Sync] Error:", err);
            });
        }

        showToast('Repuesto guardado correctamente', 'success');
        state.modals.part.hide();
    } catch (error) { 
        console.error("Error saving part: ", error);
        showToast('Error al guardar el repuesto: ' + error.message, 'error');
    } finally {
        showLoading(false);
    }
}

function deletePart(partId) {
    showToast('La eliminación de registros no está permitida por políticas de trazabilidad (ISO 55000 / 21 CFR Part 11).', 'warning');
}

async function addToPurchaseCart(partId) {
    const part = state.parts.find(p => p.id === partId);
    if (!part) return;

    if (state.purchaseCart.find(i => i.partId === partId)) {
        showToast('El repuesto ya está en el carrito.', 'warning');
        return;
    }

    try {
        showLoading(true, 'Agregando al carrito...');
        const cartRef = doc(state.collections.purchaseCart, partId);
        await setDoc(cartRef, {
            partId: part.id,
            description: part.description,
            quantity: 1,
            cost: part.cost || 0,
            addedBy: getCurrentUser().username,
            addedAt: new Date().toISOString()
        });
        showToast('Repuesto agregado al carrito.', 'success');
    } catch (error) {
        console.error("Error adding to purchase cart:", error);
        showToast('Error al agregar al carrito.', 'error');
    } finally {
        showLoading(false);
    }
}

// --- CRUD Proveedor Functions ---
function renderProveedores() {
    const tbody = document.getElementById('proveedor-list');
    const searchTerm = document.getElementById('search-proveedor-input').value.toLowerCase();
    tbody.innerHTML = '';
    
    let proveedoresToRender = state.proveedores;
    const userRole = getCurrentUser().role;
    const managedMachineIds = getCurrentUser().managedMachineIds;
    const equipoAsignado = getCurrentUser().equipoAsignado;

    if ((userRole === 'Jefe de Area' && Array.isArray(managedMachineIds)) || ((userRole === 'Técnico' || (['Operario', 'Supervisor de Area'].includes(userRole))) && Array.isArray(equipoAsignado))) {
        const relevantMachineIds = new Set(userRole === 'Jefe de Area' ? managedMachineIds : equipoAsignado);

        // Find all parts associated with the managed machines
        const relevantParts = state.parts.filter(p => {
            const machineIds = p.machineIds || [];
            return machineIds.some(mId => relevantMachineIds.has(mId));
        });

        // Get the unique supplier IDs from those parts
        const relevantSupplierIds = new Set(relevantParts.map(p => p.supplierId).filter(Boolean));

        // Filter the suppliers list
        proveedoresToRender = state.proveedores.filter(prov => relevantSupplierIds.has(prov.id));
    }

    const filtered = proveedoresToRender.filter(p => p.id.toLowerCase().includes(searchTerm) || p.nombre.toLowerCase().includes(searchTerm));
    
    if (filtered.length === 0) {
        tbody.innerHTML = '<tr><td colspan="7" class="text-center">No se encontraron proveedores.</td></tr>';
        return;
    }

    const fragment = document.createDocumentFragment();
    filtered.forEach(p => {
        const tr = document.createElement('tr');
        tr.id = `proveedor-row-${p.fb_id}`;
        tr.innerHTML = createProveedorRowHTML(p);
        fragment.appendChild(tr);
    });
    tbody.appendChild(fragment);

    tbody.querySelectorAll('.edit-proveedor').forEach(btn => btn.addEventListener('click', () => showProveedorModal(btn.dataset.id)));
    tbody.querySelectorAll('.delete-proveedor').forEach(btn => btn.addEventListener('click', () => deleteProveedor(btn.dataset.id)));
}

function addOrUpdateProveedorInTable(proveedor) {
    const tbody = document.getElementById('proveedor-list');
    let tr = document.getElementById(`proveedor-row-${proveedor.fb_id}`);

    if (tr) { // Update
        tr.innerHTML = createProveedorRowHTML(proveedor);
    } else { // Add
        const placeholder = tbody.querySelector('td[colspan="7"]');
        if (placeholder) placeholder.parentElement.remove();
        tr = document.createElement('tr');
        tr.id = `proveedor-row-${proveedor.fb_id}`;
        tr.innerHTML = createProveedorRowHTML(proveedor);
        tbody.appendChild(tr);
    }
    tr.querySelector('.edit-proveedor').addEventListener('click', () => showProveedorModal(tr.querySelector('.edit-proveedor').dataset.id));
    tr.querySelector('.delete-proveedor').addEventListener('click', () => deleteProveedor(tr.querySelector('.delete-proveedor').dataset.id));
}

function showProveedorModal(proveedorId = null) {
    const form = document.getElementById('proveedor-form');
    form.reset();
    const idInput = document.getElementById('proveedor-id');

    if (proveedorId) {
        document.getElementById('proveedor-modal-title').textContent = 'Editar Proveedor';
        const proveedor = state.proveedores.find(p => p.id === proveedorId);
        if (proveedor) {
            document.getElementById('proveedor-id-hidden').value = proveedor.id;
            idInput.value = proveedor.id;
            document.getElementById('proveedor-nombre').value = proveedor.nombre;
            document.getElementById('proveedor-rtn').value = proveedor.rtn || '';
            document.getElementById('proveedor-telefono').value = proveedor.telefono || '';
            document.getElementById('proveedor-email').value = proveedor.email || '';
            document.getElementById('proveedor-direccion').value = proveedor.direccion || '';
            idInput.setAttribute('readonly', true);
        }
    } else {
        document.getElementById('proveedor-modal-title').textContent = 'Añadir Proveedor';
        document.getElementById('proveedor-id-hidden').value = '';
        idInput.removeAttribute('readonly');
    }
    state.modals.proveedor.show();
}

async function handleProveedorSubmit(e) {
    e.preventDefault();
    const originalId = document.getElementById('proveedor-id-hidden').value;

    const proveedorData = {
        id: document.getElementById('proveedor-id').value.trim(),
        nombre: document.getElementById('proveedor-nombre').value,
        rtn: document.getElementById('proveedor-rtn').value,
        telefono: document.getElementById('proveedor-telefono').value,
        email: document.getElementById('proveedor-email').value,
        direccion: document.getElementById('proveedor-direccion').value,
    };

    if (!proveedorData.id) {
        showToast('El Código del proveedor no puede estar vacío.', 'error');
        return;
    }

    try {
        const oldProv = state.proveedores.find(p => p.fb_id === proveedorData.id) || (originalId ? state.proveedores.find(p => p.fb_id === originalId) : null);
        const docRef = doc(state.collections.proveedores, proveedorData.id);
        await setDoc(docRef, proveedorData, { merge: true });

        if (originalId && originalId !== proveedorData.id) {
             await deleteDoc(doc(state.collections.proveedores, originalId));
             await recordAuditLog('proveedores', proveedorData.id, 'CREATE', null, proveedorData);
             await recordAuditLog('proveedores', originalId, 'DELETE', oldProv, null);
        } else if (!originalId) {
             await recordAuditLog('proveedores', proveedorData.id, 'CREATE', null, proveedorData);
        } else {
             await recordAuditLog('proveedores', proveedorData.id, 'UPDATE', oldProv, proveedorData);
        }
        showToast('Proveedor guardado correctamente', 'success');
    } catch (error) {
        console.error("Error saving proveedor: ", error);
        showToast('Error al guardar el proveedor', 'error');
    }
    state.modals.proveedor.hide();
}

function deleteProveedor(proveedorId) {
    showConfirmation('Eliminar Proveedor', `¿Está seguro de que desea eliminar el proveedor ${proveedorId}`, async () => {
        try {
            const oldProv = state.proveedores.find(p => p.fb_id === proveedorId);
            await deleteDoc(doc(state.collections.proveedores, proveedorId));
            await recordAuditLog('proveedores', proveedorId, 'DELETE', oldProv, null);
            showToast('Proveedor eliminado correctamente', 'success');
        } catch (error) {
            console.error("Error deleting proveedor: ", error);
            showToast('Error al eliminar el proveedor', 'error');
        }
    });
}

// --- NEW: Centralized function to update the user modal UI based on role ---
function updateTechnicianModalUIForRole(role) {
    const salaryWrapper = document.getElementById('technician-salary-wrapper');
    const salaryInput = document.getElementById('technician-salary');
    const permissionsWrapper = document.getElementById('technician-permissions-wrapper');
    const jefeMaquinasWrapper = document.getElementById('jefe-maquinas-wrapper');
    const jefePermissionsWrapper = document.getElementById('jefe-permissions-wrapper');
    const tecnicoMaquinasWrapper = document.getElementById('tecnico-maquinas-wrapper');

    // Visibilidad de permisos por rol
    permissionsWrapper.style.display = role === 'Técnico' ? 'block' : 'none';
    jefePermissionsWrapper.classList.toggle('d-none', role !== 'Jefe de Area');

    // Visibilidad de asignación de máquinas por rol
    const showTecnicoMaquinas = role === 'Técnico' || role === 'Operario' || role === 'Supervisor de Area' || role === 'Admin' || role === 'Planificador';
    tecnicoMaquinasWrapper.classList.toggle('d-none', !showTecnicoMaquinas);

    // Ocultar botón de seleccionar todas para Operario y Supervisor en la lista de técnico
    const toggleTecnicoBtn = tecnicoMaquinasWrapper.querySelector('.toggle-all-machines');
    if (toggleTecnicoBtn) {
        toggleTecnicoBtn.classList.toggle('d-none', role === 'Operario' || role === 'Supervisor de Area');
    }

    // Visibilidad específica para Jefe de Area
    jefeMaquinasWrapper.classList.toggle('d-none', role !== 'Jefe de Area');


    const generaGasto = document.getElementById('technician-genera-gasto').checked;
    if (role === 'Operario' || role === 'Supervisor de Area' || !generaGasto) {
        salaryWrapper.classList.add('d-none');
        salaryInput.required = false;
    } else {
        salaryWrapper.classList.remove('d-none');
        salaryInput.required = true;
    }
}

// --- CRUD Technician/User Functions ---
function renderTechnicians() {
    const tbody = document.getElementById('technician-list');
    tbody.innerHTML = '';
    if (state.technicians.length === 0) {
        tbody.innerHTML = '<tr><td colspan="4" class="text-center">No se encontraron usuarios.</td></tr>';
        return;
    }

    state.technicians.forEach(tech => {
        const tr = document.createElement('tr');
        if (tech.resetRequested) {
            tr.classList.add('reset-requested-row');
        }
        const isCurrentUser = state.currentUser && getCurrentUser().username === tech.username;

        let actionsHTML = '';
        if (getCurrentUser().role === 'Admin') {
            actionsHTML += `<button class="btn btn-sm btn-outline-primary edit-tech" data-id="${tech.fb_id}" title="Editar"><i class="fas fa-edit"></i></button>`;

            const isActive = tech.isActive !== false;
            actionsHTML += `<button class="btn btn-sm ${isActive ? 'btn-outline-warning' : 'btn-outline-success'} toggle-tech-status" data-id="${tech.fb_id}" title="${isActive ? 'Desactivar' : 'Activar'}">
                <i class="fas ${isActive ? 'fa-user-slash' : 'fa-user-check'}"></i>
            </button>`;

            if (tech.resetRequested) {
                actionsHTML += `<button class="btn btn-sm btn-warning" onclick="generateTokenLink('${tech.fb_id}', 'reset')" title="Generar Link de Recuperación"><i class="fas fa-key"></i></button>`;
            } else if (!tech.password) {
                actionsHTML += `<button class="btn btn-sm btn-success" onclick="generateTokenLink('${tech.fb_id}', 'invitation')" title="Generar Link de Invitación"><i class="fas fa-envelope"></i></button>`;
            }

            actionsHTML += `<button class="btn btn-sm btn-outline-danger delete-tech" data-id="${tech.fb_id}" ${isCurrentUser ? 'disabled' : ''} title="Eliminar"><i class="fas fa-trash"></i></button>`;
        }

        if (['Técnico', 'Admin', 'Planificador'].includes(tech?.role)) {
            actionsHTML += `<button class="btn btn-sm btn-outline-info view-profile-btn" data-id="${tech.fb_id}" title="Ver Perfil"><i class="fas fa-user-chart"></i></button>`;
        }

        let resetBadge = tech.resetRequested ? '<span class="badge bg-warning text-dark ms-1">Reset solicitado</span>' : '';

        tr.innerHTML = `
            <td>${tech.username}${resetBadge}</td>
            <td>${tech.role}</td>
            <td>${tech.generaGasto !== false && tech.salario ? new Intl.NumberFormat('es-HN', { style: 'currency', currency: 'HNL' }).format(tech.salario) : 'N/A'}</td>
            <td>
                <div class="d-flex justify-content-center align-items-center gap-1">
                    ${actionsHTML || '<span class="text-muted fst-italic">Sin acciones</span>'}
                </div>
            </td>
        `;
        tbody.appendChild(tr);
    });

    // Delegated event listeners are used for edit/delete/profile, set up in setupEventListeners
}

async function handleViewLinkedPlan(workOrder, plan) {
    if (!workOrder || !plan) {
        showToast('Error: Faltan datos de la orden o del plan.', 'error');
        return;
    }
    document.activeElement.blur();

    let execution = state.workPlanExecutions.find(e => e.workOrderFbId === workOrder.fb_id);

    if (execution) {
        const readOnlyExecution = {
            ...JSON.parse(JSON.stringify(execution)),
            isReadOnly: true
        };
        startWorkPlanExecution(readOnlyExecution, plan);
    } else {
        const executionTaskGroups = (plan.taskGroups || []).map(group => ({
            ...group,
            tasks: group.tasks.map(task => ({ ...task, status: 'Pendiente' }))
        }));

        const mockExecution = {
            planId: plan.fb_id,
            workOrderFbId: workOrder.fb_id,
            executedAt: null,
            executedBy: workOrder.leadTechnician,
            taskGroups: executionTaskGroups,
            status: 'Pendiente',
            isReadOnly: true,
            isMock: true
        };
        startWorkPlanExecution(mockExecution, plan);
    }
    state.modals.workOrder.hide();
}

async function handleStartLinkedPlan(workOrder, plan) {
    if (workOrder && workOrder.isExpired) {
        showToast('Esta orden ha expirado y no puede ser modificada.', 'error');
        return;
    }
    if (!workOrder || !plan) {
        showToast('Error: Faltan datos de la orden o del plan.', 'error');
        return;
    }
    document.activeElement.blur();

    // First, check if an execution already exists for this work order.
    let execution = state.workPlanExecutions.find(e => e.workOrderFbId === workOrder.fb_id);

    if (execution) {
        // If it exists, just start the view.
        startWorkPlanExecution(execution, plan);
        state.modals.workOrder.hide();
    } else {
        // If it does not exist, create it.
        showLoading(true);
        try {
            const nowDate = new Date();
            const now = nowDate.toISOString();
            const executionTaskGroups = (plan.taskGroups || []).map(group => ({
                ...group,
                tasks: group.tasks.map(task => ({ ...task, status: 'Pendiente' }))
            }));

            const executionData = {
                planId: plan.fb_id,
                workOrderFbId: workOrder.fb_id,
                executedAt: now,
                executedBy: getCurrentUser().username,
                taskGroups: executionTaskGroups,
                status: 'En Proceso',
                workIntervals: [{ start: now, end: null }]
            };

            const executionDocRef = await addDoc(state.collections.workPlanExecutions, executionData);

            // Update the work order to link to the new execution ID, update ID prefix and status if needed
            const woUpdates = {
                executionId: executionDocRef.id,
            };

            // Only update status and start time if the order is not already in process
            if (workOrder.status !== 'En Proceso') {
                woUpdates.status = 'En Proceso';
                if (!workOrder.fechaInicioReal) {
                    woUpdates.fechaInicioReal = now;
                }
                woUpdates.date = `${nowDate.getFullYear()}-${String(nowDate.getMonth() + 1).padStart(2, '0')}-${String(nowDate.getDate()).padStart(2, '0')}`;

                let workIntervals = Array.isArray(workOrder.workIntervals) ? JSON.parse(JSON.stringify(workOrder.workIntervals)) : [];
                workIntervals.push({ start: now, end: null });
                woUpdates.workIntervals = workIntervals;
            }

            if (workOrder.id && workOrder.id.startsWith('MP')) {
                woUpdates.id = generateNextId('MA');
            }

            await updateDoc(doc(state.collections.workOrders, workOrder.fb_id), woUpdates);

            // The snapshot listener will update the local state. We can find the new execution there.
            // But to be faster, we can create a temporary object for the UI.
            const newExecutionForUI = { ...executionData, fb_id: executionDocRef.id };

            showToast('Checklist de plan iniciado. La orden ahora está En Proceso.', 'success');
            startWorkPlanExecution(newExecutionForUI, plan);
            state.modals.workOrder.hide();

        } catch (error) {
            console.error("Error creating plan execution from linked plan:", error);
            showToast('Error al iniciar el plan de trabajo.', 'error');
        } finally {
            showLoading(false);
        }
    }
}

function initSignaturePad() {
    const canvas = document.getElementById('signature-pad');
    if (!canvas) return;

    if (!state.signaturePad) {
        state.signaturePad = new SignaturePad(canvas, {
            backgroundColor: 'rgba(255, 255, 255, 0)',
            penColor: 'rgb(0, 0, 0)'
        });

        // Handle resizing
        const resizeCanvas = () => {
            const ratio = Math.max(window.devicePixelRatio || 1, 1);
            canvas.width = canvas.offsetWidth * ratio;
            canvas.height = canvas.offsetHeight * ratio;
            canvas.getContext("2d").scale(ratio, ratio);
            state.signaturePad.clear();
        };

        window.addEventListener("resize", resizeCanvas);
        resizeCanvas();
    }
}

function showTechnicianModal(techId = null) {
    const form = document.getElementById('technician-form');
    form.reset();
    const userInput = document.getElementById('technician-username');
    const passInput = document.getElementById('technician-password');
    const salaryInput = document.getElementById('technician-salary');
    const telegramIdInput = document.getElementById('technician-telegram-chat-id');
    const roleSelect = document.getElementById('technician-role');
    const isActiveToggle = document.getElementById('technician-is-active');
    const jefeMaquinasList = document.getElementById('jefe-maquinas-list');
    const tecnicoMaquinasList = document.getElementById('tecnico-maquinas-list');
    document.querySelectorAll('.permission-check, .jefe-permission-check').forEach(cb => cb.checked = false);
    jefeMaquinasList.innerHTML = ''; // Clear previous list
    tecnicoMaquinasList.innerHTML = ''; // Clear previous list
    isActiveToggle.checked = true; // Default to active for new users

    // Reset toggle all buttons text
    document.querySelectorAll('.toggle-all-machines').forEach(btn => btn.textContent = 'Seleccionar todas');

    // Reset signature UI
    document.getElementById('signature-draw-container').style.display = 'none';
    document.getElementById('signature-upload-container').style.display = 'none';
    document.getElementById('signature-preview-container').classList.add('d-none');
    document.getElementById('signature-preview').src = '';
    if (state.signaturePad) state.signaturePad.clear();

    // Populate the machine lists for Jefe de Area and Tecnico
    if (state.machines.length > 0) {
        state.machines.forEach(machine => {
            const div = document.createElement('div');
            div.className = 'form-check';
            div.innerHTML = `
                <input class="form-check-input machine-check" type="checkbox" value="${machine.id}" id="jefe-machine-check-${machine.id}">
                <label class="form-check-label" for="jefe-machine-check-${machine.id}">
                    ${machine.name} (${machine.id})
                </label>
            `;
            jefeMaquinasList.appendChild(div);

            const divTecnico = document.createElement('div');
            divTecnico.className = 'form-check';
            divTecnico.innerHTML = `
                <input class="form-check-input tecnico-machine-check" type="checkbox" value="${machine.id}" id="tecnico-machine-check-${machine.id}">
                <label class="form-check-label" for="tecnico-machine-check-${machine.id}">
                    ${machine.name} (${machine.id})
                </label>
            `;
            tecnicoMaquinasList.appendChild(divTecnico);
        });
    } else {
        const noMachinesText = '<p class="text-muted small">No hay máquinas registradas para asignar.</p>';
        jefeMaquinasList.innerHTML = noMachinesText;
        tecnicoMaquinasList.innerHTML = noMachinesText;
    }

    if (techId) {
        document.getElementById('technician-modal-title').textContent = 'Editar Usuario';
        const tech = state.technicians.find(t => t.fb_id === techId);
        if (tech) {
            document.getElementById('technician-id-hidden').value = tech.fb_id;
            userInput.value = tech.username;
            salaryInput.value = tech.salario || '';
            roleSelect.value = tech.role;
            document.getElementById('technician-telegram-chat-id').value = tech.telegramChatId || '';
            const generaGasto = tech.generaGasto !== false;
            document.getElementById('technician-genera-gasto').checked = generaGasto;
            document.getElementById('technician-salary-wrapper').classList.toggle('d-none', !generaGasto);
            salaryInput.required = generaGasto;

            isActiveToggle.checked = tech.isActive !== false;
            isActiveToggle.disabled = (state.currentUser && tech.username === getCurrentUser().username);

            userInput.setAttribute('readonly', true);
            passInput.removeAttribute('required');
            passInput.placeholder = "Dejar en blanco para no cambiar";
            if (tech.role === 'Técnico' || tech.role === 'Operario' || tech.role === 'Supervisor de Area' || tech.role === 'Admin' || tech.role === 'Planificador') {
                if (tech.role === 'Técnico' && tech.permissions) {
                    tech.permissions.forEach(perm => {
                        const cb = document.getElementById(`perm-${perm}`);
                        if(cb) cb.checked = true;
                    });
                }
                if (Array.isArray(tech.equipoAsignado)) {
                    tech.equipoAsignado.forEach(machineId => {
                        const checkbox = document.getElementById(`tecnico-machine-check-${machineId}`);
                        if (checkbox) checkbox.checked = true;
                    });
                }

                // Update toggle button text if all machines are checked
                const container = document.getElementById('tecnico-maquinas-list');
                const checkboxes = container.querySelectorAll('input[type="checkbox"]');
                if (checkboxes.length > 0 && Array.from(checkboxes).every(cb => cb.checked)) {
                    const btn = document.querySelector('.toggle-all-machines[data-target="tecnico-maquinas-list"]');
                    if (btn) btn.textContent = 'Deseleccionar todas';
                }
            }
            if (tech.role === 'Jefe de Area') {
                if (Array.isArray(tech.managedMachineIds)) {
                    tech.managedMachineIds.forEach(machineId => {
                        const checkbox = document.getElementById(`jefe-machine-check-${machineId}`);
                        if (checkbox) {
                            checkbox.checked = true;
                        }
                    });
                }

                // Update toggle button text if all machines are checked
                const container = document.getElementById('jefe-maquinas-list');
                const checkboxes = container.querySelectorAll('input[type="checkbox"]');
                if (checkboxes.length > 0 && Array.from(checkboxes).every(cb => cb.checked)) {
                    const btn = document.querySelector('.toggle-all-machines[data-target="jefe-maquinas-list"]');
                    if (btn) btn.textContent = 'Deseleccionar todas';
                }
                if (Array.isArray(tech.permissions)) {
                    tech.permissions.forEach(perm => {
                        const cb = document.getElementById(`jefe-perm-${perm}`);
                        if(cb) cb.checked = true;
                    });
                }
            }

            if (tech.signature) {
                document.getElementById('signature-preview').src = tech.signature;
                document.getElementById('signature-preview-container').classList.remove('d-none');
            }
        }
    } else {
        document.getElementById('technician-modal-title').textContent = 'Añadir Usuario';
        document.getElementById('technician-id-hidden').value = '';
        userInput.removeAttribute('readonly');
        passInput.removeAttribute('required'); // Permitir opcional para invitación
        passInput.placeholder = "Dejar en blanco para invitación";
        document.getElementById('technician-genera-gasto').checked = true;
        document.getElementById('technician-salary-wrapper').classList.remove('d-none');
        salaryInput.required = true;
    }

    // Centralized call to update UI based on the selected role
    updateTechnicianModalUIForRole(roleSelect.value);
    
    // Si generaGasto está apagado, forzar ocultar el salario (importante para el render inicial)
    const generaGasto = document.getElementById('technician-genera-gasto').checked;
    if (!generaGasto) {
        document.getElementById('technician-salary-wrapper').classList.add('d-none');
        document.getElementById('technician-salary').required = false;
    }

    // Acciones de Link de Acceso para Admins y usuarios existentes
    const linkActionContainer = document.getElementById('technician-link-action-container');
    const linkDisplay = document.getElementById('tech-modal-link-display');
    const generateLinkBtn = document.getElementById('tech-modal-generate-link');
    const linkInput = document.getElementById('tech-modal-link-input');
    const copyLinkBtn = document.getElementById('tech-modal-copy-link');

    if (getCurrentUser().role === 'Admin' && techId) {
        linkActionContainer.classList.remove('d-none');
        linkDisplay.classList.add('d-none');

        generateLinkBtn.onclick = async () => {
            const tech = state.technicians.find(t => t.fb_id === techId);
            const type = tech.resetRequested ? 'reset' : 'invitation';
            const link = await generateTokenLink(techId, type, false); // false para que solo devuelva el link
            linkInput.value = link;
            linkDisplay.classList.remove('d-none');
        };

        copyLinkBtn.onclick = () => {
            linkInput.select();
            document.execCommand('copy');
            showToast('Link copiado al portapapeles', 'success');
        };
    } else {
        linkActionContainer.classList.add('d-none');
    }

    state.modals.technician.show();
}

async function getTechnicianDataFromForm() {
    const role = document.getElementById('technician-role').value;
    const generaGasto = document.getElementById('technician-genera-gasto').checked;
    const isActive = document.getElementById('technician-is-active').checked;
    const password = document.getElementById('technician-password').value;

    const techData = {
        username: document.getElementById('technician-username').value.trim(),
        isActive: isActive,
        generaGasto: generaGasto,
        salario: (role === 'Operario' || role === 'Supervisor de Area' || !generaGasto) ? 0 : (parseFloat(document.getElementById('technician-salary').value) || 0),
        role: role,
        telegramChatId: document.getElementById('technician-telegram-chat-id').value.trim(),
        permissions: [],
        managedMachineIds: [],
    };

    if (role === 'Admin') {
        techData.permissions = ['all'];
        techData.equipoAsignado = [];
        document.querySelectorAll('#tecnico-maquinas-list .tecnico-machine-check:checked').forEach(cb => {
            techData.equipoAsignado.push(cb.value);
        });
    } else if (role === 'Planificador') {
        techData.permissions = ['maquinaria', 'repuestos', 'proveedores', 'trabajo-activo', 'planificador', 'planes-trabajo', 'reportes', 'solicitudes', 'ordenes-asignadas'];
        techData.equipoAsignado = [];
        document.querySelectorAll('#tecnico-maquinas-list .tecnico-machine-check:checked').forEach(cb => {
            techData.equipoAsignado.push(cb.value);
        });
    } else if (role === 'Jefe de Area') {
        document.querySelectorAll('#jefe-permissions .jefe-permission-check:checked').forEach(cb => {
            techData.permissions.push(cb.value);
        });
        document.querySelectorAll('#jefe-maquinas-list .machine-check:checked').forEach(cb => {
            techData.managedMachineIds.push(cb.value);
        });
    } else if (role === 'Invitado') {
        techData.permissions = ['read-only'];
    } else if (role === 'Operario' || role === 'Supervisor de Area') {
        techData.permissions = ['solicitudes', 'trabajo-activo', 'planificador'];
        techData.equipoAsignado = [];
        document.querySelectorAll('#tecnico-maquinas-list .tecnico-machine-check:checked').forEach(cb => {
            techData.equipoAsignado.push(cb.value);
        });
    } else if (role === 'Técnico') {
        techData.equipoAsignado = [];
        document.querySelectorAll('.permission-check:checked').forEach(cb => {
            techData.permissions.push(cb.value);
        });
        document.querySelectorAll('#tecnico-maquinas-list .tecnico-machine-check:checked').forEach(cb => {
            techData.equipoAsignado.push(cb.value);
        });
    }

    const techId = document.getElementById('technician-id-hidden').value;
    if (techId && !['Técnico', 'Operario', 'Supervisor de Area', 'Admin', 'Planificador'].includes(role)) {
        techData.equipoAsignado = deleteField();
    }

    if (password) {
        techData.password = await hashPassword(password);
        techData.passwordIsHashed = true;
        techData.invitationToken = deleteField();
        techData.tokenExpiry = deleteField();
        techData.resetRequested = deleteField();
    }

    const signatureDrawContainer = document.getElementById('signature-draw-container');
    const signaturePreview = document.getElementById('signature-preview');
    const signaturePreviewContainer = document.getElementById('signature-preview-container');

    if (signatureDrawContainer.style.display === 'block' && state.signaturePad && !state.signaturePad.isEmpty()) {
        techData.signature = state.signaturePad.toDataURL();
    } else if (!signaturePreviewContainer.classList.contains('d-none') && signaturePreview.src) {
        techData.signature = signaturePreview.src;
    }

    return techData;
}

async function handleTechnicianSubmit(e) {
    if (e) e.preventDefault();
    const techId = document.getElementById('technician-id-hidden').value;
    const techData = await getTechnicianDataFromForm();

    if (!techData.username) {
        showToast('El nombre de usuario es obligatorio.', 'warning');
        return;
    }
    
    try {
        let currentFbId = techId;
        const oldTech = techId ? state.technicians.find(t => t.fb_id === techId) : null;
        if (techId) {
            await setDoc(doc(state.collections.technicians, techId), techData, { merge: true });
            await recordAuditLog('technicians', techId, 'UPDATE', oldTech, techData);
            showToast('Usuario actualizado correctamente', 'success');
        } else {
            const docRef = await addDoc(state.collections.technicians, techData);
            currentFbId = docRef.id;
            document.getElementById('technician-id-hidden').value = currentFbId;
            await recordAuditLog('technicians', currentFbId, 'CREATE', null, techData);
            showToast('Usuario creado correctamente', 'success');
        }

        if (state.odoo) {
            syncTechnicianToOdoo(techData, currentFbId).catch(err => console.warn("[Odoo Sync] Error:", err));
        }
    } catch (error) { 
        console.error("Error saving technician: ", error);
        showToast('Error al guardar el usuario', 'error');
    }
    state.modals.technician.hide();
}

async function handleVerifyTelegramConnection() {
    const telegramId = document.getElementById('technician-telegram-chat-id').value.trim();
    if (!telegramId) {
        showToast('Ingrese un Telegram Chat ID primero.', 'warning');
        return;
    }

    const techData = await getTechnicianDataFromForm();
    if (!techData.username) {
        showToast('El nombre de usuario es obligatorio para la verificación.', 'warning');
        return;
    }

    const techId = document.getElementById('technician-id-hidden').value;

    try {
        let currentFbId = techId;
        const oldTech = techId ? state.technicians.find(t => t.fb_id === techId) : null;
        if (techId) {
            await setDoc(doc(state.collections.technicians, techId), techData, { merge: true });
            await recordAuditLog('technicians', techId, 'UPDATE', oldTech, techData);
        } else {
            const docRef = await addDoc(state.collections.technicians, techData);
            currentFbId = docRef.id;
            document.getElementById('technician-id-hidden').value = currentFbId;
            await recordAuditLog('technicians', currentFbId, 'CREATE', null, techData);
        }

        const welcomeMsg = `se agrego correctamente al chat bienvenido ${techData.username}`;
        const sent = await sendTelegramNotification(welcomeMsg, telegramId);

        if (sent) {
            showToast('Perfil guardado y mensaje de bienvenida enviado con éxito.', 'success');
        } else {
            showToast('Perfil guardado, pero falló el envío del mensaje a Telegram. Verifique el ID o la configuración del Bot.', 'warning');
        }

        if (state.odoo) {
            syncTechnicianToOdoo(techData, currentFbId).catch(err => console.warn("[Odoo Sync] Error:", err));
        }
    } catch (error) {
        console.error("Error verifying telegram:", error);
        showToast('Error al procesar la verificación', 'error');
    }
}

async function toggleTechnicianStatus(techId) {
    const tech = state.technicians.find(t => t.fb_id === techId);
    if (!tech) return;

    if (state.currentUser && tech.username === getCurrentUser().username) {
        showToast('No puede desactivar su propio usuario.', 'error');
        return;
    }

    const currentlyActive = tech.isActive !== false;
    const newStatus = !currentlyActive;

    if (!newStatus) {
        // Deactivating: check for pending/in-progress work orders
        const pendingWOs = state.workOrders.filter(wo =>
            wo.leadTechnician === tech.username &&
            (wo.status === 'Pendiente' || wo.status === 'En Proceso')
        );

        const confirmMsg = pendingWOs.length > 0 ? `¿Está seguro de que desea desactivar a ${tech.username}Tiene ${pendingWOs.length} órdenes de trabajo pendientes que quedarán SIN ASIGNAR.` : `¿Está seguro de que desea desactivar a ${tech.username}`;

        showConfirmation('Desactivar Usuario', confirmMsg, async () => {
            try {
                const batch = writeBatch(db);

                // Update Technician Status
                batch.update(doc(state.collections.technicians, techId), { isActive: false });

                // Unassign Work Orders
                pendingWOs.forEach(wo => {
                    const woRef = doc(state.collections.workOrders, wo.fb_id);
                    batch.update(woRef, {
                        leadTechnician: '',
                        technicians: [] // Or handle specificly if multiple techs
                    });
                    recordAuditLog('workOrders', wo.fb_id, 'UNASSIGNED_DUE_TO_DEACTIVATION', { oldLead: tech.username }, { leadTechnician: '' });
                });

                await batch.commit();
                await recordAuditLog('technicians', techId, 'DEACTIVATE', tech, { ...tech, isActive: false });
                showToast('Usuario desactivado correctamente', 'success');
            } catch (error) {
                console.error("Error deactivating technician:", error);
                showToast('Error al desactivar usuario', 'error');
            }
        });
    } else {
        // Activating
        try {
            await updateDoc(doc(state.collections.technicians, techId), { isActive: true });
            await recordAuditLog('technicians', techId, 'ACTIVATE', tech, { ...tech, isActive: true });
            showToast('Usuario activado correctamente', 'success');
        } catch (error) {
            console.error("Error activating technician:", error);
            showToast('Error al activar usuario', 'error');
        }
    }
}

function deleteTechnician(techId) {
     const tech = state.technicians.find(t => t.fb_id === techId);
     if (tech && state.currentUser && tech.username === getCurrentUser().username) {
          showToast('No puede eliminar el usuario con el que ha iniciado sesión.', 'error');
          return;
     }
     showConfirmation('Eliminar Usuario', `¿Está seguro de que desea eliminar a ${tech.username}`, async () => {
        try { 
            const oldTech = state.technicians.find(t => t.fb_id === techId);
            await deleteDoc(doc(state.collections.technicians, techId)); 
            await recordAuditLog('technicians', techId, 'DELETE', oldTech, null);
            showToast('Usuario eliminado correctamente', 'success');
        } catch (error) { 
            console.error("Error deleting technician: ", error);
            showToast('Error al eliminar el usuario', 'error');
        }
    });
}

// --- Solicitud (Request) Functions ---
function renderSolicitudes() {
    if (state.currentTab !== 'solicitudes' && initialLoadComplete) return;
    const tbody = document.getElementById('solicitud-list');
    tbody.innerHTML = '';
    
    let userSolicitudes = state.solicitudes; 
    const userRole = getCurrentUser().role;
    const managedMachineIds = getCurrentUser().managedMachineIds;
    const equipoAsignado = getCurrentUser().equipoAsignado;

    const hasRestrictiveRole = (userRole === 'Jefe de Area') || (['Técnico', 'Operario', 'Supervisor de Area'].includes(userRole));
    if (hasRestrictiveRole) {
        const ids = userRole === 'Jefe de Area' ? (managedMachineIds || []) : (equipoAsignado || []);
        const relevantMachineIds = new Set(Array.isArray(ids) ? ids : []);
        userSolicitudes = state.solicitudes.filter(s => relevantMachineIds.has(s.machineId));
    }

    const monthFilter = document.getElementById('solicitudes-month-filter');
    const selectedMonth = monthFilter ? monthFilter.value : null;

    if (selectedMonth) {
        userSolicitudes = userSolicitudes.filter(s => {
            if (!s.createdAt) return false;
            let localMonthStr = "";
            if (/^\d{4}-\d{2}-\d{2}/.test(s.createdAt)) {
                localMonthStr = s.createdAt.substring(0, 7);
            } else {
                const d = new Date(s.createdAt);
                if (isNaN(d.getTime())) return false;
                localMonthStr = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`;
            }
            return localMonthStr === selectedMonth;
        });
    }

    if (userSolicitudes.length === 0) {
        tbody.innerHTML = '<tr><td colspan="6" class="text-center">No se encontraron solicitudes para este periodo.</td></tr>';
        return;
    }

    userSolicitudes.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt));

    userSolicitudes.forEach(solicitud => {
        const machine = state.machines.find(m => m.id === solicitud.machineId) || { name: 'N/A' };
        const tr = document.createElement('tr');
        
        const solicitudFbId = solicitud?.fb_id || solicitud?.id;
        const cleanId = solicitudFbId ? String(solicitudFbId).replace(/[.#$\[\]]/g, '_') : '';
        const solMirror = state.liveSolicitudes?.[cleanId];
        let statusText = solMirror?.status || solicitud.status;

        let statusBadgeClass = '';
        let linkedWorkOrder = null;

        if (solicitud.workOrderFbId) {
            linkedWorkOrder = state.workOrders.find(wo => wo.fb_id === solicitud.workOrderFbId);
        } else if (solicitud.workOrderId) {
            linkedWorkOrder = state.workOrders.find(wo => wo.id === solicitud.workOrderId);
        }

        // Búsqueda de respaldo si los vínculos directos fallan o están desactualizados (ej. cambio de ID de MP a MA)
        if (!linkedWorkOrder) {
            linkedWorkOrder = state.workOrders.find(wo =>
                wo.sourceSolicitudId === solicitud.fb_id ||
                (wo.solicitudId === solicitud.id && solicitud.id)
            );
        }

        if (linkedWorkOrder) {
            statusText = (linkedWorkOrder.status === 'Pendiente') ? 'En Planificación' : linkedWorkOrder.status;
        } else if (solicitud.status === 'Aprobado') {
             statusText = 'En Planificación';
        }
        
        switch(statusText) {
            case 'Pendiente' : 
                statusBadgeClass = 'bg-warning text-dark'; 
                break;
            case 'Aprobado' :
            case 'En Planificación' :
                statusBadgeClass = 'bg-primary'; 
                statusText = 'En Planificación';
                break;
            case 'En Proceso' : 
                statusBadgeClass = 'bg-info text-dark'; 
                break;
            case 'Pausado' : 
                statusBadgeClass = 'bg-secondary'; 
                break;
            case 'Completado' : 
                statusBadgeClass = 'bg-success'; 
                break;
            case 'Rechazado' : 
                statusBadgeClass = 'bg-danger'; 
                break;
            case 'Cancelado' :
                statusBadgeClass = 'bg-danger'; 
                break;
            default: 
                statusBadgeClass = 'bg-light text-dark';
        }

        const canManage = ['Admin', 'Planificador', 'Técnico'].includes(userRole);
        let vinculadaContent = '';

        if (linkedWorkOrder) {
            vinculadaContent = `
                <button class="btn btn-sm btn-outline-primary view-linked-wo" data-wo-id="${linkedWorkOrder.fb_id || linkedWorkOrder.id}">
                    <i class="fas fa-external-link-alt me-1"></i>${linkedWorkOrder.id}
                </button>`;
        } else if (solicitud.status === 'Pendiente' && canManage) {
            vinculadaContent = `
                <button class="btn btn-sm btn-success create-wo-btn">
                    <i class="fas fa-plus me-1"></i>Crear OT
                </button>`;
        } else {
            vinculadaContent = '<span class="text-muted small">Sin vincular</span>';
        }

        tr.innerHTML = `
            <td class="col-id">${solicitud.id}</td>
            <td>${machine.name}</td>
            <td class="col-desc" title="${solicitud.description}">${solicitud.description}</td>
            <td>${new Date(solicitud.createdAt).toLocaleDateString('es-ES')}</td>
            <td><span class="badge ${statusBadgeClass}">${statusText}</span></td>
            <td>${vinculadaContent}</td>
        `;

        tbody.appendChild(tr);

        if (linkedWorkOrder) {
            tr.querySelector('.view-linked-wo').addEventListener('click', (e) => {
                e.preventDefault();
                showWorkOrderModal(linkedWorkOrder.fb_id || linkedWorkOrder.id);
            });
        } else if (solicitud.status === 'Pendiente' && canManage) {
            tr.querySelector('.create-wo-btn').addEventListener('click', (e) => {
                e.preventDefault();
                let targetType = 'Correctivo'; if (solicitud.type === 'mecanizado') targetType = 'Mecanizado'; else if (solicitud.type === 'calibracion') targetType = 'Calibración';
                showWorkOrderModal(null, targetType, solicitud);
            });
        }
    });
}
function showSolicitudModal(prefillData = null) {
    document.getElementById('solicitud-form').reset();
    state.tempSolicitudItems = [];
    renderSolicitudItems();

    // Reset search and item selection on modal open
    const searchInput = document.getElementById('solicitud-item-search');
    if (searchInput) searchInput.value = '';
    const itemSelect = document.getElementById('solicitud-item-part-select');
    if (itemSelect) itemSelect.innerHTML = '';

    document.getElementById('solicitud-extra-fields').classList.add('d-none');
    document.getElementById('solicitud-multi-items-section').classList.add('d-none');
    document.getElementById('solicitud-description-container').classList.remove('d-none');
    document.getElementById('solicitud-quantity-container').classList.remove('d-none');

    document.getElementById('solicitud-machine').required = true;
    document.getElementById('solicitud-description').required = true;
    document.getElementById('solicitud-machine-label').innerHTML = 'Máquina';
    document.getElementById('solicitud-description-label').innerHTML = 'Descripción del Problema';

    const machineSelect = document.getElementById('solicitud-machine');
    const descriptionText = document.getElementById('solicitud-description');
    machineSelect.innerHTML = '<option value="">Seleccione una máquina...</option>';

    let machinesToDisplay = state.machines;
    const user = state.currentUser;

    if (user) {
        if (user?.role === 'Jefe de Area') {
            const managedIds = new Set(Array.isArray(user.managedMachineIds) ? user.managedMachineIds : []);
            machinesToDisplay = state.machines.filter(m => managedIds.has(m.id));
        } else if (['Técnico', 'Operario', 'Supervisor de Area'].includes(user?.role)) {
            const assignedIds = new Set(Array.isArray(user.equipoAsignado) ? user.equipoAsignado : []);
            machinesToDisplay = state.machines.filter(m => assignedIds.has(m.id));
        }
    }

    let html = '<option value="">Seleccione una máquina...</option>';
    machinesToDisplay.forEach(m => {
        const locationText = m.location ? ` | ${m.location}` : '';
        html += `<option value="${m.id}">${m.id} - ${m.name}${locationText}</option>`;
    });
    machineSelect.innerHTML = html;

    if (prefillData) {
        if (prefillData.machineId) {
            machineSelect.value = prefillData.machineId;
        }
        if (prefillData.description) {
            descriptionText.value = prefillData.description;
        }
    }

    state.modals.solicitud.show();
}

async function handleSolicitudSubmit(e) {
    e.preventDefault();
    showLoading(true);
    
    const type = document.getElementById('solicitud-type').value;
    const machineId = document.getElementById('solicitud-machine').value;
    const description = document.getElementById('solicitud-description').value;

    if (type === 'insumos') {
        const urgency = document.getElementById('solicitud-urgency').value;

        if (state.tempSolicitudItems.length === 0) {
            showToast('Agregue al menos un ítem a la solicitud.', 'warning');
            showLoading(false);
            return;
        }

        try {
            const requestData = {
                id: generateNextPartRequestId(),
                date: new Date().toISOString(),
                workOrderFbId: '',
                workOrderId: '',
                machineId: machineId || 'General',
                requester: getCurrentUser().username || 'Desconocido',
                items: state.tempSolicitudItems,
                urgency: urgency,
                status: 'Pendiente',
                isDirectRequest: true
            };
            const docRef = await addDoc(state.collections.partRequests, requestData);
            await updateRTDBMirror('partRequests', requestData, docRef.id);
            await recordAuditLog('partRequests', docRef.id, 'CREATE', null, requestData, 'Creación Solicitud Insumos');
            showToast('Solicitud de insumos enviada correctamente', 'success');
            state.modals.solicitud.hide();

            // --- Notificación Telegram ---
            let itemsText = requestData.items.map(i => `• ${escapeHTML(i.description)} (${i.quantity})`).join('\n');
            const msg = `📦 <b>Nueva Solicitud de Insumos / Repuestos</b>\n\n` +
                        `<b>Items:</b> \n${itemsText}\n\n` +
                        `<b>Urgencia:</b> ${escapeHTML(requestData.urgency)}\n` +
                        `<b>Solicitante:</b> ${escapeHTML(requestData.requester)}\n` +
                        `<b>Equipo ? Ref :</b> ${escapeHTML(requestData.machineId)}`;

            // Notificar a administradores, planificadores y grupo global
            notifyAdminsAndPlanners(msg).catch(err => console.warn("[Telegram Notification Error]", err));

            // Personal notifications to all technicians (excluding admins/planners already notified)
            const otherTechnicians = state.technicians
                .filter(t => t?.isActive !== false && !['Admin', 'Planificador'].includes(t?.role))
                .map(t => t.username);
            notifyTechnicians(otherTechnicians, msg);

            showLoading(false);
            return;
        } catch (error) {
            console.error("Error submitting supply request:", error);
            showToast('Error al enviar la solicitud.', 'error');
            showLoading(false);
            return;
        }
    }

    // Caso Mantenimiento / Mecanizado
    let prefix = 'SOL'; if (type === 'mecanizado') prefix = 'MEC'; else if (type === 'calibracion') prefix = 'CAL';
    const allSolicitudes = await getDocs(query(state.collections.solicitudes));
    const count = allSolicitudes.docs.filter(d => d.data().id && d.data().id.startsWith(prefix)).length;
    const nextId = `${prefix}-${(count + 1).toString().padStart(4, '0')}`;

    let finalDescription = description; if (type === 'mecanizado') finalDescription = `[MECANIZADO] ${description}`; else if (type === 'calibracion') finalDescription = `[CALIBRACIÓN] ${description}`;

    const solicitudData = {
        id: nextId,
        type: type,
        machineId: machineId,
        description: finalDescription,
        requester: getCurrentUser().username,
        status: 'Pendiente',
        createdAt: new Date().toISOString()
    };

    if (!solicitudData.machineId || !solicitudData.description) {
        showToast('Todos los campos son obligatorios.', 'error');
        showLoading(false);
        return;
    }

    try {
        const docRef = await addDoc(state.collections.solicitudes, solicitudData);
        await updateRTDBMirror('solicitudes', solicitudData, docRef.id);
        const auditAction = type === 'mecanizado' ? 'Creación Solicitud Mecanizado' : (type === 'calibracion' ? 'Creación Solicitud Calibración' : 'Creación Solicitud Mantenimiento');
        await recordAuditLog('solicitudes', solicitudData.id, 'CREATE', null, solicitudData, auditAction);
        showToast('Solicitud enviada correctamente', 'success');
        state.modals.solicitud.hide();

        // --- Sincronización con Odoo (Segundo Plano) ---
        if (state.odoo) {
            syncSolicitudToOdoo(solicitudData, docRef.id, nextId).catch(err => {
                console.warn("[Odoo Background Sync] Error:", err);
            });
        }

        // --- Notificación Telegram ---
        const machine = state.machines.find(m => m.id === machineId);
        const typeTitle = type === 'mecanizado' ? 'Mecanizado' : (type === 'calibracion' ? 'Calibración' : 'Mantenimiento');
        const msg = `🆕 <b>Nueva Solicitud de ${typeTitle}</b>\n\n` +
                    `<b>ID:</b> ${nextId}\n` +
                    `<b>Equipo:</b> ${escapeHTML(machine ? machine.name : machineId)} (${escapeHTML(machineId)})\n` +
                    `<b>Solicitante:</b> ${escapeHTML(solicitudData.requester)}\n` +
                    `<b>Descripción:</b> ${escapeHTML(solicitudData.description)}\n` +
                    `<b>Fecha:</b> ${new Date().toLocaleString()}`;

        // Notificar a administradores, planificadores y grupo global
        notifyAdminsAndPlanners(msg).catch(err => console.warn("[Telegram Notification Error]", err));

        // Notificaciones personales para el resto de técnicos
        const otherUsernames = state.technicians
            .filter(t => t?.isActive !== false && !['Admin', 'Planificador'].includes(t?.role))
            .map(t => t.username);
        notifyTechnicians(otherUsernames, msg);
    } catch(error) {
        console.error("Error submitting solicitud:", error);
        showToast('Error al enviar la solicitud.', 'error');
    } finally {
        showLoading(false);
    }
}

// --- Evaluation Criteria Functions ---

function renderCriteria() {
    const tbodyOperario = document.getElementById('criteria-list-operario');
    const tbodyJefe = document.getElementById('criteria-list-jefe');

    tbodyOperario.innerHTML = '';
    tbodyJefe.innerHTML = '';

    const operarioCriteria = state.evaluationCriteria.filter(c => c?.role === 'Operario' || !c?.role);
    const jefeCriteria = state.evaluationCriteria.filter(c => c?.role === 'Jefe de Area');

    if (operarioCriteria.length === 0) {
        tbodyOperario.innerHTML = '<tr><td colspan="3" class="text-center text-muted">No hay criterios de operario.</td></tr>';
    }
    if (jefeCriteria.length === 0) {
        tbodyJefe.innerHTML = '<tr><td colspan="3" class="text-center text-muted">No hay criterios de jefe de área.</td></tr>';
    }

    state.evaluationCriteria.forEach(criteria => {
        const tr = document.createElement('tr');
        if (criteria.status === 'inactivo') tr.classList.add('table-secondary', 'opacity-50');

        const pairedCriteria = criteria.pairId ? state.evaluationCriteria.find(c => c.fb_id === criteria.pairId) : null;
        const pairText = pairedCriteria ? pairedCriteria.description : '<span class="text-muted small">Sin vincular</span>';

        tr.innerHTML = `
            <td>${criteria.description}</td>
            <td>${pairText}</td>
            <td class="text-center">
                <div class="btn-group">
                    <button class="btn btn-xs btn-outline-primary edit-criteria-btn" data-id="${criteria.fb_id}" title="Editar"><i class="fas fa-edit"></i></button>
                    <button class="btn btn-xs btn-outline-danger delete-criteria-btn" data-id="${criteria.fb_id}" title="Eliminar"><i class="fas fa-trash"></i></button>
                </div>
            </td>
        `;

        if (criteria.role === 'Jefe de Area') {
            tbodyJefe.appendChild(tr);
        } else {
            tbodyOperario.appendChild(tr);
        }
    });

    document.querySelectorAll('.edit-criteria-btn').forEach(btn => {
        btn.addEventListener('click', () => showCriteriaModal(btn.dataset.id));
    });
    document.querySelectorAll('.delete-criteria-btn').forEach(btn => {
        btn.addEventListener('click', () => deleteCriteria(btn.dataset.id));
    });
}

function showCriteriaModal(criteriaId = null) {
    const form = document.getElementById('criteria-form');
    form.reset();
    document.getElementById('criteria-id-hidden').value = '';

    const roleSelect = document.getElementById('criteria-role');
    const pairSelect = document.getElementById('criteria-pair');

    const updatePairs = () => {
        const selectedRole = roleSelect.value;
        const currentId = document.getElementById('criteria-id-hidden').value;
        pairSelect.innerHTML = '<option value="">Ninguno (Criterio independiente)</option>';

        state.evaluationCriteria.forEach(c => {
            // Solo mostrar criterios del rol OPUESTO y que no sean el actual
            if (c?.role !== selectedRole && c?.fb_id !== currentId) {
                const opt = document.createElement('option');
                opt.value = c.fb_id;
                opt.textContent = c.description;
                pairSelect.appendChild(opt);
            }
        });
    };

    roleSelect.onchange = updatePairs;

    if (criteriaId) {
        const criteria = state.evaluationCriteria.find(c => c.fb_id === criteriaId);
        if (criteria) {
            document.getElementById('criteria-modal-title').textContent = 'Editar Criterio';
            document.getElementById('criteria-id-hidden').value = criteria.fb_id;
            document.getElementById('criteria-description').value = criteria.description;
            roleSelect.value = criteria.role || 'Operario';
            updatePairs();
            pairSelect.value = criteria.pairId || '';
            document.getElementById('criteria-status').value = criteria.status;
        }
    } else {
        document.getElementById('criteria-modal-title').textContent = 'Añadir Criterio';
        updatePairs();
    }
    state.modals.criteria.show();
}

async function handleCriteriaSubmit(e) {
    e.preventDefault();
    const criteriaId = document.getElementById('criteria-id-hidden').value;
    const pairId = document.getElementById('criteria-pair').value;
    const criteriaData = {
        description: document.getElementById('criteria-description').value,
        role: document.getElementById('criteria-role').value,
        pairId: pairId || null,
        status: document.getElementById('criteria-status').value
    };

    if (!criteriaData.description) {
        showToast('La descripción es obligatoria.', 'error');
        return;
    }

    showLoading(true);
    try {
        let finalId = criteriaId;
        if (criteriaId) {
            // Si estamos editando y tenía una pareja anterior, quitar el link de esa pareja
            const oldCriteria = state.evaluationCriteria.find(c => c.fb_id === criteriaId);
            if (oldCriteria && oldCriteria.pairId && oldCriteria.pairId !== pairId) {
                await updateDoc(doc(state.collections.evaluationCriteria, oldCriteria.pairId), { pairId: null });
            }
            await setDoc(doc(state.collections.evaluationCriteria, criteriaId), criteriaData, { merge: true });
            await recordAuditLog('evaluationCriteria', criteriaId, 'UPDATE', oldCriteria, criteriaData);
            showToast('Criterio actualizado.', 'success');
        } else {
            const docRef = await addDoc(state.collections.evaluationCriteria, criteriaData);
            finalId = docRef.id;
            await recordAuditLog('evaluationCriteria', finalId, 'CREATE', null, criteriaData);
            showToast('Criterio añadido.', 'success');
        }

        // Si se seleccionó una pareja, actualizarla también para que apunte a este criterio (vínculo bidireccional)
        if (pairId) {
            await updateDoc(doc(state.collections.evaluationCriteria, pairId), { pairId: finalId });
        }

        state.modals.criteria.hide();
    } catch (error) {
        console.error("Error saving criteria:", error);
        showToast('Error al guardar el criterio.', 'error');
    } finally {
        showLoading(false);
    }
}

function deleteCriteria(criteriaId) {
    showConfirmation('Eliminar Criterio', '¿Está seguro de eliminar este criterio', async () => {
        showLoading(true);
        try {
            // Si tiene pareja, quitar el vínculo en la pareja antes de borrar
            const criteria = state.evaluationCriteria.find(c => c.fb_id === criteriaId);
            if (criteria && criteria.pairId) {
                await updateDoc(doc(state.collections.evaluationCriteria, criteria.pairId), { pairId: null });
            }
            await deleteDoc(doc(state.collections.evaluationCriteria, criteriaId));
            await recordAuditLog('evaluationCriteria', criteriaId, 'DELETE', criteria, null);
            showToast('Criterio eliminado.', 'success');
        } catch (error) {
            console.error("Error deleting criteria:", error);
            showToast('Error al eliminar el criterio.', 'error');
        } finally {
            showLoading(false);
        }
    });
}

// --- Evaluation & Profile Functions ---

function showEvaluationDetailsModal(workOrderId) {
    const order = state.workOrders.find(o => o.fb_id === workOrderId);
    if (!order) return;

    const evs = state.technicianEvaluations.filter(ev => ev.workOrderFbId === workOrderId);
    const container = document.getElementById('evaluation-details-content');

    let html = `
        <div class="mb-4">
            <h6 class="fw-bold"><i class="fas fa-info-circle me-2"></i>Información de la Orden</h6>
            <div class="row">
                <div class="col-md-6"><p class="mb-1"><strong>ID:</strong> ${order.id}</p></div>
                <div class="col-md-6"><p class="mb-1"><strong>Máquina:</strong> ${order.machineId}</p></div>
                <div class="col-12"><p class="mb-1"><strong>Descripción:</strong> ${order.description}</p></div>
            </div>
        </div>
        <hr>
    `;

    if (evs.length === 0) {
        html += '<p class="text-center text-muted">No hay evaluaciones registradas para esta orden.</p>';
    } else {
        evs.forEach(ev => {
            const dateStr = ev.evaluationDate ? new Date(ev.evaluationDate).toLocaleString() : 'N/A';
            const avgRating = ev.maxScore > 0 ? (ev.score / ev.maxScore) * 5 : 0;
            const evaluatorDisplayName = ev.evaluatorName || ev.evaluatorId || 'N/A';

            html += `
                <div class="card mb-3 shadow-sm border-0 bg-light">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <h6 class="mb-0 text-primary"><i class="fas fa-user-tag me-2"></i>${ev.evaluatorRole}: ${evaluatorDisplayName}</h6>
                            <span class="badge bg-secondary">${dateStr}</span>
                        </div>
                        <div class="mb-2">
                            <strong>Calificación: </strong>
                            <span class="text-warning">
                                ${Array.from({length: 5}, (_, i) => `<i class="${i < Math.round(avgRating) ? 'fas' : 'far'} fa-star"></i>`).join('')}
                            </span>
                            <span class="ms-2 fw-bold" style="font-size: 1.1rem;">${avgRating.toFixed(1)}</span>
                        </div>
                        <div class="mb-2">
                            <p class="mb-1"><strong>Comentarios:</strong></p>
                            <p class="mb-0 italic text-muted">"${ev.comments || 'Sin comentarios'}"</p>
                        </div>
                        <div class="mt-2">
                            <p class="mb-1 small"><strong>Criterios:</strong></p>
                            <ul class="list-inline mb-0">
                                ${(ev.results || []).map(res => {
                                    return `<li class="list-inline-item small me-3"><span class="text-muted">${res.description}:</span> <strong>${res.score ? 'Sí' : 'No'}</strong></li>`;
                                }).join('')}
                            </ul>
                        </div>
                    </div>
                </div>
            `;
        });
    }

    container.innerHTML = html;
    const modal = new bootstrap.Modal(document.getElementById('evaluation-details-modal'));
    modal.show();
}

function showEvaluationModal(workOrderId) {
    const workOrder = state.workOrders.find(wo => wo.id === workOrderId || wo.fb_id === workOrderId);
    if (!workOrder) {
        showToast('Orden de trabajo no encontrada.', 'error');
        return;
    }

    // Restricción de equipo asignado para roles restringidos
    const userRole = getCurrentUser().role;
    if (['Operario', 'Supervisor de Area', 'Técnico'].includes(userRole)) {
        const equipoAsignado = Array.isArray(getCurrentUser().equipoAsignado) ? getCurrentUser().equipoAsignado : [];
        if (!equipoAsignado.includes(workOrder.machineId)) {
            showToast('No tiene permisos para evaluar este equipo ya que no está bajo su cargo.', 'error');
            return;
        }
    } else if (userRole === 'Jefe de Area') {
        const managedIds = Array.isArray(getCurrentUser().managedMachineIds) ? getCurrentUser().managedMachineIds : [];
        if (!managedIds.includes(workOrder.machineId)) {
            showToast('No tiene permisos para evaluar este equipo ya que no pertenece a su área.', 'error');
            return;
        }
    }

    // Bloquear evaluación si es de plan o de solicitud y no ha sido validada por el planificador
    const isPlanOrder = ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(workOrder.type) || workOrder.linkedPlanId;
    const hasRequest = workOrder.solicitudId || workOrder.sourceSolicitudId;
    if ((isPlanOrder || hasRequest) && !workOrder.validatedBy) {
        showToast('Esta orden debe ser validada por el Planificador antes de proceder a la evaluación.', 'warning');
        return;
    }

    document.getElementById('evaluation-form').reset();
    document.getElementById('evaluation-wo-id-hidden').value = workOrder.fb_id;

    // Determinar qué rol está evaluando el usuario actual
    // const userRole = getCurrentUser().role; // Already declared above
    const existingEvaluations = state.technicianEvaluations.filter(ev => ev.workOrderFbId === workOrder.fb_id);
    const solicitudId = workOrder.solicitudId || workOrder.sourceSolicitudId;
    const originalSolicitud = solicitudId ? state.solicitudes.find(s => s.id === solicitudId || s.fb_id === solicitudId) : null;
    const isRequester = originalSolicitud && originalSolicitud.requester === getCurrentUser().username;
    const hasOpEval = existingEvaluations.some(ev => ev.evaluatorRole === 'Operario');
    const hasJfEval = existingEvaluations.some(ev => ev.evaluatorRole === 'Jefe de Area');
    const hasSvEval = existingEvaluations.some(ev => ev.evaluatorRole === 'Supervisor de Area');

    let evaluatingRole = '';
    if (userRole === 'Admin') {
        if (isPlanOrder) {
            if (!hasJfEval) evaluatingRole = 'Jefe de Area';
            else evaluatingRole = 'Supervisor de Area';
        } else {
            if (!hasOpEval) evaluatingRole = 'Operario';
            else evaluatingRole = 'Supervisor de Area';
        }
    } else {
        if (isPlanOrder) {
            if (userRole === 'Jefe de Area' || (userRole === 'Planificador' && ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(workOrder.type))) {
                evaluatingRole = 'Jefe de Area';
            } else if (userRole === 'Supervisor de Area') {
                evaluatingRole = 'Supervisor de Area';
            }
        } else {
            if (userRole === 'Supervisor de Area' || userRole === 'Jefe de Area') {
                evaluatingRole = 'Supervisor de Area';
            } else if ((['Operario', 'Supervisor de Area'].includes(userRole)) || isRequester) {
                evaluatingRole = 'Operario';
            }
        }
    }

    const existingEval = existingEvaluations.find(ev => ev.evaluatorRole === evaluatingRole);
    const isReadOnly = !!existingEval;

    document.getElementById('evaluation-modal-title').textContent = isReadOnly ? `Evaluación de ${evaluatingRole}: ${workOrder.id}` : `Evaluar como ${evaluatingRole}: ${workOrder.id}`;

    // Configurar UI de solo lectura
    const badge = document.getElementById('evaluation-read-only-badge');
    const saveBtn = document.getElementById('evaluation-save-btn');
    const commentsField = document.getElementById('evaluation-comments');

    if (isReadOnly) {
        badge.classList.remove('d-none');
        document.getElementById('evaluation-evaluator-name').textContent = existingEval.evaluatorId || 'N/A';
        saveBtn.classList.add('d-none');
        commentsField.value = existingEval.comments || '';
        commentsField.disabled = true;
    } else {
        badge.classList.add('d-none');
        saveBtn.classList.remove('d-none');
        commentsField.disabled = false;
    }

    // Guardar el rol evaluador en un dataset para el submit
    document.getElementById('evaluation-form').dataset.evaluatingRole = evaluatingRole;

    const criteriaContainer = document.getElementById('evaluation-criteria-container');
    criteriaContainer.innerHTML = '';

    if (isReadOnly) {
        // Renderizar los resultados de la evaluación existente
        if (!existingEval.results || existingEval.results.length === 0) {
            criteriaContainer.innerHTML = `<p class="text-muted">No se encontraron detalles de criterios para esta evaluación.</p>`;
        } else {
            existingEval.results.forEach(res => {
                const div = document.createElement('div');
                div.className = 'form-check mb-2';
                div.innerHTML = `
                    <input class="form-check-input" type="checkbox" ${res.score ? 'checked' : ''} disabled>
                    <label class="form-check-label">
                        ${res.description}
                    </label>
                `;
                criteriaContainer.appendChild(div);
            });
        }
    } else {
        // Renderizar criterios activos para nueva evaluación
        const activeCriteria = state.evaluationCriteria.filter(c => c?.status === 'activo' && (c?.role === evaluatingRole || (!c?.role && evaluatingRole === 'Operario' && evaluatingRole !== 'Supervisor de Area')));

        if (activeCriteria.length === 0) {
            criteriaContainer.innerHTML = `<p class="text-muted">No hay criterios de evaluación activos para el rol ${evaluatingRole}.</p>`;
        } else {
            activeCriteria.forEach(criteria => {
                const div = document.createElement('div');
                div.className = 'form-check mb-2';
                div.innerHTML = `
                    <input class="form-check-input evaluation-checkbox" type="checkbox" value="${criteria.fb_id}" id="eval-${criteria.fb_id}">
                    <label class="form-check-label" for="eval-${criteria.fb_id}">
                        ${criteria.description}
                    </label>
                `;
                criteriaContainer.appendChild(div);
            });
        }
    }
    state.modals.evaluation.show();
}


async function handleEvaluationSubmit(e) {
    e.preventDefault();
    const workOrderFbId = document.getElementById('evaluation-wo-id-hidden').value;
    const workOrder = state.workOrders.find(wo => wo.fb_id === workOrderFbId);
    const evaluatingRole = e.target.dataset.evaluatingRole;

    if (!workOrder) {
        showToast('Error: Orden de trabajo no encontrada.', 'error');
        return;
    }

    const evaluationResults = [];
    document.querySelectorAll('.evaluation-checkbox').forEach(checkbox => {
        evaluationResults.push({
            criteriaId: checkbox.value,
            description: checkbox.nextElementSibling.textContent.trim(),
            score: checkbox.checked ? 1 : 0
        });
    });

    const evaluationData = {
        workOrderId: workOrder.id,
        workOrderFbId: workOrder.fb_id,
        technicianId: workOrder.leadTechnician,
        evaluatorId: getCurrentUser().username,
        evaluatorName: getCurrentUser().username,
        evaluatorRole: evaluatingRole, // Guardamos el rol que evaluó
        evaluationDate: new Date().toISOString(),
        comments: document.getElementById('evaluation-comments').value,
        results: evaluationResults,
        score: evaluationResults.reduce((acc, item) => acc + item.score, 0),
        maxScore: evaluationResults.length
    };

    const confirmed = await new Promise(resolve => {
        requestSignature(() => resolve(true), () => resolve(false));
    });
    if (!confirmed) return;

    showLoading(true);
    try {
        const now = new Date().toISOString();

        // 1. Guardar la evaluación actual
        const docRef = await addDoc(state.collections.technicianEvaluations, evaluationData);
        await recordAuditLog('technicianEvaluations', evaluationData.workOrderId || docRef.id, 'CREATE', null, evaluationData, 'Evaluación Técnica (Firmada)');

        // 2. Verificar si ya están las dos evaluaciones (Operario y Jefe de Area)
        const allEvaluations = state.technicianEvaluations.filter(ev => ev.workOrderFbId === workOrderFbId);
        // Añadimos la actual ya que el snapshot puede tardar un poco
        allEvaluations.push(evaluationData);

        const isPlanOrder = ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(workOrder.type) || workOrder.linkedPlanId;
        const hasOperario = allEvaluations.some(ev => ev.evaluatorRole === 'Operario');
        const hasJefe = allEvaluations.some(ev => ev.evaluatorRole === 'Jefe de Area');
        const hasSupervisor = allEvaluations.some(ev => ev.evaluatorRole === 'Supervisor de Area');

        let isComplete = false;
        if (isPlanOrder) {
            isComplete = hasJefe && hasSupervisor;
        } else {
            isComplete = hasOperario && (hasSupervisor || hasJefe);
        }

        if (isComplete) {
            const jefeEval = allEvaluations.find(ev => ev.evaluatorRole === 'Jefe de Area');
            // Solo si están ambas, pasamos a Completado y limpiamos flag de expiración (aunque ya lo esté por el nuevo flujo)
            await updateDoc(doc(state.collections.workOrders, workOrderFbId), {
                status: 'Completado',
                isExpired: false
            });
            await completeLinkedPlanExecution(workOrderFbId, now, 'Completado', jefeEval ? jefeEval.evaluatorId : null);
            showToast('Evaluación guardada. Orden de trabajo calificada por completo.', 'success');
        } else {
            showToast(`Evaluación de ${evaluatingRole} guardada. Pendiente de la otra evaluación para calificar por completo la OT.`, 'info');
        }

        state.modals.evaluation.hide();
    } catch (error) {
        console.error("Error saving evaluation:", error);
        showToast('Error al guardar la evaluación.', 'error');
    } finally {
        showLoading(false);
    }
}

async function showTechnicianProfileModal(technicianId) {
    const technician = state.technicians.find(t => t.fb_id === technicianId);
    if (!technician) return;

    // Set basic info
    document.getElementById('technician-profile-modal-title').textContent = `Perfil de ${technician.username}`;
    document.getElementById('technician-profile-avatar').src = `https://ui-avatars.com/api/name=${technician.username}&background=0D8ABC&color=fff`;
    document.getElementById('technician-profile-name').textContent = technician.username;
    document.getElementById('technician-profile-role').textContent = technician.role;

    // Calculate and set metrics
    const assignedOrders = state.workOrders.filter(wo => wo.leadTechnician === technician.username || (wo.technicians && wo.technicians.includes(technician.username)));
    const completedOrders = assignedOrders.filter(wo => wo.status === 'Completado' || wo.status === 'Pendiente de Evaluación');
    document.getElementById('technician-profile-assigned').textContent = assignedOrders.length;
    document.getElementById('technician-profile-completed').textContent = completedOrders.length;

    // Fetch and display evaluations
    const evaluationsContainer = document.getElementById('technician-profile-evaluations');
    evaluationsContainer.innerHTML = '<div class="spinner-border spinner-border-sm"></div>';

    const technicianEvaluations = state.technicianEvaluations.filter(e => e.technicianId === technician.username);

    if (technicianEvaluations.length === 0) {
        evaluationsContainer.innerHTML = '<p class="text-muted">Este técnico aún no tiene evaluaciones.</p>';
    } else {
        const totalScore = technicianEvaluations.reduce((sum, e) => sum + e.score, 0);
        const totalMaxScore = technicianEvaluations.reduce((sum, e) => sum + e.maxScore, 0);
        const averageScorePercentage = totalMaxScore > 0 ? (totalScore / totalMaxScore) * 100 : 0;

        evaluationsContainer.innerHTML = `
            <div class="mb-3">
                <h5>Puntuación Promedio</h5>
                <div class="progress" style="height: 25px;">
                    <div class="progress-bar" role="progressbar" style="width: ${averageScorePercentage.toFixed(2)}%;" aria-valuenow="${averageScorePercentage.toFixed(2)}" aria-valuemin="0" aria-valuemax="100">
                        ${averageScorePercentage.toFixed(1)}%
                    </div>
                </div>
            </div>
            <h6>Desglose por Criterio (Últimas 10 evaluaciones)</h6>
            <ul class="list-group text-start">
                ${renderCriteriaBreakdown(technicianEvaluations.slice(-10), false)}
            </ul>
        `;
    }


    // Acciones de perfil para Administradores
    const profileActions = document.getElementById('tech-profile-actions');
    const profileLinkDisplay = document.getElementById('tech-profile-link-display');
    const profileGenerateLinkBtn = document.getElementById('tech-profile-generate-link');
    const profileLinkInput = document.getElementById('tech-profile-link-input');
    const profileCopyLinkBtn = document.getElementById('tech-profile-copy-link');

    if (getCurrentUser().role === 'Admin') {
        profileActions.classList.remove('d-none');
        profileLinkDisplay.classList.add('d-none');

        profileGenerateLinkBtn.onclick = async () => {
            const type = technician.resetRequested ? 'reset' : 'invitation';
            const link = await generateTokenLink(technicianId, type, false);
            profileLinkInput.value = link;
            profileLinkDisplay.classList.remove('d-none');
        };

        profileCopyLinkBtn.onclick = () => {
            profileLinkInput.select();
            document.execCommand('copy');
            showToast('Link copiado al portapapeles', 'success');
        };
    } else {
        profileActions.classList.add('d-none');
    }

    state.modals.technicianProfile.show();
}

function renderCriteriaBreakdown(evaluations, forPdf = false) {
    const criteriaStats = {}; // { description: { positive: 10, total: 15 } }

    evaluations.forEach(evaluation => {
        evaluation.results.forEach(result => {
            if (!criteriaStats[result.description]) {
                criteriaStats[result.description] = { positive: 0, total: 0 };
            }
            criteriaStats[result.description].total++;
            if (result.score === 1) {
                criteriaStats[result.description].positive++;
            }
        });
    });

    const breakdown = Object.entries(criteriaStats).map(([description, stats]) => {
        const percentage = stats.total > 0 ? (stats.positive / stats.total) * 100 : 0;
        return { description, percentage };
    });

    if (forPdf) {
        return breakdown;
    }

    return breakdown.map(item => `
        <li class="list-group-item d-flex justify-content-between align-items-center">
            ${item.description}
            <span class="badge bg-primary rounded-pill">${item.percentage.toFixed(0)}%</span>
        </li>
    `).join('');
}


// --- Work Plan Functions ---


function renderWorkPlansLegacy() {
    if (state.currentTab !== 'planes-trabajo' && initialLoadComplete) return;

    const gridContainer = document.getElementById('work-plans-bento-grid');
    if (!gridContainer) return;

    const searchTerm = document.getElementById('work-plan-search-input').value.toLowerCase();
    const areaFilter = document.getElementById('wp-filter-area').value;
    const critFilter = document.getElementById('wp-filter-criticality').value;

    gridContainer.innerHTML = '';

    const machineGroups = {};
    let plansToRender = state.workPlans;

    if (getCurrentUser().role === 'Jefe de Area' && Array.isArray(getCurrentUser().managedMachineIds)) {
        const managedIds = new Set(getCurrentUser().managedMachineIds);
        plansToRender = state.workPlans.filter(p => managedIds.has(p.machineId));
    } else if ((getCurrentUser().role === 'Técnico' || getCurrentUser().role === 'Operario' || getCurrentUser().role === 'Supervisor de Area') && Array.isArray(getCurrentUser().equipoAsignado)) {
        const assignedIds = new Set(getCurrentUser().equipoAsignado);
        plansToRender = state.workPlans.filter(p => assignedIds.has(p.machineId));
    }

    plansToRender.forEach(plan => {
        const machine = state.machines.find(m => m.id === plan.machineId);
        if (!machine) return;

        const matchesSearch = plan.name.toLowerCase().includes(searchTerm) || machine.name.toLowerCase().includes(searchTerm) || machine.id.toLowerCase().includes(searchTerm);
        const matchesArea = !areaFilter || machine.area === areaFilter;
        const matchesCrit = !critFilter || machine.criticidad === critFilter;

        if (matchesSearch && matchesArea && matchesCrit) {
            if (!machineGroups[machine.id]) {
                machineGroups[machine.id] = { machine, plans: [] };
            }
            machineGroups[machine.id].plans.push(plan);
        }
    });

    const machineIds = Object.keys(machineGroups);
    if (machineIds.length === 0) {
        gridContainer.innerHTML = '<div class="col-12 text-center py-5 text-muted"><i class="fas fa-search fa-3x mb-3"></i><p>No se encontraron máquinas con planes que coincidan.</p></div>';
        return;
    }

    machineIds.forEach(mId => {
        const group = machineGroups[mId];
        const machine = group.machine;
        const plans = group.plans;
        const placeholderImg = 'https://images.unsplash.com/photo-1581091226825-a6a2a5aee158q=80&w=800';
        const machinePhoto = machine.fotoUrl || machine.imageUrl || placeholderImg;

        const canEditPhoto = getCurrentUser().role === 'Admin' || getCurrentUser().role === 'Planificador';
        const photoButton = canEditPhoto ? `
            <button class="btn btn-sm btn-dark position-absolute top-0 end-0 m-2 opacity-75 rounded-circle"
                    title="Cambiar Foto"
                    onclick="event.stopPropagation(); triggerMachinePhotoUpload('${machine.fb_id}')"
                    style="z-index: 10; width: 32px; height: 32px; padding: 0;">
                <i class="fas fa-camera"></i>
            </button>
        ` : '';

        const card = document.createElement('div');
        card.className = 'wp-machine-card';
        card.innerHTML = `
            <div class="wp-card-image-container">
                <img src="${machinePhoto}" class="wp-card-image" alt="${machine.name}" id="wp-img-${machine.id}">
                <span class="wp-machine-badge">${machine.id}</span>
                ${photoButton}
            </div>
            <div class="wp-card-body">
                <h5 class="fw-bold text-primary mb-1">${machine.name}</h5>
                <p class="text-muted small mb-3"><i class="fas fa-location-dot me-1"></i>${machine.ubicacion || 'Sin ubicación'} | ${machine.area || 'Sin área'}</p>
                <div class="wp-plan-mini-list">
                    ${plans.map(p => {
                        let statusClass = 'wp-status-ontime';
                        let statusText = 'A tiempo';
                        if (p.nextDueDate) {
                            const today = new Date();
                            const due = new Date(p.nextDueDate + 'T00:00:00');
                            if (due < today) { statusClass = 'wp-status-delayed'; statusText = 'Atrasado'; }
                            else if (due.getTime() - today.getTime() < 7 * 24 * 60 * 60 * 1000) { statusClass = 'wp-status-pending'; statusText = 'Próximo'; }
                        }
                        return `
                            <div class="wp-plan-mini-item" onclick="renderWorkPlanDetails('${p.fb_id}')" style="cursor:pointer">
                                <span class="small text-truncate me-2" title="${p.name}"><i class="fas fa-circle ${statusClass} me-2" style="font-size:8px"></i>${p.name}</span>
                                <span class="badge bg-light text-dark border-0 small" style="font-size:0.65rem">${statusText}</span>
                            </div>
                        `;
                    }).join('')}
                </div>
                <div class="mt-auto pt-3 d-flex gap-2">
                    <button class="btn btn-outline-primary btn-sm flex-grow-1 fw-bold" onclick="renderWorkPlanDetails('${plans[0].fb_id}')">
                        Ver Planes <i class="fas fa-arrow-right ms-1"></i>
                    </button>
                    <button class="btn btn-light btn-sm fw-bold" title="Historial de Máquina" onclick="showMachinePlanHistory('${machine.id}')">
                        <i class="fas fa-history"></i>
                    </button>
                </div>
            </div>
        `;
        gridContainer.appendChild(card);

        // Async image loading only for Firebase Storage URLs
        if (machinePhoto.includes('firebasestorage.googleapis.com')) {
            getOptimizedImageUrlWithRetry(machinePhoto).then(url => {
                const img = document.getElementById(`wp-img-${machine.id}`);
                if (img && url) img.src = url;
            }).catch(err => console.error("Error loading optimized image:", err));
        }
    });

    const areaSelect = document.getElementById('wp-filter-area');
    if (areaSelect && areaSelect.options.length <= 1) {
        const areas = [...new Set(state.machines.map(m => m.area).filter(Boolean))].sort();
        areas.forEach(area => {
            const opt = document.createElement('option');
            opt.value = area; opt.textContent = area;
            areaSelect.appendChild(opt);
        });
    }
}

function getWorkPlanDueStatus(plan) {
    if (!plan.nextDueDate) return { key: 'ontime', label: 'Al día', className: 'wp-status-ontime', icon: 'fa-circle-check' };

    const today = new Date();
    today.setHours(0, 0, 0, 0);
    const due = new Date(plan.nextDueDate + 'T00:00:00');
    if (due < today) return { key: 'late', label: 'Vencido', className: 'wp-status-delayed', icon: 'fa-triangle-exclamation' };

    const daysUntilDue = Math.ceil((due.getTime() - today.getTime()) / (24 * 60 * 60 * 1000));
    if (daysUntilDue <= 7) return { key: 'soon', label: 'Próximo a vencer', className: 'wp-status-pending', icon: 'fa-clock' };

    return { key: 'ontime', label: 'Al día', className: 'wp-status-ontime', icon: 'fa-circle-check' };
}

function getWorkPlanGroupStatus(plans = []) {
    const statuses = plans.map(getWorkPlanDueStatus);
    return statuses.find(s => s.key === 'late') || statuses.find(s => s.key === 'soon') || statuses[0] || getWorkPlanDueStatus(null);
}

function getWorkPlanFrequencyLabel(unit) {
    return { h: 'Horas', d: 'Días', w: 'Semanas', m: 'Meses', y: 'Años' }[unit] || 'Meses';
}

function getWorkPlanLocation(machine = {}) {
    return machine.location || machine.ubicacion || '';
}

function updateWorkPlanSelectOptions(selectId, values, defaultLabel) {
    const select = document.getElementById(selectId);
    if (!select) return;
    const currentValue = select.value;
    const cleanValues = [...new Set(values.filter(Boolean))].sort((a, b) => a.localeCompare(b));
    select.innerHTML = `<option value="">${defaultLabel}</option>`;
    cleanValues.forEach(value => {
        const option = document.createElement('option');
        option.value = value;
        option.textContent = value;
        select.appendChild(option);
    });
    select.value = cleanValues.includes(currentValue) ? currentValue : '';
}

function updateWorkPlanStats(groups) {
    const total = groups.length;
    const counts = groups.reduce((acc, group) => {
        acc[getWorkPlanGroupStatus(group.plans).key] += 1;
        return acc;
    }, { ontime: 0, soon: 0, late: 0 });
    const pct = value => total ? Math.round((value / total) * 100) : 0;
    const setText = (id, value) => {
        const el = document.getElementById(id);
        if (el) el.textContent = value;
    };
    const setBar = (id, value) => {
        const el = document.getElementById(id);
        if (el) el.style.width = `${value}%`;
    };

    setText('wp-stat-total', total);
    setText('wp-stat-ontime', counts.ontime);
    setText('wp-stat-soon', counts.soon);
    setText('wp-stat-late', counts.late);
    setText('wp-stat-ontime-pct', `${pct(counts.ontime)}% del total`);
    setText('wp-stat-soon-pct', `${pct(counts.soon)}% del total`);
    setText('wp-stat-late-pct', `${pct(counts.late)}% del total`);
    setBar('wp-stat-total-bar', total ? 100 : 0);
    setBar('wp-stat-ontime-bar', pct(counts.ontime));
    setBar('wp-stat-soon-bar', pct(counts.soon));
    setBar('wp-stat-late-bar', pct(counts.late));
}

function renderWorkPlanPagination(total) {
    const info = document.getElementById('wp-pagination-info');
    const controls = document.getElementById('wp-pagination-controls');
    const pageSize = state.workPlanPageSize || 9;
    const totalPages = Math.max(1, Math.ceil(total / pageSize));
    state.workPlanPage = Math.min(Math.max(1, state.workPlanPage || 1), totalPages);
    const start = total ? ((state.workPlanPage - 1) * pageSize) + 1 : 0;
    const end = Math.min(state.workPlanPage * pageSize, total);

    if (info) info.textContent = total ? `Mostrando ${start} a ${end} de ${total} activos` : 'Mostrando 0 activos';
    if (!controls) return;

    const pageButtons = [];
    for (let page = 1; page <= totalPages; page++) {
        if (page === 1 || page === totalPages || Math.abs(page - state.workPlanPage) <= 1) {
            pageButtons.push(`<button type="button" class="${page === state.workPlanPage ? 'active' : ''}" onclick="setWorkPlanPage(${page})">${page}</button>`);
        } else if (!pageButtons[pageButtons.length - 1].includes('wp-page-ellipsis')) {
            pageButtons.push('<span class="wp-page-ellipsis">...</span>');
        }
    }

    controls.innerHTML = `
        <button type="button" ${state.workPlanPage <= 1 ? 'disabled' : ''} onclick="setWorkPlanPage(${state.workPlanPage - 1})" title="Página anterior"><i class="fas fa-chevron-left"></i></button>
        ${pageButtons.join('')}
        <button type="button" ${state.workPlanPage >= totalPages ? 'disabled' : ''} onclick="setWorkPlanPage(${state.workPlanPage + 1})" title="Página siguiente"><i class="fas fa-chevron-right"></i></button>
    `;
}

function setWorkPlanPage(page) {
    state.workPlanPage = page;
    renderWorkPlans();
}

function setWorkPlanViewMode(mode) {
    state.workPlanViewMode = mode;
    document.getElementById('wp-grid-view-btn').classList.toggle('active', mode === 'grid');
    document.getElementById('wp-list-view-btn').classList.toggle('active', mode === 'list');
    renderWorkPlans();
}

function clearWorkPlanFilters() {
    ['work-plan-search-input', 'wp-filter-location', 'wp-filter-area', 'wp-filter-criticality', 'wp-filter-status', 'wp-filter-frequency'].forEach(id => {
        const el = document.getElementById(id);
        if (el) el.value = '';
    });
    state.workPlanCategoryFilter = 'all';
    state.workPlanPage = 1;
    document.querySelectorAll('#wp-category-tabs [data-category]').forEach(btn => {
        btn.classList.toggle('active', btn.dataset.category === 'all');
    });
    renderWorkPlans();
}

function buildWorkPlanGroups() {
    let plansToRender = state.workPlans || [];

    if (getCurrentUser().role === 'Jefe de Area' && Array.isArray(getCurrentUser().managedMachineIds)) {
        const managedIds = new Set(getCurrentUser().managedMachineIds);
        plansToRender = plansToRender.filter(p => managedIds.has(p.machineId));
    } else if ((getCurrentUser().role === 'Técnico' || getCurrentUser().role === 'Operario' || getCurrentUser().role === 'Supervisor de Area') && Array.isArray(getCurrentUser().equipoAsignado)) {
        const assignedIds = new Set(getCurrentUser().equipoAsignado);
        plansToRender = plansToRender.filter(p => assignedIds.has(p.machineId));
    }

    const machineGroups = {};
    plansToRender.forEach(plan => {
        const machine = state.machines.find(m => m.id === plan.machineId);
        if (!machine) return;
        if (!machineGroups[machine.id]) machineGroups[machine.id] = { machine, plans: [] };
        machineGroups[machine.id].plans.push(plan);
    });

    return Object.values(machineGroups).sort((a, b) => (a.machine.name || '').localeCompare(b.machine.name || ''));
}

function renderWorkPlanCard(group) {
    const machine = group.machine;
    const plans = group.plans;
    const status = getWorkPlanGroupStatus(plans);
    const categoryInfo = getAssetCategoryInfo(machine);
    const placeholderImg = 'https://images.unsplash.com/photo-1581091226825-a6a2a5aee158q=80&w=800';
    const machinePhoto = machine.fotoUrl || machine.imageUrl || placeholderImg;
    const imageId = `wp-img-${machine.id}`;
    const canEditPhoto = getCurrentUser().role === 'Admin' || getCurrentUser().role === 'Planificador';
    const firstPlanId = plans[0].fb_id || '';
    const compliance = Math.round((plans.filter(p => getWorkPlanDueStatus(p).key === 'ontime').length / Math.max(plans.length, 1)) * 100);

    const photoButton = canEditPhoto ? `
        <button class="wp-photo-btn" title="Cambiar foto" onclick="event.stopPropagation(); triggerMachinePhotoUpload('${machine.fb_id}')">
            <i class="fas fa-camera"></i>
        </button>
    ` : '';

    setTimeout(() => {
        if (machinePhoto.includes('firebasestorage.googleapis.com')) {
            getOptimizedImageUrlWithRetry(machinePhoto).then(url => {
                const img = document.getElementById(imageId);
                if (img && url) img.src = url;
            }).catch(err => console.error("Error loading optimized image:", err));
        }
    }, 0);

    return `
        <article class="wp-machine-card">
            <div class="wp-card-status-bar ${status.key}"></div>
            <div class="wp-card-image-container">
                <img src="${machinePhoto}" class="wp-card-image" alt="${escapeHTML(machine.name || machine.id)}" id="${imageId}">
                <span class="wp-status-badge ${status.key}"><i class="fas ${status.icon}"></i>${status.label}</span>
                <span class="wp-machine-badge">${escapeHTML(machine.id || '')}</span>
                ${photoButton}
            </div>
            <div class="wp-card-body">
                <div class="wp-card-title-row">
                    <h5>${escapeHTML(machine.name || 'Activo sin nombre')}</h5>
                    <span class="wp-category-pill"><i class="fas ${categoryInfo.icon}"></i>${categoryInfo.label}</span>
                </div>
                <p class="wp-location-line"><i class="fas fa-location-dot"></i>${escapeHTML(getWorkPlanLocation(machine) || 'Sin ubicación')} · ${escapeHTML(getMachineArea(machine) || 'Sin área')}</p>
                <div class="wp-compliance">
                    <div><span>Cumplimiento</span><strong>${compliance}%</strong></div>
                    <div class="wp-compliance-track"><span class="${status.key}" style="width:${compliance}%"></span></div>
                </div>
                <div class="wp-plan-tags">
                    ${plans.slice(0, 3).map(p => `<button type="button" onclick="renderWorkPlanDetails('${p.fb_id}')" title="${escapeHTML(p.name || '')}"><i class="fas fa-screwdriver-wrench"></i>${escapeHTML(p.name || 'Plan')}</button>`).join('')}
                    ${plans.length > 3 ? `<span>+${plans.length - 3}</span>` : ''}
                </div>
                <div class="wp-card-actions">
                    <button class="btn btn-outline-primary btn-sm" onclick="renderWorkPlanDetails('${firstPlanId}')"><i class="fas fa-eye"></i>Ver planes</button>
                    <button class="btn btn-light btn-sm" title="Historial" onclick="showMachinePlanHistory('${machine.id}')"><i class="fas fa-clock-rotate-left"></i></button>
                </div>
            </div>
        </article>
    `;
}

function renderWorkPlanListRow(group) {
    const machine = group.machine;
    const plans = group.plans;
    const status = getWorkPlanGroupStatus(plans);
    const categoryInfo = getAssetCategoryInfo(machine);
    const nextPlan = plans
        .filter(p => p.nextDueDate)
        .sort((a, b) => new Date(a.nextDueDate) - new Date(b.nextDueDate))[0] || plans[0];

    return `
        <article class="wp-plan-list-row">
            <div class="wp-list-main">
                <span class="wp-status-badge ${status.key}"><i class="fas ${status.icon}"></i>${status.label}</span>
                <h5>${escapeHTML(machine.name || 'Activo sin nombre')}</h5>
                <p><strong>${escapeHTML(machine.id || '')}</strong> · ${escapeHTML(getWorkPlanLocation(machine) || 'Sin ubicación')} · ${escapeHTML(getMachineArea(machine) || 'Sin área')}</p>
            </div>
            <div class="wp-list-meta"><span>Categoría</span><strong><i class="fas ${categoryInfo.icon}"></i>${categoryInfo.label}</strong></div>
            <div class="wp-list-meta"><span>Planes</span><strong>${plans.length}</strong></div>
            <div class="wp-list-meta"><span>Frecuencia</span><strong>${getWorkPlanFrequencyLabel(nextPlan.frequencyUnit)}</strong></div>
            <div class="wp-list-meta"><span>Próxima fecha</span><strong>${nextPlan.nextDueDate || 'N/A'}</strong></div>
            <div class="wp-list-actions">
                <button class="btn btn-outline-primary btn-sm" onclick="renderWorkPlanDetails('${plans[0].fb_id || ''}')"><i class="fas fa-eye"></i>Ver</button>
                <button class="btn btn-light btn-sm" onclick="showMachinePlanHistory('${machine.id}')" title="Historial"><i class="fas fa-clock-rotate-left"></i></button>
            </div>
        </article>
    `;
}

function renderWorkPlans() {
    if (state.currentTab !== 'planes-trabajo' && initialLoadComplete) return;

    const gridContainer = document.getElementById('work-plans-bento-grid');
    if (!gridContainer) return;

    const searchTerm = (document.getElementById('work-plan-search-input').value || '').toLowerCase().trim();
    const locationFilter = document.getElementById('wp-filter-location').value || '';
    const areaFilter = document.getElementById('wp-filter-area').value || '';
    const critFilter = document.getElementById('wp-filter-criticality').value || '';
    const statusFilter = document.getElementById('wp-filter-status').value || '';
    const frequencyFilter = document.getElementById('wp-filter-frequency').value || '';
    const categoryFilter = state.workPlanCategoryFilter || 'all';

    const allGroups = buildWorkPlanGroups();
    updateWorkPlanSelectOptions('wp-filter-location', allGroups.map(g => getWorkPlanLocation(g.machine)), 'Todas');
    updateWorkPlanSelectOptions('wp-filter-area', allGroups.map(g => getMachineArea(g.machine)), 'Todas');

    const filteredGroups = allGroups.filter(group => {
        const machine = group.machine;
        const plans = group.plans;
        const groupStatus = getWorkPlanGroupStatus(plans).key;
        const searchPool = [
            machine.name, machine.id, getWorkPlanLocation(machine), getMachineArea(machine), machine.criticidad,
            ...plans.flatMap(p => [p.name, p.description, p.responsibleTechnician])
        ].filter(Boolean).join(' ').toLowerCase();

        return (!searchTerm || searchPool.includes(searchTerm)) &&
            (categoryFilter === 'all' || getAssetCategory(machine) === categoryFilter) &&
            (!locationFilter || getWorkPlanLocation(machine) === locationFilter) &&
            (!areaFilter || getMachineArea(machine) === areaFilter) &&
            (!critFilter || machine.criticidad === critFilter) &&
            (!statusFilter || groupStatus === statusFilter) &&
            (!frequencyFilter || plans.some(p => p.frequencyUnit === frequencyFilter));
    });

    updateWorkPlanStats(filteredGroups);

    const pageSize = state.workPlanPageSize || 9;
    const totalPages = Math.max(1, Math.ceil(filteredGroups.length / pageSize));
    state.workPlanPage = Math.min(Math.max(1, state.workPlanPage || 1), totalPages);
    const startIndex = (state.workPlanPage - 1) * pageSize;
    const paginatedGroups = filteredGroups.slice(startIndex, startIndex + pageSize);

    document.getElementById('wp-grid-view-btn').classList.toggle('active', state.workPlanViewMode !== 'list');
    document.getElementById('wp-list-view-btn').classList.toggle('active', state.workPlanViewMode === 'list');
    gridContainer.classList.toggle('wp-list-mode', state.workPlanViewMode === 'list');

    if (paginatedGroups.length === 0) {
        gridContainer.innerHTML = `
            <div class="wp-empty-state">
                <i class="fas fa-calendar-xmark"></i>
                <h5>No se encontraron planes</h5>
                <p>Ajusta los filtros o registra un nuevo plan para el activo.</p>
            </div>
        `;
    } else {
        gridContainer.innerHTML = paginatedGroups
            .map(group => state.workPlanViewMode === 'list' ? renderWorkPlanListRow(group) : renderWorkPlanCard(group))
            .join('');
    }

    renderWorkPlanPagination(filteredGroups.length);
}

window.setWorkPlanPage = setWorkPlanPage;
window.clearWorkPlanFilters = clearWorkPlanFilters;
window.setWorkPlanViewMode = setWorkPlanViewMode;

function renderWorkPlanDetailsLegacy(planId) {
    const detailView = document.getElementById('work-plan-detail-view');
    const gridView = document.getElementById('work-plans-grid-view');
    const content = document.getElementById('work-plan-details-content');
    const plan = state.workPlans.find(p => p.fb_id === planId);
    if (!plan) return;

    if (gridView) gridView.classList.add('d-none');
    if (detailView) detailView.classList.remove('d-none');
    document.getElementById('planes-trabajo').classList.add('wp-detail-active');

    const machine = state.machines.find(m => m.id === plan.machineId);
    const lastExecution = state.workPlanExecutions.filter(e => e.planId === plan.fb_id).sort((a,b) => new Date(b.executedAt) - new Date(a.executedAt))[0];
    const activeExecution = state.workPlanExecutions.find(ex => ex.planId === planId && (ex.status === 'En Proceso' || ex.status === 'Pausado'));
    const freqMap = { 'h': 'Hora(s)', 'd': 'Día(s)', 'w': 'Semana(s)', 'm': 'Mes(es)', 'y': 'Año(s)'};
    const freqText = `Cada ${plan.frequencyValue || 1} ${freqMap[plan.frequencyUnit] || 'Mes(es)'}`;
    const nextDate = plan.nextDueDate ? new Date(plan.nextDueDate + 'T00:00:00').toLocaleDateString('es-ES') : 'N/A';

    content.innerHTML = `
        <div class="wp-detail-view">
            <div class="wp-detail-header d-flex justify-content-between align-items-center">
                <div>
                    <span class="text-uppercase tracking-wider small opacity-75 fw-bold"><i class="fas fa-file-alt me-1"></i> Detalle del Plan</span>
                    <h2 class="fw-bold mb-0">${plan.name}</h2>
                </div>
                <div class="d-flex gap-2 flex-wrap">
                    <button class="btn btn-primary shadow-sm px-3 fw-bold" onclick="startWorkPlanExecutionWrapper('${plan.fb_id}')">
                        <i class="fas fa-play-circle me-2"></i>${activeExecution ? 'Continuar' : 'Ejecutar Plan'}
                    </button>
                    <button class="btn btn-outline-light px-3 fw-bold" onclick="showWorkPlanPreview('${plan.fb_id}')">
                        <i class="fas fa-eye me-2"></i>Previsualizar
                    </button>
                    <button class="btn btn-light px-3 fw-bold" onclick="showWorkPlanModal('${plan.fb_id}')">
                        <i class="fas fa-edit me-2"></i>Editar
                    </button>
                    <button class="btn btn-danger px-3 fw-bold" onclick="deleteWorkPlan('${plan.fb_id}')">
                        <i class="fas fa-trash me-2"></i>Eliminar
                    </button>
                </div>
            </div>
            <div class="p-4">
                <div class="row g-4 mb-4">
                    <div class="col-md-3"><div class="p-3 bg-light rounded-lg border text-center"><span class="text-muted small d-block text-uppercase fw-bold mb-1">Equipo</span><span class="fw-bold text-primary">${machine.name || 'N/A'}</span></div></div>
                    <div class="col-md-3"><div class="p-3 bg-light rounded-lg border text-center"><span class="text-muted small d-block text-uppercase fw-bold mb-1">Responsable</span><span class="fw-bold">${plan.responsibleTechnician || 'N/A'}</span></div></div>
                    <div class="col-md-3"><div class="p-3 bg-light rounded-lg border text-center"><span class="text-muted small d-block text-uppercase fw-bold mb-1">Frecuencia</span><span class="fw-bold text-info">${freqText}</span></div></div>
                    <div class="col-md-3"><div class="p-3 bg-light rounded-lg border text-center"><span class="text-muted small d-block text-uppercase fw-bold mb-1">Próxima Fecha</span><span class="fw-bold text-success">${nextDate}</span></div></div>
                </div>
                <div class="wp-glass-card mb-4 bg-light border-0 p-3 rounded">
                    <h6 class="text-uppercase small fw-bold text-muted mb-2">Objetivo del Plan</h6>
                    <p class="mb-0 text-dark" style="font-size: 1rem;">${plan.description || 'No se ha definido un objetivo específico.'}</p>
                </div>
                ${lastExecution ? `
                    <div class="card border-0 shadow-sm mb-4" style="background: #f8fafc; border-left: 4px solid #10b981 !important;">
                        <div class="card-body d-flex justify-content-between align-items-center">
                            <div><h6 class="mb-1 fw-bold text-success">Última ejecución</h6><p class="small text-muted mb-0">${new Date(lastExecution.executedAt).toLocaleString()} | Por: ${lastExecution.executedBy}</p></div>
                            <button class="btn btn-outline-success rounded-pill px-4" onclick="showPrettyExecutionReport('${lastExecution.id || lastExecution.fb_id}')"><i class="fas fa-file-invoice me-2"></i>Informe Ejecutivo</button>
                        </div>
                    </div>
                ` : ''}
                <div class="mt-4">
                    <h5 class="fw-bold mb-3 d-flex align-items-center"><i class="fas fa-tasks me-2 text-primary"></i>Actividades</h5>
                    <div class="row">
                        ${(plan.taskGroups || []).map(group => `
                            <div class="col-md-6 mb-3">
                                <div class="card h-100 border shadow-sm">
                                    <div class="card-header bg-white py-2"><h6 class="mb-0 fw-bold">${group.name}</h6></div>
                                    <div class="card-body p-0">
                                        <div class="list-group list-group-flush">
                                            ${group.tasks.map(task => `
                                                <div class="list-group-item py-2 d-flex justify-content-between align-items-center">
                                                    <span class="small fw-bold">${task.description}</span>
                                                    <span class="badge bg-light text-dark border x-small">${task.type}</span>
                                                </div>
                                            `).join('')}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        `).join('')}
                    </div>
                </div>
            </div>
        </div>
    `;
}

function getWorkPlanTaskRuntime(plan) {
    const executions = state.workPlanExecutions
        .filter(e => e.planId === plan.fb_id)
        .sort((a, b) => new Date(b.executedAt || b.startedAt || 0) - new Date(a.executedAt || a.startedAt || 0));
    const lastExecution = executions[0] || null;
    const sourceGroups = lastExecution?.taskGroups?.length ? lastExecution.taskGroups : (plan.taskGroups || []);
    const groups = sourceGroups.map(group => ({
        ...group,
        tasks: (group.tasks || []).map(task => ({
            ...task,
            status: task.status || 'Pendiente'
        }))
    }));
    const tasks = groups.flatMap(group => group.tasks || []);
    const isCompleted = status => ['Completada', 'Completado', 'Completa', 'OK', 'Realizado'].includes(status);
    const isNotApplicable = status => ['No aplica', 'N/A', 'No Aplicable'].includes(status);
    const completed = tasks.filter(t => isCompleted(t.status)).length;
    const notApplicable = tasks.filter(t => isNotApplicable(t.status)).length;
    const pending = Math.max(tasks.length - completed - notApplicable, 0);
    const accountable = Math.max(tasks.length - notApplicable, 0);
    const compliance = accountable ? Math.round((completed / accountable) * 100) : 0;

    return { groups, tasks, lastExecution, completed, pending, notApplicable, compliance };
}

function getWorkPlanTaskBadge(status = 'Pendiente') {
    if (['Completada', 'Completado', 'Completa', 'OK', 'Realizado'].includes(status)) {
        return { label: 'Completada', className: 'done', icon: 'fa-circle-check' };
    }
    if (['No aplica', 'N/A', 'No Aplicable'].includes(status)) {
        return { label: 'No aplica', className: 'na', icon: 'fa-circle-minus' };
    }
    return { label: 'Pendiente', className: 'pending', icon: 'fa-clock' };
}

function getTaskPriorityInfo(task = {}, index = 0) {
    const rawPriority = task.priority || task.prioridad || (task.allowAlerta ? 'Alta' : (index % 3 === 0 ? 'Alta' : 'Media'));
    const priority = ['Alta', 'Media', 'Baja'].includes(rawPriority) ? rawPriority : 'Media';
    return {
        label: priority,
        className: priority === 'Alta' ? 'high' : priority === 'Baja' ? 'low' : 'medium'
    };
}

function formatWorkPlanDate(value, includeTime = false) {
    if (!value) return 'Sin registro';
    const raw = typeof value.toDate === 'function' ? value.toDate() : value;
    const date = new Date(raw);
    if (Number.isNaN(date.getTime())) return 'Sin registro';
    return includeTime ? date.toLocaleString('es-ES') : date.toLocaleDateString('es-ES');
}

function normalizeRole(role = '') {
    return role.normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase();
}

function getUserRecordByUsername(username) {
    if (!username) return null;
    if (getCurrentUser().username === username) return state.currentUser;
    return state.technicians.find(t => t.username === username || t.name === username) || null;
}

function getUserDisplayName(username) {
    const user = getUserRecordByUsername(username);
    return user.fullName || user.displayName || user.name || user.username || username || '';
}

function isWorkPlanCreatorRole(role) {
    const normalized = normalizeRole(role);
    return normalized === 'admin' || normalized === 'planificador';
}

function isWorkPlanExecutorRole(role) {
    const normalized = normalizeRole(role);
    return normalized === 'tecnico' || normalized === 'planificador';
}

function isValidWorkPlanCreator(username) {
    const user = getUserRecordByUsername(username);
    return !!user && isWorkPlanCreatorRole(user.role);
}

function getCurrentWorkPlanCreator() {
    return isWorkPlanCreatorRole(getCurrentUser().role) ? getCurrentUser().username : '';
}

function getWorkPlanCreatorDisplay(plan = {}) {
    if (plan.createdBy && isValidWorkPlanCreator(plan.createdBy)) return getUserDisplayName(plan.createdBy);
    if (plan.createdByName && isValidWorkPlanCreator(plan.createdByName)) return getUserDisplayName(plan.createdByName);
    return getUserDisplayName(getCurrentWorkPlanCreator()) || 'Sin registro';
}

function renderWorkPlanDetails(planId) {
    const detailView = document.getElementById('work-plan-detail-view');
    const gridView = document.getElementById('work-plans-grid-view');
    const content = document.getElementById('work-plan-details-content');
    const plan = state.workPlans.find(p => p.fb_id === planId);
    if (!plan || !content) return;

    if (gridView) gridView.classList.add('d-none');
    if (detailView) detailView.classList.remove('d-none');
    document.getElementById('planes-trabajo').classList.add('wp-detail-active');

    const machine = state.machines.find(m => m.id === plan.machineId) || {};
    const activeExecution = state.workPlanExecutions.find(ex => ex.planId === planId && (ex.status === 'En Proceso' || ex.status === 'Pausado'));
    const runtime = getWorkPlanTaskRuntime(plan);
    const freqMap = { h: 'Hora(s)', d: 'Día(s)', w: 'Semana(s)', m: 'Mes(es)', y: 'Año(s)' };
    const freqText = `Cada ${plan.frequencyValue || 1} ${freqMap[plan.frequencyUnit] || 'Mes(es)'}`;
    const nextDate = plan.nextDueDate ? new Date(plan.nextDueDate + 'T00:00:00').toLocaleDateString('es-ES') : 'N/A';
    const machinePhoto = machine.fotoUrl || machine.imageUrl || 'https://images.unsplash.com/photo-1581091226825-a6a2a5aee158q=80&w=800';
    const lastExecutionDate = formatWorkPlanDate(runtime.lastExecution.executedAt || runtime.lastExecution.startedAt, true);
    const createdDate = formatWorkPlanDate(plan.createdAt);
    const updatedDate = plan.updatedAt ? formatWorkPlanDate(plan.updatedAt, true) : lastExecutionDate;
    const createdBy = getWorkPlanCreatorDisplay(plan);
    const circumference = 188.5;
    const progressOffset = circumference - (circumference * runtime.compliance / 100);

    content.innerHTML = `
        <section class="wp-preview-page">
            <div class="wp-preview-back">
                <button class="btn btn-link text-decoration-none p-0" onclick="showWorkPlansGrid()">
                    <i class="fas fa-arrow-left"></i>Volver al listado
                </button>
            </div>

            <header class="wp-preview-hero">
                <div class="wp-preview-image-wrap">
                    <img src="${machinePhoto}" alt="${escapeHTML(machine.name || plan.machineId || 'Equipo')}" class="wp-preview-image">
                </div>
                <div class="wp-preview-heading">
                    <span><i class="fas fa-clipboard-list"></i>Detalle del plan</span>
                    <h2>${escapeHTML(plan.name || 'Plan de mantenimiento')}</h2>
                    <small>${escapeHTML(machine.id || plan.machineId || 'Sin código')}</small>
                </div>
                <div class="wp-preview-actions">
                    <button class="btn btn-primary" onclick="startWorkPlanExecutionWrapper('${plan.fb_id}')"><i class="fas fa-play-circle"></i>${activeExecution ? 'Continuar' : 'Ejecutar Plan'}</button>
                    <button class="btn btn-outline-light" onclick="showWorkPlanPreview('${plan.fb_id}')"><i class="fas fa-eye"></i>Previsualizar</button>
                    <div class="wp-preview-action-row">
                        <button class="btn btn-light" onclick="showWorkPlanModal('${plan.fb_id}')"><i class="fas fa-pen-to-square"></i>Editar</button>
                        <button class="btn btn-danger" onclick="deleteWorkPlan('${plan.fb_id}')"><i class="fas fa-trash"></i>Eliminar</button>
                    </div>
                </div>
            </header>

            <div class="wp-preview-info-grid">
                <article><i class="fas fa-microchip"></i><span>Equipo</span><strong>${escapeHTML(machine.name || 'N/A')}</strong></article>
                <article><i class="fas fa-user-gear"></i><span>Responsable</span><strong>${escapeHTML(plan.responsibleTechnician || 'N/A')}</strong></article>
                <article><i class="fas fa-calendar-days"></i><span>Frecuencia</span><strong>${escapeHTML(freqText)}</strong></article>
                <article><i class="fas fa-calendar-check"></i><span>Próxima fecha</span><strong>${escapeHTML(nextDate)}</strong></article>
            </div>

            <section class="wp-objective-card">
                <div>
                    <h5>Objetivo del plan</h5>
                    <p>${escapeHTML(plan.description || 'No se ha definido un objetivo específico para este plan.')}</p>
                </div>
                <div class="wp-objective-illustration"><i class="fas fa-bullseye"></i></div>
            </section>

            <section class="wp-last-execution ${runtime.lastExecution ? '' : 'empty'}">
                <div class="wp-last-main">
                    <i class="fas ${runtime.lastExecution ? 'fa-circle-check' : 'fa-circle-info'}"></i>
                    <div>
                        <strong>${runtime.lastExecution ? 'Última ejecución' : 'Sin ejecuciones registradas'}</strong>
                        <span>${runtime.lastExecution ? `${lastExecutionDate} · por ${escapeHTML(runtime.lastExecution.executedBy || 'N/A')}` : 'El plan aún no tiene historial de ejecución.'}</span>
                    </div>
                </div>
                ${runtime.lastExecution ? `<button class="btn btn-outline-success" onclick="showPrettyExecutionReport('${runtime.lastExecution.id || runtime.lastExecution.fb_id}')"><i class="fas fa-file-invoice"></i>Informe Ejecutivo</button>` : ''}
            </section>

            <section class="wp-plan-workspace">
                <div class="wp-preview-tabs">
                    <button class="active" type="button">Actividades del plan</button>
                    <button type="button" onclick="showMachinePlanHistory('${machine.id || plan.machineId}')">Historial de ejecuciones</button>
                    <button type="button">Documentos</button>
                    <button type="button">Notas</button>
                </div>

                <div class="wp-activity-stats">
                    <article><span>Total actividades</span><strong>${runtime.tasks.length}</strong><i class="fas fa-clipboard-list"></i></article>
                    <article><span>Completadas</span><strong>${runtime.completed}</strong><i class="fas fa-circle-check"></i></article>
                    <article><span>Pendientes</span><strong>${runtime.pending}</strong><i class="fas fa-clock"></i></article>
                    <article><span>No aplicables</span><strong>${runtime.notApplicable}</strong><i class="fas fa-circle-minus"></i></article>
                    <article class="wp-compliance-ring-card">
                        <span>Cumplimiento</span>
                        <div class="wp-compliance-ring">
                            <svg viewBox="0 0 72 72" aria-hidden="true">
                                <circle cx="36" cy="36" r="30"></circle>
                                <circle cx="36" cy="36" r="30" style="stroke-dashoffset:${progressOffset}"></circle>
                            </svg>
                            <strong>${runtime.compliance}%</strong>
                        </div>
                    </article>
                </div>

                <div class="wp-activity-toolbar">
                    <label>Agrupar por: <select><option>Sección</option></select></label>
                    <button type="button"><i class="fas fa-filter"></i>Filtrar actividades</button>
                </div>

                <div class="wp-activity-groups">
                    ${runtime.groups.length ? runtime.groups.map((group, groupIndex) => {
                        const tasks = group.tasks || [];
                        const completed = tasks.filter(t => ['Completada', 'Completado', 'Completa', 'OK', 'Realizado'].includes(t.status)).length;
                        const pct = tasks.length ? Math.round((completed / tasks.length) * 100) : 0;
                        return `
                            <article class="wp-activity-group">
                                <div class="wp-activity-group-header">
                                    <div><i class="fas fa-gear"></i><h5>${escapeHTML(group.name || `Sección ${groupIndex + 1}`)}</h5></div>
                                    <div class="wp-group-progress"><span>${completed} / ${tasks.length} completadas</span><div><b style="width:${pct}%"></b></div><strong>${pct}%</strong><i class="fas fa-chevron-up"></i></div>
                                </div>
                                <div class="wp-activity-table">
                                    <div class="wp-activity-row wp-activity-head"><span>Actividad</span><span>Tipo</span><span>Estado</span><span>Prioridad</span><span></span></div>
                                    ${tasks.map((task, taskIndex) => {
                                        const status = getWorkPlanTaskBadge(task.status);
                                        const priority = getTaskPriorityInfo(task, taskIndex);
                                        return `
                                            <div class="wp-activity-row">
                                                <span>${escapeHTML(task.description || 'Actividad sin descripción')}</span>
                                                <span>${escapeHTML(task.type || 'Inspección')}</span>
                                                <span><em class="${status.className}"><i class="fas ${status.icon}"></i>${status.label}</em></span>
                                                <span><mark class="${priority.className}">${priority.label}</mark></span>
                                                <span><i class="fas fa-ellipsis-vertical"></i></span>
                                            </div>
                                        `;
                                    }).join('')}
                                </div>
                            </article>
                        `;
                    }).join('') : `
                        <div class="wp-empty-state">
                            <i class="fas fa-list-check"></i>
                            <h5>Este plan no tiene actividades</h5>
                            <p>Edita el plan para agregar secciones y tareas de mantenimiento.</p>
                        </div>
                    `}
                </div>
            </section>

            <footer class="wp-plan-audit-strip">
                <div class="wp-audit-item">
                    <i class="fas fa-circle-info"></i>
                    <span>Plan creado el</span>
                    <strong>${escapeHTML(createdDate)}</strong>
                </div>
                <div class="wp-audit-item">
                    <i class="fas fa-user-pen"></i>
                    <span>Creado por</span>
                    <strong>${escapeHTML(createdBy)}</strong>
                </div>
                <div class="wp-audit-item">
                    <i class="fas fa-clock-rotate-left"></i>
                    <span>Última actualización</span>
                    <strong>${escapeHTML(updatedDate)}</strong>
                </div>
            </footer>
        </section>
    `;
}

function showWorkPlanModal(planId = null) {
    const form = document.getElementById('work-plan-form');
    form.reset();
    document.getElementById('task-groups-container').innerHTML = '';

    const machineSelect = document.getElementById('work-plan-machine');
    let mHtml = '<option value="">Seleccione una máquina...</option>';
    state.machines.forEach(m => {
        const locationText = m.location ? ` | ${m.location}` : '';
        mHtml += `<option value="${m.id}">${m.id} - ${m.name}${locationText}</option>`;
    });
    machineSelect.innerHTML = mHtml;

    const techSelect = document.getElementById('work-plan-responsible');
    let tHtml = '<option value="">Seleccione un técnico...</option>';
    state.technicians.filter(t => ['Técnico', 'Admin', 'Planificador'].includes(t?.role) && t?.isActive !== false).forEach(t => tHtml += `<option value="${t.username}">${t.username}</option>`);
    techSelect.innerHTML = tHtml;

    if (planId) {
        document.getElementById('work-plan-modal-title').textContent = 'Editar Plan de Trabajo';
        const plan = state.workPlans.find(p => p.fb_id === planId);
        if (plan) {
            document.getElementById('work-plan-id-hidden').value = plan.fb_id;
            document.getElementById('work-plan-name').value = plan.name;
            document.getElementById('work-plan-machine').value = plan.machineId;
            document.getElementById('work-plan-description').value = plan.description;
            document.getElementById('work-plan-responsible').value = plan.responsibleTechnician;
            document.getElementById('work-plan-frequency-value').value = plan.frequencyValue || 1;
            document.getElementById('work-plan-frequency-unit').value = plan.frequencyUnit || 'm';
            document.getElementById('work-plan-start-date').value = plan.nextDueDate;

            (plan.taskGroups || []).forEach(group => {
                addTaskGroup(group.name, group.layout || 'table', group.tasks);
            });
        }
    } else {
        document.getElementById('work-plan-modal-title').textContent = 'Nuevo Plan de Trabajo';
        document.getElementById('work-plan-id-hidden').value = '';
    }

    state.modals.workPlan.show();
}

async function handleWorkPlanSubmit() {
    const planId = document.getElementById('work-plan-id-hidden').value;
    const oldPlan = planId ? state.workPlans.find(p => p.fb_id === planId) : null;
    const currentCreator = getCurrentWorkPlanCreator();
    const taskGroups = [];
    document.querySelectorAll('#task-groups-container .task-group').forEach(groupEl => {
        const groupName = groupEl.querySelector('.task-group-name-input').value;
        const groupLayout = groupEl.querySelector('.task-group-layout-select').value;
        const tasks = [];
        groupEl.querySelectorAll('.task-item').forEach(taskEl => {
            const description = taskEl.querySelector('.task-description-input').value;
            if (description) {
                const type = taskEl.querySelector('.task-type-select').value;
                const fieldsInput = taskEl.querySelector('.task-multi-fields-input').value;
                tasks.push({
                    description: description,
                    helperText: taskEl.querySelector('.task-helper-input').value,
                    type: type,
                    unit: taskEl.querySelector('.task-unit-input').value,
                    requirePhoto: taskEl.querySelector('.task-photo-check').checked,
                    allowAlerta: taskEl.querySelector('.task-alerta-check').checked,
                    fields: fieldsInput ? fieldsInput.split(',').map(s => s.trim()).filter(s => s) : null
                });
            }
        });
        if (groupName && tasks.length > 0) {
            taskGroups.push({ name: groupName, layout: groupLayout, tasks: tasks });
        }
    });

    const planData = {
        name: document.getElementById('work-plan-name').value,
        machineId: document.getElementById('work-plan-machine').value,
        description: document.getElementById('work-plan-description').value,
        responsibleTechnician: document.getElementById('work-plan-responsible').value,
        frequencyValue: parseInt(document.getElementById('work-plan-frequency-value').value),
        frequencyUnit: document.getElementById('work-plan-frequency-unit').value,
        nextDueDate: document.getElementById('work-plan-start-date').value,
        taskGroups: taskGroups,
        isActive: true,
        updatedAt: new Date().toISOString(),
        updatedBy: getCurrentUser().username || 'Sistema'
    };

    if (!planData.name || !planData.machineId || !planData.responsibleTechnician || !planData.nextDueDate) {
        showToast('Por favor, complete todos los campos obligatorios.', 'error');
        return;
    }

    const oldCreatorIsValid = oldPlan.createdBy && isValidWorkPlanCreator(oldPlan.createdBy);

    if (!oldCreatorIsValid && !currentCreator) {
        showToast('Solo un Administrador o Planificador puede crear planes de mantenimiento.', 'error');
        return;
    }

    planData.createdBy = oldCreatorIsValid ? oldPlan.createdBy : currentCreator;
    planData.createdAt = oldPlan.createdAt || new Date().toISOString();

    showLoading(true);
    try {
        if (planId) {
            await setDoc(doc(state.collections.workPlans, planId), planData, { merge: true });
            await recordAuditLog('workPlans', planId, 'UPDATE', oldPlan, planData);
            showToast('Plan de trabajo actualizado.', 'success');
        } else {
            const docRef = await addDoc(state.collections.workPlans, planData);
            await recordAuditLog('workPlans', docRef.id, 'CREATE', null, planData);
            showToast('Plan de trabajo creado.', 'success');
        }
        state.modals.workPlan.hide();
        renderWorkPlans();
    } catch (error) {
        console.error("Error saving work plan:", error);
        showToast('Error al guardar el plan de trabajo.', 'error');
    } finally {
        showLoading(false);
    }
}

function deleteWorkPlan(planId) {
    showConfirmation('Eliminar Plan de Trabajo', '¿Está seguro de que desea eliminar este planEsta acción no se puede deshacer.', async () => {
        showLoading(true);
        try {
            const oldPlan = state.workPlans.find(p => p.fb_id === planId);
            await deleteDoc(doc(state.collections.workPlans, planId));
            await recordAuditLog('workPlans', planId, 'DELETE', oldPlan, null);
            showToast('Plan de trabajo eliminado.', 'success');
            document.getElementById('work-plan-details-container').classList.add('d-none');
            document.getElementById('work-plan-placeholder').classList.remove('d-none');
        } catch (error) {
            console.error("Error deleting work plan:", error);
            showToast('Error al eliminar el plan.', 'error');
        } finally {
            showLoading(false);
        }
    });
}

function addTaskGroup(name = '', layout = 'table', tasks = []) {
    const container = document.getElementById('task-groups-container');
    const groupEl = document.createElement('div');
    groupEl.className = 'card mb-3 task-group';
    groupEl.innerHTML = `
        <div class="card-header p-2 bg-light d-flex align-items-center gap-2">
            <div class="task-group-handle" title="Arrastrar para reordenar esta sección"><i class="fas fa-grip-vertical"></i></div>
            <input type="text" class="form-control form-control-sm task-group-name-input" placeholder="Nombre de la Sección (ej: Reductor)" value="${name}">
            <select class="form-select form-select-sm task-group-layout-select" style="width: auto;">
                <option value="table" ${layout === 'table' ? 'selected' : ''}>Vista Tabla</option>
                <option value="cards" ${layout === 'cards' ? 'selected' : ''}>Vista Tarjetas</option>
            </select>
            <button class="btn btn-outline-danger btn-sm" type="button" onclick="this.closest('.task-group').remove()"><i class="fas fa-trash"></i></button>
        </div>
        <div class="card-body p-2">
            <div class="tasks-list"></div>
            <div class="mt-2 text-center">
                <button type="button" class="btn btn-outline-primary btn-sm add-task-btn"><i class="fas fa-plus me-1"></i>Añadir Item de Formulario</button>
            </div>
        </div>
    `;

    const tasksList = groupEl.querySelector('.tasks-list');

    // Initialize Sortable for individual tasks within this group
    // 'group' allows dragging tasks between different groups/sections
    new Sortable(tasksList, {
        animation: 150,
        handle: '.task-handle',
        ghostClass: 'sortable-ghost',
        group: 'tasks'
    });

    const addTaskItem = (task = null) => {
        const taskEl = document.createElement('div');
        taskEl.className = 'border rounded p-2 mb-2 bg-white shadow-sm task-item d-flex align-items-start';
        taskEl.innerHTML = `
            <div class="task-handle" title="Arrastrar para reordenar o mover a otra sección"><i class="fas fa-grip-vertical"></i></div>
            <div class="flex-grow-1">
                <div class="d-flex justify-content-between align-items-start mb-2">
                    <div class="flex-grow-1 me-2">
                        <input type="text" class="form-control form-control-sm fw-bold task-description-input mb-1" placeholder="Título de la tarea (ej: Nivel de aceite)" value="${task.description || ''}">
                        <input type="text" class="form-control form-control-sm task-helper-input" placeholder="Instrucción corta (ej: Verificar visor frontal)" value="${task.helperText || ''}">
                    </div>
                    <button class="btn btn-link text-danger p-0" type="button" onclick="this.closest('.task-item').remove()"><i class="fas fa-trash"></i></button>
                </div>
            <div class="row g-2 align-items-center">
                <div class="col-md-4">
                    <label class="x-small fw-bold mb-1 d-block">Tipo de Campo</label>
                    <select class="form-select form-select-sm task-type-select">
                        <option value="status_detail" ${task.type === 'status_detail' ? 'selected' : ''}>Estado (OK/FALLO) + Detalle</option>
                        <option value="text" ${task.type === 'text' ? 'selected' : ''}>Solo Texto/Observación</option>
                        <option value="numeric" ${task.type === 'numeric' ? 'selected' : ''}>Medición Numérica</option>
                        <option value="multi_numeric" ${task.type === 'multi_numeric' ? 'selected' : ''}>Múltiples Mediciones</option>
                    </select>
                </div>
                <div class="col-md-3 unit-container ${['numeric', 'multi_numeric'].includes(task.type) ? '' : 'd-none'}">
                    <label class="x-small fw-bold mb-1 d-block">Unidad</label>
                    <input type="text" class="form-control form-control-sm task-unit-input" placeholder="ej: °C, A, PSI" value="${task.unit || ''}">
                </div>
                <div class="col-md-5 d-flex align-items-end gap-3 pb-1">
                    <div class="form-check">
                        <input class="form-check-input task-photo-check" type="checkbox" ${task.requirePhoto ? 'checked' : ''}>
                        <label class="form-check-label x-small">Foto Oblig.</label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input task-alerta-check" type="checkbox" ${task.allowAlerta !== false ? 'checked' : ''}>
                        <label class="form-check-label x-small">Permitir ALERTA</label>
                    </div>
                </div>
            </div>
            <div class="multi-fields-container mt-2 ${task.type === 'multi_numeric' ? '' : 'd-none'}">
                <label class="x-small fw-bold mb-1 d-block">Sub-campos (ej: Fase L1, Fase L2)</label>
                <input type="text" class="form-control form-control-sm task-multi-fields-input" placeholder="Separados por coma" value="${task.fields ? task.fields.join(', ') : ''}">
            </div>
            </div>
        `;

        const typeSelect = taskEl.querySelector('.task-type-select');
        typeSelect.addEventListener('change', (e) => {
            const val = e.target.value;
            taskEl.querySelector('.unit-container').classList.toggle('d-none', !['numeric', 'multi_numeric'].includes(val));
            taskEl.querySelector('.multi-fields-container').classList.toggle('d-none', val !== 'multi_numeric');
        });

        tasksList.appendChild(taskEl);
    };

    if (tasks && tasks.length > 0) {
        tasks.forEach(task => addTaskItem(task));
    } else {
        addTaskItem();
    }

    groupEl.querySelector('.add-task-btn').addEventListener('click', () => addTaskItem());

    container.appendChild(groupEl);
}

async function executeWorkPlan(planId, isReadOnly = false) {
    const plan = state.workPlans.find(p => p.fb_id === planId);
    if (!plan) return;

    // Visualization mode: we don't create anything in Firestore until Pause or Finish is clicked.
    const executionTaskGroups = (plan.taskGroups || []).map(group => ({
        ...group,
        tasks: group.tasks.map(task => ({ ...task, status: 'Pendiente' }))
    }));

    const executionData = {
        planId: planId,
        executedAt: new Date().toISOString(),
        executedBy: getCurrentUser().username,
        taskGroups: executionTaskGroups,
        status: 'En Proceso',
        isLocal: true, // flag to indicate it's not yet in Firestore
        isReadOnly: isReadOnly
    };

    startWorkPlanExecution(executionData, plan);
}

// --- Plan Execution View Functions ---
window.updateTaskValue = (groupIndex, taskIndex, value) => {
    if (state.currentExecution && state.currentExecution.execution) {
        state.currentExecution.execution.taskGroups[groupIndex].tasks[taskIndex].value = value;
    }
};

window.updateTaskDetail = (groupIndex, taskIndex, detail) => {
    if (state.currentExecution && state.currentExecution.execution) {
        state.currentExecution.execution.taskGroups[groupIndex].tasks[taskIndex].detail = detail;
    }
};

window.updateTaskMultiValue = (groupIndex, taskIndex, fieldIndex, value) => {
    if (state.currentExecution && state.currentExecution.execution) {
        const task = state.currentExecution.execution.taskGroups[groupIndex].tasks[taskIndex];
        if (!task.multiValues) task.multiValues = [];
        task.multiValues[fieldIndex] = value;
    }
};

function startWorkPlanExecution(execution, plan) {
    if (!execution || !plan) {
        showToast('Faltan datos para iniciar la ejecución del plan.', 'error');
        return;
    }
    // Deep copy execution for session-only changes unless explicitly saved (Pause/Finish)
    state.currentExecution = {
        execution: JSON.parse(JSON.stringify(execution)),
        plan
    };

    document.getElementById('app-wrapper').classList.add('d-none');
    document.getElementById('plan-execution-view').classList.remove('d-none');

    renderWorkPlanExecution(state.currentExecution.execution, plan);
}

async function uploadExecutionFile(executionId, file) {
    if (file.size > 10 * 1024 * 1024) {
        showToast('El archivo es demasiado grande (Máximo 10MB)', 'error');
        return null;
    }
    const timestamp = Date.now();
    const cleanName = file.name.replace(/[^a-z0-9.]/gi, '_').toLowerCase();
    const fileName = `evidence_${timestamp}_${cleanName}`;
    const storageRef = ref(state.storage, `artifacts/${appId}/executions/${executionId}/${fileName}`);
    const snapshot = await uploadBytes(storageRef, file);
    return await getOptimizedStorageUrl(snapshot.ref);
}

function renderWorkPlanExecution(execution, plan) {
    const container = document.getElementById('execution-view-body');
    container.innerHTML = '';
    document.getElementById('execution-view-title').textContent = execution.isReadOnly ? `Visualizando plan: ${plan.name}` : `Ejecución de plan: ${plan.name}`;

    // Info header for better context
    const subtitle = document.getElementById('execution-view-subtitle');
    if (subtitle) {
        const dateStr = execution.executedAt ? new Date(execution.executedAt).toLocaleString() : 'No iniciada';
        subtitle.innerHTML = `
            <div class="execution-breadcrumb">
                <span>Mantenimiento</span><i class="fas fa-chevron-right"></i>
                <span>Ejecución</span><i class="fas fa-chevron-right"></i>
                <strong>${escapeHTML(plan.machineId || 'Plan')}</strong>
            </div>
            <div class="execution-meta-row">
                <span><i class="fas fa-microchip"></i>ID: ${escapeHTML(plan.machineId || 'N/A')}</span>
                <span><i class="fas fa-user-gear"></i>Responsable: ${escapeHTML(execution.executedBy || 'Sin asignar')}</span>
                <span><i class="fas fa-calendar-alt"></i>${dateStr}</span>
                <mark>${escapeHTML(execution.status || 'En Proceso')}</mark>
            </div>
            <div class="execution-objective-card">
                <i class="fas fa-bullseye"></i>
                <span>Objetivo</span>
                <strong>${escapeHTML(plan.description || 'Cumplir con el cronograma de mantenimiento preventivo.')}</strong>
            </div>
        `;
    }

    if (execution.isMock) {
        const alert = document.createElement('div');
        alert.className = 'col-12';
        alert.innerHTML = `
            <div class="alert alert-info d-flex align-items-center shadow-sm mb-0" role="alert">
                <i class="fas fa-info-circle fa-2x me-3"></i>
                <div>
                    <h5 class="alert-heading mb-1 fw-bold">Plan Pendiente de Ejecución</h5>
                    <p class="mb-0">Este plan aún no ha sido iniciado por el técnico responsable. Lo que ve a continuación es la estructura del plan sin resultados registrados.</p>
                </div>
            </div>
        `;
        container.appendChild(alert);
    } else if (execution.isReadOnly) {
        const alert = document.createElement('div');
        alert.className = 'col-12';
        alert.innerHTML = `
            <div class="alert alert-warning d-flex align-items-center shadow-sm mb-0" role="alert">
                <i class="fas fa-eye fa-2x me-3"></i>
                <div>
                    <h5 class="alert-heading mb-1 fw-bold">Modo Visualización</h5>
                    <p class="mb-0">Usted está viendo el avance actual de este plan. Solo el técnico responsable puede realizar cambios.</p>
                </div>
            </div>
        `;
        container.appendChild(alert);
    }

    const pauseBtn = document.getElementById('execution-view-pause-btn');
    const finishBtn = document.getElementById('execution-view-finish-btn');
    const historyBtn = document.getElementById('execution-view-history-btn');

    if (historyBtn) {
        if (execution.isMock) {
            historyBtn.classList.remove('d-none');
        } else {
            historyBtn.classList.add('d-none');
        }
    }

    if (execution.isReadOnly) {
        if (pauseBtn) pauseBtn.classList.add('d-none');
        if (finishBtn) finishBtn.classList.add('d-none');
    } else {
        if (pauseBtn) {
            pauseBtn.classList.remove('d-none');
            pauseBtn.innerHTML = execution.status === 'Pausado' ? '<i class="fas fa-play-circle"></i> Reanudar Trabajo' : '<i class="fas fa-save"></i> Guardar Borrador';
        }
        if (finishBtn) finishBtn.classList.remove('d-none');
    }

    const taskGroups = execution.taskGroups || [];
    if (taskGroups.length === 0) {
        container.innerHTML = '<div class="col-12"><p class="text-muted text-center">Este plan no tiene tareas definidas.</p></div>';
        return;
    }

    taskGroups.forEach((group, groupIndex) => {
        const isTable = group.layout === 'table';
        const groupCol = document.createElement('div');
        groupCol.className = 'execution-group-column col-12 col-xl-6';

        let html = `
            <div class="execution-section-header">
                <div class="execution-section-icon">
                    <i class="fas ${isTable ? 'fa-gears' : 'fa-bolt'}"></i>
                </div>
                <h4 class="mb-0 fw-bold">Sección: ${escapeHTML(group.name || `Grupo ${groupIndex + 1}`)}</h4>
            </div>
        `;

        if (isTable) {
            html += `
                <div class="execution-table-card">
                    <div class="table-responsive">
                        <table class="execution-table">
                            <thead>
                                <tr>
                                    <th>Tarea de Inspección</th>
                                    <th style="width: 160px; text-align: center;">Estado</th>
                                    <th>Medición / Detalle</th>
                                </tr>
                            </thead>
                            <tbody id="group-body-${groupIndex}"></tbody>
                        </table>
                    </div>
                </div>
            `;
        } else {
            html += `<div id="group-body-${groupIndex}" class="row g-3"></div>`;
        }

        groupCol.innerHTML = html;
        container.appendChild(groupCol);

        const groupBody = document.getElementById(`group-body-${groupIndex}`);

        group.tasks.forEach((task, taskIndex) => {
            if (isTable) {
                renderTableTaskRow(groupBody, task, groupIndex, taskIndex, execution);
            } else {
                renderCardTaskItem(groupBody, task, groupIndex, taskIndex, execution);
            }
        });
    });
}

function getExecutionStatusClass(status = '') {
    if (status === 'Aprobó') return 'ok';
    if (status === 'Fallo') return 'fallo';
    if (status === 'Alerta') return 'alerta';
    return 'pending';
}

function renderExecutionStatusSelect(task, groupIndex, taskIndex, execution) {
    const readOnly = execution.isReadOnly ? 'disabled' : '';
    const status = task.status || 'Pendiente';
    return `
        <label class="execution-status-control ${getExecutionStatusClass(status)}">
            <span>Estado</span>
            <select class="execution-status-select ${getExecutionStatusClass(status)}" data-group="${groupIndex}" data-task="${taskIndex}" ${readOnly}>
                <option value="Pendiente" ${status === 'Pendiente' ? 'selected' : ''}>Seleccionar</option>
                <option value="Aprobó" ${status === 'Aprobó' ? 'selected' : ''}>Aprobó</option>
                ${task.allowAlerta !== false ? `<option value="Alerta" ${status === 'Alerta' ? 'selected' : ''}>Alerta</option>` : ''}
                <option value="Fallo" ${status === 'Fallo' ? 'selected' : ''}>Fallo</option>
            </select>
        </label>
    `;
}

function bindExecutionStatusSelects(scope, execution) {
    scope.querySelectorAll('.execution-status-select').forEach(select => {
        select.addEventListener('change', () => {
            if (execution.isReadOnly) return;
            const groupIndex = parseInt(select.dataset.group, 10);
            const taskIndex = parseInt(select.dataset.task, 10);
            execution.taskGroups[groupIndex].tasks[taskIndex].status = select.value;
            renderWorkPlanExecution(execution, state.currentExecution.plan);
        });
    });
}

function renderTableTaskRow(container, task, groupIndex, taskIndex, execution) {
    const row = document.createElement('tr');
    row.className = 'execution-row';
    if (task.status && task.status !== 'Pendiente') {
        row.classList.add(`status-${task.status}`);
    }

    const readOnly = execution.isReadOnly ? 'disabled' : '';
    row.innerHTML = `
        <td>
            <div class="execution-task-title">${escapeHTML(task.description || 'Tarea sin descripción')}</div>
            <div class="execution-task-helper">${escapeHTML(task.helperText || '')}</div>
        </td>
        <td>
            ${renderExecutionStatusSelect(task, groupIndex, taskIndex, execution)}
        </td>
        <td>
            ${renderTaskField(task, groupIndex, taskIndex, execution)}
        </td>
    `;

    bindExecutionStatusSelects(row, execution);

    container.appendChild(row);
}

function renderCardTaskItem(container, task, groupIndex, taskIndex, execution) {
    const col = document.createElement('div');
    col.className = 'col-12';

    const card = document.createElement('div');
    card.className = 'execution-card-item execution-card';
    if (task.status && task.status !== 'Pendiente') {
        card.classList.add(`status-${task.status}`);
    }

    const readOnly = execution.isReadOnly ? 'disabled' : '';
    card.innerHTML = `
        <div class="d-flex justify-content-between align-items-start mb-3">
            <div class="execution-card-copy">
                <h5>${escapeHTML(task.description || 'Tarea sin descripción')}</h5>
                <div>${escapeHTML(task.helperText || '')}</div>
            </div>
            ${renderExecutionStatusSelect(task, groupIndex, taskIndex, execution)}
        </div>
        ${renderTaskField(task, groupIndex, taskIndex, execution, true)}
    `;

    bindExecutionStatusSelects(card, execution);

    col.appendChild(card);
    container.appendChild(col);
}

function renderTaskField(task, groupIndex, taskIndex, execution, isCard = false) {
    let html = '';
    const readOnly = execution.isReadOnly ? 'disabled' : '';

    if (task.type === 'numeric') {
        html = `
            <div class="measurement-input-wrapper">
                <input type="number" class="form-control form-control-sm task-value-input"
                    placeholder="0.0" value="${task.value || ''}" ${readOnly}
                    oninput="window.updateTaskValue(${groupIndex}, ${taskIndex}, this.value)">
                <span class="measurement-unit-label">${task.unit || ''}</span>
            </div>
        `;
    } else if (task.type === 'multi_numeric') {
        html = `<div class="row g-2">`;
        (task.fields || []).forEach((field, fIdx) => {
            const val = task.multiValues ? task.multiValues[fIdx] : '';
            html += `
                <div class="col-6">
                    <label class="x-small fw-bold text-muted text-uppercase mb-1">${field}</label>
                    <div class="measurement-input-wrapper">
                        <input type="number" class="form-control form-control-sm task-multi-value-input"
                            data-index="${fIdx}" placeholder="0.0" value="${val || ''}" ${readOnly}
                            oninput="window.updateTaskMultiValue(${groupIndex}, ${taskIndex}, ${fIdx}, this.value)">
                        <span class="measurement-unit-label">${task.unit || ''}</span>
                    </div>
                </div>
            `;
        });
        html += `</div>`;
    } else if (task.type === 'status_detail' || task.status === 'Fallo') {
        html = `
            <textarea class="form-control form-control-sm mb-2 task-detail-input"
                placeholder="Observaciones o detalle del fallo" rows="2" ${readOnly}
                oninput="window.updateTaskDetail(${groupIndex}, ${taskIndex}, this.value)">${task.detail || ''}</textarea>
        `;
    } else if (task.type === 'text') {
        html = `
            <textarea class="form-control form-control-sm task-detail-input"
                placeholder="Ingrese sus observaciones" rows="2" ${readOnly}
                oninput="window.updateTaskDetail(${groupIndex}, ${taskIndex}, this.value)">${task.detail || ''}</textarea>
        `;
    }

    // Add Photo Evidence Section if required or if it's a card
    if (task.requirePhoto || isCard) {
        const photoId = `photo-${groupIndex}-${taskIndex}`;
        html += `
            <div class="mt-2">
                ${!isCard ? `<div class="d-flex align-items-center gap-2 text-muted x-small mb-1 fw-bold text-uppercase"><i class="fas fa-camera"></i> Evidencia</div>` : ''}
                <div class="photo-evidence-container" id="${photoId}-container">
                    ${(task.photos || []).map((url, pIdx) => `
                        <div class="photo-preview-item" style="width: 120px;">
                            <img src="${url}" onclick="window.open('${url}', '_blank')" style="border-radius: 8px;">
                            ${!execution.isReadOnly ? `<button class="photo-remove-btn" onclick="removeExecutionPhoto(${groupIndex}, ${taskIndex}, ${pIdx})"><i class="fas fa-times"></i></button>` : ''}
                            ${task.detail ? `<div class="x-small text-muted mt-1 px-1 text-truncate" title="${task.detail}">${task.detail}</div>` : ''}
                        </div>
                    `).join('')}
                    ${!execution.isReadOnly ? `
                        <div class="photo-upload-btn" onclick="document.getElementById('${photoId}-input').click()">
                            <i class="fas fa-plus"></i>
                            <input type="file" id="${photoId}-input" class="d-none" accept="image/*" onchange="handleExecutionPhotoUpload(this, ${groupIndex}, ${taskIndex})">
                        </div>
                    ` : ''}
                </div>
            </div>
        `;
    }

    return html;
}

window.handleExecutionPhotoUpload = async (input, groupIdx, taskIdx) => {
    const file = input.files[0];
    if (!file) return;

    showLoading(true);
    try {
        const executionId = state.currentExecution.execution.fb_id || 'temp_' + Date.now();
        const url = await uploadExecutionFile(executionId, file);
        if (url) {
            const task = state.currentExecution.execution.taskGroups[groupIdx].tasks[taskIdx];
            if (!task.photos) task.photos = [];
            task.photos.push(url);
            renderWorkPlanExecution(state.currentExecution.execution, state.currentExecution.plan);
            showToast('Foto cargada correctamente', 'success');
        }
    } catch (error) {
        console.error("Error uploading photo:", error);
        showToast('Error al cargar la foto', 'error');
    } finally {
        showLoading(false);
    }
};

window.removeExecutionPhoto = (groupIdx, taskIdx, photoIdx) => {
    const task = state.currentExecution.execution.taskGroups[groupIdx].tasks[taskIdx];
    task.photos.splice(photoIdx, 1);
    renderWorkPlanExecution(state.currentExecution.execution, state.currentExecution.plan);
};

function handleClosePlanExecution() {
    document.getElementById('plan-execution-view').classList.add('d-none');
    document.getElementById('app-wrapper').classList.remove('d-none');
    state.currentExecution = null;

    // Ensure any overlays or modals are hidden to prevent the app from "getting stuck"
    if (state.modals.confirm) state.modals.confirm.hide();
    showLoading(false);
}

async function handlePausePlanExecution() {
    const { execution, plan } = state.currentExecution || {};
    if (!execution || !plan) return;

    showLoading(true);
    try {
        if (execution.isLocal) {
            // First time saving: Create Work Order and Execution
            const newWorkOrderData = {
                id: generateNextId('MA'),
                machineId: plan.machineId,
                description: `Mantenimiento Preventivo Basado en Plan: ${plan.name}`,
                observaciones: `Ejecución de plan: ${plan.name}. Las tareas se gestionan en el checklist interactivo.`,
                status: 'En Proceso', // Creation status is 'En Proceso' even if plan is paused, to keep OT timer independent
                type: 'Preventivo',
                date: new Date().toISOString().split('T')[0],
                startTime: execution.executedAt,
                fechaInicioReal: execution.executedAt,
                workIntervals: [{ start: execution.executedAt, end: null }],
                requester: 'Sistema (Plan de Mantenimiento)',
                leadTechnician: plan.responsibleTechnician,
                technicians: [plan.responsibleTechnician],
                sourcePlanId: plan.fb_id,
                linkedPlanId: plan.fb_id,
                executionId: '',
                createdAt: new Date().toISOString()
            };
            const workOrderDocRef = await addDoc(state.collections.workOrders, newWorkOrderData);

            const executionData = {
                planId: plan.fb_id,
                workOrderFbId: workOrderDocRef.id,
                executedAt: execution.executedAt,
                executedBy: getCurrentUser().username,
                taskGroups: execution.taskGroups,
                status: 'Pausado',
                workIntervals: [{ start: execution.executedAt, end: new Date().toISOString() }]
            };
            const executionDocRef = await addDoc(state.collections.workPlanExecutions, executionData);

            await updateDoc(workOrderDocRef, { executionId: executionDocRef.id });

            // Update local state
            execution.fb_id = executionDocRef.id;
            execution.workOrderFbId = workOrderDocRef.id;
            delete execution.isLocal;
            execution.status = 'Pausado';
            execution.workIntervals = executionData.workIntervals;
        } else {
            // Already in Firestore, just update status and taskGroups
            const executionDocRef = doc(state.collections.workPlanExecutions, execution.fb_id);

            const intervals = execution.workIntervals || [];
            if (intervals.length > 0 && !intervals[intervals.length - 1].end) {
                intervals[intervals.length - 1].end = new Date().toISOString();
            }

            await updateDoc(executionDocRef, {
                status: 'Pausado',
                taskGroups: execution.taskGroups,
                workIntervals: intervals
            });

            execution.status = 'Pausado';
            execution.workIntervals = intervals;

            // Note: Per user request, pausing the checklist does NOT automatically pause the Work Order.
            // The technician must pause the OT manually if they want the global timer to stop.
        }

        showToast('Trabajo pausado y guardado.', 'success');
        renderWorkPlanExecution(execution, plan);

    } catch (error) {
        console.error("Error pausing plan execution:", error);
        showToast('Error al pausar el trabajo.', 'error');
    } finally {
        showLoading(false);
    }
}

async function handleResumePlanExecution() {
    const { execution, plan } = state.currentExecution || {};
    if (!execution || !plan || !execution.fb_id) return;

    showLoading(true);
    try {
        const executionDocRef = doc(state.collections.workPlanExecutions, execution.fb_id);

        const intervals = execution.workIntervals || [];
        intervals.push({ start: new Date().toISOString() });

        await updateDoc(executionDocRef, {
            status: 'En Proceso',
            workIntervals: intervals
        });

        // Note: Per user request, resuming the checklist does NOT automatically resume the Work Order.
        /*
        if (execution.workOrderFbId) {
            const woRef = doc(state.collections.workOrders, execution.workOrderFbId);
            await updateDoc(woRef, { status: 'En Proceso' });
        }
        */

        execution.status = 'En Proceso';
        execution.workIntervals = intervals;

        showToast('Trabajo reanudado.', 'success');
        renderWorkPlanExecution(execution, plan);

    } catch (error) {
        console.error("Error resuming plan execution:", error);
        showToast('Error al reanudar el trabajo.', 'error');
    } finally {
        showLoading(false);
    }
}

async function reschedulePlan(planId, completedAtISO) {
    const plan = state.workPlans.find(p => p.fb_id === planId);
    if (!plan) return;

    const planDocRef = doc(state.collections.workPlans, planId);

    // Calculate next due date from completion date
    const currentCompletionDate = new Date(completedAtISO);
    let nextDueDate = new Date(currentCompletionDate);
    const value = plan.frequencyValue || 1;
    switch(plan.frequencyUnit) {
        case 'h' :
            // Horas de operación: El rastreo detallado será en un panel futuro.
            // Por ahora, se reprograma proporcionalmente al valor para evitar quedar en el pasado.
            // Asumimos 1 hora de operación ~ 1 día calendario para este placeholder.
            nextDueDate.setDate(currentCompletionDate.getDate() + value);
            break;
        case 'd' : nextDueDate.setDate(currentCompletionDate.getDate() + value); break;
        case 'w' : nextDueDate.setDate(currentCompletionDate.getDate() + (value * 7)); break;
        case 'm' : nextDueDate.setMonth(currentCompletionDate.getMonth() + value); break;
        case 'y' : nextDueDate.setFullYear(currentCompletionDate.getFullYear() + value); break;
        default: nextDueDate.setMonth(currentCompletionDate.getMonth() + 1);
    }

    const nextYear = nextDueDate.getFullYear();
    const nextMonth = String(nextDueDate.getMonth() + 1).padStart(2, '0');
    const nextDay = String(nextDueDate.getDate()).padStart(2, '0');
    const nextDueDateStr = `${nextYear}-${nextMonth}-${nextDay}`;

    await updateDoc(planDocRef, {
        lastExecutedAt: completedAtISO,
        nextDueDate: nextDueDateStr
    });

    return nextDueDateStr;
}

async function autoCreateNextWorkOrder(planId, lastWorkOrderFbId, nextDueDateOverride = null, leadTechnicianOverride = null) {
    const plan = state.workPlans.find(p => p.fb_id === planId);
    if (!plan) return;

    const lastWO = lastWorkOrderFbId ? state.workOrders.find(o => o.fb_id === lastWorkOrderFbId) : null;
    const partsToCopy = lastWO ? (lastWO.partsUsed || []) : [];

    // Prefer lead technician from last execution, then override, then plan default
    const leadTechnician = leadTechnicianOverride || (lastWO ? lastWO.leadTechnician : plan.responsibleTechnician);

    const now = new Date().toISOString();
    const newWO = {
        id: generateNextId('MA'),
        machineId: plan.machineId,
        description: `Mantenimiento Preventivo Basado en Plan: ${plan.name}`,
        observaciones: `Orden generada automáticamente por frecuencia del plan.`,
        status: 'Pendiente',
        type: 'Preventivo',
        date: nextDueDateOverride || plan.nextDueDate,
        requester: 'Sistema (Plan de Mantenimiento)',
        leadTechnician: leadTechnician,
        technicians: [leadTechnician],
        sourcePlanId: plan.fb_id,
        linkedPlanId: plan.fb_id,
        partsUsed: partsToCopy.map(p => ({ ...p, delivered: false })), // Copy parts but mark as not delivered yet
        createdAt: now
    };

    try {
        const docRef = await addDoc(state.collections.workOrders, newWO);
        await recordAuditLog('workOrders', docRef.id, 'CREATE', null, newWO, 'Creación automática por plan');
        console.log(`[Auto-Plan] Nueva OT creada: ${newWO.id} para la fecha ${plan.nextDueDate}`);

        // Notificación Telegram para OTs automáticas
        const machine = state.machines.find(m => m.id === newWO.machineId);
        const tgMsg = `🛠 <b>Nueva OT Preventiva Automática</b>\n\n` +
                      `<b>ID:</b> ${newWO.id}\n` +
                      `<b>Equipo:</b> ${escapeHTML(machine ? machine.name : newWO.machineId)}\n` +
                      `<b>Tipo:</b> ${newWO.type}\n` +
                      `<b>Técnico:</b> ${escapeHTML(newWO.leadTechnician)}\n` +
                      `<b>Fecha ? Programada :</b> ${newWO.date}`;

        // Notificar a administradores, planificadores y grupo global
        notifyAdminsAndPlanners(tgMsg).catch(err => console.warn("[Telegram WO Error]", err));

        // Personal notifications for assigned technicians
        notifyTechnicians([newWO.leadTechnician, ...(newWO.technicians || [])], tgMsg);

    } catch (error) {
        console.error("Error auto-creating next Work Order:", error);
    }
}

async function completeLinkedPlanExecution(workOrderFbId, nowISO, status = 'Completado', validatedBy = null) {
    // Search locally to avoid Firestore composite index requirement
    const execution = state.workPlanExecutions.find(e =>
        e.workOrderFbId === workOrderFbId && e.status !== 'Completado'
    );

    if (execution) {
        const intervals = execution.workIntervals || [];
        if (intervals.length > 0 && !intervals[intervals.length - 1].end) {
            intervals[intervals.length - 1].end = nowISO;
        }

        const updates = {
            status: status,
            completedAt: nowISO,
            workIntervals: intervals
        };

        if (validatedBy) {
            updates.validatedBy = validatedBy;
        }

        const oldExecution = JSON.parse(JSON.stringify(execution));
        await updateDoc(doc(state.collections.workPlanExecutions, execution.fb_id), updates);
        await updateRTDBMirror('workPlanExecutions', { ...execution, ...updates }, execution.fb_id);
        await recordAuditLog('workPlanExecutions', execution.fb_id, 'UPDATE', oldExecution, { ...execution, ...updates });

        // If it wasn't finished by handleFinishPlanExecution (e.g., direct WO completion from Kanban),
        // we should reschedule and auto-create here if the execution was in a state that didn't do it yet.
        if (execution.status === 'En Proceso' || execution.status === 'Pausado') {
             const plan = state.workPlans.find(p => p.fb_id === execution.planId);
             if (plan) {
                const nextDate = await reschedulePlan(plan.fb_id, execution.executedAt || nowISO);
                await autoCreateNextWorkOrder(plan.fb_id, execution.workOrderFbId, nextDate);
             }
        }
    }
}

async function handleFinishPlanExecution() {
    const { execution, plan } = state.currentExecution || {};
    if (!execution || !plan) return;

    // Validation: Check for mandatory photos and missing data
    const missingPhotos = [];
    const pendingTasks = [];
    const missingFalloDetails = [];

    execution.taskGroups.forEach(group => {
        group.tasks.forEach(task => {
            if (task.status === 'Pendiente') {
                pendingTasks.push(task.description);
            }
            if (task.status === 'Fallo' && (!task.detail || task.detail.trim().length < 5)) {
                missingFalloDetails.push(task.description);
            }
            if (task.requirePhoto && (!task.photos || task.photos.length === 0)) {
                missingPhotos.push(task.description);
            }
        });
    });

    if (pendingTasks.length > 0) {
        showToast(`Hay ${pendingTasks.length} tareas pendientes de calificar.`, 'warning');
        return;
    }

    if (missingFalloDetails.length > 0) {
        showToast('Debe ingresar el detalle del fallo para: ' + missingFalloDetails[0], 'warning');
        return;
    }

    if (missingPhotos.length > 0) {
        showToast('Falta evidencia fotográfica obligatoria para: ' + missingPhotos[0], 'warning');
        return;
    }

    showConfirmation('Finalizar Plan', '¿Está seguroEsto completará la ejecución y enviará la orden a evaluación técnica.', async () => {
        const confirmed = await new Promise(resolve => {
            requestSignature(() => resolve(true), () => resolve(false));
        });
        if (!confirmed) return;

        showLoading(true);
        try {
            const now = new Date().toISOString();

            if (execution.isLocal) {
                // Create documents if it was never paused
                const workIntervals = [{ start: execution.executedAt, end: now }];
                const totalMs = new Date(now) - new Date(execution.executedAt);

                const newWorkOrderData = {
                    id: generateNextId('MA'),
                    machineId: plan.machineId,
                    description: `Mantenimiento Preventivo Basado en Plan: ${plan.name}`,
                    observaciones: `Ejecución de plan: ${plan.name}. Las tareas se gestionan en el checklist interactivo.`,
                    status: 'Pendiente de Aprobación',
                    type: 'Preventivo',
                    date: new Date().toISOString().split('T')[0],
                    startTime: execution.executedAt,
                    fechaInicioReal: execution.executedAt,
                    fechaFinalizacionReal: now,
                    workIntervals: workIntervals,
                    totalWorkDurationMs: totalMs,
                    requester: 'Sistema (Plan de Mantenimiento)',
                    leadTechnician: plan.responsibleTechnician,
                    technicians: [plan.responsibleTechnician],
                    sourcePlanId: plan.fb_id,
                    linkedPlanId: plan.fb_id,
                    executionId: '',
                    createdAt: now
                };
                const workOrderDocRef = await addDoc(state.collections.workOrders, newWorkOrderData);

                const executionData = {
                    planId: plan.fb_id,
                    workOrderFbId: workOrderDocRef.id,
                    executedAt: execution.executedAt,
                    completedAt: now,
                    executedBy: getCurrentUser().username,
                    taskGroups: execution.taskGroups,
                    status: 'Pendiente de Aprobación',
                    workIntervals: workIntervals
                };
                const executionDocRef = await addDoc(state.collections.workPlanExecutions, executionData);
                await updateRTDBMirror('workPlanExecutions', executionData, executionDocRef.id);
                await updateDoc(workOrderDocRef, { executionId: executionDocRef.id });
            } else {
                const executionDocRef = doc(state.collections.workPlanExecutions, execution.fb_id);

                const intervals = execution.workIntervals || [];
                if (intervals.length > 0 && !intervals[intervals.length - 1].end) {
                    intervals[intervals.length - 1].end = now;
                }

                const executionUpdates = {
                    status: 'Pendiente de Aprobación',
                    taskGroups: execution.taskGroups,
                    completedAt: now,
                    workIntervals: intervals
                };
                await updateDoc(executionDocRef, executionUpdates);
                await updateRTDBMirror('workPlanExecutions', { ...execution, ...executionUpdates }, execution.fb_id);

                if (execution.workOrderFbId) {
                    const woRef = doc(state.collections.workOrders, execution.workOrderFbId);
                    const existingWO = state.workOrders.find(o => o.fb_id === execution.workOrderFbId);

                    if (existingWO) {
                        let woIntervals = Array.isArray(existingWO.workIntervals) ? JSON.parse(JSON.stringify(existingWO.workIntervals)) : [];
                        // Close open interval if exists in the Work Order
                        const lastInterval = woIntervals[woIntervals.length - 1];
                        if (lastInterval && !lastInterval.end) {
                            lastInterval.end = now;
                        }

                        const totalMs = getTotalWorkDurationMs({ workIntervals: woIntervals });

                        const woUpdates = {
                            status: 'Pendiente de Aprobación',
                            fechaFinalizacionReal: now,
                            workIntervals: woIntervals,
                            totalWorkDurationMs: totalMs
                        };

                        if (!existingWO.fechaInicioReal) {
                            woUpdates.fechaInicioReal = execution.executedAt;
                        }

                        await updateDoc(woRef, woUpdates);
                    }
                }
            }

            // User requested that rescheduling be based on the start time (executedAt)
            // of the plan to accurately project the next maintenance date.
            const nextDate = await reschedulePlan(plan.fb_id, execution.executedAt || now);

            // Auto-create next Work Order with updated date and last technician
            await autoCreateNextWorkOrder(plan.fb_id, execution.workOrderFbId, nextDate, getCurrentUser().username);

            // Create Corrective OT if there are Alerts or Failures
            const issues = [];
            execution.taskGroups.forEach(group => {
                group.tasks.forEach(task => {
                    if (task.status === 'Fallo' || task.status === 'Alerta') {
                        issues.push(task);
                    }
                });
            });

            if (issues.length > 0) {
                const correctiveWOId = generateNextId('MA');
                const machine = state.machines.find(m => m.id === plan.machineId);
                const machineName = machine ? machine.name : plan.machineId;

                const correctiveWO = {
                    id: correctiveWOId,
                    machineId: plan.machineId,
                    description: `Corrección de fallas/alertas - Plan: ${plan.name}`,
                    observaciones: `Orden generada automáticamente por fallas detectadas en la ejecución del plan ${plan.name}.\n\nDetalle de hallazgos:\n` +
                                   issues.map(t => `- [${t.status.toUpperCase()}] ${t.description}${t.detail ? ': ' + t.detail : ''}`).join('\n'),
                    status: 'Pendiente',
                    type: 'Correctivo',
                    failureType: 'Mecánica', // Default failure type
                    priority: 'Alta',
                    date: new Date().toISOString().split('T')[0],
                    requester: 'Sistema (Plan de Mantenimiento)',
                    leadTechnician: getCurrentUser().username,
                    technicians: [getCurrentUser().username],
                    createdAt: now
                };

                try {
                    const correctiveRef = await addDoc(state.collections.workOrders, correctiveWO);
                    await updateRTDBMirror('workOrders', correctiveWO, correctiveRef.id);
                    await recordAuditLog('workOrders', correctiveWOId, 'CREATE', null, correctiveWO, 'Generación automática por fallas en plan');

                    // Telegram Notification
                    const telegramMsg = `🚨 <b>Nueva OT Correctiva Automática</b>\n\n` +
                                       `<b>ID:</b> ${correctiveWOId}\n` +
                                       `<b>Equipo:</b> ${escapeHTML(machineName)} (${escapeHTML(plan.machineId)})\n` +
                                       `<b>Técnico:</b> ${escapeHTML(getCurrentUser().username)}\n` +
                                       `<b>Origen:</b> Plan ${escapeHTML(plan.name)}\n\n` +
                                       `<b>Hallazgos:</b> \n` +
                                       issues.map(t => `• ${escapeHTML(t.description)} (${t.status})`).join('\n');

                    // Notificar a administradores, planificadores y grupo global
                    notifyAdminsAndPlanners(telegramMsg).catch(err => console.error("Error Telegram Global:", err));

                    // Personal notification
                    notifyTechnicians(getCurrentUser().username, telegramMsg);
                } catch (error) {
                    console.error("Error creating corrective WO from plan failures:", error);
                }
            }

            // Notificación de Validación para Plan Finalizado
            const machine = state.machines.find(m => m.id === plan.machineId);
            const valMsg = `✅ <b>Plan Finalizado - Lista para Validación</b>\n\n` +
                          `La ejecución del plan "${plan.name}" ha sido finalizada y requiere su revisión.\n\n` +
                          `<b>Equipo:</b> ${escapeHTML(machine ? machine.name : plan.machineId)}\n` +
                          `<b>Técnico:</b> ${escapeHTML(getCurrentUser().username)}\n` +
                          `<b>Estado:</b> Pendiente de Aprobación`;

            notifyAdminsAndPlanners(valMsg).catch(err => console.warn("[Telegram Validation Notification Error]", err));

            showToast('Plan finalizado, enviado a aprobación y reprogramado con éxito.', 'success');
            handleClosePlanExecution();

        } catch (error) {
            console.error("Error finalizing plan execution:", error);
            showToast('Error al finalizar la ejecución del plan.', 'error');
        } finally {
            showLoading(false);
        }
    });
}


// --- Work Order & Kanban Functions ---
function clearActiveTimers() {
    for (const orderId in state.activeTimers) {
        clearInterval(state.activeTimers[orderId]);
    }
    state.activeTimers = {};
}

function renderActiveWorkView() {
    clearActiveTimers();
    const requestsContainer = document.getElementById('pending-requests');
    const plannedContainer = document.getElementById('planned-tasks');
    const inProgressContainer = document.getElementById('in-progress-tasks');
    const pausedContainer = document.getElementById('paused-tasks');
    const evaluationContainer = document.getElementById('evaluation-tasks');
    const completedContainer = document.getElementById('completed-tasks');
    const cancelledContainer = document.getElementById('cancelled-tasks');

    requestsContainer.innerHTML = '';
    plannedContainer.innerHTML = '';
    inProgressContainer.innerHTML = '';
    pausedContainer.innerHTML = '';
    if (evaluationContainer) evaluationContainer.innerHTML = '';
    if (completedContainer) completedContainer.innerHTML = '';
    if (cancelledContainer) cancelledContainer.innerHTML = '';

    let workOrdersToDisplay = state.workOrders;
    let solicitudesToDisplay = state.solicitudes;
    const userRole = getCurrentUser().role;
    const managedMachineIds = getCurrentUser().managedMachineIds;
    const equipoAsignado = getCurrentUser().equipoAsignado;

    const hasRestrictiveRole = (userRole === 'Jefe de Area') || (['Técnico', 'Operario', 'Supervisor de Area'].includes(userRole));
    if (hasRestrictiveRole) {
        const ids = userRole === 'Jefe de Area' ? (managedMachineIds || []) : (equipoAsignado || []);
        const relevantMachineIds = new Set(Array.isArray(ids) ? ids : []);
        workOrdersToDisplay = state.workOrders.filter(wo => relevantMachineIds.has(wo.machineId));
        solicitudesToDisplay = state.solicitudes.filter(s => relevantMachineIds.has(s.machineId));
    }

    const monthFilter = document.getElementById('kanban-month-filter');
    const selectedMonth = monthFilter ? monthFilter.value : null;

    if (selectedMonth) {
        const [year, month] = selectedMonth.split('-').map(Number);
        const monthName = new Date(year, month - 1, 1)
            .toLocaleDateString('es-ES', { month: 'long', year: 'numeric' })
            .replace(/^\w/, c => c.toUpperCase());
        document.getElementById('page-title').textContent = `Trabajo Activo - ${monthName}`;

        const currentMonthObj = new Date(year, month - 1, 1);

        solicitudesToDisplay = solicitudesToDisplay.filter(s => {
            if (!s.createdAt) return false;

            let localMonthStr = "";
            let d;
            if (/^\d{4}-\d{2}-\d{2}$/.test(s.createdAt)) {
                localMonthStr = s.createdAt.substring(0, 7);
                d = new Date(s.createdAt + 'T12:00:00');
            } else {
                d = new Date(s.createdAt);
                if (isNaN(d.getTime())) return false;
                localMonthStr = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`;
            }

            // Si es Pendiente y de un mes anterior, lo consideramos "No Ejecutada"
            if (s.status === 'Pendiente') {
                const sDate = new Date(d.getFullYear(), d.getMonth(), 1);
                if (sDate < currentMonthObj) return true;
            }

            return localMonthStr === selectedMonth;
        });

        workOrdersToDisplay = workOrdersToDisplay.filter(wo => {
            const dateStr = wo.fechaFinalizacionReal || wo.date || wo.createdAt;
            if (!dateStr) return false;

            let localMonthStr = "";
            let d;
            if (/^\d{4}-\d{2}-\d{2}$/.test(dateStr)) {
                localMonthStr = dateStr.substring(0, 7);
                d = new Date(dateStr + 'T12:00:00');
            } else {
                d = new Date(dateStr);
                if (isNaN(d.getTime())) return false;
                localMonthStr = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`;
            }

            // Si la orden es del mes seleccionado, se incluye siempre.
            if (localMonthStr === selectedMonth) return true;

            // Para órdenes de meses anteriores:
            // 1. Si está marcada como expirada, se incluye (irá a la columna de Canceladas/No Ejecutadas).
            if (wo.isExpired) return true;

            // 2. Si está ACTIVA (Pendiente, En Proceso, Pausado, etc. y NO es de este mes),
            // se incluye para que aparezca como arrastre (carry-over) en su columna correspondiente.
            // Exceptuamos las que ya están terminadas, canceladas o rechazadas de meses previos.
            if (wo.status !== 'Completado' && wo.status !== 'Cancelado' && wo.status !== 'Rechazado') {
                const woMonthDate = new Date(d.getFullYear(), d.getMonth(), 1);
                if (woMonthDate < currentMonthObj) return true;
            }

            return false;
        });
    }

    const pendingRequests = solicitudesToDisplay.filter(s => {
        if (s.status !== 'Pendiente') return false;
        if (selectedMonth) {
            const [year, month] = selectedMonth.split('-').map(Number);
            const currentMonthObj = new Date(year, month - 1, 1);
            const d = new Date(s.createdAt);
            const sDate = new Date(d.getFullYear(), d.getMonth(), 1);
            if (sDate < currentMonthObj) return false; // Overdue go to cancelled
        }
        return true;
    });

    const cancelledSolicitudes = solicitudesToDisplay.filter(s => {
        if (s.status === 'Cancelado' || s.status === 'Rechazado') return true;
        if (s.status === 'Pendiente' && selectedMonth) {
            const [year, month] = selectedMonth.split('-').map(Number);
            const currentMonthObj = new Date(year, month - 1, 1);
            const d = new Date(s.createdAt);
            const sDate = new Date(d.getFullYear(), d.getMonth(), 1);
            return sDate < currentMonthObj;
        }
        return false;
    });

    if (pendingRequests.length === 0) {
         requestsContainer.innerHTML = '<p class="text-center text-muted">No hay solicitudes pendientes.</p>';
    } else {
        pendingRequests.forEach(req => requestsContainer.appendChild(createSolicitudCard(req)));
    }

    workOrdersToDisplay.forEach(wo => {
        const fbId = wo?.fb_id || wo?.id;
        if (!fbId) return;
        const cleanId = String(fbId).replace(/[.#$\[\]]/g, '_');
        const woMirror = state.liveWorkOrders?.[cleanId];
        const currentStatus = woMirror?.status || wo.status;

        const card = createWorkOrderCard(wo, currentStatus);

        if (wo.isExpired || currentStatus === 'Cancelado' || currentStatus === 'Rechazado') {
            if (cancelledContainer) cancelledContainer.appendChild(card);
        } else if (currentStatus === 'Completado') {
            // Unificamos lógica: si es Completado pero falta alguna evaluación, va a la columna de Evaluación
            const evs = state.technicianEvaluations.filter(ev => ev.workOrderFbId === wo.fb_id);
            const hasOp = evs.some(e => e.evaluatorRole === 'Operario');
            const hasJf = evs.some(e => e.evaluatorRole === 'Jefe de Area');

            if (hasOp && hasJf) {
                if (completedContainer) completedContainer.appendChild(card);
            } else {
                if (evaluationContainer) evaluationContainer.appendChild(card);
            }
        } else if (currentStatus === 'Pendiente de Evaluación' || currentStatus === 'Pendiente de Aprobación') {
            if (evaluationContainer) evaluationContainer.appendChild(card);
        } else if (currentStatus === 'En Proceso') {
            inProgressContainer.appendChild(card);
            if (wo.workIntervals) {
                const lastInterval = wo.workIntervals[wo.workIntervals.length - 1];
                if (lastInterval && lastInterval.start && !lastInterval.end) {
                   updateTimerDisplay(wo.id, wo);
                }
            }
        } else if (currentStatus === 'Pausado') {
            pausedContainer.appendChild(card);
        } else if (currentStatus === 'Pendiente' || currentStatus === 'Planificada') {
            plannedContainer.appendChild(card);
        }
    });

    if (cancelledSolicitudes.length > 0) {
        cancelledSolicitudes.forEach(s => cancelledContainer.appendChild(createSolicitudCard(s)));
    }

    if (plannedContainer.innerHTML === '') plannedContainer.innerHTML = '<p class="text-center text-muted">No hay tareas planificadas.</p>';
    if (inProgressContainer.innerHTML === '') inProgressContainer.innerHTML = '<p class="text-center text-muted">No hay tareas en ejecución.</p>';
    if (pausedContainer.innerHTML === '') pausedContainer.innerHTML = '<p class="text-center text-muted">No hay tareas pausadas.</p>';
    if (evaluationContainer && evaluationContainer.innerHTML === '') {
        evaluationContainer.innerHTML = '<p class="text-center text-muted">No hay tareas para evaluar.</p>';
    }
    if (completedContainer && completedContainer.innerHTML === '') {
        completedContainer.innerHTML = '<p class="text-center text-muted">No hay tareas evaluadas.</p>';
    }
    if (cancelledContainer && cancelledContainer.innerHTML === '') {
        cancelledContainer.innerHTML = '<p class="text-center text-muted">No hay tareas canceladas o no ejecutadas.</p>';
    }
}

function createSolicitudCard(solicitud) {
    const card = document.createElement('div');
    card.className = 'kanban-card';
    const machine = state.machines.find(m => m.id === solicitud.machineId) || { name: 'Desconocido' };
    const userRole = getCurrentUser().role;
    const isReadOnly = userRole !== 'Admin' && userRole !== 'Planificador' && userRole !== 'Técnico';

    let linkedWorkOrder = null;
    if (solicitud.workOrderFbId) {
        linkedWorkOrder = state.workOrders.find(wo => wo.fb_id === solicitud.workOrderFbId);
    } else if (solicitud.workOrderId) {
        linkedWorkOrder = state.workOrders.find(wo => wo.id === solicitud.workOrderId);
    }

    card.innerHTML = `
        <div class="d-flex justify-content-between align-items-start">
            <div>
                <h6 class="card-title mb-0">${machine.name}</h6>
                <div class="d-flex flex-column">
                    <small class="text-muted">Solicitante: ${solicitud.requester}</small>
                    ${linkedWorkOrder ? `<small class="text-primary fw-bold">OT: ${linkedWorkOrder.id}</small>` : ''}
                </div>
            </div>
             <span class="badge bg-secondary">${new Date(solicitud.createdAt).toLocaleDateString('es-ES')}</span>
        </div>
        <p class="card-text my-2">${solicitud.description}</p>
        <div class="card-footer bg-transparent p-0 pt-2 border-top">
             ${isReadOnly ? `<p class="text-muted text-center mb-0 small">Solo visualización</p>` : `<button class="btn btn-sm btn-success convert-solicitud-btn w-100"><i class="fas fa-check me-2"></i>Crear OT</button>`
             }
        </div>
    `;
    if (!isReadOnly) {
        card.querySelector('.convert-solicitud-btn').addEventListener('click', () => {
            let targetType = 'Correctivo'; if (solicitud.type === 'mecanizado') targetType = 'Mecanizado'; else if (solicitud.type === 'calibracion') targetType = 'Calibración';
            showWorkOrderModal(null, targetType, solicitud);
        });
    }
    return card;
}

function createWorkOrderCard(order, statusOverride = null) {
    const card = document.createElement('div');
    card.className = 'kanban-card';
    const currentStatus = statusOverride || order.status;

    if (currentStatus === 'Pendiente de Aprobación') {
        card.classList.add('status-pending-approval');
    }
    if (order.type === 'Mecanizado') {
        card.classList.add('type-mecanizado');
    }
    card.dataset.id = order.id;
    const machine = state.machines.find(m => m.id === order.machineId) || { name: 'Desconocido' };
    const isLeadTechnician = getCurrentUser().username === order.leadTechnician;
    const canAction = getCurrentUser().role === 'Admin' || getCurrentUser().role === 'Planificador' || isLeadTechnician;
    const isPlanOrder = ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(order.type) || order.linkedPlanId;
    
    let footerContent = '';
    
    const originalSolicitudId = order.solicitudId || order.sourceSolicitudId;
    const originalSolicitud = originalSolicitudId ? state.solicitudes.find(s => s.id === originalSolicitudId || s.fb_id === originalSolicitudId) : null;

    // Nueva lógica de doble evaluación
    const evs = state.technicianEvaluations.filter(ev => ev.workOrderFbId === order.fb_id);
    const hasOp = evs.some(e => e.evaluatorRole === 'Operario');
    const hasJf = evs.some(e => e.evaluatorRole === 'Jefe de Area');

    let canEvaluate = false;
    const userRole = getCurrentUser().role;
    const isRequester = originalSolicitud && originalSolicitud.requester === getCurrentUser().username;
    const isAssignedOperario = (['Operario', 'Supervisor de Area'].includes(userRole)) && Array.isArray(getCurrentUser().equipoAsignado) && getCurrentUser().equipoAsignado.includes(order.machineId);

    const canEvalAsJefe = (userRole === 'Admin' || (userRole === 'Planificador' && ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(order.type)) || (userRole === 'Jefe de Area' && Array.isArray(getCurrentUser().managedMachineIds) && getCurrentUser().managedMachineIds.includes(order.machineId)));
    const canEvalAsOp = (userRole === 'Admin' || ((['Operario', 'Supervisor de Area'].includes(userRole)) && Array.isArray(getCurrentUser().equipoAsignado) && getCurrentUser().equipoAsignado.includes(order.machineId)) || isRequester);

    if ((canEvalAsJefe && !hasJf) || (canEvalAsOp && !hasOp)) {
        canEvaluate = true;
    }

    if (order.isExpired) {
        footerContent = `<p class="text-muted text-center mb-0 small"><i class="fas fa-lock me-1"></i>Expirada - Solo Lectura</p>`;
    } else if (currentStatus === 'Pendiente de Evaluación' || (currentStatus === 'Completado' && (!hasOp || !hasJf))) {
        if (canEvaluate) {
            const label = (hasOp || hasJf) ? 'Completar Evaluación' : 'Evaluar';
            const pendingBadge = (currentStatus === 'Completado' && (!hasOp || !hasJf)) ? '<div class="text-center mb-1"><span class="badge bg-danger text-white pulse-animation" style="font-size: 0.7rem;"><i class="fas fa-exclamation-triangle me-1"></i>Evaluación Pendiente</span></div>' : '';
            const partialBadge = (hasOp || hasJf) ? '<div class="text-center mb-1"><span class="badge bg-info text-dark" style="font-size: 0.7rem;">Evaluación Parcial</span></div>' : '';
            footerContent = `${pendingBadge || partialBadge}<button class="btn btn-sm btn-info evaluate-task-btn w-100"><i class="fas fa-star-half-alt me-2"></i>${label}</button>`;
        } else {
            const missing = !hasOp ? 'Operario' : 'Jefe de Área';
            const pendingText = (currentStatus === 'Completado') ? `<div class="text-center mb-1"><span class="badge bg-danger text-white" style="font-size: 0.7rem;"><i class="fas fa-exclamation-triangle me-1"></i>Falta Calificar</span></div>` : '';
            footerContent = `${pendingText}<p class="text-muted text-center mb-0 small"><i class="fas fa-clock me-1"></i>Esperando a ${missing}</p>`;
        }
    } else if (currentStatus === 'Completado') {
        footerContent = `<button class="btn btn-sm btn-outline-primary view-evaluation-btn w-100"><i class="fas fa-eye me-2"></i>Ver Evaluación</button>`;
    } else if (!canAction) {
        footerContent = `<p class="text-muted text-center mb-0 small">Solo visualización</p>`;
    } else {
        switch(currentStatus) {
            case 'Pendiente' :
                footerContent = `<button class="btn btn-sm btn-primary start-task-btn w-100"><i class="fas fa-play me-2"></i>Iniciar</button>`;
                break;
            case 'En Proceso' :
                const completeLabel = isPlanOrder ? 'Finalizar' : 'Completar';
                footerContent = `
                    <div class="d-flex justify-content-between align-items-center">
                        <span class="timer" id="timer-${order.id}">00:00:00</span>
                        <div class="btn-group">
                            <button class="btn btn-sm btn-warning pause-task-btn" title="Pausar"><i class="fas fa-pause"></i></button>
                            <button class="btn btn-sm btn-success complete-task-btn" title="${completeLabel}"><i class="fas fa-check"></i></button>
                        </div>
                    </div>`;
                break;
            case 'Pausado' :
                footerContent = `<button class="btn btn-sm btn-info resume-task-btn w-100"><i class="fas fa-play me-2"></i>Reanudar</button>`;
                break;
            case 'Cancelado' :
            case 'Rechazado' :
                footerContent = `<p class="text-danger text-center mb-0 small"><i class="fas fa-ban me-1"></i>${order.status}</p>`;
                break;
            default:
                footerContent = `<p class="text-muted text-center mb-0 small">${order.status}</p>`;
        }
    }


    const totalScore = evs.reduce((acc, ev) => acc + (ev.score || 0), 0);
    const totalMaxScore = evs.reduce((acc, ev) => acc + (ev.maxScore || 0), 0);
    const avgRating = totalMaxScore > 0 ? (totalScore / totalMaxScore) * 5 : 0;
    const starDisplay = currentStatus === 'Completado' && avgRating > 0 ? `<span class="text-warning ms-1 fw-bold" style="font-size: 0.8rem;" title="Calificación: ${avgRating.toFixed(1)}">⭐${avgRating.toFixed(1)}</span>` : '';

    const cardTitle = order.id ? order.id : `(${machine.id || 'N/A'})`;
    const expiredBadge = order.isExpired ? '<br><span class="badge bg-dark text-white mt-1"><i class="fas fa-hourglass-end me-1"></i>Expirada</span>' : '';
    let typeBadgeColor = 'danger';
    if (['Preventivo', 'Predictivo', 'Calibración'].includes(order.type)) {
        typeBadgeColor = 'primary';
    } else if (order.type === 'Mecanizado') {
        typeBadgeColor = 'info';
    }

    card.innerHTML = `
        <div class="d-flex justify-content-between align-items-start">
            <div>
                <h6 class="card-title mb-0" style="cursor: pointer;">${cardTitle}${starDisplay}</h6>
                <small class="text-muted">${machine.name}</small>
                ${originalSolicitud ? `<br><small class="text-info fw-bold">Solicitud: ${originalSolicitud.id}</small>` : ''}
            </div>
            <div class="text-end">
                <span class="badge bg-${typeBadgeColor}">${order.type}</span>
                ${expiredBadge}
            </div>
        </div>
        <p class="card-text my-2">${order.description}</p>
        <div class="card-footer bg-transparent p-0 pt-2 border-top">
            ${footerContent}
        </div>
    `;
    
    card.querySelector('.card-title').addEventListener('click', () => showWorkOrderModal(order.fb_id));

    // Attach listeners for tech actions
    card.querySelector('.view-evaluation-btn').addEventListener('click', (e) => {
        e.stopPropagation();
        showEvaluationDetailsModal(order.fb_id);
    });

    if (canAction) {
        card.querySelector('.start-task-btn').addEventListener('click', () => handleKanbanWorkOrderAction(order.fb_id, 'En Proceso'));
        card.querySelector('.pause-task-btn').addEventListener('click', () => handleKanbanWorkOrderAction(order.fb_id, 'Pausado'));
        card.querySelector('.resume-task-btn').addEventListener('click', () => handleKanbanWorkOrderAction(order.fb_id, 'En Proceso'));
        card.querySelector('.complete-task-btn').addEventListener('click', () => {
            const isPlanOrder = ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(order.type) || order.linkedPlanId;
            const confirmTitle = isPlanOrder ? 'Finalizar Trabajo' : 'Completar Orden de Trabajo';
            const confirmMsg = isPlanOrder
                ? `¿Está seguro que desea finalizar el trabajo en la orden ${order.id || `(${order.machineId})`}? Se notificará al planificador.`
                : `¿Está seguro que desea completar la orden ${order.id || `(${order.machineId})`}`;

             showConfirmation(
                confirmTitle,
                confirmMsg,
                () => handleKanbanWorkOrderAction(order.fb_id, 'Completado')
            );
        });
    }

    // Attach listener for evaluation action
    if (order.status === 'Pendiente de Evaluación' || order.status === 'Completado') {
        const evaluateBtn = card.querySelector('.evaluate-task-btn');
        if (evaluateBtn) {
            evaluateBtn.addEventListener('click', () => showEvaluationModal(order.id));
        }
    }

    return card;
}

function getTotalWorkDurationMs(order) {
    let totalMs = 0;
    if (order.workIntervals && Array.isArray(order.workIntervals)) {
         order.workIntervals.forEach(interval => {
            if (interval.start && interval.end) {
                const start = new Date(interval.start);
                const end = new Date(interval.end);
                if (!isNaN(start.getTime()) && !isNaN(end.getTime())) {
                    totalMs += end - start;
                }
            }
        });
    }
    return totalMs;
}

function getActiveWorkDurationMs(order) {
    let totalMs = getTotalWorkDurationMs(order);
    if (order.status === 'En Proceso' && order.workIntervals) {
         const lastInterval = order.workIntervals[order.workIntervals.length - 1];
         if (lastInterval && lastInterval.start && !lastInterval.end) {
             totalMs += new Date() - new Date(lastInterval.start);
         }
    }
    return totalMs;
}


function updateTimerDisplay(orderId, order) {
    const timerEl = document.getElementById(`timer-${orderId}`);
    if (!timerEl) return;

    if(state.activeTimers[orderId]) clearInterval(state.activeTimers[orderId]);

    const intervalId = setInterval(() => {
        const totalMs = getActiveWorkDurationMs(order);
        if (totalMs < 0) {
             timerEl.textContent = '00:00:00';
             return;
        }
        const hours = Math.floor(totalMs / (1000 * 60 * 60));
        const minutes = Math.floor((totalMs % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((totalMs % (1000 * 60)) / 1000);
        timerEl.textContent = `${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`;
    }, 1000);

    state.activeTimers[orderId] = intervalId;
}

function addPartMovementToBatch(batch, partId, quantity, type, details = {}) {
    const movementRef = doc(state.collections.partMovements);
    batch.set(movementRef, buildPartMovementData(partId, quantity, type, details));
}

function isWorkOrderFinishStatus(status) {
    return ['Pendiente de Evaluación', 'Completado', 'Pendiente de Aprobación'].includes(status);
}

function getOpenPartRequestsForOrder(order) {
    if (!order) return [];
    const terminalStatuses = ['Recibido', 'Rechazado', 'Cancelado'];
    return (state.partRequests || []).filter(request => {
        const belongsToOrder = (order.fb_id && request.workOrderFbId === order.fb_id) ||
            (order.id && request.workOrderId === order.id);
        if (!belongsToOrder) return false;

        const requestStatus = request.status || 'Pendiente';
        const requestClosed = terminalStatuses.includes(requestStatus);
        const hasOpenItems = (request.items || []).some(item =>
            !terminalStatuses.includes(item.status || requestStatus)
        );

        return !requestClosed || hasOpenItems;
    });
}

function validateWorkOrderPartsStock(order, oldOrder = { partsUsed: [] }) {
    const oldPartsMap = new Map((oldOrder.partsUsed || []).map(p => [p.partId, p]));
    const issues = [];

    (order.partsUsed || []).forEach(item => {
        const partId = item.partId;
        if (!partId) return;

        const oldEntry = oldPartsMap.get(partId);
        const oldQtyDelivered = (oldEntry && oldEntry.delivered !== false) ? (Number(oldEntry.quantity) || 0) : 0;
        const requestedQty = Number(item.quantity) || 0;
        const qtyToConsume = requestedQty - oldQtyDelivered;
        if (qtyToConsume <= 0) return;

        const part = state.parts.find(p => p.id === partId || p.fb_id === partId);
        if (!part) {
            issues.push(`${item.description || partId}: no existe en inventario`);
            return;
        }

        const available = Number(part.stockActual) || 0;
        if (available < qtyToConsume) {
            issues.push(`${part.description || item.description || partId}: disponible ${available}, requerido ${qtyToConsume}`);
        }
    });

    return issues;
}

async function enforceWorkOrderInventoryBeforeFinish(order, oldOrder = { partsUsed: [] }) {
    const openRequests = getOpenPartRequestsForOrder(order);
    if (openRequests.length > 0) {
        const refs = openRequests.map(r => r.id || r.workOrderId || r.fb_id).join(', ');
        showToast(`Debe confirmar la recepción de las solicitudes de repuestos/insumos antes de finalizar: ${refs}`, 'error');
        return false;
    }

    const stockIssues = validateWorkOrderPartsStock(order, oldOrder);
    if (stockIssues.length > 0) {
        const note = prompt(`Falta stock para uno o mas repuestos/insumos:\n- ${stockIssues.join('\n- ')}\n\nIngrese una nota obligatoria para finalizar la OT con faltante de inventario:`);
        if (!note || !note.trim()) {
            showToast('Debe ingresar una nota para finalizar con faltante de stock.', 'warning');
            return false;
        }

        const timestamp = new Date().toLocaleString();
        const justification = `[Justificacion cierre con faltante de stock - ${timestamp}] ${note.trim()} | Faltantes: ${stockIssues.join(' | ')} (Usuario: ${getCurrentUser().username || 'Sistema'})`;
        order.allowStockShortageOnFinish = true;
        order.stockShortageJustification = note.trim();
        order.observaciones = `${order.observaciones || ''}${order.observaciones ? '\n' : ''}${justification}`;
        const obsTextarea = document.getElementById('wo-observaciones');
        if (obsTextarea) obsTextarea.value = order.observaciones;
    }

    const unreceivedParts = (order.partsUsed || []).filter(item => item.delivered === false);
    if (unreceivedParts.length === 0) return true;

    const materialList = unreceivedParts
        .map(item => item.description || item.partDescription || item.partId)
        .join(', ');
    const note = prompt(`Hay repuestos/insumos agregados que no fueron marcados como recibidos: ${materialList}\n\nIngrese una justificación obligatoria para finalizar y descontar estos materiales del inventario:`);

    if (!note || !note.trim()) {
        showToast('Debe ingresar una justificación para finalizar con materiales no recibidos.', 'warning');
        return false;
    }

    const timestamp = new Date().toLocaleString();
    const justification = `[Justificación cierre con materiales no recibidos - ${timestamp}] ${note.trim()} (Usuario: ${getCurrentUser().username || 'Sistema'})`;
    order.observaciones = `${order.observaciones || ''}${order.observaciones ? '\n' : ''}${justification}`;
    const obsTextarea = document.getElementById('wo-observaciones');
    if (obsTextarea) obsTextarea.value = order.observaciones;
    return true;
}

async function updateStockForOrder(oldOrder, newOrder) {
    const batch = writeBatch(db);
    const oldPartsMap = new Map((oldOrder.partsUsed || []).map(p => [p.partId, p]));
    const newPartsMap = new Map((newOrder.partsUsed || []).map(p => [p.partId, p]));

    const updatedPartsUsed = [];
    const allPartIds = new Set([...oldPartsMap.keys(), ...newPartsMap.keys()]);

    for (const partId of allPartIds) {
        const oldEntry = oldPartsMap.get(partId);
        const newEntry = newPartsMap.get(partId);

        const oldQtyDelivered = (oldEntry && oldEntry.delivered !== false) ? oldEntry.quantity : 0;
        const newQtyRequested = newEntry ? newEntry.quantity : 0;

        let delta = newQtyRequested - oldQtyDelivered;

        if (delta === 0) {
            if (newEntry) updatedPartsUsed.push(newEntry);
            continue;
        }

        const part = state.parts.find(p => p.id === partId);
        if (part) {
            const currentStock = part.stockActual || 0;
            const newStock = currentStock - delta;
            const partRef = doc(state.collections.parts, partId);

            if (newStock < 0 && delta > 0) {
                if (newOrder.allowStockShortageOnFinish) {
                    const consumedQty = Math.max(0, currentStock);
                    const shortageQty = delta - consumedQty;
                    if (consumedQty > 0) {
                        batch.update(partRef, { stockActual: 0 });
                        addPartMovementToBatch(batch, partId, -consumedQty, 'Salida OT Parcial por Falta de Stock', {
                            orderId: newOrder.fb_id || 'nuevo',
                            orderHumanId: newOrder.id,
                            stockBefore: currentStock,
                            stockAfter: 0,
                            referenceType: 'ORDEN_TRABAJO',
                            reason: 'FALTANTE_STOCK',
                            details: `Consumo parcial registrado desde OT ${newOrder.id}. Faltante no descontado: ${shortageQty}. Nota: ${newOrder.stockShortageJustification || ''}`
                        });
                    }
                    if (newEntry) {
                        updatedPartsUsed.push({
                            ...newEntry,
                            delivered: false,
                            consumedQuantity: consumedQty,
                            stockShortageQty: shortageQty,
                            stockShortageJustification: newOrder.stockShortageJustification || ''
                        });
                    }
                    continue;
                }
                throw new Error(`Stock insuficiente para ${part.description || partId}. Disponible: ${currentStock}, requerido: ${delta}.`);
                if (newEntry) {
                    if (oldQtyDelivered > 0) {
                        const revertedStock = currentStock + oldQtyDelivered;
                        batch.update(partRef, { stockActual: revertedStock });
                        addPartMovementToBatch(batch, partId, oldQtyDelivered, 'Reversión por Falta de Stock', {
                            orderId: newOrder.fb_id || 'nuevo',
                            orderHumanId: newOrder.id,
                            stockBefore: currentStock,
                            stockAfter: revertedStock,
                            referenceType: 'ORDEN_TRABAJO',
                            details: `Reversion automatica por stock insuficiente en OT ${newOrder.id}`
                        });
                    }
                    updatedPartsUsed.push({ ...newEntry, delivered: false });
                }
            } else {
                batch.update(partRef, { stockActual: newStock });
                if (delta !== 0) {
                    addPartMovementToBatch(batch, partId, -delta, delta > 0 ? 'Salida OT' : 'Devolución OT', {
                        orderId: newOrder.fb_id || 'nuevo',
                        orderHumanId: newOrder.id,
                        stockBefore: currentStock,
                        stockAfter: newStock,
                        referenceType: 'ORDEN_TRABAJO',
                        details: `${delta > 0 ? 'Consumo' : 'Devolucion'} registrado desde OT ${newOrder.id}`
                    });
                }
                if (newEntry) {
                    updatedPartsUsed.push({ ...newEntry, delivered: true });
                }
            }
        } else {
            if (newEntry) updatedPartsUsed.push({ ...newEntry, delivered: false });
        }
    }
    await batch.commit();
    newOrder.partsUsed = updatedPartsUsed;

    // Sincronizar stockActual de repuestos utilizados con Odoo
    if (state.odoo) {
        for (const partId of allPartIds) {
            const part = state.parts.find(p => p.id === partId);
            if (part) {
                const oldEntry = oldPartsMap.get(partId);
                const newEntry = newPartsMap.get(partId);
                const oldQtyDelivered = (oldEntry && oldEntry.delivered !== false) ? oldEntry.quantity : 0;
                const newQtyRequested = (newEntry && newEntry.delivered !== false) ? newEntry.quantity : 0;
                let delta = newQtyRequested - oldQtyDelivered;

                if (delta !== 0) {
                    const newStock = (part.stockActual || 0) - delta;
                    syncPartStockToOdoo(partId, { ...part, stockActual: newStock }).catch(err =>
                        console.warn(`[Odoo Stock Sync] Error en ${partId}:`, err)
                    );
                }
            }
        }
    }
}

async function handleKanbanWorkOrderAction(workOrderFbId, newStatus) {
    showLoading(true);
    try {
        const orderRef = doc(state.collections.workOrders, workOrderFbId);
        const orderSnapshot = await getDoc(orderRef);
        if (!orderSnapshot.exists()) {
            throw new Error("Work order not found");
        }

        const orderData = { fb_id: orderSnapshot.id, ...orderSnapshot.data() };

        if (orderData.isExpired) {
            showToast('Esta orden ha expirado y no puede ser modificada.', 'error');
            showLoading(false);
            return;
        }

        let finalStatus = newStatus;
        const oldStatus = orderData.status;

        let extraUpdates = {};
        if (finalStatus === 'Pausado' && oldStatus !== 'Pausado') {
            const machineStatusOnPause = await requestMachineStatusOnPause(orderData.type);
            extraUpdates.machineStatusOnPause = machineStatusOnPause;
            showLoading(true); // Restaurar el cargador para el proceso de guardado
        }

        // 21 CFR Part 11: Re-autenticación para firma electrónica
        const isFinishing = (finalStatus === 'Pendiente de Evaluación' || finalStatus === 'Completado' || finalStatus === 'Pendiente de Aprobación');
        if (isFinishing && oldStatus !== finalStatus) {
            const confirmed = await new Promise(resolve => {
                requestSignature(() => resolve(true), () => resolve(false));
            });
            if (!confirmed) {
                showLoading(false);
                return;
            }
            showLoading(true);
        }

        const updates = {};

        // Si es de plan o viene de una solicitud y el técnico intenta completar sin validación, cambiar a Pendiente de Aprobación
        const isPlanOrder = ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(orderData.type) || orderData.linkedPlanId;
        const hasRequest = orderData.solicitudId || orderData.sourceSolicitudId;
        if (newStatus === 'Completado' && (isPlanOrder || hasRequest) && !orderData.validatedBy) {
            finalStatus = 'Pendiente de Aprobación';
            updates.technicianFinished = true;
            showToast('Trabajo finalizado. Pendiente de validación por el Planificador.', 'info');
        }

        if (isWorkOrderFinishStatus(finalStatus)) {
            const orderForInventoryCheck = { ...orderData, ...updates, status: finalStatus };
            const oldOrderForStock = isWorkOrderFinishStatus(oldStatus) ? orderData : { partsUsed: [] };
            const canFinishWithInventory = await enforceWorkOrderInventoryBeforeFinish(orderForInventoryCheck, oldOrderForStock);
            if (!canFinishWithInventory) {
                showLoading(false);
                return;
            }
            if (orderForInventoryCheck.observaciones !== orderData.observaciones) {
                updates.observaciones = orderForInventoryCheck.observaciones;
                orderData.observaciones = orderForInventoryCheck.observaciones;
            }
            if (orderForInventoryCheck.allowStockShortageOnFinish) {
                updates.allowStockShortageOnFinish = true;
                updates.stockShortageJustification = orderForInventoryCheck.stockShortageJustification || '';
                orderData.allowStockShortageOnFinish = true;
                orderData.stockShortageJustification = orderForInventoryCheck.stockShortageJustification || '';
            }
        }

        updates.status = finalStatus;

        // Solo generar nuevo ID si está iniciando por primera vez (cambio de MP a MA o no tiene ID)
        const isPlanned = !orderData.id || orderData.id.startsWith('MP');
        const isFirstStart = newStatus === 'En Proceso' && oldStatus !== 'En Proceso' && isPlanned;

        const now = new Date();
        if (isFirstStart) {
            updates.id = generateNextId('MA');
            updates.fechaInicioReal = now.toISOString();
            // Actualizar la fecha principal de la orden a la fecha local actual para que se mueva en el planificador
            updates.date = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`;

            // Filtrar repuestos sin stockActual al iniciar la orden desde Kanban
            let removedParts = [];
            let adjustedParts = [];
            const processedParts = [];

            for (const item of (orderData.partsUsed || [])) {
                const part = state.parts.find(p => p.id === item.partId);
                const stock = part ? (part.stockActual || 0) : 0;

                if (stock <= 0) {
                    removedParts.push(part ? part.description : item.partId);
                } else {
                    let finalQty = item.quantity;
                    if (stock < item.quantity) {
                        finalQty = stock;
                        adjustedParts.push(`${part ? part.description : item.partId} (ajustado de ${item.quantity} a ${stock})`);
                    }
                    processedParts.push({ ...item, quantity: finalQty });
                }
            }

            if (removedParts.length > 0 || adjustedParts.length > 0) {
                updates.partsUsed = processedParts;
                let note = "\n\nNota del Sistema (Inicio de OT desde Kanban):";
                if (removedParts.length > 0) {
                    note += `\n- Se eliminaron por falta de stockActual: ${removedParts.join(', ')}.`;
                }
                if (adjustedParts.length > 0) {
                    note += `\n- Se ajustó cantidad por stockActual insuficiente stockActual: ${adjustedParts.join(', ')}.`;
                }
                updates.observaciones = (orderData.observaciones || "") + note;
            }
        }

        let workIntervals = Array.isArray(orderData.workIntervals) ? JSON.parse(JSON.stringify(orderData.workIntervals)) : [];

        if (finalStatus === 'En Proceso') {
            workIntervals.push({ start: now.toISOString(), end: null });
        } else if (['Pausado', 'Pendiente de Evaluación', 'Completado', 'Pendiente de Aprobación'].includes(finalStatus) && oldStatus === 'En Proceso') {
            const lastInterval = workIntervals.find(i => !i.end);
            if (lastInterval) {
                lastInterval.end = now.toISOString();
            }
        }
        updates.workIntervals = workIntervals;

        if (['Pendiente de Evaluación', 'Completado', 'Pendiente de Aprobación'].includes(finalStatus)) {
            if (!orderData.fechaFinalizacionReal) {
                updates.fechaFinalizacionReal = now.toISOString();
            }
            const updatedOrderForCalc = { ...orderData, ...updates };
            updates.totalWorkDurationMs = getActiveWorkDurationMs(updatedOrderForCalc);
        }

        if (finalStatus === 'Pendiente de Evaluación' || finalStatus === 'Completado' || finalStatus === 'Pendiente de Aprobación') {
            const wasAlreadyProcessed = ['Pendiente de Evaluación', 'Completado', 'Pendiente de Aprobación'].includes(oldStatus);
            // Si ya estaba en un estado que descuenta stockActual, pasamos el objeto original para calcular el delta (que será 0 si no cambiaron repuestos)
            // Si no, pasamos un objeto vacío para que descuente todo por primera vez
            await updateStockForOrder(wasAlreadyProcessed ? orderData : { partsUsed: [] }, orderData);
        }

        const oldOrder = JSON.parse(JSON.stringify(orderData));
        const finalOrderData = { ...orderData, ...updates };
        await updateDoc(orderRef, updates);
        await updateRTDBMirror('workOrders', finalOrderData, workOrderFbId);
        const detailedAction = getWorkOrderDetailedAction(oldOrder, finalOrderData) + (isFinishing ? ' (Firmado)' : '');
        await recordAuditLog('workOrders', orderData.id || workOrderFbId, 'UPDATE', oldOrder, finalOrderData, detailedAction);

        // If finishing the order, also finish any linked plan execution
        if (finalStatus === 'Pendiente de Evaluación' || finalStatus === 'Completado' || finalStatus === 'Pendiente de Aprobación') {
            await completeLinkedPlanExecution(workOrderFbId, now.toISOString(), finalStatus);
        }

        // If the work order is linked to a solicitud, ensure the solicitud has the workOrderFbId
        if (orderData.sourceSolicitudId || orderData.solicitudId) {
            const solicitud = state.solicitudes.find(s => s.fb_id === orderData.sourceSolicitudId || s.id === orderData.solicitudId);
            if (solicitud) {
                const solUpdates = {
                    workOrderFbId: workOrderFbId,
                    workOrderId: updates.id || orderData.id
                };
                if (solicitud.status === 'Pendiente') {
                    solUpdates.status = 'Aprobado';
                }
                await updateDoc(doc(state.collections.solicitudes, solicitud.fb_id), solUpdates);
            }
        }

        // Manually trigger a UI update to reflect the change immediately, as onSnapshot can have a slight delay.
        const updatedOrder = { ...orderData, ...updates };
        const orderIndex = state.workOrders.findIndex(wo => wo.fb_id === workOrderFbId);
        if (orderIndex > -1) {
            state.workOrders[orderIndex] = updatedOrder;
        } else {
            state.workOrders.push(updatedOrder);
        }
        renderActiveWorkView(); // Re-render the Kanban view

        showToast(`Orden de trabajo actualizada a: ${newStatus}`, 'success');
    } catch (error) {
        console.error("Error updating work order from Kanban:", error);
        showToast(error.message || 'Error al actualizar la orden de trabajo.', 'error');
    } finally {
        showLoading(false);
    }
}

// --- Assigned Orders Functions (for Technicians) ---
function renderAssignedOrders() {
    if (state.currentTab !== 'ordenes-asignadas' && initialLoadComplete) return;
    const container = document.getElementById('assigned-orders-container');
    if (!container) return;
    container.innerHTML = '';

    if (!state.currentUser || (getCurrentUser().role !== 'Técnico' && getCurrentUser().role !== 'Admin')) {
        container.innerHTML = '<div class="alert alert-warning">No tiene permisos para ver esta sección.</div>';
        return;
    }

    const monthFilter = document.getElementById('assigned-month-filter');
    const selectedMonth = monthFilter ? monthFilter.value : null;

    if (selectedMonth) {
        const [year, month] = selectedMonth.split('-').map(Number);
        const monthName = new Date(year, month - 1, 1)
            .toLocaleDateString('es-ES', { month: 'long', year: 'numeric' })
            .replace(/^\w/, c => c.toUpperCase());
        const titleEl = document.getElementById('assigned-orders-title');
        if (titleEl) titleEl.textContent = `Mis Órdenes de Trabajo - ${monthName}`;
    }

    const assignedOrders = state.workOrders.filter(order => {
        const isLead = order.leadTechnician === getCurrentUser().username;
        const isSupport = order.technicians && order.technicians.includes(getCurrentUser().username);
        const isAssigned = isLead || isSupport;
        const isActiveStatus = ['Pendiente', 'En Proceso', 'Pausado'].includes(order.status);

        if (!isAssigned || !isActiveStatus) return false;

        if (selectedMonth) {
            const orderDateStr = order.date;
            if (!orderDateStr) return false;

            let orderMonthStr = "";
            if (/^\d{4}-\d{2}-\d{2}$/.test(orderDateStr)) {
                orderMonthStr = orderDateStr.substring(0, 7);
            } else {
                const d = new Date(orderDateStr);
                if (isNaN(d.getTime())) return false;
                orderMonthStr = `${d.getFullYear()}-${(d.getMonth() + 1).toString().padStart(2, '0')}`;
            }
            return orderMonthStr === selectedMonth;
        }

        return true;
    });

    if (assignedOrders.length === 0) {
        container.innerHTML = '<p class="text-center text-muted mt-4">No tiene órdenes de trabajo asignadas para este período.</p>';
        return;
    }

    assignedOrders.sort((a, b) => new Date(a.date) - new Date(b.date));

    assignedOrders.forEach(order => {
        container.appendChild(createAssignedOrderCard(order));
    });
}

function createAssignedOrderCard(order) {
    const card = document.createElement('div');
    card.className = 'card kanban-card mb-3';
    const machine = state.machines.find(m => m.id === order.machineId) || { name: 'Desconocido' };
    const isLead = getCurrentUser().username === order.leadTechnician;
    const isAdminOrPlanner = getCurrentUser().role === 'Admin' || getCurrentUser().role === 'Planificador';

    let actionButtonsHTML = '';
    if(isLead || isAdminOrPlanner) {
        let statusButtons = '';
        switch (order.status) {
            case 'Pendiente' :
                statusButtons = `<button class="btn btn-primary start-task-btn"><i class="fas fa-play me-2"></i>Iniciar</button>`;
                break;
            case 'En Proceso' :
                statusButtons = `
                    <button class="btn btn-warning pause-task-btn"><i class="fas fa-pause me-2"></i>Pausar</button>
                    <button class="btn btn-success complete-task-btn ms-2"><i class="fas fa-check me-2"></i>Finalizar</button>
                `;
                break;
            case 'Pausado' :
                statusButtons = `<button class="btn btn-info resume-task-btn"><i class="fas fa-play me-2"></i>Reanudar</button>`;
                break;
        }
        actionButtonsHTML = `
            <div class="btn-group">
                ${statusButtons}
            </div>
            <div class="btn-group ms-2" role="group">
                <button class="btn btn-outline-secondary edit-details-btn" title="Editar Detalles y Falla"><i class="fas fa-edit"></i></button>
                <button class="btn btn-outline-secondary manage-parts-btn" title="Gestionar Repuestos"><i class="fas fa-box-open"></i></button>
            </div>
        `;
    } else {
         actionButtonsHTML = `<button class="btn btn-outline-secondary view-details-btn"><i class="fas fa-eye me-2"></i>Ver Detalles</button>`;
    }

    const cardTitle = order.id ? order.id : `(${machine.id || 'N/A'})`;
    const fechaLimite = order.date && !isNaN(new Date(order.date))
        ? new Date(order.date).toLocaleDateString('es-ES')
        : 'No especificada';

    const typeBadge = `<span class="badge bg-${['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(order.type) ? 'primary' : 'danger'} mb-2">${order.type}</span>`;
    const statusBadge = `<span class="badge bg-${order.status === 'En Proceso' ? 'info text-dark' : (order.status === 'Pausado' ? 'secondary' : 'warning text-dark')}">${order.status}</span>`;

    card.innerHTML = `
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-start">
                <div>
                    ${typeBadge}
                    <h5 class="card-title mb-1" style="cursor: pointer;">${cardTitle}</h5>
                    <h6 class="card-subtitle mb-3 text-muted"><i class="fas fa-cogs me-1"></i>${machine.name}</h6>
                </div>
                ${statusBadge}
            </div>

            <div class="row mb-3">
                <div class="col-md-6">
                    <p class="card-text mb-1"><small class="text-muted">Responsable:</small><br><strong>${order.leadTechnician || 'N/A'}</strong></p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="card-text mb-1"><small class="text-muted">Fecha Límite:</small><br><strong>${fechaLimite}</strong></p>
                </div>
            </div>

            <div class="p-3 bg-light rounded mb-3 border-start border-4 border-${['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(order.type) ? 'primary' : 'danger'}">
                <p class="card-text mb-0">${order.description}</p>
            </div>

            <div class="d-flex justify-content-end align-items-center">
                <div>${actionButtonsHTML}</div>
            </div>
        </div>
    `;

    if (isLead) {
        card.querySelector('.start-task-btn').addEventListener('click', () => handleKanbanWorkOrderAction(order.fb_id, 'En Proceso'));
        card.querySelector('.pause-task-btn').addEventListener('click', () => handleKanbanWorkOrderAction(order.fb_id, 'Pausado'));
        card.querySelector('.resume-task-btn').addEventListener('click', () => handleKanbanWorkOrderAction(order.fb_id, 'En Proceso'));
        card.querySelector('.complete-task-btn').addEventListener('click', () => {
            showConfirmation(
                'Finalizar Orden de Trabajo',
                `¿Está seguro que desea marcar la orden ${order.id || `(${order.machineId})`} como finalizada`,
                () => handleKanbanWorkOrderAction(order.fb_id, 'Completado')
            );
        });
        card.querySelector('.manage-parts-btn').addEventListener('click', () => showManagePartsModal(order.id));
        card.querySelector('.edit-details-btn').addEventListener('click', () => showWorkOrderModal(order.fb_id));
    } else {
        card.querySelector('.view-details-btn').addEventListener('click', () => showWorkOrderModal(order.fb_id));
    }
    
    card.querySelector('.card-title').addEventListener('click', () => showWorkOrderModal(order.fb_id));

    return card;
}

function showManagePartsModal(orderId) {
    const order = state.workOrders.find(o => o.id === orderId);
    if (!order) {
        showToast('Orden de trabajo no encontrada.', 'error');
        return;
    }

    document.getElementById('manage-parts-modal-title').textContent = `Gestionar Repuestos para OT: ${orderId}`;
    document.getElementById('manage-parts-wo-id').value = orderId;

    const partsListContainer = document.getElementById('current-parts-list');
    partsListContainer.innerHTML = '';
    
    const currentParts = order.partsUsed || [];
    if (currentParts.length > 0) {
        currentParts.forEach(partUsage => {
            renderPartInManageModal(partUsage.partId, partUsage.quantity);
        });
    } else {
        partsListContainer.innerHTML = '<p class="text-muted list-group-item">No hay repuestos asignados a esta orden.</p>';
    }

    const partSelect = document.getElementById('manage-part-select');
    let pHtml = '<option value="">Seleccione un repuesto...</option>';

    // Robust filtering: convert IDs to strings and handle both arrays and single IDs
    const targetMachineId = String(order.machineId);

    const relevantParts = state.parts.filter(p => {
        const mIds = Array.isArray(p.machineIds) ? p.machineIds.map(String) : [];
        const mId = p.machineId ? String(p.machineId) : null;
        return mIds.includes(targetMachineId) || mId === targetMachineId;
    });

    // If no relevant parts for this machine, show all as fallback
    const partsToDisplay = relevantParts.length > 0 ? relevantParts : state.parts;

    partsToDisplay.sort((a, b) => a.description.localeCompare(b.description)).forEach(p => {
        pHtml += `<option value="${p.id}">${p.description} (Stock: ${p.stockActual})</option>`;
    });
    partSelect.innerHTML = pHtml;

    state.modals.manageParts.show();
}

function renderPartInManageModal(partId, quantity) {
    const part = state.parts.find(p => p.id === partId);
    if (!part) return;

    const listContainer = document.getElementById('current-parts-list');
    
    const placeholder = listContainer.querySelector('p');
    if (placeholder) placeholder.remove();

    let partRow = listContainer.querySelector(`[data-part-id="${partId}"]`);
    if(partRow) {
        partRow.dataset.quantity = quantity;
        partRow.querySelector('.part-quantity-badge').textContent = `Cant: ${quantity}`;
    } else {
        partRow = document.createElement('div');
        partRow.className = 'list-group-item d-flex justify-content-between align-items-center';
        partRow.dataset.partId = partId;
        partRow.dataset.quantity = quantity;

        partRow.innerHTML = `
            <span>${part.description}</span>
            <div>
                <span class="badge bg-secondary me-2 part-quantity-badge">Cant: ${quantity}</span>
                <button type="button" class="btn btn-sm btn-outline-danger remove-managed-part-btn"><i class="fas fa-times"></i></button>
            </div>
        `;
        listContainer.appendChild(partRow);
        
        partRow.querySelector('.remove-managed-part-btn').addEventListener('click', () => {
            partRow.remove();
            if (listContainer.children.length === 0) {
                 listContainer.innerHTML = '<p class="text-muted list-group-item">No hay repuestos asignados a esta orden.</p>';
            }
        });
    }
}

function handleAddOrUpdatePartInModal() {
    const partSelect = document.getElementById('manage-part-select');
    const qtyInput = document.getElementById('manage-part-quantity');
    const selectedPartId = partSelect.value;
    const selectedQty = parseInt(qtyInput.value);

    if (!selectedPartId) {
        showToast('Seleccione un repuesto.', 'warning');
        return;
    }
    if (isNaN(selectedQty) || selectedQty < 1) {
        showToast('La cantidad debe ser un número positivo.', 'warning');
        return;
    }
    renderPartInManageModal(selectedPartId, selectedQty);
    partSelect.value = '';
    qtyInput.value = 1;
}

async function handleSaveManagedParts() {
    const orderId = document.getElementById('manage-parts-wo-id').value;
    if (!orderId) return;

    showLoading(true);

    const originalOrder = state.workOrders.find(o => o.id === orderId);
    if (!originalOrder) {
        showToast('Orden no encontrada.', 'error');
        showLoading(false);
        return;
    }
    
    const originalPartsMap = new Map((originalOrder.partsUsed || []).map(p => [p.partId, p]));
    
    const newPartsList = [];
    const newPartsMap = new Map();
    document.querySelectorAll('#current-parts-list .list-group-item[data-part-id]').forEach(item => {
        const partId = item.dataset.partId;
        const quantity = parseInt(item.dataset.quantity);
        newPartsList.push({ partId, quantity });
        newPartsMap.set(partId, quantity);
    });

    const stockDeltasActual = new Map();
    newPartsMap.forEach((newQty, partId) => {
        const oldEntry = originalPartsMap.get(partId);
        const oldQty = (oldEntry && oldEntry.delivered !== false) ? oldEntry.quantity : 0;
        const delta = newQty - oldQty;
        if (delta !== 0) {
            stockDeltasActual.set(partId, (stockDeltasActual.get(partId) || 0) - delta); // Subtract delta from stock
        }
    });
    
    originalPartsMap.forEach((oldEntry, partId) => {
        if (!newPartsMap.has(partId)) {
            const oldQty = (oldEntry && oldEntry.delivered !== false) ? oldEntry.quantity : 0;
            if (oldQty > 0) {
                stockDeltasActual.set(partId, (stockDeltasActual.get(partId) || 0) + oldQty); // Add back to stock
            }
        }
    });

    try {
        const finalizedPartsList = [];
        for (const [partId, delta] of stockDeltasActual.entries()) {
            const partDocRef = doc(state.collections.parts, partId);
            const partDoc = await getDoc(partDocRef);
            const newEntry = newPartsMap.get(partId);

            if (partDoc.exists()) {
                const currentStock = partDoc.data().stockActual;
                const newStock = currentStock + delta;

                if (newStock < 0) {
                    // Marcar como no entregado si no hay stock
                    if (newEntry) finalizedPartsList.push({ ...newEntry, delivered: false });
                } else {
                    await updateDoc(partDocRef, { stockActual: newStock });
                    if (delta !== 0) {
                        await recordPartMovement(partId, delta, delta > 0 ? 'Devolución OT' : 'Salida OT', {
                            orderId: originalOrder.fb_id,
                            orderHumanId: originalOrder.id
                        });
                    }
                    if (newEntry) finalizedPartsList.push({ ...newEntry, delivered: true });
                }
            } else if (newEntry) {
                 finalizedPartsList.push({ ...newEntry, delivered: false });
            }
        }

        // Add back parts that didn't have stockActual change but were already in finalized list
        newPartsList.forEach(p => {
            if (!stockDeltasActual.has(p.partId)) {
                const oldEntry = originalPartsMap.get(p.partId);
                finalizedPartsList.push(oldEntry || p);
            }
        });
        
        const orderDocRef = doc(state.collections.workOrders, originalOrder.fb_id);
        await updateDoc(orderDocRef, { partsUsed: finalizedPartsList });
        
        showToast('Repuestos actualizados correctamente.', 'success');
        state.modals.manageParts.hide();

    } catch (error) {
        console.error("Error saving managed parts:", error);
        showToast(error.message || 'Error al guardar los cambios en los repuestos.', 'error');
    } finally {
        showLoading(false);
    }
}


// --- Planner/Calendar Functions ---
function renderCalendar() {
    cleanupPopovers();
    const grid = document.getElementById('calendar-grid');
    const monthYearEl = document.getElementById('calendar-month-year');
    grid.innerHTML = '';

    const date = state.calendarDate;
    const year = date.getFullYear();
    const month = date.getMonth();

    monthYearEl.textContent = date.toLocaleDateString('es-ES', { month: 'long', year: 'numeric' });

    const firstDay = new Date(year, month, 1).getDay();
    const daysInMonth = new Date(year, month + 1, 0).getDate();

    let workOrdersToDisplay = state.workOrders;
    const userRole = getCurrentUser().role;
    const managedMachineIds = getCurrentUser().managedMachineIds;
    const equipoAsignado = getCurrentUser().equipoAsignado;

    if ((userRole === 'Jefe de Area' && Array.isArray(managedMachineIds)) || (userRole === 'Técnico' && Array.isArray(equipoAsignado)) || ((['Operario', 'Supervisor de Area'].includes(userRole)) && Array.isArray(equipoAsignado))) {
        const relevantMachineIds = new Set(userRole === 'Jefe de Area' ? managedMachineIds : equipoAsignado);
        workOrdersToDisplay = state.workOrders.filter(wo => relevantMachineIds.has(wo.machineId));
    }

    const searchTerm = document.getElementById('planner-search-input').value.toLowerCase();
    if (searchTerm) {
        workOrdersToDisplay = workOrdersToDisplay.filter(wo => {
            const machineName = state.machines.find(m => m.id === wo.machineId).name || '';
            const leadTechnician = wo.leadTechnician || '';
            return machineName.toLowerCase().includes(searchTerm) ||
                   wo.machineId.toLowerCase().includes(searchTerm) ||
                   leadTechnician.toLowerCase().includes(searchTerm);
        });
    }

    let calendarHtml = '';
    for (let i = 0; i < firstDay; i++) {
        calendarHtml += '<div class="calendar-day not-month"></div>';
    }
    grid.innerHTML = calendarHtml;

    for (let day = 1; day <= daysInMonth; day++) {
        const dayEl = document.createElement('div');
        dayEl.className = 'calendar-day';
        dayEl.innerHTML = `<div class="day-number">${day}</div>`;
        
        const currentDateStr = `${year}-${String(month + 1).padStart(2, '0')}-${String(day).padStart(2, '0')}`;
        
        const tasksForDay = workOrdersToDisplay.filter(wo => wo.date === currentDateStr);
        
        tasksForDay.forEach(task => {
            const taskEl = document.createElement('div');
            taskEl.className = `calendar-task task-${task.type}`;
            if (task.status === 'Pendiente de Aprobación') {
                taskEl.classList.add('task-pending-approval');
            } else if (task.status === 'Cancelado') {
                taskEl.classList.add('task-cancelled');
            }
            const isCompleted = task.status === 'Completado' || task.status === 'Pendiente de Evaluación';
            const displayText = task.id || `(${task.machineId})`;
            taskEl.innerHTML = isCompleted ? `<i class="fas fa-star text-warning"></i> ${displayText}` : displayText;
            taskEl.dataset.fb_id = task.fb_id;
            
            taskEl.setAttribute('data-bs-toggle', 'popover');
            taskEl.setAttribute('data-bs-trigger', 'hover focus');
            taskEl.setAttribute('data-bs-title', `Detalles de OT: ${task.id || 'Pendiente'}`);
            taskEl.setAttribute('data-bs-content', `<b>Máquina:</b> ${task.machineId}<br><b>Tarea:</b> ${task.description}`);
            taskEl.setAttribute('data-bs-html', 'true');

            taskEl.addEventListener('click', (e) => {
                e.stopPropagation();
                const instance = bootstrap.Popover.getInstance(taskEl);
                if (instance) instance.dispose();
                showWorkOrderModal(task.fb_id);
            });
            dayEl.appendChild(taskEl);
        });

        grid.appendChild(dayEl);
    }

    initializeCalendarPopovers();
}

function initializeCalendarPopovers() {
    const calendar = document.getElementById('calendar-grid');
    if (!calendar || state.currentTab !== 'planificador') return;

    calendar.querySelectorAll('[data-bs-toggle="popover"]').forEach(popoverTriggerEl => {
        const existing = bootstrap.Popover.getInstance(popoverTriggerEl);
        if (existing) existing.dispose();

        new bootstrap.Popover(popoverTriggerEl, {
            container: 'body',
            html: true,
            trigger: 'hover focus'
        });
    });
}

/**
 * Limpia todos los popovers de Bootstrap activos para evitar que queden huérfanos.
 */
function cleanupPopovers() {
    const popovers = document.querySelectorAll('.popover');
    popovers.forEach(p => p.remove());

    // También intentar destruir las instancias de Bootstrap si están disponibles
    const triggers = document.querySelectorAll('[data-bs-toggle="popover"]');
    triggers.forEach(el => {
        const instance = bootstrap.Popover.getInstance(el);
        if (instance) {
            instance.dispose();
        }
    });
}

function changeMonth(offset) {
    state.calendarDate.setMonth(state.calendarDate.getMonth() + offset);
    renderCalendar();
}

// --- Comprehensive Work Order Modal Functions ---
/**
 * Obtiene o crea un ID de equipo en Odoo basado en el ID de la máquina de la app.
 * @param {string} machineId - ID de la máquina en la aplicación (ej: 'M001')
 * @returns {Promise<number|null>} - ID entero de Odoo o null si falla
 */
/**
 * Sincroniza una máquina con Odoo. Si no existe, la crea. Si existe, la actualiza.
 * Devuelve el ID de Odoo.
 */
async function syncAllMachinesToOdoo() {
    if (!state.odoo) {
        showToast('Odoo no está configurado o conectado.', 'error');
        return;
    }

    // Optimization: Identify only machines that might need syncing (no odoo_id or modified since last sync)
    const machinesToSync = state.machines.filter(m => !m.odoo_id || !m.lastOdooSync || (m.lastModified && m.lastModified > m.lastOdooSync));

    if (machinesToSync.length === 0) {
        showToast('Todos los equipos ya están sincronizados.', 'info');
        return;
    }

    const confirmSync = confirm(`Se han detectado ${machinesToSync.length} máquinas con cambios pendientes. ¿Desea sincronizarlas ahora`);
    if (!confirmSync) return;

    showLoading(true);
    state.odooSyncError = null;
    let successCount = 0;
    let errorCount = 0;

    const batchSize = 5;

    for (let i = 0; i < machinesToSync.length; i += batchSize) {
        const batch = machinesToSync.slice(i, i + batchSize);
        await Promise.all(batch.map(async (machine) => {
            try {
                await syncMachineToOdoo(machine);
                successCount++;
            } catch (error) {
                console.error(`Error sincronizando máquina ${machine.id}:`, error);
                errorCount++;
            }
        }));
    }

    showLoading(false);
    if (errorCount === 0) {
        showToast(`Sincronización completada: ${successCount} máquinas procesadas.`, 'success');
    } else {
        showToast(`Sincronización terminada con algunos errores: ${successCount} éxito, ${errorCount} fallos.`, 'warning');
    }
}

async function triggerRTDBFullSync() {
    if (!state.currentUser || getCurrentUser().role !== 'Admin') return;
    showLoading(true);
    try {
        const promises = [];
        state.machines.forEach(m => promises.push(updateRTDBMirror('machines', m, m.fb_id || m.id)));
        state.solicitudes.forEach(s => promises.push(updateRTDBMirror('solicitudes', s, s.fb_id)));
        state.workOrders.forEach(wo => promises.push(updateRTDBMirror('workOrders', wo, wo.fb_id)));

        await Promise.all(promises);
        showToast('Sincronización RTDB completada', 'success');
    } catch (error) {
        console.error("RTDB Full Sync error:", error);
        showToast('Error en sincronización RTDB', 'error');
    } finally {
        showLoading(false);
    }
}
window.triggerRTDBFullSync = triggerRTDBFullSync;

async function syncAllPartsToOdoo() {
    if (!state.odoo) {
        showToast('Odoo no está configurado o conectado.', 'error');
        return;
    }

    // Optimization: Identify only parts that might need syncing
    const partsToSync = state.parts.filter(p => !p.odoo_id || !p.lastOdooSync || (p.lastModified && p.lastModified > p.lastOdooSync));

    if (partsToSync.length === 0) {
        showToast('Todos los repuestos ya están sincronizados.', 'info');
        return;
    }

    const confirmSync = confirm(`Se han detectado ${partsToSync.length} repuestos con cambios pendientes. ¿Desea sincronizarlos ahora`);
    if (!confirmSync) return;

    showLoading(true);
    state.odooSyncError = null;
    let successCount = 0;
    let errorCount = 0;

    const batchSize = 8;

    for (let i = 0; i < partsToSync.length; i += batchSize) {
        const batch = partsToSync.slice(i, i + batchSize);
        await Promise.all(batch.map(async (part) => {
            try {
                // Sincronizar metadata y stock
                await syncPartStockToOdoo(part.id, part);
                successCount++;
            } catch (error) {
                console.error(`Error sincronizando repuesto ${part.id}:`, error);
                errorCount++;
            }
        }));
    }

    showLoading(false);
    if (errorCount === 0) {
        showToast(`Sincronización completada: ${successCount} repuestos procesados.`, 'success');
    } else {
        showToast(`Sincronización terminada con algunos errores: ${successCount} éxito, ${errorCount} fallos.`, 'warning');
    }
}

async function syncPartToOdoo(partData) {
    if (!state.odoo || !partData) return null;

    try {
        const isInsumo = partData.classification === 'insumo';
        const isOdoo18Plus = !state.odooVersion || state.odooVersion >= 18;

        const odooProduct = {
            default_code: partData.id,
            name: partData.description,
            standard_price: partData.cost || 0,
            purchase_ok: true,
            sale_ok: false
        };

        if (isOdoo18Plus) {
            // Odoo 18+ (SaaS 19.1+): 'detailed_type' eliminado, 'type' es 'consu' para bienes (Goods)
            // Se usa 'is_storable' para activar el seguimiento de inventario (Track Inventory)
            odooProduct.type = 'consu';
            odooProduct.is_storable = !isInsumo;
        } else {
            // Odoo 17 y anteriores: 'type' limitado, se requiere 'detailed_type'
            odooProduct.type = 'consu';
            odooProduct.detailed_type = isInsumo ? 'consu' : 'product';
        }

        let odooId = partData.odoo_id;

        // Manejo de ID corrupto
        if (odooId && (typeof odooId !== 'number' && String(odooId).includes('[object'))) {
            console.warn(`[Odoo Sync] ID de Odoo corrupto detectado para repuesto ${partData.id}: ${odooId}. Re-sincronizando...`);
            odooId = null;
        }

        if (!odooId) {
            // Buscar si ya existe por código de referencia (default_code) en product.product
            const results = await state.odoo.searchRead('product.product', [['default_code', '=', partData.id]], ['id', 'default_code', 'name']);

            if (results && results.length > 0) {
                // Verificar coincidencia exacta por código
                const exactMatch = results.find(r => r.default_code === partData.id);
                if (exactMatch) {
                    odooId = exactMatch.id;
                }
            }

            // Si no se encontró por código, intentar por nombre
            if (!odooId) {
                const searchName = partData.description.trim();
                const nameResults = await state.odoo.searchRead('product.product', [['name', '=', searchName]], ['id', 'name']);
                if (nameResults && nameResults.length > 0) {
                    const exactMatch = nameResults.find(r => r.name.trim() === searchName);
                    if (exactMatch) odooId = exactMatch.id;
                }
            }
        }

        if (odooId) {
            console.log(`[Odoo Sync] Actualizando repuesto existente en Odoo (ID: ${odooId})`);
            await state.odoo.write('product.product', odooId, odooProduct);
            // Limpiar error previo si la actualización fue exitosa
            if (state.odooSyncError) {
                state.odooSyncError = null;
                if (typeof renderParts === 'function') renderParts();
            }
        } else {
            console.log(`[Odoo Sync] Creando nuevo repuesto en Odoo...`);
            // Nota: Al crear product.product, Odoo crea automáticamente el product.template asociado.
            // Esto asegura que el ID que obtenemos sea el de product.product (variante) para el stockActual.
            odooId = await state.odoo.create('product.product', odooProduct);
            console.log(`[Odoo Sync] Nuevo repuesto creado en Odoo con ID: ${odooId}`);
            // Limpiar error previo si la creación fue exitosa
            if (state.odooSyncError) {
                state.odooSyncError = null;
                if (typeof renderParts === 'function') renderParts();
            }
        }

        if (odooId) {
            console.log(`[Odoo Sync] Actualizando marca de tiempo de sincronización para repuesto ${partData.id}`);
            const syncTime = new Date().toISOString();
            const partDocId = partData.fb_id || partData.id;
            await updateDoc(doc(state.collections.parts, partDocId), {
                odoo_id: odooId,
                lastOdooSync: syncTime
            });
            const localPart = state.parts.find(p => p.fb_id === partData.fb_id || p.id === partData.id);
            if (localPart) {
                localPart.odoo_id = odooId;
                localPart.lastOdooSync = syncTime;
            }
        }

        return odooId;
    } catch (e) {
        console.error("[Odoo Sync] Error sincronizando repuesto:", e);
        state.odooSyncError = `Error sincronizando repuesto: ${e.message || e}`;
        if (typeof renderParts === 'function') renderParts();
        throw e;
    }
}

async function uploadPartFile(partId, file, type) {
    if (!file) return null;
    const MAX_SIZE = 10 * 1024 * 1024;
    if (file.size > MAX_SIZE) {
        throw new Error(`El archivo ${file.name} excede el límite de 10MB.`);
    }
    const fileName = `${type}_${Date.now()}_${file.name}`;
    const storageRef = ref(state.storage, `artifacts/${appId}/parts/${partId}/${fileName}`);
    try {
        const snapshot = await uploadBytes(storageRef, file);
        return await getOptimizedStorageUrl(snapshot.ref);
    } catch (error) {
        console.error(`Error uploading part ${type}:`, error);
        throw error;
    }
}

async function syncMachineToOdoo(machineData) {
    if (!state.odoo || !machineData) return null;

    console.log(`[Odoo Sync] Iniciando sincronización para máquina: ${machineData.id} (${machineData.name})`);

    try {
        let locationId = null;
        if (machineData.location && machineData.location.trim()) {
            const locName = machineData.location.trim();
            try {
                console.log(`[Odoo Sync] Buscando ubicación en Odoo: "${locName}"`);
                const locations = await state.odoo.searchRead('stock.location', [['name', '=', locName]], ['id']);
                if (locations && locations.length > 0) {
                    locationId = locations[0].id;
                    console.log(`[Odoo Sync] Ubicación encontrada en Odoo con ID: ${locationId}`);
                } else {
                    console.log(`[Odoo Sync] Ubicación "${locName}" no encontrada. Creándola...`);
                    // Intentamos crear la ubicación básica.
                    locationId = await state.odoo.create('stock.location', { name: locName });
                    console.log(`[Odoo Sync] Nueva ubicación creada en Odoo con ID: ${locationId}`);
                }
            } catch (locError) {
                console.warn(`[Odoo Sync] No se pudo obtener/crear la ubicación ? "${locName}" :`, locError);
                // Según requerimiento: si falla, se sincroniza sin ubicación
            }
        }

        const odooMachine = {
            name: machineData.name,
            model: machineData.type || 'maquina',
            serial_no: machineData.id,
            note: `Sincronizado desde App CMMS - ID Local: ${machineData.id}`
        };

        if (locationId) {
            odooMachine.location_id = locationId;
        }

        let odooId = machineData.odoo_id;

        // Si el ID guardado parece corrupto (ej. "[object Object]"), lo ignoramos
        if (odooId && (typeof odooId !== 'number' && String(odooId).includes('[object'))) {
            console.warn(`[Odoo Sync] ID de Odoo corrupto detectado para ${machineData.id}: ${odooId}. Re-sincronizando...`);
            odooId = null;
        }

        // Si no tenemos odoo_id, buscamos por Número de Serie (ID local)
        if (!odooId) {
            const searchSerial = machineData.id.trim();
            console.log(`[Odoo Sync] Buscando equipo en Odoo por Número de Serie: "${searchSerial}"`);

            const results = await state.odoo.searchRead('maintenance.equipment', [['serial_no', '=', searchSerial]], ['id', 'name', 'serial_no']);
            console.log(`[Odoo Sync] Resultados de búsqueda por serial_no:`, results);

            if (results && results.length > 0) {
                // Verificamos si alguno de los resultados coincide exactamente por serial_no
                const exactMatch = results.find(r => (r.serial_no || '').trim() === searchSerial);

                if (exactMatch) {
                    odooId = exactMatch.id;
                    console.log(`[Odoo Sync] Equipo encontrado en Odoo con ID: ${odooId} (Coincidencia por Serial)`);
                } else {
                    console.log(`[Odoo Sync] Se encontraron equipos (${results.length}) pero ninguno coincide por Serial "${searchSerial}"`);
                    results.forEach((r, idx) => console.log(`  [${idx}] Encontrado en Odoo: "${r.name}" (ID: ${r.id}, Serial: ${r.serial_no})`));
                }
            }
        }

        if (odooId) {
            console.log(`[Odoo Sync] Actualizando equipo existente en Odoo (ID: ${odooId})`);
            await state.odoo.write('maintenance.equipment', odooId, odooMachine);
        } else {
            console.log(`[Odoo Sync] Creando nuevo equipo en Odoo...`);
            const createdId = await state.odoo.create('maintenance.equipment', odooMachine);
            odooId = createdId;
            console.log(`[Odoo Sync] Nuevo equipo creado en Odoo con ID: ${odooId}`);
        }

        // Guardar el odoo_id en Firestore si es nuevo o ha cambiado
        if (odooId) {
            console.log(`[Odoo Sync] Actualizando marca de tiempo de sincronización para ${machineData.id}`);
            const syncTime = new Date().toISOString();
            await updateDoc(doc(state.collections.machines, machineData.fb_id), {
                odoo_id: odooId,
                lastOdooSync: syncTime
            });
            // Actualizar también en el estado local para evitar re-sincronizaciones innecesarias
            const localMachine = state.machines.find(m => m.fb_id === machineData.fb_id);
            if (localMachine) {
                localMachine.odoo_id = odooId;
                localMachine.lastOdooSync = syncTime;
            }
        }

        // Bridge to RTDB for immediate UI feedback on machine status
        if (machineData.id) {
            rtdbUpdate(rtdbRef(state.rtdb, `artifacts/${appId}/monitoring/${machineData.id}`), {
                lastOdooSync: new Date().toISOString()
                // liveStatus will be updated by RTDB sensors, but we can nudge it here if Odoo had a status
            });
        }

        return odooId;
    } catch (e) {
        console.error("[Odoo Sync] Error sincronizando máquina:", e);
        throw e; // Lanzamos para que el llamador lo maneje
    }
}

async function getOrCreateOdooEquipmentId(machineId) {
    if (!state.odoo || !machineId) return null;

    let machine = state.machines.find(m => m.id === machineId);

    if (!machine) {
        try {
            const machineDoc = await getDoc(doc(state.collections.machines, machineId));
            if (machineDoc.exists()) {
                machine = { id: machineDoc.id, ...machineDoc.data() };
            }
        } catch (e) {
            console.warn("[Odoo Sync] No se pudo obtener la máquina de Firestore:", e);
        }
    }

    if (!machine) {
        console.warn(`[Odoo Sync] No se encontró información para la máquina ${machineId}`);
        return null;
    }

    return await syncMachineToOdoo(machine);
}

async function syncTechnicianToOdoo(techData, fbId) {
    if (!state.odoo || !techData) return;
    try {
        const odooUser = {
            name: techData.username,
            login: techData.username + '@corinfar.com' // Odoo requiere un login (email)
        };

        let odooId = null;
        const existingUser = await state.odoo.searchRead('res.users', [['name', '=', techData.username]], ['id']);
        if (existingUser && existingUser.length > 0) {
            odooId = existingUser[0].id;
            console.log(`[Odoo Sync] Usuario encontrado en Odoo (ID: ${odooId}). Actualizando...`);
            await state.odoo.write('res.users', odooId, odooUser);
        } else {
            console.log(`[Odoo Sync] Usuario "${techData.username}" no encontrado en Odoo para sincronización.`);
        }

        if (odooId) {
            await updateDoc(doc(state.collections.technicians, fbId), { odoo_id: odooId });
            const localTech = state.technicians.find(t => t.fb_id === fbId);
            if (localTech) localTech.odoo_id = odooId;
        }
    } catch (odooError) {
        console.warn("[Odoo Sync] Fallo sincronización de usuario:", odooError);
    }
}

/**
 * Sincroniza el nivel de stockActual de un repuesto con Odoo (para productos 'storable')
 */
async function syncPartStockToOdoo(partId, partData = null) {
    if (!state.odoo) return;

    // Usar partData proporcionado o buscar en el estado local por ID
    const part = partData || (state.parts && state.parts.find(p => p.id === partId));

    if (!part) {
        console.warn(`[Odoo Sync] No se pudo encontrar datos para el repuesto ${partId} para sincronizar stockActual.`);
        return;
    }

    try {
        // 1. Asegurarse de que el producto existe en Odoo y sincronizar metadata (Nombre, Precio, Tipo)
        // Esto se hace tanto para Repuestos como para Insumos.
        const odooProductId = await syncPartToOdoo(part);

        // 2. Solo sincronizar stockActual para repuestos (storable/product), no para insumos (consu)
        if (part.classification === 'insumo') {
            console.log(`[Odoo Sync] El ítem ${partId} es un insumo (consumible), solo se sincronizó metadata en Odoo.`);
            return;
        }

        console.log(`[Odoo Sync] Sincronizando stockActual para repuesto: ${partId} (Cantidad: ${part.stockActual})`);
        if (!odooProductId) {
            console.warn(`[Odoo Sync] No se pudo obtener o crear el producto en Odoo para el repuesto ${partId}`);
            return;
        }

        // Determinar ubicación en Odoo
        let odooLocationId = null;
        if (part.location && part.location.trim()) {
            const locName = part.location.trim();
            // Buscar por nombre o por nombre completo (display_name)
            const locations = await state.odoo.searchRead('stock.location', [
                '|', ['name', '=', locName], ['display_name', '=', locName],
                ['usage', '=', 'internal']
            ], ['id']);
            if (locations && locations.length > 0) {
                odooLocationId = locations[0].id;
            }
        }

        // Fallback a ubicación estándar si no se encuentra o no tiene definida
        if (!odooLocationId) {
            // Intentamos buscar una ubicación llamada 'Stock' que sea interna
            const stockLocs = await state.odoo.searchRead('stock.location', [['name', '=', 'Stock'], ['usage', '=', 'internal']], ['id']);
            if (stockLocs && stockLocs.length > 0) {
                odooLocationId = stockLocs[0].id;
            } else {
                // Si aún no hay, buscamos cualquier ubicación interna
                const anyInternal = await state.odoo.searchRead('stock.location', [['usage', '=', 'internal']], ['id'], { limit: 1 });
                if (anyInternal && anyInternal.length > 0) odooLocationId = anyInternal[0].id;
            }
        }

        if (!odooLocationId) {
            console.warn(`[Odoo Sync] No se encontró una ubicación interna válida en Odoo para sincronizar stockActual de ${partId}`);
            return;
        }

        const currentStock = Number(part.stockActual) || 0;

        // En Odoo 17+, se usa stock.quant para ajustar el stockActual disponible (Quantity on Hand)
        // Buscamos si ya existe un quant para este producto y ubicación
        const quants = await state.odoo.searchRead('stock.quant', [
            ['product_id', '=', odooProductId],
            ['location_id', '=', odooLocationId]
        ], ['id', 'quantity']);

        if (quants && quants.length > 0) {
            const quantId = quants[0].id;
            console.log(`[Odoo Sync] Actualizando stockActual en quant existente ID ${quantId} para ${partId}`);
            // Establecemos la cantidad inventariada y aplicamos
            await state.odoo.write('stock.quant', quantId, {
                inventory_quantity: currentStock
            });
            await state.odoo.executeKw('stock.quant', 'action_apply_inventory', [[quantId]]);
        } else {
            console.log(`[Odoo Sync] Creando nuevo quant en Odoo para ${partId} en ubicación ${odooLocationId}`);
            const newQuantId = await state.odoo.create('stock.quant', {
                product_id: odooProductId,
                location_id: odooLocationId,
                inventory_quantity: currentStock
            });
            await state.odoo.executeKw('stock.quant', 'action_apply_inventory', [[newQuantId]]);
        }

        // Bridge to RTDB for immediate UI feedback on high-priority stockActual change
        if (currentStock <= (Number(part.minStock) || 0)) {
             rtdbUpdate(rtdbRef(state.rtdb, `artifacts/${appId}/notifications/critical_stock`), {
                 [part.id]: {
                     name: part.name,
                     stockActual: currentStock,
                     timestamp: new Date().toISOString()
                 }
             });
        }

    } catch (error) {
        console.error(`[Odoo Sync] Error sincronizando stockActual para repuesto ${partId}:`, error);
        state.odooSyncError = `Error stockActual ${partId}: ${error.message || error}`;
        if (typeof renderParts === 'function') renderParts();
    }
}

async function syncSolicitudToOdoo(solicitudData, fbId, nextId) {
    if (!state.odoo || !solicitudData) return;
    try {
        const machine = state.machines.find(m => m.id === solicitudData.machineId) || { name: solicitudData.machineId };
        const equipmentId = await getOrCreateOdooEquipmentId(solicitudData.machineId);
        const odooData = {
            name: `Fallo en ${machine.name}: ${solicitudData.description.substring(0, 30)}...`,
            description: `${solicitudData.description} (Ref App: ${nextId})`,
            maintenance_type: 'corrective',
            equipment_id: equipmentId
        };

        console.log(`[Odoo Sync] Enviando solicitud a Odoo: ${odooData.name}`);
        const newOdooId = await state.odoo.create('maintenance.request', odooData);
        if (newOdooId) {
            await updateDoc(doc(state.collections.solicitudes, fbId), { odoo_id: newOdooId });
            const localSol = state.solicitudes.find(s => s.fb_id === fbId);
            if (localSol) localSol.odoo_id = newOdooId;
        }
    } catch (odooError) {
        console.warn("[Odoo Sync] Fallo sincronización de solicitud:", odooError);
        showToast(`Error sincronización Odoo: ${odooError.message}`, 'warning');
    }
}

async function syncWorkOrderToOdoo(orderData, fbId) {
    if (!state.odoo || !orderData) return;

    try {
        const machine = state.machines.find(m => m.id === orderData.machineId) || { name: orderData.machineId };
        const equipmentId = await getOrCreateOdooEquipmentId(orderData.machineId);

        // Buscar ID de Odoo del técnico responsable
        let technicianOdooId = null;
        const leadTech = state.technicians.find(t => t.username === orderData.leadTechnician);
        if (leadTech && leadTech.odoo_id) {
            technicianOdooId = leadTech.odoo_id;
        } else if (orderData.leadTechnician) {
            console.log(`[Odoo Sync] Buscando ID de Odoo para el técnico: ${orderData.leadTechnician}`);
            const techResults = await state.odoo.searchRead('res.users', [['name', '=', orderData.leadTechnician]], ['id']);
            if (techResults && techResults.length > 0) {
                technicianOdooId = techResults[0].id;
                if (leadTech) leadTech.odoo_id = technicianOdooId;
            }
        }

        const odooData = {
            name: `${orderData.id}: ${machine.name}`,
            description: orderData.description,
            maintenance_type: orderData.type.toLowerCase() === 'preventivo' ? 'preventive' : 'corrective',
            priority: orderData.priority === 'Alta' ? '3' : (orderData.priority === 'Media' ? '2' : '1'),
            equipment_id: equipmentId
        };

        if (technicianOdooId) {
            odooData.user_id = technicianOdooId;
        }

        // Si ya tiene un ID de Odoo guardado, lo actualizamos, si no, creamos uno nuevo
        if (orderData.odoo_id) {
            await state.odoo.write('maintenance.request', orderData.odoo_id, odooData);
        } else {
            const newOdooId = await state.odoo.create('maintenance.request', odooData);
            if (newOdooId) {
                await updateDoc(doc(state.collections.workOrders, fbId), { odoo_id: newOdooId });
                // Actualizar en el estado local
                const localWo = state.workOrders.find(wo => wo.fb_id === fbId);
                if (localWo) localWo.odoo_id = newOdooId;
            }
        }
        console.log(`[Odoo Sync] Orden ${orderData.id} sincronizada correctamente.`);
    } catch (odooError) {
        console.warn(`[Odoo Sync] Fallo sincronización de OT ${orderData.id}:`, odooError);
        showToast(`Error sincronización Odoo (OT ${orderData.id}): ${odooError.message}`, 'warning');
    }
}

async function saveWorkOrder(updates = {}) {
    showLoading(true);
    try {
        const fbId = document.getElementById('wo-fb-id-hidden').value;
        if (fbId) {
            const order = state.workOrders.find(o => o.fb_id === fbId);
            if (order && order.isExpired) {
                showToast('Esta orden ha expirado y no puede ser modificada.', 'error');
                showLoading(false);
                return;
            }
        }
        const isNew = !fbId;
        const orderId = document.getElementById('wo-id').value;

        const partsUsed = [];
        document.querySelectorAll('#wo-parts-list > div').forEach(row => {
            partsUsed.push({
                partId: row.dataset.partId,
                quantity: parseInt(row.dataset.quantity),
                delivered: row.dataset.delivered !== 'false'
            });
        });

        const leadTechnician = document.getElementById('wo-lead-technician-select').value;
        const supportTechnicians = [];
        document.querySelectorAll('#wo-support-technicians-list .assigned-technician-badge').forEach(badge => {
            supportTechnicians.push(badge.dataset.username);
        });

        const sourceSolicitudId = document.getElementById('source-solicitud-id-hidden').value;
        const solicitud = sourceSolicitudId ? state.solicitudes.find(s => s.fb_id === sourceSolicitudId) : null;

        const startDate = document.getElementById('wo-start-date').value;
        const startTime = document.getElementById('wo-start-time').value;
        const endDate = document.getElementById('wo-end-date').value;
        const endTime = document.getElementById('wo-end-time').value;

        let startTimeISO = null;
        if (startDate && startTime) {
            startTimeISO = new Date(`${startDate}T${startTime}`).toISOString();
        } else if (startDate) {
            startTimeISO = new Date(startDate).toISOString();
        }

        let endTimeISO = null;
        if (endDate && endTime) {
            endTimeISO = new Date(`${endDate}T${endTime}`).toISOString();
        } else if (endDate) {
            endTimeISO = new Date(endDate).toISOString();
        }

        if (startTimeISO && endTimeISO && new Date(endTimeISO) <= new Date(startTimeISO)) {
            showToast('La fecha/hora final debe ser posterior a la fecha/hora de inicio.', 'error');
            showLoading(false);
            return;
        }

        const linkedPlanId = document.getElementById('wo-work-plan').value || null;

        // Final validation to prevent duplicate plan assignments in the same month
        if (linkedPlanId && startDate) {
            const targetDate = new Date(startDate + 'T12:00:00Z');
            const targetMonth = targetDate.getUTCMonth();
            const targetYear = targetDate.getUTCFullYear();

            const conflictingOrder = state.workOrders.find(wo => {
                // Exclude the current work order from the check
                if (fbId && wo.fb_id === fbId) return false;

                if (!wo.linkedPlanId || wo.linkedPlanId !== linkedPlanId || !wo.date) return false;

                const woDate = new Date(wo.date + 'T12:00:00Z');
                return woDate.getUTCMonth() === targetMonth && woDate.getUTCFullYear() === targetYear;
            });

            if (conflictingOrder) {
                showToast(`Error: Este plan ya está asignado a la orden ${conflictingOrder.id} en este mes.`, 'error');
                showLoading(false);
                return;
            }
        }

        const existingOrder = isNew ? {} : state.workOrders.find(wo => wo.fb_id === fbId) || {};

        const formData = {
            id: isNew ? '' : orderId,
            machineId: document.getElementById('wo-machine').value,
            description: document.getElementById('wo-description').value,
            observaciones: document.getElementById('wo-observaciones').value || "",
        images: state.currentWoImages,
            status: document.getElementById('wo-status').value,
            type: document.getElementById('wo-type').value,
            date: startDate, // Main date for calendar
            startTime: startTimeISO,
            endTime: endTimeISO,
            requester: document.getElementById('wo-requester').value,
            leadTechnician: leadTechnician,
            supportTechnicians: supportTechnicians,
            technicians: [...new Set([leadTechnician, ...supportTechnicians].filter(Boolean))],
            partsUsed: partsUsed,
            failureType: ['Correctivo', 'Emergencia'].includes(document.getElementById('wo-type').value)
                ? document.getElementById('wo-failure-type').value 
                : null,
            maintenanceType: ['Preventivo', 'Predictivo'].includes(document.getElementById('wo-type').value)
                ? document.getElementById('wo-maintenance-type').value
                : null,
            priority: document.getElementById('wo-priority').value || 'Media',
            solicitudId: solicitud ? solicitud.id : (existingOrder.solicitudId || null),
            sourceSolicitudId: solicitud ? solicitud.fb_id : (existingOrder.sourceSolicitudId || null),
            linkedPlanId: linkedPlanId,
            externalCost: parseFloat(document.getElementById('wo-external-cost').value) || 0,
        };

        if (['Correctivo', 'Emergencia'].includes(formData.type) && !formData.failureType) {
            showToast('Para órdenes correctivas o de emergencia, es obligatorio seleccionar un tipo de falla.', 'error');
            showLoading(false);
            return;
        }

        if (['Preventivo', 'Predictivo'].includes(formData.type) && !formData.maintenanceType) {
            showToast('Para órdenes preventivas o predictivas, es obligatorio seleccionar un tipo de mantenimiento.', 'error');
            showLoading(false);
            return;
        }

        if (!formData.leadTechnician && formData.status !== 'Cancelado') {
            showToast('Debe asignar un técnico responsable para guardar la orden.', 'error');
            showLoading(false);
            return;
        }
        
        const orderData = { ...existingOrder, ...formData, ...updates };

        // Ensure originalDate is preserved
        if (!orderData.originalDate && orderData.date) {
            orderData.originalDate = orderData.date;
        }

        const oldStatus = existingOrder.status;
        let newStatus = orderData.status;

        if (newStatus === 'Pausado' && oldStatus !== 'Pausado' && !updates.machineStatusOnPause) {
            const machineStatusOnPause = await requestMachineStatusOnPause(orderData.type);
            orderData.machineStatusOnPause = machineStatusOnPause;
            showLoading(true); // Restaurar el cargador para el proceso de guardado
        }

        // 21 CFR Part 11: Re-autenticación para firma electrónica al completar/evaluar o validar
        const isFinishing = (newStatus === 'Pendiente de Evaluación' || newStatus === 'Completado' || newStatus === 'Pendiente de Aprobación');
        const isValidating = !existingOrder.validatedBy && orderData.validatedBy;
        if ((isFinishing && oldStatus !== newStatus) || isValidating) {
            const confirmed = await new Promise(resolve => {
                requestSignature(() => resolve(true), () => resolve(false));
            });
            if (!confirmed) {
                showLoading(false);
                return;
            }
            showLoading(true);
        }

        // Si es de plan o viene de una solicitud y el técnico intenta completar sin validación, cambiar a Pendiente de Aprobación
        const isPlanOrder = ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(orderData.type) || orderData.linkedPlanId;
        const hasRequest = orderData.solicitudId || orderData.sourceSolicitudId;
        if (newStatus === 'Completado' && (isPlanOrder || hasRequest) && !orderData.validatedBy) {
            newStatus = 'Pendiente de Aprobación';
            orderData.status = 'Pendiente de Aprobación';
            orderData.technicianFinished = true;
            showToast('Trabajo finalizado. Pendiente de validación por el Planificador.', 'info');
        }

        if (isWorkOrderFinishStatus(newStatus)) {
            const oldOrderForStock = isWorkOrderFinishStatus(oldStatus) ? existingOrder : { partsUsed: [] };
            const canFinishWithInventory = await enforceWorkOrderInventoryBeforeFinish(orderData, oldOrderForStock);
            if (!canFinishWithInventory) {
                showLoading(false);
                return;
            }
        }

        // Strict blocking: Don't allow starting if there are pending parts
        if (newStatus === 'En Proceso' && oldStatus !== 'En Proceso') {
            const hasPendingParts = (orderData.partsUsed || []).some(p => !p.delivered);
            const pendingRequests = state.partRequests.filter(r =>
                r.workOrderFbId === fbId &&
                ['Pendiente', 'Pedido', 'Aprobado', 'En Despacho'].includes(r.status)
            );

            if (hasPendingParts || pendingRequests.length > 0) {
                showToast('No puede iniciar la ejecución sin haber recibido todos los repuestos/insumos. Recíbalos o elimínelos de la orden.', 'error');
                showLoading(false);
                return;
            }
        }

        // Track extension if started or paused in its original month
        if ((newStatus === 'Pausado' || newStatus === 'En Proceso') && (oldStatus !== 'Pausado' && oldStatus !== 'En Proceso')) {
            const scheduledDate = new Date((orderData.originalDate || orderData.date) + 'T12:00:00');
            const now = new Date();
            if (now.getMonth() === scheduledDate.getMonth() && now.getFullYear() === scheduledDate.getFullYear()) {
                orderData.hadPauseExtension = true;
            }
        }

        // Solo generar nuevo ID si está iniciando por primera vez (cambio de MP a MA o no tiene ID)
        const isPlanned = !orderData.id || orderData.id.startsWith('MP');
        const isFirstStart = newStatus === 'En Proceso' && oldStatus !== 'En Proceso' && isPlanned;

        if (isFirstStart) {
            const prefix = orderData.type === 'Mecanizado' ? 'ME' : 'MA';
            orderData.id = generateNextId(prefix); // Assign active ID

            // Filtrar repuestos sin stockActual al iniciar la orden
            let removedParts = [];
            let adjustedParts = [];
            const processedParts = [];

            for (const item of (orderData.partsUsed || [])) {
                const part = state.parts.find(p => p.id === item.partId);
                const stock = part ? (part.stockActual || 0) : 0;

                if (stock <= 0) {
                    removedParts.push(part ? part.description : item.partId);
                } else {
                    let finalQty = item.quantity;
                    if (stock < item.quantity) {
                        finalQty = stock;
                        adjustedParts.push(`${part ? part.description : item.partId} (ajustado de ${item.quantity} a ${stock})`);
                    }
                    processedParts.push({ ...item, quantity: finalQty });
                }
            }

            if (removedParts.length > 0 || adjustedParts.length > 0) {
                orderData.partsUsed = processedParts;
                let note = "\n\nNota del Sistema (Inicio de OT):";
                if (removedParts.length > 0) {
                    note += `\n- Se eliminaron por falta de stockActual: ${removedParts.join(', ')}.`;
                }
                if (adjustedParts.length > 0) {
                    note += `\n- Se ajustó cantidad por stockActual insuficiente stockActual: ${adjustedParts.join(', ')}.`;
                }
                orderData.observaciones = (orderData.observaciones || "") + note;
                const obsTextarea = document.getElementById('wo-observaciones');
                if (obsTextarea) obsTextarea.value = orderData.observaciones;
            }
        } else if (isNew) {
            orderData.id = generateNextId('MP'); // Assign planned ID
        }

        if (newStatus && newStatus !== oldStatus) {
            let workIntervals = Array.isArray(orderData.workIntervals) ? JSON.parse(JSON.stringify(orderData.workIntervals)) : [];
            const now = new Date();

            if (isFirstStart) {
                orderData.fechaInicioReal = now.toISOString();
                // Actualizar la fecha principal de la orden a la fecha local actual para que se mueva en el planificador
                orderData.date = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`;
            }

            if (newStatus === 'En Proceso') {
                 workIntervals.push({ start: now.toISOString(), end: null });
                 // Al reanudar, borramos el estado de equipo pausado para que vuelva a estar en mantenimiento/parado
                 delete orderData.machineStatusOnPause;
            } else if (['Pausado', 'Pendiente de Evaluación', 'Completado', 'Pendiente de Aprobación'].includes(newStatus) && oldStatus === 'En Proceso') {
                const lastInterval = workIntervals.find(i => !i.end);
                if (lastInterval) {
                    lastInterval.end = now.toISOString();
                }
            }
            orderData.workIntervals = workIntervals;

            if (newStatus === 'Pendiente de Evaluación' || newStatus === 'Completado' || newStatus === 'Pendiente de Aprobación') {
                if (!orderData.fechaFinalizacionReal) {
                    orderData.fechaFinalizacionReal = now.toISOString();
                }
                const totalMs = getActiveWorkDurationMs({ ...orderData, status: newStatus }); // Pass new status to calculate correctly
                orderData.totalWorkDurationMs = totalMs;
            }
        }

        // Stock validation only happens when trying to complete the order.
        if (orderData.status === 'Pendiente de Evaluación' || orderData.status === 'Completado' || orderData.status === 'Pendiente de Aprobación') {
            if (isNew) {
                // For a new order, all parts are new deductions.
                const newOrderForStock = { ...orderData, partsUsed: orderData.partsUsed || [] };
                await updateStockForOrder({ partsUsed: [] }, newOrderForStock);
            } else {
                // For an existing order, calculate the delta of parts used.
                await updateStockForOrder(existingOrder, orderData);
            }
        }

        let currentFbId = fbId;
        if (isNew) {
            orderData.createdAt = new Date().toISOString();
            const docRef = await addDoc(state.collections.workOrders, orderData);
            document.getElementById('wo-fb-id-hidden').value = docRef.id;
            document.getElementById('wo-id').value = orderData.id;
            currentFbId = docRef.id;
            await updateRTDBMirror('workOrders', orderData, docRef.id);
            await recordAuditLog('workOrders', orderData.id, 'CREATE', null, orderData, 'Creación OT');

            // Notificación Telegram para nuevas OTs
            if (orderData.status !== 'Cancelado') {
                const machine = state.machines.find(m => m.id === orderData.machineId);
                const tgMsg = `🛠 <b>Nueva Orden de Trabajo</b>\n\n` +
                              `<b>ID:</b> ${orderData.id}\n` +
                              `<b>Equipo:</b> ${escapeHTML(machine ? machine.name : orderData.machineId)}\n` +
                              `<b>Tipo:</b> ${orderData.type}\n` +
                              `<b>Responsable:</b> ${escapeHTML(orderData.leadTechnician)}\n` +
                              `<b>Prioridad:</b> ${orderData.priority}` +
                              (sourceSolicitudId ? `\n<b>Origen:</b> Solicitud ${orderData.solicitudId}` : '');

                // Notificar a administradores, planificadores y grupo global
                notifyAdminsAndPlanners(tgMsg).catch(err => console.warn("[Telegram WO Error]", err));

                // Notificaciones personales para los técnicos asignados (si no son admin/planificador ya notificados)
                const techniciansToNotify = [orderData.leadTechnician, ...(orderData.technicians || [])];
                notifyTechnicians(techniciansToNotify, tgMsg);
            }
        } else {
            const docRef = doc(state.collections.workOrders, fbId);
            await setDoc(docRef, orderData, { merge: true });
            await updateRTDBMirror('workOrders', orderData, fbId);
            const detailedAction = getWorkOrderDetailedAction(existingOrder, orderData) + (isFinishing ? ' (Firmado)' : '');
            await recordAuditLog('workOrders', orderData.id, 'UPDATE', existingOrder, orderData, detailedAction);

            // Notificación Telegram si cambió el técnico o si se requiere re-notificar
            const oldTechnicians = [existingOrder.leadTechnician, ...(existingOrder.technicians || [])].filter(u => u);
            const newTechnicians = [orderData.leadTechnician, ...(orderData.technicians || [])].filter(u => u);

            // Si hay técnicos nuevos asignados, notificarles
            const addedTechnicians = newTechnicians.filter(u => !oldTechnicians.includes(u));
            if (addedTechnicians.length > 0) {
                const machine = state.machines.find(m => m.id === orderData.machineId);
                const tgMsg = `🛠 <b>Reasignación de OT</b>\n\n` +
                              `Se han asignado nuevos técnicos a la orden:\n\n` +
                              `<b>ID:</b> ${orderData.id}\n` +
                              `<b>Equipo:</b> ${escapeHTML(machine ? machine.name : orderData.machineId)}\n` +
                              `<b>Tipo:</b> ${orderData.type}\n` +
                              `<b>Responsable:</b> ${escapeHTML(orderData.leadTechnician)}\n` +
                              `<b>Prioridad:</b> ${orderData.priority}\n\n` +
                              `<b>Técnicos agregados:</b> ${addedTechnicians.join(', ')}`;

                // Notificar al grupo global y a los administradores del cambio
                notifyAdminsAndPlanners(tgMsg).catch(err => console.warn("[Telegram Notification Error]", err));

                // Notificar personalmente a los técnicos recién agregados
                notifyTechnicians(addedTechnicians, tgMsg);
            }
        }

        // --- Sincronización con Odoo (Segundo Plano) ---
        if (state.odoo) {
            syncWorkOrderToOdoo(orderData, currentFbId).catch(err => {
                console.warn("[Odoo Background Sync] Error:", err);
            });
        }

        // If finishing the order, also finish any linked plan execution
        if (orderData.status === 'Pendiente de Evaluación' || orderData.status === 'Completado' || orderData.status === 'Pendiente de Aprobación') {
            await completeLinkedPlanExecution(currentFbId, new Date().toISOString(), orderData.status);
        }
        
        if (sourceSolicitudId) {
            const solicitud = state.solicitudes.find(s => s.fb_id === sourceSolicitudId);
            if (solicitud) {
                const solUpdates = {
                    workOrderId: orderData.id,
                    workOrderFbId: currentFbId
                };
                // Solo cambiar estado a Aprobado si estaba Pendiente
                if (solicitud.status === 'Pendiente') {
                    solUpdates.status = 'Aprobado';
                }
                await updateDoc(doc(state.collections.solicitudes, sourceSolicitudId), solUpdates);
            }
        }

        // Notificación de Validación (Si cambió a estado de validación)
        const valStatus = ['Pendiente de Evaluación', 'Pendiente de Aprobación'];
        if (valStatus.includes(orderData.status) && orderData.status !== oldStatus) {
            const machine = state.machines.find(m => m.id === orderData.machineId);
            const valMsg = `✅ <b>OT Lista para Validación</b>\n\n` +
                          `La orden ${orderData.id} ha sido finalizada y requiere su revisión.\n\n` +
                          `<b>Equipo:</b> ${escapeHTML(machine ? machine.name : orderData.machineId)}\n` +
                          `<b>Responsable:</b> ${escapeHTML(orderData.leadTechnician)}\n` +
                          `<b>Estado:</b> ${orderData.status}`;

            notifyAdminsAndPlanners(valMsg).catch(err => console.warn("[Telegram Validation Notification Error]", err));
        }

        showToast('Orden de trabajo guardada.', 'success');
        state.modals.workOrder.hide();

    } catch (error) {
        console.error("Error saving work order:", error);
        showToast(error.message || 'Error al guardar la orden de trabajo.', 'error');
    } finally {
        showLoading(false);
    }
}

function populateWorkPlanSelector(machineId, workOrderDate, currentPlanId = null) {
    const planSelect = document.getElementById('wo-work-plan');
    if (!planSelect) return;

    planSelect.innerHTML = '<option value="">No vincular ningún plan</option>';

    if (!machineId || !workOrderDate) {
        return; // No machine or date selected yet
    }

    const plansForMachine = state.workPlans.filter(p => p.machineId === machineId);

    const targetMonthYear = workOrderDate.substring(0, 7); // YYYY-MM format

    const usedPlanIds = new Set(
        state.workOrders
            .filter(wo => {
                // Ensure the work order has a linked plan and a date that starts with the same YYYY-MM string.
                return wo.linkedPlanId && wo.date && wo.date.startsWith(targetMonthYear);
            })
            .map(wo => wo.linkedPlanId)
    );

    plansForMachine.forEach(plan => {
        // The plan is available if it's not used OR if it's the plan currently linked to the order being edited.
        if (!usedPlanIds.has(plan.fb_id) || plan.fb_id === currentPlanId) {
            const option = document.createElement('option');
            option.value = plan.fb_id;
            option.textContent = plan.name;
            planSelect.appendChild(option);
        }
    });
}


async function getOptimizedImageUrlWithRetry(originalUrl) {
    if (!originalUrl || !originalUrl.includes('firebasestorage.googleapis.com')) return originalUrl;
    if (originalUrl.includes('_1024x1024.webp')) return originalUrl;

    try {
        const decodedUrl = decodeURIComponent(originalUrl);
        const pathMatch = decodedUrl.match(/\/o\/(.+?)\?/);
        if (!pathMatch) return originalUrl;

        const originalPath = pathMatch[1];
        const lastDotIndex = originalPath.lastIndexOf('.');
        const pathWithoutExtension = lastDotIndex !== -1 ? originalPath.substring(0, lastDotIndex) : originalPath;
        const optimizedPath = `${pathWithoutExtension}_1024x1024.webp`;
        const optimizedRef = ref(state.storage, optimizedPath);

        if (optimizedImageUrlCache.has(optimizedPath)) return optimizedImageUrlCache.get(optimizedPath);
        if (missingOptimizedImagePaths.has(optimizedPath)) return originalUrl;

        const maxRetries = 1;
        const delay = 800;

        for (let i = 0; i < maxRetries; i++) {
            try {
                const url = await getDownloadURL(optimizedRef);
                optimizedImageUrlCache.set(optimizedPath, url);
                return url;
            } catch (error) {
                if (error.code === 'storage/object-not-found') {
                    if (i < maxRetries - 1) {
                        await new Promise(resolve => setTimeout(resolve, delay));
                    }
                } else {
                    console.warn("[Storage] No se pudo obtener URL optimizada, usando original:", error.code || error.message || error);
                    break;
                }
            }
        }
        missingOptimizedImagePaths.add(optimizedPath);
    } catch (e) {
        console.warn("[Storage] Error al procesar URL optimizada:", e);
    }
    return originalUrl;
}

async function imageUrlToBase64(url, useRetry = false) {
    if (!url) return null;

    let targetUrl = url;
    if (useRetry && url.includes('firebasestorage.googleapis.com') && !url.includes('_1024x1024.webp')) {
        targetUrl = await getOptimizedImageUrlWithRetry(url);
    }

    const attemptLoad = (src) => {
        return new Promise((resolve) => {
            const img = new Image();
            img.crossOrigin = 'Anonymous';
            let timeout = setTimeout(() => {
                img.src = "";
                resolve(null);
            }, 12000);

            img.onload = () => {
                clearTimeout(timeout);
                try {
                    const canvas = document.createElement("canvas");
                    canvas.width = img.width;
                    canvas.height = img.height;
                    const ctx = canvas.getContext("2d");
                    ctx.drawImage(img, 0, 0);
                    resolve({
                        data: canvas.toDataURL("image/jpeg", 0.95), // Higher quality for audit reports
                        width: img.width,
                        height: img.height
                    });
                } catch (e) {
                    console.error("Canvas error:", e);
                    resolve(null);
                }
            };
            img.onerror = () => {
                clearTimeout(timeout);
                resolve(null);
            };
            img.src = src;
        });
    };

    // Optimization: For known cloud storage URLs (Firebase, Google), the proxy is often necessary
    // to avoid CORS errors and console noise.
    const needsProxy = targetUrl.includes('firebasestorage.googleapis.com') || targetUrl.includes('googleusercontent.com');

    if (needsProxy) {
        // Priority 1: Wsrv.nl Proxy (Modern, fast, and handles CORS very well)
        const wsrvProxy = `https://wsrv.nl/url=${encodeURIComponent(targetUrl)}&output=jpg&q=80`;
        let result = await attemptLoad(wsrvProxy);
        if (result) return result;

    }

    // Priority 2 / Direct: Direct Load (Works for local files or properly configured CORS)
    const directResult = await attemptLoad(targetUrl);
    if (directResult) return directResult;

    return null;
}

async function handleWoImageUpload(e) {
    const file = e.target.files[0];
    if (!file) return;

    if (state.currentWoImages.length >= 5) {
        showToast('Límite de 5 imágenes alcanzado.', 'warning');
        return;
    }

    showLoading(true);
    try {
        const storageRef = ref(state.storage, `artifacts/${appId}/workOrders/${Date.now()}_${file.name}`);
        const snapshot = await uploadBytes(storageRef, file);
        const url = await getOptimizedStorageUrl(snapshot.ref);

        state.currentWoImages.push({
            url: url,
            caption: ''
        });

        renderWoImages();
        showToast('Imagen subida correctamente.', 'success');
    } catch (error) {
        console.error("Error uploading image:", error);
        showToast('Error al subir la imagen.', 'error');
    } finally {
        showLoading(false);
        e.target.value = '';
    }
}

function renderWoImages() {
    const container = document.getElementById('wo-images-container');
    container.innerHTML = '';

    state.currentWoImages.forEach((img, idx) => {
        const div = document.createElement('div');
        div.className = 'wo-image-item';
        div.innerHTML = `
            <div class="wo-image-preview-wrapper">
                <button type="button" class="wo-image-remove-btn" onclick="removeWoImage(${idx})">
                    <i class="fas fa-times"></i>
                </button>
                <img src="${img.url}" alt="Actividad ${idx + 1}" onclick="window.open('${img.url}', '_blank')">
            </div>
            <input type="text" class="wo-image-caption-input" placeholder="Descripción..."
                value="${img.caption || ''}" onchange="updateWoImageCaption(${idx}, this.value)">
        `;
        container.appendChild(div);
    });

    // Hide add button if limit reached
    document.getElementById('wo-add-image-btn').style.display = state.currentWoImages.length >= 5 ? 'none' : 'inline-block';
}

function removeWoImage(index) {
    state.currentWoImages.splice(index, 1);
    renderWoImages();
}

function updateWoImageCaption(index, value) {
    state.currentWoImages[index].caption = value;
}

window.handleWoImageUpload = handleWoImageUpload;
window.removeWoImage = removeWoImage;
window.updateWoImageCaption = updateWoImageCaption;

async function handleWorkOrderStart() {
    const fbId = document.getElementById('wo-fb-id-hidden').value;
    const partsUsedRows = document.querySelectorAll('#wo-parts-list > div');

    // Si no hay FB ID, guardamos primero como pendiente (saveWorkOrder lo hará si detecta que es nuevo)
    if (!fbId) {
        await saveWorkOrder();
    }

    // Obtenemos el fbId actualizado (podría haberse generado en el paso anterior)
    const currentFbId = document.getElementById('wo-fb-id-hidden').value;
    if (!currentFbId) return; // Si falló el guardado inicial

    // Verificar si ya hay solicitudes pendientes para esta OT
    const pendingRequests = state.partRequests.filter(r =>
        r.workOrderFbId === currentFbId &&
        ['Pendiente', 'Pedido', 'Aprobado', 'En Despacho'].includes(r.status)
    );

    if (pendingRequests.length > 0) {
        showToast('Ya existe una solicitud de repuestos en proceso para esta orden. Debe recibirlos en el almacén para poder iniciar.', 'warning');
        return;
    }

    const partsToRequest = [];
    partsUsedRows.forEach(row => {
        const delivered = row.dataset.delivered !== 'false';
        if (!delivered) {
            const partId = row.dataset.partId;
            const quantity = parseInt(row.dataset.quantity);
            const part = state.parts.find(p => p.id === partId);
            partsToRequest.push({
                partId: partId,
                fb_id: part ? part.fb_id : null,
                description: part ? part.name : 'Desconocido',
                quantity: quantity
            });
        }
    });

    if (partsToRequest.length > 0) {
        const listContainer = document.getElementById('auto-part-request-list');
        listContainer.innerHTML = '';
        partsToRequest.forEach((item, index) => {
            const tr = document.createElement('tr');
            tr.innerHTML = `
                <td>${item.description}</td>
                <td>
                    <input type="number" class="form-control form-control-sm auto-req-qty"
                           data-index="${index}" value="${item.quantity}" min="1">
                </td>
                <td>
                    <button type="button" class="btn btn-outline-danger btn-sm remove-auto-req-item">
                        <i class="fas fa-trash"></i>
                    </button>
                </td>
            `;
            tr.dataset.partId = item.partId;
            tr.dataset.fbId = item.fb_id;
            tr.dataset.description = item.description;
            listContainer.appendChild(tr);
        });

        // Event listener for removal
        listContainer.querySelectorAll('.remove-auto-req-item').forEach(btn => {
            btn.addEventListener('click', (e) => {
                e.target.closest('tr').remove();
                const remaining = document.querySelectorAll('#auto-part-request-list tr').length;
                const justification = document.getElementById('auto-part-justification-container');
                if (remaining === 0) {
                    justification.classList.remove('d-none');
                }
            });
        });

        // Reset justification
        document.getElementById('auto-part-justification-container').classList.add('d-none');
        document.getElementById('auto-part-request-justification').value = '';

        state.modals.autoPartRequest.show();
    } else {
        // No hay repuestos pendientes de entrega, iniciar directamente
        saveWorkOrder({ status: 'En Proceso' });
    }
}

async function submitAutomaticPartRequest() {
    const listRows = document.querySelectorAll('#auto-part-request-list tr');
    const items = [];
    listRows.forEach(row => {
        const qtyInput = row.querySelector('.auto-req-qty');
        items.push({
            partId: row.dataset.fbId, // Usamos el fbId para la solicitud
            description: row.dataset.description,
            quantity: parseInt(qtyInput.value),
            deliveredQuantity: 0,
            status: 'Pendiente'
        });
    });

    const justificationText = document.getElementById('auto-part-request-justification').value;

    if (items.length === 0) {
        if (!justificationText.trim()) {
            showToast('Debe proporcionar una justificación si elimina todos los repuestos.', 'warning');
            return;
        }

        // Si no hay items, pedimos firma para la justificación
        const confirmed = await new Promise(resolve => {
            requestSignature(() => resolve(true), () => resolve(false));
        });
        if (!confirmed) return;

        showLoading(true, 'Registrando inicio con justificación...');
        const woFbId = document.getElementById('wo-fb-id-hidden').value;
        const woId = document.getElementById('wo-id').value;
        const obsField = document.getElementById('wo-observaciones');
        const currentObs = obsField.value;
        const newObs = (currentObs ? currentObs + '\n' : '') + `[Justificación Inicio sin Repuestos]: ${justificationText} (Firmado por ${getCurrentUser().username})`;

        await saveWorkOrder({
            status: 'En Proceso',
            observaciones: newObs,
            partsUsed: [] // Limpiar repuestos ya que se eliminaron todos
        });
        state.modals.autoPartRequest.hide();
        return;
    }

    const urgency = document.getElementById('auto-part-request-urgency').value;
    const woFbId = document.getElementById('wo-fb-id-hidden').value;
    const woId = document.getElementById('wo-id').value;
    const machineId = document.getElementById('wo-machine').value;

    showLoading(true, 'Generando solicitud de materiales...');
    try {
        const requestData = {
            id: generateNextPartRequestId(),
            date: new Date().toISOString(),
            workOrderFbId: woFbId,
            workOrderId: woId,
            machineId: machineId,
            requester: getCurrentUser().username || 'Desconocido',
            items: items,
            urgency: urgency,
            status: 'Pendiente'
        };
        const docRef = await addDoc(state.collections.partRequests, requestData);
        await updateRTDBMirror('partRequests', requestData, docRef.id);
        await recordAuditLog('partRequests', woId || docRef.id, 'CREATE', null, requestData, 'Solicitud Automática al Iniciar OT');

        state.modals.autoPartRequest.hide();
        showToast('Solicitud de repuestos enviada. El almacén debe procesarla y usted debe confirmar la recepción para iniciar la orden.', 'info');

        // NO iniciamos la orden automáticamente si hay pedido pendiente
        await saveWorkOrder(); // Solo guardar cambios si hubo ajustes de cantidad
    } catch (error) {
        console.error("Error submitting auto part request:", error);
        showToast('Error al enviar la solicitud automática.', 'error');
        showLoading(false);
    }
}

async function showWorkOrderModal(identifier = null, type = 'Preventivo', sourceSolicitud = null) {
    cleanupPopovers();
    const form = document.getElementById('work-order-form');
    form.reset();
    document.getElementById('wo-fb-id-hidden').value = '';
    document.getElementById('wo-parts-list').innerHTML = '';
    document.getElementById('wo-support-technicians-list').innerHTML = '';
    document.getElementById('source-solicitud-id-hidden').value = '';
    state.currentWoImages = [];
    renderWoImages();

    // Hide checklist and show observations by default
    document.getElementById('wo-checklist-group').classList.add('d-none');
    document.getElementById('wo-observaciones-group').classList.remove('d-none');

    document.getElementById('wo-start-date').value = '';
    document.getElementById('wo-start-time').value = '';
    document.getElementById('wo-end-date').value = '';
    document.getElementById('wo-end-time').value = '';

    const idInput = document.getElementById('wo-id');
    const saveBtn = document.getElementById('wo-save-btn');
    const addPartBtn = document.getElementById('wo-add-part-btn');
    const addSupportTechBtn = document.getElementById('wo-add-support-technician-btn');
    const printBtn = document.getElementById('print-work-order-modal-btn');
    if (printBtn) printBtn.style.display = 'none';

    const allFields = document.querySelectorAll('#work-order-form input, #work-order-form select, #work-order-form textarea');
    allFields.forEach(field => field.disabled = false);
    saveBtn.style.display = 'inline-block';
    addPartBtn.disabled = false;
    addSupportTechBtn.disabled = false;
    const startPlanBtnStatic = document.getElementById('wo-start-plan-btn');
    if (startPlanBtnStatic) startPlanBtnStatic.disabled = false;
    
    const leadTechSelect = document.getElementById('wo-lead-technician-select');
    const supportTechSelect = document.getElementById('wo-support-technician-select');
    let leadHtml = '<option value="">Seleccione responsable...</option>';
    let supportHtml = '<option value="">Seleccione apoyo...</option>';
    state.technicians.forEach(t => {
        if(['Técnico', 'Admin', 'Planificador'].includes(t?.role) && t?.isActive !== false) {
            leadHtml += `<option value="${t.username}">${t.username}</option>`;
            supportHtml += `<option value="${t.username}">${t.username}</option>`;
        }
    });
    leadTechSelect.innerHTML = leadHtml;
    supportTechSelect.innerHTML = supportHtml;

    const partSelect = document.getElementById('wo-part-select');
    let partHtml = '<option value="">Seleccione repuesto...</option>';
    state.parts.forEach(p => partHtml += `<option value="${p.id}">${p.description} (Stock: ${p.stockActual})</option>`);
    partSelect.innerHTML = partHtml;
    
    const statusSelect = document.getElementById('wo-status');
    statusSelect.innerHTML = ['Pendiente', 'En Proceso', 'Pausado', 'Pendiente de Aprobación', 'Pendiente de Evaluación', 'Completado', 'Cancelado'].map(s => `<option>${s}</option>`).join('');
    
    let order = null;
    if (identifier) {
        order = state.workOrders.find(o => o.fb_id === identifier || o.id === identifier);
        const expiredText = order.isExpired ? ' <span class="badge bg-dark text-white ms-2" style="font-size: 0.8rem;"><i class="fas fa-hourglass-end me-1"></i>EXPIRADA</span>' : '';
        document.getElementById('work-order-modal-title').innerHTML = `Detalles de OT: ${order ? (order.id || 'Pendiente') : 'Nueva'}${expiredText}`;
        idInput.readOnly = true;
        if (order) {
            if (printBtn && order.id) {
                printBtn.style.display = 'inline-block';
                printBtn.onclick = () => generateSingleWorkOrderReport(order.id);
            }
            populateWorkOrderMachineSelector();
            document.getElementById('work-order-id-hidden').value = order.id;
            document.getElementById('wo-fb-id-hidden').value = order.fb_id;
            idInput.value = order.id || 'ID pendiente';
            document.getElementById('wo-machine').value = order.machineId;

            const machine = state.machines.find(m => m.id === order.machineId);
            if (machine) {
                document.getElementById('wo-machine-search').value = machine.name;
            }

            document.getElementById('wo-description').value = order.description;
            document.getElementById('wo-observaciones').value = order.observaciones || "";
            state.currentWoImages = order.images || [];
            renderWoImages();
            document.getElementById('wo-status').value = order.status;
            document.getElementById('wo-type').value = order.type;
            document.getElementById('wo-priority').value = order.priority || 'Media';
            
            const plannedStartTime = order.startTime ? new Date(order.startTime) : (order.date ? new Date(order.date) : null);
            if (plannedStartTime) {
                document.getElementById('wo-start-date').value = plannedStartTime.toISOString().split('T')[0];
                document.getElementById('wo-start-time').value = plannedStartTime.toTimeString().split(' ')[0].substring(0, 5);
            }

            if (order.endTime) {
                const endDate = new Date(order.endTime);
                document.getElementById('wo-end-date').value = endDate.toISOString().split('T')[0];
                document.getElementById('wo-end-time').value = endDate.toTimeString().split(' ')[0].substring(0, 5);
            }

            // Prioritize real dates for display if they exist
            const realStartTimestamp = order.fechaInicioReal || order.start;
            if (realStartTimestamp) {
                const realStartDate = new Date(realStartTimestamp);
                document.getElementById('wo-start-date').value = realStartDate.toISOString().split('T')[0];
                document.getElementById('wo-start-time').value = realStartDate.toTimeString().split(' ')[0].substring(0, 5);
                 document.getElementById('wo-real-start-date').value = realStartDate.toISOString().split('T')[0];
                 document.getElementById('wo-real-start-time').value = realStartDate.toTimeString().split(' ')[0].substring(0, 5);
            }

            const realEndTimestamp = order.fechaFinalizacionReal || order.end;
            if (realEndTimestamp) {
                const realEndDate = new Date(realEndTimestamp);
                document.getElementById('wo-end-date').value = realEndDate.toISOString().split('T')[0];
                document.getElementById('wo-end-time').value = realEndDate.toTimeString().split(' ')[0].substring(0, 5);
                 document.getElementById('wo-real-end-date').value = realEndDate.toISOString().split('T')[0];
                 document.getElementById('wo-real-end-time').value = realEndDate.toTimeString().split(' ')[0].substring(0, 5);
            }

            const durationMs = order.totalWorkDurationMs || getActiveWorkDurationMs(order);
            if (durationMs > 0) {
                const hours = Math.floor(durationMs / (1000 * 60 * 60));
                const minutes = Math.floor((durationMs % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((durationMs % (1000 * 60)) / 1000);
                document.getElementById('wo-total-duration').value = `${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`;
            } else {
                document.getElementById('wo-total-duration').value = '00:00:00';
            }
            
            const solicitudRefId = order.solicitudId || order.sourceSolicitudId;
            const solicitudRef = solicitudRefId ? state.solicitudes.find(s => s.id === solicitudRefId || s.fb_id === solicitudRefId) : null;
            const requesterLabel = document.querySelector('label[for="wo-requester"]');
            if (requesterLabel) {
                requesterLabel.innerHTML = solicitudRef ? `Solicitante <small class="text-info fw-bold">(Solicitud: ${solicitudRef.id})</small>` : 'Solicitante';
            }
            if (solicitudRef) {
                document.getElementById('source-solicitud-id-hidden').value = solicitudRef.fb_id;
            }
            document.getElementById('wo-requester').value = order.requester;
            document.getElementById('wo-external-cost').value = order.externalCost || 0;
            document.getElementById('wo-failure-type').value = order.failureType || '';
            document.getElementById('wo-maintenance-type').value = order.maintenanceType || '';
            
            const lead = order.leadTechnician || (order.technicians && order.technicians[0]) || '';
            const support = order.supportTechnicians || (order.technicians && order.technicians.slice(1)) || [];
            
            leadTechSelect.value = lead;
            support.forEach(techUsername => addSupportTechnicianToWorkOrderUI(techUsername));

            if(order.partsUsed) {
                order.partsUsed.forEach(part => addPartToWorkOrder(part.partId, part.quantity, true, part.delivered));
            }
            renderWoPendingRequests();

            // Populate and set the work plan selector
            const initialDate = document.getElementById('wo-start-date').value;
            populateWorkPlanSelector(order.machineId, initialDate, order.linkedPlanId);
            if (order.linkedPlanId) {
                document.getElementById('wo-work-plan').value = order.linkedPlanId;
            }

            // If the work order is linked to a plan, show the plan info and action button
            if (order.linkedPlanId) {
                const plan = state.workPlans.find(p => p.fb_id === order.linkedPlanId);
                if (plan) {
                    document.getElementById('wo-observaciones-group').classList.add('d-none');
                    const checklistGroup = document.getElementById('wo-checklist-group');
                    checklistGroup.classList.remove('d-none');

                    document.getElementById('wo-plan-name').textContent = plan.name;
                    const startPlanBtn = document.getElementById('wo-start-plan-btn');

                    const isLeadTechnician = getCurrentUser().username === order.leadTechnician;
                    const isEditable = !['Completado', 'Cancelado', 'Rechazado'].includes(order.status) && !order.isExpired;
                    const canEdit = isLeadTechnician && isEditable;

                    const existingExecution = state.workPlanExecutions.find(e => e.workOrderFbId === order.fb_id);

                    if (canEdit) {
                        if (existingExecution) {
                            startPlanBtn.innerHTML = '<i class="fas fa-play me-2"></i>Continuar Plan';
                        } else {
                            startPlanBtn.innerHTML = '<i class="fas fa-play me-2"></i>Iniciar Plan';
                        }
                        startPlanBtn.className = 'btn btn-primary';
                    } else {
                        startPlanBtn.innerHTML = '<i class="fas fa-eye me-2"></i>Ver Plan';
                        startPlanBtn.className = 'btn btn-info';
                    }

                    // Clone and replace the button to remove any old event listeners
                    const newBtn = startPlanBtn.cloneNode(true);
                    startPlanBtn.parentNode.replaceChild(newBtn, startPlanBtn);

                    // Add the new event listener
                    newBtn.addEventListener('click', () => {
                        if (canEdit) {
                            handleStartLinkedPlan(order, plan);
                        } else {
                            handleViewLinkedPlan(order, plan);
                        }
                    });
                }
            }
        }
    } else {
        populateWorkOrderMachineSelector();
        populateWorkPlanSelector(null, null); // Clear it for new orders
        document.getElementById('work-order-modal-title').textContent = sourceSolicitud ? `Nueva OT desde Solicitud` : `Nueva Orden ${type}`;
        document.getElementById('work-order-id-hidden').value = '';
        idInput.value = 'ID pendiente';
        idInput.readOnly = true;
        document.getElementById('wo-type').value = type;
        document.getElementById('wo-priority').value = 'Media';
        document.getElementById('wo-start-date').value = new Date().toISOString().split('T')[0];
        const requesterLabel = document.querySelector('label[for="wo-requester"]');
        if (requesterLabel) {
            requesterLabel.innerHTML = sourceSolicitud ? `Solicitante <small class="text-info fw-bold">(Solicitud: ${sourceSolicitud.id})</small>` : 'Solicitante';
        }
        document.getElementById('wo-requester').value = sourceSolicitud ? sourceSolicitud.requester : getCurrentUser().username;
        document.getElementById('wo-status').value = 'Pendiente';

        if (sourceSolicitud) {
            const isFromMonitoring = typeof sourceSolicitud === 'string';

            if (isFromMonitoring) {
                document.getElementById('wo-machine').value = sourceSolicitud;
                const machine = state.machines.find(m => m.id === sourceSolicitud);
                if (machine) {
                    document.getElementById('wo-machine-search').value = machine.name;
                }
            } else {
                document.getElementById('source-solicitud-id-hidden').value = sourceSolicitud.fb_id;
                document.getElementById('wo-machine').value = sourceSolicitud.machineId;
                document.getElementById('wo-description').value = sourceSolicitud.description;

                const machine = state.machines.find(m => m.id === sourceSolicitud.machineId);
                if (machine) {
                    document.getElementById('wo-machine-search').value = machine.name;
                }
            }

            if (getCurrentUser().role === 'Técnico') {
                leadTechSelect.value = getCurrentUser().username;
                leadTechSelect.disabled = true;
                addSupportTechBtn.disabled = true;
                document.getElementById('wo-status').value = 'En Proceso';
            }
        }
    }
    
    const userRole = getCurrentUser().role;
    const isLeadTechnician = order ? getCurrentUser().username === order.leadTechnician : false;
    const canControl = userRole === 'Admin' || userRole === 'Planificador' || isLeadTechnician || !identifier;
    const canExecute = userRole === 'Admin' || userRole === 'Planificador' || isLeadTechnician;

    // An order is read-only if it's finished, cancelled or expired
    const isFinished = order.status === 'Completado' || order.status === 'Cancelado' || order.status === 'Rechazado' || order.status === 'Pendiente de Evaluación' || order.status === 'Pendiente de Aprobación' || order.isExpired;

    if (!canControl || getCurrentUser().role === 'Invitado' || isFinished) {
        allFields.forEach(field => field.disabled = true);
        saveBtn.style.display = 'none';
        addPartBtn.disabled = true;
        addSupportTechBtn.disabled = true;
        document.querySelectorAll('#wo-parts-list button, #wo-support-technicians-list button').forEach(btn => btn.disabled = true);
    }

    // Restringir botón de inicio de plan a Admin o Técnico Líder
    const startPlanBtn = document.getElementById('wo-start-plan-btn');
    if (startPlanBtn) {
        startPlanBtn.disabled = !canExecute || isFinished;
    }

    const machineSelector = document.getElementById('wo-machine');
    const dateSelector = document.getElementById('wo-start-date');

    // Remove any existing listeners to prevent duplicates
    const selectedMachineId = machineSelector.value; // Store the current value
    const newMachineSelector = machineSelector.cloneNode(true);
    machineSelector.parentNode.replaceChild(newMachineSelector, machineSelector);
    newMachineSelector.value = selectedMachineId; // Restore the value

    const newDateSelector = dateSelector.cloneNode(true);
    dateSelector.parentNode.replaceChild(newDateSelector, dateSelector);

    const updatePlansListener = () => {
        const machineId = newMachineSelector.value;
        const date = newDateSelector.value;
        const currentPlanId = order ? order.linkedPlanId : null;
        populateWorkPlanSelector(machineId, date, currentPlanId);
    };

    newMachineSelector.addEventListener('change', updatePlansListener);
    newDateSelector.addEventListener('change', updatePlansListener);

    handleWorkOrderTypeChange();
    updateWorkOrderModalButtons(order ? order.status : 'Pendiente');
    state.modals.workOrder.show();
}

function handleWorkOrderTypeChange() {
    const type = document.getElementById('wo-type').value;
    const failureGroup = document.getElementById('wo-failure-type-group');
    const maintenanceGroup = document.getElementById('wo-maintenance-type-group');
    failureGroup.classList.toggle('d-none', !['Correctivo', 'Emergencia'].includes(type));
    maintenanceGroup.classList.toggle('d-none', !['Preventivo', 'Predictivo'].includes(type));
}

function generateNextId(prefix) {
    const currentYear = new Date().getFullYear().toString().slice(-2);
    const fullPrefix = `${prefix}-${currentYear}-`;

    const relevantOrders = state.workOrders
        .filter(wo => wo.id && wo.id.startsWith(fullPrefix))
        .map(wo => parseInt(wo.id.split('-')[2]))
        .filter(num => !isNaN(num));

    const maxNumber = relevantOrders.length > 0 ? Math.max(...relevantOrders) : 0;
    const nextNumber = maxNumber + 1;
    const nextSequence = nextNumber.toString().padStart(4, '0');

    return fullPrefix + nextSequence;
}

function generateNextPartRequestId(dateStr = null) {
    const date = dateStr ? new Date(dateStr) : new Date();
    const currentYear = date.getFullYear().toString().slice(-2);
    const fullPrefix = `REQ-${currentYear}-`;

    const relevantRequests = state.partRequests
        .filter(r => r.id && r.id.startsWith(fullPrefix))
        .map(r => parseInt(r.id.split('-')[2]))
        .filter(num => !isNaN(num));

    const maxNumber = relevantRequests.length > 0 ? Math.max(...relevantRequests) : 0;
    const nextNumber = maxNumber + 1;
    const nextSequence = nextNumber.toString().padStart(4, '0');

    return fullPrefix + nextSequence;
}

async function migratePartRequestIds() {
    const requestsWithoutId = state.partRequests.filter(r => !r.id);
    if (requestsWithoutId.length === 0) return;

    console.log(`Migrating ${requestsWithoutId.length} part requests...`);

    // Agrupar por año
    const byYear = {};
    requestsWithoutId.sort((a, b) => new Date(a.date) - new Date(b.date));

    for (const r of requestsWithoutId) {
        const year = new Date(r.date).getFullYear().toString().slice(-2);
        if (!byYear[year]) byYear[year] = [];
        byYear[year].push(r);
    }

    for (const year in byYear) {
        const fullPrefix = `REQ-${year}-`;

        // Buscar el máximo número actual para ese año (incluyendo los que ya tienen ID)
        const relevantRequests = state.partRequests
            .filter(r => r.id && r.id.startsWith(fullPrefix))
            .map(r => parseInt(r.id.split('-')[2]))
            .filter(num => !isNaN(num));

        let maxNumber = relevantRequests.length > 0 ? Math.max(...relevantRequests) : 0;

        for (const r of byYear[year]) {
            maxNumber++;
            const nextSequence = maxNumber.toString().padStart(4, '0');
            const newId = fullPrefix + nextSequence;

            try {
                await updateDoc(doc(state.collections.partRequests, r.fb_id), { id: newId });
                r.id = newId; // Update local state immediately
                await updateRTDBMirror('partRequests', r, r.fb_id);
            } catch (err) {
                console.error(`Error migrating request ${r.fb_id}:`, err);
            }
        }
    }
}

function addPartToWorkOrder(partId, quantity, isInitialLoad = false, delivered = true) {
    const partSelect = document.getElementById('wo-part-select');
    const qtyInput = document.getElementById('wo-part-quantity');

    const selectedPartId = isInitialLoad ? partId : partSelect.value;
    const selectedQty = isInitialLoad ? quantity : parseInt(qtyInput.value);

    if (!selectedPartId || isNaN(selectedQty) || selectedQty < 1) return;

    const part = state.parts.find(p => p.id === selectedPartId);
    if (!part) return;

    const list = document.getElementById('wo-parts-list');
    
    if (!isInitialLoad) {
        const existingRow = list.querySelector(`[data-part-id="${selectedPartId}"]`);
        if (existingRow) {
            showToast('Este repuesto ya ha sido añadido.', 'warning');
            return;
        }
    }

    const partRow = document.createElement('div');
    partRow.className = 'd-flex justify-content-between align-items-center border rounded p-2 mb-1';
    partRow.style.backgroundColor = 'var(--content-bg)';
    partRow.dataset.partId = part.id;
    partRow.dataset.quantity = selectedQty;
    partRow.dataset.delivered = (delivered !== false);

    let deliveredBadge = '';
    if (delivered === false) {
        deliveredBadge = '<span class="badge bg-danger me-2"><i class="fas fa-exclamation-triangle me-1"></i>No entregado (Sin stock)</span>';
        partRow.classList.add('border-danger');
        partRow.style.backgroundColor = '#fff5f5';
    }

    const canReturn = delivered !== false && ['En Proceso', 'Pausado'].includes(document.getElementById('wo-status').value);

    partRow.innerHTML = `
        <span>${part.description}</span>
        <div class="d-flex align-items-center">
            ${deliveredBadge}
            <span class="badge bg-secondary me-2">Cant: ${selectedQty}</span>
            ${canReturn ? `<button type="button" class="btn btn-xs btn-outline-warning me-1" onclick="handlePartReturn('${part.id}', ${selectedQty}, '${part.description.replace(/'/g, "\\'")}')" title="Devolver a Almacén"><i class="fas fa-undo"></i> Devolver</button>` : ''}
            <button type="button" class="btn btn-sm btn-outline-danger" onclick="removePartFromWorkOrder('${part.id}', '${part.description.replace(/'/g, "\\'")}')"><i class="fas fa-times"></i></button>
        </div>
    `;
    list.appendChild(partRow);

    if (!isInitialLoad) {
        partSelect.value = '';
        qtyInput.value = 1;
    }
}

function updateWorkOrderModalButtons(status) {
    const fbId = document.getElementById('wo-fb-id-hidden').value;
    const order = state.workOrders.find(wo => wo.fb_id === fbId);

    const startBtn = document.getElementById('wo-start-btn');
    const pauseBtn = document.getElementById('wo-pause-btn');
    const resumeBtn = document.getElementById('wo-resume-btn');
    const completeBtn = document.getElementById('wo-complete-btn');
    const validateBtn = document.getElementById('wo-validate-btn');
    const rejectBtn = document.getElementById('wo-reject-btn');

    startBtn.style.display = 'none';
    pauseBtn.style.display = 'none';
    resumeBtn.style.display = 'none';
    completeBtn.style.display = 'none';
    validateBtn.style.display = 'none';
    rejectBtn.style.display = 'none';

    // If it's a new order (no order object yet), we can only decide based on the passed status.
    // The only relevant status for a new order is 'Pendiente'.
    if (!order) {
        if (status === 'Pendiente') {
            startBtn.style.display = 'inline-block';
        }
        return;
    }

    // For existing orders, check permissions.
    const isLeadTechnician = getCurrentUser().username === order.leadTechnician;
    const isPlanificador = getCurrentUser().role === 'Planificador' || getCurrentUser().role === 'Admin';
    const isPlanOrder = ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(order.type) || order.linkedPlanId;
    const canControl = getCurrentUser().role === 'Admin' || getCurrentUser().role === 'Planificador' || isLeadTechnician;

    if (canControl) {
        switch (status) {
            case 'Pendiente' :
                startBtn.style.display = 'inline-block';
                break;
            case 'En Proceso' :
                pauseBtn.style.display = 'inline-block';
                completeBtn.style.display = 'inline-block';
                if (isPlanOrder) {
                    completeBtn.innerHTML = '<i class="fas fa-check me-2"></i>Finalizar Trabajo';
                } else {
                    completeBtn.innerHTML = '<i class="fas fa-check me-2"></i>Completar';
                }
                break;
            case 'Pausado' :
                resumeBtn.style.display = 'inline-block';
                completeBtn.style.display = 'inline-block';
                if (isPlanOrder) {
                    completeBtn.innerHTML = '<i class="fas fa-check me-2"></i>Finalizar Trabajo';
                } else {
                    completeBtn.innerHTML = '<i class="fas fa-check me-2"></i>Completar';
                }
                break;
        }
    }

    if (isPlanificador) {
        const hasRequest = order.solicitudId || order.sourceSolicitudId;
        const needsValidation = (isPlanOrder || hasRequest) && !order.validatedBy;

        if (status === 'Pendiente de Aprobación' || (needsValidation && ['Pendiente de Evaluación', 'En Proceso', 'Pausado'].includes(status))) {
            validateBtn.style.display = 'inline-block';
            rejectBtn.style.display = 'inline-block';
        }
    }
}

function handleAddSupportTechnicianClick() {
    const techSelect = document.getElementById('wo-support-technician-select');
    const username = techSelect.value;
    if (!username) {
        showToast('Por favor, seleccione un técnico de apoyo.', 'warning');
        return;
    }
    const leadUsername = document.getElementById('wo-lead-technician-select').value;
    if (username === leadUsername) {
        showToast('Este técnico ya es el responsable de la orden.', 'warning');
        return;
    }
    addSupportTechnicianToWorkOrderUI(username);
    techSelect.value = '';
}

function addSupportTechnicianToWorkOrderUI(username) {
    const listContainer = document.getElementById('wo-support-technicians-list');
    const existing = listContainer.querySelector(`[data-username="${username}"]`);
    if (existing) {
        showToast(`El técnico ${username} ya está en la lista de apoyo.`, 'warning');
        return;
    }

    const badge = document.createElement('span');
    badge.className = 'assigned-technician-badge';
    badge.dataset.username = username;
    badge.innerHTML = `
        ${username}
        <button type="button" class="btn-remove-tech"><i class="fas fa-times"></i></button>
    `;
    
    badge.querySelector('.btn-remove-tech').addEventListener('click', (e) => {
        e.currentTarget.closest('.assigned-technician-badge').remove();
    });

    listContainer.appendChild(badge);
}

function handleLeadTechnicianChange(e) {
    const leadUsername = e.target.value;
    if (!leadUsername) return;
    
    const supportList = document.getElementById('wo-support-technicians-list');
    const existingSupportBadge = supportList.querySelector(`[data-username="${leadUsername}"]`);
    if (existingSupportBadge) {
        existingSupportBadge.remove();
        showToast(`${leadUsername} ha sido movido a técnico responsable.`, 'info');
    }
}

// --- Reports Functions ---
function generateGeneralReport() {
    showLoading(true);
    setTimeout(() => {
        const { jsPDF } = window.jspdf;
        const doc = new jsPDF();

        doc.text("Reporte General de Inventario", 14, 20);
        doc.setFontSize(10);
        doc.text(`Fecha de generación: ${new Date().toLocaleDateString('es-ES')}`, 14, 26);
        
        doc.autoTable({
            startY: 35,
            head: [['ID', 'Nombre', 'Ubicación', 'Área', 'Criticidad']],
            body: state.machines.map(m => [m.id, m.name, m.location, getMachineArea(m) || 'N/A', m.criticidad || 'Baja']),
            headStyles: { fillColor: [44, 62, 80] },
            didDrawPage: (data) => {
                doc.setFontSize(16);
                doc.text("Listado de Máquinas", 14, 30);
            }
        });

        const finalY = doc.autoTable.previous.finalY;
        doc.setFontSize(16);
        doc.text("Inventario de Repuestos", 14, finalY + 20);

        doc.autoTable({
            startY: finalY + 25,
            head: [['ID', 'Descripción', 'Stock', 'Costo Unitario']],
            body: state.parts.map(p => [p.id, p.description, p.stockActual, formatCurrency(p.cost)]),
             headStyles: { fillColor: [44, 62, 80] },
        });

        doc.save("Reporte_General_Inventario.pdf");
        showToast('Reporte generado correctamente', 'success');
        showLoading(false);
    }, 1000);
}

function generateWorkOrderReport() {
    showLoading(true);
    setTimeout(() => {
        const { jsPDF } = window.jspdf;
        const doc = new jsPDF();
        
        doc.text("Historial de Órdenes de Trabajo", 14, 20);
        doc.setFontSize(10);
        doc.text(`Fecha de generación: ${new Date().toLocaleDateString('es-ES')}`, 14, 26);

        const body = state.workOrders.map(o => {
            const machine = state.machines.find(m => m.id === o.machineId) || { name: 'N/A' };
            return [
                o.id,
                o.type,
                machine.name,
                o.status,
                new Date(o.date).toLocaleDateString('es-ES'),
                o.leadTechnician || 'N/A',
                o.supportTechnicians.join(', ') || ''
            ];
        });

        doc.autoTable({
            startY: 35,
            head: [['ID', 'Tipo', 'Máquina', 'Estado', 'Fecha', 'Responsable', 'Apoyo']],
            body: body,
            headStyles: { fillColor: [44, 62, 80] },
            columnStyles: {
                2: { cellWidth: 40 }, 
                6: { cellWidth: 'auto'}
            }
        });

        doc.save("Reporte_Ordenes_de_Trabajo.pdf");
        showToast('Reporte generado correctamente', 'success');
        showLoading(false);
    }, 1000);
}

async function generateMachineReport() {
    const machineId = document.getElementById('report-machine-select').value;
    if (!machineId) {
        showToast('Seleccione una máquina para generar el reporte', 'warning');
        return;
    }
    
    showLoading(true);

    try {
        const machine = state.machines.find(m => m.id === machineId);
        if (!machine) {
            showToast('Máquina no encontrada.', 'error');
            showLoading(false);
            return;
        }
        const machineWorkOrders = state.workOrders.filter(o => o.machineId === machineId);

        // --- KPI Calculations ---
        const correctiveTypes = ['Correctivo', 'Emergencia'];
        const correctiveOrdersForKpi = machineWorkOrders
            .filter(o => correctiveTypes.includes(o.type))
            .sort((a, b) => new Date(a.createdAt || a.date) - new Date(b.createdAt || b.date));
        
        let mtbf = 'N/A';
        const now = new Date();
        const dailyUptimeMs = getMachineDailyUptimeMs(machine);

        // Dynamic counter: Uptime since last failure
        const lastFailure = state.workOrders
            .filter(o => o.machineId === machineId && correctiveTypes.includes(o.type) && o.status !== 'Cancelado' && o.status !== 'Rechazado')
            .sort((a, b) => new Date(b.createdAt || b.date) - new Date(a.createdAt || a.date))[0];

        const calcStart = lastFailure ? new Date(lastFailure.createdAt || lastFailure.date) : (machine.createdAt ? new Date(machine.createdAt) : null);

        if (calcStart) {
            const uptimeMs = getPreciseUptimeMs(machine, calcStart, now);
            const mtbfVal = dailyUptimeMs > 0 ? uptimeMs / dailyUptimeMs : 0;
            mtbf = `${mtbfVal.toFixed(2)} días`;
        }

        let mttr = 'N/A';
        const repairOrdersForKpi = correctiveOrdersForKpi.filter(o => o.status === 'Completado');
        if (repairOrdersForKpi.length > 0) {
            const totalRepairTimeMs = repairOrdersForKpi.reduce((sum, o) => sum + getTotalWorkDurationMs(o), 0);
            const mttrMs = totalRepairTimeMs / repairOrdersForKpi.length;
            mttr = `${parseFloat((mttrMs / (1000 * 60 * 60)).toFixed(2))} horas`;
        }

        // --- Chart Generation ---
        const chartContainer = document.createElement('div');
        chartContainer.style.position = 'absolute';
        chartContainer.style.left = '-9999px';
        chartContainer.style.width = '1000px';
        chartContainer.style.height = '400px';
        document.body.appendChild(chartContainer);

        const effectivenessCanvas = document.createElement('canvas');
        effectivenessCanvas.width = 300; effectivenessCanvas.height = 300;
        const failureTrendCanvas = document.createElement('canvas');
        failureTrendCanvas.width = 450; failureTrendCanvas.height = 300;
        chartContainer.appendChild(effectivenessCanvas);
        chartContainer.appendChild(failureTrendCanvas);

        const preventiveCount = machineWorkOrders.filter(o => ['Preventivo', 'Predictivo', 'Calibración', 'Mecanizado'].includes(o.type)).length;
        const correctiveCount = machineWorkOrders.filter(o => ['Correctivo', 'Emergencia'].includes(o.type)).length;

        const effectivenessChartPromise = new Promise((resolve) => {
             new Chart(effectivenessCanvas, {
                type: 'doughnut',
                data: {
                    labels: ['Preventivos', 'Correctivos'],
                    datasets: [{
                        data: [preventiveCount, correctiveCount],
                        backgroundColor: ['#3498db', '#e74c3c']
                    }]
                },
                options: {
                    responsive: false,
                    animation: { onComplete: () => resolve(effectivenessCanvas.toDataURL('image/png')) },
                    plugins: {
                        legend: { position: 'right' },
                        title: { display: true, text: 'Efectividad de Mantenimientos', font: { size: 14 } }
                    }
                }
            });
        });

        const failureTypes = ['Mecánica', 'Eléctrica', 'Electrónica', 'Falla de Operación'];
        const failureCounts = failureTypes.map(type => 
            correctiveOrdersForKpi.filter(o => o.failureType === type).length
        );
        
        const failureTrendChartPromise = new Promise((resolve) => {
            new Chart(failureTrendCanvas, {
                type: 'bar',
                data: {
                    labels: failureTypes,
                    datasets: [{
                        label: 'Cantidad de Fallas',
                        data: failureCounts,
                        backgroundColor: '#8e44ad',
                    }]
                },
                options: {
                    responsive: false,
                    animation: { onComplete: () => resolve(failureTrendCanvas.toDataURL('image/png')) },
                    plugins: {
                        legend: { display: false },
                        title: { display: true, text: 'Tendencia de Fallas Comunes', font: { size: 14 } }
                    },
                     scales: { y: { beginAtZero: true, ticks: { stepSize: 1 } } }
                }
            });
        });
        
        const [effectivenessChartImg, failureTrendChartImg] = await Promise.all([effectivenessChartPromise, failureTrendChartPromise]);

        // --- PDF Generation ---
        const { jsPDF } = window.jspdf;
        const doc = new jsPDF();
        
        doc.setFontSize(18);
        doc.text(`Reporte Histórico de Máquina: ${machine.name}`, 14, 22);
        doc.setFontSize(12);
        doc.text(`ID: ${machine.id} | Ubicación: ${machine.location || 'N/A'} | Área: ${getMachineArea(machine) || 'N/A'} | Criticidad: ${machine.criticidad || 'Baja'}`, 14, 30);
        
        // Add charts side-by-side
        doc.addImage(effectivenessChartImg, 'PNG', 14, 40, 70, 70);
        doc.addImage(failureTrendChartImg, 'PNG', 100, 40, 95, 60);

        const workOrderBody = machineWorkOrders.map(o => [
            o.id, o.type, ['Correctivo', 'Emergencia', 'Calibración'].includes(o.type) ? o.failureType || 'N/A' : 'N/A',
            o.status, new Date(o.date).toLocaleDateString('es-ES'), o.description
        ]);
        
        doc.autoTable({
            startY: 115,
            head: [['ID Orden', 'Tipo', 'Tipo Falla', 'Estado', 'Fecha', 'Descripción']],
            body: workOrderBody,
            headStyles: { fillColor: [44, 62, 80] },
            columnStyles: { 5: { cellWidth: 'auto' } }
        });

        let finalY = doc.autoTable.previous.finalY;

        // --- KPIs, Availability, and Costs ---
        doc.addPage();
        doc.setFontSize(16);
        doc.text('Análisis Detallado', 14, 22);

        // KPI Table
        doc.setFontSize(12);
        doc.text('Indicadores Clave (Mantenimiento Correctivo)', 14, 32);
        doc.autoTable({
            startY: 35,
            body: [
                ['Tiempo Medio Entre Fallos (MTBF):', mtbf],
                ['Tiempo Medio de Reparación (MTTR):', mttr],
            ],
            theme: 'plain',
            styles: { fontSize: 10 }
        });
        finalY = doc.autoTable.previous.finalY;

        // Availability Table
        const availabilityBody = [];
        const processedDates = new Set();
        const completedCorrectives = machineWorkOrders.filter(o => ['Correctivo', 'Emergencia', 'Calibración'].includes(o.type) && o.status === 'Completado' && o.date);
        
        completedCorrectives.forEach(o => {
            if (processedDates.has(o.date)) return;
            
            const dayDate = new Date(o.date + 'T12:00:00Z');
            const startDate = new Date(Date.UTC(dayDate.getUTCFullYear(), dayDate.getUTCMonth(), dayDate.getUTCDate(), 0, 0, 0));
            const endDate = new Date(Date.UTC(dayDate.getUTCFullYear(), dayDate.getUTCMonth(), dayDate.getUTCDate(), 23, 59, 59));
            const scheduledUptimeMs = calculateScheduledUptimeMs([machine], startDate, endDate);
            
            const downtimeOrdersThisDay = completedCorrectives.filter(wo => wo.date === o.date);
            const totalDowntimeMs = downtimeOrdersThisDay.reduce((sum, wo) => sum + getTotalWorkDurationMs(wo), 0);

            let availabilityText = 'N/A';
            if (scheduledUptimeMs > 0) {
                const availability = ((scheduledUptimeMs - totalDowntimeMs) / scheduledUptimeMs) * 100;
                availabilityText = `${Math.max(0, availability).toFixed(2)}%`;
            }
            availabilityBody.push([new Date(o.date + 'T12:00:00Z').toLocaleDateString('es-ES'), availabilityText]);
            processedDates.add(o.date);
        });

        if (availabilityBody.length > 0) {
            doc.text('Análisis de Disponibilidad por Día de Falla Correctiva', 14, finalY + 10);
            doc.autoTable({
                startY: finalY + 13,
                head: [['Fecha de Reparación', 'Disponibilidad del Día']],
                body: availabilityBody,
                headStyles: { fillColor: [44, 62, 80] },
            });
            finalY = doc.autoTable.previous.finalY;
        }

        // Cost Table
        const f = new Intl.NumberFormat('es-HN', { style: 'currency', currency: 'HNL' });
        const completedOrders = machineWorkOrders.filter(o => o.status === 'Completado');
        const costBody = completedOrders.map(order => {
            const { partsCost, laborCost, totalCost } = calculateTotalCost(order, true);
            return [order.id, f.format(partsCost), f.format(laborCost), f.format(totalCost)];
        });

        if (costBody.length > 0) {
             doc.text('Análisis de Costos por Orden de Trabajo', 14, finalY + 10);
            doc.autoTable({
                startY: finalY + 13,
                head: [['ID Orden', 'Costo Repuestos', 'Costo Mano de Obra', 'Costo Total']],
                body: costBody,
                headStyles: { fillColor: [44, 62, 80] },
            });
            finalY = doc.autoTable.previous.finalY;
        }
        
        const { totalCost: grandTotalCost } = calculateTotalCostForMultiple(completedOrders);
        doc.setFontSize(12);
        doc.setFont(undefined, 'bold');
        doc.text(`Costo Total de Mantenimiento para esta Máquina: ${f.format(grandTotalCost)}`, 14, finalY + 15);

        doc.save(`Reporte_Maquina_${machine.id}.pdf`);
        
        // Cleanup
        document.body.removeChild(chartContainer);
        showToast(`Reporte de ${machine.name} generado`, 'success');

    } catch (error) {
        console.error("Error generating machine report:", error);
        showToast('Error al generar el reporte de máquina', 'error');
    } finally {
        showLoading(false);
    }
}

async function handlePartReturn(partId, quantity, description) {
    const woId = document.getElementById('wo-id').value;
    const woFbId = document.getElementById('wo-fb-id-hidden').value;

    const returnQtyStr = prompt(`Cantidad a devolver de "${description}" (Máx: ${quantity}):`, quantity);
    if (returnQtyStr === null) return;

    const returnQty = parseInt(returnQtyStr);
    if (isNaN(returnQty) || returnQty < 1 || returnQty > quantity) {
        showToast('Cantidad inválida.', 'error');
        return;
    }

    const reason = prompt('Indique el motivo de la devolución:');
    if (reason === null) return;
    if (!reason.trim()) {
        showToast('El motivo de devolución es obligatorio.', 'warning');
        return;
    }

    requestSignature(async () => {
        showLoading(true, "Procesando devolución...");
        try {
            const now = new Date().toISOString();
            const part = state.parts.find(p => p.id === partId);
            if (!part) throw new Error("Repuesto no encontrado");

            // 1. Reintegrar Stock
            const partRef = doc(state.collections.parts, part.fb_id);
            await updateDoc(partRef, {
                stockActual: (part.stockActual || 0) + returnQty,
                lastModified: now
            });

            // 2. Registrar Movimiento DEVOLUCION
            await recordPartMovement(partId, returnQty, 'DEVOLUCION', {
                orderHumanId: woId,
                details: `Devolución desde OT ${woId}. Motivo: ${reason}`,
                recipientUser: getCurrentUser().username
            });

            // 3. Log de Auditoría
            await recordAuditLog('parts', partId, 'UPDATE',
                { stockActual: part.stockActual },
                { stockActual: part.stockActual + returnQty },
                `DEVOLUCIÓN DESDE OT ${woId}: ${reason}`
            );

            // 4. Actualizar la OT (Observaciones y Cantidad si aplica parcial)
            const obsField = document.getElementById('wo-observaciones');
            const timestamp = new Date().toLocaleString();
            obsField.value += `\n[${timestamp}] DEVOLUCIÓN de ${returnQty} un. de "${description}". Motivo: ${reason}`;

            if (returnQty === quantity) {
                // Eliminar la fila
                const list = document.getElementById('wo-parts-list');
                const row = list.querySelector(`[data-part-id="${partId}"]`);
                if (row) row.remove();
            } else {
                // Actualizar cantidad en la fila
                const list = document.getElementById('wo-parts-list');
                const row = list.querySelector(`[data-part-id="${partId}"]`);
                if (row) {
                    const newQty = quantity - returnQty;
                    row.dataset.quantity = newQty;
                    row.querySelector('.badge.bg-secondary').textContent = `Cant: ${newQty}`;

                    // Re-renderizar botones para que el prompt use el nuevo max
                    const btnReturn = row.querySelector('.btn-outline-warning');
                    if (btnReturn) {
                        btnReturn.setAttribute('onclick', `handlePartReturn('${partId}', ${newQty}, '${description.replace(/'/g, "\\'")}')`);
                    }
                }
            }

            // Forzar guardado de la OT para persistir observaciones y cambios en partsUsed
            await saveWorkOrder();

            showToast('Devolución procesada y registrada.', 'success');

        } catch (error) {
            console.error("Error returning part:", error);
            showToast('Error al procesar la devolución.', 'error');
        } finally {
            showLoading(false);
        }
    }, () => {
        showToast('Devolución cancelada: se requiere firma (contraseña) para confirmar.', 'info');
    });
}

function generateMachinePartsReport() {
    const machineId = document.getElementById('report-machine-parts-select').value;
    if (!machineId) {
        showToast('Seleccione una máquina para generar el reporte', 'warning');
        return;
    }
    showLoading(true);

    setTimeout(() => {
        try {
            const machine = state.machines.find(m => m.id === machineId);
            if (!machine) {
                throw new Error("Máquina no encontrada.");
            }
            const partsForMachine = state.parts.filter(p => (p.machineIds && p.machineIds.includes(machineId)) || p.machineId === machineId);

            const { jsPDF } = window.jspdf;
            const doc = new jsPDF();

            doc.text(`Reporte de Repuestos para: ${machine.name} (${machine.id})`, 14, 20);
            doc.setFontSize(10);
            doc.text(`Fecha de generación: ${new Date().toLocaleDateString('es-ES')}`, 14, 26);

            const body = partsForMachine.map(p => [
                p.id,
                p.description,
                p.stockActual,
                p.minStock,
                p.location || 'N/A'
            ]);

            doc.autoTable({
                startY: 35,
                head: [['ID Repuesto', 'Descripción', 'Stock Actual', 'Stock Mínimo', 'Ubicación']],
                body: body,
                headStyles: { fillColor: [44, 62, 80] },
            });

            doc.save(`Reporte_Repuestos_${machine.id}.pdf`);
            showToast('Reporte de repuestos generado correctamente', 'success');
        } catch (error) {
            console.error("Error generating parts report:", error);
            showToast('Error al generar el reporte de repuestos.', 'error');
        } finally {
            showLoading(false);
        }
    }, 500);
}

async function generateSingleWorkOrderReport(explicitWoId = null) {
    const headerConfig = {
        line1Left: 'Fecha de elaboración: 27/01/2026',
        line2Left: 'Fecha de revisión: 03/02/2026',
        line3Left: 'Fecha de aprobación: 05/02/2026',
        line1Center: 'Código: FO-MNDO-001',
        line2Center: 'Versión : 7.0',
        line1Right: 'SISTEMA DE GESTIÓN DE CALIDAD',
        logoBase64: "data:image/png;base64,"
    };
    const workOrderId = (typeof explicitWoId === 'string') ? explicitWoId : document.getElementById('report-wo-select').value;
    if (!workOrderId || typeof workOrderId !== 'string') {
        showToast('Seleccione una orden de trabajo para imprimir', 'warning');
        return;
    }
    
    showLoading(true, "Procesando imágenes de alta resolución para el informe...");
    
    const order = state.workOrders.find(o => o.id === workOrderId);
    if (!order) {
        showToast('Orden de trabajo no encontrada.', 'error');
        showLoading(false);
        return;
    }

    const machine = state.machines.find(m => m.id === order.machineId) || { name: 'N/A', id: 'N/A' };

    setTimeout(async () => { // Use timeout to allow loading spinner to show
        try {
            const { jsPDF } = window.jspdf;
            const doc = new jsPDF('p', 'pt', 'letter');

            // --- Helper Functions ---

            const drawFooter = () => {
                const pageWidth = doc.internal.pageSize.getWidth();
                const pageCount = doc.internal.getNumberOfPages();
                for (let i = 1; i <= pageCount; i++) {
                    doc.setPage(i);
                    doc.setFontSize(8);
                    doc.setFont(undefined, 'normal');
                    const footerY = doc.internal.pageSize.getHeight() - 20;
                    doc.text('FO-MNDO-001 Orden de trabajo de mantenimiento', margin, footerY);
                    doc.text(`Copia Controlada Página  ${i} de ${pageCount}`, pageWidth - margin, footerY, { align: 'right' });
                }
            };

            const drawHeader = (customTitle = 'Orden de Trabajo de Mantenimiento') => {
                const pageWidth = doc.internal.pageSize.getWidth();
                const margin = 40;
                const headerY = 30;
                const headerHeight = 60;
                const contentWidth = pageWidth - (margin * 2);
                const colWidth = contentWidth / 3;

                doc.setFontSize(9);
                doc.setFont(undefined, 'normal');

                // Column 1
                doc.text(headerConfig.line1Left, margin, headerY + 15);
                doc.text(headerConfig.line2Left, margin, headerY + 30);
                doc.text(headerConfig.line3Left, margin, headerY + 45);

                // Column 2
                doc.text(headerConfig.line1Center, margin + colWidth, headerY + 15);
                doc.text(headerConfig.line2Center, margin + colWidth, headerY + 30);

                // Column 3
                doc.text(headerConfig.line1Right, margin + colWidth * 2, headerY + 15);

                // Logo
                if (headerConfig.logoBase64) {
                    try {
                        const logoW = 80;
                        const logoH = 25;
                        const textWidth = doc.getTextWidth(headerConfig.line1Right);
                        const logoX = margin + colWidth * 2 + textWidth - logoW;
                        doc.addImage(headerConfig.logoBase64, 'PNG', logoX, headerY + 25, logoW, logoH);
                    } catch (err) {
                        console.error("Error adding logo to PDF:", err);
                    }
                }

                // Main Title
                doc.setFontSize(14);
                doc.setFont(undefined, 'bold');
                doc.text(customTitle, pageWidth / 2, headerY + headerHeight + 20, { align: 'center' });

                doc.setFontSize(10);
                doc.setFont(undefined, 'normal');
            };

            const drawBoxWithValue = (x, y, w, h, label, value) => {
                doc.setLineWidth(0.5);
                doc.rect(x, y, w, h);
                doc.setFontSize(8);
                doc.text(label, x + 3, y + 10);
                doc.setFontSize(10);
                doc.setFont(undefined, 'bold');
                doc.text(value || 'N/A', x + 3, y + 25);
                doc.setFont(undefined, 'normal');
            };

            // --- Document Body ---
            const pageWidth = doc.internal.pageSize.getWidth();
            const margin = 40;
            const contentWidth = pageWidth - (margin * 2);

            drawHeader();
            
            drawBoxWithValue(margin, 120, contentWidth * 0.4, 30, 'Código Máquina', machine.id);
            drawBoxWithValue(margin + contentWidth * 0.4 + 10, 120, contentWidth * 0.6 - 10, 30, 'Máquina', `${machine.name} - Departamento: ${machine.location || 'N/A'}`);
            
            doc.setFontSize(12);
            doc.setFont(undefined, 'bold');
            doc.text(`Número de Orden: ${order.id} - Prioridad ${machine.criticidad || 'N/A'}`, margin, 175);

            drawBoxWithValue(margin, 190, contentWidth / 2 - 5, 30, 'Clase de Mantenimiento', order.type);
            drawBoxWithValue(margin + contentWidth / 2 + 5, 190, contentWidth / 2 - 5, 30, 'Tipo de Falla', order.failureType || 'N/A');

            const requestDate = order.createdAt ? new Date(order.createdAt).toLocaleDateString('es-ES') : 'N/A';
            const solicitudRefId = order.solicitudId || order.sourceSolicitudId;
            const solicitudRef = solicitudRefId ? state.solicitudes.find(s => s.id === solicitudRefId || s.fb_id === solicitudRefId) : null;
            const humanSolicitudId = solicitudRef ? solicitudRef.id : order.solicitudId;
            const requesterDisplay = humanSolicitudId ? `${order.requester || 'N/A'} (Solicitud: ${humanSolicitudId})` : (order.requester || 'N/A');
            drawBoxWithValue(margin, 230, contentWidth / 2 - 5, 30, 'Solicitado por:', requesterDisplay);
            drawBoxWithValue(margin + contentWidth / 2 + 5, 230, contentWidth / 2 - 5, 30, 'Fecha de Solicitud:', requestDate);

            // Dynamic Description Box
            const descText = order.description || 'N/A';
            const descLines = doc.splitTextToSize(descText, contentWidth - 10);
            const descBoxHeight = Math.max(40, (descLines.length * 12) + 25);
            doc.rect(margin, 270, contentWidth, descBoxHeight);
            doc.setFontSize(8);
            doc.text('Descripción de la actividad solicitada:', margin + 3, 280);
            doc.setFontSize(10);
            doc.setFont(undefined, 'normal');
            doc.text(descLines, margin + 5, 295);
            
            let currentY = 270 + descBoxHeight + 10;

            const startTimestamp = order.fechaInicioReal || order.startTime;
            const startDate = startTimestamp ? new Date(startTimestamp).toLocaleDateString('es-ES') : (order.date || 'N/A');
            const startTime = startTimestamp ? new Date(startTimestamp).toLocaleTimeString('es-ES', {hour: '2-digit', minute:'2-digit'}) : 'N/A';
            drawBoxWithValue(margin, currentY, contentWidth / 2 - 5, 30, 'Asignado a:', order.leadTechnician || 'N/A');
            drawBoxWithValue(margin + contentWidth / 2 + 5, currentY, contentWidth / 2 - 5, 30, 'Fecha de Inicio:', startDate);
            
            currentY += 40;
            const collaborators = order.supportTechnicians ? order.supportTechnicians.join(', ') : 'N/A';
            drawBoxWithValue(margin, currentY, contentWidth / 2 - 5, 30, 'Colaboradores:', collaborators);
            drawBoxWithValue(margin + contentWidth / 2 + 5, currentY, contentWidth / 2 - 5, 30, 'Hora de Inicio:', startTime);

            currentY += 40;
            const endTimestamp = order.fechaFinalizacionReal || order.endTime;
            const endDate = endTimestamp ? new Date(endTimestamp).toLocaleDateString('es-ES') : 'N/A';
            const endTime = endTimestamp ? new Date(endTimestamp).toLocaleTimeString('es-ES', {hour: '2-digit', minute:'2-digit'}) : 'N/A';
            const totalHours = (getTotalWorkDurationMs(order) / (1000 * 60 * 60)).toFixed(2);

            const row4W = (contentWidth - 15) / 4;
            drawBoxWithValue(margin, currentY, row4W, 45, 'Estado de la orden:', order.status);
            drawBoxWithValue(margin + row4W + 5, currentY, row4W, 45, 'Fecha Final:', endDate);
            drawBoxWithValue(margin + 2 * (row4W + 5), currentY, row4W, 45, 'Hora Final:', endTime);
            drawBoxWithValue(margin + 3 * (row4W + 5), currentY, row4W, 45, 'Horas empleadas:', `${totalHours} hrs`);

            currentY += 65;
            
            const partsBody = (order.partsUsed || []).map(pInfo => {
                const part = state.parts.find(p => p.id === pInfo.partId);
                return [
                    pInfo.partId,
                    part ? part.description : 'N/A',
                    pInfo.quantity,
                    formatCurrency(part.cost),
                    formatCurrency(part.cost * pInfo.quantity)
                ];
            });
            
            let finalY = currentY;
            if (partsBody.length > 0) {
                doc.setFontSize(12);
                doc.setFont(undefined, 'bold');
                doc.text('Repuestos / Materiales Utilizados', margin, currentY);

                 doc.autoTable({
                    startY: currentY + 10,
                    head: [['ID', 'Descripción', 'Cantidad', 'Costo Unit.', 'Costo Total']],
                    body: partsBody,
                    headStyles: { fillColor: [44, 62, 80] },
                    margin: { left: margin, right: margin, top: 100 },
                    didDrawPage: function(data) {
                        // Draw header on new pages if table spans multiple pages
                        if (data.pageNumber > 1) {
                            drawHeader();
                        }
                    }
                });
                finalY = doc.autoTable.previous.finalY;
                finalY += 20;
            }

            // Dynamic Observaciones Box
            const obsText = order.observaciones || (order.workPerformed || 'N/A');
            const obsLines = doc.splitTextToSize(obsText, contentWidth - 10);
            const obsBoxHeight = Math.max(40, (obsLines.length * 12) + 25);

            // Check if observaciones box fits on current page
            if (finalY + obsBoxHeight + 40 > doc.internal.pageSize.getHeight()) {
                doc.addPage();
                drawHeader();
                finalY = 120;
            }

            doc.rect(margin, finalY, contentWidth, obsBoxHeight);
            doc.setFontSize(8);
            doc.text('Observaciones / Trabajo Realizado:', margin + 3, finalY + 10);
            doc.setFontSize(10);
            doc.setFont(undefined, 'normal');
            doc.text(obsLines, margin + 5, finalY + 25);

            finalY += obsBoxHeight;
            
            // Check if signatures fit on current page
            if (finalY + 120 > doc.internal.pageSize.getHeight()) {
                doc.addPage();
                drawHeader();
                finalY = 120;
            } else {
                finalY += 40;
            }

            const evals = state.technicianEvaluations.filter(ev => ev.workOrderFbId === order.fb_id);
            const opEval = evals.find(ev => ev.evaluatorRole === 'Operario');
            const jefeEval = evals.find(ev => ev.evaluatorRole === 'Jefe de Area');
            const supervisorEval = evals.find(ev => ev.evaluatorRole === 'Supervisor de Area');

            const sigWidth = (contentWidth - 60) / 4;
            const sigH = 30;
            const sigW = sigWidth - 5;

            const getSignatureByUsername = (username) => {
                if (!username || username === 'Sistema (Plan de Mantenimiento)') return null;
                const tech = state.technicians.find(t => t.username === username);
                return tech ? tech.signature : null;
            };

            // Signature 1: Ejecutado por
            const sigExec = getSignatureByUsername(order.leadTechnician);
            if (sigExec) {
                try { doc.addImage(sigExec, 'PNG', margin + 2.5, finalY + 10, sigW, sigH); } catch(err){}
            }
            doc.line(margin, finalY + 45, margin + sigWidth, finalY + 45);
            doc.setFontSize(7);
            doc.text(`Ejecutado por:`, margin + sigWidth / 2, finalY + 55, { align: 'center' });
            doc.text(`${order.leadTechnician || ''}`, margin + sigWidth / 2, finalY + 65, { align: 'center' });

            // Signature 2: Recibido por (Operario o Supervisor según tipo de OT)
            let sigLabel = 'Operario';
            const isPlanOrder = ['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(order.type) || order.linkedPlanId;
            const hasRequest = order.solicitudId || order.sourceSolicitudId;

            let receiverName = '';
            if (isPlanOrder) {
                sigLabel = 'Supervisor';
                receiverName = supervisorEval ? supervisorEval.evaluatorId : '';
            } else {
                receiverName = hasRequest ? (order.requester || '') : (opEval ? opEval.evaluatorId : '');
            }

            if (receiverName === 'Sistema (Plan de Mantenimiento)') receiverName = '';
            const sigRec = getSignatureByUsername(receiverName);
            if (sigRec) {
                try { doc.addImage(sigRec, 'PNG', margin + sigWidth + 20 + 2.5, finalY + 10, sigW, sigH); } catch(err){}
            }
            doc.line(margin + sigWidth + 20, finalY + 45, margin + 2 * sigWidth + 20, finalY + 45);
            doc.text(`${sigLabel}:`, margin + sigWidth + 20 + sigWidth / 2, finalY + 55, { align: 'center' });
            doc.text(`${receiverName}`, margin + sigWidth + 20 + sigWidth / 2, finalY + 65, { align: 'center' });

            // Signature 3: Jefe de Mantenimiento (Planner/Admin, WITH name)
            const validatorName = order.validatedBy || '';
            const sigVal = getSignatureByUsername(validatorName);
            if (sigVal) {
                try { doc.addImage(sigVal, 'PNG', margin + 2 * (sigWidth + 20) + 2.5, finalY + 10, sigW, sigH); } catch(err){}
            }
            doc.line(margin + 2 * (sigWidth + 20), finalY + 45, margin + 3 * sigWidth + 40, finalY + 45);
            doc.text(`Jefe de Mantenimiento:`, margin + 2 * (sigWidth + 20) + sigWidth / 2, finalY + 55, { align: 'center' });
            doc.text(`${validatorName}`, margin + 2 * (sigWidth + 20) + sigWidth / 2, finalY + 65, { align: 'center' });

            // Signature 4: Jefe de área / Supervisor (Area Head evaluator, WITH name)
            let jefeLabel = 'Jefe de área';
            let jefeName = '';
            if (isPlanOrder) {
                jefeName = jefeEval ? jefeEval.evaluatorId : '';
            } else {
                if (supervisorEval) {
                    jefeLabel = 'Supervisor';
                    jefeName = supervisorEval.evaluatorId;
                } else {
                    jefeName = jefeEval ? jefeEval.evaluatorId : '';
                }
            }

            const sigJefe = getSignatureByUsername(jefeName);
            if (sigJefe) {
                try { doc.addImage(sigJefe, 'PNG', margin + 3 * (sigWidth + 20) + 2.5, finalY + 10, sigW, sigH); } catch(err){}
            }
            doc.line(margin + 3 * (sigWidth + 20), finalY + 45, pageWidth - margin, finalY + 45);
            doc.text(`${jefeLabel}:`, margin + 3 * (sigWidth + 20) + sigWidth / 2, finalY + 55, { align: 'center' });
            doc.text(`${jefeName}`, margin + 3 * (sigWidth + 20) + sigWidth / 2, finalY + 65, { align: 'center' });
            
            // --- Anexos Fotográficos ---
            if (order.images && order.images.length > 0) {
                doc.addPage();
                drawHeader('Anexo: Registro Fotográfico de Actividades');

                let imgY = 140;
                const maxImages = 5;
                const imagesToProcess = order.images.slice(0, maxImages);
                const targetWidthPt = 8.5 * 28.346; // 8.5 cm to points

                for (let idx = 0; idx < imagesToProcess.length; idx++) {
                    const img = imagesToProcess[idx];

                    // 2 images per page (one above the other)
                    if (idx > 0 && idx % 2 === 0) {
                        doc.addPage();
                        drawHeader('Anexo: Registro Fotográfico de Actividades');
                        imgY = 140;
                    }

                    try {
                        // useRetry = true to handle Firebase optimization naming and delay
                        const imgResult = await imageUrlToBase64(img.url, true);
                        if (imgResult) {
                            let finalW = targetWidthPt;
                            let finalH = (imgResult.height * finalW) / imgResult.width;

                            // Prevent image from being too tall (max 300pt)
                            if (finalH > 300) {
                                finalH = 300;
                                finalW = (imgResult.width * finalH) / imgResult.height;
                            }

                            const centerX = (pageWidth - finalW) / 2;
                            doc.addImage(imgResult.data, 'JPEG', centerX, imgY, finalW, finalH, undefined, 'FAST');

                            doc.setFontSize(10);
                            doc.setFont(undefined, 'italic');
                            const caption = img.caption || `Imagen ${idx + 1}`;
                            const splitCaption = doc.splitTextToSize(caption, contentWidth);
                            doc.text(splitCaption, pageWidth / 2, imgY + finalH + 15, { align: 'center' });
                            imgY += finalH + 60;
                        } else {
                            throw new Error("Base64 conversion failed");
                        }
                    } catch (err) {
                        console.error("Error adding activity image to PDF:", err);
                        doc.setFontSize(10);
                        doc.text(`[No se pudo cargar la imagen ${idx + 1}]`, pageWidth / 2, imgY + 20, { align: 'center' });
                        imgY += 40;
                    }
                }
            }

            drawFooter();
            doc.save(`OT_${workOrderId}.pdf`);
            
        } catch (e) {
            console.error("Error generating single WO report:", e);
            showToast('Ocurrió un error al generar el PDF.', 'error');
        } finally {
            showLoading(false);
        }
    }, 500);
}

async function generateMaterialOutPDF(data) {
    const { jsPDF } = window.jspdf;
    const doc = new jsPDF();
    const margin = 20;
    const logoBase64 = typeof GLOBAL_LOGO !== 'undefined' ? GLOBAL_LOGO : "";

    // Header
    if (logoBase64) {
        try { doc.addImage(logoBase64, 'PNG', margin, 10, 50, 15); } catch(e){}
    }

    doc.setFontSize(16);
    doc.setFont(undefined, 'bold');
    doc.text('REGISTRO DE SALIDA DE MATERIAL', 105, 35, { align: 'center' });

    doc.setFontSize(10);
    doc.setFont(undefined, 'normal');
    doc.text(`Documento N°: OUT-${data.fb_id.substring(0, 8).toUpperCase()}`, 190, 15, { align: 'right' });
    doc.text(`Fecha: ${new Date(data.date).toLocaleString()}`, 190, 20, { align: 'right' });

    // General Data
    doc.setFillColor(240, 240, 240);
    doc.rect(margin, 45, 170, 35, 'F');

    doc.setFont(undefined, 'bold');
    doc.text('DATOS GENERALES', margin + 5, 52);
    doc.setFont(undefined, 'normal');

    const machine = state.machines.find(m => m.id === data.machineId);
    const area = machine ? machine.location : 'N/A';

    doc.text(`Área: ${area}`, margin + 5, 60);
    doc.text(`Operario / Recibe: ${data.recipientUser}`, margin + 5, 67);
    doc.text(`Equipo / Máquina: ${data.machineId || 'N/A'} - ${machine ? machine.name : ''}`, margin + 5, 74);

    doc.text(`OT Relacionada: ${data.workOrderId || 'N/A'}`, 110, 60);
    doc.text(`Despachado por: ${data.deliveryUser}`, 110, 67);
    doc.text(`Motivo: ${data.notes || 'Entrega de material solicitado'}`, 110, 74);

    // Table
    const tableData = data.items.map(item => [
        item.partId,
        item.description,
        item.quantity
    ]);

    doc.autoTable({
        startY: 85,
        head: [['Código', 'Descripción', 'Cantidad Entregada']],
        body: tableData,
        theme: 'grid',
        headStyles: { fillColor: [44, 62, 80] },
        margin: { left: margin, right: margin }
    });

    let finalY = doc.autoTable.previous.finalY + 30;

    // Signatures
    const deliverer = state.technicians.find(t => t.username === data.deliveryUser);
    if (deliverer && deliverer.signature) {
        try { doc.addImage(deliverer.signature, 'PNG', margin, finalY - 30, 50, 25); } catch(e){}
    }
    doc.line(margin, finalY, margin + 60, finalY);
    doc.text('Firma Entrega (Almacén)', margin, finalY + 5);
    doc.setFontSize(8);
    doc.text(`Digitalmente por: ${data.deliveryUser}`, margin, finalY + 10);

    doc.line(130, finalY, 130 + 60, finalY);
    doc.setFontSize(10);
    doc.text('Firma Recibe (Operario)', 130, finalY + 5);
    doc.setFontSize(8);
    doc.text(`Responsable: ${data.recipientUser}`, 130, finalY + 10);

    // QR Code
    const qrContainer = document.createElement('div');
    const qr = new QRCode(qrContainer, {
        text: `CORINFAR-OUT-${data.fb_id}`,
        width: 100,
        height: 100
    });

    // Wait a bit for QR to generate
    setTimeout(() => {
        const qrCanvas = qrContainer.querySelector('canvas');
        if (qrCanvas) {
            const qrDataUrl = qrCanvas.toDataURL('image/png');
            doc.addImage(qrDataUrl, 'PNG', 160, 240, 30, 30);
            doc.setFontSize(7);
            doc.text('Validación Interna QR', 175, 272, { align: 'center' });
        }

        doc.save(`Salida_Material_${data.fb_id.substring(0, 8)}.pdf`);
    }, 500);
}

async function generatePurchaseRequestPDF(request) {
    const { jsPDF } = window.jspdf;
    const doc = new jsPDF();
    const margin = 20;
    const logoBase64 = typeof GLOBAL_LOGO !== 'undefined' ? GLOBAL_LOGO : "";

    // Header
    if (logoBase64) {
        try { doc.addImage(logoBase64, 'PNG', margin, 10, 40, 12); } catch(e){}
    }

    doc.setFontSize(10);
    doc.setFont(undefined, 'bold');
    doc.text(`CÓDIGO: ${request.isoCode || 'FO-ALM-001'}`, 190, 15, { align: 'right' });
    doc.text(`FOLIO: ${request.folioStr}`, 190, 20, { align: 'right' });
    doc.setFont(undefined, 'normal');
    doc.text(`Fecha: ${new Date(request.date).toLocaleString()}`, 190, 25, { align: 'right' });

    doc.setFontSize(14);
    doc.setFont(undefined, 'bold');
    doc.text('CORINFAR - GESTIÓN DE MANTENIMIENTO', 105, 35, { align: 'center' });
    doc.setFontSize(12);
    doc.text('SOLICITUD DE COMPRA / COTIZACIÓN', 105, 42, { align: 'center' });

    // Info Box
    doc.setFillColor(245, 245, 245);
    doc.rect(margin, 55, 170, 32, 'F');
    doc.setFont(undefined, 'bold');
    doc.text('DATOS GENERALES', margin + 5, 62);
    doc.setFont(undefined, 'normal');
    doc.text(`Solicitado por: ${request.createdBy}`, margin + 5, 69);
    doc.text(`Tipo de Documento: Solicitud de Repuestos e Insumos para Mantenimiento`, margin + 5, 76);
    doc.text(`Estado: ${request.status}`, margin + 5, 83);

    // Table
    const tableData = request.items.map(item => [
        item.partId,
        item.description,
        item.quantity,
        formatCurrency(item.cost),
        formatCurrency(item.subtotal)
    ]);

    doc.autoTable({
        startY: 92,
        head: [['ID', 'Descripción', 'Cant.', 'Precio Est.', 'Subtotal']],
        body: tableData,
        theme: 'striped',
        headStyles: { fillColor: [30, 59, 138] },
        styles: { fontSize: 9 },
        columnStyles: {
            2: { halign: 'left' },
            3: { halign: 'left' },
            4: { halign: 'left' }
        }
    });

    const finalY = doc.autoTable.previous.finalY;

    doc.setFont(undefined, 'bold');
    doc.text('TOTAL ESTIMADO:', 150, finalY + 10, { align: 'right' });
    doc.setTextColor(30, 59, 138);
    doc.text(formatCurrency(request.totalCost), 190, finalY + 10, { align: 'right' });
    doc.setTextColor(0, 0, 0);

    // Signature blocks (ISO style)
    const sigY = Math.max(finalY + 30, 240);
    const sigWidth = 50;

    // Solicitante
    doc.line(margin, sigY, margin + sigWidth, sigY);
    doc.setFontSize(8);
    doc.text('SOLICITADO POR', margin + sigWidth/2, sigY + 4, { align: 'center' });
    doc.text(request.createdBy, margin + sigWidth/2, sigY + 8, { align: 'center' });

    // Revisado (Planificador)
    doc.line(105 - sigWidth/2, sigY, 105 + sigWidth/2, sigY);
    doc.text('REVISADO POR (PLANIFICACIÓN)', 105, sigY + 4, { align: 'center' });

    // Aprobado (Gerencia)
    doc.line(190 - sigWidth, sigY, 190, sigY);
    doc.text('APROBADO POR (GERENCIA)', 190 - sigWidth/2, sigY + 4, { align: 'center' });

    // Footer
    doc.setFontSize(7);
    doc.setTextColor(150, 150, 150);
    doc.text('Documento generado por CORINFAR CMMS - Sistema de Gestión de Mantenimiento ISO', 105, 285, { align: 'center' });

    doc.save(`Solicitud_Compra_${request.folioStr}.pdf`);
}

async function generatePartRequestGroupPDF(fbId) {
    const request = state.partRequests.find(r => r.fb_id === fbId);
    if (!request) return;

    const { jsPDF } = window.jspdf;
    const doc = new jsPDF();
    const margin = 20;
    const logoBase64 = typeof GLOBAL_LOGO !== 'undefined' ? GLOBAL_LOGO : "";

    // Header
    if (logoBase64) {
        try { doc.addImage(logoBase64, 'PNG', margin, 10, 50, 15); } catch(e){}
    }

    doc.setFontSize(16);
    doc.setFont(undefined, 'bold');
    doc.text('REPORTE DE SOLICITUD DE REPUESTOS', 105, 35, { align: 'center' });

    doc.setFontSize(10);
    doc.setFont(undefined, 'normal');
    doc.text(`Solicitud N°: ${request.id || '---'}`, 190, 15, { align: 'right' });
    doc.text(`Fecha: ${new Date(request.date).toLocaleString()}`, 190, 20, { align: 'right' });

    // General Data
    doc.setFillColor(240, 240, 240);
    doc.rect(margin, 45, 170, 35, 'F');

    doc.setFont(undefined, 'bold');
    doc.text('INFORMACIÓN DE LA SOLICITUD', margin + 5, 52);
    doc.setFont(undefined, 'normal');

    const machine = state.machines.find(m => m.id === request.machineId);
    const area = machine ? machine.location : 'N/A';

    doc.text(`Área: ${area}`, margin + 5, 60);
    doc.text(`Solicitante: ${request.requester}`, margin + 5, 67);
    doc.text(`Equipo: ${request.machineId || 'N/A'} - ${machine ? machine.name : ''}`, margin + 5, 74);

    doc.text(`OT Relacionada: ${request.workOrderId || 'N/A'}`, 110, 60);
    doc.text(`Estado Global: ${request.status}`, 110, 67);
    doc.text(`Urgencia: ${request.urgency}`, 110, 74);

    // Items Table
    const items = request.items || [{
        description: request.description,
        quantity: request.quantity,
        status: request.status,
        deliveredQuantity: request.deliveredQuantity || 0
    }];

    const tableData = items.map(item => [
        item.description,
        item.quantity,
        item.deliveredQuantity || 0,
        item.status
    ]);

    doc.autoTable({
        startY: 85,
        head: [['Descripción', 'Cant. Solicitada', 'Cant. Entregada', 'Estado']],
        body: tableData,
        theme: 'grid',
        headStyles: { fillColor: [44, 62, 80] },
        margin: { left: margin, right: margin }
    });

    let finalY = doc.autoTable.previous.finalY + 40;
    const sigWidth = 50;
    const sigHeight = 25;

    // Determinar si es una solicitud de técnico (basado en la OT vinculada o el rol del solicitante)
    const isTechnicianRequest = request.workOrderId && (request.workOrderId.startsWith('MA') || request.workOrderId.startsWith('ME') || request.workOrderId.startsWith('MP'));

    if (isTechnicianRequest) {
        // Reporte para Técnicos (2 firmas)
        // Firma Entrega (Almacén)
        if (request.deliverySignature) {
            try { doc.addImage(request.deliverySignature, 'PNG', margin, finalY - 30, sigWidth, sigHeight); } catch(e){}
        }
        doc.line(margin, finalY, margin + sigWidth + 20, finalY);
        doc.setFontSize(8);
        doc.text('Firma Entrega (Almacén)', margin, finalY + 5);
        doc.text(request.processedBy || 'N/A', margin, finalY + 10);

        // Firma Recibe (Técnico)
        if (request.receivedSignature) {
            try { doc.addImage(request.receivedSignature, 'PNG', 130, finalY - 30, sigWidth, sigHeight); } catch(e){}
        }
        doc.line(130, finalY, 130 + sigWidth + 20, finalY);
        doc.text('Firma Recibe (Técnico)', 130, finalY + 5);
        doc.text(request.receivedBy || 'N/A', 130, finalY + 10);
    } else {
        // Reporte para Operarios (4 firmas ahora para incluir entrega y recibe por separado)
        const opSigWidth = 40;

        // Firma Solicitante
        doc.line(margin, finalY, margin + opSigWidth, finalY);
        doc.setFontSize(8);
        doc.text('Firma Solicitante', margin, finalY + 5);
        doc.text(request.requester || 'N/A', margin, finalY + 10);

        // Firma Aprueba (Jefe Área)
        if (request.approvedSignature) {
            try { doc.addImage(request.approvedSignature, 'PNG', 65, finalY - 30, opSigWidth, sigHeight); } catch(e){}
        }
        doc.line(65, finalY, 65 + opSigWidth, finalY);
        doc.text('Aprueba (Jefe Área)', 65, finalY + 5);
        doc.text(request.approvedBy || 'N/A', 65, finalY + 10);

        // Firma Entrega (Almacén)
        if (request.deliverySignature) {
            try { doc.addImage(request.deliverySignature, 'PNG', 110, finalY - 30, opSigWidth, sigHeight); } catch(e){}
        }
        doc.line(110, finalY, 110 + opSigWidth, finalY);
        doc.text('Firma Entrega (Almacén)', 110, finalY + 5);
        doc.text(request.processedBy || 'N/A', 110, finalY + 10);

        // Firma Recibe (Operario)
        if (request.receivedSignature) {
            try { doc.addImage(request.receivedSignature, 'PNG', 155, finalY - 30, opSigWidth, sigHeight); } catch(e){}
        }
        doc.line(155, finalY, 155 + opSigWidth, finalY);
        doc.text('Firma Recibe (Operario)', 155, finalY + 5);
        doc.text(request.receivedBy || 'N/A', 155, finalY + 10);
    }

    if (request.status === 'Rechazado') {
        doc.setTextColor(200, 0, 0);
        doc.setFontSize(12);
        doc.text(`MOTIVO DE RECHAZO: ${request.rejectionReason || 'No especificado'}`, margin, finalY + 25);
        doc.text(`Denegado por: ${request.rejectedBy || 'N/A'}`, margin, finalY + 32);
        finalY += 35;
    } else {
        finalY += 15;
    }

    // QR Code for internal validation (posicionamiento dinámico)
    if (finalY > 250) {
        doc.addPage();
        finalY = 40;
    }

    const qrContainer = document.createElement('div');
    const qr = new QRCode(qrContainer, {
        text: `CORINFAR-${request.id || request.fb_id}`,
        width: 100,
        height: 100
    });

    // Wait for QR to generate
    setTimeout(() => {
        const qrCanvas = qrContainer.querySelector('canvas');
        if (qrCanvas) {
            const qrDataUrl = qrCanvas.toDataURL('image/png');
            doc.addImage(qrDataUrl, 'PNG', 160, finalY, 30, 30);
            doc.setFontSize(7);
            doc.setTextColor(0, 0, 0);
            doc.text('Validación Interna QR', 175, finalY + 32, { align: 'center' });
        }
        doc.save(`Solicitud_${request.workOrderId || request.fb_id.substring(0, 8)}.pdf`);
    }, 500);
}

function generateGlobalReport() {
    const month = parseInt(document.getElementById('report-global-month').value);
    const year = parseInt(document.getElementById('report-global-year').value);

    if (isNaN(month) || isNaN(year)) {
        showToast('Por favor, seleccione un mes y año válidos.', 'error');
        return;
    }

    showLoading(true);

    setTimeout(() => {
        try {
            // 1. Filter Work Orders for the selected period
            const startDate = new Date(year, month, 1);
            const endDate = new Date(year, month + 1, 0, 23, 59, 59);

            const ordersInPeriod = state.workOrders.filter(o => {
                if (!o.date) return false;
                const orderDate = new Date(o.date + 'T12:00:00Z');
                return orderDate >= startDate && orderDate <= endDate;
            });

            if (ordersInPeriod.length === 0) {
                showToast('No hay datos de órdenes de trabajo para el período seleccionado.', 'warning');
                showLoading(false);
                return;
            }

            // 2. Calculate KPIs
            // 2.1. % de trabajo no planificado
            const totalCorrective = ordersInPeriod.filter(o => ['Correctivo', 'Emergencia', 'Calibración'].includes(o.type)).length;
            const unplannedWorkPercentage = (totalCorrective / ordersInPeriod.length) * 100;

            // 2.2. Disponibilidad operacional
            const machinesInPeriod = [...new Set(ordersInPeriod.map(o => o.machineId))];
            const machineObjects = state.machines.filter(m => machinesInPeriod.includes(m.id));
            const scheduledUptimeMs = calculateScheduledUptimeMs(machineObjects, startDate, endDate);

            const completedCorrectives = ordersInPeriod.filter(o => ['Correctivo', 'Emergencia', 'Calibración'].includes(o.type) && o.status === 'Completado');
            const totalDowntimeMs = completedCorrectives.reduce((sum, o) => sum + getTotalWorkDurationMs(o), 0);

            let operationalAvailability = 'N/A';
            if (scheduledUptimeMs > 0) {
                const availability = ((scheduledUptimeMs - totalDowntimeMs) / scheduledUptimeMs) * 100;
                operationalAvailability = Math.max(0, availability);
            }

            // 2.3. Top 5 equipos problemáticos
            const correctiveCountsByMachine = ordersInPeriod
                .filter(o => ['Correctivo', 'Emergencia', 'Calibración'].includes(o.type))
                .reduce((acc, order) => {
                    acc[order.machineId] = (acc[order.machineId] || 0) + 1;
                    return acc;
                }, {});

            const top5ProblematicMachines = Object.entries(correctiveCountsByMachine)
                .sort(([, a], [, b]) => b - a)
                .slice(0, 5)
                .map(([machineId, count]) => ({
                    machineId,
                    name: state.machines.find(m => m.id === machineId).name || 'Desconocido',
                    failures: count
                }));

            // 2.4. Índice de confiabilidad
            const leastReliableMachine = top5ProblematicMachines.length > 0 ? top5ProblematicMachines[0] : null;

            // 3. Generate PDF
            const { jsPDF } = window.jspdf;
            const doc = new jsPDF();
            const monthName = document.getElementById('report-global-month').options[month].text;

            // Header
            doc.setFontSize(18);
            doc.text('Informe de Indicadores Globales', 14, 22);
            doc.setFontSize(12);
            doc.text(`Período: ${monthName} ${year}`, 14, 30);

            // Summary Table
            doc.setFontSize(14);
            doc.text('Resumen de Indicadores', 14, 45);
            doc.autoTable({
                startY: 50,
                theme: 'grid',
                headStyles: { fillColor: [44, 62, 80] },
                head: [['Indicador', 'Valor']],
                body: [
                    ['% de Trabajo No Planificado', `${unplannedWorkPercentage.toFixed(2)}%`],
                    ['Disponibilidad Operacional', `${typeof operationalAvailability === 'number' ? operationalAvailability.toFixed(2) : operationalAvailability}%`],
                    ['Máquina Menos Confiable', leastReliableMachine ? `${leastReliableMachine.name} (${leastReliableMachine.failures} fallos)` : 'N/A'],
                ]
            });

            let finalY = doc.autoTable.previous.finalY;

            // Top 5 Problematic Machines Details
            doc.setFontSize(14);
            doc.text('Top 5 Equipos Problemáticos', 14, finalY + 15);

            top5ProblematicMachines.forEach((machine, index) => {
                finalY = doc.autoTable.previous.finalY;
                if (finalY > 250) { // Add new page if content gets too long
                    doc.addPage();
                    finalY = 20;
                }

                doc.setFontSize(12);
                doc.setFont(undefined, 'bold');
                doc.text(`${index + 1}. ${machine.name} (${machine.failures} fallos)`, 14, finalY + 10);

                const machineOrders = ordersInPeriod.filter(o => o.machineId === machine.machineId && ['Correctivo', 'Emergencia', 'Calibración'].includes(o.type));
                const body = machineOrders.map(o => {
                    const durationMs = getTotalWorkDurationMs(o);
                    const hours = (durationMs / (1000 * 60 * 60)).toFixed(2);
                    return [
                        o.id,
                        o.description,
                        new Date(o.date + 'T12:00:00Z').toLocaleDateString('es-ES'),
                        `${hours} hrs`
                    ];
                });

                doc.autoTable({
                    startY: finalY + 15,
                    head: [['ID Orden', 'Falla Reportada', 'Fecha', 'Tiempo Reparación']],
                    body: body,
                    theme: 'striped',
                    headStyles: { fillColor: [108, 117, 125] },
                    margin: { left: 14 }
                });
            });

            doc.save(`Informe_Indicadores_Globales_${monthName}_${year}.pdf`);
            showToast('Reporte global generado correctamente', 'success');

        } catch (error) {
            console.error("Error generating global report:", error);
            showToast('Ocurrió un error al generar el reporte.', 'error');
        } finally {
            showLoading(false);
        }
    }, 1000);
}

async function generateExecutionReportPDF() {
    const executionId = document.getElementById('report-execution-select').value;
    if (!executionId) {
        showToast('Por favor, seleccione una ejecución para generar el reporte.', 'warning');
        return;
    }


    showLoading(true);

    try {
        const execution = state.workPlanExecutions.find(e => e.fb_id === executionId);
        if (!execution) {
            throw new Error('No se encontró la ejecución del plan.');
        }

        const plan = state.workPlans.find(p => p.fb_id === execution.planId);
        if (!plan) {
            throw new Error('No se encontró el plan de mantenimiento asociado.');
        }

        const workOrder = state.workOrders.find(wo =>
            (execution.workOrderFbId && wo.fb_id === execution.workOrderFbId) || // New way
            (!execution.workOrderFbId && wo.id === execution.workOrderId)      // Old way for backward compatibility
        );
        if (!workOrder) {
            throw new Error('No se encontró la orden de trabajo asociada.');
        }

        const machine = state.machines.find(m => m.id === plan.machineId);
        if (!machine) {
            throw new Error('No se encontró la máquina asociada.');
        }

        const { jsPDF } = window.jspdf;
        const doc = new jsPDF('p', 'pt', 'letter');
        const pageWidth = doc.internal.pageSize.getWidth();
        const margin = 40;
        let finalY = 0;

        // --- Helper Functions ---
        const headerConfig = {
            line1Left: 'Fecha de elaboración: 27/01/2026',
            line2Left: 'Fecha de revisión: 03/02/2026',
            line3Left: 'Fecha de aprobación: 05/02/2026',
            line1Center: 'Código: FO-MNDO-005',
            line2Center: 'Versión : 2.0',
            line1Right: 'SISTEMA DE GESTIÓN DE CALIDAD',
            logoBase64: "data:image/png;base64,"
        };

        const drawHeader = () => {
            const headerY = 30;
            const headerHeight = 60;
            const contentWidth = pageWidth - (margin * 2);
            const colWidth = contentWidth / 3;

            doc.setFontSize(9);
            doc.setFont(undefined, 'normal');

            // Column 1
            doc.text(headerConfig.line1Left, margin, headerY + 15);
            doc.text(headerConfig.line2Left, margin, headerY + 30);
            doc.text(headerConfig.line3Left, margin, headerY + 45);

            // Column 2
            doc.text(headerConfig.line1Center, margin + colWidth, headerY + 15);
            doc.text(headerConfig.line2Center, margin + colWidth, headerY + 30);

            // Column 3
            doc.text(headerConfig.line1Right, margin + colWidth * 2, headerY + 15);

            // Logo
            if (headerConfig.logoBase64) {
                try {
                    const logoW = 80;
                    const logoH = 25;
                    const textWidth = doc.getTextWidth(headerConfig.line1Right);
                    const logoX = margin + colWidth * 2 + textWidth - logoW;
                    doc.addImage(headerConfig.logoBase64, 'PNG', logoX, headerY + 25, logoW, logoH);
                } catch (err) {
                    console.error("Error adding logo to PDF:", err);
                }
            }

            // Main Title
            doc.setFontSize(14);
            doc.setFont(undefined, 'bold');
            doc.text('Reporte de trabajo de mantenimiento preventivo de equipo', pageWidth / 2, headerY + headerHeight + 25, { align: 'center' });

            doc.setFontSize(10);
            doc.setFont(undefined, 'normal');
        };

        drawHeader();

        const FONT_SIZE = 10;
        const LINE_HEIGHT = FONT_SIZE * 1.2;
        let currentY = 150;

        const headerInfo = [
            { label: 'Máquina:', value: machine.name },
            { label: 'Código Máquina:', value: machine.id },
            { label: 'Plan de Mantenimiento:', value: plan.name },
            { label: 'Frecuencia:', value: `Cada ${plan.frequencyValue || 1} ${ {'d': 'Día(s)', 'w': 'Semana(s)', 'm': 'Mes(es)', 'y': 'Año(s)'}[plan.frequencyUnit] || 'Mes(es)'}`},
            { label: 'Ejecutado por:', value: execution.executedBy },
            { label: 'ID Orden de Trabajo:', value: workOrder.id || 'Pendiente' },
            { label: 'Fecha de Inicio:', value: execution.executedAt ? new Date(execution.executedAt).toLocaleString('es-ES') : 'N/A' },
            { label: 'Fecha de Finalización:', value: execution.completedAt ? new Date(execution.completedAt).toLocaleString('es-ES') : 'N/A' }
        ];

        doc.autoTable({
            startY: currentY,
            head: [['Campo', 'Valor']],
            body: headerInfo.map(info => [info.label, info.value]),
            theme: 'grid',
            headStyles: { fillColor: [44, 62, 80] },
            columnStyles: { 0: { fontStyle: 'bold' } },
            margin: { top: 150, left: margin, right: margin },
            didDrawPage: (data) => {
                if (data.pageNumber > 1) {
                    drawHeader();
                }
            }
        });

        finalY = doc.autoTable.previous.finalY;

        // --- PDF Task List ---
        doc.setFontSize(12);
        doc.setFont(undefined, 'bold');
        doc.text('Detalle de Tareas Realizadas', margin, finalY + 30);

        let taskY = finalY + 50;

        (execution.taskGroups || []).forEach(group => {
            // Add space and title for the group
            if (taskY > doc.internal.pageSize.getHeight() - 150) {
                doc.addPage();
                drawHeader();
                taskY = 150;
            }

            doc.setFontSize(10);
            doc.setFont(undefined, 'bold');
            doc.text(group.name, margin, taskY);
            taskY += 15;

            const body = group.tasks.map(task => {
                let measurement = '';
                if (task.type === 'numeric') {
                    measurement = `${task.value || ''} ${task.unit || ''}`;
                } else if (task.type === 'multi_numeric') {
                    measurement = (task.fields || []).map((f, i) => {
                        const val = task.multiValues ? task.multiValues[i] || '' : '';
                        return f ? `${f}: ${val}` : val;
                    }).join('\n') + ` ${task.unit || ''}`;
                }

                return [
                    { content: task.description + (task.detail ? `\nObs: ${task.detail}` : ''), hasDetail: !!task.detail },
                    measurement,
                    task.status || 'Pendiente'
                ];
            });

            doc.autoTable({
                startY: taskY,
                head: [['Tarea', 'Medición', 'Resultado']],
                body: body,
                theme: 'grid',
                headStyles: { fillColor: [108, 117, 125] },
                styles: {
                    cellPadding: 4,
                    fontSize: 9,
                    valign: 'middle',
                    overflow: 'linebreak'
                },
                didDrawCell: (data) => {
                    if (data.column.index === 2 && data.cell.section === 'body') {
                        const status = data.cell.text[0];
                        if (status === 'Aprobó') {
                            doc.setTextColor(39, 174, 96); // Green
                        } else if (status === 'Fallo') {
                            doc.setTextColor(192, 57, 43); // Red
                        } else if (status === 'Alerta') {
                            doc.setTextColor(243, 156, 18); // Orange/Yellow
                        }
                    }
                },
                willDrawCell: (data) => {
                    if (data.column.index === 2 && data.cell.section === 'body') {
                        data.cell.styles.halign = 'left';
                        data.cell.styles.fontStyle = 'bold';
                        data.cell.styles.fontSize = 10;
                    }
                    if (data.column.index === 0 && data.cell.section === 'body' && data.cell.raw && data.cell.raw.hasDetail) {
                        data.cell.styles.fontSize = 8;
                    }
                },
                margin: { top: 150, left: margin, right: margin },
                didDrawPage: (data) => {
                    if (data.pageNumber > 1) {
                        drawHeader();
                    }
                }
            });
            taskY = doc.autoTable.previous.finalY + 25;
        });

        // Check if there's enough space for signatures on the last page
        if (taskY > doc.internal.pageSize.getHeight() - 150) {
            doc.addPage();
            drawHeader();
        }


        // --- PDF Footer (Signature lines) ---
        const signatureY = doc.internal.pageSize.getHeight() - 100;
        const contentWidth = pageWidth - (margin * 2);
        const sigWidth = (contentWidth - 40) / 3;
        const sigH = 35;
        const sigW = sigWidth - 10;

        const getSignatureByUsername = (username) => {
            if (!username) return null;
            const tech = state.technicians.find(t => t.username === username);
            return tech ? tech.signature : null;
        };

        const jefeEval = state.technicianEvaluations.find(ev => ev.workOrderFbId === execution.workOrderFbId && ev.evaluatorRole === 'Jefe de Area');
        const evaluatorName = jefeEval ? jefeEval.evaluatorId : '';

        doc.setFontSize(8);

        // Signature 1: Ejecutado por
        const sigExec = getSignatureByUsername(execution.executedBy);
        if (sigExec) {
            try { doc.addImage(sigExec, 'PNG', margin + 5, signatureY - 40, sigW, sigH); } catch(err){}
        }
        doc.line(margin, signatureY, margin + sigWidth, signatureY);
        doc.text(`Ejecutado por: ${execution.executedBy || ''}`, margin, signatureY + 15);

        // Signature 2: Jefe de Mantenimiento (WITH name)
        const sigRec = getSignatureByUsername(execution.validatedBy);
        if (sigRec) {
            try { doc.addImage(sigRec, 'PNG', margin + sigWidth + 25, signatureY - 40, sigW, sigH); } catch(err){}
        }
        doc.line(margin + sigWidth + 20, signatureY, margin + 2 * sigWidth + 20, signatureY);
        doc.text(`Jefe de Mantenimiento:`, margin + sigWidth + 20, signatureY + 15);
        doc.text(`${execution.validatedBy || ''}`, margin + sigWidth + 20, signatureY + 25);

        // Signature 3: Jefe de área
        const sigJefe = getSignatureByUsername(evaluatorName);
        if (sigJefe) {
            try { doc.addImage(sigJefe, 'PNG', margin + 2 * sigWidth + 45, signatureY - 40, sigW, sigH); } catch(err){}
        }
        doc.line(margin + 2 * sigWidth + 40, signatureY, pageWidth - margin, signatureY);
        doc.text(`Jefe de área: ${evaluatorName}`, margin + 2 * sigWidth + 40, signatureY + 15);

        // --- PDF Annexes (Photos) ---
        const photosToInclude = [];
        (execution.taskGroups || []).forEach(group => {
            group.tasks.forEach(task => {
                if (task.photos && task.photos.length > 0) {
                    task.photos.forEach(photoUrl => {
                        photosToInclude.push({
                            url: photoUrl,
                            description: task.description,
                            detail: task.detail
                        });
                    });
                }
            });
        });

        if (photosToInclude.length > 0) {
            doc.addPage();
            drawHeader();
            doc.setFontSize(14);
            doc.setFont(undefined, 'bold');
            doc.text('Anexos - Evidencia Fotográfica', margin, 150);

            let annexY = 170;
            const maxWidth = pageWidth - (margin * 2);

            for (const item of photosToInclude) {
                // Approximate space needed: description (wrap) + detail (wrap) + image (90) + margins
                // We'll calculate more accurately inside
                if (annexY > doc.internal.pageSize.getHeight() - 200) {
                    doc.addPage();
                    drawHeader();
                    annexY = 150;
                }

                doc.setFontSize(10);
                doc.setFont(undefined, 'bold');
                const descLines = doc.splitTextToSize(`Tarea: ${item.description}`, maxWidth);
                doc.text(descLines, margin, annexY);
                annexY += (descLines.length * 12);

                if (item.detail) {
                    doc.setFontSize(9);
                    doc.setFont(undefined, 'italic');
                    const detailLines = doc.splitTextToSize(`Obs: ${item.detail}`, maxWidth);
                    doc.text(detailLines, margin, annexY);
                    annexY += (detailLines.length * 11);
                }
                annexY += 5;

                try {
                    const imgResult = await imageUrlToBase64(item.url);
                    if (imgResult) {
                        const maxW = 350; // Aumentado de 130 a 350 para mejor visualización
                        const maxH = 250; // Aumentado de 90 a 250

                        let finalW = imgResult.width;
                        let finalH = imgResult.height;
                        const ratio = Math.min(maxW / finalW, maxH / finalH);

                        finalW = finalW * ratio;
                        finalH = finalH * ratio;

                        // Centrar la imagen en el ancho de la página
                        const centerX = margin + (maxWidth - finalW) / 2;
                        doc.addImage(imgResult.data, 'JPEG', centerX, annexY, finalW, finalH);
                        annexY += finalH + 15;
                    } else {
                        doc.setTextColor(180, 180, 180);
                        doc.setFontSize(8);
                        doc.text("[La imagen no se pudo cargar en el reporte. Verifique la imagen en el sistema.]", margin, annexY);
                        doc.setTextColor(0, 0, 0);
                        annexY += 12;
                    }
                } catch (err) {
                    console.error("Error adding image to PDF:", err);
                    doc.text("[Error al procesar imagen]", margin, annexY);
                    annexY += 15;
                }
                annexY += 20;
            }
        }

        // --- Save the PDF ---
        const drawFooter = () => {
            const pageWidth = doc.internal.pageSize.getWidth();
            const pageCount = doc.internal.getNumberOfPages();
            for (let i = 1; i <= pageCount; i++) {
                doc.setPage(i);
                doc.setFontSize(8);
                doc.setFont(undefined, 'normal');
                const footerY = doc.internal.pageSize.getHeight() - 20;
                doc.text('FO-MNDO-005 Reporte de trabajo de mantenimiento preventivo de equipo', margin, footerY);
                doc.text(`Copia Controlada Página  ${i} de ${pageCount}`, pageWidth - margin, footerY, { align: 'right' });
            }
        };
        drawFooter();
        doc.save(`Reporte_Ejecucion_${plan.name.replace(/\s/g, '_')}_${new Date(execution.executedAt).toLocaleDateString('es-ES').replace(/\//g, '-')}.pdf`);
        showToast('Reporte PDF generado correctamente.', 'success');


    } catch (error) {
        console.error("Error al generar el reporte de ejecución:", error);
        showToast(error.message, 'error');
    } finally {
        showLoading(false);
    }
}

function handlePeriodChange() {
    const period = document.getElementById('dashboard-period-select').value;
    const dateSelect = document.getElementById('dashboardDate');
    const weekSelect = document.getElementById('dashboardWeek');
    const monthSelect = document.getElementById('dashboardMonth');
    const yearSelect = document.getElementById('dashboardYear');

    dateSelect.style.display = 'none';
    weekSelect.style.display = 'none';
    monthSelect.style.display = 'none';
    yearSelect.style.display = 'none';

    if (period === 'day') {
        dateSelect.style.display = 'inline-block';
    } else if (period === 'week') {
        populateWeekSelector();
        weekSelect.style.display = 'inline-block';
        monthSelect.style.display = 'inline-block';
        yearSelect.style.display = 'inline-block';
    } else if (period === 'month') {
        monthSelect.style.display = 'inline-block';
        yearSelect.style.display = 'inline-block';
    } else if (period === 'year') {
        yearSelect.style.display = 'inline-block';
    }
    updateDashboardData();
}

function getDashboardPeriod() {
    const period = document.getElementById('dashboard-period-select').value;
    let startDate, endDate, periodLabel;

    const dateSelect = document.getElementById('dashboardDate');
    const weekSelect = document.getElementById('dashboardWeek');
    const monthSelect = document.getElementById('dashboardMonth');
    const yearSelect = document.getElementById('dashboardYear');

    switch (period) {
        case 'day' :
            const selectedDateVal = dateSelect.value;
            if (!selectedDateVal) {
                const now = new Date();
                startDate = new Date(now.getFullYear(), now.getMonth(), now.getDate());
            } else {
                const selectedDate = new Date(selectedDateVal + 'T00:00:00');
                startDate = new Date(selectedDate.getFullYear(), selectedDate.getMonth(), selectedDate.getDate());
            }
            endDate = new Date(startDate.getFullYear(), startDate.getMonth(), startDate.getDate(), 23, 59, 59);
            periodLabel = startDate.toLocaleDateString('es-ES', { day: 'numeric', month: 'long' });
            break;
        case 'week' :
             if (weekSelect.value) {
                const [weekStartStr, weekEndStr] = weekSelect.value.split('|');
                startDate = new Date(weekStartStr + 'T00:00:00');
                endDate = new Date(weekEndStr + 'T23:59:59');
                periodLabel = `Semana del ${startDate.toLocaleDateString('es-ES')}`;
            } else { // Fallback
                const now = new Date();
                const firstDayOfWeek = now.getDate() - now.getDay();
                startDate = new Date(now.getFullYear(), now.getMonth(), firstDayOfWeek);
                endDate = new Date(startDate.getFullYear(), startDate.getMonth(), startDate.getDate() + 6, 23, 59, 59);
                periodLabel = 'Esta Semana';
            }
            break;
        case 'year' :
            const yearForYear = parseInt(yearSelect.value);
            startDate = new Date(yearForYear, 0, 1);
            endDate = new Date(yearForYear, 11, 31, 23, 59, 59);
            periodLabel = `Año ${yearForYear}`;
            break;
        case 'month' :
        default:
            const month = parseInt(monthSelect.value);
            const yearForMonth = parseInt(yearSelect.value);
            startDate = new Date(yearForMonth, month, 1);
            endDate = new Date(yearForMonth, month + 1, 0, 23, 59, 59);
            const monthName = monthSelect.options[monthSelect.selectedIndex].text;
            periodLabel = `${monthName} ${yearForMonth}`;
            break;
    }
    return { startDate, endDate, periodLabel };
}

const dashboardCache = {
    lastDataHash: null,
    lastPeriod: null
};

function getDashboardDataHash(orders, solicitudes, partRequests, startDate, endDate) {
    // Resumen de estados para detectar cambios sin necesidad de un hash gigante
    const woStatusSummary = orders.reduce((acc, o) => {
        acc[o.status] = (acc[o.status] || 0) + 1;
        return acc;
    }, {});
    const solStatusSummary = solicitudes.reduce((acc, s) => {
        acc[s.status] = (acc[s.status] || 0) + 1;
        return acc;
    }, {});
    const partReqStatusSummary = partRequests.reduce((acc, r) => {
        acc[r.status] = (acc[r.status] || 0) + 1;
        return acc;
    }, {});

    // Incluir máquinas en el hash para actualizar el gráfico de criticidad
    const machineCritSummary = state.machines.reduce((acc, m) => {
        const crit = m.criticidad || 'Baja';
        acc[crit] = (acc[crit] || 0) + 1;
        return acc;
    }, {});

    const liveCountersHash = JSON.stringify(state.liveCounters || {});

    return `${JSON.stringify(woStatusSummary)}-${JSON.stringify(solStatusSummary)}-${JSON.stringify(partReqStatusSummary)}-${JSON.stringify(machineCritSummary)}-${liveCountersHash}-${startDate.getTime()}-${endDate.getTime()}`;
}

let initialLoadComplete = false;

async function updateDashboardData(force = false) {
    if (state.currentTab !== 'dashboard' && !force && initialLoadComplete) return;

    // Si el wrapper de la app está oculto, no intentamos actualizar gráficos (evita errores de tamaño en Chart.js)
    if (document.getElementById('app-wrapper').classList.contains('d-none')) return;

    if (force) {
        dashboardCache.lastDataHash = null;
        dashboardCache.lastPeriod = null;
    }

    const { startDate, endDate, periodLabel } = getDashboardPeriod();

    document.querySelectorAll('.period-label').forEach(el => {
        el.textContent = `(${periodLabel})`;
    });

    const userRole = getCurrentUser().role;
    const managedMachineIds = getCurrentUser().managedMachineIds;
    const equipoAsignado = getCurrentUser().equipoAsignado;
    const hasMachineFilter = (userRole === 'Jefe de Area') || (['Técnico', 'Operario', 'Supervisor de Area'].includes(userRole));
    let relevantMachineIds = null;
    if (hasMachineFilter) {
        const ids = userRole === 'Jefe de Area' ? (managedMachineIds || []) : (equipoAsignado || []);
        relevantMachineIds = new Set(Array.isArray(ids) ? ids : []);
    }

    const startIso = startDate.toISOString().split('T')[0];
    const endIso = endDate.toISOString().split('T')[0];

    // Optimized single filter pass for dashboard data
    const ordersThisPeriod = state.workOrders.filter(o => {
        if (!o.date) return false;
        if (relevantMachineIds && (!o.machineId || !relevantMachineIds.has(o.machineId))) return false;
        return o.date >= startIso && o.date <= endIso;
    });

    const solicitudesThisPeriod = state.solicitudes.filter(s => {
        if (!s.createdAt) return false;
        if (relevantMachineIds && (!s.machineId || !relevantMachineIds.has(s.machineId))) return false;
        const createdDate = s.createdAt.split('T')[0];
        return createdDate >= startIso && createdDate <= endIso;
    });

    const partRequestsThisPeriod = state.partRequests.filter(r => {
        if (!r.date) return false;
        if (relevantMachineIds && r.machineId && r.machineId !== 'General' && !relevantMachineIds.has(r.machineId)) return false;
        const reqDate = r.date.split('T')[0];
        return reqDate >= startIso && reqDate <= endIso;
    });

    const dataHash = getDashboardDataHash(ordersThisPeriod, solicitudesThisPeriod, partRequestsThisPeriod, startDate, endDate);
    const selectedMachine = document.getElementById('kpi-machine-select').value || 'all';

    // Incluir estado de visibilidad en el hash para asegurar renderizado cuando la app se vuelve visible
    const isVisible = !document.getElementById('app-wrapper').classList.contains('d-none');
    const fullHash = `${dataHash}-${selectedMachine}-${isVisible}`;

    if (dashboardCache.lastDataHash === fullHash && dashboardCache.lastPeriod === periodLabel) {
        updatePlanNotifications();
        return;
    }

    dashboardCache.lastDataHash = fullHash;
    dashboardCache.lastPeriod = periodLabel;

    const kpis = calculateKpisForPeriod(ordersThisPeriod, startDate, endDate);
    updateStats(ordersThisPeriod, kpis, solicitudesThisPeriod, partRequestsThisPeriod);
    updateKpis(ordersThisPeriod, startDate, endDate, kpis);
    updateCharts(ordersThisPeriod);
    updatePlanNotifications();
    if (!initialLoadComplete) {
        migratePartRequestIds();
    }
    initialLoadComplete = true;
}

function updatePlanNotifications() {
    if (!state.currentUser) return;

    const userRole = getCurrentUser().role;
    const username = getCurrentUser().username;
    const notificationsList = document.getElementById('notifications-list');
    const notificationsCount = document.getElementById('notifications-count');

    if (!notificationsList || !notificationsCount) return;

    const now = new Date();
    now.setHours(0, 0, 0, 0);

    const alerts = [];

    state.workPlans.forEach(plan => {
        if (!plan.isActive || !plan.nextDueDate) return;

        // Permission check: Admin, Planificador or the assigned technician
        const isAuthorized = userRole === 'Admin' || userRole === 'Planificador' || plan.responsibleTechnician === username;
        if (!isAuthorized) return;

        const dueDate = new Date(plan.nextDueDate + 'T00:00:00');
        const diffTime = dueDate.getTime() - now.getTime();
        const diffDays = Math.round(diffTime / (1000 * 60 * 60 * 24));

        let type = '';
        let message = '';
        let icon = '';
        let badgeClass = '';

        if (diffDays < 0) {
            type = 'Vencido';
            message = `El plan "${plan.name}" está vencido desde el ${new Date(dueDate).toLocaleDateString()}.`;
            icon = 'fa-exclamation-triangle';
            badgeClass = 'bg-danger';
        } else if (diffDays === 0) {
            type = 'Hoy';
            message = `El plan "${plan.name}" debe ejecutarse hoy.`;
            icon = 'fa-clock';
            badgeClass = 'bg-warning text-dark';
        } else if (diffDays <= 3) {
            type = 'Próximo';
            message = `El plan "${plan.name}" vence en ${diffDays} ${diffDays === 1 ? 'día' : 'días'} (${new Date(dueDate).toLocaleDateString()}).`;
            icon = 'fa-info-circle';
            badgeClass = 'bg-info text-dark';
        }

        if (type) {
            alerts.push({
                planId: plan.fb_id,
                name: plan.name,
                type,
                message,
                icon,
                badgeClass,
                dueDate: plan.nextDueDate
            });
        }
    });

    // Sort alerts: Vencido first, then Hoy, then Próximo
    alerts.sort((a, b) => {
        const priority = { 'Vencido': 1, 'Hoy': 2, 'Próximo': 3 };
        return priority[a.type] - priority[b.type] || new Date(a.dueDate) - new Date(b.dueDate);
    });

    notificationsCount.textContent = alerts.length;
    notificationsCount.classList.toggle('d-none', alerts.length === 0);

    if (alerts.length === 0) {
        notificationsList.innerHTML = '<li class="p-3 text-center text-muted">No hay notificaciones</li>';
    } else {
        notificationsList.innerHTML = '';
        alerts.forEach(alert => {
            const li = document.createElement('li');
            li.className = 'notification-item p-2 border-bottom';
            li.style.cursor = 'pointer';
            li.onclick = () => window.scrollToPlan(alert.planId);

            const content = `
                <div class="d-flex align-items-start">
                    <div class="rounded-circle ${alert.badgeClass} p-2 me-2 d-flex align-items-center justify-content-center" style="width: 32px; height: 32px; flex-shrink: 0;">
                        <i class="fas ${alert.icon} fa-sm"></i>
                    </div>
                    <div class="flex-grow-1 overflow-hidden">
                        <div class="fw-bold small d-flex justify-content-between align-items-center">
                            <span>${alert.type}</span>
                            <span class="text-muted" style="font-size: 0.7rem;">${alert.dueDate}</span>
                        </div>
                        <p class="mb-0 small text-truncate-2" id="notif-msg-${alert.planId}"></p>
                    </div>
                </div>
            `;
            li.innerHTML = content;
            li.querySelector(`#notif-msg-${alert.planId}`).textContent = alert.message;
            notificationsList.appendChild(li);
        });
    }
}

function scrollToPlan(planId) {
    switchTab('planes-trabajo');

    // Small delay to allow rendering
    setTimeout(() => {
        const planEl = document.querySelector(`[data-plan-id="${planId}"]`);
        if (planEl) {
            planEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
            planEl.classList.add('highlight-plan');
            setTimeout(() => planEl.classList.remove('highlight-plan'), 3000);
        }
    }, 100);
}

window.scrollToPlan = scrollToPlan;


// --- Dashboard & Charts ---
function getPreciseUptimeMs(machine, startDate, endDate) {
    if (!startDate || !endDate || startDate >= endDate) return 0;

    let totalMs = 0;
    let current = new Date(startDate);

    while (current < endDate) {
        const day = current.getDay();
        let schedule = null;

        if (machine.scheduleDisabled) {
            schedule = { startTime: "00:00", endTime: "24:00" };
        } else if (!machine.schedule) {
            schedule = { startTime: "08:00", endTime: "18:00" };
        } else {
            if (day >= 1 && day <= 5) {
                if (machine.schedule.weekday.activeDays.includes(day.toString())) {
                    schedule = machine.schedule.weekday;
                }
            } else if (day === 6 && machine.schedule.saturday.active) {
                schedule = machine.schedule.saturday;
            } else if (day === 0 && machine.schedule.sunday.active) {
                schedule = machine.schedule.sunday;
            }
        }

        const nextDay = new Date(current);
        nextDay.setDate(nextDay.getDate() + 1);
        nextDay.setHours(0, 0, 0, 0);
        const dayEnd = nextDay < endDate ? nextDay : endDate;

        if (schedule) {
            const [sH, sM] = schedule.startTime.split(':').map(Number);
            const [eH, eM] = schedule.endTime.split(':').map(Number);

            const schedStart = new Date(current);
            schedStart.setHours(sH || 0, sM || 0, 0, 0);

            const schedEnd = new Date(current);
            if (eH === 24) {
                schedEnd.setDate(schedEnd.getDate() + 1);
                schedEnd.setHours(0, 0, 0, 0);
            } else {
                schedEnd.setHours(eH || 0, eM || 0, 0, 0);
            }

            const effectiveStart = Math.max(current, schedStart);
            const effectiveEnd = Math.min(dayEnd, schedEnd);

            if (effectiveEnd > effectiveStart) {
                totalMs += (effectiveEnd - effectiveStart);
            }
        }
        current = dayEnd;
    }
    return totalMs;
}

function getMachineDailyUptimeMs(machine) {
    if (machine.scheduleDisabled) return 24 * 60 * 60 * 1000;
    if (!machine.schedule) return 10 * 60 * 60 * 1000;

    const getUptimeFromSched = (s) => {
        if (!s || !s.startTime || !s.endTime) return 0;
        const [sH, sM] = s.startTime.split(':').map(Number);
        const [eH, eM] = s.endTime.split(':').map(Number);
        let start = (sH || 0) * 60 + (sM || 0);
        let end = (eH || 0) * 60 + (eM || 0);
        if (eH === 24) end = 24 * 60;
        return Math.max(0, (end - start) * 60 * 1000);
    };

    const weekday = getUptimeFromSched(machine.schedule.weekday);
    if (weekday > 0) return weekday;

    const sat = getUptimeFromSched(machine.schedule.saturday);
    if (sat > 0) return sat;

    const sun = getUptimeFromSched(machine.schedule.sunday);
    if (sun > 0) return sun;

    return 10 * 60 * 60 * 1000;
}

function calculateScheduledUptimeMs(machines, startDate, endDate) {
    let totalScheduledMs = 0;

    // Normalize dates to avoid time-of-day issues
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    const end = new Date(endDate);
    end.setHours(0, 0, 0, 0);

    const diffDays = Math.round((end - start) / (24 * 60 * 60 * 1000)) + 1;
    if (diffDays <= 0) return 0;

    // Pre-calculate day counts for the range to avoid redundant loops
    const dayCounts = new Array(7).fill(0);
    const fullWeeks = Math.floor(diffDays / 7);
    for (let i = 0; i < 7; i++) dayCounts[i] = fullWeeks;

    const remainingDays = diffDays % 7;
    let tempDay = start.getDay();
    for (let i = 0; i < remainingDays; i++) {
        dayCounts[tempDay]++;
        tempDay = (tempDay + 1) % 7;
    }

    const getUptimeFromSchedule = (sched) => {
        if (!sched || !sched.startTime || !sched.endTime) return 0;
        const [startH, startM] = sched.startTime.split(':').map(Number);
        const [endH, endM] = sched.endTime.split(':').map(Number);
        return Math.max(0, ((endH * 60 + endM) - (startH * 60 + startM)) * 60 * 1000);
    };

    for (const machine of machines) {
        if (machine.scheduleDisabled) continue;

        if (!machine.schedule) {
            totalScheduledMs += diffDays * 10 * 60 * 60 * 1000; // Default 10h if no schedule
            continue;
        }

        // Mon-Fri
        const weekdayUptime = getUptimeFromSchedule(machine.schedule.weekday);
        if (weekdayUptime > 0 && machine.schedule.weekday.activeDays) {
            machine.schedule.weekday.activeDays.forEach(dayStr => {
                const dayIdx = parseInt(dayStr);
                if (dayIdx >= 1 && dayIdx <= 5) {
                    totalScheduledMs += dayCounts[dayIdx] * weekdayUptime;
                }
            });
        }

        // Saturday
        if (machine.schedule.saturday.active) {
            totalScheduledMs += dayCounts[6] * getUptimeFromSchedule(machine.schedule.saturday);
        }

        // Sunday
        if (machine.schedule.sunday.active) {
            totalScheduledMs += dayCounts[0] * getUptimeFromSchedule(machine.schedule.sunday);
        }
    }
    return totalScheduledMs;
}

function calculateTotalCost(order, returnParts = false, partsMap = null, techMap = null) {
    let partsCost = 0;
    if (order.partsUsed && order.partsUsed.length > 0) {
        partsCost = order.partsUsed.reduce((sum, partUsage) => {
            if (partUsage.delivered === false) return sum;
            const part = partsMap ? partsMap.get(partUsage.partId) : state.parts.find(p => p.id === partUsage.partId);
            return sum + (part ? (part.cost || 0) * partUsage.quantity : 0);
        }, 0);
    }

    const totalWorkMs = getTotalWorkDurationMs(order);
    const hoursWorked = totalWorkMs / (1000 * 60 * 60);
    
    let laborCost = 0;
    const techniciansOnOrder = order.technicians || [];
    if (techniciansOnOrder.length > 0 && hoursWorked > 0) {
        laborCost = techniciansOnOrder.reduce((sum, techUsername) => {
            const technician = techMap ? techMap.get(techUsername) : state.technicians.find(t => t.username === techUsername);
            if (technician && technician.generaGasto !== false && technician.salario) {
                const hourlyRate = technician.salario / 160;
                return sum + (hoursWorked * hourlyRate);
            }
            return sum;
        }, 0);
    }

    const totalCost = partsCost + laborCost + (Number(order.externalCost) || 0);
    return returnParts ? { partsCost, laborCost, totalCost } : totalCost;
}

function calculateTotalCostForMultiple(orders) {
    // Optimization: Pre-calculate maps for faster lookups
    const partsMap = new Map(state.parts.map(p => [p.id, p]));
    const techMap = new Map(state.technicians.map(t => [t.username, t]));

    return orders.reduce((totals, order) => {
        // Inline partially calculateTotalCost logic with pre-calculated maps
        let partsCost = 0;
        if (order.partsUsed && order.partsUsed.length > 0) {
            partsCost = order.partsUsed.reduce((sum, partUsage) => {
                if (partUsage.delivered === false) return sum;
                const part = partsMap.get(partUsage.partId);
                return sum + (part ? (part.cost || 0) * partUsage.quantity : 0);
            }, 0);
        }

        const totalWorkMs = getTotalWorkDurationMs(order);
        const hoursWorked = totalWorkMs / (1000 * 60 * 60);

        let laborCost = 0;
        const techniciansOnOrder = order.technicians || [];
        if (techniciansOnOrder.length > 0 && hoursWorked > 0) {
            laborCost = techniciansOnOrder.reduce((sum, techUsername) => {
                const technician = techMap.get(techUsername);
                if (technician && technician.generaGasto !== false && technician.salario) {
                    const hourlyRate = technician.salario / 160;
                    return sum + (hoursWorked * hourlyRate);
                }
                return sum;
            }, 0);
        }

        const totalCost = partsCost + laborCost + (order.externalCost || order.additionalCost || 0);

        totals.partsCost += partsCost;
        totals.laborCost += laborCost;
        totals.externalCost += (order.externalCost || order.additionalCost || 0);
        totals.totalCost += totalCost;
        return totals;
    }, { partsCost: 0, laborCost: 0, externalCost: 0, totalCost: 0 });
}


function updateMachineCriticidadChart() {
    if (!state.charts.machineCriticidad) {
        initCharts();
    }
    if (!state.charts.machineCriticidad) return;

    let machinesToCount = state.machines;
    const userRole = getCurrentUser().role;
    const hasRestrictiveRole = (userRole === 'Jefe de Area') || (['Técnico', 'Operario', 'Supervisor de Area'].includes(userRole));
    if (hasRestrictiveRole) {
        const ids = userRole === 'Jefe de Area' ? (getCurrentUser().managedMachineIds || []) : (getCurrentUser().equipoAsignado || []);
        const relevantMachineIds = new Set(Array.isArray(ids) ? ids : []);
        machinesToCount = state.machines.filter(m => relevantMachineIds.has(m.id));
    }

    const counts = { 'Baja': 0, 'Media': 0, 'Alta': 0 };
    machinesToCount.forEach(m => {
        const crit = m.criticidad || 'Baja';
        if (counts.hasOwnProperty(crit)) {
            counts[crit]++;
        } else {
            counts['Baja']++;
        }
    });

    state.charts.machineCriticidad.data.datasets[0].data = [counts['Baja'], counts['Media'], counts['Alta']];
    state.charts.machineCriticidad.update();

    const legendContainer = document.getElementById('machine-criticidad-legend');
    if (legendContainer) {
        legendContainer.innerHTML = `
            <div class="legend-item"><div class="legend-color" style="background-color: #2ecc71;"></div>Baja: ${counts['Baja']}</div>
            <div class="legend-item"><div class="legend-color" style="background-color: #f39c12;"></div>Media: ${counts['Media']}</div>
            <div class="legend-item"><div class="legend-color" style="background-color: #e74c3c;"></div>Alta: ${counts['Alta']}</div>
        `;
    }

    const statMaquinas = document.getElementById('stat-maquinas');
    if (statMaquinas) statMaquinas.textContent = machinesToCount.length;
}

function updateStats(ordersThisPeriod, kpis, solicitudesThisPeriod, partRequestsThisPeriod) {
    updateMachineCriticidadChart();

    // Prioritize Live RTDB Counters for instant UI response ONLY if no specific period is selected (default dashboard view)
    const { periodLabel } = getDashboardPeriod();
    const now = new Date();
    const currentMonthLabel = `${now.toLocaleDateString('es-ES', { month: 'long' })} ${now.getFullYear()}`;
    const isCurrentMonth = periodLabel.toLowerCase() === currentMonthLabel.toLowerCase();

    const lc = state.liveCounters || {};
    let pendingCount = 0;

    if (isCurrentMonth && lc.pendingSolicitudes !== undefined && !solicitudesThisPeriod) {
        pendingCount = lc.pendingSolicitudes;
    } else {
        // Fallback or specific period filtering
        const sols = solicitudesThisPeriod || state.solicitudes;
        const partReqs = partRequestsThisPeriod || state.partRequests;

        const pendingSols = sols.filter(s => {
            // Maintenance request is pending if status is Pendiente AND it doesn't have a linked WO or the linked WO is still pending/draft
            if (s.status !== 'Pendiente') return false;

            // Check if there is a linked work order
            let linkedWO = state.workOrders.find(wo =>
                wo.fb_id === s.workOrderFbId ||
                wo.id === s.workOrderId ||
                wo.sourceSolicitudId === s.fb_id ||
                (wo.solicitudId === s.id && s.id)
            );

            if (!linkedWO) return true;
            return false;
        }).length;

        const pendingPartReqs = partReqs.filter(r => r.status === 'Pendiente').length;
        pendingCount = pendingSols + pendingPartReqs;
    }

    document.getElementById('stat-solicitudes').textContent = pendingCount;
    
    let preventiveCount = 0;
    let correctiveCount = 0;

    ordersThisPeriod.forEach(o => {
        if (!o) return;
        const fbId = o.fb_id || o.id;
        if (!fbId) return;
        const cleanId = String(fbId).replace(/[.#$\[\]]/g, '_');
        const mirror = state.liveWorkOrders?.[cleanId];
        const currentType = mirror?.type || o.type;
        const currentStatus = mirror?.status || o.status;

        // Filtrar órdenes canceladas, rechazadas o expiradas del conteo del Dashboard
        if (currentStatus === 'Cancelado' || currentStatus === 'Rechazado' || o.isExpired) return;

        if (['Preventivo', 'Predictivo', 'Calibración', 'Mecanizado'].includes(currentType)) preventiveCount++;
        else if (['Correctivo', 'Emergencia'].includes(currentType)) correctiveCount++;
    });


    document.getElementById('stat-preventivos').textContent = preventiveCount;
    document.getElementById('stat-correctivos').textContent = correctiveCount;
    
    const f = new Intl.NumberFormat('es-HN', { style: 'currency', currency: 'HNL' });

    document.getElementById('stat-gasto-ejecutado').textContent = f.format(kpis.totalExecutedCost || 0);
    document.getElementById('stat-gasto-planificado').textContent = f.format(kpis.totalPlannedCost || 0);
}

function calculateKpisForPeriod(ordersInPeriod, startDate, endDate, machineId = 'all') {
    const periodOrders = (machineId === 'all' ? ordersInPeriod : ordersInPeriod.filter(wo => wo.machineId === machineId))
        .filter(wo => wo.status !== 'Cancelado' && wo.status !== 'Rechazado' && !wo.isExpired);

    const kpis = {
        mtbf: null,
        mttr: null,
        availability: null,
        pmp: null,
        avgCost: null,
        otsCompleted: 0,
        otsPlanned: periodOrders.length,
        totalExecutedCost: 0,
        totalPlannedCost: 0
    };

    const correctiveTypes = ['Correctivo', 'Emergencia'];
    const preventiveTypes = ['Preventivo', 'Predictivo', 'Calibración', 'Mecanizado'];

    let correctiveCount = 0;
    let repairOrdersForPeriod = [];
    let preventivePlannedCount = 0;
    let preventiveCompletedCount = 0;
    let completedOrders = [];

    // Single loop to categorize all orders in period
    for (const o of periodOrders) {
        const isCorrective = correctiveTypes.includes(o.type);
        const isPreventive = preventiveTypes.includes(o.type);
        const isCompleted = o.status === 'Completado';

        if (isCorrective) {
            correctiveCount++;
            if (isCompleted) repairOrdersForPeriod.push(o);
        }
        if (isPreventive) {
            preventivePlannedCount++;
            if (isCompleted) preventiveCompletedCount++;
        }
        if (isCompleted) {
            completedOrders.push(o);
            kpis.otsCompleted++;
        }
    }

    // MTBF Calculation
    const now = new Date();
    const isCurrentPeriod = endDate >= now;

    if (machineId !== 'all') {
        const machine = state.machines.find(m => m.id === machineId);
        if (machine) {
            const dailyUptimeMs = getMachineDailyUptimeMs(machine);
            if (isCurrentPeriod) {
                // Dynamic counter: Uptime since last failure
                const lastFailure = state.workOrders
                    .filter(o => o.machineId === machineId && correctiveTypes.includes(o.type) && o.status !== 'Cancelado' && o.status !== 'Rechazado')
                    .sort((a, b) => new Date(b.createdAt || b.date) - new Date(a.createdAt || a.date))[0];

                const calcStart = lastFailure ? new Date(lastFailure.createdAt || lastFailure.date) : (machine.createdAt ? new Date(machine.createdAt) : startDate);
                const uptimeMs = getPreciseUptimeMs(machine, calcStart, now);
                kpis.mtbf = dailyUptimeMs > 0 ? uptimeMs / dailyUptimeMs : 0;
            } else {
                // Past period: Average of intervals in period
                const scheduledUptimeMs = getPreciseUptimeMs(machine, startDate, endDate);
                if (scheduledUptimeMs > 0) {
                    const mtbfMs = scheduledUptimeMs / (correctiveCount + 1);
                    kpis.mtbf = dailyUptimeMs > 0 ? mtbfMs / dailyUptimeMs : 0;
                }
            }
        }
    } else {
        // Global logic (unchanged as requested)
        const effectiveEndDate = endDate > now ? now : endDate;
        const timeElapsedMs = Math.max(0, effectiveEndDate - startDate);
        if (timeElapsedMs > 0) {
            const mtbfMs = timeElapsedMs / (correctiveCount + 1);
            kpis.mtbf = mtbfMs / (1000 * 60 * 60 * 24);
        }
    }

    // MTTR Calculation
    let totalRepairTimeMs = 0;
    if (repairOrdersForPeriod.length > 0) {
        totalRepairTimeMs = repairOrdersForPeriod.reduce((sum, o) => sum + (o.totalWorkDurationMs || getTotalWorkDurationMs(o)), 0);
        kpis.mttr = (totalRepairTimeMs / repairOrdersForPeriod.length) / (1000 * 60 * 60);
    }

    // Availability
    let machinesForKpi = machineId === 'all' ? state.machines : [state.machines.find(m => m.id === machineId)].filter(Boolean);
    if (getCurrentUser().role === 'Jefe de Area' && Array.isArray(getCurrentUser().managedMachineIds)) {
        const managedIds = new Set(getCurrentUser().managedMachineIds);
        machinesForKpi = machinesForKpi.filter(m => managedIds.has(m.id));
    }

    if (machinesForKpi.length > 0) {
        const scheduledUptimeMs = calculateScheduledUptimeMs(machinesForKpi, startDate, endDate);
        if (scheduledUptimeMs > 0) {
            const actualUptimeMs = Math.max(0, scheduledUptimeMs - totalRepairTimeMs);
            kpis.availability = (actualUptimeMs / scheduledUptimeMs) * 100;
        }
    }

    // PMP Calculation
    if (preventivePlannedCount > 0) {
        kpis.pmp = (preventiveCompletedCount / preventivePlannedCount) * 100;
    }

    // Cost Calculation - Optimized to single pass inside calculateTotalCostForMultiple
    if (completedOrders.length > 0) {
        const { totalCost } = calculateTotalCostForMultiple(completedOrders);
        kpis.avgCost = totalCost / completedOrders.length;
        kpis.totalExecutedCost = totalCost;
    }

    const { totalCost: totalPlannedCost } = calculateTotalCostForMultiple(periodOrders);
    kpis.totalPlannedCost = totalPlannedCost;

    return kpis;
}

function getPreviousPeriod(startDate, endDate) {
    const diff = endDate.getTime() - startDate.getTime();
    const prevEndDate = new Date(startDate.getTime() - 1);
    const prevStartDate = new Date(prevEndDate.getTime() - diff);
    return { prevStartDate, prevEndDate };
}

function updateKpis(ordersInPeriod, startDate, endDate, allMachineKpis = null) {
    const machineId = document.getElementById('kpi-machine-select').value;

    const currentKpis = (machineId === 'all' && allMachineKpis)
        allMachineKpis
        : calculateKpisForPeriod(ordersInPeriod, startDate, endDate, machineId);

    // Get previous period data
    const { prevStartDate, prevEndDate } = getPreviousPeriod(startDate, endDate);
    const prevPeriodOrders = state.workOrders.filter(o => {
        if (!o.date) return false;
        const orderDate = new Date(o.date + 'T12:00:00Z');
        return orderDate >= prevStartDate && orderDate <= prevEndDate;
    });
    const prevKpis = calculateKpisForPeriod(prevPeriodOrders, prevStartDate, prevEndDate, machineId);

    // Update UI
    document.getElementById('kpi-mtbf').textContent = currentKpis.mtbf ? currentKpis.mtbf.toFixed(2) : 'N/A';
    document.getElementById('kpi-mttr').textContent = currentKpis.mttr ? currentKpis.mttr.toFixed(2) : 'N/A';
    document.getElementById('kpi-availability').textContent = currentKpis.availability ? `${currentKpis.availability.toFixed(2)}%` : 'N/A';
    document.getElementById('kpi-pmp').textContent = currentKpis.pmp ? `${currentKpis.pmp.toFixed(2)}%` : 'N/A';
    document.getElementById('kpi-cost').textContent = currentKpis.avgCost ? new Intl.NumberFormat('es-HN', { style: 'currency', currency: 'HNL' }).format(currentKpis.avgCost) : 'N/A';

    // Update comparison texts
    updateComparison('mtbf', currentKpis.mtbf, prevKpis.mtbf, true); // Higher is better
    updateComparison('mttr', currentKpis.mttr, prevKpis.mttr, false); // Lower is better
    updateComparison('availability', currentKpis.availability, prevKpis.availability, true); // Higher is better

    updateComparison('pmp', currentKpis.pmp, prevKpis.pmp, true); // Higher is better

    updateComparison('cost', currentKpis.avgCost, prevKpis.avgCost, false); // Lower is better
}

function updateComparison(kpiName, current, previous, higherIsBetter) {
    const element = document.getElementById(`kpi-${kpiName}-comparison`);
    if (!element) return;

    if (current === null || previous === null || previous === 0) {
        element.innerHTML = '';
        return;
    }

    const diff = current - previous;
    const percentageChange = (diff / previous) * 100;

    let className = 'neutral';
    let icon = '<i class="fas fa-minus icon"></i>';

    if (percentageChange > 0) {
        className = higherIsBetter ? 'positive' : 'negative';
        icon = higherIsBetter ? '<i class="fas fa-arrow-up icon"></i>' : '<i class="fas fa-arrow-down icon"></i>';
    } else if (percentageChange < 0) {
        className = higherIsBetter ? 'negative' : 'positive';
        icon = higherIsBetter ? '<i class="fas fa-arrow-down icon"></i>' : '<i class="fas fa-arrow-up icon"></i>';
    }

    const periodLabel = document.getElementById('dashboard-period-select').selectedOptions[0].text.toLowerCase();

    element.className = `kpi-comparison ${className}`;
    element.innerHTML = `${icon} ${percentageChange.toFixed(1)}% vs ${periodLabel} anterior`;
}

function updateCharts(ordersForPeriod) {
    if (!state.charts.maintenance) {
        initCharts();
    }
    if (!state.charts.maintenance) return;

    let preventiveCount = 0;
    let correctiveCount = 0;
    const failureCounts = { 'Mecánica': 0, 'Eléctrica': 0, 'Electrónica': 0, 'Falla de Operación': 0 };
    const statusCounts = { 'Pendiente': 0, 'En Proceso': 0, 'Pausado': 0, 'Pendiente de Evaluación': 0, 'Completado': 0, 'Cancelado': 0 };

    ordersForPeriod.forEach(o => {
        if (!o) return;
        const fbId = o.fb_id || o.id;
        if (!fbId) return;

        const cleanId = String(fbId).replace(/[.#$\[\]]/g, '_');
        const mirror = state.liveWorkOrders?.[cleanId];
        const currentType = mirror?.type || o.type;
        const currentStatus = mirror?.status || o.status;

        // Excluir canceladas, rechazadas o expiradas de los gráficos principales de volumen y tipo
        if (currentStatus === 'Cancelado' || currentStatus === 'Rechazado' || o.isExpired) {
            statusCounts['Cancelado']++;
            return;
        }

        if (['Preventivo', 'Predictivo', 'Calibración', 'Mecanizado'].includes(currentType)) preventiveCount++;
        else if (['Correctivo', 'Emergencia'].includes(currentType)) correctiveCount++;

        if (currentType === 'Calibración') {
            failureCounts['Falla de Operación']++;
        } else if (o.failureType && failureCounts.hasOwnProperty(o.failureType)) {
            failureCounts[o.failureType]++;
        }

        let statusKey = currentStatus;
        if (statusKey === 'Pendiente de Aprobación') statusKey = 'Pendiente de Evaluación';
        if (statusCounts.hasOwnProperty(statusKey)) {
            statusCounts[statusKey]++;
        }
    });

    // Actualizar datos y forzar redibujado
    if (state.charts.maintenance) {
        state.charts.maintenance.data.datasets[0].data = [preventiveCount, correctiveCount];
        state.charts.maintenance.update('none'); // Update without animation for speed
    }

    if (state.charts.failureType) {
        state.charts.failureType.data.datasets[0].data = Object.values(failureCounts);
        state.charts.failureType.update('none');
    }

    if (state.charts.taskStatus) {
        state.charts.taskStatus.data.datasets[0].data = Object.values(statusCounts);
        state.charts.taskStatus.update('none');
    }

    // Asegurar dimensiones de canvas en primera carga
    if (!initialLoadComplete) {
        Object.values(state.charts).forEach(c => {
            if (c && typeof c.resize === 'function') c.resize();
        });
    }

    const trendLabels = [];
    const preventiveCounts = [];
    const correctiveCounts = [];
    
    const { startDate } = getDashboardPeriod();
    const baseDate = startDate;
    
    // Optimization: Group all work orders by YYYY-MM once
    const ordersByMonthAndType = {};
    state.workOrders.forEach(o => {
        if (!o.date || !o.type) return;
        const monthKey = o.date.substring(0, 7); // YYYY-MM
        if (!ordersByMonthAndType[monthKey]) {
            ordersByMonthAndType[monthKey] = { 'Preventivo': 0, 'Correctivo': 0 };
        }
        if (['Preventivo', 'Predictivo', 'Calibración', 'Mecanizado'].includes(o.type)) {
            ordersByMonthAndType[monthKey]['Preventivo']++;
        } else if (['Correctivo', 'Emergencia'].includes(o.type)) {
            ordersByMonthAndType[monthKey]['Correctivo']++;
        }
    });

    for (let i = 11; i >= 0; i--) {
        const d = new Date(baseDate.getFullYear(), baseDate.getMonth() - i, 1);
        trendLabels.push(d.toLocaleDateString('es-ES', { month: 'short', year: '2-digit' }));

        const monthKey = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`;
        const stats = ordersByMonthAndType[monthKey] || { 'Preventivo': 0, 'Correctivo': 0 };

        preventiveCounts.push(stats['Preventivo']);
        correctiveCounts.push(stats['Correctivo']);
    }
    state.charts.correctiveTrends.data.labels = trendLabels;
    state.charts.correctiveTrends.data.datasets[0].data = preventiveCounts;
    state.charts.correctiveTrends.data.datasets[1].data = correctiveCounts;
    state.charts.correctiveTrends.update();
}

function populateWorkOrderSelectors(searchTerm = '') {
    const selector = document.getElementById('report-wo-select');
    if (selector) {
        const currentVal = selector.value || selector.dataset.lastValue || "";
        const lowerSearch = searchTerm.toLowerCase();
        selector.innerHTML = '<option value="">Seleccione una Orden de Trabajo...</option>';
        
        let workOrdersToPopulate = state.workOrders;

        // If current user is a technician, only show work orders they are responsible for
        if (getCurrentUser().role === 'Técnico') {
            workOrdersToPopulate = workOrdersToPopulate.filter(wo => wo.leadTechnician === getCurrentUser().username);
        }

        if (lowerSearch) {
            workOrdersToPopulate = workOrdersToPopulate.filter(wo =>
                wo.id.toLowerCase().includes(lowerSearch) ||
                wo.description.toLowerCase().includes(lowerSearch)
            );
        }

        const sortedWOs = [...workOrdersToPopulate].sort((a, b) => b.id.localeCompare(a.id));

        let html = '<option value="">Seleccione una Orden de Trabajo...</option>';
        sortedWOs.forEach(wo => {
            html += `<option value="${wo.id}">${wo.id} - ${wo.description.substring(0, 30)}...</option>`;
        });
        selector.innerHTML = html;
        if (currentVal) {
            const exists = Array.from(selector.options).some(opt => opt.value === currentVal);
            if (exists) {
                selector.value = currentVal;
            }
            selector.dataset.lastValue = currentVal;
        }
    }
}

async function showPrettyExecutionReport(executionId) {
    const execution = state.workPlanExecutions.find(e => e.fb_id === executionId || e.id === executionId);
    if (!execution) {
        showToast('Ejecución no encontrada.', 'error');
        return;
    }

    const plan = state.workPlans.find(p => p.fb_id === execution.planId);
    const machine = state.machines.find(m => m.id === plan.machineId);

    document.getElementById('pretty-report-title').textContent = `Reporte: ${plan.name || 'Ejecución de Plan'}`;
    document.getElementById('pretty-report-subtitle').textContent = `ID Ejecución: ${execution.fb_id} | Fecha: ${new Date(execution.executedAt).toLocaleString()}`;

    const content = document.getElementById('pretty-report-content');
    content.innerHTML = `
        <div class="p-4">
            <div class="row g-4 mb-4">
                <div class="col-md-4">
                    <div class="card h-100 border-0 shadow-sm">
                        <div class="card-body">
                            <h6 class="text-muted small text-uppercase fw-bold mb-3">Información del Activo</h6>
                            <div class="d-flex align-items-center gap-3">
                                <div class="bg-primary text-white rounded-lg p-2">
                                    <i class="fas fa-microchip fa-2x"></i>
                                </div>
                                <div>
                                    <h5 class="mb-0 fw-bold">${machine.name || 'N/A'}</h5>
                                    <p class="small text-muted mb-0">ID: ${machine.id || 'N/A'}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card h-100 border-0 shadow-sm">
                        <div class="card-body">
                            <h6 class="text-muted small text-uppercase fw-bold mb-3">Ejecución</h6>
                            <div class="d-flex align-items-center gap-3">
                                <div class="bg-success text-white rounded-lg p-2">
                                    <i class="fas fa-user-check fa-2x"></i>
                                </div>
                                <div>
                                    <h5 class="mb-0 fw-bold">${execution.executedBy}</h5>
                                    <p class="small text-muted mb-0">Responsable</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card h-100 border-0 shadow-sm">
                        <div class="card-body">
                            <h6 class="text-muted small text-uppercase fw-bold mb-3">Estado Final</h6>
                            <div class="d-flex align-items-center gap-3">
                                <div class="bg-info text-white rounded-lg p-2">
                                    <i class="fas fa-flag-checkered fa-2x"></i>
                                </div>
                                <div>
                                    <h5 class="mb-0 fw-bold">${execution.status}</h5>
                                    <p class="small text-muted mb-0">Duración: ${calculateExecutionDuration(execution)}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card border-0 shadow-sm mb-4">
                <div class="card-header bg-white py-3">
                    <h6 class="mb-0 fw-bold text-primary">Resultados de las Actividades</h6>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th class="ps-4" style="width: 40%;">Tarea</th>
                                    <th style="width: 20%;">Tipo</th>
                                    <th style="width: 20%;">Resultado</th>
                                    <th style="width: 20%;" class="text-center">Estado</th>
                                </tr>
                            </thead>
                            <tbody>
                                ${(execution.taskGroups || []).map(group => `
                                    <tr class="table-secondary">
                                        <td colspan="4" class="ps-4 fw-bold small text-uppercase text-muted">${group.name}</td>
                                    </tr>
                                    ${group.tasks.map(task => `
                                        <tr>
                                            <td class="ps-4">
                                                <div class="fw-bold text-dark" style="font-size: 0.9rem;">${task.description}</div>
                                                ${task.helperText ? `<div class="x-small text-muted">${task.helperText}</div>` : ''}
                                                ${task.notes ? `<div class="mt-1 small p-2 bg-light border-start border-3" style="font-style: italic;">Obs: ${task.notes}</div>` : ''}
                                            </td>
                                            <td><span class="badge bg-light text-muted border small">${task.type === 'status_detail' ? 'Estado' : task.type}</span></td>
                                            <td>${task.type === 'status_detail' ? (task.detail || '---') : renderTaskResult(task)}</td>
                                            <td class="text-center">
                                                <span class="badge ${task.status === 'Aprobó' ? 'bg-success' : (task.status === 'Alerta' ? 'bg-warning text-dark' : 'bg-danger')} py-2 px-3 rounded-pill">
                                                    ${task.status || 'Pendiente'}
                                                </span>
                                            </td>
                                        </tr>
                                        ${task.photos && task.photos.length > 0 ? `
                                            <tr>
                                                <td colspan="4" class="ps-4 py-3 bg-light bg-opacity-50">
                                                    <div class="d-flex flex-wrap gap-2">
                                                        ${task.photos.map(p => `<img src="${p}" class="rounded border shadow-sm" style="height: 80px; cursor: pointer;" onclick="window.open('${p}', '_blank')">`).join('')}
                                                    </div>
                                                </td>
                                            </tr>
                                        ` : ''}
                                    `).join('')}
                                `).join('')}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    `;

    document.getElementById('print-pretty-report-btn').onclick = () => {
        const selector = document.getElementById('report-execution-select');
        if (selector) {
            selector.value = execution.fb_id;
            generateExecutionReportPDF();
        } else {
            showToast('Utilice el panel de reportes para imprimir este informe.', 'info');
        }
    };

    state.modals.prettyExecutionReport.show();
}

function calculateExecutionDuration(execution) {
    if (!execution.workIntervals || execution.workIntervals.length === 0) return 'N/A';

    let totalMs = 0;
    execution.workIntervals.forEach(interval => {
        if (interval.start && interval.end) {
            totalMs += (new Date(interval.end) - new Date(interval.start));
        }
    });

    if (totalMs === 0) return '0h 0m';

    const hours = Math.floor(totalMs / 3600000);
    const minutes = Math.floor((totalMs % 3600000) / 60000);
    return `${hours}h ${minutes}m`;
}

function renderTaskResult(task) {
    if (task.type === 'check') return task.value ? '<i class="fas fa-check-circle text-success"></i> Realizado' : 'No realizado';
    if (task.type === 'numeric') return `<span class="fw-bold">${task.value || 0}</span> <small class="text-muted">${task.unit || ''}</small>`;
    if (task.type === 'multi_numeric') {
        if (!task.fields || !task.multiValues) return 'N/A';
        return task.fields.map((f, i) => `<div class="small"><strong>${f}:</strong> ${task.multiValues[i] || 0} ${task.unit || ''}</div>`).join('');
    }
    if (task.type === 'text') return task.value || 'S/N';
    return '---';
}

function populateExecutionReportSelector(searchTerm = '') {
    const selector = document.getElementById('report-execution-select');
    if (!selector) return;

    const currentVal = selector.value || selector.dataset.lastValue || "";
    const lowerSearch = searchTerm.toLowerCase();
    selector.innerHTML = '<option value="">Seleccione una Ejecución de Plan...</option>';

    // Sort executions by date, newest first, filtering only for those finished or pending evaluation
    let filteredExecutions = state.workPlanExecutions
        .filter(e => e.status === 'Pendiente de Evaluación' || e.status === 'Completado');

    // If current user is a technician, only show plan executions they performed
    if (getCurrentUser().role === 'Técnico') {
        filteredExecutions = filteredExecutions.filter(e => e.executedBy === getCurrentUser().username);
    }

    const sortedExecutions = filteredExecutions.sort((a, b) => {
        return new Date(b.executedAt) - new Date(a.executedAt);
    });

    const finalExecutions = [];
    let html = '<option value="">Seleccione una Ejecución de Plan...</option>';
    sortedExecutions.forEach(execution => {
        const plan = state.workPlans.find(p => p.fb_id === execution.planId);
        if (plan) {
            const executionDate = new Date(execution.executedAt).toLocaleDateString('es-ES');
            const optionText = `${plan.name} - ${executionDate}`;

            if (lowerSearch && !plan.name.toLowerCase().includes(lowerSearch)) {
                return;
            }

            html += `<option value="${execution.fb_id}">${optionText}</option>`;
            finalExecutions.push(execution);
        }
    });
    selector.innerHTML = html;

    // Check if the previously selected value still exists in the new list
    if (currentVal) {
        const exists = finalExecutions.some(e => e.fb_id === currentVal);
        if (exists) {
            selector.value = currentVal;
        }
        selector.dataset.lastValue = currentVal;
    }
}


function populateSupplierSelectors() {
    const selectors = [
        document.getElementById('part-supplier'),
    ];
    selectors.forEach(selector => {
        if (selector) {
            const currentVal = selector.value;
            let html = '<option value="">Seleccione un proveedor...</option>';
            state.proveedores.forEach(p => {
                html += `<option value="${p.id}">${p.nombre}</option>`;
            });
            selector.innerHTML = html;
            selector.value = currentVal;
        }
    });
}

function populateWorkOrderMachineSelector(searchTerm = '') {
    const selector = document.getElementById('wo-machine');
    const noResultsDiv = document.getElementById('wo-machine-no-results');
    if (!selector) return;

    const currentVal = selector.value;

    // Default state: selector visible, message hidden.
    noResultsDiv.classList.add('d-none');
    selector.classList.remove('d-none');
    let html = '<option value="">Seleccione una máquina...</option>';

    let machinesToPopulate = state.machines;
    const userRole = getCurrentUser().role;
    const hasRestrictiveRole = (userRole === 'Jefe de Area') || (['Técnico', 'Operario', 'Supervisor de Area'].includes(userRole));
    if (hasRestrictiveRole) {
        const ids = userRole === 'Jefe de Area' ? (getCurrentUser().managedMachineIds || []) : (getCurrentUser().equipoAsignado || []);
        const relevantMachineIds = new Set(Array.isArray(ids) ? ids : []);
        machinesToPopulate = state.machines.filter(m => relevantMachineIds.has(m.id));
    }

    const lowerCaseSearchTerm = searchTerm.toLowerCase();
    const filteredMachines = machinesToPopulate.filter(machine =>
        (machine.name.toLowerCase().includes(lowerCaseSearchTerm) ||
        machine.id.toLowerCase().includes(lowerCaseSearchTerm))
    );

    if (filteredMachines.length === 0) {
        // If there are no results...
        if (searchTerm !== '') {
            // ... and we are searching, show the message.
            noResultsDiv.classList.remove('d-none');
            selector.classList.add('d-none');
        }
        // ... and we are NOT searching (initial state), do nothing. The selector remains empty but visible.
    } else {
        // If there are results, populate the selector.
        filteredMachines.forEach(machine => {
            const locationText = machine.location ? ` | ${machine.location}` : '';
            html += `<option value="${machine.id}">${machine.id} - ${machine.name}${locationText}</option>`;
        });
    }
    selector.innerHTML = html;

    if (filteredMachines.some(m => m.id === currentVal)) {
        selector.value = currentVal;
    }
}

async function handleExpiredWorkOrders() {
    if (state.workOrders.length === 0) return;

    const now = new Date();
    // Use local components for comparison as per project standard
    const currentYear = now.getFullYear();
    const currentMonth = now.getMonth();

    // 1. Recover orders that were incorrectly marked as expired but are in a finished/evaluation state or active with extension
    const ordersToRecover = state.workOrders.filter(wo => {
        if (!wo.isExpired) return false;

        // Siempre recuperar si están en estados finales o de evaluación
        if (wo.status === 'Pendiente de Evaluación' || wo.status === 'Pendiente de Aprobación' || wo.status === 'Completado') return true;

        // Recuperar si están activas (o fueron canceladas automáticamente) y aún están dentro del plazo de prórroga
        const scheduledDateStr = wo.originalDate || wo.date;
        if (!scheduledDateStr) return false;

        const sDate = new Date(scheduledDateStr + 'T12:00:00');
        const sYear = sDate.getFullYear();
        const sMonth = sDate.getMonth();

        const wasAutoCancelled = wo.status === 'Cancelado' && wo.observaciones.includes('Nota del Sistema: Cancelada y marcada como expirada automáticamente');

        if (wo.status === 'En Proceso' || wo.status === 'Pausado' || wasAutoCancelled) {
            let deadline = new Date(sYear, sMonth + 2, 1, 0, 0, 0); // Plazo con prórroga: fin del siguiente mes
            if (now < deadline) return true;
        }

        return false;
    });

    for (const order of ordersToRecover) {
        try {
            const updates = { isExpired: false };
            // Si fue cancelada automáticamente pero merece recuperación, podríamos intentar devolver el estado,
            // pero para seguridad solo quitamos el flag de expiración por ahora.
            await updateDoc(doc(state.collections.workOrders, order.fb_id), updates);
            console.log(`Orden ${order.id} recuperada de estado expirado.`);
        } catch (e) { console.error(e); }
    }

    // 2. Process new expirations
    const ordersToProcess = state.workOrders.filter(wo =>
        wo.status !== 'Completado' &&
        wo.status !== 'Cancelado' &&
        wo.status !== 'Rechazado' &&
        wo.status !== 'Pendiente de Evaluación' &&
        wo.status !== 'Pendiente de Aprobación' &&
        !wo.isExpired
    );

    for (const order of ordersToProcess) {
        let shouldUpdate = false;
        let nextStatus = '';
        let isExpired = false;
        let note = '';

        {
            // Pending/Planned/In Progress/Paused orders
            const scheduledDateStr = order.originalDate || order.date;
            if (!scheduledDateStr) continue;

            const sDate = new Date(scheduledDateStr + 'T12:00:00');
            const sYear = sDate.getFullYear();
            const sMonth = sDate.getMonth();

            let hasExtension = order.hadPauseExtension;

            // Robust check: if the order is currently In Progress or Paused,
            // it automatically gets an extension to avoid premature cancellation of active work.
            if (!hasExtension && (order.status === 'En Proceso' || order.status === 'Pausado')) {
                hasExtension = true;
            }

            let deadline;
            if (hasExtension) {
                // Allowed until the end of the next month (1st day of month + 2)
                deadline = new Date(sYear, sMonth + 2, 1, 0, 0, 0);
            } else {
                // Allowed until the end of the scheduled month (1st day of month + 1)
                deadline = new Date(sYear, sMonth + 1, 1, 0, 0, 0);
            }

            if (now >= deadline) {
                shouldUpdate = true;
                nextStatus = 'Cancelado';
                isExpired = true;
                note = 'Cancelada y marcada como expirada automáticamente por exceder el tiempo máximo de ejecución permitido. La orden ahora es de solo lectura.';
            }
        }

        if (shouldUpdate) {
            try {
                const updateData = {
                    status: nextStatus,
                    observaciones: `${order.observaciones || ''}\n\nNota del Sistema: ${note}`.trim()
                };
                if (isExpired) updateData.isExpired = true;

                const oldOrder = JSON.parse(JSON.stringify(order));
                await updateDoc(doc(state.collections.workOrders, order.fb_id), updateData);
                await recordAuditLog('workOrders', order.id, 'UPDATE_AUTO', oldOrder, { ...order, ...updateData }, 'Expiración Automática');
                console.log(`Orden ${order.id} procesada por expiración: ${nextStatus}${isExpired ? ' (Expirada)' : ''}`);
            } catch (error) {
                console.error(`Error procesando expiración de orden ${order.id}:`, error);
            }
        }
    }
}

function drawSingleStar(doc, cx, cy, outerRadius = 3, innerRadius = 1.2, part = 'full') {
    const spikes = 5;
    let rot = Math.PI / 2 * 3;
    let step = Math.PI / spikes;
    let allPoints = [];

    for (let i = 0; i < spikes; i++) {
        let x = cx + Math.cos(rot) * outerRadius;
        let y = cy + Math.sin(rot) * outerRadius;
        allPoints.push([x, y]);
        rot += step;

        x = cx + Math.cos(rot) * innerRadius;
        y = cy + Math.sin(rot) * innerRadius;
        allPoints.push([x, y]);
        rot += step;
    }

    let points = [];
    if (part === 'left') {
        // Puntos 5 (abajo centro), 6, 7, 8, 9 y 0 (arriba centro)
        points = [allPoints[5], allPoints[6], allPoints[7], allPoints[8], allPoints[9], allPoints[0]];
    } else if (part === 'right') {
        // Puntos 0 (arriba centro), 1, 2, 3, 4 y 5 (abajo centro)
        points = [allPoints[0], allPoints[1], allPoints[2], allPoints[3], allPoints[4], allPoints[5]];
    } else {
        points = allPoints;
    }

    // Usar el método general de dibujo si existe, o caer en polygon
    if (typeof doc.polygon === 'function') {
        doc.polygon(points, 'FD');
    } else {
        // Alternativa con lines (puntos relativos)
        let relativePoints = [];
        let lastX = points[0][0];
        let lastY = points[0][1];
        for (let i = 1; i < points.length; i++) {
            relativePoints.push([points[i][0] - lastX, points[i][1] - lastY]);
            lastX = points[i][0];
            lastY = points[i][1];
        }
        doc.lines(relativePoints, points[0][0], points[0][1], [1, 1], 'FD', true);
    }
}

/**
 * Dibuja estrellas de calificación con soporte para 50/50 (Operario/Jefe)
 * @param {Object} doc Instancia de jsPDF
 * @param {number} x Posición X
 * @param {number} y Posición Y
 * @param {number|Array} data Porcentaje (0-100) o Array de pares [{op: bool, jefe: bool}]
 * @param {number} starSize Tamaño de la estrella
 */
function drawRatingStars(doc, x, y, data, starSize = 3) {
    const spacing = starSize * 2.5;
    let starResults = [];

    if (Array.isArray(data)) {
        starResults = data;
    } else {
        // Convertir porcentaje a formato de 5 estrellas (retrocompatibilidad)
        const percentage = data;
        let filledCount = 0;
        if (percentage >= 95) filledCount = 5;
        else if (percentage >= 80) filledCount = 4;
        else if (percentage >= 60) filledCount = 3;
        else if (percentage >= 40) filledCount = 2;
        else if (percentage > 0) filledCount = 1;

        for (let i = 0; i < 5; i++) {
            starResults.push({ op: i < filledCount, jefe: i < filledCount });
        }
    }

    starResults.forEach((res, i) => {
        const starX = x + (i * spacing);
        const innerRadius = starSize * 0.4;

        // Dibujar fondo gris (estrella vacía)
        doc.setFillColor(220, 220, 220);
        doc.setDrawColor(180, 180, 180);
        drawSingleStar(doc, starX, y, starSize, innerRadius);

        // Dibujar mitad Operario (Izquierda)
        if (res.op) {
            doc.setFillColor(255, 215, 0);
            doc.setDrawColor(218, 165, 32);
            drawSingleStar(doc, starX, y, starSize, innerRadius, 'left');
        }

        // Dibujar mitad Jefe (Derecha)
        if (res.jefe) {
            doc.setFillColor(255, 215, 0);
            doc.setDrawColor(218, 165, 32);
            drawSingleStar(doc, starX, y, starSize, innerRadius, 'right');
        }
    });
}

async function generateTechnicianPerformanceReport(username = null) {
    // Si se llama desde un event listener, el primer argumento es un objeto Event
    const technicianUsername = (username && typeof username === 'string') ? username : document.getElementById('report-technician-select').value;
    const startDateString = document.getElementById('report-technician-start-date').value;
    const endDateString = document.getElementById('report-technician-end-date').value;

    if (!technicianUsername) {
        showToast('Por favor, seleccione un técnico.', 'error');
        return;
    }
    if (!startDateString || !endDateString) {
        showToast('Por favor, seleccione un rango de fechas válido.', 'error');
        return;
    }

    showLoading(true);

    try {
        const technician = state.technicians.find(t => t.username === technicianUsername);
        if (!technician) throw new Error('Técnico no encontrado.');

        const startDate = new Date(startDateString + 'T00:00:00');
        const endDate = new Date(endDateString + 'T23:59:59');

        const evaluationsInPeriod = state.technicianEvaluations.filter(e =>
            e.technicianId === technicianUsername &&
            new Date(e.evaluationDate) >= startDate &&
            new Date(e.evaluationDate) <= endDate
        );

        // --- PDF Generation (Executive Style) ---
        const { jsPDF } = window.jspdf;
        const doc = new jsPDF();
        const periodName = `${startDate.toLocaleDateString('es-ES')} al ${endDate.toLocaleDateString('es-ES')}`;

        // Header
        doc.setFontSize(20);
        doc.setFont(undefined, 'bold');
        doc.text('Informe de Desempeño Ejecutivo', 105, 20, { align: 'center' });
        doc.setFontSize(12);
        doc.setFont(undefined, 'normal');
        doc.text(`Técnico: ${technician.username}`, 14, 40);
        doc.text(`Período: ${periodName}`, 14, 48);
        doc.line(14, 55, 196, 55); // Horizontal line

        // Summary Cards
        const totalCompleted = state.workOrders.filter(wo =>
            wo.technicians.includes(technicianUsername) &&
            (wo.status === 'Completado' || wo.status === 'Pendiente de Evaluación') &&
            wo.fechaFinalizacionReal &&
            new Date(wo.fechaFinalizacionReal) >= startDate &&
            new Date(wo.fechaFinalizacionReal) <= endDate
        ).length;

        // Agrupar evaluaciones por OT para consolidar doble evaluación
        const evaluationsByWO = {};
        evaluationsInPeriod.forEach(e => {
            if (!evaluationsByWO[e.workOrderFbId]) evaluationsByWO[e.workOrderFbId] = [];
            evaluationsByWO[e.workOrderFbId].push(e);
        });

        const totalWOsEvaluated = Object.keys(evaluationsByWO).length;

        // Calcular promedio global basado en pares
        const activePairs = state.evaluationCriteria.filter(c => c?.status === 'activo' && c?.role === 'Operario' && c?.pairId);
        let globalPairTotal = 0;
        let globalPairChecked = 0;

        const tableBody = Object.keys(evaluationsByWO).map(woFbId => {
            const evs = evaluationsByWO[woFbId];
            const workOrder = state.workOrders.find(wo => wo.fb_id === woFbId);
            const opEval = evs.find(e => e.evaluatorRole === 'Operario');
            const jefeEval = evs.find(e => e.evaluatorRole === 'Jefe de Area');

            const starResults = activePairs.map(p => {
                const opOk = opEval ? opEval.results.find(r => r.criteriaId === p.fb_id).score === 1 : false;
                const jefeOk = jefeEval ? jefeEval.results.find(r => r.criteriaId === p.pairId).score === 1 : false;

                globalPairTotal += 2;
                if (opOk) globalPairChecked++;
                if (jefeOk) globalPairChecked++;

                return { op: opOk, jefe: jefeOk };
            });

            return [
                workOrder ? workOrder.id : 'N/A',
                workOrder ? workOrder.description : 'Descripción no encontrada',
                new Date(evs[0].evaluationDate).toLocaleDateString('es-ES'),
                starResults // Guardamos el array de estrellas para el hook
            ];
        });

        const avgScorePercentage = globalPairTotal > 0 ? (globalPairChecked / globalPairTotal * 100) : 0;

        doc.setFontSize(10);
        doc.setTextColor(100, 100, 100);
        doc.text('Tareas Completadas', 30, 70);
        doc.text('Tareas Evaluadas', 90, 70);
        doc.text('Calificación Promedio', 150, 70);

        doc.setFontSize(18);
        doc.setTextColor(0, 0, 0);
        doc.text(String(totalCompleted), 30, 80);
        doc.text(String(totalWOsEvaluated), 90, 80);

        // Estrellas en el resumen (usamos porcentaje para el promedio global)
        drawRatingStars(doc, 150, 78, avgScorePercentage, 4);

        // Detailed List of Evaluated Work Orders
        doc.setFontSize(14);
        doc.text('Detalle de Órdenes de Trabajo Evaluadas', 14, 100);

        if (tableBody.length > 0) {
            doc.autoTable({
                startY: 105,
                head: [['ID Orden', 'Descripción del Problema', 'Fecha Evaluación', 'Calificación']],
                body: tableBody,
                headStyles: { fillColor: [44, 62, 80] },
                columnStyles: {
                    1: { cellWidth: 80 },
                    3: { cellWidth: 40 }
                },
                didParseCell: (data) => {
                    if (data.section === 'body' && data.column.index === 3) {
                        data.cell.text = ['']; // Vaciar el texto para que no se superponga
                    }
                },
                didDrawCell: (data) => {
                    if (data.section === 'body' && data.column.index === 3) {
                        const starResults = data.cell.raw;
                        // Centrar las estrellas en la celda
                        const posX = data.cell.x + 4;
                        const posY = data.cell.y + (data.cell.height / 2);
                        drawRatingStars(doc, posX, posY, starResults, 2.5);
                    }
                }
            });
        } else {
            doc.setFontSize(10);
            doc.text('No se encontraron evaluaciones para este técnico en el período seleccionado.', 14, 110);
        }

        doc.save(`Informe_Desempeno_${technician.username}_${startDateString}_a_${endDateString}.pdf`);
        showToast('Informe de desempeño generado.', 'success');

    } catch (error) {
        console.error("Error generating technician performance report:", error);
        showToast('Error al generar el informe: ' + error.message, 'error');
    } finally {
        showLoading(false);
    }
}


function downloadBackup() {
    showLoading(true);
    try {
        const backupData = {
            version: "1.0",
            backupDate: new Date().toISOString(),
            machines: state.machines,
            parts: state.parts,
            proveedores: state.proveedores,
            technicians: state.technicians,
            workOrders: state.workOrders,
            solicitudes: state.solicitudes,
            workPlans: state.workPlans,
            workPlanExecutions: state.workPlanExecutions,
            evaluationCriteria: state.evaluationCriteria,
            technicianEvaluations: state.technicianEvaluations,
            partMovements: state.partMovements,
            partRequests: state.partRequests
        };

        const jsonString = JSON.stringify(backupData, null, 2);
        const blob = new Blob([jsonString], { type: "application/json" });
        const url = URL.createObjectURL(blob);

        const a = document.createElement('a');
        const now = new Date();
        const timestamp = `${now.getFullYear()}${String(now.getMonth()+1).padStart(2,'0')}${String(now.getDate()).padStart(2,'0')}_${String(now.getHours()).padStart(2,'0')}${String(now.getMinutes()).padStart(2,'0')}`;

        a.href = url;
        a.download = `Respaldo_CORINFAR_${timestamp}.json`;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        URL.revokeObjectURL(url);

        showToast('Respaldo generado y descargado correctamente.', 'success');
    } catch (error) {
        console.error("Error generating backup:", error);
        showToast('Error al generar el respaldo.', 'error');
    } finally {
        showLoading(false);
    }
}

function populateDynamicSelectors(searchTerm = '', specificId = null) {
    const selectorsConfig = [
        { id: 'report-technician-select', tab: 'reportes' },
        { id: 'report-machine-select', tab: 'reportes' },
        { id: 'report-machine-parts-select', tab: 'reportes' },
        { id: 'kpi-machine-select', tab: 'dashboard' },
        { id: 'solicitud-machine', tab: 'solicitudes' },
        { id: 'decommission-replacement', tab: 'equipos-baja' },
        { id: 'report-executive-area', tab: 'reportes' },
        { id: 'report-executive-cost-center', tab: 'reportes' },
        { id: 'work-plan-machine', tab: 'planes-mantenimiento' }
    ];
    
    selectorsConfig.forEach(cfg => {
        const selector = document.getElementById(cfg.id);
        if (!selector) return;

        // Skip if a specific ID is requested and doesn't match
        if (specificId && cfg.id !== specificId) return;

        // Performance: Only update if the containing tab is active, or if it's an explicit search/specific update
        const isTabActive = state.currentTab === cfg.tab;
        if (!specificId && !isTabActive) return;

        const currentVal = selector.value || selector.dataset.lastValue || "";
        const lowerSearch = (specificId === selector.id) ? searchTerm.toLowerCase() : '';

        let html = '';
        if (selector.id === 'report-technician-select') {
                html = '<option value="">Seleccione un técnico...</option>';
                let techniciansToPopulate = state.technicians.filter(t => ['Técnico', 'Admin', 'Planificador'].includes(t?.role));

                // If current user is a technician, only show themselves
                if (getCurrentUser().role === 'Técnico') {
                    techniciansToPopulate = techniciansToPopulate.filter(t => t.username === getCurrentUser().username);
                }

                techniciansToPopulate.forEach(tech => {
                    const statusText = tech.isActive === false ? ' (Inactivo)' : '';
                    html += `<option value="${tech.username}">${tech.username}${statusText}</option>`;
                });
            } else if (selector.id === 'report-executive-area') {
                const areas = [...new Set(state.machines.map(m => m.location).filter(Boolean))].sort();
                html = '<option value="all">Todas las Áreas</option>';
                areas.forEach(area => {
                    html += `<option value="${area}">${area}</option>`;
                });
            } else if (selector.id === 'report-executive-cost-center') {
                const ccs = [...new Set(state.machines.map(m => m.centroCosto).filter(Boolean))].sort();
                html = '<option value="all">Todos los Centros de Costo</option>';
                ccs.forEach(cc => {
                    html += `<option value="${cc}">${cc}</option>`;
                });
            } else {
                const isKpiSelector = selector.id === 'kpi-machine-select';
                html = isKpiSelector ? '<option value="all">Todas las Máquinas</option>' : '<option value="">Seleccione una máquina...</option>';

                let machinesToPopulate = state.machines;
                const role = getCurrentUser().role;

                if (role !== 'Admin' && role !== 'Planificador' && role !== 'Invitado') {
                    if (role === 'Jefe de Area') {
                        const managedIds = new Set(Array.isArray(getCurrentUser().managedMachineIds) ? getCurrentUser().managedMachineIds : []);
                        machinesToPopulate = state.machines.filter(m => m.id && managedIds.has(m.id));
                    } else if (['Técnico', 'Operario', 'Supervisor de Area'].includes(role)) {
                        const assignedIds = new Set(Array.isArray(getCurrentUser().equipoAsignado) ? getCurrentUser().equipoAsignado : []);
                        machinesToPopulate = state.machines.filter(m => m.id && assignedIds.has(m.id));
                    }
                }

                if (lowerSearch) {
                    machinesToPopulate = machinesToPopulate.filter(m =>
                        (m.id && m.id.toLowerCase().includes(lowerSearch)) ||
                        (m.name && m.name.toLowerCase().includes(lowerSearch)) ||
                        (m.location && m.location.toLowerCase().includes(lowerSearch)) ||
                        (getMachineArea(m) && getMachineArea(m).toLowerCase().includes(lowerSearch))
                    );
                }

                // Show/hide no-results warning if present
                const noResultsDiv = document.getElementById(`${selector.id}-no-results`);
                if (noResultsDiv) {
                    if (machinesToPopulate.length === 0 && lowerSearch) {
                        noResultsDiv.classList.remove('d-none');
                    } else {
                        noResultsDiv.classList.add('d-none');
                    }
                }

                if (selector.id !== 'report-machine-select' && selector.id !== 'kpi-machine-select') {
                    machinesToPopulate = machinesToPopulate.filter(m => m.status !== 'DADO_DE_BAJA');
                }

                machinesToPopulate.forEach(machine => {
                    if (machine.id && machine.name) {
                        const locationText = machine.location ? ` | ${machine.location}${getMachineArea(machine) ? ' / ' + getMachineArea(machine) : ''}` : '';
                        html += `<option value="${machine.id}">${machine.id} - ${machine.name}${locationText}</option>`;
                    }
                });
            }
            selector.innerHTML = html;
            if (currentVal) {
                const exists = Array.from(selector.options).some(opt => opt.value === currentVal);
                if (exists) {
                    selector.value = currentVal;
                }
                selector.dataset.lastValue = currentVal;
            }
    });
}

window.state = state;
window.getTechnicianDataFromForm = getTechnicianDataFromForm;
window.renderCalendar = renderCalendar;
window.renderActiveWorkView = renderActiveWorkView;
window.showWorkOrderModal = showWorkOrderModal;
window.updateDashboardData = updateDashboardData;
window.renderMachines = renderMachines;
window.showPartDetail = showPartDetail;
window.showMachineDetail = showMachineDetail;
window.renderParts = renderParts;
window.renderSolicitudes = renderSolicitudes;
window.renderWorkPlans = renderWorkPlans;
window.toggleTheme = toggleTheme;
window.syncPartToOdoo = syncPartToOdoo;
window.syncPartStockToOdoo = syncPartStockToOdoo;
window.renderAuditLogs = renderAuditLogs;
window.viewAuditDetail = viewAuditDetail;
window.populateDynamicSelectors = populateDynamicSelectors;
window.populateWorkOrderSelectors = populateWorkOrderSelectors;
window.populateExecutionReportSelector = populateExecutionReportSelector;
window.generateSingleWorkOrderReport = generateSingleWorkOrderReport;
window.showMachineHistory = showMachineHistory;
window.generateFichaMaestra = generateFichaMaestra;
window.requestSignature = requestSignature;
window.handleViewLinkedPlan = handleViewLinkedPlan;
window.handleStartLinkedPlan = handleStartLinkedPlan;
window.handlePausePlanExecution = handlePausePlanExecution;
window.handleResumePlanExecution = handleResumePlanExecution;
window.handleFinishPlanExecution = handleFinishPlanExecution;
window.showTechnicianModal = showTechnicianModal;
window.showSolicitudModal = showSolicitudModal;
window.showEvaluationModal = showEvaluationModal;
window.renderTechnicians = renderTechnicians;
window.showManagePartsModal = showManagePartsModal;
window.addTaskGroup = addTaskGroup;
window.updateUIFromRTCounters = updateUIFromRTCounters;
window.handleForgotPasswordRequest = handleForgotPasswordRequest;
window.showDecommissionModal = showDecommissionModal;
window.renderDecommissionedMachines = renderDecommissionedMachines;
window.handleDecommissionSubmit = handleDecommissionSubmit;
window.showStockAdjustmentModal = showStockAdjustmentModal;
window.handleStockAdjustmentSubmit = handleStockAdjustmentSubmit;
window.showDeliveryModal = showDeliveryModal;
window.handleMaterialDeliverySubmit = handleMaterialDeliverySubmit;
window.updateInventoryDashboard = updateInventoryDashboard;
window.uploadMachineFile = uploadMachineFile;
window.uploadExecutionFile = uploadExecutionFile;
window.uploadPartFile = uploadPartFile;
window.loadAIConfig = loadAIConfig;
window.saveAIConfig = saveAIConfig;
window.loadAIUsage = loadAIUsage;
window.testAIConnection = testAIConnection;
window.removePartFromWorkOrder = removePartFromWorkOrder;
window.removeRequestFromWorkOrder = removeRequestFromWorkOrder;
window.handlePartReturn = handlePartReturn;
window.renderInventoryTraceability = renderInventoryTraceability;

async function generateTokenLink(fbId, type, showModal = true) {
    const tech = state.technicians.find(t => t.fb_id === fbId);
    if (!tech) return "";

    const token = Math.random().toString(36).substring(2, 15) + Math.random().toString(36).substring(2, 15);
    const expiry = new Date();
    expiry.setHours(expiry.getHours() + 24);

    try {
        await updateDoc(doc(state.collections.technicians, fbId), {
            invitationToken: token,
            tokenExpiry: expiry.toISOString(),
            resetRequested: false
        });

        const baseUrl = window.location.origin + window.location.pathname;
        const link = `${baseUrl}token=${token}`;

        if (showModal) {
            document.getElementById('generated-link-input').value = link;
            document.getElementById('link-modal-desc').textContent = type === 'reset' ? 'Copie este link de recuperación y envíelo al usuario. Es válido por 24 horas.' : 'Copie este link de invitación y envíelo al usuario. Es válido por 24 horas.';

            state.modals.invitationLink.show();
        }

        return link;
    } catch (error) {
        console.error("Error generating token link:", error);
        showToast('Error al generar el link', 'error');
        return "";
    }
}
window.generateTokenLink = generateTokenLink;

async function handleInvitationToken(token) {
    showLoading(true);
    let tech = null;
    try {
        const q = query(state.collections.technicians, where("invitationToken", "==", token));
        const snapshot = await getDocs(q);
        if (!snapshot.empty) {
            tech = { fb_id: snapshot.docs[0].id, ...snapshot.docs[0].data() };
        }
    } catch (error) {
        console.error("Error validating token:", error);
    } finally {
        showLoading(false);
    }

    if (!tech) {
        showToast('Link inválido o ya utilizado.', 'error');
        return;
    }

    const now = new Date();
    const expiry = tech.tokenExpiry ? new Date(tech.tokenExpiry) : null;

    if (!expiry || now > expiry) {
        showToast('Este link ha expirado (validez de 24h).', 'error');
        return;
    }

    // Mostrar UI de set password
    document.getElementById('login-overlay').classList.add('d-none');
    document.getElementById('set-password-overlay').classList.remove('d-none');
    const setPasswordForm = document.getElementById('set-password-form');
    if (setPasswordForm) {
        const usernameField = setPasswordForm.querySelector('input[name="username"]');
        if (usernameField) usernameField.value = tech.username;
    }
    document.getElementById('set-password-token').value = token;
    document.getElementById('set-password-message').textContent = `Hola ${tech.username}, establezca su nueva contraseña.`;
}

async function handleSetPasswordSubmit(e) {
    e.preventDefault();
    const token = document.getElementById('set-password-token').value;
    const newPass = document.getElementById('new-password').value;
    const confirmPass = document.getElementById('confirm-new-password').value;

    if (newPass !== confirmPass) {
        showToast('Las contraseñas no coinciden.', 'warning');
        return;
    }

    if (newPass.length < 4) {
        showToast('La contraseña debe tener al menos 4 caracteres.', 'warning');
        return;
    }

    showLoading(true);
    try {
        const tech = state.technicians.find(t => t.invitationToken === token);
        if (!tech) throw new Error('Usuario no encontrado');

        const hashedPassword = await hashPassword(newPass);
        await updateDoc(doc(state.collections.technicians, tech.fb_id), {
            password: hashedPassword,
            passwordIsHashed: true,
            invitationToken: deleteField(),
            tokenExpiry: deleteField(),
            resetRequested: false
        });

        showToast('¡Contraseña establecida correctamente!', 'success');

        // Limpiar URL
        const url = new URL(window.location);
        url.searchParams.delete('token');
        window.history.replaceState({}, '', url);

        // Volver al login y resetear campos inmediatamente
        document.getElementById('set-password-form').reset();
        document.getElementById('set-password-overlay').classList.add('d-none');
        document.getElementById('login-overlay').classList.remove('d-none');

        document.getElementById('username').value = tech.username;
        document.getElementById('password').value = '';
        setTimeout(() => {
            document.getElementById('password').focus();
        }, 100);

    } catch (error) {
        console.error("Error setting password:", error);
        showToast('Error al guardar la contraseña.', 'error');
    } finally {
        showLoading(false);
    }
}

async function handleForgotPasswordRequest() {
    const usernameInput = document.getElementById('forgot-username');
    const username = usernameInput.value.trim();

    if (!username) {
        showToast('Ingrese su nombre de usuario', 'warning');
        return;
    }

    showLoading(true);

    try {
        // Búsqueda insensible a mayúsculas para evitar errores comunes
        const tech = state.technicians.find(t => t.username.toLowerCase() === username.toLowerCase());

        if (!tech) {
            showToast('Usuario no encontrado en la base de datos.', 'error');
            showLoading(false);
            return;
        }

        // Actualizar Firestore
        await updateDoc(doc(state.collections.technicians, tech.fb_id), {
            resetRequested: true
        });

        // Notificar al administrador vía Telegram
        const message = `🚨 <b>Solicitud de Restablecimiento</b>\n\nEl usuario <b>${escapeHTML(tech.username)}</b> ha solicitado una nueva contraseña.\n\nPor favor, genere un link de recuperación desde el panel de usuarios.`;

        // Intentar enviar notificación pero no bloquear si falla Telegram
        try {
            await notifyAdminsAndPlanners(message);
        } catch (tgError) {
            console.warn("Error enviando notificación por Telegram:", tgError);
        }

        showToast('Solicitud enviada con éxito. El administrador le enviará un link de recuperación pronto.', 'success');

        // Limpiar y cerrar
        usernameInput.value = '';
        state.modals.forgotPassword.hide();

    } catch (error) {
        console.error("Error requesting password reset:", error);
        showToast('Error al procesar la solicitud. Verifique su conexión.', 'error');
    } finally {
        showLoading(false);
    }
}

async function loadAuditLogs() {
    const list = document.getElementById('audit-log-list');
    if (list) {
        list.innerHTML = '<tr><td colspan="6" class="text-center text-muted"><i class="fas fa-spinner fa-spin"></i> Cargando registros...</td></tr>';
    }

    try {
        const q = query(state.collections.auditLogs, orderBy("timestamp", "desc"), limit(500));
        const snapshot = await getDocs(q);
        state.auditLogs = snapshot.docs.map(doc => ({ fb_id: doc.id, ...doc.data() }));
        renderAuditLogs();
    } catch (error) {
        console.error("Error loading audit logs:", error);
        showToast("Error al cargar registros de auditoría", "error");
    }
}

function getAuditCollectionLabel(collectionName) {
    const map = {
        'workOrders': 'OT',
        'solicitudes': 'Solicitud',
        'machines': 'Máquina',
        'parts': 'Repuesto',
        'partMovements': 'Inventario',
        'partRequests': 'Solicitud de Repuesto',
        'technicians': 'Usuario',
        'proveedores': 'Proveedor',
        'workPlans': 'Plan de Mantenimiento',
        'workPlanExecutions': 'Ejecución de Plan',
        'evaluationCriteria': 'Criterios de Evaluación',
        'technicianEvaluations': 'Evaluación de Técnico'
    };
    return map[collectionName] || collectionName;
}

function getWorkOrderDetailedAction(oldOrder, newOrder) {
    if (!oldOrder) return 'Creación OT';

    let actions = [];

    // Cambios de Estado
    if (oldOrder.status !== newOrder.status) {
        if (newOrder.status === 'En Proceso') {
            actions.push(oldOrder.status === 'Pausado' ? 'Reanudación OT' : 'Inicio OT');
        } else if (newOrder.status === 'Pausado') {
            actions.push('Pausa OT');
        } else if (newOrder.status === 'Cancelado') {
            actions.push('Cancelación OT');
        } else if (newOrder.status === 'Completado') {
            actions.push('Finalización OT');
        } else if (newOrder.status === 'Pendiente de Evaluación') {
            actions.push('Evaluación Técnica (Enviada)');
        } else if (newOrder.status === 'Pendiente de Aprobación') {
            actions.push('Trabajo Finalizado (Pendiente Aprobación)');
        } else {
            actions.push(`Estado: ${newOrder.status}`);
        }
    }

    // Validación
    if (!oldOrder.validatedBy && newOrder.validatedBy) {
        actions.push('OT Validada');
    }

    // Repuestos
    const oldParts = oldOrder.partsUsed || [];
    const newParts = newOrder.partsUsed || [];
    if (newParts.length > oldParts.length) {
        actions.push('Repuestos agregados');
    } else if (newParts.length < oldParts.length) {
        actions.push('Repuestos eliminados');
    }

    // Técnicos
    if (oldOrder.leadTechnician !== newOrder.leadTechnician) {
        actions.push('Cambio de responsable');
    }

    if (actions.length === 0) return 'Modificación OT';
    return actions.join(', ');
}

function getAuditActionLabel(log) {
    // Si hay una acción detallada específica y no es genérica, usarla
    const genericActions = ['Modificación OT', 'Actualización OT', 'Modificación'];
    if (log.detailedAction && !genericActions.includes(log.detailedAction) && !log.detailedAction.startsWith('Cambio estado a')) {
        return log.detailedAction;
    }

    if (log.action === 'CREATE') return 'Creación';
    if (log.action === 'DELETE') return 'Eliminación';
    if (log.action === 'UPDATE_AUTO') return 'Actualización Automática';

    if (log.action === 'UPDATE' && log.collectionName === 'workOrders' && log.newData && log.oldData) {
        let action = getWorkOrderDetailedAction(log.oldData, log.newData);
        // Si el log original tenía información de firma, preservarla
        if (log.detailedAction && log.detailedAction.includes('(Firmado)')) {
            action += ' (Firmado)';
        }
        return action;
    }

    if (log.detailedAction) return log.detailedAction;
    return log.action === 'UPDATE' ? 'Modificación' : log.action;
}

function getAuditDocLabel(log) {
    if (log.docId && log.docId.length < 20 && !/^[a-z0-9]{20,}$/i.test(log.docId)) return log.docId;

    const data = log.newData || log.oldData;
    if (data) {
        return data.id || data.serial_no || data.partId || data.workOrderId || log.docId;
    }
    return log.docId;
}

function renderAuditLogs() {
    if (state.currentTab !== 'auditoria' && initialLoadComplete) return;
    const list = document.getElementById('audit-log-list');
    if (!list) return;

    const filterCollection = document.getElementById('audit-filter-collection').value;
    const filterUser = document.getElementById('audit-filter-user').value.toLowerCase();
    const filterStart = document.getElementById('audit-filter-start').value;
    const filterEnd = document.getElementById('audit-filter-end').value;

    let filtered = [...state.auditLogs];

    if (filterCollection !== 'all') {
        filtered = filtered.filter(log => log.collectionName === filterCollection);
    }
    if (filterUser) {
        filtered = filtered.filter(log => log.username.toLowerCase().includes(filterUser));
    }
    if (filterStart) {
        filtered = filtered.filter(log => log.timestamp >= filterStart);
    }
    if (filterEnd) {
        filtered = filtered.filter(log => log.timestamp <= filterEnd + 'T23:59:59');
    }

    filtered.sort((a, b) => new Date(b.timestamp) - new Date(a.timestamp));

    list.innerHTML = filtered.map(log => {
        const actionLabel = getAuditActionLabel(log);
        const collectionLabel = getAuditCollectionLabel(log.collectionName);
        const docLabel = getAuditDocLabel(log);
        const badgeClass = log.action === 'CREATE' ? 'success' : (log.action === 'DELETE' ? 'danger' : 'primary');

        return `
            <tr>
                <td><small>${new Date(log.timestamp).toLocaleString()}</small></td>
                <td>${log.username}</td>
                <td><span class="badge bg-${badgeClass}">${actionLabel}</span></td>
                <td>${collectionLabel}</td>
                <td><small>${docLabel}</small></td>
                <td>
                    <button class="btn btn-sm btn-outline-secondary" onclick="viewAuditDetail('${log.fb_id}')">
                        <i class="fas fa-eye"></i> Ver
                    </button>
                </td>
            </tr>
        `;
    }).join('') || '<tr><td colspan="6" class="text-center text-muted">No se encontraron registros</td></tr>';
}

async function generateAuditReport() {
    showLoading(true, "Generando reporte de auditoría...");
    try {
        const { jsPDF } = window.jspdf;
        const doc = new jsPDF('l', 'mm', 'a4');
        const logoBase64 = typeof GLOBAL_LOGO !== 'undefined' ? GLOBAL_LOGO : "";

        // Obtener filtros de fecha
        const filterStart = document.getElementById('report-audit-start').value;
        const filterEnd = document.getElementById('report-audit-end').value;

        let logs = [];

        // Si hay filtros, consultar Firestore con filtros. Si no, usar los de memoria o consultar últimos 500.
        if (filterStart || filterEnd) {
            let constraints = [orderBy("timestamp", "desc")];
            if (filterStart) constraints.push(where("timestamp", ">=", filterStart + 'T00:00:00'));
            if (filterEnd) constraints.push(where("timestamp", "<=", filterEnd + 'T23:59:59'));

            const q = query(state.collections.auditLogs, ...constraints);
            const snapshot = await getDocs(q);
            logs = snapshot.docs.map(doc => doc.data());
        } else if (state.auditLogs && state.auditLogs.length > 0) {
            logs = [...state.auditLogs];
        } else {
            // Carga de emergencia si no hay datos en memoria
            const q = query(state.collections.auditLogs, orderBy("timestamp", "desc"), limit(500));
            const snapshot = await getDocs(q);
            logs = snapshot.docs.map(doc => doc.data());
        }

        doc.setFontSize(18);
        doc.text('REGISTRO DE AUDITORÍA Y TRAZABILIDAD', 14, 20);

        if (logoBase64) {
            try {
                doc.addImage(logoBase64, 'PNG', 200, 10, 80, 25);
            } catch (e) { console.error("Logo error:", e); }
        }

        doc.setFontSize(10);
        doc.text(`Sistema: CORINFAR CMMS - Cumplimiento 21 CFR Part 11`, 14, 28);
        doc.text(`Fecha de Reporte: ${new Date().toLocaleString()}`, 14, 34);
        doc.text(`Generado por: ${getCurrentUser().username}`, 14, 40);

        if (filterStart || filterEnd) {
            doc.text(`Rango de fechas: ${filterStart || 'Inicio'} hasta ${filterEnd || 'Fin'}`, 14, 46);
        }

        const tableData = logs.map(log => [
            new Date(log.timestamp).toLocaleString(),
            log.username,
            getAuditActionLabel(log),
            getAuditCollectionLabel(log.collectionName),
            getAuditDocLabel(log)
        ]);

        doc.autoTable({
            startY: filterStart || filterEnd ? 50 : 45,
            head: [['Fecha/Hora', 'Usuario', 'Acción', 'Colección', 'ID Documento']],
            body: tableData,
            theme: 'striped',
            headStyles: { fillColor: [44, 62, 80] },
        });

        doc.save(`Audit_Log_${new Date().toISOString().split('T')[0]}.pdf`);
    } catch (error) {
        console.error("Error generating audit report:", error);
        showToast("Error al generar el reporte de auditoría", "error");
    } finally {
        showLoading(false);
    }
}

function renderVisualAudit(log) {
    const container = document.getElementById('audit-visual-content');
    container.innerHTML = '';

    const fieldLabels = {
        'description': 'Descripción',
        'technicians': 'Técnicos',
        'failureType': 'Tipo de Falla',
        'linkedPlanId': 'Plan Vinculado',
        'workIntervals': 'Intervalos de Trabajo',
        'externalCost': 'Costo Externo',
        'priority': 'Prioridad',
        'totalWorkDurationMs': 'Duración Total',
        'solicitudId': 'N° Solicitud',
        'validatedBy': 'Validado por',
        'images': 'Imágenes/Evidencias',
        'odoo_id': 'ID Odoo',
        'supportTechnicians': 'Técnicos de Apoyo',
        'date': 'Fecha Programada',
        'technicianFinished': 'Técnico Finalizó',
        'sourceSolicitudId': 'ID Solicitud Origen',
        'type': 'Tipo de Trabajo',
        'originalDate': 'Fecha Original',
        'leadTechnician': 'Técnico Líder',
        'status': 'Estado',
        'machineId': 'Equipo (ID)',
        'createdAt': 'Fecha de Creación',
        'requester': 'Solicitante',
        'fechaInicioReal': 'Inicio Real',
        'endTime': 'Hora Fin',
        'id': 'ID Interno',
        'fechaFinalizacionReal': 'Finalización Real',
        'startTime': 'Hora Inicio',
        'partsUsed': 'Repuestos/Materiales',
        'observations': 'Observaciones',
        'observaciones': 'Observaciones',
        'justification': 'Justificación',
        'signature': 'Firma Digital',
        'isActive': 'Estado de Activación',
        'username': 'Nombre de Usuario',
        'role': 'Rol de Usuario',
        'telegramChatId': 'ID Telegram',
        'equipoAsignado': 'Equipo Asignado',
        'stockActual': 'Stock Actual',
        'minStock': 'Stock Mínimo',
        'location': 'Ubicación',
        'price': 'Precio/Costo',
        'classification': 'Clasificación',
        'provider': 'Proveedor',
        'frequency': 'Frecuencia',
        'tasks': 'Tareas del Plan'
    };

    const oldData = log.oldData || {};
    const newData = log.newData || {};

    // Obtener todas las llaves únicas de ambos objetos, excluyendo campos técnicos ruidosos
    const excludeFields = ['fb_id', 'lastUpdated', 'updatedAt', 'appId'];
    const allKeys = Array.from(new Set([...Object.keys(oldData), ...Object.keys(newData)]))
        .filter(k => !excludeFields.includes(k));

    let changesFound = 0;

    allKeys.forEach(key => {
        const oldVal = oldData[key];
        const newVal = newData[key];

        // Solo mostrar si hay cambio real
        if (JSON.stringify(oldVal) === JSON.stringify(newVal)) return;

        changesFound++;

        const card = document.createElement('div');
        card.className = 'card mb-3 border-0 shadow-sm';
        card.style.borderLeft = '4px solid #3498db';

        const label = fieldLabels[key] || key;

        const formatValue = (val) => {
            if (val === null || val === undefined) return '<span class="text-muted fst-italic">Vacio</span>';
            if (typeof val === 'boolean') return val ? 'Sí' : 'No';
            if (Array.isArray(val)) {
                if (val.length === 0) return '<span class="text-muted fst-italic">Lista vacía</span>';
                // Si es array de objetos (ej: partsUsed, tasks)
                if (typeof val[0] === 'object') {
                    return `<ul class="mb-0 ps-3 small">
                        ${val.map(item => `<li>${item.name || item.partName || item.description || JSON.stringify(item)}</li>`).join('')}
                    </ul>`;
                }
                return `<ul class="mb-0 ps-3 small">${val.map(v => `<li>${v}</li>`).join('')}</ul>`;
            }
            if (typeof val === 'string' && (val.includes('T') && val.includes('Z') && !isNaN(Date.parse(val)))) {
                return new Date(val).toLocaleString();
            }
            if (key === 'totalWorkDurationMs') {
                const hours = (val / 3600000).toFixed(2);
                return `${hours} horas`;
            }
            return val;
        };

        card.innerHTML = `
            <div class="card-body p-3">
                <div class="d-flex justify-content-between align-items-center mb-2">
                    <h6 class="card-title mb-0 text-primary"><i class="fas fa-tag me-2 small"></i>${label}</h6>
                </div>
                <div class="row align-items-center">
                    <div class="col-5">
                        <div class="p-2 rounded bg-light border small text-wrap" style="min-height: 40px;">
                            <div class="text-muted x-small mb-1">Anterior:</div>
                            ${formatValue(oldVal)}
                        </div>
                    </div>
                    <div class="col-2 text-center text-muted">
                        <i class="fas fa-long-arrow-alt-right fa-lg"></i>
                    </div>
                    <div class="col-5">
                        <div class="p-2 rounded bg-light border small text-wrap" style="min-height: 40px; border-left: 3px solid #2ecc71 !important;">
                            <div class="text-muted x-small mb-1">Nuevo:</div>
                            ${formatValue(newVal)}
                        </div>
                    </div>
                </div>
            </div>
        `;
        container.appendChild(card);
    });

    if (changesFound === 0) {
        if (!log.oldData) {
            container.innerHTML = `<div class="alert alert-success"><i class="fas fa-plus-circle me-2"></i>Este registro fue <strong>CREADO</strong> con todos los valores mostrados en la vista técnica.</div>`;
        } else if (!log.newData) {
            container.innerHTML = `<div class="alert alert-danger"><i class="fas fa-trash-alt me-2"></i>Este registro fue <strong>ELIMINADO</strong>.</div>`;
        } else {
            container.innerHTML = `<div class="alert alert-info"><i class="fas fa-info-circle me-2"></i>No se detectaron cambios visuales significativos.</div>`;
        }
        document.getElementById('audit-technical-section').classList.remove('d-none');
    } else {
        document.getElementById('audit-technical-section').classList.add('d-none');
    }
}

function viewAuditDetail(fb_id) {
    const log = state.auditLogs.find(l => l.fb_id === fb_id);
    if (!log) return;

    document.getElementById('audit-old-data').textContent = log.oldData ? JSON.stringify(log.oldData, null, 2) : '--- N/A (CREACIÓN) ---';
    document.getElementById('audit-new-data').textContent = log.newData ? JSON.stringify(log.newData, null, 2) : '--- N/A (ELIMINACIÓN) ---';

    renderVisualAudit(log);

    state.modals.auditDetail.show();
}

function requestSignature(onSuccess, onCancel) {
    if (!state.currentUser) {
        if (onCancel) onCancel();
        return;
    }

    // Asegurarse de quitar el overlay de carga para que el usuario pueda interactuar
    showLoading(false);

    const modalEl = document.getElementById('passwordConfirmModal');
    const form = document.getElementById('password-confirm-form');
    const newForm = form.cloneNode(true);
    form.parentNode.replaceChild(newForm, form);

    const usernameEl = newForm.querySelector('#confirm-password-username');
    const passwordInputEl = newForm.querySelector('#confirm-password-input');
    const errorEl = newForm.querySelector('#confirm-password-error');

    usernameEl.textContent = getCurrentUser().username;
    const usernameHiddenEl = newForm.querySelector('#confirm-password-username-hidden');
    if (usernameHiddenEl) usernameHiddenEl.value = getCurrentUser().username;
    passwordInputEl.value = '';
    errorEl.classList.add('d-none');

    let successCalled = false;

    const onHidden = () => {
        modalEl.removeEventListener('hidden.bs.modal', onHidden);
        if (!successCalled && onCancel) onCancel();
    };
    modalEl.addEventListener('hidden.bs.modal', onHidden);

    const handleConfirm = async (e) => {
        if (e) e.preventDefault();
        const passwordInput = passwordInputEl.value;
        const hashedPassword = await hashPassword(passwordInput);

        let isValid = false;
        if (state.currentUser.passwordIsHashed) {
            isValid = state.currentUser.password === hashedPassword;
        } else {
            isValid = state.currentUser.password === passwordInput;
        }

        if (isValid) {
            successCalled = true;
            state.modals.passwordConfirm.hide();
            onSuccess();
        } else {
            errorEl.classList.remove('d-none');
            passwordInputEl.focus();
        }
    };

    newForm.addEventListener('submit', handleConfirm);

    state.modals.passwordConfirm.show();

    // Enfocar el input automáticamente
    setTimeout(() => passwordInputEl.focus(), 500);
}

// --- Theme Management ---
async function toggleTheme() {
    const isDarkMode = document.body.classList.toggle('dark-mode');
    state.currentTheme = isDarkMode ? 'dark' : 'light';

    // Guardar preferencia si el usuario está logueado
    if (state.currentUser && state.currentUser.fb_id) {
        try {
            await updateDoc(doc(state.collections.technicians, state.currentUser.fb_id), {
                theme: state.currentTheme
            });
            state.currentUser.theme = state.currentTheme;
        } catch (error) {
            console.error("Error al guardar preferencia de tema:", error);
        }
    }

    updateChartsTheme();
    showToast(`Modo ${state.currentTheme === 'dark' ? 'oscuro' : 'claro'} activado`, 'info');
}

function updateChartsTheme() {
    const isDark = document.body.classList.contains('dark-mode');
    const textColor = isDark ? '#f1f2f6' : '#666';
    const gridColor = isDark ? 'rgba(255, 255, 255, 0.1)' : 'rgba(0, 0, 0, 0.1)';

    // Actualizar defaults de Chart.js
    Chart.defaults.color = textColor;
    if (Chart.defaults.scale && Chart.defaults.scale.grid) {
        Chart.defaults.scale.grid.color = gridColor;
    }

    // Actualizar instancias existentes
    Object.values(state.charts).forEach(chart => {
        if (!chart || !chart.options) return;

        if (chart.options.scales) {
            Object.values(chart.options.scales).forEach(scale => {
                if (scale.grid) scale.grid.color = gridColor;
                if (scale.ticks) scale.ticks.color = textColor;
            });
        }

        if (chart.options.plugins) {
            if (chart.options.plugins.legend && chart.options.plugins.legend.labels) {
                chart.options.plugins.legend.labels.color = textColor;
            }
            if (chart.options.plugins.title) {
                chart.options.plugins.title.color = textColor;
            }
        }

        chart.update();
    });
}

window.updateChartsTheme = updateChartsTheme;
window.applyUserPermissions = applyUserPermissions;

// Export functions to window for verification and external access
window.showPartModal = showPartModal;
window.renderPartDetailHTML = renderPartDetailHTML;
window.renderParts = renderParts;
window.showMachineDetail = showMachineDetail;
window.renderMachines = renderMachines;
window.executeWorkPlan = executeWorkPlan;
window.renderWorkPlans = renderWorkPlans;
window.showWorkPlanModal = showWorkPlanModal;
window.renderWorkPlanExecution = renderWorkPlanExecution;
window.updatePlanNotifications = updatePlanNotifications;
window.notifyAdminsAndPlanners = notifyAdminsAndPlanners;
window.checkMaintenanceExpirations = checkMaintenanceExpirations;

// --- Smart Monitoring Functions ---
function renderSmartMonitoring() {
    if (state.currentTab !== 'monitoreo-inteligente' && state.currentTab !== 'dashboard' && initialLoadComplete) return;
    requestAnimationFrame(() => {
    const container = document.getElementById('smart-monitoring-container');
    if (!container) return;
    container.innerHTML = '';

    const locations = new Set();
    state.machines.forEach(m => { if (m.location) locations.add(m.location); });
    renderSmartMonitoringFilters(Array.from(locations));

    let machinesToRender = [...state.machines];
    if (getCurrentUser().role === 'Jefe de Area' && Array.isArray(getCurrentUser().managedMachineIds)) {
        const managedIds = new Set(getCurrentUser().managedMachineIds);
        machinesToRender = state.machines.filter(m => managedIds.has(m.id));
    } else if (getCurrentUser().role === 'Técnico' && Array.isArray(getCurrentUser().equipoAsignado)) {
        const assignedIds = new Set(getCurrentUser().equipoAsignado);
        machinesToRender = state.machines.filter(m => assignedIds.has(m.id));
    }

    if (state.monitoringLocationFilter !== 'all') {
        machinesToRender = machinesToRender.filter(m => m.location === state.monitoringLocationFilter);
    }

    // Filter out machines with monitoring disabled
    machinesToRender = machinesToRender.filter(m => m.monitoringEnabled !== false);

    if (machinesToRender.length === 0) {
        container.innerHTML = '<div class="col-12 text-center py-5 text-muted">No se encontraron equipos para monitorear.</div>';
        return;
    }

    machinesToRender.forEach(machine => {
        const cardCol = document.createElement('div');
        cardCol.className = 'col-12 col-md-6 col-xl-4';

        const machinePlans = state.workPlans.filter(p => p.machineId === machine.id);
        const planIds = new Set(machinePlans.map(p => p.fb_id));
        const executions = state.workPlanExecutions
            .filter(e => planIds.has(e.planId) && (e.status === 'Completado' || e.status === 'Pendiente de Evaluación'))
            .sort((a, b) => new Date(b.executedAt) - new Date(a.executedAt));

        // Get latest measurements
        const measurements = {}; // key: task description, value: { value, unit, status, date, trend, history: [] }

        // Collect history first to determine trend
        const historyMap = {};

        [...executions].reverse().forEach(ex => {
            (ex.taskGroups || []).forEach(group => {
                group.tasks.forEach(task => {
                    if (task.type === 'numeric') {
                        const key = task.description;
                        if (!historyMap[key]) historyMap[key] = [];
                        historyMap[key].push({
                            value: parseFloat(task.value),
                            unit: task.unit,
                            status: task.status,
                            date: ex.executedAt,
                            executedBy: ex.executedBy,
                            workOrderId: ex.workOrderId || 'N/A'
                        });
                    } else if (task.type === 'multi_numeric') {
                        (task.fields || []).forEach((field, fIdx) => {
                            const key = `${task.description} - ${field}`;
                            if (!historyMap[key]) historyMap[key] = [];
                            const val = task.multiValues ? parseFloat(task.multiValues[fIdx]) : NaN;
                            historyMap[key].push({
                                value: val,
                                unit: task.unit,
                                status: task.status,
                                date: ex.executedAt,
                                executedBy: ex.executedBy,
                                workOrderId: ex.workOrderId || 'N/A'
                            });
                        });
                    }
                });
            });
        });

        // Integration with RTDB Sensors
        const rtdbMachineData = state.rtdbMonitoring && state.rtdbMonitoring[machine.id];
        const rtdbSensors = rtdbMachineData?.sensors || {};

        const latestMeasurements = [];

        // Combine Keys from History and RTDB Sensors
        const allKeys = new Set([...Object.keys(historyMap), ...Object.keys(rtdbSensors)]);

        allKeys.forEach(key => {
            const history = historyMap[key] || [];
            const rtdbValue = rtdbSensors[key];

            let latestValue, unit, status, date, trend, isRtdb = false;

            if (rtdbValue !== undefined) {
                // RTDB sensor data takes precedence for "latest"
                latestValue = typeof rtdbValue === 'object' ? rtdbValue.value : rtdbValue;
                unit = typeof rtdbValue === 'object' ? rtdbValue.unit : (history[0].unit || '');
                date = typeof rtdbValue === 'object' ? rtdbValue.timestamp : new Date().toISOString();
                isRtdb = true;

                // Determine trend from last history point if available
                const lastHistory = history[history.length - 1];
                if (lastHistory) {
                    if (latestValue > lastHistory.value) trend = 'up';
                    else if (latestValue < lastHistory.value) trend = 'down';
                    else trend = 'stable';
                } else {
                    trend = 'stable';
                }
            } else if (history.length > 0) {
                const latest = history[history.length - 1];
                latestValue = latest.value;
                unit = latest.unit;
                status = latest.status;
                date = latest.date;

                if (history.length > 1) {
                    const prev = history[history.length - 2];
                    if (latestValue > prev.value) trend = 'up';
                    else if (latestValue < prev.value) trend = 'down';
                    else trend = 'stable';
                } else {
                    trend = 'stable';
                }
            } else {
                return; // No data for this key
            }

            // Get config for this variable
            const config = state.monitoringConfigs.find(c => c.machineId === machine.id);
            if (!config || !config.variables) return;
            const varConfig = config.variables.find(v => v.name === key);

            // Skip if explicitly disabled in config
            if (varConfig && varConfig.enabled === false) return;

            let currentStatus = status || 'Aprobó';

            // Apply custom thresholds if available
            if (varConfig && varConfig.thresholds) {
                const val = parseFloat(latestValue);
                const t = varConfig.thresholds;

                const isInRange = (thr) => {
                    if (!thr) return false;
                    const min = thr.min !== "" ? parseFloat(thr.min) : null;
                    const max = thr.max !== "" ? parseFloat(thr.max) : null;

                    if (min !== null && max !== null) return val >= min && val <= max;
                    if (min !== null) return val >= min;
                    if (max !== null) return val <= max;
                    return false;
                };

                if (isInRange(t.normal)) currentStatus = 'Aprobó';
                else if (isInRange(t.warning)) currentStatus = 'Alerta';
                else if (isInRange(t.critical)) currentStatus = 'Fallo';
                else currentStatus = 'Fallo';
            }

            if (currentStatus === 'Alerta' || currentStatus === 'Fallo') trend = 'alert';

            latestMeasurements.push({
                key: key,
                value: latestValue,
                unit: unit,
                status: currentStatus,
                date: date,
                trend: trend,
                isLive: isRtdb
            });
        });

        // Machine Status logic
        let overallStatus = 'operativo';
        if (latestMeasurements.some(m => m.status === 'Fallo')) overallStatus = 'fallo';
        else if (latestMeasurements.some(m => m.status === 'Alerta')) overallStatus = 'alerta';

        // Override with RTDB Live Status if available
        if (rtdbMachineData && rtdbMachineData.liveStatus) {
            overallStatus = rtdbMachineData.liveStatus;
        }

        // --- Telegram Monitoring Alert ---
        const lastStatus = state.lastNotifiedMachineStatus[machine.id];
        if (overallStatus !== 'operativo' && overallStatus !== lastStatus) {
            const failingVars = latestMeasurements.filter(m => m.status === 'Fallo' || m.status === 'Alerta');
            let alertMsg = `⚠️ <b>ALERTA DE MONITOREO: ${escapeHTML(machine.name)}</b>\n\n` +
                           `<b>Estado:</b> ${overallStatus.toUpperCase()}\n` +
                           `<b>ID:</b> ${escapeHTML(machine.id)}\n` +
                           `<b>Ubicación:</b> ${escapeHTML(machine.location || 'N/A')}\n\n` +
                           `<b>Variables fuera de parámetro:</b>\n`;

            failingVars.forEach(v => {
                alertMsg += `• ${escapeHTML(v.key)}: ${v.value}${escapeHTML(v.unit || '')} (${v.status})\n`;
            });

            notifyAdminsAndPlanners(alertMsg).catch(err => console.warn("[Telegram Monitoring Alert Error]", err));
        }
        state.lastNotifiedMachineStatus[machine.id] = overallStatus;

        const statusLabel = overallStatus.toUpperCase();

        let measurementsHTML = '';
        latestMeasurements.forEach((m, idx) => {
            const trendIcon = {
                up: '<i class="fas fa-long-arrow-alt-up trend-up"></i>',
                down: '<i class="fas fa-long-arrow-alt-down trend-down"></i>',
                stable: '<i class="fas fa-check trend-stable"></i>',
                alert: '<i class="fas fa-exclamation-triangle trend-alert"></i>'
            }[m.trend];

            measurementsHTML += `
                <div class="measurement-item ${m.isLive ? 'live-sensor' : ''}" onclick="showMeasurementHistory('${machine.id}', '${m.key.replace(/'/g, "\\'")}')">
                    <div class="measurement-label">${m.key} ${m.isLive ? '<span class="live-dot" title="Tiempo Real"></span>' : ''}</div>
                    <div class="measurement-value">${m.value}${m.unit ? ' ' + m.unit : ''}</div>
                    <div class="measurement-trend">${trendIcon}</div>
                </div>
            `;
        });

        if (latestMeasurements.length === 0) {
            measurementsHTML = '<div class="col-12 text-center py-4 text-muted small fst-italic">Sin mediciones registradas</div>';
        }

        cardCol.innerHTML = `
            <div class="monitoring-card">
                <div class="monitoring-card-header">
                    <div class="machine-icon-wrapper">
                        <i class="fas fa-microchip"></i>
                        <div class="status-indicator-top ${overallStatus}"></div>
                    </div>
                    <div class="machine-info-main">
                        <h5>${machine.name}</h5>
                        <div class="sn-text">SN: ${machine.serial || machine.id}</div>
                    </div>
                    <div class="dropdown">
                        <button class="btn btn-link text-muted p-0" data-bs-toggle="dropdown"><i class="fas fa-ellipsis-v"></i></button>
                        <ul class="dropdown-menu shadow border-0">
                            <li><a class="dropdown-item" href="#" onclick="showMachineDetail('${machine.fb_id}')"><i class="fas fa-info-circle me-2"></i>Ver Detalle</a></li>
                            <li><a class="dropdown-item" href="#" onclick="executeWorkPlanForMachine('${machine.id}')"><i class="fas fa-play-circle me-2"></i>Nuevo Plan</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="#" onclick="showMonitoringConfig('${machine.id}')"><i class="fas fa-cog me-2"></i>Configuración</a></li>
                        </ul>
                    </div>
                </div>

                <div class="measurements-grid">
                    ${measurementsHTML}
                </div>

                <div class="monitoring-card-footer">
                    <div class="footer-status-text status-text-${overallStatus}">
                        ${statusLabel}
                    </div>
                    ${(overallStatus === 'fallo' || overallStatus === 'alerta') ? `<button class="btn-wo-monitoring" onclick="createWorkOrderFromMonitoring('${machine.id}')">Orden de Trabajo</button>` :
                        `<a href="#" class="footer-action-link" onclick="showMachineDetail('${machine.fb_id}')">Ver detalle <i class="fas fa-arrow-right ms-1"></i></a>`
                    }
                </div>
            </div>
        `;
        container.appendChild(cardCol);
    });
    });
}

function renderSmartMonitoringFilters(locations) {
    const container = document.getElementById('monitoring-filters');
    if (!container) return;

    const currentFilter = state.monitoringLocationFilter;
    const activeBtn = document.getElementById('active-location-filter');

    container.innerHTML = `<li><button class="dropdown-item filter-btn ${currentFilter === 'all' ? 'active' : ''}" data-location="all">Todos</button></li>`;

    locations.sort().forEach(loc => {
        const li = document.createElement('li');
        li.innerHTML = `<button class="dropdown-item filter-btn ${currentFilter === loc ? 'active' : ''}" data-location="${loc}">${loc}</button>`;
        container.appendChild(li);
    });

    if (activeBtn) {
        activeBtn.innerHTML = `<i class="fas fa-map-marker-alt me-2"></i> Área: ${currentFilter === 'all' ? 'Todos' : currentFilter}`;
    }

    container.querySelectorAll('.filter-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
            state.monitoringLocationFilter = btn.dataset.location;
            renderSmartMonitoring();
        });
    });
}

async function showMonitoringConfig(machineId) {
    const machine = state.machines.find(m => m.id === machineId);
    if (!machine) return;

    showLoading(true);

    // Get current config or create default
    let config = state.monitoringConfigs.find(c => c.machineId === machineId);
    if (!config) {
        config = {
            machineId: machineId,
            variables: []
        };
    }

    // Ensure all variables from plans are present
    const planVars = new Set();
    state.workPlans.filter(p => p.machineId === machineId).forEach(p => {
        (p.taskGroups || []).forEach(g => {
            g.tasks.forEach(t => {
                if (t.type === 'numeric') planVars.add(t.description);
                else if (t.type === 'multi_numeric') {
                    (t.fields || []).forEach(f => planVars.add(`${t.description} - ${f}`));
                }
            });
        });
    });

    planVars.forEach(vName => {
        if (!config.variables.find(v => v.name === vName)) {
            config.variables.push({
                name: vName,
                enabled: true,
                description: '',
                thresholds: {
                    normal: { min: "", max: "" },
                    warning: { min: "", max: "" },
                    critical: { min: "", max: "" }
                }
            });
        }
    });

    state.currentMonitoringConfig = JSON.parse(JSON.stringify(config));
    state.activeMonitoringVar = null;

    document.getElementById('m-config-machine-name').textContent = machine.name;
    document.getElementById('monitoring-config-view').classList.remove('d-none');

    renderMonitoringConfigVariables();
    clearThresholdInputs();

    showLoading(false);
}

function hideMonitoringConfig() {
    document.getElementById('monitoring-config-view').classList.add('d-none');
    state.currentMonitoringConfig = null;
    state.activeMonitoringVar = null;
}

function renderMonitoringConfigVariables() {
    const container = document.getElementById('m-config-variables-container');
    container.innerHTML = '';

    const vars = state.currentMonitoringConfig.variables;

    if (vars.length === 0) {
        container.innerHTML = '<div class="text-center py-5 text-muted">No hay variables configuradas para este equipo.</div>';
        return;
    }

    const varIcons = {
        'temperatura': 'fa-thermometer-half',
        'voltaje': 'fa-bolt',
        'amperios': 'fa-bolt',
        'presión': 'fa-compress-arrows-alt',
        'aceite': 'fa-oil-can',
        'motor': 'fa-clock',
        'horas': 'fa-clock'
    };

    vars.forEach((v, idx) => {
        const card = document.createElement('div');
        card.className = `m-config-var-card ${state.activeMonitoringVar === v.name ? 'active' : ''}`;
        card.onclick = () => selectMonitoringVar(v.name);

        let iconClass = 'fa-chart-line';
        const lowerName = v.name.toLowerCase();
        for (const [key, icon] of Object.entries(varIcons)) {
            if (lowerName.includes(key)) {
                iconClass = icon;
                break;
            }
        }

        card.innerHTML = `
            <div class="m-config-var-icon">
                <i class="fas ${iconClass}"></i>
            </div>
            <div class="m-config-var-info">
                <h6>${v.name}</h6>
                <p>${v.description || 'Variable de monitoreo'}</p>
            </div>
            <label class="m-config-switch" onclick="event.stopPropagation()">
                <input type="checkbox" ${v.enabled !== false ? 'checked' : ''} onchange="toggleMonitoringVar('${v.name.replace(/'/g, "\\'")}', this.checked)">
                <span class="m-config-slider"></span>
            </label>
        `;
        container.appendChild(card);
    });
}

function selectMonitoringVar(varName) {
    state.activeMonitoringVar = varName;
    renderMonitoringConfigVariables();

    const v = state.currentMonitoringConfig.variables.find(v => v.name === varName);
    document.getElementById('m-config-active-var-name').textContent = v.name;

    const t = v.thresholds || {};
    ['normal', 'warning', 'critical'].forEach(type => {
        const tt = t[type] || { min: "", max: "" };
        document.getElementById(`m-threshold-${type}-min`).value = tt.min;
        document.getElementById(`m-threshold-${type}-max`).value = tt.max;
    });
}

function toggleMonitoringVar(varName, enabled) {
    const v = state.currentMonitoringConfig.variables.find(v => v.name === varName);
    if (v) v.enabled = enabled;
}

function clearThresholdInputs() {
    document.getElementById('m-config-active-var-name').textContent = 'Ninguna';
    ['normal', 'warning', 'critical'].forEach(type => {
        document.getElementById(`m-threshold-${type}-min`).value = "";
        document.getElementById(`m-threshold-${type}-max`).value = "";
    });
}

function updateActiveVarThresholds() {
    if (!state.activeMonitoringVar) return;

    const v = state.currentMonitoringConfig.variables.find(v => v.name === state.activeMonitoringVar);
    if (!v) return;

    if (!v.thresholds) v.thresholds = {};

    ['normal', 'warning', 'critical'].forEach(type => {
        v.thresholds[type] = {
            min: document.getElementById(`m-threshold-${type}-min`).value,
            max: document.getElementById(`m-threshold-${type}-max`).value
        };
    });
}

async function handleSaveMonitoringConfig() {
    if (!state.currentMonitoringConfig) return;

    showLoading(true);
    try {
        const config = state.currentMonitoringConfig;
        const existing = state.monitoringConfigs.find(c => c.machineId === config.machineId);

        if (existing) {
            await updateDoc(doc(state.collections.monitoringConfigs, existing.fb_id), config);
        } else {
            await addDoc(state.collections.monitoringConfigs, config);
        }

        showToast('Configuración guardada correctamente.', 'success');
        hideMonitoringConfig();
    } catch (error) {
        console.error("Error saving monitoring config:", error);
        showToast('Error al guardar la configuración.', 'error');
    } finally {
        showLoading(false);
    }
}

function handleAddMonitoringVariable() {
    const varName = document.getElementById('new-variable-name').value.trim();
    if (!varName) {
        showToast('Ingrese un nombre para la variable', 'warning');
        return;
    }

    if (state.currentMonitoringConfig.variables.find(v => v.name === varName)) {
        showToast('Esta variable ya existe.', 'warning');
        return;
    }

    state.currentMonitoringConfig.variables.push({
        name: varName,
        enabled: true,
        description: 'Variable personalizada',
        thresholds: {
            normal: { min: "", max: "" },
            warning: { min: "", max: "" },
            critical: { min: "", max: "" }
        }
    });

    state.modals.variableName.hide();
    renderMonitoringConfigVariables();
    selectMonitoringVar(varName);
}

async function showMeasurementHistory(machineId, measurementKey) {
    const machine = state.machines.find(m => m.id === machineId);
    if (!machine) return;

    const machinePlans = state.workPlans.filter(p => p.machineId === machine.id);
    const planIds = new Set(machinePlans.map(p => p.fb_id));
    const executions = state.workPlanExecutions
        .filter(e => planIds.has(e.planId) && (e.status === 'Completado' || e.status === 'Pendiente de Evaluación'))
        .sort((a, b) => new Date(a.executedAt) - new Date(b.executedAt));

    const history = [];
    executions.forEach(ex => {
        (ex.taskGroups || []).forEach(group => {
            group.tasks.forEach(task => {
                if (task.type === 'numeric' && task.description === measurementKey) {
                    history.push({
                        value: parseFloat(task.value),
                        date: ex.executedAt,
                        executedBy: ex.executedBy,
                        workOrderId: ex.workOrderId || 'N/A'
                    });
                } else if (task.type === 'multi_numeric') {
                    (task.fields || []).forEach((field, fIdx) => {
                        const key = `${task.description} - ${field}`;
                        if (key === measurementKey) {
                            const val = task.multiValues ? parseFloat(task.multiValues[fIdx]) : NaN;
                            if (!isNaN(val)) {
                                history.push({
                                    value: val,
                                    date: ex.executedAt,
                                    executedBy: ex.executedBy,
                                    workOrderId: ex.workOrderId || 'N/A'
                                });
                            }
                        }
                    });
                }
            });
        });
    });

    if (history.length === 0) return;

    const data = history.map(h => h.value);
    const minVal = Math.min(...data);
    const maxVal = Math.max(...data);

    document.getElementById('history-modal-title').innerHTML = `
        Historial: ${measurementKey} (${machine.name})
        <span class="ms-3 badge bg-info x-small">Mín: ${minVal}</span>
        <span class="badge bg-warning text-dark x-small">Máx: ${maxVal}</span>
    `;

    const tableBody = document.getElementById('measurement-history-table-body');
    tableBody.innerHTML = history.slice().reverse().map(h => `
        <tr>
            <td>${new Date(h.date).toLocaleString()}</td>
            <td class="fw-bold">${h.value}</td>
            <td>${h.executedBy}</td>
            <td>${h.workOrderId}</td>
        </tr>
    `).join('');

    state.modals.measurementHistory.show();

    // Render Chart
    setTimeout(() => {
        const ctx = document.getElementById('measurementHistoryChart').getContext('2d');
        if (state.charts.measurementHistory) {
            state.charts.measurementHistory.destroy();
        }

        const labels = history.map(h => new Date(h.date).toLocaleDateString());
        const padding = (maxVal - minVal) * 0.2 || 1;

        state.charts.measurementHistory = new Chart(ctx, {
            type: 'line',
            data: {
                labels: labels,
                datasets: [{
                    label: measurementKey,
                    data: data,
                    borderColor: '#3498db',
                    backgroundColor: 'rgba(52, 152, 219, 0.1)',
                    fill: true,
                    tension: 0.3,
                    pointRadius: 5,
                    pointHoverRadius: 8
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                scales: {
                    y: {
                        beginAtZero: false,
                        suggestedMin: minVal - padding,
                        suggestedMax: maxVal + padding,
                        grid: { color: state.currentTheme === 'dark' ? 'rgba(255,255,255,0.1)' : 'rgba(0,0,0,0.1)' }
                    },
                    x: {
                        grid: { display: false }
                    }
                },
                plugins: {
                    legend: { display: false },
                    tooltip: {
                        callbacks: {
                            label: (context) => `Valor: ${context.parsed.y}`
                        }
                    }
                }
            }
        });
    }, 300);
}

function getMonitoringFailureDescription(machineId) {
    const machine = state.machines.find(m => m.id === machineId);
    if (!machine) return "";

    const machinePlans = state.workPlans.filter(p => p.machineId === machine.id);
    const planIds = new Set(machinePlans.map(p => p.fb_id));
    const executions = state.workPlanExecutions
        .filter(e => planIds.has(e.planId) && (e.status === 'Completado' || e.status === 'Pendiente de Evaluación'))
        .sort((a, b) => new Date(b.executedAt) - new Date(a.executedAt));

    const historyMap = {};
    [...executions].reverse().forEach(ex => {
        (ex.taskGroups || []).forEach(group => {
            group.tasks.forEach(task => {
                if (task.type === 'numeric') {
                    const key = task.description;
                    if (!historyMap[key]) historyMap[key] = [];
                    historyMap[key].push({ value: parseFloat(task.value), unit: task.unit });
                } else if (task.type === 'multi_numeric') {
                    (task.fields || []).forEach((field, fIdx) => {
                        const key = `${task.description} - ${field}`;
                        if (!historyMap[key]) historyMap[key] = [];
                        const val = task.multiValues ? parseFloat(task.multiValues[fIdx]) : NaN;
                        historyMap[key].push({ value: val, unit: task.unit });
                    });
                }
            });
        });
    });

    const config = state.monitoringConfigs.find(c => c.machineId === machine.id);
    const failures = [];

    Object.keys(historyMap).forEach(key => {
        const history = historyMap[key].filter(h => !isNaN(h.value));
        if (history.length === 0) return;
        const latest = history[history.length - 1];
        const val = latest.value;

        const varConfig = config.variables.find(v => v.name === key);
        if (varConfig && varConfig.enabled !== false && varConfig.thresholds) {
            const t = varConfig.thresholds;
            const normal = t.normal;

            const min = normal.min !== "" ? parseFloat(normal.min) : null;
            const max = normal.max !== "" ? parseFloat(normal.max) : null;

            let isOk = true;
            if (min !== null && max !== null) isOk = val >= min && val <= max;
            else if (min !== null) isOk = val >= min;
            else if (max !== null) isOk = val <= max;

            if (!isOk) {
                let label = key;
                let suffix = "fuera de parámetro";

                const lowerKey = key.toLowerCase();
                if (lowerKey.includes("voltaje")) label = "Voltaje";
                else if (lowerKey.includes("amperaje") || lowerKey.includes("amperio")) label = "Amperios";

                if (min !== null && val < min) {
                    suffix = "bajo";
                } else if (max !== null && val > max) {
                    suffix = "alto";
                }

                failures.push(`${label} ${suffix} (${val}${latest.unit || ''})`);
            }
        }
    });

    return failures.length > 0 ? failures.join(", ") : "Parámetros fuera de rango detectados en monitoreo.";
}

async function createWorkOrderFromMonitoring(machineId) {
    const failureDesc = getMonitoringFailureDescription(machineId);
    await showWorkOrderModal(null, 'Correctivo', machineId);
    if (failureDesc) {
        document.getElementById('wo-description').value = failureDesc;
    }
}

function executeWorkPlanForMachine(machineId) {
    const plans = state.workPlans.filter(p => p.machineId === machineId && p.isActive);
    if (plans.length === 0) {
        showToast('No hay planes activos para este equipo.', 'warning');
        return;
    }
    // Si hay más de uno, ir a la pestaña de planes. Si hay uno, ejecutarlo.
    if (plans.length === 1) {
        executeWorkPlan(plans[0].fb_id);
    } else {
        switchTab('planes-trabajo');
    }
}

window.renderSmartMonitoring = renderSmartMonitoring;
window.showMeasurementHistory = showMeasurementHistory;
window.createWorkOrderFromMonitoring = createWorkOrderFromMonitoring;
window.executeWorkPlanForMachine = executeWorkPlanForMachine;
window.addToPurchaseCart = addToPurchaseCart;
window.switchTab = switchTab;
window.showMonitoringConfig = showMonitoringConfig;
window.hideMonitoringConfig = hideMonitoringConfig;
window.selectMonitoringVar = selectMonitoringVar;
window.toggleMonitoringVar = toggleMonitoringVar;
window.saveWorkOrder = saveWorkOrder;
window.notifyTechnicians = notifyTechnicians;
window.handleKanbanWorkOrderAction = handleKanbanWorkOrderAction;
window.showLoading = showLoading;
window.requestMachineStatusOnPause = requestMachineStatusOnPause;

// --- Executive Report Functions ---
async function generateExecutiveReportData() {
    const month = parseInt(document.getElementById('report-executive-month').value);
    const year = parseInt(document.getElementById('report-executive-year').value);
    const startDateInput = document.getElementById('report-executive-start-date').value;
    const endDateInput = document.getElementById('report-executive-end-date').value;

    let startDate, endDate, reportTitle;

    if (startDateInput && endDateInput) {
        startDate = new Date(startDateInput + 'T00:00:00');
        endDate = new Date(endDateInput + 'T23:59:59');
        reportTitle = `Periodo: ${startDate.toLocaleDateString()} al ${endDate.toLocaleDateString()}`;
    } else {
        if (isNaN(month) || isNaN(year)) {
            showToast('Seleccione un mes/año o un rango de fechas.', 'error');
            return;
        }
        startDate = new Date(year, month, 1);
        endDate = new Date(year, month + 1, 0, 23, 59, 59);
        const monthName = document.getElementById('report-executive-month').options[month].text;
        reportTitle = `${monthName} ${year}`;
    }

    showLoading(true);
    switchTab('executive-report-preview');

    setTimeout(async () => {
        try {
            const data = await calculateExecutiveMetrics(startDate, endDate);
            data.reportTitle = reportTitle;
            data.startDate = startDate;
            data.endDate = endDate;
            renderExecutiveReportPreview(data);

            document.getElementById('export-executive-excel-btn').onclick = () => exportExecutiveToExcel(data);
            document.getElementById('export-executive-pdf-btn').onclick = () => exportExecutiveToPDF(data);
        } catch (error) {
            console.error("Error generating executive report:", error);
            showToast('Error al generar el informe ejecutivo.', 'error');
        } finally {
            showLoading(false);
        }
    }, 500);
}

async function calculateExecutiveMetrics(startDate, endDate) {
    const prevMonthStart = new Date(startDate);
    prevMonthStart.setMonth(prevMonthStart.getMonth() - 1);
    const prevMonthEnd = new Date(startDate.getTime() - 1);

    const filterArea = document.getElementById('report-executive-area').value;
    const filterCC = document.getElementById('report-executive-cost-center').value;
    const filterType = document.getElementById('report-executive-type').value;

    const applyFilters = (orders) => {
        return orders.filter(o => {
            const machine = state.machines.find(m => m.id === o.machineId);
            const areaMatch = filterArea === 'all' || (machine && machine.location === filterArea);
            const ccMatch = filterCC === 'all' || (machine && machine.centroCosto === filterCC);
            const typeMatch = filterType === 'all' || o.type === filterType;
            return areaMatch && ccMatch && typeMatch;
        });
    };

    const ordersInPeriod = applyFilters(state.workOrders.filter(o => {
        if (!o.date) return false;
        const d = new Date(o.date + 'T12:00:00Z');
        return d >= startDate && d <= endDate;
    }));

    const ordersPrevPeriod = applyFilters(state.workOrders.filter(o => {
        if (!o.date) return false;
        const d = new Date(o.date + 'T12:00:00Z');
        return d >= prevMonthStart && d <= prevMonthEnd;
    }));

    const failureTypes = ['Correctivo', 'Emergencia'];

    // 1. Resumen General
    const totalGenerated = ordersInPeriod.length;
    const totalAttended = ordersInPeriod.filter(o => ['Completado', 'Pendiente de Evaluación', 'Pendiente de Aprobación'].includes(o.status)).length;
    const pendingAtClose = ordersInPeriod.filter(o => ['Pendiente', 'En Proceso', 'Pausado'].includes(o.status)).length;

    const byType = ordersInPeriod.reduce((acc, o) => {
        acc[o.type] = (acc[o.type] || 0) + 1;
        return acc;
    }, {});

    const byArea = ordersInPeriod.reduce((acc, o) => {
        const machine = state.machines.find(m => m.id === o.machineId);
        const area = machine.location || 'Sin Área';
        acc[area] = (acc[area] || 0) + 1;
        return acc;
    }, {});

    // 2. Costos
    const completedCurrent = ordersInPeriod.filter(o => o.status === 'Completado');
    const currentCosts = calculateTotalCostForMultiple(completedCurrent);
    const prevCosts = calculateTotalCostForMultiple(ordersPrevPeriod.filter(o => o.status === 'Completado'));

    const costByMachine = {};
    const costByType = {};

    const partsMap = new Map(state.parts.map(p => [p.id, p]));
    const techMap = new Map(state.technicians.map(t => [t.username, t]));

    completedCurrent.forEach(o => {
        const cost = calculateTotalCost(o, false, partsMap, techMap);
        costByMachine[o.machineId] = (costByMachine[o.machineId] || 0) + cost;
        costByType[o.type] = (costByType[o.type] || 0) + cost;
    });

    // 3. KPIs
    const kpis = calculateKpisForPeriod(ordersInPeriod, startDate, endDate);
    const prevKpis = calculateKpisForPeriod(ordersPrevPeriod, prevMonthStart, prevMonthEnd);

    let reincidenceCount = 0;
    const sortedOrders = [...state.workOrders].sort((a,b) => new Date(a.date) - new Date(b.date));
    ordersInPeriod.filter(o => failureTypes.includes(o.type)).forEach(order => {
        const prevFailures = sortedOrders.filter(so =>
            so.machineId === order.machineId &&
            failureTypes.includes(so.type) &&
            so.fb_id !== order.fb_id &&
            new Date(so.date) < new Date(order.date) &&
            (new Date(order.date) - new Date(so.date)) < (30 * 24 * 60 * 60 * 1000) &&
            (so.failureType === order.failureType || (so.description && order.description && so.description.toLowerCase() === order.description.toLowerCase()))
        );
        if (prevFailures.length > 0) reincidenceCount++;
    });

    const completedInPeriod = ordersInPeriod.filter(o => o.status === 'Completado' && o.date && o.fechaFinalizacionReal);
    let avgAttentionTimeHours = 0;
    if (completedInPeriod.length > 0) {
        const totalAttentionMs = completedInPeriod.reduce((sum, o) => {
            const start = new Date(o.date + 'T00:00:00Z');
            const end = new Date(o.fechaFinalizacionReal);
            return sum + Math.max(0, end - start);
        }, 0);
        avgAttentionTimeHours = (totalAttentionMs / completedInPeriod.length) / (1000 * 60 * 60);
    }

    // 4. Análisis de Fallas por Equipo (Incluye todos los equipos intervenidos)
    const machineIdsInPeriod = [...new Set(ordersInPeriod.map(o => o.machineId))];
    const machinesInPeriod = state.machines.filter(m => machineIdsInPeriod.includes(m.id));

    const machineAnalysis = machinesInPeriod.map(m => {
        const mOrders = ordersInPeriod.filter(o => o.machineId === m.id);
        const failures = mOrders.filter(o => failureTypes.includes(o.type));
        const downtimeMs = failures.reduce((sum, o) => sum + (o.totalWorkDurationMs || getTotalWorkDurationMs(o)), 0);

        const causes = failures.reduce((acc, o) => {
            const fType = o.type === 'Calibración' ? 'Falla de Operación' : (o.failureType || 'Otras');
            acc[fType] = (acc[fType] || 0) + 1;
            return acc;
        }, {});
        const mainCause = Object.entries(causes).sort((a,b) => b[1] - a[1])[0]?.[0] || 'N/A';

        return {
            id: m.id,
            name: m.name,
            failures: failures.length,
            downtimeHours: downtimeMs / (1000 * 60 * 60),
            mainCause,
            cost: mOrders.filter(o => o.status === 'Completado').reduce((sum, o) => sum + calculateTotalCost(o), 0)
        };
    }).sort((a,b) => b.cost - a.cost);

    // 5. Paros
    const downtimeByArea = {};
    ordersInPeriod.filter(o => failureTypes.includes(o.type)).forEach(o => {
        const machine = state.machines.find(m => m.id === o.machineId);
        const area = machine.location || 'Sin Área';
        const hours = (o.totalWorkDurationMs || getTotalWorkDurationMs(o)) / (1000 * 60 * 60);
        downtimeByArea[area] = (downtimeByArea[area] || 0) + hours;
    });

    return {
        summary: { totalGenerated, totalAttended, pendingAtClose, byType, byArea },
        costs: {
            current: currentCosts,
            prev: prevCosts,
            variation: prevCosts.totalCost > 0 ? ((currentCosts.totalCost - prevCosts.totalCost) / prevCosts.totalCost) * 100 : 0,
            byMachine: Object.entries(costByMachine).map(([id, cost]) => ({ id, name: state.machines.find(m => m.id === id).name || id, cost })).sort((a,b) => b.cost - a.cost),
            byType: costByType
        },
        kpis: {
            current: { ...kpis, reincidence: reincidenceCount, avgAttentionTime: avgAttentionTimeHours },
            prev: { ...prevKpis, reincidence: 0, avgAttentionTime: 0 }
        },
        machineAnalysis,
        downtime: {
            byMachine: machineAnalysis.map(ma => ({ name: ma.name, hours: ma.downtimeHours, impact: ma.downtimeHours * (state.machines.find(m => m.id === ma.id).costoParoHora || 0) })).sort((a,b) => b.hours - a.hours),
            byArea: Object.entries(downtimeByArea).map(([name, hours]) => ({ name, hours }))
        }
    };
}

function renderExecutiveReportPreview(data) {
    const container = document.getElementById('executive-report-content');

    const getTrendIcon = (curr, prev, higherIsBetter) => {
        if (curr === null || prev === null || isNaN(curr) || isNaN(prev)) return '';
        const diff = curr - prev;
        if (Math.abs(diff) < 0.001) return '<i class="fas fa-minus text-muted ms-1" title="Sin variación"></i>';
        const up = diff > 0;
        const color = (up === higherIsBetter) ? 'text-success' : 'text-danger';
        return `<i class="fas fa-arrow-${up ? 'up' : 'down'} ${color} ms-1"></i>`;
    };

    const getTrafficLight = (val, type, machineId = 'all') => {
        if (type === 'mtbf') {
            if (val < 15) return 'bg-danger';
            if (val <= 19.9) return 'bg-warning text-dark';
            return 'bg-success';
        }
        if (type === 'mttr') {
            if (val > 8) return 'bg-danger';
            if (val >= 5) return 'bg-warning text-dark';
            return 'bg-success';
        }
        if (type === 'availability') return val >= 95 ? 'bg-success' : (val >= 90 ? 'bg-warning text-dark' : 'bg-danger');
        if (type === 'pmp') return val >= 90 ? 'bg-success' : (val >= 75 ? 'bg-warning text-dark' : 'bg-danger');
        return 'bg-secondary';
    };

    const formatKpiValue = (val, type) => {
        if (val === null || val === undefined) return 'N/A';
        if (type === 'mtbf' || type === 'mttr') return val.toFixed(1);
        if (type === 'availability' || type === 'pmp') return val.toFixed(1) + '%';
        return val;
    };

    container.innerHTML = `
        <div class="row mb-4 border-bottom pb-3">
            <div class="col-md-8">
                <h3 class="text-primary fw-bold">RESUMEN EJECUTIVO DE MANTENIMIENTO</h3>
                <h5 class="text-muted">${data.reportTitle}</h5>
            </div>
            <div class="col-md-4 text-end">
                <p class="mb-0"><strong>Responsable:</strong> ${getCurrentUser().username}</p>
                <p class="mb-0"><strong>Fecha de Emisión:</strong> ${new Date().toLocaleDateString()}</p>
            </div>
        </div>

        <div class="row g-3 mb-4">
            <div class="col-md-2">
                <div class="card h-100 text-center border-0 bg-light shadow-sm">
                    <div class="card-body">
                        <small class="text-uppercase fw-bold text-muted d-block mb-1" title="${getKpiFormula('mtbf')}">MTBF (días)</small>
                        <h3 class="mb-0">${formatKpiValue(data.kpis.current.mtbf, 'mtbf')}</h3>
                        <div class="mt-2">
                            <span class="badge ${getTrafficLight(data.kpis.current.mtbf, 'mtbf')}">Estado</span>
                            ${getTrendIcon(data.kpis.current.mtbf, data.kpis.prev.mtbf, true)}
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="card h-100 text-center border-0 bg-light shadow-sm">
                    <div class="card-body">
                        <small class="text-uppercase fw-bold text-muted d-block mb-1" title="${getKpiFormula('mttr')}">MTTR (horas)</small>
                        <h3 class="mb-0">${formatKpiValue(data.kpis.current.mttr, 'mttr')}</h3>
                        <div class="mt-2">
                            <span class="badge ${getTrafficLight(data.kpis.current.mttr, 'mttr')}">Estado</span>
                            ${getTrendIcon(data.kpis.current.mttr, data.kpis.prev.mttr, false)}
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="card h-100 text-center border-0 bg-light shadow-sm">
                    <div class="card-body px-1">
                        <small class="text-uppercase fw-bold text-muted d-block mb-1" title="${getKpiFormula('availability')}" style="font-size: 0.75rem;">Disponibilidad</small>
                        <h3 class="mb-0">${formatKpiValue(data.kpis.current.availability, 'availability')}</h3>
                        <div class="mt-2">
                            <span class="badge ${getTrafficLight(data.kpis.current.availability, 'availability')}">Meta</span>
                            ${getTrendIcon(data.kpis.current.availability, data.kpis.prev.availability, true)}
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="card h-100 text-center border-0 bg-light shadow-sm">
                    <div class="card-body">
                        <small class="text-uppercase fw-bold text-muted d-block mb-1" title="${getKpiFormula('pmp')}">Cumplimiento PMP</small>
                        <h3 class="mb-0">${formatKpiValue(data.kpis.current.pmp, 'pmp')}</h3>
                        <div class="mt-2">
                            <span class="badge ${getTrafficLight(data.kpis.current.pmp, 'pmp')}">Meta</span>
                            ${getTrendIcon(data.kpis.current.pmp, data.kpis.prev.pmp, true)}
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="card h-100 text-center border-0 bg-light shadow-sm">
                    <div class="card-body">
                        <small class="text-uppercase fw-bold text-muted d-block mb-1" title="${getKpiFormula('backlog')}">Backlog</small>
                        <h3 class="mb-0">${data.summary.pendingAtClose}</h3>
                        <p class="small text-muted mb-0">Órdenes Pendientes</p>
                    </div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="card h-100 text-center border-0 bg-light shadow-sm">
                    <div class="card-body">
                        <small class="text-uppercase fw-bold text-muted d-block mb-1" title="${getKpiFormula('reincidence')}">Reincidencia</small>
                        <h3 class="mb-0">${data.kpis.current.reincidence}</h3>
                        <p class="small text-muted mb-0">Repetición Fallas</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="row g-3 mb-4">
            <div class="col-md-4">
                <div class="card h-100 text-center border-0 bg-light shadow-sm">
                    <div class="card-body">
                        <small class="text-uppercase fw-bold text-muted d-block mb-1" title="${getKpiFormula('attention')}">Tiempo Prom. Atención (Horas)</small>
                        <h3 class="mb-0">${data.kpis.current.avgAttentionTime.toFixed(1) || '0'}</h3>
                        <div class="mt-2">
                            ${getTrendIcon(data.kpis.current.avgAttentionTime, data.kpis.prev.avgAttentionTime, false)}
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mb-4">
            <div class="col-md-6">
                <h6 class="fw-bold border-start border-primary border-4 ps-2 mb-3">ANÁLISIS DE COSTOS</h6>
                <table class="table table-sm table-bordered">
                    <thead class="table-light">
                        <tr><th>Concepto</th><th class="text-end">Costo (Lps)</th></tr>
                    </thead>
                    <tbody>
                        <tr><td>Mano de Obra</td><td class="text-end">${formatCurrency(data.costs.current.laborCost)}</td></tr>
                        <tr><td>Repuestos / Insumos</td><td class="text-end">${formatCurrency(data.costs.current.partsCost)}</td></tr>
                        <tr><td>Servicios Externos</td><td class="text-end">${formatCurrency(data.costs.current.externalCost)}</td></tr>
                        <tr class="table-info fw-bold">
                            <td>COSTO TOTAL</td><td class="text-end">${formatCurrency(data.costs.current.totalCost)}</td>
                        </tr>
                        <tr>
                            <td>Variación vs Periodo Ant.</td>
                            <td class="text-end ${data.costs.variation > 0 ? 'text-danger' : 'text-success'}">
                                ${data.costs.variation.toFixed(1)}% ${getTrendIcon(data.costs.current.totalCost, data.costs.prev.totalCost, false)}
                            </td>
                        </tr>
                    </tbody>
                </table>
                <h6 class="fw-bold border-start border-primary border-4 ps-2 mb-3 mt-4">DISTRIBUCIÓN POR TIPO</h6>
                <table class="table table-sm table-hover">
                    <tbody>
                        ${Object.entries(data.costs.byType).map(([type, cost]) => `<tr><td>${type}</td><td class="text-end">${formatCurrency(cost)}</td></tr>`).join('')}
                    </tbody>
                </table>
            </div>
            <div class="col-md-6">
                <h6 class="fw-bold border-start border-primary border-4 ps-2 mb-3">RESUMEN OPERATIVO</h6>
                <div class="row text-center mb-3">
                    <div class="col-4">
                        <div class="p-2 border rounded bg-light">
                            <small class="d-block text-muted">Generadas</small>
                            <span class="h4 fw-bold">${data.summary.totalGenerated}</span>
                        </div>
                    </div>
                    <div class="col-4">
                        <div class="p-2 border rounded bg-light">
                            <small class="d-block text-muted">Atendidas</small>
                            <span class="h4 fw-bold">${data.summary.totalAttended}</span>
                        </div>
                    </div>
                    <div class="col-4">
                        <div class="p-2 border rounded bg-light">
                            <small class="d-block text-muted">Pendientes</small>
                            <span class="h4 fw-bold">${data.summary.pendingAtClose}</span>
                        </div>
                    </div>
                </div>
                <div style="height: 200px;">
                    <canvas id="executiveOrdersChart"></canvas>
                </div>
            </div>
        </div>

        <div class="row mb-4">
            <div class="col-md-6">
                <h6 class="fw-bold border-start border-primary border-4 ps-2 mb-3">RANKING EQUIPOS MÁS COSTOSOS</h6>
                <div class="table-responsive">
                    <table class="table table-sm table-hover">
                        <thead class="table-light">
                            <tr><th>Equipo</th><th class="text-end">Costo (Lps)</th></tr>
                        </thead>
                        <tbody>
                            ${data.costs.byMachine.slice(0, 5).map(m => `<tr><td>${m.name}</td><td class="text-end">${formatCurrency(m.cost)}</td></tr>`).join('')}
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col-md-6">
                <h6 class="fw-bold border-start border-primary border-4 ps-2 mb-3">EQUIPOS CON MAYOR TIEMPO DE PARO</h6>
                <div class="table-responsive">
                    <table class="table table-sm table-hover">
                        <thead class="table-light">
                            <tr><th>Equipo</th><th class="text-end">Horas Paro</th><th class="text-end">Impacto Est.</th></tr>
                        </thead>
                        <tbody>
                            ${data.downtime.byMachine.slice(0, 5).map(m => `<tr><td>${m.name}</td><td class="text-end">${m.hours.toFixed(1)}h</td><td class="text-end">${formatCurrency(m.impact)}</td></tr>`).join('')}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <h6 class="fw-bold border-start border-primary border-4 ps-2 mb-3">ANÁLISIS DE FALLAS POR EQUIPO</h6>
                <div class="table-responsive">
                    <table class="table table-sm table-bordered">
                        <thead class="table-dark">
                            <tr>
                                <th>Equipo</th>
                                <th class="text-center">Num. Fallas</th>
                                <th class="text-center">Tiempo Paro (h)</th>
                                <th>Causa Principal</th>
                                <th class="text-end">Costo Asociado</th>
                            </tr>
                        </thead>
                        <tbody>
                            ${data.machineAnalysis.map(ma => `
                                <tr>
                                    <td>${ma.name}</td>
                                    <td class="text-center">${ma.failures}</td>
                                    <td class="text-center">${ma.downtimeHours.toFixed(1)}</td>
                                    <td>${ma.mainCause}</td>
                                    <td class="text-end">${formatCurrency(ma.cost)}</td>
                                </tr>
                            `).join('') || '<tr><td colspan="5" class="text-center">No hay equipos críticos registrados</td></tr>'}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-12">
                <h6 class="fw-bold border-start border-primary border-4 ps-2 mb-3">TENDENCIA DE MANTENIMIENTO (12 MESES)</h6>
                <div style="height: 300px;">
                    <canvas id="executiveTrendChart"></canvas>
                </div>
            </div>
        </div>
    `;

    const ctx = document.getElementById('executiveOrdersChart').getContext('2d');
    if (state.charts.executiveOrders) state.charts.executiveOrders.destroy();

    state.charts.executiveOrders = new Chart(ctx, {
        type: 'bar',
        data: {
            labels: Object.keys(data.summary.byType),
            datasets: [{
                label: 'Cant. Órdenes',
                data: Object.values(data.summary.byType),
                backgroundColor: ['#3498db', '#e74c3c', '#f1c40f', '#8e44ad', '#2ecc71', '#d35400']
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } }
        }
    });

    // 12-Month Trend Chart
    const trendCtx = document.getElementById('executiveTrendChart').getContext('2d');
    if (state.charts.executiveTrend) state.charts.executiveTrend.destroy();

    const trendLabels = [];
    const preventiveCounts = [];
    const correctiveCounts = [];
    const baseDate = data.startDate || new Date();

    const ordersByMonthAndType = {};
    state.workOrders.forEach(o => {
        if (!o.date || !o.type) return;
        const monthKey = o.date.substring(0, 7);
        if (!ordersByMonthAndType[monthKey]) {
            ordersByMonthAndType[monthKey] = { 'Preventivo': 0, 'Correctivo': 0 };
        }
        if (['Preventivo', 'Predictivo', 'Calibración', 'Mecanizado'].includes(o.type)) {
            ordersByMonthAndType[monthKey]['Preventivo']++;
        } else if (['Correctivo', 'Emergencia'].includes(o.type)) {
            ordersByMonthAndType[monthKey]['Correctivo']++;
        }
    });

    for (let i = 11; i >= 0; i--) {
        const d = new Date(baseDate.getFullYear(), baseDate.getMonth() - i, 1);
        trendLabels.push(d.toLocaleDateString('es-ES', { month: 'short', year: '2-digit' }));
        const monthKey = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`;
        const stats = ordersByMonthAndType[monthKey] || { 'Preventivo': 0, 'Correctivo': 0 };
        preventiveCounts.push(stats['Preventivo']);
        correctiveCounts.push(stats['Correctivo']);
    }

    state.charts.executiveTrend = new Chart(trendCtx, {
        type: 'line',
        data: {
            labels: trendLabels,
            datasets: [
                {
                    label: 'Preventivos',
                    data: preventiveCounts,
                    borderColor: '#3498db',
                    backgroundColor: 'rgba(52, 152, 219, 0.1)',
                    fill: true,
                    tension: 0.3
                },
                {
                    label: 'Correctivos',
                    data: correctiveCounts,
                    borderColor: '#e74c3c',
                    backgroundColor: 'rgba(231, 76, 60, 0.1)',
                    fill: true,
                    tension: 0.3
                }
            ]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { position: 'top' } }
        }
    });
}

async function exportExecutiveToPDF(data) {
    const { jsPDF } = window.jspdf;
    const doc = new jsPDF();
    const margin = 14;
    const logoBase64 = typeof GLOBAL_LOGO !== 'undefined' ? GLOBAL_LOGO : "";

    doc.setFillColor(30, 59, 138);
    doc.rect(0, 0, 210, 40, 'F');

    if (logoBase64) {
        try { doc.addImage(logoBase64, 'PNG', margin, 7, 60, 20); } catch(e){}
    }

    doc.setTextColor(255, 255, 255);
    doc.setFontSize(20);
    doc.setFont(undefined, 'bold');
    doc.text('INFORME MENSUAL EJECUTIVO', 200, 20, { align: 'right' });
    doc.setFontSize(12);
    doc.setFont(undefined, 'normal');
    doc.text(data.reportTitle, 200, 27, { align: 'right' });
    doc.setFontSize(10);
    doc.text(`Responsable: ${getCurrentUser().username}`, 200, 34, { align: 'right' });

    doc.setTextColor(0, 0, 0);
    let y = 50;

    doc.setFontSize(14);
    doc.setFont(undefined, 'bold');
    doc.text('1. Resumen Ejecutivo', margin, y);
    y += 10;
    doc.setFontSize(10);
    doc.setFont(undefined, 'normal');
    const summaryText = `Durante el periodo ${data.reportTitle}, se gestionaron un total de ${data.summary.totalGenerated} órdenes de trabajo, de las cuales ${data.summary.totalAttended} fueron atendidas satisfactoriamente. Se cerró el periodo con ${data.summary.pendingAtClose} órdenes pendientes (Backlog). El costo total de mantenimiento ascendió a ${formatCurrency(data.costs.current.totalCost)}, representando una variación del ${data.costs.variation.toFixed(1)}% respecto al periodo anterior.`;
    const splitSummary = doc.splitTextToSize(summaryText, 180);
    doc.text(splitSummary, margin, y);
    y += (splitSummary.length * 5) + 10;

    doc.autoTable({
        startY: y,
        head: [['Indicador KPI', 'Valor Actual', 'Tendencia', 'Meta']],
        body: [
            ['Disponibilidad (%)', `${data.kpis.current.availability.toFixed(1) || '0'}%`, data.kpis.current.availability > data.kpis.prev.availability ? 'Mejora' : 'Descenso', '95%'],
            ['MTBF (días)', data.kpis.current.mtbf.toFixed(1) || 'N/A', data.kpis.current.mtbf > data.kpis.prev.mtbf ? 'Mejora' : 'Descenso', '20-30'],
            ['MTTR (horas)', data.kpis.current.mttr.toFixed(1) || 'N/A', data.kpis.current.mttr < data.kpis.prev.mttr ? 'Mejora' : 'Descenso', '< 5h'],
            ['Cumplimiento PMP (%)', `${data.kpis.current.pmp.toFixed(1) || '0'}%`, data.kpis.current.pmp > data.kpis.prev.pmp ? 'Mejora' : 'Descenso', '90%'],
            ['Reincidencia de Fallas', data.kpis.current.reincidence, '-', '0']
        ],
        theme: 'grid',
        headStyles: { fillColor: [44, 62, 80] }
    });
    y = doc.autoTable.previous.finalY + 15;

    doc.setFontSize(14);
    doc.setFont(undefined, 'bold');
    doc.text('2. Análisis de Costos', margin, y);
    y += 7;
    doc.autoTable({
        startY: y,
        head: [['Categoría', 'Monto (Lps)', '% del Total']],
        body: [
            ['Mano de Obra', formatCurrency(data.costs.current.laborCost), `${((data.costs.current.laborCost / data.costs.current.totalCost)*100 || 0).toFixed(1)}%`],
            ['Repuestos', formatCurrency(data.costs.current.partsCost), `${((data.costs.current.partsCost / data.costs.current.totalCost)*100 || 0).toFixed(1)}%`],
            ['Servicios Externos', formatCurrency(data.costs.current.externalCost), `${((data.costs.current.externalCost / data.costs.current.totalCost)*100 || 0).toFixed(1)}%`],
            ['TOTAL', formatCurrency(data.costs.current.totalCost), '100%']
        ],
        theme: 'striped'
    });
    y = doc.autoTable.previous.finalY + 15;

    doc.setFontSize(14);
    doc.setFont(undefined, 'bold');
    doc.text('3. Rankings de Desempeño', margin, y);
    y += 7;

    const rankingData = [];
    const maxRank = Math.max(data.costs.byMachine.length, data.downtime.byMachine.length);
    for (let i = 0; i < Math.min(maxRank, 10); i++) {
        rankingData.push([
            i + 1,
            data.costs.byMachine[i].name || '-',
            formatCurrency(data.costs.byMachine[i].cost || 0),
            data.downtime.byMachine[i].name || '-',
            (data.downtime.byMachine[i].hours || 0).toFixed(1) + ' h'
        ]);
    }

    doc.autoTable({
        startY: y,
        head: [['#', 'Equipo (Más Costoso)', 'Costo', 'Equipo (Mayor Paro)', 'Horas Paro']],
        body: rankingData,
        theme: 'striped',
        headStyles: { fillColor: [127, 140, 141] }
    });
    y = doc.autoTable.previous.finalY + 15;

    if (y > 200) { doc.addPage(); y = 20; }
    doc.setFontSize(14);
    doc.setFont(undefined, 'bold');
    doc.text('4. Análisis de Fallas por Equipo', margin, y);
    y += 7;
    doc.autoTable({
        startY: y,
        head: [['Equipo', 'Fallas', 'Paro (h)', 'Causa Principal', 'Costo Asociado']],
        body: data.machineAnalysis.map(ma => [ma.name, ma.failures, ma.downtimeHours.toFixed(1), ma.mainCause, formatCurrency(ma.cost)]),
        theme: 'grid',
        headStyles: { fillColor: [30, 59, 138] }
    });

    y = doc.autoTable.previous.finalY + 15;
    if (y > 220) { doc.addPage(); y = 20; }
    doc.setFontSize(14);
    doc.setFont(undefined, 'bold');
    doc.text('5. Análisis de Paros por Área', margin, y);
    y += 7;
    doc.autoTable({
        startY: y,
        head: [['Área / Ubicación', 'Total Horas Paro']],
        body: data.downtime.byArea.map(a => [a.name, a.hours.toFixed(1) + ' h']),
        theme: 'striped'
    });

    const pageCount = doc.internal.getNumberOfPages();
    for(let i = 1; i <= pageCount; i++) {
        doc.setPage(i);
        doc.setFontSize(8);
        doc.text(`CORINFAR CMMS - Informe Ejecutivo - Página ${i} de ${pageCount}`, margin, 285);
        doc.text(`Fecha Emisión: ${new Date().toLocaleString()}`, 200, 285, { align: 'right' });
    }

    doc.save(`Informe_Ejecutivo_${data.reportTitle.replace(/\s/g, '_')}.pdf`);
}

function exportExecutiveToExcel(data) {
    const wb = XLSX.utils.book_new();

    const summaryData = [
        ["INFORME MENSUAL EJECUTIVO"],
        ["Periodo", data.reportTitle],
        ["Emitido por", getCurrentUser().username],
        ["Fecha Emisión", new Date().toLocaleString()],
        [],
        ["INDICADORES KPI", "Valor", "Meta"],
        ["MTBF (días)", data.kpis.current.mtbf || 0, "20-30"],
        ["MTTR (horas)", data.kpis.current.mttr || 0, "< 5"],
        ["Disponibilidad (%)", data.kpis.current.availability || 0, "95%"],
        ["Cumplimiento PMP (%)", data.kpis.current.pmp || 0, "90%"],
        ["Reincidencia", data.kpis.current.reincidence, "0"],
        ["Tiempo Prom. Atención (h)", data.kpis.current.avgAttentionTime || 0, "-"],
        [],
        ["RESUMEN OPERATIVO", "Cantidad"],
        ["OT Generadas", data.summary.totalGenerated],
        ["OT Atendidas", data.summary.totalAttended],
        ["Backlog (Pendientes)", data.summary.pendingAtClose],
    ];
    const wsSummary = XLSX.utils.aoa_to_sheet(summaryData);
    XLSX.utils.book_append_sheet(wb, wsSummary, "Resumen y KPIs");

    const costData = [
        ["ANÁLISIS DE COSTOS", "Monto (Lps)"],
        ["Mano de Obra", data.costs.current.laborCost],
        ["Repuestos", data.costs.current.partsCost],
        ["Servicios Externos", data.costs.current.externalCost],
        ["COSTO TOTAL", data.costs.current.totalCost],
        ["Variación vs Anterior (%)", data.costs.variation],
        [],
        ["COSTO POR EQUIPO (TOP 20)", "Costo (Lps)"],
        ...data.costs.byMachine.slice(0, 20).map(m => [m.name, m.cost])
    ];
    const wsCosts = XLSX.utils.aoa_to_sheet(costData);
    XLSX.utils.book_append_sheet(wb, wsCosts, "Costos");

    const assetData = [
        ["ANÁLISIS DE FALLAS POR EQUIPO", "Fallas", "Horas Paro", "Causa Principal", "Costo"],
        ...data.machineAnalysis.map(ma => [ma.name, ma.failures, ma.downtimeHours, ma.mainCause, ma.cost]),
        [],
        ["PAROS POR ÁREA", "Horas Totales"],
        ...data.downtime.byArea.map(a => [a.name, a.hours])
    ];
    const wsAssets = XLSX.utils.aoa_to_sheet(assetData);
    XLSX.utils.book_append_sheet(wb, wsAssets, "Análisis Equipos");

    XLSX.writeFile(wb, `Informe_Ejecutivo_${data.reportTitle.replace(/\s/g, '_')}.xlsx`);
}

function getKpiFormula(type) {
    const formulas = {
        'mtbf': 'MTBF = (Tiempo Operativo Total) / (Número de Fallas)',
        'mttr': 'MTTR = (Tiempo Total de Reparación) / (Número de Reparaciones)',
        'availability': 'Disponibilidad = [(Tiempo Programado - Tiempo Parada) / Tiempo Programado] x 100',
        'pmp': 'Cumplimiento PMP = (Mantenimientos Preventivos Completados / Programados) x 100',
        'backlog': 'Backlog = Órdenes de trabajo pendientes al cierre del periodo',
        'attention': 'Tiempo Atención = Promedio de tiempo desde creación hasta finalización de OT',
        'reincidence': 'Reincidencia = Cantidad de fallas repetidas en el mismo equipo en < 30 días'
    };
    return formulas[type] || '';
}

window.generateExecutiveReportData = generateExecutiveReportData;
window.renderPublicMachineReport = renderPublicMachineReport;
window.showPartHistoryModal = showPartHistoryModal;
window.showPartDetail = showPartDetail;
window.showPartModal = showPartModal;

async function renderPublicMachineReport(machineId) {
    const machine = state.machines.find(m => m.id === machineId || m.fb_id === machineId);
    if (!machine) {
        showToast('Activo no encontrado', 'error');
        return;
    }

    // Prepare UI
    const loginOverlay = document.getElementById('login-overlay');
    if (loginOverlay) loginOverlay.classList.add('d-none');

    const appWrapper = document.getElementById('app-wrapper');
    if (appWrapper) appWrapper.classList.remove('d-none');

    const sidebar = document.querySelector('.sidebar');
    if (sidebar) sidebar.classList.add('d-none');

    const header = document.querySelector('.header');
    if (header) header.classList.add('d-none');

    const mainContent = document.querySelector('.main-content');
    if (mainContent) {
        mainContent.style.marginLeft = '0';
        mainContent.style.paddingTop = '0';
    }

    // Direct tab switch logic without side effects
    document.querySelectorAll('.main-content > .tab-content').forEach(tab => tab.classList.add('d-none'));
    const publicTab = document.getElementById('public-machine-report');
    if (publicTab) publicTab.classList.remove('d-none');
    state.currentTab = 'public-machine-report';

    const content = document.getElementById('public-report-content');

    // Calculate KPIs (simplified version of generateMachineReport logic)
    const machineWorkOrders = state.workOrders.filter(o => o.machineId === machine.id);
    const correctiveOrders = machineWorkOrders.filter(o => ['Correctivo', 'Emergencia'].includes(o.type));

    let mtbf = 'N/A';
    // Prioritize createdAt, then commissioningDate, then purchaseDate. Default to 30 days ago if none exist.
    let startDateRaw = machine.createdAt || machine.commissioningDate || machine.purchaseDate;
    let startDate = startDateRaw ? new Date(startDateRaw) : new Date(Date.now() - (30 * 24 * 60 * 60 * 1000));

    if (startDate && !isNaN(startDate.getTime())) {
        const totalTimeMs = Math.max(0, new Date() - startDate);
        const mtbfMs = totalTimeMs / (correctiveOrders.length + 1);
        mtbf = `${(mtbfMs / (1000 * 60 * 60 * 24)).toFixed(1)}d`;
    }

    let mttr = '0h';
    const completedCorrective = correctiveOrders.filter(o => o.status === 'Completado');
    if (completedCorrective.length > 0) {
        const totalRepairTimeMs = completedCorrective.reduce((sum, o) => sum + getTotalWorkDurationMs(o), 0);
        mttr = `${(totalRepairTimeMs / (completedCorrective.length * 1000 * 60 * 60)).toFixed(1)}h`;
    }

    const recentOrders = machineWorkOrders
        .sort((a, b) => new Date(b.date || 0) - new Date(a.date || 0))
        .slice(0, 5);

    const iqVal = machine.iqStatus ? 'Validado' : 'Pendiente';
    const oqVal = machine.oqStatus ? 'Validado' : 'Pendiente';
    const pqVal = machine.pqStatus ? 'Vigente' : 'Vencido';

    content.innerHTML = `
        <div class="row g-4">
            <!-- Columna de Imagen e Info Básica -->
            <div class="col-lg-4">
                <div class="card shadow-sm border-0 mb-4 overflow-hidden">
                    <div class="p-3 bg-light border-bottom text-center">
                        <h6 class="fw-bold mb-0">${machine.name}</h6>
                    </div>
                    <div class="p-4 text-center">
                        ${machine.imageUrl ? `<img src="${machine.imageUrl}" class="img-fluid rounded shadow-sm mb-3" style="max-height: 200px;">` :
                            `<div class="text-muted py-4"><i class="fas fa-image fa-4x mb-2"></i><br>Sin Imagen</div>`
                        }
                        <div class="badge bg-primary-subtle text-primary px-3 py-2 rounded-pill">ID: ${machine.id}</div>
                    </div>
                </div>

                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-body">
                        <h6 class="fw-bold mb-3"><i class="fas fa-info-circle me-2 text-primary"></i>Datos Técnicos</h6>
                        <ul class="list-unstyled mb-0">
                            <li class="mb-2"><small class="text-muted d-block">Marca/Modelo</small> ${machine.brand || 'N/A'} - ${machine.model || 'N/A'}</li>
                            <li class="mb-2"><small class="text-muted d-block">Serie</small> <span class="font-mono">${machine.serialNumber || 'N/A'}</span></li>
                            <li class="mb-0"><small class="text-muted d-block">Ubicación</small> ${machine.location || 'N/A'}</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Columna de Estado y KPIs -->
            <div class="col-lg-8">
                <!-- KPIs Row -->
                <div class="row g-3 mb-4">
                    <div class="col-md-4">
                        <div class="card border-0 shadow-sm text-center p-3 h-100">
                            <div class="text-muted small mb-1">MTBF (Confiabilidad)</div>
                            <h4 class="fw-bold text-success mb-0">${mtbf}</h4>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card border-0 shadow-sm text-center p-3 h-100">
                            <div class="text-muted small mb-1">MTTR (Mantenibilidad)</div>
                            <h4 class="fw-bold text-warning mb-0">${mttr}</h4>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card border-0 shadow-sm text-center p-3 h-100">
                            <div class="text-muted small mb-1">Criticidad</div>
                            <h4 class="fw-bold text-danger mb-0">${machine.criticidad || 'Baja'}</h4>
                        </div>
                    </div>
                </div>

                <!-- Qualification Status -->
                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-body">
                        <h6 class="fw-bold mb-3"><i class="fas fa-certificate me-2 text-primary"></i>Estado de Calificación</h6>
                        <div class="row g-2">
                            <div class="col-4">
                                <div class="p-2 border rounded text-center ${machine.iqStatus ? 'bg-success bg-opacity-10 border-success' : 'bg-light'}">
                                    <small class="d-block text-muted">IQ</small>
                                    <span class="fw-bold small ${machine.iqStatus ? 'text-success' : ''}">${iqVal}</span>
                                </div>
                            </div>
                            <div class="col-4">
                                <div class="p-2 border rounded text-center ${machine.oqStatus ? 'bg-success bg-opacity-10 border-success' : 'bg-light'}">
                                    <small class="d-block text-muted">OQ</small>
                                    <span class="fw-bold small ${machine.oqStatus ? 'text-success' : ''}">${oqVal}</span>
                                </div>
                            </div>
                            <div class="col-4">
                                <div class="p-2 border rounded text-center ${machine.pqStatus ? 'bg-primary bg-opacity-10 border-primary' : 'bg-light'}">
                                    <small class="d-block text-muted">PQ</small>
                                    <span class="fw-bold small ${machine.pqStatus ? 'text-primary' : ''}">${pqVal}</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Recent History -->
                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-body">
                        <h6 class="fw-bold mb-3"><i class="fas fa-history me-2 text-primary"></i>Últimos Mantenimientos</h6>
                        <div class="table-responsive">
                            <table class="table table-sm table-hover align-middle mb-0">
                                <thead class="table-light">
                                    <tr>
                                        <th>Fecha</th>
                                        <th>Tipo</th>
                                        <th>Estado</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    ${recentOrders.map(o => `
                                        <tr>
                                            <td class="small">${o.date || 'N/A'}</td>
                                            <td><span class="badge ${['Preventivo', 'Predictivo', 'Mecanizado', 'Calibración'].includes(o.type) ? 'bg-info' : 'bg-danger'}" style="font-size: 0.6rem;">${o.type}</span></td>
                                            <td><span class="badge ${o.status === 'Completado' ? 'bg-success' : 'bg-warning text-dark'}" style="font-size: 0.6rem;">${o.status}</span></td>
                                        </tr>
                                    `).join('')}
                                    ${recentOrders.length === 0 ? '<tr><td colspan="3" class="text-center text-muted py-2">No hay registros</td></tr>' : ''}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Documentation -->
                <div class="card shadow-sm border-0">
                    <div class="card-body">
                        <h6 class="fw-bold mb-3"><i class="fas fa-book me-2 text-primary"></i>Manuales y Documentos</h6>
                        <div class="d-flex gap-3">
                            ${machine.userManualUrl ? `<a href="${machine.userManualUrl}" target="_blank" class="btn btn-outline-danger btn-sm flex-grow-1"><i class="fas fa-file-pdf me-2"></i>Manual Usuario</a>` :
                                `<button class="btn btn-light btn-sm flex-grow-1 disabled"><i class="fas fa-file-pdf me-2"></i>No hay Manual</button>`
                            }
                            ${machine.techManualUrl ? `<a href="${machine.techManualUrl}" target="_blank" class="btn btn-outline-primary btn-sm flex-grow-1"><i class="fas fa-file-pdf me-2"></i>Manual Técnico</a>` :
                                `<button class="btn btn-light btn-sm flex-grow-1 disabled"><i class="fas fa-file-pdf me-2"></i>No hay Técnico</button>`
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    `;
}

// --- Decommissioning Functions ---

function showStockAdjustmentModal() {
    const partId = document.getElementById('part-id').value;
    if (!partId) return;
    const part = state.parts.find(p => p.id === partId);
    if (!part) return;

    document.getElementById('adjustment-part-id').value = part.id;
    document.getElementById('adjustment-current-stock').value = part.stockActual;
    document.getElementById('adjustment-new-stock').value = part.stockActual;
    document.getElementById('adjustment-reason').value = '';
    document.getElementById('adjustment-notes').value = '';

    state.modals.stockAdjustment.show();
}

async function handleStockAdjustmentSubmit(e) {
    e.preventDefault();
    const partId = document.getElementById('adjustment-part-id').value;
    const newStock = parseInt(document.getElementById('adjustment-new-stock').value);
    const reason = document.getElementById('adjustment-reason').value;
    const notes = document.getElementById('adjustment-notes').value;

    if (isNaN(newStock) || newStock < 0) {
        showToast('El stockActual no puede ser negativo.', 'error');
        return;
    }

    showLoading(true);
    try {
        const part = state.parts.find(p => p.id === partId);
        const oldStock = part.stockActual || 0;
        const delta = newStock - oldStock;

        if (delta === 0) {
            state.modals.stockAdjustment.hide();
            showLoading(false);
            return;
        }

        const partRef = doc(state.collections.parts, partId);
        await updateDoc(partRef, { stockActual: newStock, lastModified: new Date().toISOString() });

        await recordPartMovement(partId, delta, 'AJUSTE', {
            details: `${reason}: ${notes}`.trim(),
            adjustmentReason: reason
        });

        await recordAuditLog('parts', partId, 'UPDATE', { stockActual: oldStock }, { stockActual: newStock }, `AJUSTE DE STOCK: ${reason}`);

        showToast('Stock ajustado correctamente.', 'success');
        state.modals.stockAdjustment.hide();

        // Notificación Telegram para ajuste de stock
        const stockMsg = `🔧 <b>Ajuste de Stock Manual</b>\n\n` +
                        `<b>Repuesto:</b> ${escapeHTML(part.description)} (${escapeHTML(part.id)})\n` +
                        `<b>Anterior:</b> ${oldStock}\n` +
                        `<b>Nuevo:</b> ${newStock}\n` +
                        `<b>Motivo:</b> ${escapeHTML(reason)}\n` +
                        `<b>Usuario:</b> ${escapeHTML(getCurrentUser().username)}`;

        notifyAdminsAndPlanners(stockMsg).catch(err => console.warn("[Telegram Stock Adjustment Error]", err));

        // Actualizar el input en el modal de repuesto si sigue abierto
        const stockInput = document.getElementById('part-stock');
        if (stockInput) stockInput.value = newStock;

    } catch (error) {
        console.error("Error adjusting stockActual:", error);
        showToast('Error al ajustar el stockActual.', 'error');
    } finally {
        showLoading(false);
    }
}

function showDeliveryModal(fbId) {
    const request = state.partRequests.find(r => r.fb_id === fbId);
    if (!request) return;

    document.getElementById('delivery-request-fb-id').value = fbId;
    document.getElementById('delivery-notes').value = '';

    const itemSelect = document.getElementById('delivery-item-index');
    const singleInfo = document.getElementById('single-item-delivery-info');
    const multiInfo = document.getElementById('delivery-items-selection');

    if (request.items && Array.isArray(request.items)) {
        multiInfo.style.display = 'block';
        singleInfo.style.display = 'none';

        let html = '';
        request.items.forEach((item, index) => {
            const pending = item.quantity - (item.deliveredQuantity || 0);
            if (pending > 0) {
                html += `<option value="${index}">${item.description} (Pte: ${pending})</option>`;
            }
        });
        itemSelect.innerHTML = html;
        handleDeliveryItemChange();
    } else {
        multiInfo.style.display = 'none';
        singleInfo.style.display = 'block';
        document.getElementById('delivery-material-desc').value = request.description;
        const pending = request.quantity - (request.deliveredQuantity || 0);
        document.getElementById('delivery-pending-qty').value = pending;
        document.getElementById('delivery-quantity').value = pending;
    }

    // Poblado de repuestos
    const select = document.getElementById('delivery-part-id');
    let partHtml = '<option value="">Seleccione un repuesto...</option>';
    state.parts.forEach(p => {
        partHtml += `<option value="${p.id}">${p.id} - ${p.description} (Stock: ${p.stockActual || 0})</option>`;
    });
    select.innerHTML = partHtml;

    state.modals.materialDelivery.show();
}

function handleDeliveryItemChange() {
    const fbId = document.getElementById('delivery-request-fb-id').value;
    const request = state.partRequests.find(r => r.fb_id === fbId);
    if (!request || !request.items) return;

    const index = parseInt(document.getElementById('delivery-item-index').value);
    const item = request.items[index];
    if (!item) return;

    const pending = item.quantity - (item.deliveredQuantity || 0);
    document.getElementById('delivery-pending-qty').value = pending;
    document.getElementById('delivery-quantity').value = pending;

    // Intentar pre-seleccionar repuesto
    const select = document.getElementById('delivery-part-id');
    const bestMatch = state.parts.find(p => p.id === item.description || p.description.toLowerCase().includes(item.description.toLowerCase()));
    if (bestMatch) select.value = bestMatch.id;
}

async function handleMaterialDeliverySubmit(e) {
    e.preventDefault();
    const fbId = document.getElementById('delivery-request-fb-id').value;
    const request = state.partRequests.find(r => r.fb_id === fbId);
    if (!request) return;

    const deliveryQty = parseInt(document.getElementById('delivery-quantity').value);
    const partId = document.getElementById('delivery-part-id').value;
    const notes = document.getElementById('delivery-notes').value;

    let pending = 0;
    let deliveredSoFar = 0;
    let itemIndex = -1;

    if (request.items) {
        itemIndex = parseInt(document.getElementById('delivery-item-index').value);
        const item = request.items[itemIndex];
        deliveredSoFar = item.deliveredQuantity || 0;
        pending = item.quantity - deliveredSoFar;
    } else {
        deliveredSoFar = request.deliveredQuantity || 0;
        pending = request.quantity - deliveredSoFar;
    }

    if (deliveryQty > pending) {
        showToast(`No puede entregar más de la cantidad pendiente (${pending}).`, 'error');
        return;
    }

    const part = state.parts.find(p => p.id === partId);
    if (!part || (part.stockActual || 0) < deliveryQty) {
        showToast('Stock insuficiente para realizar la entrega.', 'error');
        return;
    }

    // Paso de Firma Digital (Warehouse & Technician)
    requestSignature(async () => {
        showLoading(true);
        try {
            const batch = writeBatch(db);
            const now = new Date().toISOString();

            // 1. Crear Registro de Salida (MaterialOut)
            const materialOutRef = doc(collection(db, `/artifacts/${appId}/public/data/materialOut`));
            const materialOutData = {
                fb_id: materialOutRef.id,
                requestId: fbId,
                workOrderId: request.workOrderId || null,
                machineId: request.machineId || null,
                date: now,
                deliveryUser: getCurrentUser().username,
                recipientUser: request.requester,
                notes: notes,
                items: [
                    {
                        partId: partId,
                        description: part.description,
                        quantity: deliveryQty
                    }
                ]
            };
            batch.set(materialOutRef, materialOutData);

            // 2. Descontar Stock y Registrar Kardex
            const stockBeforeDelivery = part.stockActual || 0;
            const stockAfterDelivery = stockBeforeDelivery - deliveryQty;
            const partRef = doc(state.collections.parts, part.fb_id);
            batch.update(partRef, {
                stockActual: stockAfterDelivery,
                lastModified: now
            });

            const movementRef = doc(collection(db, `/artifacts/${appId}/public/data/partMovements`));
            batch.set(movementRef, buildPartMovementData(partId, -deliveryQty, 'SALIDA_MATERIAL', {
                date: now,
                user: getCurrentUser().username,
                recipientUser: request.requester,
                orderId: request.workOrderFbId || null,
                orderHumanId: request.workOrderId || 'SOL-' + fbId.substring(0,5),
                referenceType: request.workOrderId ? 'ORDEN_TRABAJO' : 'SOLICITUD_MATERIAL',
                movementSource: 'ALMACEN',
                stockBefore: stockBeforeDelivery,
                stockAfter: stockAfterDelivery,
                details: `Entrega material solicitud ${fbId}. ${notes || ''}`.trim()
            }));

            // 3. Actualizar Solicitud de Material
            const requestRef = doc(state.collections.partRequests, fbId);
            let updates = {
                lastModified: now,
                processedBy: getCurrentUser().username,
                deliverySignature: state.currentUser.signature || null
            };

            if (request.items) {
                const newItems = [...request.items];
                newItems[itemIndex].deliveredQuantity = (newItems[itemIndex].deliveredQuantity || 0) + deliveryQty;
                if (newItems[itemIndex].deliveredQuantity >= newItems[itemIndex].quantity) {
                    newItems[itemIndex].status = 'Entregado';
                }
                updates.items = newItems;

                const allDelivered = newItems.every(i => i.status === 'Entregado' || i.status === 'Recibido');
                updates.status = allDelivered ? 'Entregado' : 'En Despacho';
            } else {
                const newDeliveredQty = deliveredSoFar + deliveryQty;
                updates.deliveredQuantity = newDeliveredQty;
                updates.status = (newDeliveredQty >= request.quantity) ? 'Entregado' : 'En Despacho';
            }

            batch.update(requestRef, updates);

            await batch.commit();
            await updateRTDBMirror('partRequests', { ...request, ...updates }, fbId);
            await updateRTDBMirror('parts', { ...part, stockActual: stockAfterDelivery }, part.fb_id);

            // Log Auditoría
            await recordAuditLog('materialOut', materialOutRef.id, 'CREATE', null, materialOutData, 'Nueva Salida de Material');

            state.modals.materialDelivery.hide();
            showToast('Entrega realizada con éxito.', 'success');

            // Notificar a administradores, planificadores y grupo global
            const deliveryMsg = `📦 <b>Entrega de Material Realizada</b>\n\n` +
                               `<b>OT/Solicitud:</b> ${request.workOrderId || 'SOL-' + fbId.substring(0,5)}\n` +
                               `<b>Material:</b> ${escapeHTML(part.description)}\n` +
                               `<b>Cantidad:</b> ${deliveryQty}\n` +
                               `<b>Entregó:</b> ${escapeHTML(getCurrentUser().username)}\n` +
                               `<b>Recibió:</b> ${escapeHTML(request.requester)}`;

            notifyAdminsAndPlanners(deliveryMsg).catch(err => console.warn("[Telegram Delivery Notification Error]", err));

            // 4. Generar PDF Automático (DESHABILITADO por solicitud de usuario)
            // generateMaterialOutPDF(materialOutData);

        } catch (error) {
            console.error("Error confirming delivery:", error);
            showToast('Error al registrar la entrega.', 'error');
        } finally {
            showLoading(false);
        }
    }, () => {
        showToast('Entrega cancelada: se requiere firma (contraseña) para confirmar.', 'info');
    });
}

function updateInventoryDashboard() {
    if (state.currentTab !== 'inventario-dashboard' && initialLoadComplete) return;

    // 1. Calcular Estadísticas Básicas
    const totalValue = state.parts.reduce((sum, p) => sum + (p.stockActual * (p.cost || 0)), 0);
    const criticalCount = state.parts.filter(p => p.stockActual === 0).length;
    const reorderCount = state.parts.filter(p => p.stockActual > 0 && p.stockActual <= (p.reorderPoint || 0)).length;

    const now = new Date();
    const firstDayMonth = new Date(now.getFullYear(), now.getMonth(), 1).toISOString();
    const deliveriesMonth = (state.materialOut || []).filter(m => m.date >= firstDayMonth).length;

    // Métricas ISO
    const pendingRequests = state.partRequests.filter(r => ['Pendiente', 'Aprobado', 'Pedido', 'En Despacho'].includes(r.status));
    const pendingItemsCount = pendingRequests.reduce((sum, r) => {
        if (r.items) return sum + r.items.filter(i => !['Entregado', 'Recibido', 'Rechazado', 'Cancelado'].includes(i.status)).length;
        return sum + 1;
    }, 0);

    const returnsMonth = (state.partMovements || []).filter(m =>
        m.date >= firstDayMonth && (m.type === 'DEVOLUCION' || m.type === 'Devolución OT')
    ).length;

    document.getElementById('inv-stat-total-value').textContent = formatCurrency(totalValue);
    document.getElementById('inv-stat-critical').textContent = criticalCount;
    document.getElementById('inv-stat-reorder').textContent = reorderCount;
    document.getElementById('inv-stat-deliveries').textContent = deliveriesMonth;
    document.getElementById('inv-stat-pending').textContent = pendingItemsCount;
    document.getElementById('inv-stat-returns').textContent = returnsMonth;

    // 2. Tabla de Trazabilidad
    renderInventoryTraceability();

    // 2.1 Tabla de Solicitudes de Compra
    renderPurchaseRequestsTable();

    // 3. Tabla de Alertas
    const alertsTable = document.getElementById('inv-reorder-alerts-list');
    const alertParts = state.parts
        .filter(p => p.stockActual <= (p.reorderPoint || 0))
        .sort((a,b) => a.stockActual - b.stockActual);

    alertsTable.innerHTML = alertParts.map(p => `
        <tr>
            <td><span class="badge bg-light text-dark border">${p.id}</span></td>
            <td>${p.description}</td>
            <td class="fw-bold ${p.stockActual === 0 ? 'text-danger' : 'text-warning'}">${p.stockActual}</td>
            <td>${p.reorderPoint || 0}</td>
            <td><span class="text-primary">Pedir ${Math.max(0, (p.maxStock || 0) - p.stockActual)}</span></td>
            <td>
                <button class="btn btn-xs btn-outline-primary" onclick="showPartModal('${p.id}')">
                    <i class="fas fa-shopping-cart"></i> Solicitar
                </button>
            </td>
        </tr>
    `).join('') || '<tr><td colspan="6" class="text-center text-muted py-4">No hay alertas de reposición</td></tr>';

    // 3. Gráfico Top Consumidos
    const consumptionMap = {};
    (state.materialOut || []).filter(m => m.date >= firstDayMonth).forEach(m => {
        (m.items || []).forEach(item => {
            consumptionMap[item.description] = (consumptionMap[item.description] || 0) + item.quantity;
        });
    });

    const topConsumed = Object.entries(consumptionMap)
        .sort((a,b) => b[1] - a[1])
        .slice(0, 5);

    renderInvChart('invTopConsumedChart', {
        labels: topConsumed.map(x => x[0]),
        datasets: [{
            label: 'Cantidad Consumida',
            data: topConsumed.map(x => x[1]),
            backgroundColor: '#3498db'
        }]
    }, 'bar');

    // 4. Gráfico Consumo por Tipo de Mantenimiento
    const typeMap = {};
    (state.materialOut || []).forEach(m => {
        const workOrder = state.workOrders.find(wo => wo.id === m.workOrderId);
        const type = workOrder.type || 'No Vinculado';
        const cost = (m.items || []).reduce((sum, item) => {
            const part = state.parts.find(p => p.id === item.partId);
            return sum + (item.quantity * (part.cost || 0));
        }, 0);
        typeMap[type] = (typeMap[type] || 0) + cost;
    });

    renderInvChart('invTypeConsumptionChart', {
        labels: Object.keys(typeMap),
        datasets: [{
            data: Object.values(typeMap),
            backgroundColor: ['#1abc9c', '#3498db', '#9b59b6', '#f1c40f', '#e67e22', '#e74c3c']
        }]
    }, 'doughnut');

    // 5. Gráfico Consumo por Máquina
    const machineMap = {};
    (state.materialOut || []).forEach(m => {
        if (!m.machineId) return;
        const machine = state.machines.find(mac => mac.id === m.machineId);
        const name = machine ? `${machine.id} - ${machine.name}` : m.machineId;
        const cost = (m.items || []).reduce((sum, item) => {
            const part = state.parts.find(p => p.id === item.partId);
            return sum + (item.quantity * (part.cost || 0));
        }, 0);
        machineMap[name] = (machineMap[name] || 0) + cost;
    });

    const topMachines = Object.entries(machineMap)
        .sort((a,b) => b[1] - a[1])
        .slice(0, 5);

    renderInvChart('invMachineConsumptionChart', {
        labels: topMachines.map(x => x[0]),
        datasets: [{
            label: 'Costo Total (Lps)',
            data: topMachines.map(x => x[1]),
            backgroundColor: '#e67e22'
        }]
    }, 'bar');
}

function renderInventoryTraceability() {
    const container = document.getElementById('inv-traceability-list');
    if (!container) return;

    const searchTerm = document.getElementById('inv-trace-search').value.toLowerCase();

    // Unir movimientos de partes con datos de materialOut para vista unificada
    const movements = [...(state.partMovements || [])].sort((a, b) => new Date(b.date) - new Date(a.date));

    const filtered = movements.filter(m => {
        return m.partId.toLowerCase().includes(searchTerm) ||
               (m.partDescription || '').toLowerCase().includes(searchTerm) ||
               (m.orderHumanId || '').toLowerCase().includes(searchTerm) ||
               (m.user || '').toLowerCase().includes(searchTerm) ||
               (m.recipientUser || '').toLowerCase().includes(searchTerm);
    }).slice(0, 100); // Limitar a los últimos 100 para rendimiento

    container.innerHTML = filtered.map(m => {
        let typeClass = 'bg-secondary';
        if (m.type === 'Salida OT' || m.type === 'SALIDA_MATERIAL') typeClass = 'bg-danger bg-opacity-10 text-danger';
        if (m.type === 'Entrada Compra' || m.type === 'AJUSTE' && m.quantity > 0) typeClass = 'bg-success bg-opacity-10 text-success';
        if (m.type === 'DEVOLUCION' || m.type === 'Devolución OT') typeClass = 'bg-info bg-opacity-10 text-info';

        return `
            <tr>
                <td><small>${new Date(m.date).toLocaleString()}</small></td>
                <td>
                    <div class="fw-bold">${m.partId}</div>
                    <div class="x-small text-muted text-truncate" style="max-width: 150px;">${m.partDescription}</div>
                </td>
                <td class="fw-bold ${m.quantity > 0 ? 'text-success' : 'text-danger'}">${m.quantity > 0 ? '+' : ''}${m.quantity}</td>
                <td><span class="badge ${typeClass} border-0">${m.type}</span></td>
                <td><span class="badge bg-light text-dark border font-mono">${m.orderHumanId || 'N/A'}</span></td>
                <td><small>${m.user}</small></td>
                <td><small>${m.recipientUser || '-'}</small></td>
                <td><div class="x-small text-muted text-truncate" style="max-width: 200px;" title="${m.details || ''}">${m.details || '-'}</div></td>
            </tr>
        `;
    }).join('') || '<tr><td colspan="8" class="text-center py-4 text-muted">No se encontraron movimientos.</td></tr>';
}

renderInventoryTraceability = function() {
    const container = document.getElementById('inv-traceability-list');
    if (!container) return;

    const searchTerm = (document.getElementById('inv-trace-search').value || '').toLowerCase();
    const typeFilter = document.getElementById('inv-trace-type-filter').value || 'all';
    const movements = [...(state.partMovements || [])].sort((a, b) => new Date(b.date) - new Date(a.date));

    const filtered = movements.filter(m => {
        const typeText = `${m.type || ''} ${m.movementDirection || ''}`.toUpperCase();
        const matchesType = typeFilter === 'all' ||
            (typeFilter === 'AJUSTE' && typeText.includes('AJUST')) ||
            (typeFilter === 'DEVOLUCION' && (typeText.includes('DEVOL') || typeText.includes('REVERSION'))) ||
            (typeFilter === 'ENTRADA' && (m.movementDirection === 'ENTRADA' || Number(m.quantity) > 0)) ||
            (typeFilter === 'SALIDA' && (m.movementDirection === 'SALIDA' || Number(m.quantity) < 0));

        return matchesType && (
            (m.partId || '').toLowerCase().includes(searchTerm) ||
            (m.partDescription || '').toLowerCase().includes(searchTerm) ||
            (m.orderHumanId || '').toLowerCase().includes(searchTerm) ||
            (m.user || '').toLowerCase().includes(searchTerm) ||
            (m.recipientUser || '').toLowerCase().includes(searchTerm) ||
            (m.referenceType || '').toLowerCase().includes(searchTerm) ||
            (m.details || '').toLowerCase().includes(searchTerm)
        );
    }).slice(0, 100);

    container.innerHTML = filtered.map(m => {
        let typeClass = 'bg-secondary';
        const typeText = `${m.type || ''}`.toUpperCase();
        if (m.movementDirection === 'SALIDA' || typeText.includes('SALIDA')) typeClass = 'bg-danger bg-opacity-10 text-danger';
        if (m.movementDirection === 'ENTRADA' || typeText.includes('ENTRADA')) typeClass = 'bg-success bg-opacity-10 text-success';
        if (typeText.includes('DEVOL') || typeText.includes('REVERSION')) typeClass = 'bg-info bg-opacity-10 text-info';
        if (typeText.includes('AJUST')) typeClass = 'bg-warning bg-opacity-10 text-warning';

        const stockBefore = m.stockBefore ?? '-';
        const stockAfter = m.stockAfter ?? '-';
        const movementValue = Number(m.movementValue || 0);
        const qtyClass = Number(m.quantity) < 0 ? 'text-danger' : (Number(m.quantity) > 0 ? 'text-success' : 'text-muted');

        return `
            <tr>
                <td><small>${new Date(m.date).toLocaleString()}</small></td>
                <td>
                    <div class="fw-bold">${escapeHTML(m.partId || '-')}</div>
                    <div class="x-small text-muted text-truncate" style="max-width: 150px;">${escapeHTML(m.partDescription || '-')}</div>
                </td>
                <td class="fw-bold ${qtyClass}">${m.quantity > 0 ? '+' : ''}${m.quantity}</td>
                <td><span class="text-muted">${stockBefore}</span> <i class="fas fa-arrow-right mx-1 text-muted"></i> <span class="fw-bold">${stockAfter}</span></td>
                <td>${movementValue ? formatCurrency(movementValue) : '-'}</td>
                <td><span class="badge ${typeClass} border-0">${escapeHTML(m.type || '-')}</span></td>
                <td><span class="badge bg-light text-dark border font-mono">${escapeHTML(m.orderHumanId || m.reference || 'N/A')}</span></td>
                <td><small>${escapeHTML(m.user || '-')}</small></td>
                <td><small>${escapeHTML(m.recipientUser || '-')}</small></td>
                <td><div class="x-small text-muted text-truncate" style="max-width: 200px;" title="${escapeHTML(m.details || '')}">${escapeHTML(m.details || '-')}</div></td>
            </tr>
        `;
    }).join('') || '<tr><td colspan="10" class="text-center py-4 text-muted">No se encontraron movimientos.</td></tr>';
};

function exportInventoryTraceabilityCSV() {
    const movements = [...(state.partMovements || [])].sort((a, b) => new Date(b.date) - new Date(a.date));
    if (movements.length === 0) {
        showToast('No hay movimientos para exportar.', 'info');
        return;
    }

    const headers = [
        'Fecha', 'Codigo', 'Descripcion', 'Clasificacion', 'Tipo', 'Direccion',
        'Cantidad', 'Stock anterior', 'Stock posterior', 'Costo unitario',
        'Valor movimiento', 'Referencia', 'Origen', 'Entrega', 'Recibe', 'Detalle'
    ];
    const rows = movements.map(m => [
        m.date || '',
        m.partId || '',
        m.partDescription || '',
        m.classification || '',
        m.type || '',
        m.movementDirection || '',
        m.quantity ?? '',
        m.stockBefore ?? '',
        m.stockAfter ?? '',
        m.unitCost ?? '',
        m.movementValue ?? '',
        m.orderHumanId || m.reference || '',
        m.referenceType || m.movementSource || '',
        m.user || '',
        m.recipientUser || '',
        m.details || m.reason || ''
    ]);

    const csv = [headers, ...rows]
        .map(row => row.map(value => `"${String(value).replace(/"/g, '""')}"`).join(','))
        .join('\n');
    const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
    const url = URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `kardex-inventario-${new Date().toISOString().slice(0, 10)}.csv`;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    URL.revokeObjectURL(url);
}

function renderInvChart(canvasId, data, type) {
    const ctx = document.getElementById(canvasId).getContext('2d');
    if (state.charts[canvasId]) state.charts[canvasId].destroy();

    state.charts[canvasId] = new Chart(ctx, {
        type: type,
        data: data,
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: { display: type === 'doughnut', position: 'bottom' }
            }
        }
    });
}

function renderDecommissionedMachines() {
    if (state.currentTab !== 'equipos-baja' && initialLoadComplete) return;

    const container = document.getElementById('decommissioned-machines-list');
    if (!container) return;

    const searchTerm = document.getElementById('search-decommissioned-input').value.toLowerCase();

    const decommissioned = state.machines.filter(m =>
        m.status === 'DADO_DE_BAJA' && (
            m.id.toLowerCase().includes(searchTerm) ||
            m.name.toLowerCase().includes(searchTerm)
        )
    );

    if (decommissioned.length === 0) {
        container.innerHTML = '<tr><td colspan="7" class="text-center py-4 text-muted">No se encontraron equipos dados de baja.</td></tr>';
        return;
    }

    container.innerHTML = decommissioned.map(m => `
        <tr>
            <td><span class="badge bg-light text-dark border font-mono">${m.id}</span></td>
            <td class="fw-bold">${m.name}</td>
            <td>${m.decommissionDate || 'N/A'}</td>
            <td><span class="badge bg-danger bg-opacity-10 text-danger">${m.decommissionReason || 'N/A'}</span></td>
            <td><span class="badge bg-secondary">${m.decommissionType || 'N/A'}</span></td>
            <td><small class="text-muted">${m.decommissionNotes || '-'}</small></td>
            <td class="text-center">
                <button class="btn btn-sm btn-outline-primary" onclick="showMachineDetail('${m.fb_id}')" title="Ver Detalle">
                    <i class="fas fa-eye"></i>
                </button>
            </td>
        </tr>
    `).join('');
}

function showDecommissionModal(machineFbId) {
    const machine = state.machines.find(m => m.fb_id === machineFbId);
    if (!machine) return;

    const form = document.getElementById('decommission-form');
    form.reset();

    document.getElementById('decommission-machine-fb-id').value = machine.fb_id;
    document.getElementById('decommission-machine-name-display').value = `${machine.id} - ${machine.name}`;
    document.getElementById('decommission-date').value = new Date().toISOString().split('T')[0];

    // Populate replacement dropdown
    const replacementSelect = document.getElementById('decommission-replacement');
    let html = '<option value="">Ninguno / Por adquirir</option>';
    state.machines.filter(m => m.status !== 'DADO_DE_BAJA' && m.id !== machine.id).forEach(m => {
        html += `<option value="${m.id}">${m.id} - ${m.name}</option>`;
    });
    replacementSelect.innerHTML = html;

    // Logic to close machine detail if open
    const machineDetailEl = document.getElementById('machine-detail-modal');
    if (machineDetailEl && machineDetailEl.classList.contains('show')) {
        state.reopenMachineDetail = machine.id;
        state.modals.machineDetail.hide();
    } else {
        state.reopenMachineDetail = null;
    }

    state.modals.decommission.show();
}

async function handleDecommissionSubmit(e) {
    e.preventDefault();
    const fbId = document.getElementById('decommission-machine-fb-id').value;
    const machine = state.machines.find(m => m.fb_id === fbId);
    if (!machine) return;

    if (!['Admin', 'Planificador'].includes(getCurrentUser().role)) {
        showToast('No tiene permisos para realizar esta acción.', 'error');
        return;
    }

    showLoading(true);

    const decommissionData = {
        status: 'DADO_DE_BAJA',
        decommissionDate: document.getElementById('decommission-date').value,
        decommissionType: document.getElementById('decommission-type').value,
        decommissionReason: document.getElementById('decommission-reason').value,
        decommissionReplacementId: document.getElementById('decommission-replacement').value,
        decommissionNotes: document.getElementById('decommission-notes').value,
        lastModified: new Date().toISOString()
    };

    try {
        const oldData = JSON.parse(JSON.stringify(machine));
        const machineRef = doc(state.collections.machines, machine.fb_id);

        // 1. Update Machine Document
        await updateDoc(machineRef, decommissionData);
        await updateRTDBMirror('machines', decommissionData, machine.fb_id);

        // 2. Deactivate Maintenance Plans
        const batch = writeBatch(db);
        const machinePlans = state.workPlans.filter(p => p.machineId === machine.id && p.isActive);
        machinePlans.forEach(plan => {
            const planRef = doc(state.collections.workPlans, plan.fb_id);
            batch.update(planRef, { isActive: false, lastModified: new Date().toISOString() });
        });
        await batch.commit();

        // 3. Register Status History
        const historyData = {
            equipment_id: machine.id,
            equipment_fb_id: machine.fb_id,
            old_status: machine.status || 'ACTIVO',
            new_status: 'DADO_DE_BAJA',
            changed_by: getCurrentUser().username,
            change_date: new Date().toISOString(),
            reason: decommissionData.decommissionReason,
            notes: decommissionData.decommissionNotes
        };
        await addDoc(state.collections.equipmentStatusHistory, historyData);

        // 4. Audit Log
        await recordAuditLog('machines', machine.id, 'UPDATE', oldData, { ...machine, ...decommissionData }, 'BAJA DE EQUIPO');

        // Notificar a administradores, planificadores y grupo global
        const tgMsg = `⚠️ <b>EQUIPO DADO DE BAJA</b>\n\n` +
                      `<b>Equipo:</b> ${escapeHTML(machine.name)} (${escapeHTML(machine.id)})\n` +
                      `<b>Motivo:</b> ${escapeHTML(decommissionData.decommissionReason)}\n` +
                      `<b>Por:</b> ${escapeHTML(getCurrentUser().username)}`;
        notifyAdminsAndPlanners(tgMsg).catch(err => console.warn("[Telegram Decommission Error]", err));

        showToast('Equipo dado de baja correctamente.', 'success');
        state.reopenMachineDetail = null;
        state.modals.decommission.hide();
        state.modals.machineDetail.hide();

        if (state.currentTab === 'maquinaria') renderMachines();
        if (state.currentTab === 'equipos-baja') renderDecommissionedMachines();

    } catch (error) {
        console.error("Error decommissioning machine:", error);
        showToast('Error al dar de baja el equipo.', 'error');
    } finally {
        showLoading(false);
    }
}
window.renderPartRequests = renderPartRequests;
window.generatePartRequestGroupPDF = generatePartRequestGroupPDF;
window.showConfirmReceiptModal = showConfirmReceiptModal;

function populateSolicitudItemSelector() {
    const select = document.getElementById('solicitud-item-part-select');
    const searchInput = document.getElementById('solicitud-item-search');
    if (!select) return;

    const filter = (searchInput ? searchInput.value : '').toLowerCase();

    // Filtrar por clasificación (insumo/repuesto), stock > 0 y por el filtro de búsqueda
    const availableItems = state.parts.filter(p => {
        const isAllowedType = p.classification === 'insumo' || p.classification === 'repuesto';
        const stock = p.stockActual || p.currentStock || 0;
        const hasStock = stock > 0;

        const name = (p.description || p.name || '').toLowerCase();
        const id = (p.id || p.fb_id || '').toLowerCase();

        const matchesSearch = name.includes(filter) || id.includes(filter);
        return isAllowedType && hasStock && matchesSearch;
    });

    // Ordenar alfabéticamente por nombre/descripción
    availableItems.sort((a, b) => {
        const nameA = (a.description || a.name || '').toLowerCase();
        const nameB = (b.description || b.name || '').toLowerCase();
        return nameA.localeCompare(nameB);
    });

    let html = '<option value="">-- Seleccionar Ítem del Inventario --</option>';
    availableItems.forEach(p => {
        const stock = p.stockActual || p.currentStock || 0;
        const name = p.description || p.name || 'Sin descripción';
        const id = p.id || p.fb_id || 'S/ID';
        html += `<option value="${p.id || p.fb_id}">${name} [${id}] (Stock: ${stock})</option>`;
    });
    select.innerHTML = html;
}

function renderSolicitudItems() {
    const tbody = document.getElementById('solicitud-items-table-body');
    if (!tbody) return;

    tbody.innerHTML = '';
    state.tempSolicitudItems.forEach((item, index) => {
        const tr = document.createElement('tr');
        tr.innerHTML = `
            <td class="small">${item.description}</td>
            <td class="small text-center">${item.quantity}</td>
            <td class="text-center">
                <button type="button" class="btn btn-xs btn-outline-danger" onclick="removeTempSolicitudItem(${index})">
                    <i class="fas fa-times"></i>
                </button>
            </td>
        `;
        tbody.appendChild(tr);
    });
}

function removeTempSolicitudItem(index) {
    state.tempSolicitudItems.splice(index, 1);
    renderSolicitudItems();
}

window.removeTempSolicitudItem = removeTempSolicitudItem;

async function analyzeAssetWithAI(asset) {
    console.log("Enviando asset:", asset);

    if (!asset) {
        showToast("Error: No se proporcionó el activo para el análisis.", "error");
        throw new Error("No asset provided from frontend");
    }

    const aiModal = state.modals.aiResults;
    const loading = document.getElementById('ai-loading');
    const content = document.getElementById('ai-content');

    aiModal.show();
    loading.classList.remove('d-none');
    content.classList.add('d-none');

    try {
        const ordenes = state.workOrders.filter(wo => wo.machineId === asset.id);

        const result = await apiRequest('/api/ai/analizar-activo', {
            method: 'POST',
            body: JSON.stringify({
                asset: asset,
                historial: ordenes || []
            })
        });

        renderAIResults(result, asset.id);

    } catch (error) {
        console.error("AI Analysis Error:", error);
        showToast(`Error de IA: ${error.message}`, 'error');
        aiModal.hide();
    } finally {
        loading.classList.add('d-none');
    }
}

function renderAIResults(data, assetId) {
    const content = document.getElementById('ai-content');
    content.classList.remove('d-none');

    document.getElementById('ai-analisis-detallado').textContent = data.analisis || data.analisis_detallado || 'Análisis técnico no disponible.';

    const problemasList = document.getElementById('ai-problemas-list');
    problemasList.innerHTML = (data.problemas || []).map(p => `<li>${p}</li>`).join('') || '<li>No se detectaron problemas críticos inmediatos.</li>';

    const accionesList = document.getElementById('ai-acciones-list');
    accionesList.innerHTML = (data.acciones || []).map(a => `<li>${a}</li>`).join('') || '<li>Siga el plan de mantenimiento preventivo estándar.</li>';

    const priBadge = document.getElementById('ai-prioridad-badge');
    const priMap = {
        'alta': 'bg-danger',
        'media': 'bg-warning text-dark',
        'baja': 'bg-success'
    };
    const priClass = priMap[data.prioridad.toLowerCase()] || 'bg-secondary';
    priBadge.innerHTML = `<span class="badge ${priClass} w-100 py-2 fs-6 fw-bold text-uppercase">${data.prioridad || 'Desconocida'}</span>`;

    const suggestion = data.sugerencia_ot || {};
    document.getElementById('ai-ot-tipo').textContent = suggestion.tipo || 'Preventivo';
    document.getElementById('ai-ot-descripcion').textContent = suggestion.descripcion || 'Sin descripción sugerida.';
    document.getElementById('ai-ot-tiempo').textContent = suggestion.tiempo_estimado || 'Por definir';

    const pasosList = document.getElementById('ai-ot-pasos');
    pasosList.innerHTML = (suggestion.pasos || []).map(p => `<li>${p}</li>`).join('') || '<li>No se especificaron pasos técnicos.</li>';

    // Evento para el botón de usar sugerencia
    const btnUseSuggestion = document.getElementById('btn-create-wo-from-ai');
    btnUseSuggestion.onclick = async () => {
        state.modals.aiResults.hide();
        state.modals.machineDetail.hide(); // Opcional, cerrar detalle para ver la OT

        await showWorkOrderModal(null, suggestion.tipo || 'Preventivo', assetId);

        // Formatear descripción incluyendo pasos si existen
        let fullDescription = suggestion.descripcion || '';
        if (suggestion.pasos && suggestion.pasos.length > 0) {
            fullDescription += "\n\nPasos técnicos sugeridos:\n" + suggestion.pasos.map((p, index) => `${index + 1}. ${p}`).join('\n');
        }

        document.getElementById('wo-description').value = fullDescription;
        showToast('Sugerencia de IA aplicada a la nueva Orden de Trabajo.', 'success');
    };
}

window.analyzeAssetWithAI = analyzeAssetWithAI;
window.renderAIResults = renderAIResults;
window.getOptimizedStorageUrl = getOptimizedStorageUrl;
window.getOptimizedImageUrlWithRetry = getOptimizedImageUrlWithRetry;

// --- AI Module Logic ---

async function loadAIConfig() {
    try {
        const config = await apiRequest('/api/settings/ai-config');
        document.getElementById('ai-apikey').value = config.apiKey || '';
        document.getElementById('ai-budget-limit').value = config.monthlyBudget || 30;
    } catch (error) {
        console.error("Load AI Config Error:", error);
    }
}

async function handleAIConfigSubmit(e) {
    e.preventDefault();
    const apiKey = document.getElementById('ai-apikey').value.trim();
    const budget = document.getElementById('ai-budget-limit').value;

    if (!apiKey) {
        showToast('La API Key es obligatoria', 'warning');
        return;
    }

    saveAIConfig(apiKey, budget);
}

async function saveAIConfig(apiKey, monthlyBudget) {
    showLoading(true);
    try {
        await apiRequest('/api/settings/ai-config', {
            method: 'POST',
            body: JSON.stringify({ apiKey, monthlyBudget })
        });

        showToast('Configuración de IA guardada correctamente', 'success');
        loadAIConfig(); // Reload to get masked key
    } catch (error) {
        console.error("Save AI Config Error:", error);
        showToast(`Error al guardar configuración: ${error.message}`, 'error');
    } finally {
        showLoading(false);
    }
}

async function loadAIUsage() {
    const listBody = document.getElementById('ai-usage-daily-list');
    try {
        const usage = await apiRequest('/api/settings/ai-usage');

        // Update stats
        document.getElementById('ai-stat-tokens').textContent = usage.tokens.toLocaleString();
        document.getElementById('ai-stat-requests').textContent = usage.requests.toLocaleString();
        document.getElementById('ai-stat-cost').textContent = `$${usage.estimated_cost.toFixed(2)}`;

        // Update Budget UI
        const budgetLabel = document.getElementById('ai-budget-label');
        const progressBar = document.getElementById('ai-budget-progress');
        const badge = document.getElementById('ai-budget-status-badge');
        const alert = document.getElementById('ai-budget-alert');

        const percentage = Math.min(100, usage.usage_percentage || 0);
        budgetLabel.textContent = `Presupuesto: $${usage.estimated_cost.toFixed(2)} / $${usage.limit.toFixed(2)}`;
        progressBar.style.width = `${percentage}%`;

        if (percentage > 90) {
            progressBar.className = 'progress-bar bg-danger';
            badge.className = 'badge bg-danger';
            badge.textContent = 'Crítico';
            alert.classList.remove('d-none');
            alert.className = 'alert alert-danger py-2 px-3 small';
            alert.innerHTML = '<i class="fas fa-exclamation-circle me-2"></i> Has superado el 90% de tu presupuesto. El servicio podría interrumpirse.';
        } else if (percentage > 70) {
            progressBar.className = 'progress-bar bg-warning';
            badge.className = 'badge bg-warning text-dark';
            badge.textContent = 'Atención';
            alert.classList.remove('d-none');
            alert.className = 'alert alert-warning py-2 px-3 small';
            alert.innerHTML = '<i class="fas fa-exclamation-triangle me-2"></i> Has superado el 70% de tu presupuesto mensual.';
        } else {
            progressBar.className = 'progress-bar bg-success';
            badge.className = 'badge bg-success';
            badge.textContent = 'Excelente';
            alert.classList.add('d-none');
        }

        // Render daily list
        if (usage.daily && Object.keys(usage.daily).length > 0) {
            const sortedDays = Object.keys(usage.daily).sort().reverse();
            listBody.innerHTML = sortedDays.map(day => `
                <tr>
                    <td class="px-4 py-3 small fw-bold">${day}</td>
                    <td class="py-3 small">${usage.daily[day].tokens.toLocaleString()}</td>
                    <td class="py-3 small">${usage.daily[day].requests}</td>
                </tr>
            `).join('');
        } else {
            listBody.innerHTML = '<tr><td colspan="3" class="text-center py-4 text-muted small">No hay datos de uso para este mes.</td></tr>';
        }

    } catch (error) {
        console.error("Load AI Usage Error:", error);
        listBody.innerHTML = '<tr><td colspan="3" class="text-center py-4 text-danger small">Error al cargar datos de uso.</td></tr>';
    }
}

async function testAIConnection() {
    const resultDiv = document.getElementById('ai-test-result');
    const messageSpan = document.getElementById('ai-test-message');
    const icon = document.getElementById('ai-test-icon');
    const durationSpan = document.getElementById('ai-test-duration');

    resultDiv.classList.remove('d-none');
    messageSpan.textContent = 'Probando conexión...';
    icon.className = 'fas fa-circle-notch fa-spin me-2 text-primary';
    durationSpan.textContent = '';

    try {
        const data = await apiRequest('/api/settings/ai-test-connection', {
            method: 'POST'
        });

        if (data.status === 'connected') {
            icon.className = 'fas fa-check-circle me-2 text-success';
            messageSpan.textContent = 'Conexión exitosa. El asistente de IA está listo.';
            durationSpan.textContent = data.duration;
            showToast('Conexión con IA establecida correctamente', 'success');
        } else {
            throw new Error(data.error || 'Error desconocido');
        }
    } catch (error) {
        console.error("Test AI Error:", error);
        icon.className = 'fas fa-times-circle me-2 text-danger';
        messageSpan.textContent = 'Error de conexión: ' + error.message;
        showToast('Error al probar conexión con IA', 'error');
    }
}

window.showPrettyExecutionReport = showPrettyExecutionReport;
window.handleClosePlanExecution = handleClosePlanExecution;
window.renderPurchaseCart = renderPurchaseCart;
window.addToPurchaseCart = addToPurchaseCart;
window.updatePurchaseCartQty = updatePurchaseCartQty;
window.setPurchaseCartQty = setPurchaseCartQty;
window.removeFromPurchaseCart = removeFromPurchaseCart;
window.clearPurchaseCart = clearPurchaseCart;
window.submitPurchaseRequest = submitPurchaseRequest;

window.generatePurchaseRequestPDF = generatePurchaseRequestPDF;

function renderPurchaseRequestsTable() {
    const list = document.getElementById('inv-purchase-requests-list');
    if (!list) return;

    console.log("Rendering Purchase Requests Table. Total count:", state.purchaseRequests.length);

    const filtered = [...state.purchaseRequests]
        .sort((a, b) => new Date(b.date) - new Date(a.date))
        .slice(0, 30);

    list.innerHTML = filtered.map(r => {
        const date = new Date(r.date);
        let statusClass = 'bg-secondary';
        if (r.status === 'Pendiente') statusClass = 'bg-warning text-dark';
        if (r.status === 'Pedido') statusClass = 'bg-info text-white';
        if (r.status === 'Recibido' || r.status === 'Completado') statusClass = 'bg-success';
        if (r.status === 'Cancelado') statusClass = 'bg-danger';

        const itemCount = r.items ? r.items.length : 0;
        const totalReceived = r.items ? r.items.reduce((sum, i) => sum + (i.receivedQuantity || 0), 0) : 0;
        const totalRequested = r.items ? r.items.reduce((sum, i) => sum + i.quantity, 0) : 0;

        let actions = `
            <button class="btn btn-xs btn-outline-danger me-1" onclick="generatePurchaseRequestPDF(${JSON.stringify(r).replace(/"/g, '&quot;')})" title="Ver PDF">
                <i class="fas fa-file-pdf"></i>
            </button>
        `;

        if (r.status !== 'Recibido' && r.status !== 'Completado' && r.status !== 'Cancelado') {
            actions += `
                <button class="btn btn-xs btn-success" onclick="showPurchaseReceiveModal('${r.fb_id}')">
                    <i class="fas fa-box-open me-1"></i>Recibir
                </button>
            `;
        }

        return `
            <tr>
                <td>${date.toLocaleDateString()}</td>
                <td><span class="fw-bold">${r.folioStr || 'N/A'}</span></td>
                <td>${r.createdBy}</td>
                <td>${totalReceived} / ${totalRequested} units (${itemCount} items)</td>
                <td>${formatCurrency(r.totalCost || 0)}</td>
                <td><span class="badge ${statusClass}">${r.status}</span></td>
                <td class="text-center">${actions}</td>
            </tr>
        `;
    }).join('') || '<tr><td colspan="7" class="text-center text-muted py-4">No hay solicitudes de compra recientes</td></tr>';
}

function showPurchaseReceiveModal(fbId) {
    const request = state.purchaseRequests.find(r => r.fb_id === fbId);
    if (!request) return;

    document.getElementById('receive-request-fb-id').value = fbId;
    document.getElementById('receive-folio-display').textContent = request.folioStr;
    document.getElementById('receive-requester-display').textContent = request.createdBy;
    document.getElementById('receive-date-display').textContent = new Date(request.date).toLocaleString();
    document.getElementById('receive-notes').value = '';
    document.getElementById('receive-close-request').checked = false;

    const list = document.getElementById('receive-items-list');
    list.innerHTML = '';

    request.items.forEach((item, index) => {
        const received = item.receivedQuantity || 0;
        const pending = item.quantity - received;
        const part = state.parts.find(p => p.id === item.partId);

        const tr = document.createElement('tr');
        tr.innerHTML = `
            <td>
                <div class="fw-bold">${item.description}</div>
                <div class="small text-muted">${item.partId}</div>
            </td>
            <td class="text-center fw-bold">${pending}</td>
            <td>
                <input type="number" class="form-control form-control-sm receive-qty-input"
                    data-index="${index}" value="${pending}" min="0" max="${pending}">
            </td>
            <td>
                <div class="input-group input-group-sm">
                    <span class="input-group-text">L</span>
                    <input type="number" class="form-control receive-cost-input"
                        data-index="${index}" value="${item.cost || 0}" step="0.01">
                </div>
            </td>
            <td>
                <select class="form-select form-select-sm receive-supplier-select" data-index="${index}">
                    <option value="">Seleccione...</option>
                    ${state.proveedores.map(p => `<option value="${p.id}" ${(part.supplierId === p.id) ? 'selected' : ''}>${p.nombre}</option>`).join('')}
                </select>
            </td>
            <td class="text-end fw-bold receive-item-subtotal">L 0.00</td>
        `;
        list.appendChild(tr);
    });

    const updateTotals = () => {
        let grandTotal = 0;
        document.querySelectorAll('.receive-qty-input').forEach(input => {
            const index = input.dataset.index;
            const qty = parseFloat(input.value) || 0;
            const cost = parseFloat(document.querySelector(`.receive-cost-input[data-index="${index}"]`).value) || 0;
            const subtotal = qty * cost;
            grandTotal += subtotal;
            input.closest('tr').querySelector('.receive-item-subtotal').textContent = formatCurrency(subtotal);
        });
        document.getElementById('receive-total-display').textContent = formatCurrency(grandTotal);
    };

    list.querySelectorAll('input').forEach(el => el.addEventListener('input', updateTotals));
    updateTotals();

    state.modals.purchaseReceive.show();
}

async function handlePurchaseReceiveSubmit(e) {
    e.preventDefault();
    const fbId = document.getElementById('receive-request-fb-id').value;
    const request = state.purchaseRequests.find(r => r.fb_id === fbId);
    if (!request) return;

    const notes = document.getElementById('receive-notes').value;
    const shouldClose = document.getElementById('receive-close-request').checked;

    const itemsToReceive = [];
    let hasQuantity = false;

    document.querySelectorAll('.receive-qty-input').forEach(input => {
        const index = parseInt(input.dataset.index);
        const qty = parseFloat(input.value) || 0;
        const cost = parseFloat(document.querySelector(`.receive-cost-input[data-index="${index}"]`).value) || 0;
        const supplierId = document.querySelector(`.receive-supplier-select[data-index="${index}"]`).value;

        if (qty > 0) {
            itemsToReceive.push({
                index,
                partId: request.items[index].partId,
                description: request.items[index].description,
                quantity: qty,
                cost: cost,
                supplierId: supplierId
            });
            hasQuantity = true;
        }
    });

    if (!hasQuantity && !shouldClose) {
        showToast('Debe ingresar al menos una cantidad a recibir o cerrar la solicitud.', 'warning');
        return;
    }

    showLoading(true, 'Procesando ingreso a inventario...');
    try {
        const batch = writeBatch(db);
        const now = new Date().toISOString();

        for (const item of itemsToReceive) {
            const part = state.parts.find(p => p.id === item.partId);
            if (!part) continue;

            const oldStock = part.stockActual || 0;
            const newStock = oldStock + item.quantity;

            const partRef = doc(state.collections.parts, part.fb_id);
            batch.update(partRef, {
                stockActual: newStock,
                cost: item.cost,
                supplierId: item.supplierId,
                lastModified: now
            });

            const movementData = {
                date: now,
                partId: item.partId,
                partDescription: item.description,
                quantity: item.quantity,
                type: 'Entrada Compra',
                details: `Recepción de Compra ${request.folioStr}. ${notes}`,
                orderHumanId: request.folioStr,
                user: getCurrentUser().username,
                cost: item.cost,
                supplierId: item.supplierId
            };
            const movRef = doc(state.collections.partMovements);
            batch.set(movRef, buildPartMovementData(item.partId, item.quantity, 'Entrada Compra', {
                ...movementData,
                referenceType: 'COMPRA',
                movementSource: 'COMPRAS',
                stockBefore: oldStock,
                stockAfter: newStock,
                unitCost: item.cost
            }));

            await recordAuditLog('parts', part.fb_id, 'UPDATE', { stockActual: oldStock }, { stockActual: newStock }, `ENTRADA POR COMPRA: ${request.folioStr}`);
        }

        const updatedItems = [...request.items];
        itemsToReceive.forEach(item => {
            updatedItems[item.index].receivedQuantity = (updatedItems[item.index].receivedQuantity || 0) + item.quantity;
            updatedItems[item.index].cost = item.cost;
        });

        const allReceived = updatedItems.every(i => (i.receivedQuantity || 0) >= i.quantity);
        let newStatus = request.status;
        if (shouldClose || allReceived) {
            newStatus = 'Recibido';
        } else if (itemsToReceive.length > 0) {
            newStatus = 'Pedido';
        }

        const requestRef = doc(state.collections.purchaseRequests, fbId);
        batch.update(requestRef, {
            items: updatedItems,
            status: newStatus,
            lastModified: now,
            receptionNotes: notes
        });

        await batch.commit();

        showToast('Ingreso a inventario completado correctamente.', 'success');
        state.modals.purchaseReceive.hide();

        // Notificación Telegram para recepción de compra
        if (itemsToReceive.length > 0) {
            const receiveMsg = `📥 <b>Recepción de Compra Realizada</b>\n\n` +
                              `<b>Folio:</b> ${request.folioStr}\n` +
                              `<b>Items:</b> ${itemsToReceive.length}\n` +
                              `<b>Estado:</b> ${newStatus}\n` +
                              `<b>Usuario:</b> ${escapeHTML(getCurrentUser().username)}\n\n` +
                              itemsToReceive.map(i => `• ${escapeHTML(i.description)} (${i.quantity})`).join('\n');

            notifyAdminsAndPlanners(receiveMsg).catch(err => console.warn("[Telegram Purchase Receive Notification Error]", err));
        }

        updateInventoryDashboard();

    } catch (error) {
        console.error("Error processing purchase receipt:", error);
        showToast('Error al procesar la recepción.', 'error');
    } finally {
        showLoading(false);
    }
}

window.showPurchaseReceiveModal = showPurchaseReceiveModal;
window.handlePurchaseReceiveSubmit = handlePurchaseReceiveSubmit;
window.formatCurrency = formatCurrency;
window.renderPurchaseRequestsTable = renderPurchaseRequestsTable;

window.showWorkPlanPreview = showWorkPlanPreview;
window.showMachinePlanHistory = showMachinePlanHistory;
window.renderWorkPlanDetails = renderWorkPlanDetails;
window.deleteWorkPlan = deleteWorkPlan;
window.handleWorkPlanSubmit = handleWorkPlanSubmit;
window.startWorkPlanExecution = startWorkPlanExecution;

function showWorkPlansGrid() {
    const gridView = document.getElementById('work-plans-grid-view');
    const detailView = document.getElementById('work-plan-detail-view');
    if (gridView) gridView.classList.remove('d-none');
    if (detailView) detailView.classList.add('d-none');
    document.getElementById('planes-trabajo').classList.remove('wp-detail-active');
    renderWorkPlans();
}

function startWorkPlanExecutionWrapper(planId) {
    if (!isWorkPlanExecutorRole(getCurrentUser().role)) {
        showToast('Solo los roles Técnico o Planificador pueden ejecutar planes de mantenimiento.', 'error');
        return;
    }
    const plan = state.workPlans.find(p => p.fb_id === planId);
    const active = state.workPlanExecutions.find(ex => ex.planId === planId && (ex.status === 'En Proceso' || ex.status === 'Pausado'));
    startWorkPlanExecution(active || null, plan);
}

function showWorkPlanPreview(planId) {
    if (!isWorkPlanExecutorRole(getCurrentUser().role) && !isWorkPlanCreatorRole(getCurrentUser().role)) {
        showToast('No tiene permisos para previsualizar este plan.', 'error');
        return;
    }
    const plan = state.workPlans.find(p => p.fb_id === planId);
    if (!plan) return;
    showToast('Generando vista previa...', 'info');
    const mock = {
        fb_id: 'preview',
        planId: plan.fb_id,
        status: 'Vista Previa',
        isMock: true,
        isReadOnly: true,
        taskGroups: (plan.taskGroups || []).map(g => ({
            ...g,
            tasks: g.tasks.map(t => ({ ...t, status: 'Pendiente' }))
        }))
    };
    startWorkPlanExecution(mock, plan);
}

function showMachinePlanHistory(machineId) {
    const machine = state.machines.find(m => m.id === machineId);
    if (!machine) return;
    const plans = state.workPlans.filter(p => p.machineId === machineId);
    const planIds = plans.map(p => p.fb_id);
    const executions = state.workPlanExecutions.filter(e => planIds.includes(e.planId)).sort((a,b) => new Date(b.executedAt) - new Date(a.executedAt));
    const historyModalHTML = `
        <div class="modal fade" id="machinePlanHistoryModal" tabindex="-1">
            <div class="modal-dialog modal-xl modal-dialog-scrollable">
                <div class="modal-content border-0 shadow-lg" style="border-radius: 1.5rem;">
                    <div class="modal-header bg-primary text-white p-4">
                        <h5 class="modal-title fw-bold">Historial: ${machine.name}</h5>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                    </div>
                    <div class="modal-body p-4 bg-light">
                        <div class="table-responsive bg-white rounded-3 shadow-sm">
                            <table class="table table-hover align-middle mb-0">
                                <thead class="bg-light"><tr><th class="ps-4">Plan</th><th>Fecha</th><th>Responsable</th><th>Estado</th><th class="text-center">Reporte</th></tr></thead>
                                <tbody>
                                    ${executions.length > 0 ? executions.map(ex => {
                                        const p = plans.find(pl => pl.fb_id === ex.planId);
                                        return `<tr><td class="ps-4"><b>${p.name || 'N/A'}</b></td><td>${new Date(ex.executedAt).toLocaleString()}</td><td>${ex.executedBy}</td>
                                        <td><span class="badge ${ex.status === 'Completado' ? 'bg-success' : 'bg-warning'} rounded-pill">${ex.status}</span></td>
                                        <td class="text-center"><button class="btn btn-sm btn-outline-primary rounded-pill px-3" onclick="showPrettyExecutionReport('${ex.id || ex.fb_id}')">Ver Reporte</button></td></tr>`;
                                    }).join('') : '<tr><td colspan="5" class="text-center py-4">No hay registros.</td></tr>'}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>`;
    const existing = document.getElementById('machinePlanHistoryModal');
    if (existing) existing.remove();
    document.body.insertAdjacentHTML('beforeend', historyModalHTML);
    new bootstrap.Modal(document.getElementById('machinePlanHistoryModal')).show();
}

window.showWorkPlansGrid = showWorkPlansGrid;
window.startWorkPlanExecutionWrapper = startWorkPlanExecutionWrapper;
window.triggerMachinePhotoUpload = triggerMachinePhotoUpload;

let currentMachineFbIdForPhoto = null;
function triggerMachinePhotoUpload(machineFbId) {
    currentMachineFbIdForPhoto = machineFbId;
    document.getElementById('global-machine-photo-input').click();
}

async function handleGlobalMachinePhotoChange(event) {
    const file = event.target.files[0];
    if (!file || !currentMachineFbIdForPhoto) return;

    const machine = state.machines.find(m => m.fb_id === currentMachineFbIdForPhoto);
    if (!machine) {
        showToast("No se encontró el equipo seleccionado", "danger");
        return;
    }

    showLoading(true, "Subiendo y procesando imagen...");
    try {
        const imageUrl = await uploadMachineFile(machine.id, file, 'img');
        if (imageUrl) {
            const docRef = doc(state.collections.machines, machine.id);
            const oldData = JSON.parse(JSON.stringify(machine));
            const newData = { ...machine, fotoUrl: imageUrl };

            await updateDoc(docRef, { fotoUrl: imageUrl });
            await updateRTDBMirror('machines', newData, machine.id);
            await recordAuditLog('machines', machine.id, 'UPDATE', oldData, newData, 'Actualización de foto desde pestaña Planes');

            // Actualizar estado local
            machine.fotoUrl = imageUrl;

            showToast("Imagen del equipo actualizada correctamente", "success");
            renderWorkPlans();
        }
    } catch (error) {
        console.error("Error updating machine photo:", error);
        showToast("Error al subir la imagen: " + error.message, "danger");
    } finally {
        showLoading(false);
        event.target.value = '';
        currentMachineFbIdForPhoto = null;
    }
}
window.handleVerifyTelegramConnection = handleVerifyTelegramConnection;
window.populateSolicitudItemSelector = populateSolicitudItemSelector;

